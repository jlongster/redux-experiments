/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};

/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {

/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;

/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};

/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);

/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;

/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}


/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;

/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;

/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";

/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__(221);


/***/ },
/* 1 */,
/* 2 */
/***/ function(module, exports, __webpack_require__) {

	module.exports = __webpack_require__(3);


/***/ },
/* 3 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule React
	 */

	/* globals __REACT_DEVTOOLS_GLOBAL_HOOK__*/

	'use strict';

	var EventPluginUtils = __webpack_require__(5);
	var ReactChildren = __webpack_require__(9);
	var ReactComponent = __webpack_require__(23);
	var ReactClass = __webpack_require__(38);
	var ReactContext = __webpack_require__(13);
	var ReactCurrentOwner = __webpack_require__(18);
	var ReactElement = __webpack_require__(12);
	var ReactElementValidator = __webpack_require__(31);
	var ReactDOM = __webpack_require__(41);
	var ReactDOMTextComponent = __webpack_require__(43);
	var ReactDefaultInjection = __webpack_require__(92);
	var ReactInstanceHandles = __webpack_require__(21);
	var ReactMount = __webpack_require__(68);
	var ReactPerf = __webpack_require__(27);
	var ReactPropTypes = __webpack_require__(123);
	var ReactReconciler = __webpack_require__(28);
	var ReactServerRendering = __webpack_require__(155);

	var assign = __webpack_require__(14);
	var findDOMNode = __webpack_require__(112);
	var onlyChild = __webpack_require__(157);

	ReactDefaultInjection.inject();

	var createElement = ReactElement.createElement;
	var createFactory = ReactElement.createFactory;
	var cloneElement = ReactElement.cloneElement;

	if ("production" !== process.env.NODE_ENV) {
	  createElement = ReactElementValidator.createElement;
	  createFactory = ReactElementValidator.createFactory;
	  cloneElement = ReactElementValidator.cloneElement;
	}

	var render = ReactPerf.measure('React', 'render', ReactMount.render);

	var React = {
	  Children: {
	    map: ReactChildren.map,
	    forEach: ReactChildren.forEach,
	    count: ReactChildren.count,
	    only: onlyChild
	  },
	  Component: ReactComponent,
	  DOM: ReactDOM,
	  PropTypes: ReactPropTypes,
	  initializeTouchEvents: function(shouldUseTouch) {
	    EventPluginUtils.useTouchEvents = shouldUseTouch;
	  },
	  createClass: ReactClass.createClass,
	  createElement: createElement,
	  cloneElement: cloneElement,
	  createFactory: createFactory,
	  createMixin: function(mixin) {
	    // Currently a noop. Will be used to validate and trace mixins.
	    return mixin;
	  },
	  constructAndRenderComponent: ReactMount.constructAndRenderComponent,
	  constructAndRenderComponentByID: ReactMount.constructAndRenderComponentByID,
	  findDOMNode: findDOMNode,
	  render: render,
	  renderToString: ReactServerRendering.renderToString,
	  renderToStaticMarkup: ReactServerRendering.renderToStaticMarkup,
	  unmountComponentAtNode: ReactMount.unmountComponentAtNode,
	  isValidElement: ReactElement.isValidElement,
	  withContext: ReactContext.withContext,

	  // Hook for JSX spread, don't use this for anything else.
	  __spread: assign
	};

	// Inject the runtime into a devtools global hook regardless of browser.
	// Allows for debugging when the hook is injected on the page.
	if (
	  typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ !== 'undefined' &&
	  typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.inject === 'function') {
	  __REACT_DEVTOOLS_GLOBAL_HOOK__.inject({
	    CurrentOwner: ReactCurrentOwner,
	    InstanceHandles: ReactInstanceHandles,
	    Mount: ReactMount,
	    Reconciler: ReactReconciler,
	    TextComponent: ReactDOMTextComponent
	  });
	}

	if ("production" !== process.env.NODE_ENV) {
	  var ExecutionEnvironment = __webpack_require__(52);
	  if (ExecutionEnvironment.canUseDOM && window.top === window.self) {

	    // If we're in Chrome, look for the devtools marker and provide a download
	    // link if not installed.
	    if (navigator.userAgent.indexOf('Chrome') > -1) {
	      if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === 'undefined') {
	        console.debug(
	          'Download the React DevTools for a better development experience: ' +
	          'https://fb.me/react-devtools'
	        );
	      }
	    }

	    var expectedFeatures = [
	      // shims
	      Array.isArray,
	      Array.prototype.every,
	      Array.prototype.forEach,
	      Array.prototype.indexOf,
	      Array.prototype.map,
	      Date.now,
	      Function.prototype.bind,
	      Object.keys,
	      String.prototype.split,
	      String.prototype.trim,

	      // shams
	      Object.create,
	      Object.freeze
	    ];

	    for (var i = 0; i < expectedFeatures.length; i++) {
	      if (!expectedFeatures[i]) {
	        console.error(
	          'One or more ES5 shim/shams expected by React are not available: ' +
	          'https://fb.me/react-warning-polyfills'
	        );
	        break;
	      }
	    }
	  }
	}

	React.version = '0.13.3';

	module.exports = React;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 4 */
/***/ function(module, exports) {

	// shim for using process in browser

	var process = module.exports = {};
	var queue = [];
	var draining = false;
	var currentQueue;
	var queueIndex = -1;

	function cleanUpNextTick() {
	    draining = false;
	    if (currentQueue.length) {
	        queue = currentQueue.concat(queue);
	    } else {
	        queueIndex = -1;
	    }
	    if (queue.length) {
	        drainQueue();
	    }
	}

	function drainQueue() {
	    if (draining) {
	        return;
	    }
	    var timeout = setTimeout(cleanUpNextTick);
	    draining = true;

	    var len = queue.length;
	    while(len) {
	        currentQueue = queue;
	        queue = [];
	        while (++queueIndex < len) {
	            currentQueue[queueIndex].run();
	        }
	        queueIndex = -1;
	        len = queue.length;
	    }
	    currentQueue = null;
	    draining = false;
	    clearTimeout(timeout);
	}

	process.nextTick = function (fun) {
	    var args = new Array(arguments.length - 1);
	    if (arguments.length > 1) {
	        for (var i = 1; i < arguments.length; i++) {
	            args[i - 1] = arguments[i];
	        }
	    }
	    queue.push(new Item(fun, args));
	    if (queue.length === 1 && !draining) {
	        setTimeout(drainQueue, 0);
	    }
	};

	// v8 likes predictible objects
	function Item(fun, array) {
	    this.fun = fun;
	    this.array = array;
	}
	Item.prototype.run = function () {
	    this.fun.apply(null, this.array);
	};
	process.title = 'browser';
	process.browser = true;
	process.env = {};
	process.argv = [];
	process.version = ''; // empty string to avoid regexp issues
	process.versions = {};

	function noop() {}

	process.on = noop;
	process.addListener = noop;
	process.once = noop;
	process.off = noop;
	process.removeListener = noop;
	process.removeAllListeners = noop;
	process.emit = noop;

	process.binding = function (name) {
	    throw new Error('process.binding is not supported');
	};

	// TODO(shtylman)
	process.cwd = function () { return '/' };
	process.chdir = function (dir) {
	    throw new Error('process.chdir is not supported');
	};
	process.umask = function() { return 0; };


/***/ },
/* 5 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule EventPluginUtils
	 */

	'use strict';

	var EventConstants = __webpack_require__(6);

	var invariant = __webpack_require__(8);

	/**
	 * Injected dependencies:
	 */

	/**
	 * - `Mount`: [required] Module that can convert between React dom IDs and
	 *   actual node references.
	 */
	var injection = {
	  Mount: null,
	  injectMount: function(InjectedMount) {
	    injection.Mount = InjectedMount;
	    if ("production" !== process.env.NODE_ENV) {
	      ("production" !== process.env.NODE_ENV ? invariant(
	        InjectedMount && InjectedMount.getNode,
	        'EventPluginUtils.injection.injectMount(...): Injected Mount module ' +
	        'is missing getNode.'
	      ) : invariant(InjectedMount && InjectedMount.getNode));
	    }
	  }
	};

	var topLevelTypes = EventConstants.topLevelTypes;

	function isEndish(topLevelType) {
	  return topLevelType === topLevelTypes.topMouseUp ||
	         topLevelType === topLevelTypes.topTouchEnd ||
	         topLevelType === topLevelTypes.topTouchCancel;
	}

	function isMoveish(topLevelType) {
	  return topLevelType === topLevelTypes.topMouseMove ||
	         topLevelType === topLevelTypes.topTouchMove;
	}
	function isStartish(topLevelType) {
	  return topLevelType === topLevelTypes.topMouseDown ||
	         topLevelType === topLevelTypes.topTouchStart;
	}


	var validateEventDispatches;
	if ("production" !== process.env.NODE_ENV) {
	  validateEventDispatches = function(event) {
	    var dispatchListeners = event._dispatchListeners;
	    var dispatchIDs = event._dispatchIDs;

	    var listenersIsArr = Array.isArray(dispatchListeners);
	    var idsIsArr = Array.isArray(dispatchIDs);
	    var IDsLen = idsIsArr ? dispatchIDs.length : dispatchIDs ? 1 : 0;
	    var listenersLen = listenersIsArr ?
	      dispatchListeners.length :
	      dispatchListeners ? 1 : 0;

	    ("production" !== process.env.NODE_ENV ? invariant(
	      idsIsArr === listenersIsArr && IDsLen === listenersLen,
	      'EventPluginUtils: Invalid `event`.'
	    ) : invariant(idsIsArr === listenersIsArr && IDsLen === listenersLen));
	  };
	}

	/**
	 * Invokes `cb(event, listener, id)`. Avoids using call if no scope is
	 * provided. The `(listener,id)` pair effectively forms the "dispatch" but are
	 * kept separate to conserve memory.
	 */
	function forEachEventDispatch(event, cb) {
	  var dispatchListeners = event._dispatchListeners;
	  var dispatchIDs = event._dispatchIDs;
	  if ("production" !== process.env.NODE_ENV) {
	    validateEventDispatches(event);
	  }
	  if (Array.isArray(dispatchListeners)) {
	    for (var i = 0; i < dispatchListeners.length; i++) {
	      if (event.isPropagationStopped()) {
	        break;
	      }
	      // Listeners and IDs are two parallel arrays that are always in sync.
	      cb(event, dispatchListeners[i], dispatchIDs[i]);
	    }
	  } else if (dispatchListeners) {
	    cb(event, dispatchListeners, dispatchIDs);
	  }
	}

	/**
	 * Default implementation of PluginModule.executeDispatch().
	 * @param {SyntheticEvent} SyntheticEvent to handle
	 * @param {function} Application-level callback
	 * @param {string} domID DOM id to pass to the callback.
	 */
	function executeDispatch(event, listener, domID) {
	  event.currentTarget = injection.Mount.getNode(domID);
	  var returnValue = listener(event, domID);
	  event.currentTarget = null;
	  return returnValue;
	}

	/**
	 * Standard/simple iteration through an event's collected dispatches.
	 */
	function executeDispatchesInOrder(event, cb) {
	  forEachEventDispatch(event, cb);
	  event._dispatchListeners = null;
	  event._dispatchIDs = null;
	}

	/**
	 * Standard/simple iteration through an event's collected dispatches, but stops
	 * at the first dispatch execution returning true, and returns that id.
	 *
	 * @return id of the first dispatch execution who's listener returns true, or
	 * null if no listener returned true.
	 */
	function executeDispatchesInOrderStopAtTrueImpl(event) {
	  var dispatchListeners = event._dispatchListeners;
	  var dispatchIDs = event._dispatchIDs;
	  if ("production" !== process.env.NODE_ENV) {
	    validateEventDispatches(event);
	  }
	  if (Array.isArray(dispatchListeners)) {
	    for (var i = 0; i < dispatchListeners.length; i++) {
	      if (event.isPropagationStopped()) {
	        break;
	      }
	      // Listeners and IDs are two parallel arrays that are always in sync.
	      if (dispatchListeners[i](event, dispatchIDs[i])) {
	        return dispatchIDs[i];
	      }
	    }
	  } else if (dispatchListeners) {
	    if (dispatchListeners(event, dispatchIDs)) {
	      return dispatchIDs;
	    }
	  }
	  return null;
	}

	/**
	 * @see executeDispatchesInOrderStopAtTrueImpl
	 */
	function executeDispatchesInOrderStopAtTrue(event) {
	  var ret = executeDispatchesInOrderStopAtTrueImpl(event);
	  event._dispatchIDs = null;
	  event._dispatchListeners = null;
	  return ret;
	}

	/**
	 * Execution of a "direct" dispatch - there must be at most one dispatch
	 * accumulated on the event or it is considered an error. It doesn't really make
	 * sense for an event with multiple dispatches (bubbled) to keep track of the
	 * return values at each dispatch execution, but it does tend to make sense when
	 * dealing with "direct" dispatches.
	 *
	 * @return The return value of executing the single dispatch.
	 */
	function executeDirectDispatch(event) {
	  if ("production" !== process.env.NODE_ENV) {
	    validateEventDispatches(event);
	  }
	  var dispatchListener = event._dispatchListeners;
	  var dispatchID = event._dispatchIDs;
	  ("production" !== process.env.NODE_ENV ? invariant(
	    !Array.isArray(dispatchListener),
	    'executeDirectDispatch(...): Invalid `event`.'
	  ) : invariant(!Array.isArray(dispatchListener)));
	  var res = dispatchListener ?
	    dispatchListener(event, dispatchID) :
	    null;
	  event._dispatchListeners = null;
	  event._dispatchIDs = null;
	  return res;
	}

	/**
	 * @param {SyntheticEvent} event
	 * @return {bool} True iff number of dispatches accumulated is greater than 0.
	 */
	function hasDispatches(event) {
	  return !!event._dispatchListeners;
	}

	/**
	 * General utilities that are useful in creating custom Event Plugins.
	 */
	var EventPluginUtils = {
	  isEndish: isEndish,
	  isMoveish: isMoveish,
	  isStartish: isStartish,

	  executeDirectDispatch: executeDirectDispatch,
	  executeDispatch: executeDispatch,
	  executeDispatchesInOrder: executeDispatchesInOrder,
	  executeDispatchesInOrderStopAtTrue: executeDispatchesInOrderStopAtTrue,
	  hasDispatches: hasDispatches,
	  injection: injection,
	  useTouchEvents: false
	};

	module.exports = EventPluginUtils;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 6 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule EventConstants
	 */

	'use strict';

	var keyMirror = __webpack_require__(7);

	var PropagationPhases = keyMirror({bubbled: null, captured: null});

	/**
	 * Types of raw signals from the browser caught at the top level.
	 */
	var topLevelTypes = keyMirror({
	  topBlur: null,
	  topChange: null,
	  topClick: null,
	  topCompositionEnd: null,
	  topCompositionStart: null,
	  topCompositionUpdate: null,
	  topContextMenu: null,
	  topCopy: null,
	  topCut: null,
	  topDoubleClick: null,
	  topDrag: null,
	  topDragEnd: null,
	  topDragEnter: null,
	  topDragExit: null,
	  topDragLeave: null,
	  topDragOver: null,
	  topDragStart: null,
	  topDrop: null,
	  topError: null,
	  topFocus: null,
	  topInput: null,
	  topKeyDown: null,
	  topKeyPress: null,
	  topKeyUp: null,
	  topLoad: null,
	  topMouseDown: null,
	  topMouseMove: null,
	  topMouseOut: null,
	  topMouseOver: null,
	  topMouseUp: null,
	  topPaste: null,
	  topReset: null,
	  topScroll: null,
	  topSelectionChange: null,
	  topSubmit: null,
	  topTextInput: null,
	  topTouchCancel: null,
	  topTouchEnd: null,
	  topTouchMove: null,
	  topTouchStart: null,
	  topWheel: null
	});

	var EventConstants = {
	  topLevelTypes: topLevelTypes,
	  PropagationPhases: PropagationPhases
	};

	module.exports = EventConstants;


/***/ },
/* 7 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule keyMirror
	 * @typechecks static-only
	 */

	'use strict';

	var invariant = __webpack_require__(8);

	/**
	 * Constructs an enumeration with keys equal to their value.
	 *
	 * For example:
	 *
	 *   var COLORS = keyMirror({blue: null, red: null});
	 *   var myColor = COLORS.blue;
	 *   var isColorValid = !!COLORS[myColor];
	 *
	 * The last line could not be performed if the values of the generated enum were
	 * not equal to their keys.
	 *
	 *   Input:  {key1: val1, key2: val2}
	 *   Output: {key1: key1, key2: key2}
	 *
	 * @param {object} obj
	 * @return {object}
	 */
	var keyMirror = function(obj) {
	  var ret = {};
	  var key;
	  ("production" !== process.env.NODE_ENV ? invariant(
	    obj instanceof Object && !Array.isArray(obj),
	    'keyMirror(...): Argument must be an object.'
	  ) : invariant(obj instanceof Object && !Array.isArray(obj)));
	  for (key in obj) {
	    if (!obj.hasOwnProperty(key)) {
	      continue;
	    }
	    ret[key] = key;
	  }
	  return ret;
	};

	module.exports = keyMirror;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 8 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule invariant
	 */

	"use strict";

	/**
	 * Use invariant() to assert state which your program assumes to be true.
	 *
	 * Provide sprintf-style format (only %s is supported) and arguments
	 * to provide information about what broke and what you were
	 * expecting.
	 *
	 * The invariant message will be stripped in production, but the invariant
	 * will remain to ensure logic does not differ in production.
	 */

	var invariant = function(condition, format, a, b, c, d, e, f) {
	  if ("production" !== process.env.NODE_ENV) {
	    if (format === undefined) {
	      throw new Error('invariant requires an error message argument');
	    }
	  }

	  if (!condition) {
	    var error;
	    if (format === undefined) {
	      error = new Error(
	        'Minified exception occurred; use the non-minified dev environment ' +
	        'for the full error message and additional helpful warnings.'
	      );
	    } else {
	      var args = [a, b, c, d, e, f];
	      var argIndex = 0;
	      error = new Error(
	        'Invariant Violation: ' +
	        format.replace(/%s/g, function() { return args[argIndex++]; })
	      );
	    }

	    error.framesToPop = 1; // we don't care about invariant's own frame
	    throw error;
	  }
	};

	module.exports = invariant;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 9 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactChildren
	 */

	'use strict';

	var PooledClass = __webpack_require__(10);
	var ReactFragment = __webpack_require__(11);

	var traverseAllChildren = __webpack_require__(19);
	var warning = __webpack_require__(16);

	var twoArgumentPooler = PooledClass.twoArgumentPooler;
	var threeArgumentPooler = PooledClass.threeArgumentPooler;

	/**
	 * PooledClass representing the bookkeeping associated with performing a child
	 * traversal. Allows avoiding binding callbacks.
	 *
	 * @constructor ForEachBookKeeping
	 * @param {!function} forEachFunction Function to perform traversal with.
	 * @param {?*} forEachContext Context to perform context with.
	 */
	function ForEachBookKeeping(forEachFunction, forEachContext) {
	  this.forEachFunction = forEachFunction;
	  this.forEachContext = forEachContext;
	}
	PooledClass.addPoolingTo(ForEachBookKeeping, twoArgumentPooler);

	function forEachSingleChild(traverseContext, child, name, i) {
	  var forEachBookKeeping = traverseContext;
	  forEachBookKeeping.forEachFunction.call(
	    forEachBookKeeping.forEachContext, child, i);
	}

	/**
	 * Iterates through children that are typically specified as `props.children`.
	 *
	 * The provided forEachFunc(child, index) will be called for each
	 * leaf child.
	 *
	 * @param {?*} children Children tree container.
	 * @param {function(*, int)} forEachFunc.
	 * @param {*} forEachContext Context for forEachContext.
	 */
	function forEachChildren(children, forEachFunc, forEachContext) {
	  if (children == null) {
	    return children;
	  }

	  var traverseContext =
	    ForEachBookKeeping.getPooled(forEachFunc, forEachContext);
	  traverseAllChildren(children, forEachSingleChild, traverseContext);
	  ForEachBookKeeping.release(traverseContext);
	}

	/**
	 * PooledClass representing the bookkeeping associated with performing a child
	 * mapping. Allows avoiding binding callbacks.
	 *
	 * @constructor MapBookKeeping
	 * @param {!*} mapResult Object containing the ordered map of results.
	 * @param {!function} mapFunction Function to perform mapping with.
	 * @param {?*} mapContext Context to perform mapping with.
	 */
	function MapBookKeeping(mapResult, mapFunction, mapContext) {
	  this.mapResult = mapResult;
	  this.mapFunction = mapFunction;
	  this.mapContext = mapContext;
	}
	PooledClass.addPoolingTo(MapBookKeeping, threeArgumentPooler);

	function mapSingleChildIntoContext(traverseContext, child, name, i) {
	  var mapBookKeeping = traverseContext;
	  var mapResult = mapBookKeeping.mapResult;

	  var keyUnique = !mapResult.hasOwnProperty(name);
	  if ("production" !== process.env.NODE_ENV) {
	    ("production" !== process.env.NODE_ENV ? warning(
	      keyUnique,
	      'ReactChildren.map(...): Encountered two children with the same key, ' +
	      '`%s`. Child keys must be unique; when two children share a key, only ' +
	      'the first child will be used.',
	      name
	    ) : null);
	  }

	  if (keyUnique) {
	    var mappedChild =
	      mapBookKeeping.mapFunction.call(mapBookKeeping.mapContext, child, i);
	    mapResult[name] = mappedChild;
	  }
	}

	/**
	 * Maps children that are typically specified as `props.children`.
	 *
	 * The provided mapFunction(child, key, index) will be called for each
	 * leaf child.
	 *
	 * TODO: This may likely break any calls to `ReactChildren.map` that were
	 * previously relying on the fact that we guarded against null children.
	 *
	 * @param {?*} children Children tree container.
	 * @param {function(*, int)} mapFunction.
	 * @param {*} mapContext Context for mapFunction.
	 * @return {object} Object containing the ordered map of results.
	 */
	function mapChildren(children, func, context) {
	  if (children == null) {
	    return children;
	  }

	  var mapResult = {};
	  var traverseContext = MapBookKeeping.getPooled(mapResult, func, context);
	  traverseAllChildren(children, mapSingleChildIntoContext, traverseContext);
	  MapBookKeeping.release(traverseContext);
	  return ReactFragment.create(mapResult);
	}

	function forEachSingleChildDummy(traverseContext, child, name, i) {
	  return null;
	}

	/**
	 * Count the number of children that are typically specified as
	 * `props.children`.
	 *
	 * @param {?*} children Children tree container.
	 * @return {number} The number of children.
	 */
	function countChildren(children, context) {
	  return traverseAllChildren(children, forEachSingleChildDummy, null);
	}

	var ReactChildren = {
	  forEach: forEachChildren,
	  map: mapChildren,
	  count: countChildren
	};

	module.exports = ReactChildren;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 10 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule PooledClass
	 */

	'use strict';

	var invariant = __webpack_require__(8);

	/**
	 * Static poolers. Several custom versions for each potential number of
	 * arguments. A completely generic pooler is easy to implement, but would
	 * require accessing the `arguments` object. In each of these, `this` refers to
	 * the Class itself, not an instance. If any others are needed, simply add them
	 * here, or in their own files.
	 */
	var oneArgumentPooler = function(copyFieldsFrom) {
	  var Klass = this;
	  if (Klass.instancePool.length) {
	    var instance = Klass.instancePool.pop();
	    Klass.call(instance, copyFieldsFrom);
	    return instance;
	  } else {
	    return new Klass(copyFieldsFrom);
	  }
	};

	var twoArgumentPooler = function(a1, a2) {
	  var Klass = this;
	  if (Klass.instancePool.length) {
	    var instance = Klass.instancePool.pop();
	    Klass.call(instance, a1, a2);
	    return instance;
	  } else {
	    return new Klass(a1, a2);
	  }
	};

	var threeArgumentPooler = function(a1, a2, a3) {
	  var Klass = this;
	  if (Klass.instancePool.length) {
	    var instance = Klass.instancePool.pop();
	    Klass.call(instance, a1, a2, a3);
	    return instance;
	  } else {
	    return new Klass(a1, a2, a3);
	  }
	};

	var fiveArgumentPooler = function(a1, a2, a3, a4, a5) {
	  var Klass = this;
	  if (Klass.instancePool.length) {
	    var instance = Klass.instancePool.pop();
	    Klass.call(instance, a1, a2, a3, a4, a5);
	    return instance;
	  } else {
	    return new Klass(a1, a2, a3, a4, a5);
	  }
	};

	var standardReleaser = function(instance) {
	  var Klass = this;
	  ("production" !== process.env.NODE_ENV ? invariant(
	    instance instanceof Klass,
	    'Trying to release an instance into a pool of a different type.'
	  ) : invariant(instance instanceof Klass));
	  if (instance.destructor) {
	    instance.destructor();
	  }
	  if (Klass.instancePool.length < Klass.poolSize) {
	    Klass.instancePool.push(instance);
	  }
	};

	var DEFAULT_POOL_SIZE = 10;
	var DEFAULT_POOLER = oneArgumentPooler;

	/**
	 * Augments `CopyConstructor` to be a poolable class, augmenting only the class
	 * itself (statically) not adding any prototypical fields. Any CopyConstructor
	 * you give this may have a `poolSize` property, and will look for a
	 * prototypical `destructor` on instances (optional).
	 *
	 * @param {Function} CopyConstructor Constructor that can be used to reset.
	 * @param {Function} pooler Customizable pooler.
	 */
	var addPoolingTo = function(CopyConstructor, pooler) {
	  var NewKlass = CopyConstructor;
	  NewKlass.instancePool = [];
	  NewKlass.getPooled = pooler || DEFAULT_POOLER;
	  if (!NewKlass.poolSize) {
	    NewKlass.poolSize = DEFAULT_POOL_SIZE;
	  }
	  NewKlass.release = standardReleaser;
	  return NewKlass;
	};

	var PooledClass = {
	  addPoolingTo: addPoolingTo,
	  oneArgumentPooler: oneArgumentPooler,
	  twoArgumentPooler: twoArgumentPooler,
	  threeArgumentPooler: threeArgumentPooler,
	  fiveArgumentPooler: fiveArgumentPooler
	};

	module.exports = PooledClass;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 11 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	* @providesModule ReactFragment
	*/

	'use strict';

	var ReactElement = __webpack_require__(12);

	var warning = __webpack_require__(16);

	/**
	 * We used to allow keyed objects to serve as a collection of ReactElements,
	 * or nested sets. This allowed us a way to explicitly key a set a fragment of
	 * components. This is now being replaced with an opaque data structure.
	 * The upgrade path is to call React.addons.createFragment({ key: value }) to
	 * create a keyed fragment. The resulting data structure is opaque, for now.
	 */

	if ("production" !== process.env.NODE_ENV) {
	  var fragmentKey = '_reactFragment';
	  var didWarnKey = '_reactDidWarn';
	  var canWarnForReactFragment = false;

	  try {
	    // Feature test. Don't even try to issue this warning if we can't use
	    // enumerable: false.

	    var dummy = function() {
	      return 1;
	    };

	    Object.defineProperty(
	      {},
	      fragmentKey,
	      {enumerable: false, value: true}
	    );

	    Object.defineProperty(
	      {},
	      'key',
	      {enumerable: true, get: dummy}
	    );

	    canWarnForReactFragment = true;
	  } catch (x) { }

	  var proxyPropertyAccessWithWarning = function(obj, key) {
	    Object.defineProperty(obj, key, {
	      enumerable: true,
	      get: function() {
	        ("production" !== process.env.NODE_ENV ? warning(
	          this[didWarnKey],
	          'A ReactFragment is an opaque type. Accessing any of its ' +
	          'properties is deprecated. Pass it to one of the React.Children ' +
	          'helpers.'
	        ) : null);
	        this[didWarnKey] = true;
	        return this[fragmentKey][key];
	      },
	      set: function(value) {
	        ("production" !== process.env.NODE_ENV ? warning(
	          this[didWarnKey],
	          'A ReactFragment is an immutable opaque type. Mutating its ' +
	          'properties is deprecated.'
	        ) : null);
	        this[didWarnKey] = true;
	        this[fragmentKey][key] = value;
	      }
	    });
	  };

	  var issuedWarnings = {};

	  var didWarnForFragment = function(fragment) {
	    // We use the keys and the type of the value as a heuristic to dedupe the
	    // warning to avoid spamming too much.
	    var fragmentCacheKey = '';
	    for (var key in fragment) {
	      fragmentCacheKey += key + ':' + (typeof fragment[key]) + ',';
	    }
	    var alreadyWarnedOnce = !!issuedWarnings[fragmentCacheKey];
	    issuedWarnings[fragmentCacheKey] = true;
	    return alreadyWarnedOnce;
	  };
	}

	var ReactFragment = {
	  // Wrap a keyed object in an opaque proxy that warns you if you access any
	  // of its properties.
	  create: function(object) {
	    if ("production" !== process.env.NODE_ENV) {
	      if (typeof object !== 'object' || !object || Array.isArray(object)) {
	        ("production" !== process.env.NODE_ENV ? warning(
	          false,
	          'React.addons.createFragment only accepts a single object.',
	          object
	        ) : null);
	        return object;
	      }
	      if (ReactElement.isValidElement(object)) {
	        ("production" !== process.env.NODE_ENV ? warning(
	          false,
	          'React.addons.createFragment does not accept a ReactElement ' +
	          'without a wrapper object.'
	        ) : null);
	        return object;
	      }
	      if (canWarnForReactFragment) {
	        var proxy = {};
	        Object.defineProperty(proxy, fragmentKey, {
	          enumerable: false,
	          value: object
	        });
	        Object.defineProperty(proxy, didWarnKey, {
	          writable: true,
	          enumerable: false,
	          value: false
	        });
	        for (var key in object) {
	          proxyPropertyAccessWithWarning(proxy, key);
	        }
	        Object.preventExtensions(proxy);
	        return proxy;
	      }
	    }
	    return object;
	  },
	  // Extract the original keyed object from the fragment opaque type. Warn if
	  // a plain object is passed here.
	  extract: function(fragment) {
	    if ("production" !== process.env.NODE_ENV) {
	      if (canWarnForReactFragment) {
	        if (!fragment[fragmentKey]) {
	          ("production" !== process.env.NODE_ENV ? warning(
	            didWarnForFragment(fragment),
	            'Any use of a keyed object should be wrapped in ' +
	            'React.addons.createFragment(object) before being passed as a ' +
	            'child.'
	          ) : null);
	          return fragment;
	        }
	        return fragment[fragmentKey];
	      }
	    }
	    return fragment;
	  },
	  // Check if this is a fragment and if so, extract the keyed object. If it
	  // is a fragment-like object, warn that it should be wrapped. Ignore if we
	  // can't determine what kind of object this is.
	  extractIfFragment: function(fragment) {
	    if ("production" !== process.env.NODE_ENV) {
	      if (canWarnForReactFragment) {
	        // If it is the opaque type, return the keyed object.
	        if (fragment[fragmentKey]) {
	          return fragment[fragmentKey];
	        }
	        // Otherwise, check each property if it has an element, if it does
	        // it is probably meant as a fragment, so we can warn early. Defer,
	        // the warning to extract.
	        for (var key in fragment) {
	          if (fragment.hasOwnProperty(key) &&
	              ReactElement.isValidElement(fragment[key])) {
	            // This looks like a fragment object, we should provide an
	            // early warning.
	            return ReactFragment.extract(fragment);
	          }
	        }
	      }
	    }
	    return fragment;
	  }
	};

	module.exports = ReactFragment;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 12 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactElement
	 */

	'use strict';

	var ReactContext = __webpack_require__(13);
	var ReactCurrentOwner = __webpack_require__(18);

	var assign = __webpack_require__(14);
	var warning = __webpack_require__(16);

	var RESERVED_PROPS = {
	  key: true,
	  ref: true
	};

	/**
	 * Warn for mutations.
	 *
	 * @internal
	 * @param {object} object
	 * @param {string} key
	 */
	function defineWarningProperty(object, key) {
	  Object.defineProperty(object, key, {

	    configurable: false,
	    enumerable: true,

	    get: function() {
	      if (!this._store) {
	        return null;
	      }
	      return this._store[key];
	    },

	    set: function(value) {
	      ("production" !== process.env.NODE_ENV ? warning(
	        false,
	        'Don\'t set the %s property of the React element. Instead, ' +
	        'specify the correct value when initially creating the element.',
	        key
	      ) : null);
	      this._store[key] = value;
	    }

	  });
	}

	/**
	 * This is updated to true if the membrane is successfully created.
	 */
	var useMutationMembrane = false;

	/**
	 * Warn for mutations.
	 *
	 * @internal
	 * @param {object} element
	 */
	function defineMutationMembrane(prototype) {
	  try {
	    var pseudoFrozenProperties = {
	      props: true
	    };
	    for (var key in pseudoFrozenProperties) {
	      defineWarningProperty(prototype, key);
	    }
	    useMutationMembrane = true;
	  } catch (x) {
	    // IE will fail on defineProperty
	  }
	}

	/**
	 * Base constructor for all React elements. This is only used to make this
	 * work with a dynamic instanceof check. Nothing should live on this prototype.
	 *
	 * @param {*} type
	 * @param {string|object} ref
	 * @param {*} key
	 * @param {*} props
	 * @internal
	 */
	var ReactElement = function(type, key, ref, owner, context, props) {
	  // Built-in properties that belong on the element
	  this.type = type;
	  this.key = key;
	  this.ref = ref;

	  // Record the component responsible for creating this element.
	  this._owner = owner;

	  // TODO: Deprecate withContext, and then the context becomes accessible
	  // through the owner.
	  this._context = context;

	  if ("production" !== process.env.NODE_ENV) {
	    // The validation flag and props are currently mutative. We put them on
	    // an external backing store so that we can freeze the whole object.
	    // This can be replaced with a WeakMap once they are implemented in
	    // commonly used development environments.
	    this._store = {props: props, originalProps: assign({}, props)};

	    // To make comparing ReactElements easier for testing purposes, we make
	    // the validation flag non-enumerable (where possible, which should
	    // include every environment we run tests in), so the test framework
	    // ignores it.
	    try {
	      Object.defineProperty(this._store, 'validated', {
	        configurable: false,
	        enumerable: false,
	        writable: true
	      });
	    } catch (x) {
	    }
	    this._store.validated = false;

	    // We're not allowed to set props directly on the object so we early
	    // return and rely on the prototype membrane to forward to the backing
	    // store.
	    if (useMutationMembrane) {
	      Object.freeze(this);
	      return;
	    }
	  }

	  this.props = props;
	};

	// We intentionally don't expose the function on the constructor property.
	// ReactElement should be indistinguishable from a plain object.
	ReactElement.prototype = {
	  _isReactElement: true
	};

	if ("production" !== process.env.NODE_ENV) {
	  defineMutationMembrane(ReactElement.prototype);
	}

	ReactElement.createElement = function(type, config, children) {
	  var propName;

	  // Reserved names are extracted
	  var props = {};

	  var key = null;
	  var ref = null;

	  if (config != null) {
	    ref = config.ref === undefined ? null : config.ref;
	    key = config.key === undefined ? null : '' + config.key;
	    // Remaining properties are added to a new props object
	    for (propName in config) {
	      if (config.hasOwnProperty(propName) &&
	          !RESERVED_PROPS.hasOwnProperty(propName)) {
	        props[propName] = config[propName];
	      }
	    }
	  }

	  // Children can be more than one argument, and those are transferred onto
	  // the newly allocated props object.
	  var childrenLength = arguments.length - 2;
	  if (childrenLength === 1) {
	    props.children = children;
	  } else if (childrenLength > 1) {
	    var childArray = Array(childrenLength);
	    for (var i = 0; i < childrenLength; i++) {
	      childArray[i] = arguments[i + 2];
	    }
	    props.children = childArray;
	  }

	  // Resolve default props
	  if (type && type.defaultProps) {
	    var defaultProps = type.defaultProps;
	    for (propName in defaultProps) {
	      if (typeof props[propName] === 'undefined') {
	        props[propName] = defaultProps[propName];
	      }
	    }
	  }

	  return new ReactElement(
	    type,
	    key,
	    ref,
	    ReactCurrentOwner.current,
	    ReactContext.current,
	    props
	  );
	};

	ReactElement.createFactory = function(type) {
	  var factory = ReactElement.createElement.bind(null, type);
	  // Expose the type on the factory and the prototype so that it can be
	  // easily accessed on elements. E.g. <Foo />.type === Foo.type.
	  // This should not be named `constructor` since this may not be the function
	  // that created the element, and it may not even be a constructor.
	  // Legacy hook TODO: Warn if this is accessed
	  factory.type = type;
	  return factory;
	};

	ReactElement.cloneAndReplaceProps = function(oldElement, newProps) {
	  var newElement = new ReactElement(
	    oldElement.type,
	    oldElement.key,
	    oldElement.ref,
	    oldElement._owner,
	    oldElement._context,
	    newProps
	  );

	  if ("production" !== process.env.NODE_ENV) {
	    // If the key on the original is valid, then the clone is valid
	    newElement._store.validated = oldElement._store.validated;
	  }
	  return newElement;
	};

	ReactElement.cloneElement = function(element, config, children) {
	  var propName;

	  // Original props are copied
	  var props = assign({}, element.props);

	  // Reserved names are extracted
	  var key = element.key;
	  var ref = element.ref;

	  // Owner will be preserved, unless ref is overridden
	  var owner = element._owner;

	  if (config != null) {
	    if (config.ref !== undefined) {
	      // Silently steal the ref from the parent.
	      ref = config.ref;
	      owner = ReactCurrentOwner.current;
	    }
	    if (config.key !== undefined) {
	      key = '' + config.key;
	    }
	    // Remaining properties override existing props
	    for (propName in config) {
	      if (config.hasOwnProperty(propName) &&
	          !RESERVED_PROPS.hasOwnProperty(propName)) {
	        props[propName] = config[propName];
	      }
	    }
	  }

	  // Children can be more than one argument, and those are transferred onto
	  // the newly allocated props object.
	  var childrenLength = arguments.length - 2;
	  if (childrenLength === 1) {
	    props.children = children;
	  } else if (childrenLength > 1) {
	    var childArray = Array(childrenLength);
	    for (var i = 0; i < childrenLength; i++) {
	      childArray[i] = arguments[i + 2];
	    }
	    props.children = childArray;
	  }

	  return new ReactElement(
	    element.type,
	    key,
	    ref,
	    owner,
	    element._context,
	    props
	  );
	};

	/**
	 * @param {?object} object
	 * @return {boolean} True if `object` is a valid component.
	 * @final
	 */
	ReactElement.isValidElement = function(object) {
	  // ReactTestUtils is often used outside of beforeEach where as React is
	  // within it. This leads to two different instances of React on the same
	  // page. To identify a element from a different React instance we use
	  // a flag instead of an instanceof check.
	  var isElement = !!(object && object._isReactElement);
	  // if (isElement && !(object instanceof ReactElement)) {
	  // This is an indicator that you're using multiple versions of React at the
	  // same time. This will screw with ownership and stuff. Fix it, please.
	  // TODO: We could possibly warn here.
	  // }
	  return isElement;
	};

	module.exports = ReactElement;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 13 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactContext
	 */

	'use strict';

	var assign = __webpack_require__(14);
	var emptyObject = __webpack_require__(15);
	var warning = __webpack_require__(16);

	var didWarn = false;

	/**
	 * Keeps track of the current context.
	 *
	 * The context is automatically passed down the component ownership hierarchy
	 * and is accessible via `this.context` on ReactCompositeComponents.
	 */
	var ReactContext = {

	  /**
	   * @internal
	   * @type {object}
	   */
	  current: emptyObject,

	  /**
	   * Temporarily extends the current context while executing scopedCallback.
	   *
	   * A typical use case might look like
	   *
	   *  render: function() {
	   *    var children = ReactContext.withContext({foo: 'foo'}, () => (
	   *
	   *    ));
	   *    return <div>{children}</div>;
	   *  }
	   *
	   * @param {object} newContext New context to merge into the existing context
	   * @param {function} scopedCallback Callback to run with the new context
	   * @return {ReactComponent|array<ReactComponent>}
	   */
	  withContext: function(newContext, scopedCallback) {
	    if ("production" !== process.env.NODE_ENV) {
	      ("production" !== process.env.NODE_ENV ? warning(
	        didWarn,
	        'withContext is deprecated and will be removed in a future version. ' +
	        'Use a wrapper component with getChildContext instead.'
	      ) : null);

	      didWarn = true;
	    }

	    var result;
	    var previousContext = ReactContext.current;
	    ReactContext.current = assign({}, previousContext, newContext);
	    try {
	      result = scopedCallback();
	    } finally {
	      ReactContext.current = previousContext;
	    }
	    return result;
	  }

	};

	module.exports = ReactContext;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 14 */
/***/ function(module, exports) {

	/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule Object.assign
	 */

	// https://people.mozilla.org/~jorendorff/es6-draft.html#sec-object.assign

	'use strict';

	function assign(target, sources) {
	  if (target == null) {
	    throw new TypeError('Object.assign target cannot be null or undefined');
	  }

	  var to = Object(target);
	  var hasOwnProperty = Object.prototype.hasOwnProperty;

	  for (var nextIndex = 1; nextIndex < arguments.length; nextIndex++) {
	    var nextSource = arguments[nextIndex];
	    if (nextSource == null) {
	      continue;
	    }

	    var from = Object(nextSource);

	    // We don't currently support accessors nor proxies. Therefore this
	    // copy cannot throw. If we ever supported this then we must handle
	    // exceptions and side-effects. We don't support symbols so they won't
	    // be transferred.

	    for (var key in from) {
	      if (hasOwnProperty.call(from, key)) {
	        to[key] = from[key];
	      }
	    }
	  }

	  return to;
	}

	module.exports = assign;


/***/ },
/* 15 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule emptyObject
	 */

	"use strict";

	var emptyObject = {};

	if ("production" !== process.env.NODE_ENV) {
	  Object.freeze(emptyObject);
	}

	module.exports = emptyObject;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 16 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule warning
	 */

	"use strict";

	var emptyFunction = __webpack_require__(17);

	/**
	 * Similar to invariant but only logs a warning if the condition is not met.
	 * This can be used to log issues in development environments in critical
	 * paths. Removing the logging code for production environments will keep the
	 * same logic and follow the same code paths.
	 */

	var warning = emptyFunction;

	if ("production" !== process.env.NODE_ENV) {
	  warning = function(condition, format ) {for (var args=[],$__0=2,$__1=arguments.length;$__0<$__1;$__0++) args.push(arguments[$__0]);
	    if (format === undefined) {
	      throw new Error(
	        '`warning(condition, format, ...args)` requires a warning ' +
	        'message argument'
	      );
	    }

	    if (format.length < 10 || /^[s\W]*$/.test(format)) {
	      throw new Error(
	        'The warning format should be able to uniquely identify this ' +
	        'warning. Please, use a more descriptive format than: ' + format
	      );
	    }

	    if (format.indexOf('Failed Composite propType: ') === 0) {
	      return; // Ignore CompositeComponent proptype check.
	    }

	    if (!condition) {
	      var argIndex = 0;
	      var message = 'Warning: ' + format.replace(/%s/g, function()  {return args[argIndex++];});
	      console.warn(message);
	      try {
	        // --- Welcome to debugging React ---
	        // This error was thrown as a convenience so that you can use this stack
	        // to find the callsite that caused this warning to fire.
	        throw new Error(message);
	      } catch(x) {}
	    }
	  };
	}

	module.exports = warning;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 17 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule emptyFunction
	 */

	function makeEmptyFunction(arg) {
	  return function() {
	    return arg;
	  };
	}

	/**
	 * This function accepts and discards inputs; it has no side effects. This is
	 * primarily useful idiomatically for overridable function endpoints which
	 * always need to be callable, since JS lacks a null-call idiom ala Cocoa.
	 */
	function emptyFunction() {}

	emptyFunction.thatReturns = makeEmptyFunction;
	emptyFunction.thatReturnsFalse = makeEmptyFunction(false);
	emptyFunction.thatReturnsTrue = makeEmptyFunction(true);
	emptyFunction.thatReturnsNull = makeEmptyFunction(null);
	emptyFunction.thatReturnsThis = function() { return this; };
	emptyFunction.thatReturnsArgument = function(arg) { return arg; };

	module.exports = emptyFunction;


/***/ },
/* 18 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactCurrentOwner
	 */

	'use strict';

	/**
	 * Keeps track of the current owner.
	 *
	 * The current owner is the component who should own any components that are
	 * currently being constructed.
	 *
	 * The depth indicate how many composite components are above this render level.
	 */
	var ReactCurrentOwner = {

	  /**
	   * @internal
	   * @type {ReactComponent}
	   */
	  current: null

	};

	module.exports = ReactCurrentOwner;


/***/ },
/* 19 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule traverseAllChildren
	 */

	'use strict';

	var ReactElement = __webpack_require__(12);
	var ReactFragment = __webpack_require__(11);
	var ReactInstanceHandles = __webpack_require__(21);

	var getIteratorFn = __webpack_require__(20);
	var invariant = __webpack_require__(8);
	var warning = __webpack_require__(16);

	var SEPARATOR = ReactInstanceHandles.SEPARATOR;
	var SUBSEPARATOR = ':';

	/**
	 * TODO: Test that a single child and an array with one item have the same key
	 * pattern.
	 */

	var userProvidedKeyEscaperLookup = {
	  '=': '=0',
	  '.': '=1',
	  ':': '=2'
	};

	var userProvidedKeyEscapeRegex = /[=.:]/g;

	var didWarnAboutMaps = false;

	function userProvidedKeyEscaper(match) {
	  return userProvidedKeyEscaperLookup[match];
	}

	/**
	 * Generate a key string that identifies a component within a set.
	 *
	 * @param {*} component A component that could contain a manual key.
	 * @param {number} index Index that is used if a manual key is not provided.
	 * @return {string}
	 */
	function getComponentKey(component, index) {
	  if (component && component.key != null) {
	    // Explicit key
	    return wrapUserProvidedKey(component.key);
	  }
	  // Implicit key determined by the index in the set
	  return index.toString(36);
	}

	/**
	 * Escape a component key so that it is safe to use in a reactid.
	 *
	 * @param {*} key Component key to be escaped.
	 * @return {string} An escaped string.
	 */
	function escapeUserProvidedKey(text) {
	  return ('' + text).replace(
	    userProvidedKeyEscapeRegex,
	    userProvidedKeyEscaper
	  );
	}

	/**
	 * Wrap a `key` value explicitly provided by the user to distinguish it from
	 * implicitly-generated keys generated by a component's index in its parent.
	 *
	 * @param {string} key Value of a user-provided `key` attribute
	 * @return {string}
	 */
	function wrapUserProvidedKey(key) {
	  return '$' + escapeUserProvidedKey(key);
	}

	/**
	 * @param {?*} children Children tree container.
	 * @param {!string} nameSoFar Name of the key path so far.
	 * @param {!number} indexSoFar Number of children encountered until this point.
	 * @param {!function} callback Callback to invoke with each child found.
	 * @param {?*} traverseContext Used to pass information throughout the traversal
	 * process.
	 * @return {!number} The number of children in this subtree.
	 */
	function traverseAllChildrenImpl(
	  children,
	  nameSoFar,
	  indexSoFar,
	  callback,
	  traverseContext
	) {
	  var type = typeof children;

	  if (type === 'undefined' || type === 'boolean') {
	    // All of the above are perceived as null.
	    children = null;
	  }

	  if (children === null ||
	      type === 'string' ||
	      type === 'number' ||
	      ReactElement.isValidElement(children)) {
	    callback(
	      traverseContext,
	      children,
	      // If it's the only child, treat the name as if it was wrapped in an array
	      // so that it's consistent if the number of children grows.
	      nameSoFar === '' ? SEPARATOR + getComponentKey(children, 0) : nameSoFar,
	      indexSoFar
	    );
	    return 1;
	  }

	  var child, nextName, nextIndex;
	  var subtreeCount = 0; // Count of children found in the current subtree.

	  if (Array.isArray(children)) {
	    for (var i = 0; i < children.length; i++) {
	      child = children[i];
	      nextName = (
	        (nameSoFar !== '' ? nameSoFar + SUBSEPARATOR : SEPARATOR) +
	        getComponentKey(child, i)
	      );
	      nextIndex = indexSoFar + subtreeCount;
	      subtreeCount += traverseAllChildrenImpl(
	        child,
	        nextName,
	        nextIndex,
	        callback,
	        traverseContext
	      );
	    }
	  } else {
	    var iteratorFn = getIteratorFn(children);
	    if (iteratorFn) {
	      var iterator = iteratorFn.call(children);
	      var step;
	      if (iteratorFn !== children.entries) {
	        var ii = 0;
	        while (!(step = iterator.next()).done) {
	          child = step.value;
	          nextName = (
	            (nameSoFar !== '' ? nameSoFar + SUBSEPARATOR : SEPARATOR) +
	            getComponentKey(child, ii++)
	          );
	          nextIndex = indexSoFar + subtreeCount;
	          subtreeCount += traverseAllChildrenImpl(
	            child,
	            nextName,
	            nextIndex,
	            callback,
	            traverseContext
	          );
	        }
	      } else {
	        if ("production" !== process.env.NODE_ENV) {
	          ("production" !== process.env.NODE_ENV ? warning(
	            didWarnAboutMaps,
	            'Using Maps as children is not yet fully supported. It is an ' +
	            'experimental feature that might be removed. Convert it to a ' +
	            'sequence / iterable of keyed ReactElements instead.'
	          ) : null);
	          didWarnAboutMaps = true;
	        }
	        // Iterator will provide entry [k,v] tuples rather than values.
	        while (!(step = iterator.next()).done) {
	          var entry = step.value;
	          if (entry) {
	            child = entry[1];
	            nextName = (
	              (nameSoFar !== '' ? nameSoFar + SUBSEPARATOR : SEPARATOR) +
	              wrapUserProvidedKey(entry[0]) + SUBSEPARATOR +
	              getComponentKey(child, 0)
	            );
	            nextIndex = indexSoFar + subtreeCount;
	            subtreeCount += traverseAllChildrenImpl(
	              child,
	              nextName,
	              nextIndex,
	              callback,
	              traverseContext
	            );
	          }
	        }
	      }
	    } else if (type === 'object') {
	      ("production" !== process.env.NODE_ENV ? invariant(
	        children.nodeType !== 1,
	        'traverseAllChildren(...): Encountered an invalid child; DOM ' +
	        'elements are not valid children of React components.'
	      ) : invariant(children.nodeType !== 1));
	      var fragment = ReactFragment.extract(children);
	      for (var key in fragment) {
	        if (fragment.hasOwnProperty(key)) {
	          child = fragment[key];
	          nextName = (
	            (nameSoFar !== '' ? nameSoFar + SUBSEPARATOR : SEPARATOR) +
	            wrapUserProvidedKey(key) + SUBSEPARATOR +
	            getComponentKey(child, 0)
	          );
	          nextIndex = indexSoFar + subtreeCount;
	          subtreeCount += traverseAllChildrenImpl(
	            child,
	            nextName,
	            nextIndex,
	            callback,
	            traverseContext
	          );
	        }
	      }
	    }
	  }

	  return subtreeCount;
	}

	/**
	 * Traverses children that are typically specified as `props.children`, but
	 * might also be specified through attributes:
	 *
	 * - `traverseAllChildren(this.props.children, ...)`
	 * - `traverseAllChildren(this.props.leftPanelChildren, ...)`
	 *
	 * The `traverseContext` is an optional argument that is passed through the
	 * entire traversal. It can be used to store accumulations or anything else that
	 * the callback might find relevant.
	 *
	 * @param {?*} children Children tree object.
	 * @param {!function} callback To invoke upon traversing each child.
	 * @param {?*} traverseContext Context for traversal.
	 * @return {!number} The number of children in this subtree.
	 */
	function traverseAllChildren(children, callback, traverseContext) {
	  if (children == null) {
	    return 0;
	  }

	  return traverseAllChildrenImpl(children, '', 0, callback, traverseContext);
	}

	module.exports = traverseAllChildren;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 20 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getIteratorFn
	 * @typechecks static-only
	 */

	'use strict';

	/* global Symbol */
	var ITERATOR_SYMBOL = typeof Symbol === 'function' && Symbol.iterator;
	var FAUX_ITERATOR_SYMBOL = '@@iterator'; // Before Symbol spec.

	/**
	 * Returns the iterator method function contained on the iterable object.
	 *
	 * Be sure to invoke the function with the iterable as context:
	 *
	 *     var iteratorFn = getIteratorFn(myIterable);
	 *     if (iteratorFn) {
	 *       var iterator = iteratorFn.call(myIterable);
	 *       ...
	 *     }
	 *
	 * @param {?object} maybeIterable
	 * @return {?function}
	 */
	function getIteratorFn(maybeIterable) {
	  var iteratorFn = maybeIterable && (
	    (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL])
	  );
	  if (typeof iteratorFn === 'function') {
	    return iteratorFn;
	  }
	}

	module.exports = getIteratorFn;


/***/ },
/* 21 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactInstanceHandles
	 * @typechecks static-only
	 */

	'use strict';

	var ReactRootIndex = __webpack_require__(22);

	var invariant = __webpack_require__(8);

	var SEPARATOR = '.';
	var SEPARATOR_LENGTH = SEPARATOR.length;

	/**
	 * Maximum depth of traversals before we consider the possibility of a bad ID.
	 */
	var MAX_TREE_DEPTH = 100;

	/**
	 * Creates a DOM ID prefix to use when mounting React components.
	 *
	 * @param {number} index A unique integer
	 * @return {string} React root ID.
	 * @internal
	 */
	function getReactRootIDString(index) {
	  return SEPARATOR + index.toString(36);
	}

	/**
	 * Checks if a character in the supplied ID is a separator or the end.
	 *
	 * @param {string} id A React DOM ID.
	 * @param {number} index Index of the character to check.
	 * @return {boolean} True if the character is a separator or end of the ID.
	 * @private
	 */
	function isBoundary(id, index) {
	  return id.charAt(index) === SEPARATOR || index === id.length;
	}

	/**
	 * Checks if the supplied string is a valid React DOM ID.
	 *
	 * @param {string} id A React DOM ID, maybe.
	 * @return {boolean} True if the string is a valid React DOM ID.
	 * @private
	 */
	function isValidID(id) {
	  return id === '' || (
	    id.charAt(0) === SEPARATOR && id.charAt(id.length - 1) !== SEPARATOR
	  );
	}

	/**
	 * Checks if the first ID is an ancestor of or equal to the second ID.
	 *
	 * @param {string} ancestorID
	 * @param {string} descendantID
	 * @return {boolean} True if `ancestorID` is an ancestor of `descendantID`.
	 * @internal
	 */
	function isAncestorIDOf(ancestorID, descendantID) {
	  return (
	    descendantID.indexOf(ancestorID) === 0 &&
	    isBoundary(descendantID, ancestorID.length)
	  );
	}

	/**
	 * Gets the parent ID of the supplied React DOM ID, `id`.
	 *
	 * @param {string} id ID of a component.
	 * @return {string} ID of the parent, or an empty string.
	 * @private
	 */
	function getParentID(id) {
	  return id ? id.substr(0, id.lastIndexOf(SEPARATOR)) : '';
	}

	/**
	 * Gets the next DOM ID on the tree path from the supplied `ancestorID` to the
	 * supplied `destinationID`. If they are equal, the ID is returned.
	 *
	 * @param {string} ancestorID ID of an ancestor node of `destinationID`.
	 * @param {string} destinationID ID of the destination node.
	 * @return {string} Next ID on the path from `ancestorID` to `destinationID`.
	 * @private
	 */
	function getNextDescendantID(ancestorID, destinationID) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    isValidID(ancestorID) && isValidID(destinationID),
	    'getNextDescendantID(%s, %s): Received an invalid React DOM ID.',
	    ancestorID,
	    destinationID
	  ) : invariant(isValidID(ancestorID) && isValidID(destinationID)));
	  ("production" !== process.env.NODE_ENV ? invariant(
	    isAncestorIDOf(ancestorID, destinationID),
	    'getNextDescendantID(...): React has made an invalid assumption about ' +
	    'the DOM hierarchy. Expected `%s` to be an ancestor of `%s`.',
	    ancestorID,
	    destinationID
	  ) : invariant(isAncestorIDOf(ancestorID, destinationID)));
	  if (ancestorID === destinationID) {
	    return ancestorID;
	  }
	  // Skip over the ancestor and the immediate separator. Traverse until we hit
	  // another separator or we reach the end of `destinationID`.
	  var start = ancestorID.length + SEPARATOR_LENGTH;
	  var i;
	  for (i = start; i < destinationID.length; i++) {
	    if (isBoundary(destinationID, i)) {
	      break;
	    }
	  }
	  return destinationID.substr(0, i);
	}

	/**
	 * Gets the nearest common ancestor ID of two IDs.
	 *
	 * Using this ID scheme, the nearest common ancestor ID is the longest common
	 * prefix of the two IDs that immediately preceded a "marker" in both strings.
	 *
	 * @param {string} oneID
	 * @param {string} twoID
	 * @return {string} Nearest common ancestor ID, or the empty string if none.
	 * @private
	 */
	function getFirstCommonAncestorID(oneID, twoID) {
	  var minLength = Math.min(oneID.length, twoID.length);
	  if (minLength === 0) {
	    return '';
	  }
	  var lastCommonMarkerIndex = 0;
	  // Use `<=` to traverse until the "EOL" of the shorter string.
	  for (var i = 0; i <= minLength; i++) {
	    if (isBoundary(oneID, i) && isBoundary(twoID, i)) {
	      lastCommonMarkerIndex = i;
	    } else if (oneID.charAt(i) !== twoID.charAt(i)) {
	      break;
	    }
	  }
	  var longestCommonID = oneID.substr(0, lastCommonMarkerIndex);
	  ("production" !== process.env.NODE_ENV ? invariant(
	    isValidID(longestCommonID),
	    'getFirstCommonAncestorID(%s, %s): Expected a valid React DOM ID: %s',
	    oneID,
	    twoID,
	    longestCommonID
	  ) : invariant(isValidID(longestCommonID)));
	  return longestCommonID;
	}

	/**
	 * Traverses the parent path between two IDs (either up or down). The IDs must
	 * not be the same, and there must exist a parent path between them. If the
	 * callback returns `false`, traversal is stopped.
	 *
	 * @param {?string} start ID at which to start traversal.
	 * @param {?string} stop ID at which to end traversal.
	 * @param {function} cb Callback to invoke each ID with.
	 * @param {?boolean} skipFirst Whether or not to skip the first node.
	 * @param {?boolean} skipLast Whether or not to skip the last node.
	 * @private
	 */
	function traverseParentPath(start, stop, cb, arg, skipFirst, skipLast) {
	  start = start || '';
	  stop = stop || '';
	  ("production" !== process.env.NODE_ENV ? invariant(
	    start !== stop,
	    'traverseParentPath(...): Cannot traverse from and to the same ID, `%s`.',
	    start
	  ) : invariant(start !== stop));
	  var traverseUp = isAncestorIDOf(stop, start);
	  ("production" !== process.env.NODE_ENV ? invariant(
	    traverseUp || isAncestorIDOf(start, stop),
	    'traverseParentPath(%s, %s, ...): Cannot traverse from two IDs that do ' +
	    'not have a parent path.',
	    start,
	    stop
	  ) : invariant(traverseUp || isAncestorIDOf(start, stop)));
	  // Traverse from `start` to `stop` one depth at a time.
	  var depth = 0;
	  var traverse = traverseUp ? getParentID : getNextDescendantID;
	  for (var id = start; /* until break */; id = traverse(id, stop)) {
	    var ret;
	    if ((!skipFirst || id !== start) && (!skipLast || id !== stop)) {
	      ret = cb(id, traverseUp, arg);
	    }
	    if (ret === false || id === stop) {
	      // Only break //after// visiting `stop`.
	      break;
	    }
	    ("production" !== process.env.NODE_ENV ? invariant(
	      depth++ < MAX_TREE_DEPTH,
	      'traverseParentPath(%s, %s, ...): Detected an infinite loop while ' +
	      'traversing the React DOM ID tree. This may be due to malformed IDs: %s',
	      start, stop
	    ) : invariant(depth++ < MAX_TREE_DEPTH));
	  }
	}

	/**
	 * Manages the IDs assigned to DOM representations of React components. This
	 * uses a specific scheme in order to traverse the DOM efficiently (e.g. in
	 * order to simulate events).
	 *
	 * @internal
	 */
	var ReactInstanceHandles = {

	  /**
	   * Constructs a React root ID
	   * @return {string} A React root ID.
	   */
	  createReactRootID: function() {
	    return getReactRootIDString(ReactRootIndex.createReactRootIndex());
	  },

	  /**
	   * Constructs a React ID by joining a root ID with a name.
	   *
	   * @param {string} rootID Root ID of a parent component.
	   * @param {string} name A component's name (as flattened children).
	   * @return {string} A React ID.
	   * @internal
	   */
	  createReactID: function(rootID, name) {
	    return rootID + name;
	  },

	  /**
	   * Gets the DOM ID of the React component that is the root of the tree that
	   * contains the React component with the supplied DOM ID.
	   *
	   * @param {string} id DOM ID of a React component.
	   * @return {?string} DOM ID of the React component that is the root.
	   * @internal
	   */
	  getReactRootIDFromNodeID: function(id) {
	    if (id && id.charAt(0) === SEPARATOR && id.length > 1) {
	      var index = id.indexOf(SEPARATOR, 1);
	      return index > -1 ? id.substr(0, index) : id;
	    }
	    return null;
	  },

	  /**
	   * Traverses the ID hierarchy and invokes the supplied `cb` on any IDs that
	   * should would receive a `mouseEnter` or `mouseLeave` event.
	   *
	   * NOTE: Does not invoke the callback on the nearest common ancestor because
	   * nothing "entered" or "left" that element.
	   *
	   * @param {string} leaveID ID being left.
	   * @param {string} enterID ID being entered.
	   * @param {function} cb Callback to invoke on each entered/left ID.
	   * @param {*} upArg Argument to invoke the callback with on left IDs.
	   * @param {*} downArg Argument to invoke the callback with on entered IDs.
	   * @internal
	   */
	  traverseEnterLeave: function(leaveID, enterID, cb, upArg, downArg) {
	    var ancestorID = getFirstCommonAncestorID(leaveID, enterID);
	    if (ancestorID !== leaveID) {
	      traverseParentPath(leaveID, ancestorID, cb, upArg, false, true);
	    }
	    if (ancestorID !== enterID) {
	      traverseParentPath(ancestorID, enterID, cb, downArg, true, false);
	    }
	  },

	  /**
	   * Simulates the traversal of a two-phase, capture/bubble event dispatch.
	   *
	   * NOTE: This traversal happens on IDs without touching the DOM.
	   *
	   * @param {string} targetID ID of the target node.
	   * @param {function} cb Callback to invoke.
	   * @param {*} arg Argument to invoke the callback with.
	   * @internal
	   */
	  traverseTwoPhase: function(targetID, cb, arg) {
	    if (targetID) {
	      traverseParentPath('', targetID, cb, arg, true, false);
	      traverseParentPath(targetID, '', cb, arg, false, true);
	    }
	  },

	  /**
	   * Traverse a node ID, calling the supplied `cb` for each ancestor ID. For
	   * example, passing `.0.$row-0.1` would result in `cb` getting called
	   * with `.0`, `.0.$row-0`, and `.0.$row-0.1`.
	   *
	   * NOTE: This traversal happens on IDs without touching the DOM.
	   *
	   * @param {string} targetID ID of the target node.
	   * @param {function} cb Callback to invoke.
	   * @param {*} arg Argument to invoke the callback with.
	   * @internal
	   */
	  traverseAncestors: function(targetID, cb, arg) {
	    traverseParentPath('', targetID, cb, arg, true, false);
	  },

	  /**
	   * Exposed for unit testing.
	   * @private
	   */
	  _getFirstCommonAncestorID: getFirstCommonAncestorID,

	  /**
	   * Exposed for unit testing.
	   * @private
	   */
	  _getNextDescendantID: getNextDescendantID,

	  isAncestorIDOf: isAncestorIDOf,

	  SEPARATOR: SEPARATOR

	};

	module.exports = ReactInstanceHandles;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 22 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactRootIndex
	 * @typechecks
	 */

	'use strict';

	var ReactRootIndexInjection = {
	  /**
	   * @param {function} _createReactRootIndex
	   */
	  injectCreateReactRootIndex: function(_createReactRootIndex) {
	    ReactRootIndex.createReactRootIndex = _createReactRootIndex;
	  }
	};

	var ReactRootIndex = {
	  createReactRootIndex: null,
	  injection: ReactRootIndexInjection
	};

	module.exports = ReactRootIndex;


/***/ },
/* 23 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactComponent
	 */

	'use strict';

	var ReactUpdateQueue = __webpack_require__(24);

	var invariant = __webpack_require__(8);
	var warning = __webpack_require__(16);

	/**
	 * Base class helpers for the updating state of a component.
	 */
	function ReactComponent(props, context) {
	  this.props = props;
	  this.context = context;
	}

	/**
	 * Sets a subset of the state. Always use this to mutate
	 * state. You should treat `this.state` as immutable.
	 *
	 * There is no guarantee that `this.state` will be immediately updated, so
	 * accessing `this.state` after calling this method may return the old value.
	 *
	 * There is no guarantee that calls to `setState` will run synchronously,
	 * as they may eventually be batched together.  You can provide an optional
	 * callback that will be executed when the call to setState is actually
	 * completed.
	 *
	 * When a function is provided to setState, it will be called at some point in
	 * the future (not synchronously). It will be called with the up to date
	 * component arguments (state, props, context). These values can be different
	 * from this.* because your function may be called after receiveProps but before
	 * shouldComponentUpdate, and this new state, props, and context will not yet be
	 * assigned to this.
	 *
	 * @param {object|function} partialState Next partial state or function to
	 *        produce next partial state to be merged with current state.
	 * @param {?function} callback Called after state is updated.
	 * @final
	 * @protected
	 */
	ReactComponent.prototype.setState = function(partialState, callback) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    typeof partialState === 'object' ||
	    typeof partialState === 'function' ||
	    partialState == null,
	    'setState(...): takes an object of state variables to update or a ' +
	    'function which returns an object of state variables.'
	  ) : invariant(typeof partialState === 'object' ||
	  typeof partialState === 'function' ||
	  partialState == null));
	  if ("production" !== process.env.NODE_ENV) {
	    ("production" !== process.env.NODE_ENV ? warning(
	      partialState != null,
	      'setState(...): You passed an undefined or null state object; ' +
	      'instead, use forceUpdate().'
	    ) : null);
	  }
	  ReactUpdateQueue.enqueueSetState(this, partialState);
	  if (callback) {
	    ReactUpdateQueue.enqueueCallback(this, callback);
	  }
	};

	/**
	 * Forces an update. This should only be invoked when it is known with
	 * certainty that we are **not** in a DOM transaction.
	 *
	 * You may want to call this when you know that some deeper aspect of the
	 * component's state has changed but `setState` was not called.
	 *
	 * This will not invoke `shouldComponentUpdate`, but it will invoke
	 * `componentWillUpdate` and `componentDidUpdate`.
	 *
	 * @param {?function} callback Called after update is complete.
	 * @final
	 * @protected
	 */
	ReactComponent.prototype.forceUpdate = function(callback) {
	  ReactUpdateQueue.enqueueForceUpdate(this);
	  if (callback) {
	    ReactUpdateQueue.enqueueCallback(this, callback);
	  }
	};

	/**
	 * Deprecated APIs. These APIs used to exist on classic React classes but since
	 * we would like to deprecate them, we're not going to move them over to this
	 * modern base class. Instead, we define a getter that warns if it's accessed.
	 */
	if ("production" !== process.env.NODE_ENV) {
	  var deprecatedAPIs = {
	    getDOMNode: [
	      'getDOMNode',
	      'Use React.findDOMNode(component) instead.'
	    ],
	    isMounted: [
	      'isMounted',
	      'Instead, make sure to clean up subscriptions and pending requests in ' +
	      'componentWillUnmount to prevent memory leaks.'
	    ],
	    replaceProps: [
	      'replaceProps',
	      'Instead, call React.render again at the top level.'
	    ],
	    replaceState: [
	      'replaceState',
	      'Refactor your code to use setState instead (see ' +
	      'https://github.com/facebook/react/issues/3236).'
	    ],
	    setProps: [
	      'setProps',
	      'Instead, call React.render again at the top level.'
	    ]
	  };
	  var defineDeprecationWarning = function(methodName, info) {
	    try {
	      Object.defineProperty(ReactComponent.prototype, methodName, {
	        get: function() {
	          ("production" !== process.env.NODE_ENV ? warning(
	            false,
	            '%s(...) is deprecated in plain JavaScript React classes. %s',
	            info[0],
	            info[1]
	          ) : null);
	          return undefined;
	        }
	      });
	    } catch (x) {
	      // IE will fail on defineProperty (es5-shim/sham too)
	    }
	  };
	  for (var fnName in deprecatedAPIs) {
	    if (deprecatedAPIs.hasOwnProperty(fnName)) {
	      defineDeprecationWarning(fnName, deprecatedAPIs[fnName]);
	    }
	  }
	}

	module.exports = ReactComponent;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 24 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactUpdateQueue
	 */

	'use strict';

	var ReactLifeCycle = __webpack_require__(36);
	var ReactCurrentOwner = __webpack_require__(18);
	var ReactElement = __webpack_require__(12);
	var ReactInstanceMap = __webpack_require__(37);
	var ReactUpdates = __webpack_require__(25);

	var assign = __webpack_require__(14);
	var invariant = __webpack_require__(8);
	var warning = __webpack_require__(16);

	function enqueueUpdate(internalInstance) {
	  if (internalInstance !== ReactLifeCycle.currentlyMountingInstance) {
	    // If we're in a componentWillMount handler, don't enqueue a rerender
	    // because ReactUpdates assumes we're in a browser context (which is
	    // wrong for server rendering) and we're about to do a render anyway.
	    // See bug in #1740.
	    ReactUpdates.enqueueUpdate(internalInstance);
	  }
	}

	function getInternalInstanceReadyForUpdate(publicInstance, callerName) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    ReactCurrentOwner.current == null,
	    '%s(...): Cannot update during an existing state transition ' +
	    '(such as within `render`). Render methods should be a pure function ' +
	    'of props and state.',
	    callerName
	  ) : invariant(ReactCurrentOwner.current == null));

	  var internalInstance = ReactInstanceMap.get(publicInstance);
	  if (!internalInstance) {
	    if ("production" !== process.env.NODE_ENV) {
	      // Only warn when we have a callerName. Otherwise we should be silent.
	      // We're probably calling from enqueueCallback. We don't want to warn
	      // there because we already warned for the corresponding lifecycle method.
	      ("production" !== process.env.NODE_ENV ? warning(
	        !callerName,
	        '%s(...): Can only update a mounted or mounting component. ' +
	        'This usually means you called %s() on an unmounted ' +
	        'component. This is a no-op.',
	        callerName,
	        callerName
	      ) : null);
	    }
	    return null;
	  }

	  if (internalInstance === ReactLifeCycle.currentlyUnmountingInstance) {
	    return null;
	  }

	  return internalInstance;
	}

	/**
	 * ReactUpdateQueue allows for state updates to be scheduled into a later
	 * reconciliation step.
	 */
	var ReactUpdateQueue = {

	  /**
	   * Enqueue a callback that will be executed after all the pending updates
	   * have processed.
	   *
	   * @param {ReactClass} publicInstance The instance to use as `this` context.
	   * @param {?function} callback Called after state is updated.
	   * @internal
	   */
	  enqueueCallback: function(publicInstance, callback) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      typeof callback === 'function',
	      'enqueueCallback(...): You called `setProps`, `replaceProps`, ' +
	      '`setState`, `replaceState`, or `forceUpdate` with a callback that ' +
	      'isn\'t callable.'
	    ) : invariant(typeof callback === 'function'));
	    var internalInstance = getInternalInstanceReadyForUpdate(publicInstance);

	    // Previously we would throw an error if we didn't have an internal
	    // instance. Since we want to make it a no-op instead, we mirror the same
	    // behavior we have in other enqueue* methods.
	    // We also need to ignore callbacks in componentWillMount. See
	    // enqueueUpdates.
	    if (!internalInstance ||
	        internalInstance === ReactLifeCycle.currentlyMountingInstance) {
	      return null;
	    }

	    if (internalInstance._pendingCallbacks) {
	      internalInstance._pendingCallbacks.push(callback);
	    } else {
	      internalInstance._pendingCallbacks = [callback];
	    }
	    // TODO: The callback here is ignored when setState is called from
	    // componentWillMount. Either fix it or disallow doing so completely in
	    // favor of getInitialState. Alternatively, we can disallow
	    // componentWillMount during server-side rendering.
	    enqueueUpdate(internalInstance);
	  },

	  enqueueCallbackInternal: function(internalInstance, callback) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      typeof callback === 'function',
	      'enqueueCallback(...): You called `setProps`, `replaceProps`, ' +
	      '`setState`, `replaceState`, or `forceUpdate` with a callback that ' +
	      'isn\'t callable.'
	    ) : invariant(typeof callback === 'function'));
	    if (internalInstance._pendingCallbacks) {
	      internalInstance._pendingCallbacks.push(callback);
	    } else {
	      internalInstance._pendingCallbacks = [callback];
	    }
	    enqueueUpdate(internalInstance);
	  },

	  /**
	   * Forces an update. This should only be invoked when it is known with
	   * certainty that we are **not** in a DOM transaction.
	   *
	   * You may want to call this when you know that some deeper aspect of the
	   * component's state has changed but `setState` was not called.
	   *
	   * This will not invoke `shouldUpdateComponent`, but it will invoke
	   * `componentWillUpdate` and `componentDidUpdate`.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @internal
	   */
	  enqueueForceUpdate: function(publicInstance) {
	    var internalInstance = getInternalInstanceReadyForUpdate(
	      publicInstance,
	      'forceUpdate'
	    );

	    if (!internalInstance) {
	      return;
	    }

	    internalInstance._pendingForceUpdate = true;

	    enqueueUpdate(internalInstance);
	  },

	  /**
	   * Replaces all of the state. Always use this or `setState` to mutate state.
	   * You should treat `this.state` as immutable.
	   *
	   * There is no guarantee that `this.state` will be immediately updated, so
	   * accessing `this.state` after calling this method may return the old value.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @param {object} completeState Next state.
	   * @internal
	   */
	  enqueueReplaceState: function(publicInstance, completeState) {
	    var internalInstance = getInternalInstanceReadyForUpdate(
	      publicInstance,
	      'replaceState'
	    );

	    if (!internalInstance) {
	      return;
	    }

	    internalInstance._pendingStateQueue = [completeState];
	    internalInstance._pendingReplaceState = true;

	    enqueueUpdate(internalInstance);
	  },

	  /**
	   * Sets a subset of the state. This only exists because _pendingState is
	   * internal. This provides a merging strategy that is not available to deep
	   * properties which is confusing. TODO: Expose pendingState or don't use it
	   * during the merge.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @param {object} partialState Next partial state to be merged with state.
	   * @internal
	   */
	  enqueueSetState: function(publicInstance, partialState) {
	    var internalInstance = getInternalInstanceReadyForUpdate(
	      publicInstance,
	      'setState'
	    );

	    if (!internalInstance) {
	      return;
	    }

	    var queue =
	      internalInstance._pendingStateQueue ||
	      (internalInstance._pendingStateQueue = []);
	    queue.push(partialState);

	    enqueueUpdate(internalInstance);
	  },

	  /**
	   * Sets a subset of the props.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @param {object} partialProps Subset of the next props.
	   * @internal
	   */
	  enqueueSetProps: function(publicInstance, partialProps) {
	    var internalInstance = getInternalInstanceReadyForUpdate(
	      publicInstance,
	      'setProps'
	    );

	    if (!internalInstance) {
	      return;
	    }

	    ("production" !== process.env.NODE_ENV ? invariant(
	      internalInstance._isTopLevel,
	      'setProps(...): You called `setProps` on a ' +
	      'component with a parent. This is an anti-pattern since props will ' +
	      'get reactively updated when rendered. Instead, change the owner\'s ' +
	      '`render` method to pass the correct value as props to the component ' +
	      'where it is created.'
	    ) : invariant(internalInstance._isTopLevel));

	    // Merge with the pending element if it exists, otherwise with existing
	    // element props.
	    var element = internalInstance._pendingElement ||
	                  internalInstance._currentElement;
	    var props = assign({}, element.props, partialProps);
	    internalInstance._pendingElement = ReactElement.cloneAndReplaceProps(
	      element,
	      props
	    );

	    enqueueUpdate(internalInstance);
	  },

	  /**
	   * Replaces all of the props.
	   *
	   * @param {ReactClass} publicInstance The instance that should rerender.
	   * @param {object} props New props.
	   * @internal
	   */
	  enqueueReplaceProps: function(publicInstance, props) {
	    var internalInstance = getInternalInstanceReadyForUpdate(
	      publicInstance,
	      'replaceProps'
	    );

	    if (!internalInstance) {
	      return;
	    }

	    ("production" !== process.env.NODE_ENV ? invariant(
	      internalInstance._isTopLevel,
	      'replaceProps(...): You called `replaceProps` on a ' +
	      'component with a parent. This is an anti-pattern since props will ' +
	      'get reactively updated when rendered. Instead, change the owner\'s ' +
	      '`render` method to pass the correct value as props to the component ' +
	      'where it is created.'
	    ) : invariant(internalInstance._isTopLevel));

	    // Merge with the pending element if it exists, otherwise with existing
	    // element props.
	    var element = internalInstance._pendingElement ||
	                  internalInstance._currentElement;
	    internalInstance._pendingElement = ReactElement.cloneAndReplaceProps(
	      element,
	      props
	    );

	    enqueueUpdate(internalInstance);
	  },

	  enqueueElementInternal: function(internalInstance, newElement) {
	    internalInstance._pendingElement = newElement;
	    enqueueUpdate(internalInstance);
	  }

	};

	module.exports = ReactUpdateQueue;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 25 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactUpdates
	 */

	'use strict';

	var CallbackQueue = __webpack_require__(26);
	var PooledClass = __webpack_require__(10);
	var ReactCurrentOwner = __webpack_require__(18);
	var ReactPerf = __webpack_require__(27);
	var ReactReconciler = __webpack_require__(28);
	var Transaction = __webpack_require__(35);

	var assign = __webpack_require__(14);
	var invariant = __webpack_require__(8);
	var warning = __webpack_require__(16);

	var dirtyComponents = [];
	var asapCallbackQueue = CallbackQueue.getPooled();
	var asapEnqueued = false;

	var batchingStrategy = null;

	function ensureInjected() {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    ReactUpdates.ReactReconcileTransaction && batchingStrategy,
	    'ReactUpdates: must inject a reconcile transaction class and batching ' +
	    'strategy'
	  ) : invariant(ReactUpdates.ReactReconcileTransaction && batchingStrategy));
	}

	var NESTED_UPDATES = {
	  initialize: function() {
	    this.dirtyComponentsLength = dirtyComponents.length;
	  },
	  close: function() {
	    if (this.dirtyComponentsLength !== dirtyComponents.length) {
	      // Additional updates were enqueued by componentDidUpdate handlers or
	      // similar; before our own UPDATE_QUEUEING wrapper closes, we want to run
	      // these new updates so that if A's componentDidUpdate calls setState on
	      // B, B will update before the callback A's updater provided when calling
	      // setState.
	      dirtyComponents.splice(0, this.dirtyComponentsLength);
	      flushBatchedUpdates();
	    } else {
	      dirtyComponents.length = 0;
	    }
	  }
	};

	var UPDATE_QUEUEING = {
	  initialize: function() {
	    this.callbackQueue.reset();
	  },
	  close: function() {
	    this.callbackQueue.notifyAll();
	  }
	};

	var TRANSACTION_WRAPPERS = [NESTED_UPDATES, UPDATE_QUEUEING];

	function ReactUpdatesFlushTransaction() {
	  this.reinitializeTransaction();
	  this.dirtyComponentsLength = null;
	  this.callbackQueue = CallbackQueue.getPooled();
	  this.reconcileTransaction =
	    ReactUpdates.ReactReconcileTransaction.getPooled();
	}

	assign(
	  ReactUpdatesFlushTransaction.prototype,
	  Transaction.Mixin, {
	  getTransactionWrappers: function() {
	    return TRANSACTION_WRAPPERS;
	  },

	  destructor: function() {
	    this.dirtyComponentsLength = null;
	    CallbackQueue.release(this.callbackQueue);
	    this.callbackQueue = null;
	    ReactUpdates.ReactReconcileTransaction.release(this.reconcileTransaction);
	    this.reconcileTransaction = null;
	  },

	  perform: function(method, scope, a) {
	    // Essentially calls `this.reconcileTransaction.perform(method, scope, a)`
	    // with this transaction's wrappers around it.
	    return Transaction.Mixin.perform.call(
	      this,
	      this.reconcileTransaction.perform,
	      this.reconcileTransaction,
	      method,
	      scope,
	      a
	    );
	  }
	});

	PooledClass.addPoolingTo(ReactUpdatesFlushTransaction);

	function batchedUpdates(callback, a, b, c, d) {
	  ensureInjected();
	  batchingStrategy.batchedUpdates(callback, a, b, c, d);
	}

	/**
	 * Array comparator for ReactComponents by mount ordering.
	 *
	 * @param {ReactComponent} c1 first component you're comparing
	 * @param {ReactComponent} c2 second component you're comparing
	 * @return {number} Return value usable by Array.prototype.sort().
	 */
	function mountOrderComparator(c1, c2) {
	  return c1._mountOrder - c2._mountOrder;
	}

	function runBatchedUpdates(transaction) {
	  var len = transaction.dirtyComponentsLength;
	  ("production" !== process.env.NODE_ENV ? invariant(
	    len === dirtyComponents.length,
	    'Expected flush transaction\'s stored dirty-components length (%s) to ' +
	    'match dirty-components array length (%s).',
	    len,
	    dirtyComponents.length
	  ) : invariant(len === dirtyComponents.length));

	  // Since reconciling a component higher in the owner hierarchy usually (not
	  // always -- see shouldComponentUpdate()) will reconcile children, reconcile
	  // them before their children by sorting the array.
	  dirtyComponents.sort(mountOrderComparator);

	  for (var i = 0; i < len; i++) {
	    // If a component is unmounted before pending changes apply, it will still
	    // be here, but we assume that it has cleared its _pendingCallbacks and
	    // that performUpdateIfNecessary is a noop.
	    var component = dirtyComponents[i];

	    // If performUpdateIfNecessary happens to enqueue any new updates, we
	    // shouldn't execute the callbacks until the next render happens, so
	    // stash the callbacks first
	    var callbacks = component._pendingCallbacks;
	    component._pendingCallbacks = null;

	    ReactReconciler.performUpdateIfNecessary(
	      component,
	      transaction.reconcileTransaction
	    );

	    if (callbacks) {
	      for (var j = 0; j < callbacks.length; j++) {
	        transaction.callbackQueue.enqueue(
	          callbacks[j],
	          component.getPublicInstance()
	        );
	      }
	    }
	  }
	}

	var flushBatchedUpdates = function() {
	  // ReactUpdatesFlushTransaction's wrappers will clear the dirtyComponents
	  // array and perform any updates enqueued by mount-ready handlers (i.e.,
	  // componentDidUpdate) but we need to check here too in order to catch
	  // updates enqueued by setState callbacks and asap calls.
	  while (dirtyComponents.length || asapEnqueued) {
	    if (dirtyComponents.length) {
	      var transaction = ReactUpdatesFlushTransaction.getPooled();
	      transaction.perform(runBatchedUpdates, null, transaction);
	      ReactUpdatesFlushTransaction.release(transaction);
	    }

	    if (asapEnqueued) {
	      asapEnqueued = false;
	      var queue = asapCallbackQueue;
	      asapCallbackQueue = CallbackQueue.getPooled();
	      queue.notifyAll();
	      CallbackQueue.release(queue);
	    }
	  }
	};
	flushBatchedUpdates = ReactPerf.measure(
	  'ReactUpdates',
	  'flushBatchedUpdates',
	  flushBatchedUpdates
	);

	/**
	 * Mark a component as needing a rerender, adding an optional callback to a
	 * list of functions which will be executed once the rerender occurs.
	 */
	function enqueueUpdate(component) {
	  ensureInjected();

	  // Various parts of our code (such as ReactCompositeComponent's
	  // _renderValidatedComponent) assume that calls to render aren't nested;
	  // verify that that's the case. (This is called by each top-level update
	  // function, like setProps, setState, forceUpdate, etc.; creation and
	  // destruction of top-level components is guarded in ReactMount.)
	  ("production" !== process.env.NODE_ENV ? warning(
	    ReactCurrentOwner.current == null,
	    'enqueueUpdate(): Render methods should be a pure function of props ' +
	    'and state; triggering nested component updates from render is not ' +
	    'allowed. If necessary, trigger nested updates in ' +
	    'componentDidUpdate.'
	  ) : null);

	  if (!batchingStrategy.isBatchingUpdates) {
	    batchingStrategy.batchedUpdates(enqueueUpdate, component);
	    return;
	  }

	  dirtyComponents.push(component);
	}

	/**
	 * Enqueue a callback to be run at the end of the current batching cycle. Throws
	 * if no updates are currently being performed.
	 */
	function asap(callback, context) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    batchingStrategy.isBatchingUpdates,
	    'ReactUpdates.asap: Can\'t enqueue an asap callback in a context where' +
	    'updates are not being batched.'
	  ) : invariant(batchingStrategy.isBatchingUpdates));
	  asapCallbackQueue.enqueue(callback, context);
	  asapEnqueued = true;
	}

	var ReactUpdatesInjection = {
	  injectReconcileTransaction: function(ReconcileTransaction) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      ReconcileTransaction,
	      'ReactUpdates: must provide a reconcile transaction class'
	    ) : invariant(ReconcileTransaction));
	    ReactUpdates.ReactReconcileTransaction = ReconcileTransaction;
	  },

	  injectBatchingStrategy: function(_batchingStrategy) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      _batchingStrategy,
	      'ReactUpdates: must provide a batching strategy'
	    ) : invariant(_batchingStrategy));
	    ("production" !== process.env.NODE_ENV ? invariant(
	      typeof _batchingStrategy.batchedUpdates === 'function',
	      'ReactUpdates: must provide a batchedUpdates() function'
	    ) : invariant(typeof _batchingStrategy.batchedUpdates === 'function'));
	    ("production" !== process.env.NODE_ENV ? invariant(
	      typeof _batchingStrategy.isBatchingUpdates === 'boolean',
	      'ReactUpdates: must provide an isBatchingUpdates boolean attribute'
	    ) : invariant(typeof _batchingStrategy.isBatchingUpdates === 'boolean'));
	    batchingStrategy = _batchingStrategy;
	  }
	};

	var ReactUpdates = {
	  /**
	   * React references `ReactReconcileTransaction` using this property in order
	   * to allow dependency injection.
	   *
	   * @internal
	   */
	  ReactReconcileTransaction: null,

	  batchedUpdates: batchedUpdates,
	  enqueueUpdate: enqueueUpdate,
	  flushBatchedUpdates: flushBatchedUpdates,
	  injection: ReactUpdatesInjection,
	  asap: asap
	};

	module.exports = ReactUpdates;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 26 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule CallbackQueue
	 */

	'use strict';

	var PooledClass = __webpack_require__(10);

	var assign = __webpack_require__(14);
	var invariant = __webpack_require__(8);

	/**
	 * A specialized pseudo-event module to help keep track of components waiting to
	 * be notified when their DOM representations are available for use.
	 *
	 * This implements `PooledClass`, so you should never need to instantiate this.
	 * Instead, use `CallbackQueue.getPooled()`.
	 *
	 * @class ReactMountReady
	 * @implements PooledClass
	 * @internal
	 */
	function CallbackQueue() {
	  this._callbacks = null;
	  this._contexts = null;
	}

	assign(CallbackQueue.prototype, {

	  /**
	   * Enqueues a callback to be invoked when `notifyAll` is invoked.
	   *
	   * @param {function} callback Invoked when `notifyAll` is invoked.
	   * @param {?object} context Context to call `callback` with.
	   * @internal
	   */
	  enqueue: function(callback, context) {
	    this._callbacks = this._callbacks || [];
	    this._contexts = this._contexts || [];
	    this._callbacks.push(callback);
	    this._contexts.push(context);
	  },

	  /**
	   * Invokes all enqueued callbacks and clears the queue. This is invoked after
	   * the DOM representation of a component has been created or updated.
	   *
	   * @internal
	   */
	  notifyAll: function() {
	    var callbacks = this._callbacks;
	    var contexts = this._contexts;
	    if (callbacks) {
	      ("production" !== process.env.NODE_ENV ? invariant(
	        callbacks.length === contexts.length,
	        'Mismatched list of contexts in callback queue'
	      ) : invariant(callbacks.length === contexts.length));
	      this._callbacks = null;
	      this._contexts = null;
	      for (var i = 0, l = callbacks.length; i < l; i++) {
	        callbacks[i].call(contexts[i]);
	      }
	      callbacks.length = 0;
	      contexts.length = 0;
	    }
	  },

	  /**
	   * Resets the internal queue.
	   *
	   * @internal
	   */
	  reset: function() {
	    this._callbacks = null;
	    this._contexts = null;
	  },

	  /**
	   * `PooledClass` looks for this.
	   */
	  destructor: function() {
	    this.reset();
	  }

	});

	PooledClass.addPoolingTo(CallbackQueue);

	module.exports = CallbackQueue;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 27 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactPerf
	 * @typechecks static-only
	 */

	'use strict';

	/**
	 * ReactPerf is a general AOP system designed to measure performance. This
	 * module only has the hooks: see ReactDefaultPerf for the analysis tool.
	 */
	var ReactPerf = {
	  /**
	   * Boolean to enable/disable measurement. Set to false by default to prevent
	   * accidental logging and perf loss.
	   */
	  enableMeasure: false,

	  /**
	   * Holds onto the measure function in use. By default, don't measure
	   * anything, but we'll override this if we inject a measure function.
	   */
	  storedMeasure: _noMeasure,

	  /**
	   * @param {object} object
	   * @param {string} objectName
	   * @param {object<string>} methodNames
	   */
	  measureMethods: function(object, objectName, methodNames) {
	    if ("production" !== process.env.NODE_ENV) {
	      for (var key in methodNames) {
	        if (!methodNames.hasOwnProperty(key)) {
	          continue;
	        }
	        object[key] = ReactPerf.measure(
	          objectName,
	          methodNames[key],
	          object[key]
	        );
	      }
	    }
	  },

	  /**
	   * Use this to wrap methods you want to measure. Zero overhead in production.
	   *
	   * @param {string} objName
	   * @param {string} fnName
	   * @param {function} func
	   * @return {function}
	   */
	  measure: function(objName, fnName, func) {
	    if ("production" !== process.env.NODE_ENV) {
	      var measuredFunc = null;
	      var wrapper = function() {
	        if (ReactPerf.enableMeasure) {
	          if (!measuredFunc) {
	            measuredFunc = ReactPerf.storedMeasure(objName, fnName, func);
	          }
	          return measuredFunc.apply(this, arguments);
	        }
	        return func.apply(this, arguments);
	      };
	      wrapper.displayName = objName + '_' + fnName;
	      return wrapper;
	    }
	    return func;
	  },

	  injection: {
	    /**
	     * @param {function} measure
	     */
	    injectMeasure: function(measure) {
	      ReactPerf.storedMeasure = measure;
	    }
	  }
	};

	/**
	 * Simply passes through the measured function, without measuring it.
	 *
	 * @param {string} objName
	 * @param {string} fnName
	 * @param {function} func
	 * @return {function}
	 */
	function _noMeasure(objName, fnName, func) {
	  return func;
	}

	module.exports = ReactPerf;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 28 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactReconciler
	 */

	'use strict';

	var ReactRef = __webpack_require__(29);
	var ReactElementValidator = __webpack_require__(31);

	/**
	 * Helper to call ReactRef.attachRefs with this composite component, split out
	 * to avoid allocations in the transaction mount-ready queue.
	 */
	function attachRefs() {
	  ReactRef.attachRefs(this, this._currentElement);
	}

	var ReactReconciler = {

	  /**
	   * Initializes the component, renders markup, and registers event listeners.
	   *
	   * @param {ReactComponent} internalInstance
	   * @param {string} rootID DOM ID of the root node.
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @return {?string} Rendered markup to be inserted into the DOM.
	   * @final
	   * @internal
	   */
	  mountComponent: function(internalInstance, rootID, transaction, context) {
	    var markup = internalInstance.mountComponent(rootID, transaction, context);
	    if ("production" !== process.env.NODE_ENV) {
	      ReactElementValidator.checkAndWarnForMutatedProps(
	        internalInstance._currentElement
	      );
	    }
	    transaction.getReactMountReady().enqueue(attachRefs, internalInstance);
	    return markup;
	  },

	  /**
	   * Releases any resources allocated by `mountComponent`.
	   *
	   * @final
	   * @internal
	   */
	  unmountComponent: function(internalInstance) {
	    ReactRef.detachRefs(internalInstance, internalInstance._currentElement);
	    internalInstance.unmountComponent();
	  },

	  /**
	   * Update a component using a new element.
	   *
	   * @param {ReactComponent} internalInstance
	   * @param {ReactElement} nextElement
	   * @param {ReactReconcileTransaction} transaction
	   * @param {object} context
	   * @internal
	   */
	  receiveComponent: function(
	    internalInstance, nextElement, transaction, context
	  ) {
	    var prevElement = internalInstance._currentElement;

	    if (nextElement === prevElement && nextElement._owner != null) {
	      // Since elements are immutable after the owner is rendered,
	      // we can do a cheap identity compare here to determine if this is a
	      // superfluous reconcile. It's possible for state to be mutable but such
	      // change should trigger an update of the owner which would recreate
	      // the element. We explicitly check for the existence of an owner since
	      // it's possible for an element created outside a composite to be
	      // deeply mutated and reused.
	      return;
	    }

	    if ("production" !== process.env.NODE_ENV) {
	      ReactElementValidator.checkAndWarnForMutatedProps(nextElement);
	    }

	    var refsChanged = ReactRef.shouldUpdateRefs(
	      prevElement,
	      nextElement
	    );

	    if (refsChanged) {
	      ReactRef.detachRefs(internalInstance, prevElement);
	    }

	    internalInstance.receiveComponent(nextElement, transaction, context);

	    if (refsChanged) {
	      transaction.getReactMountReady().enqueue(attachRefs, internalInstance);
	    }
	  },

	  /**
	   * Flush any dirty changes in a component.
	   *
	   * @param {ReactComponent} internalInstance
	   * @param {ReactReconcileTransaction} transaction
	   * @internal
	   */
	  performUpdateIfNecessary: function(
	    internalInstance,
	    transaction
	  ) {
	    internalInstance.performUpdateIfNecessary(transaction);
	  }

	};

	module.exports = ReactReconciler;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 29 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactRef
	 */

	'use strict';

	var ReactOwner = __webpack_require__(30);

	var ReactRef = {};

	function attachRef(ref, component, owner) {
	  if (typeof ref === 'function') {
	    ref(component.getPublicInstance());
	  } else {
	    // Legacy ref
	    ReactOwner.addComponentAsRefTo(component, ref, owner);
	  }
	}

	function detachRef(ref, component, owner) {
	  if (typeof ref === 'function') {
	    ref(null);
	  } else {
	    // Legacy ref
	    ReactOwner.removeComponentAsRefFrom(component, ref, owner);
	  }
	}

	ReactRef.attachRefs = function(instance, element) {
	  var ref = element.ref;
	  if (ref != null) {
	    attachRef(ref, instance, element._owner);
	  }
	};

	ReactRef.shouldUpdateRefs = function(prevElement, nextElement) {
	  // If either the owner or a `ref` has changed, make sure the newest owner
	  // has stored a reference to `this`, and the previous owner (if different)
	  // has forgotten the reference to `this`. We use the element instead
	  // of the public this.props because the post processing cannot determine
	  // a ref. The ref conceptually lives on the element.

	  // TODO: Should this even be possible? The owner cannot change because
	  // it's forbidden by shouldUpdateReactComponent. The ref can change
	  // if you swap the keys of but not the refs. Reconsider where this check
	  // is made. It probably belongs where the key checking and
	  // instantiateReactComponent is done.

	  return (
	    nextElement._owner !== prevElement._owner ||
	    nextElement.ref !== prevElement.ref
	  );
	};

	ReactRef.detachRefs = function(instance, element) {
	  var ref = element.ref;
	  if (ref != null) {
	    detachRef(ref, instance, element._owner);
	  }
	};

	module.exports = ReactRef;


/***/ },
/* 30 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactOwner
	 */

	'use strict';

	var invariant = __webpack_require__(8);

	/**
	 * ReactOwners are capable of storing references to owned components.
	 *
	 * All components are capable of //being// referenced by owner components, but
	 * only ReactOwner components are capable of //referencing// owned components.
	 * The named reference is known as a "ref".
	 *
	 * Refs are available when mounted and updated during reconciliation.
	 *
	 *   var MyComponent = React.createClass({
	 *     render: function() {
	 *       return (
	 *         <div onClick={this.handleClick}>
	 *           <CustomComponent ref="custom" />
	 *         </div>
	 *       );
	 *     },
	 *     handleClick: function() {
	 *       this.refs.custom.handleClick();
	 *     },
	 *     componentDidMount: function() {
	 *       this.refs.custom.initialize();
	 *     }
	 *   });
	 *
	 * Refs should rarely be used. When refs are used, they should only be done to
	 * control data that is not handled by React's data flow.
	 *
	 * @class ReactOwner
	 */
	var ReactOwner = {

	  /**
	   * @param {?object} object
	   * @return {boolean} True if `object` is a valid owner.
	   * @final
	   */
	  isValidOwner: function(object) {
	    return !!(
	      (object &&
	      typeof object.attachRef === 'function' && typeof object.detachRef === 'function')
	    );
	  },

	  /**
	   * Adds a component by ref to an owner component.
	   *
	   * @param {ReactComponent} component Component to reference.
	   * @param {string} ref Name by which to refer to the component.
	   * @param {ReactOwner} owner Component on which to record the ref.
	   * @final
	   * @internal
	   */
	  addComponentAsRefTo: function(component, ref, owner) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      ReactOwner.isValidOwner(owner),
	      'addComponentAsRefTo(...): Only a ReactOwner can have refs. This ' +
	      'usually means that you\'re trying to add a ref to a component that ' +
	      'doesn\'t have an owner (that is, was not created inside of another ' +
	      'component\'s `render` method). Try rendering this component inside of ' +
	      'a new top-level component which will hold the ref.'
	    ) : invariant(ReactOwner.isValidOwner(owner)));
	    owner.attachRef(ref, component);
	  },

	  /**
	   * Removes a component by ref from an owner component.
	   *
	   * @param {ReactComponent} component Component to dereference.
	   * @param {string} ref Name of the ref to remove.
	   * @param {ReactOwner} owner Component on which the ref is recorded.
	   * @final
	   * @internal
	   */
	  removeComponentAsRefFrom: function(component, ref, owner) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      ReactOwner.isValidOwner(owner),
	      'removeComponentAsRefFrom(...): Only a ReactOwner can have refs. This ' +
	      'usually means that you\'re trying to remove a ref to a component that ' +
	      'doesn\'t have an owner (that is, was not created inside of another ' +
	      'component\'s `render` method). Try rendering this component inside of ' +
	      'a new top-level component which will hold the ref.'
	    ) : invariant(ReactOwner.isValidOwner(owner)));
	    // Check that `component` is still the current ref because we do not want to
	    // detach the ref if another component stole it.
	    if (owner.getPublicInstance().refs[ref] === component.getPublicInstance()) {
	      owner.detachRef(ref);
	    }
	  }

	};

	module.exports = ReactOwner;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 31 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactElementValidator
	 */

	/**
	 * ReactElementValidator provides a wrapper around a element factory
	 * which validates the props passed to the element. This is intended to be
	 * used only in DEV and could be replaced by a static type checker for languages
	 * that support it.
	 */

	'use strict';

	var ReactElement = __webpack_require__(12);
	var ReactFragment = __webpack_require__(11);
	var ReactPropTypeLocations = __webpack_require__(32);
	var ReactPropTypeLocationNames = __webpack_require__(33);
	var ReactCurrentOwner = __webpack_require__(18);
	var ReactNativeComponent = __webpack_require__(34);

	var getIteratorFn = __webpack_require__(20);
	var invariant = __webpack_require__(8);
	var warning = __webpack_require__(16);

	function getDeclarationErrorAddendum() {
	  if (ReactCurrentOwner.current) {
	    var name = ReactCurrentOwner.current.getName();
	    if (name) {
	      return ' Check the render method of `' + name + '`.';
	    }
	  }
	  return '';
	}

	/**
	 * Warn if there's no key explicitly set on dynamic arrays of children or
	 * object keys are not valid. This allows us to keep track of children between
	 * updates.
	 */
	var ownerHasKeyUseWarning = {};

	var loggedTypeFailures = {};

	var NUMERIC_PROPERTY_REGEX = /^\d+$/;

	/**
	 * Gets the instance's name for use in warnings.
	 *
	 * @internal
	 * @return {?string} Display name or undefined
	 */
	function getName(instance) {
	  var publicInstance = instance && instance.getPublicInstance();
	  if (!publicInstance) {
	    return undefined;
	  }
	  var constructor = publicInstance.constructor;
	  if (!constructor) {
	    return undefined;
	  }
	  return constructor.displayName || constructor.name || undefined;
	}

	/**
	 * Gets the current owner's displayName for use in warnings.
	 *
	 * @internal
	 * @return {?string} Display name or undefined
	 */
	function getCurrentOwnerDisplayName() {
	  var current = ReactCurrentOwner.current;
	  return (
	    current && getName(current) || undefined
	  );
	}

	/**
	 * Warn if the element doesn't have an explicit key assigned to it.
	 * This element is in an array. The array could grow and shrink or be
	 * reordered. All children that haven't already been validated are required to
	 * have a "key" property assigned to it.
	 *
	 * @internal
	 * @param {ReactElement} element Element that requires a key.
	 * @param {*} parentType element's parent's type.
	 */
	function validateExplicitKey(element, parentType) {
	  if (element._store.validated || element.key != null) {
	    return;
	  }
	  element._store.validated = true;

	  warnAndMonitorForKeyUse(
	    'Each child in an array or iterator should have a unique "key" prop.',
	    element,
	    parentType
	  );
	}

	/**
	 * Warn if the key is being defined as an object property but has an incorrect
	 * value.
	 *
	 * @internal
	 * @param {string} name Property name of the key.
	 * @param {ReactElement} element Component that requires a key.
	 * @param {*} parentType element's parent's type.
	 */
	function validatePropertyKey(name, element, parentType) {
	  if (!NUMERIC_PROPERTY_REGEX.test(name)) {
	    return;
	  }
	  warnAndMonitorForKeyUse(
	    'Child objects should have non-numeric keys so ordering is preserved.',
	    element,
	    parentType
	  );
	}

	/**
	 * Shared warning and monitoring code for the key warnings.
	 *
	 * @internal
	 * @param {string} message The base warning that gets output.
	 * @param {ReactElement} element Component that requires a key.
	 * @param {*} parentType element's parent's type.
	 */
	function warnAndMonitorForKeyUse(message, element, parentType) {
	  var ownerName = getCurrentOwnerDisplayName();
	  var parentName = typeof parentType === 'string' ?
	    parentType : parentType.displayName || parentType.name;

	  var useName = ownerName || parentName;
	  var memoizer = ownerHasKeyUseWarning[message] || (
	    (ownerHasKeyUseWarning[message] = {})
	  );
	  if (memoizer.hasOwnProperty(useName)) {
	    return;
	  }
	  memoizer[useName] = true;

	  var parentOrOwnerAddendum =
	    ownerName ? (" Check the render method of " + ownerName + ".") :
	    parentName ? (" Check the React.render call using <" + parentName + ">.") :
	    '';

	  // Usually the current owner is the offender, but if it accepts children as a
	  // property, it may be the creator of the child that's responsible for
	  // assigning it a key.
	  var childOwnerAddendum = '';
	  if (element &&
	      element._owner &&
	      element._owner !== ReactCurrentOwner.current) {
	    // Name of the component that originally created this child.
	    var childOwnerName = getName(element._owner);

	    childOwnerAddendum = (" It was passed a child from " + childOwnerName + ".");
	  }

	  ("production" !== process.env.NODE_ENV ? warning(
	    false,
	    message + '%s%s See https://fb.me/react-warning-keys for more information.',
	    parentOrOwnerAddendum,
	    childOwnerAddendum
	  ) : null);
	}

	/**
	 * Ensure that every element either is passed in a static location, in an
	 * array with an explicit keys property defined, or in an object literal
	 * with valid key property.
	 *
	 * @internal
	 * @param {ReactNode} node Statically passed child of any type.
	 * @param {*} parentType node's parent's type.
	 */
	function validateChildKeys(node, parentType) {
	  if (Array.isArray(node)) {
	    for (var i = 0; i < node.length; i++) {
	      var child = node[i];
	      if (ReactElement.isValidElement(child)) {
	        validateExplicitKey(child, parentType);
	      }
	    }
	  } else if (ReactElement.isValidElement(node)) {
	    // This element was passed in a valid location.
	    node._store.validated = true;
	  } else if (node) {
	    var iteratorFn = getIteratorFn(node);
	    // Entry iterators provide implicit keys.
	    if (iteratorFn) {
	      if (iteratorFn !== node.entries) {
	        var iterator = iteratorFn.call(node);
	        var step;
	        while (!(step = iterator.next()).done) {
	          if (ReactElement.isValidElement(step.value)) {
	            validateExplicitKey(step.value, parentType);
	          }
	        }
	      }
	    } else if (typeof node === 'object') {
	      var fragment = ReactFragment.extractIfFragment(node);
	      for (var key in fragment) {
	        if (fragment.hasOwnProperty(key)) {
	          validatePropertyKey(key, fragment[key], parentType);
	        }
	      }
	    }
	  }
	}

	/**
	 * Assert that the props are valid
	 *
	 * @param {string} componentName Name of the component for error messages.
	 * @param {object} propTypes Map of prop name to a ReactPropType
	 * @param {object} props
	 * @param {string} location e.g. "prop", "context", "child context"
	 * @private
	 */
	function checkPropTypes(componentName, propTypes, props, location) {
	  for (var propName in propTypes) {
	    if (propTypes.hasOwnProperty(propName)) {
	      var error;
	      // Prop type validation may throw. In case they do, we don't want to
	      // fail the render phase where it didn't fail before. So we log it.
	      // After these have been cleaned up, we'll let them throw.
	      try {
	        // This is intentionally an invariant that gets caught. It's the same
	        // behavior as without this statement except with a better message.
	        ("production" !== process.env.NODE_ENV ? invariant(
	          typeof propTypes[propName] === 'function',
	          '%s: %s type `%s` is invalid; it must be a function, usually from ' +
	          'React.PropTypes.',
	          componentName || 'React class',
	          ReactPropTypeLocationNames[location],
	          propName
	        ) : invariant(typeof propTypes[propName] === 'function'));
	        error = propTypes[propName](props, propName, componentName, location);
	      } catch (ex) {
	        error = ex;
	      }
	      if (error instanceof Error && !(error.message in loggedTypeFailures)) {
	        // Only monitor this failure once because there tends to be a lot of the
	        // same error.
	        loggedTypeFailures[error.message] = true;

	        var addendum = getDeclarationErrorAddendum(this);
	        ("production" !== process.env.NODE_ENV ? warning(false, 'Failed propType: %s%s', error.message, addendum) : null);
	      }
	    }
	  }
	}

	var warnedPropsMutations = {};

	/**
	 * Warn about mutating props when setting `propName` on `element`.
	 *
	 * @param {string} propName The string key within props that was set
	 * @param {ReactElement} element
	 */
	function warnForPropsMutation(propName, element) {
	  var type = element.type;
	  var elementName = typeof type === 'string' ? type : type.displayName;
	  var ownerName = element._owner ?
	    element._owner.getPublicInstance().constructor.displayName : null;

	  var warningKey = propName + '|' + elementName + '|' + ownerName;
	  if (warnedPropsMutations.hasOwnProperty(warningKey)) {
	    return;
	  }
	  warnedPropsMutations[warningKey] = true;

	  var elementInfo = '';
	  if (elementName) {
	    elementInfo = ' <' + elementName + ' />';
	  }
	  var ownerInfo = '';
	  if (ownerName) {
	    ownerInfo = ' The element was created by ' + ownerName + '.';
	  }

	  ("production" !== process.env.NODE_ENV ? warning(
	    false,
	    'Don\'t set .props.%s of the React component%s. Instead, specify the ' +
	    'correct value when initially creating the element or use ' +
	    'React.cloneElement to make a new element with updated props.%s',
	    propName,
	    elementInfo,
	    ownerInfo
	  ) : null);
	}

	// Inline Object.is polyfill
	function is(a, b) {
	  if (a !== a) {
	    // NaN
	    return b !== b;
	  }
	  if (a === 0 && b === 0) {
	    // +-0
	    return 1 / a === 1 / b;
	  }
	  return a === b;
	}

	/**
	 * Given an element, check if its props have been mutated since element
	 * creation (or the last call to this function). In particular, check if any
	 * new props have been added, which we can't directly catch by defining warning
	 * properties on the props object.
	 *
	 * @param {ReactElement} element
	 */
	function checkAndWarnForMutatedProps(element) {
	  if (!element._store) {
	    // Element was created using `new ReactElement` directly or with
	    // `ReactElement.createElement`; skip mutation checking
	    return;
	  }

	  var originalProps = element._store.originalProps;
	  var props = element.props;

	  for (var propName in props) {
	    if (props.hasOwnProperty(propName)) {
	      if (!originalProps.hasOwnProperty(propName) ||
	          !is(originalProps[propName], props[propName])) {
	        warnForPropsMutation(propName, element);

	        // Copy over the new value so that the two props objects match again
	        originalProps[propName] = props[propName];
	      }
	    }
	  }
	}

	/**
	 * Given an element, validate that its props follow the propTypes definition,
	 * provided by the type.
	 *
	 * @param {ReactElement} element
	 */
	function validatePropTypes(element) {
	  if (element.type == null) {
	    // This has already warned. Don't throw.
	    return;
	  }
	  // Extract the component class from the element. Converts string types
	  // to a composite class which may have propTypes.
	  // TODO: Validating a string's propTypes is not decoupled from the
	  // rendering target which is problematic.
	  var componentClass = ReactNativeComponent.getComponentClassForElement(
	    element
	  );
	  var name = componentClass.displayName || componentClass.name;
	  if (componentClass.propTypes) {
	    checkPropTypes(
	      name,
	      componentClass.propTypes,
	      element.props,
	      ReactPropTypeLocations.prop
	    );
	  }
	  if (typeof componentClass.getDefaultProps === 'function') {
	    ("production" !== process.env.NODE_ENV ? warning(
	      componentClass.getDefaultProps.isReactClassApproved,
	      'getDefaultProps is only used on classic React.createClass ' +
	      'definitions. Use a static property named `defaultProps` instead.'
	    ) : null);
	  }
	}

	var ReactElementValidator = {

	  checkAndWarnForMutatedProps: checkAndWarnForMutatedProps,

	  createElement: function(type, props, children) {
	    // We warn in this case but don't throw. We expect the element creation to
	    // succeed and there will likely be errors in render.
	    ("production" !== process.env.NODE_ENV ? warning(
	      type != null,
	      'React.createElement: type should not be null or undefined. It should ' +
	        'be a string (for DOM elements) or a ReactClass (for composite ' +
	        'components).'
	    ) : null);

	    var element = ReactElement.createElement.apply(this, arguments);

	    // The result can be nullish if a mock or a custom function is used.
	    // TODO: Drop this when these are no longer allowed as the type argument.
	    if (element == null) {
	      return element;
	    }

	    for (var i = 2; i < arguments.length; i++) {
	      validateChildKeys(arguments[i], type);
	    }

	    validatePropTypes(element);

	    return element;
	  },

	  createFactory: function(type) {
	    var validatedFactory = ReactElementValidator.createElement.bind(
	      null,
	      type
	    );
	    // Legacy hook TODO: Warn if this is accessed
	    validatedFactory.type = type;

	    if ("production" !== process.env.NODE_ENV) {
	      try {
	        Object.defineProperty(
	          validatedFactory,
	          'type',
	          {
	            enumerable: false,
	            get: function() {
	              ("production" !== process.env.NODE_ENV ? warning(
	                false,
	                'Factory.type is deprecated. Access the class directly ' +
	                'before passing it to createFactory.'
	              ) : null);
	              Object.defineProperty(this, 'type', {
	                value: type
	              });
	              return type;
	            }
	          }
	        );
	      } catch (x) {
	        // IE will fail on defineProperty (es5-shim/sham too)
	      }
	    }


	    return validatedFactory;
	  },

	  cloneElement: function(element, props, children) {
	    var newElement = ReactElement.cloneElement.apply(this, arguments);
	    for (var i = 2; i < arguments.length; i++) {
	      validateChildKeys(arguments[i], newElement.type);
	    }
	    validatePropTypes(newElement);
	    return newElement;
	  }

	};

	module.exports = ReactElementValidator;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 32 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactPropTypeLocations
	 */

	'use strict';

	var keyMirror = __webpack_require__(7);

	var ReactPropTypeLocations = keyMirror({
	  prop: null,
	  context: null,
	  childContext: null
	});

	module.exports = ReactPropTypeLocations;


/***/ },
/* 33 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactPropTypeLocationNames
	 */

	'use strict';

	var ReactPropTypeLocationNames = {};

	if ("production" !== process.env.NODE_ENV) {
	  ReactPropTypeLocationNames = {
	    prop: 'prop',
	    context: 'context',
	    childContext: 'child context'
	  };
	}

	module.exports = ReactPropTypeLocationNames;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 34 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactNativeComponent
	 */

	'use strict';

	var assign = __webpack_require__(14);
	var invariant = __webpack_require__(8);

	var autoGenerateWrapperClass = null;
	var genericComponentClass = null;
	// This registry keeps track of wrapper classes around native tags
	var tagToComponentClass = {};
	var textComponentClass = null;

	var ReactNativeComponentInjection = {
	  // This accepts a class that receives the tag string. This is a catch all
	  // that can render any kind of tag.
	  injectGenericComponentClass: function(componentClass) {
	    genericComponentClass = componentClass;
	  },
	  // This accepts a text component class that takes the text string to be
	  // rendered as props.
	  injectTextComponentClass: function(componentClass) {
	    textComponentClass = componentClass;
	  },
	  // This accepts a keyed object with classes as values. Each key represents a
	  // tag. That particular tag will use this class instead of the generic one.
	  injectComponentClasses: function(componentClasses) {
	    assign(tagToComponentClass, componentClasses);
	  },
	  // Temporary hack since we expect DOM refs to behave like composites,
	  // for this release.
	  injectAutoWrapper: function(wrapperFactory) {
	    autoGenerateWrapperClass = wrapperFactory;
	  }
	};

	/**
	 * Get a composite component wrapper class for a specific tag.
	 *
	 * @param {ReactElement} element The tag for which to get the class.
	 * @return {function} The React class constructor function.
	 */
	function getComponentClassForElement(element) {
	  if (typeof element.type === 'function') {
	    return element.type;
	  }
	  var tag = element.type;
	  var componentClass = tagToComponentClass[tag];
	  if (componentClass == null) {
	    tagToComponentClass[tag] = componentClass = autoGenerateWrapperClass(tag);
	  }
	  return componentClass;
	}

	/**
	 * Get a native internal component class for a specific tag.
	 *
	 * @param {ReactElement} element The element to create.
	 * @return {function} The internal class constructor function.
	 */
	function createInternalComponent(element) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    genericComponentClass,
	    'There is no registered component for the tag %s',
	    element.type
	  ) : invariant(genericComponentClass));
	  return new genericComponentClass(element.type, element.props);
	}

	/**
	 * @param {ReactText} text
	 * @return {ReactComponent}
	 */
	function createInstanceForText(text) {
	  return new textComponentClass(text);
	}

	/**
	 * @param {ReactComponent} component
	 * @return {boolean}
	 */
	function isTextComponent(component) {
	  return component instanceof textComponentClass;
	}

	var ReactNativeComponent = {
	  getComponentClassForElement: getComponentClassForElement,
	  createInternalComponent: createInternalComponent,
	  createInstanceForText: createInstanceForText,
	  isTextComponent: isTextComponent,
	  injection: ReactNativeComponentInjection
	};

	module.exports = ReactNativeComponent;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 35 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule Transaction
	 */

	'use strict';

	var invariant = __webpack_require__(8);

	/**
	 * `Transaction` creates a black box that is able to wrap any method such that
	 * certain invariants are maintained before and after the method is invoked
	 * (Even if an exception is thrown while invoking the wrapped method). Whoever
	 * instantiates a transaction can provide enforcers of the invariants at
	 * creation time. The `Transaction` class itself will supply one additional
	 * automatic invariant for you - the invariant that any transaction instance
	 * should not be run while it is already being run. You would typically create a
	 * single instance of a `Transaction` for reuse multiple times, that potentially
	 * is used to wrap several different methods. Wrappers are extremely simple -
	 * they only require implementing two methods.
	 *
	 * <pre>
	 *                       wrappers (injected at creation time)
	 *                                      +        +
	 *                                      |        |
	 *                    +-----------------|--------|--------------+
	 *                    |                 v        |              |
	 *                    |      +---------------+   |              |
	 *                    |   +--|    wrapper1   |---|----+         |
	 *                    |   |  +---------------+   v    |         |
	 *                    |   |          +-------------+  |         |
	 *                    |   |     +----|   wrapper2  |--------+   |
	 *                    |   |     |    +-------------+  |     |   |
	 *                    |   |     |                     |     |   |
	 *                    |   v     v                     v     v   | wrapper
	 *                    | +---+ +---+   +---------+   +---+ +---+ | invariants
	 * perform(anyMethod) | |   | |   |   |         |   |   | |   | | maintained
	 * +----------------->|-|---|-|---|-->|anyMethod|---|---|-|---|-|-------->
	 *                    | |   | |   |   |         |   |   | |   | |
	 *                    | |   | |   |   |         |   |   | |   | |
	 *                    | |   | |   |   |         |   |   | |   | |
	 *                    | +---+ +---+   +---------+   +---+ +---+ |
	 *                    |  initialize                    close    |
	 *                    +-----------------------------------------+
	 * </pre>
	 *
	 * Use cases:
	 * - Preserving the input selection ranges before/after reconciliation.
	 *   Restoring selection even in the event of an unexpected error.
	 * - Deactivating events while rearranging the DOM, preventing blurs/focuses,
	 *   while guaranteeing that afterwards, the event system is reactivated.
	 * - Flushing a queue of collected DOM mutations to the main UI thread after a
	 *   reconciliation takes place in a worker thread.
	 * - Invoking any collected `componentDidUpdate` callbacks after rendering new
	 *   content.
	 * - (Future use case): Wrapping particular flushes of the `ReactWorker` queue
	 *   to preserve the `scrollTop` (an automatic scroll aware DOM).
	 * - (Future use case): Layout calculations before and after DOM updates.
	 *
	 * Transactional plugin API:
	 * - A module that has an `initialize` method that returns any precomputation.
	 * - and a `close` method that accepts the precomputation. `close` is invoked
	 *   when the wrapped process is completed, or has failed.
	 *
	 * @param {Array<TransactionalWrapper>} transactionWrapper Wrapper modules
	 * that implement `initialize` and `close`.
	 * @return {Transaction} Single transaction for reuse in thread.
	 *
	 * @class Transaction
	 */
	var Mixin = {
	  /**
	   * Sets up this instance so that it is prepared for collecting metrics. Does
	   * so such that this setup method may be used on an instance that is already
	   * initialized, in a way that does not consume additional memory upon reuse.
	   * That can be useful if you decide to make your subclass of this mixin a
	   * "PooledClass".
	   */
	  reinitializeTransaction: function() {
	    this.transactionWrappers = this.getTransactionWrappers();
	    if (!this.wrapperInitData) {
	      this.wrapperInitData = [];
	    } else {
	      this.wrapperInitData.length = 0;
	    }
	    this._isInTransaction = false;
	  },

	  _isInTransaction: false,

	  /**
	   * @abstract
	   * @return {Array<TransactionWrapper>} Array of transaction wrappers.
	   */
	  getTransactionWrappers: null,

	  isInTransaction: function() {
	    return !!this._isInTransaction;
	  },

	  /**
	   * Executes the function within a safety window. Use this for the top level
	   * methods that result in large amounts of computation/mutations that would
	   * need to be safety checked.
	   *
	   * @param {function} method Member of scope to call.
	   * @param {Object} scope Scope to invoke from.
	   * @param {Object?=} args... Arguments to pass to the method (optional).
	   *                           Helps prevent need to bind in many cases.
	   * @return Return value from `method`.
	   */
	  perform: function(method, scope, a, b, c, d, e, f) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      !this.isInTransaction(),
	      'Transaction.perform(...): Cannot initialize a transaction when there ' +
	      'is already an outstanding transaction.'
	    ) : invariant(!this.isInTransaction()));
	    var errorThrown;
	    var ret;
	    try {
	      this._isInTransaction = true;
	      // Catching errors makes debugging more difficult, so we start with
	      // errorThrown set to true before setting it to false after calling
	      // close -- if it's still set to true in the finally block, it means
	      // one of these calls threw.
	      errorThrown = true;
	      this.initializeAll(0);
	      ret = method.call(scope, a, b, c, d, e, f);
	      errorThrown = false;
	    } finally {
	      try {
	        if (errorThrown) {
	          // If `method` throws, prefer to show that stack trace over any thrown
	          // by invoking `closeAll`.
	          try {
	            this.closeAll(0);
	          } catch (err) {
	          }
	        } else {
	          // Since `method` didn't throw, we don't want to silence the exception
	          // here.
	          this.closeAll(0);
	        }
	      } finally {
	        this._isInTransaction = false;
	      }
	    }
	    return ret;
	  },

	  initializeAll: function(startIndex) {
	    var transactionWrappers = this.transactionWrappers;
	    for (var i = startIndex; i < transactionWrappers.length; i++) {
	      var wrapper = transactionWrappers[i];
	      try {
	        // Catching errors makes debugging more difficult, so we start with the
	        // OBSERVED_ERROR state before overwriting it with the real return value
	        // of initialize -- if it's still set to OBSERVED_ERROR in the finally
	        // block, it means wrapper.initialize threw.
	        this.wrapperInitData[i] = Transaction.OBSERVED_ERROR;
	        this.wrapperInitData[i] = wrapper.initialize ?
	          wrapper.initialize.call(this) :
	          null;
	      } finally {
	        if (this.wrapperInitData[i] === Transaction.OBSERVED_ERROR) {
	          // The initializer for wrapper i threw an error; initialize the
	          // remaining wrappers but silence any exceptions from them to ensure
	          // that the first error is the one to bubble up.
	          try {
	            this.initializeAll(i + 1);
	          } catch (err) {
	          }
	        }
	      }
	    }
	  },

	  /**
	   * Invokes each of `this.transactionWrappers.close[i]` functions, passing into
	   * them the respective return values of `this.transactionWrappers.init[i]`
	   * (`close`rs that correspond to initializers that failed will not be
	   * invoked).
	   */
	  closeAll: function(startIndex) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      this.isInTransaction(),
	      'Transaction.closeAll(): Cannot close transaction when none are open.'
	    ) : invariant(this.isInTransaction()));
	    var transactionWrappers = this.transactionWrappers;
	    for (var i = startIndex; i < transactionWrappers.length; i++) {
	      var wrapper = transactionWrappers[i];
	      var initData = this.wrapperInitData[i];
	      var errorThrown;
	      try {
	        // Catching errors makes debugging more difficult, so we start with
	        // errorThrown set to true before setting it to false after calling
	        // close -- if it's still set to true in the finally block, it means
	        // wrapper.close threw.
	        errorThrown = true;
	        if (initData !== Transaction.OBSERVED_ERROR && wrapper.close) {
	          wrapper.close.call(this, initData);
	        }
	        errorThrown = false;
	      } finally {
	        if (errorThrown) {
	          // The closer for wrapper i threw an error; close the remaining
	          // wrappers but silence any exceptions from them to ensure that the
	          // first error is the one to bubble up.
	          try {
	            this.closeAll(i + 1);
	          } catch (e) {
	          }
	        }
	      }
	    }
	    this.wrapperInitData.length = 0;
	  }
	};

	var Transaction = {

	  Mixin: Mixin,

	  /**
	   * Token to look for to determine if an error occured.
	   */
	  OBSERVED_ERROR: {}

	};

	module.exports = Transaction;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 36 */
/***/ function(module, exports) {

	/**
	 * Copyright 2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactLifeCycle
	 */

	'use strict';

	/**
	 * This module manages the bookkeeping when a component is in the process
	 * of being mounted or being unmounted. This is used as a way to enforce
	 * invariants (or warnings) when it is not recommended to call
	 * setState/forceUpdate.
	 *
	 * currentlyMountingInstance: During the construction phase, it is not possible
	 * to trigger an update since the instance is not fully mounted yet. However, we
	 * currently allow this as a convenience for mutating the initial state.
	 *
	 * currentlyUnmountingInstance: During the unmounting phase, the instance is
	 * still mounted and can therefore schedule an update. However, this is not
	 * recommended and probably an error since it's about to be unmounted.
	 * Therefore we still want to trigger in an error for that case.
	 */

	var ReactLifeCycle = {
	  currentlyMountingInstance: null,
	  currentlyUnmountingInstance: null
	};

	module.exports = ReactLifeCycle;


/***/ },
/* 37 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactInstanceMap
	 */

	'use strict';

	/**
	 * `ReactInstanceMap` maintains a mapping from a public facing stateful
	 * instance (key) and the internal representation (value). This allows public
	 * methods to accept the user facing instance as an argument and map them back
	 * to internal methods.
	 */

	// TODO: Replace this with ES6: var ReactInstanceMap = new Map();
	var ReactInstanceMap = {

	  /**
	   * This API should be called `delete` but we'd have to make sure to always
	   * transform these to strings for IE support. When this transform is fully
	   * supported we can rename it.
	   */
	  remove: function(key) {
	    key._reactInternalInstance = undefined;
	  },

	  get: function(key) {
	    return key._reactInternalInstance;
	  },

	  has: function(key) {
	    return key._reactInternalInstance !== undefined;
	  },

	  set: function(key, value) {
	    key._reactInternalInstance = value;
	  }

	};

	module.exports = ReactInstanceMap;


/***/ },
/* 38 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactClass
	 */

	'use strict';

	var ReactComponent = __webpack_require__(23);
	var ReactCurrentOwner = __webpack_require__(18);
	var ReactElement = __webpack_require__(12);
	var ReactErrorUtils = __webpack_require__(39);
	var ReactInstanceMap = __webpack_require__(37);
	var ReactLifeCycle = __webpack_require__(36);
	var ReactPropTypeLocations = __webpack_require__(32);
	var ReactPropTypeLocationNames = __webpack_require__(33);
	var ReactUpdateQueue = __webpack_require__(24);

	var assign = __webpack_require__(14);
	var invariant = __webpack_require__(8);
	var keyMirror = __webpack_require__(7);
	var keyOf = __webpack_require__(40);
	var warning = __webpack_require__(16);

	var MIXINS_KEY = keyOf({mixins: null});

	/**
	 * Policies that describe methods in `ReactClassInterface`.
	 */
	var SpecPolicy = keyMirror({
	  /**
	   * These methods may be defined only once by the class specification or mixin.
	   */
	  DEFINE_ONCE: null,
	  /**
	   * These methods may be defined by both the class specification and mixins.
	   * Subsequent definitions will be chained. These methods must return void.
	   */
	  DEFINE_MANY: null,
	  /**
	   * These methods are overriding the base class.
	   */
	  OVERRIDE_BASE: null,
	  /**
	   * These methods are similar to DEFINE_MANY, except we assume they return
	   * objects. We try to merge the keys of the return values of all the mixed in
	   * functions. If there is a key conflict we throw.
	   */
	  DEFINE_MANY_MERGED: null
	});


	var injectedMixins = [];

	/**
	 * Composite components are higher-level components that compose other composite
	 * or native components.
	 *
	 * To create a new type of `ReactClass`, pass a specification of
	 * your new class to `React.createClass`. The only requirement of your class
	 * specification is that you implement a `render` method.
	 *
	 *   var MyComponent = React.createClass({
	 *     render: function() {
	 *       return <div>Hello World</div>;
	 *     }
	 *   });
	 *
	 * The class specification supports a specific protocol of methods that have
	 * special meaning (e.g. `render`). See `ReactClassInterface` for
	 * more the comprehensive protocol. Any other properties and methods in the
	 * class specification will available on the prototype.
	 *
	 * @interface ReactClassInterface
	 * @internal
	 */
	var ReactClassInterface = {

	  /**
	   * An array of Mixin objects to include when defining your component.
	   *
	   * @type {array}
	   * @optional
	   */
	  mixins: SpecPolicy.DEFINE_MANY,

	  /**
	   * An object containing properties and methods that should be defined on
	   * the component's constructor instead of its prototype (static methods).
	   *
	   * @type {object}
	   * @optional
	   */
	  statics: SpecPolicy.DEFINE_MANY,

	  /**
	   * Definition of prop types for this component.
	   *
	   * @type {object}
	   * @optional
	   */
	  propTypes: SpecPolicy.DEFINE_MANY,

	  /**
	   * Definition of context types for this component.
	   *
	   * @type {object}
	   * @optional
	   */
	  contextTypes: SpecPolicy.DEFINE_MANY,

	  /**
	   * Definition of context types this component sets for its children.
	   *
	   * @type {object}
	   * @optional
	   */
	  childContextTypes: SpecPolicy.DEFINE_MANY,

	  // ==== Definition methods ====

	  /**
	   * Invoked when the component is mounted. Values in the mapping will be set on
	   * `this.props` if that prop is not specified (i.e. using an `in` check).
	   *
	   * This method is invoked before `getInitialState` and therefore cannot rely
	   * on `this.state` or use `this.setState`.
	   *
	   * @return {object}
	   * @optional
	   */
	  getDefaultProps: SpecPolicy.DEFINE_MANY_MERGED,

	  /**
	   * Invoked once before the component is mounted. The return value will be used
	   * as the initial value of `this.state`.
	   *
	   *   getInitialState: function() {
	   *     return {
	   *       isOn: false,
	   *       fooBaz: new BazFoo()
	   *     }
	   *   }
	   *
	   * @return {object}
	   * @optional
	   */
	  getInitialState: SpecPolicy.DEFINE_MANY_MERGED,

	  /**
	   * @return {object}
	   * @optional
	   */
	  getChildContext: SpecPolicy.DEFINE_MANY_MERGED,

	  /**
	   * Uses props from `this.props` and state from `this.state` to render the
	   * structure of the component.
	   *
	   * No guarantees are made about when or how often this method is invoked, so
	   * it must not have side effects.
	   *
	   *   render: function() {
	   *     var name = this.props.name;
	   *     return <div>Hello, {name}!</div>;
	   *   }
	   *
	   * @return {ReactComponent}
	   * @nosideeffects
	   * @required
	   */
	  render: SpecPolicy.DEFINE_ONCE,



	  // ==== Delegate methods ====

	  /**
	   * Invoked when the component is initially created and about to be mounted.
	   * This may have side effects, but any external subscriptions or data created
	   * by this method must be cleaned up in `componentWillUnmount`.
	   *
	   * @optional
	   */
	  componentWillMount: SpecPolicy.DEFINE_MANY,

	  /**
	   * Invoked when the component has been mounted and has a DOM representation.
	   * However, there is no guarantee that the DOM node is in the document.
	   *
	   * Use this as an opportunity to operate on the DOM when the component has
	   * been mounted (initialized and rendered) for the first time.
	   *
	   * @param {DOMElement} rootNode DOM element representing the component.
	   * @optional
	   */
	  componentDidMount: SpecPolicy.DEFINE_MANY,

	  /**
	   * Invoked before the component receives new props.
	   *
	   * Use this as an opportunity to react to a prop transition by updating the
	   * state using `this.setState`. Current props are accessed via `this.props`.
	   *
	   *   componentWillReceiveProps: function(nextProps, nextContext) {
	   *     this.setState({
	   *       likesIncreasing: nextProps.likeCount > this.props.likeCount
	   *     });
	   *   }
	   *
	   * NOTE: There is no equivalent `componentWillReceiveState`. An incoming prop
	   * transition may cause a state change, but the opposite is not true. If you
	   * need it, you are probably looking for `componentWillUpdate`.
	   *
	   * @param {object} nextProps
	   * @optional
	   */
	  componentWillReceiveProps: SpecPolicy.DEFINE_MANY,

	  /**
	   * Invoked while deciding if the component should be updated as a result of
	   * receiving new props, state and/or context.
	   *
	   * Use this as an opportunity to `return false` when you're certain that the
	   * transition to the new props/state/context will not require a component
	   * update.
	   *
	   *   shouldComponentUpdate: function(nextProps, nextState, nextContext) {
	   *     return !equal(nextProps, this.props) ||
	   *       !equal(nextState, this.state) ||
	   *       !equal(nextContext, this.context);
	   *   }
	   *
	   * @param {object} nextProps
	   * @param {?object} nextState
	   * @param {?object} nextContext
	   * @return {boolean} True if the component should update.
	   * @optional
	   */
	  shouldComponentUpdate: SpecPolicy.DEFINE_ONCE,

	  /**
	   * Invoked when the component is about to update due to a transition from
	   * `this.props`, `this.state` and `this.context` to `nextProps`, `nextState`
	   * and `nextContext`.
	   *
	   * Use this as an opportunity to perform preparation before an update occurs.
	   *
	   * NOTE: You **cannot** use `this.setState()` in this method.
	   *
	   * @param {object} nextProps
	   * @param {?object} nextState
	   * @param {?object} nextContext
	   * @param {ReactReconcileTransaction} transaction
	   * @optional
	   */
	  componentWillUpdate: SpecPolicy.DEFINE_MANY,

	  /**
	   * Invoked when the component's DOM representation has been updated.
	   *
	   * Use this as an opportunity to operate on the DOM when the component has
	   * been updated.
	   *
	   * @param {object} prevProps
	   * @param {?object} prevState
	   * @param {?object} prevContext
	   * @param {DOMElement} rootNode DOM element representing the component.
	   * @optional
	   */
	  componentDidUpdate: SpecPolicy.DEFINE_MANY,

	  /**
	   * Invoked when the component is about to be removed from its parent and have
	   * its DOM representation destroyed.
	   *
	   * Use this as an opportunity to deallocate any external resources.
	   *
	   * NOTE: There is no `componentDidUnmount` since your component will have been
	   * destroyed by that point.
	   *
	   * @optional
	   */
	  componentWillUnmount: SpecPolicy.DEFINE_MANY,



	  // ==== Advanced methods ====

	  /**
	   * Updates the component's currently mounted DOM representation.
	   *
	   * By default, this implements React's rendering and reconciliation algorithm.
	   * Sophisticated clients may wish to override this.
	   *
	   * @param {ReactReconcileTransaction} transaction
	   * @internal
	   * @overridable
	   */
	  updateComponent: SpecPolicy.OVERRIDE_BASE

	};

	/**
	 * Mapping from class specification keys to special processing functions.
	 *
	 * Although these are declared like instance properties in the specification
	 * when defining classes using `React.createClass`, they are actually static
	 * and are accessible on the constructor instead of the prototype. Despite
	 * being static, they must be defined outside of the "statics" key under
	 * which all other static methods are defined.
	 */
	var RESERVED_SPEC_KEYS = {
	  displayName: function(Constructor, displayName) {
	    Constructor.displayName = displayName;
	  },
	  mixins: function(Constructor, mixins) {
	    if (mixins) {
	      for (var i = 0; i < mixins.length; i++) {
	        mixSpecIntoComponent(Constructor, mixins[i]);
	      }
	    }
	  },
	  childContextTypes: function(Constructor, childContextTypes) {
	    if ("production" !== process.env.NODE_ENV) {
	      validateTypeDef(
	        Constructor,
	        childContextTypes,
	        ReactPropTypeLocations.childContext
	      );
	    }
	    Constructor.childContextTypes = assign(
	      {},
	      Constructor.childContextTypes,
	      childContextTypes
	    );
	  },
	  contextTypes: function(Constructor, contextTypes) {
	    if ("production" !== process.env.NODE_ENV) {
	      validateTypeDef(
	        Constructor,
	        contextTypes,
	        ReactPropTypeLocations.context
	      );
	    }
	    Constructor.contextTypes = assign(
	      {},
	      Constructor.contextTypes,
	      contextTypes
	    );
	  },
	  /**
	   * Special case getDefaultProps which should move into statics but requires
	   * automatic merging.
	   */
	  getDefaultProps: function(Constructor, getDefaultProps) {
	    if (Constructor.getDefaultProps) {
	      Constructor.getDefaultProps = createMergedResultFunction(
	        Constructor.getDefaultProps,
	        getDefaultProps
	      );
	    } else {
	      Constructor.getDefaultProps = getDefaultProps;
	    }
	  },
	  propTypes: function(Constructor, propTypes) {
	    if ("production" !== process.env.NODE_ENV) {
	      validateTypeDef(
	        Constructor,
	        propTypes,
	        ReactPropTypeLocations.prop
	      );
	    }
	    Constructor.propTypes = assign(
	      {},
	      Constructor.propTypes,
	      propTypes
	    );
	  },
	  statics: function(Constructor, statics) {
	    mixStaticSpecIntoComponent(Constructor, statics);
	  }
	};

	function validateTypeDef(Constructor, typeDef, location) {
	  for (var propName in typeDef) {
	    if (typeDef.hasOwnProperty(propName)) {
	      // use a warning instead of an invariant so components
	      // don't show up in prod but not in __DEV__
	      ("production" !== process.env.NODE_ENV ? warning(
	        typeof typeDef[propName] === 'function',
	        '%s: %s type `%s` is invalid; it must be a function, usually from ' +
	        'React.PropTypes.',
	        Constructor.displayName || 'ReactClass',
	        ReactPropTypeLocationNames[location],
	        propName
	      ) : null);
	    }
	  }
	}

	function validateMethodOverride(proto, name) {
	  var specPolicy = ReactClassInterface.hasOwnProperty(name) ?
	    ReactClassInterface[name] :
	    null;

	  // Disallow overriding of base class methods unless explicitly allowed.
	  if (ReactClassMixin.hasOwnProperty(name)) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      specPolicy === SpecPolicy.OVERRIDE_BASE,
	      'ReactClassInterface: You are attempting to override ' +
	      '`%s` from your class specification. Ensure that your method names ' +
	      'do not overlap with React methods.',
	      name
	    ) : invariant(specPolicy === SpecPolicy.OVERRIDE_BASE));
	  }

	  // Disallow defining methods more than once unless explicitly allowed.
	  if (proto.hasOwnProperty(name)) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      specPolicy === SpecPolicy.DEFINE_MANY ||
	      specPolicy === SpecPolicy.DEFINE_MANY_MERGED,
	      'ReactClassInterface: You are attempting to define ' +
	      '`%s` on your component more than once. This conflict may be due ' +
	      'to a mixin.',
	      name
	    ) : invariant(specPolicy === SpecPolicy.DEFINE_MANY ||
	    specPolicy === SpecPolicy.DEFINE_MANY_MERGED));
	  }
	}

	/**
	 * Mixin helper which handles policy validation and reserved
	 * specification keys when building React classses.
	 */
	function mixSpecIntoComponent(Constructor, spec) {
	  if (!spec) {
	    return;
	  }

	  ("production" !== process.env.NODE_ENV ? invariant(
	    typeof spec !== 'function',
	    'ReactClass: You\'re attempting to ' +
	    'use a component class as a mixin. Instead, just use a regular object.'
	  ) : invariant(typeof spec !== 'function'));
	  ("production" !== process.env.NODE_ENV ? invariant(
	    !ReactElement.isValidElement(spec),
	    'ReactClass: You\'re attempting to ' +
	    'use a component as a mixin. Instead, just use a regular object.'
	  ) : invariant(!ReactElement.isValidElement(spec)));

	  var proto = Constructor.prototype;

	  // By handling mixins before any other properties, we ensure the same
	  // chaining order is applied to methods with DEFINE_MANY policy, whether
	  // mixins are listed before or after these methods in the spec.
	  if (spec.hasOwnProperty(MIXINS_KEY)) {
	    RESERVED_SPEC_KEYS.mixins(Constructor, spec.mixins);
	  }

	  for (var name in spec) {
	    if (!spec.hasOwnProperty(name)) {
	      continue;
	    }

	    if (name === MIXINS_KEY) {
	      // We have already handled mixins in a special case above
	      continue;
	    }

	    var property = spec[name];
	    validateMethodOverride(proto, name);

	    if (RESERVED_SPEC_KEYS.hasOwnProperty(name)) {
	      RESERVED_SPEC_KEYS[name](Constructor, property);
	    } else {
	      // Setup methods on prototype:
	      // The following member methods should not be automatically bound:
	      // 1. Expected ReactClass methods (in the "interface").
	      // 2. Overridden methods (that were mixed in).
	      var isReactClassMethod =
	        ReactClassInterface.hasOwnProperty(name);
	      var isAlreadyDefined = proto.hasOwnProperty(name);
	      var markedDontBind = property && property.__reactDontBind;
	      var isFunction = typeof property === 'function';
	      var shouldAutoBind =
	        isFunction &&
	        !isReactClassMethod &&
	        !isAlreadyDefined &&
	        !markedDontBind;

	      if (shouldAutoBind) {
	        if (!proto.__reactAutoBindMap) {
	          proto.__reactAutoBindMap = {};
	        }
	        proto.__reactAutoBindMap[name] = property;
	        proto[name] = property;
	      } else {
	        if (isAlreadyDefined) {
	          var specPolicy = ReactClassInterface[name];

	          // These cases should already be caught by validateMethodOverride
	          ("production" !== process.env.NODE_ENV ? invariant(
	            isReactClassMethod && (
	              (specPolicy === SpecPolicy.DEFINE_MANY_MERGED || specPolicy === SpecPolicy.DEFINE_MANY)
	            ),
	            'ReactClass: Unexpected spec policy %s for key %s ' +
	            'when mixing in component specs.',
	            specPolicy,
	            name
	          ) : invariant(isReactClassMethod && (
	            (specPolicy === SpecPolicy.DEFINE_MANY_MERGED || specPolicy === SpecPolicy.DEFINE_MANY)
	          )));

	          // For methods which are defined more than once, call the existing
	          // methods before calling the new property, merging if appropriate.
	          if (specPolicy === SpecPolicy.DEFINE_MANY_MERGED) {
	            proto[name] = createMergedResultFunction(proto[name], property);
	          } else if (specPolicy === SpecPolicy.DEFINE_MANY) {
	            proto[name] = createChainedFunction(proto[name], property);
	          }
	        } else {
	          proto[name] = property;
	          if ("production" !== process.env.NODE_ENV) {
	            // Add verbose displayName to the function, which helps when looking
	            // at profiling tools.
	            if (typeof property === 'function' && spec.displayName) {
	              proto[name].displayName = spec.displayName + '_' + name;
	            }
	          }
	        }
	      }
	    }
	  }
	}

	function mixStaticSpecIntoComponent(Constructor, statics) {
	  if (!statics) {
	    return;
	  }
	  for (var name in statics) {
	    var property = statics[name];
	    if (!statics.hasOwnProperty(name)) {
	      continue;
	    }

	    var isReserved = name in RESERVED_SPEC_KEYS;
	    ("production" !== process.env.NODE_ENV ? invariant(
	      !isReserved,
	      'ReactClass: You are attempting to define a reserved ' +
	      'property, `%s`, that shouldn\'t be on the "statics" key. Define it ' +
	      'as an instance property instead; it will still be accessible on the ' +
	      'constructor.',
	      name
	    ) : invariant(!isReserved));

	    var isInherited = name in Constructor;
	    ("production" !== process.env.NODE_ENV ? invariant(
	      !isInherited,
	      'ReactClass: You are attempting to define ' +
	      '`%s` on your component more than once. This conflict may be ' +
	      'due to a mixin.',
	      name
	    ) : invariant(!isInherited));
	    Constructor[name] = property;
	  }
	}

	/**
	 * Merge two objects, but throw if both contain the same key.
	 *
	 * @param {object} one The first object, which is mutated.
	 * @param {object} two The second object
	 * @return {object} one after it has been mutated to contain everything in two.
	 */
	function mergeIntoWithNoDuplicateKeys(one, two) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    one && two && typeof one === 'object' && typeof two === 'object',
	    'mergeIntoWithNoDuplicateKeys(): Cannot merge non-objects.'
	  ) : invariant(one && two && typeof one === 'object' && typeof two === 'object'));

	  for (var key in two) {
	    if (two.hasOwnProperty(key)) {
	      ("production" !== process.env.NODE_ENV ? invariant(
	        one[key] === undefined,
	        'mergeIntoWithNoDuplicateKeys(): ' +
	        'Tried to merge two objects with the same key: `%s`. This conflict ' +
	        'may be due to a mixin; in particular, this may be caused by two ' +
	        'getInitialState() or getDefaultProps() methods returning objects ' +
	        'with clashing keys.',
	        key
	      ) : invariant(one[key] === undefined));
	      one[key] = two[key];
	    }
	  }
	  return one;
	}

	/**
	 * Creates a function that invokes two functions and merges their return values.
	 *
	 * @param {function} one Function to invoke first.
	 * @param {function} two Function to invoke second.
	 * @return {function} Function that invokes the two argument functions.
	 * @private
	 */
	function createMergedResultFunction(one, two) {
	  return function mergedResult() {
	    var a = one.apply(this, arguments);
	    var b = two.apply(this, arguments);
	    if (a == null) {
	      return b;
	    } else if (b == null) {
	      return a;
	    }
	    var c = {};
	    mergeIntoWithNoDuplicateKeys(c, a);
	    mergeIntoWithNoDuplicateKeys(c, b);
	    return c;
	  };
	}

	/**
	 * Creates a function that invokes two functions and ignores their return vales.
	 *
	 * @param {function} one Function to invoke first.
	 * @param {function} two Function to invoke second.
	 * @return {function} Function that invokes the two argument functions.
	 * @private
	 */
	function createChainedFunction(one, two) {
	  return function chainedFunction() {
	    one.apply(this, arguments);
	    two.apply(this, arguments);
	  };
	}

	/**
	 * Binds a method to the component.
	 *
	 * @param {object} component Component whose method is going to be bound.
	 * @param {function} method Method to be bound.
	 * @return {function} The bound method.
	 */
	function bindAutoBindMethod(component, method) {
	  var boundMethod = method.bind(component);
	  if ("production" !== process.env.NODE_ENV) {
	    boundMethod.__reactBoundContext = component;
	    boundMethod.__reactBoundMethod = method;
	    boundMethod.__reactBoundArguments = null;
	    var componentName = component.constructor.displayName;
	    var _bind = boundMethod.bind;
	    /* eslint-disable block-scoped-var, no-undef */
	    boundMethod.bind = function(newThis ) {for (var args=[],$__0=1,$__1=arguments.length;$__0<$__1;$__0++) args.push(arguments[$__0]);
	      // User is trying to bind() an autobound method; we effectively will
	      // ignore the value of "this" that the user is trying to use, so
	      // let's warn.
	      if (newThis !== component && newThis !== null) {
	        ("production" !== process.env.NODE_ENV ? warning(
	          false,
	          'bind(): React component methods may only be bound to the ' +
	          'component instance. See %s',
	          componentName
	        ) : null);
	      } else if (!args.length) {
	        ("production" !== process.env.NODE_ENV ? warning(
	          false,
	          'bind(): You are binding a component method to the component. ' +
	          'React does this for you automatically in a high-performance ' +
	          'way, so you can safely remove this call. See %s',
	          componentName
	        ) : null);
	        return boundMethod;
	      }
	      var reboundMethod = _bind.apply(boundMethod, arguments);
	      reboundMethod.__reactBoundContext = component;
	      reboundMethod.__reactBoundMethod = method;
	      reboundMethod.__reactBoundArguments = args;
	      return reboundMethod;
	      /* eslint-enable */
	    };
	  }
	  return boundMethod;
	}

	/**
	 * Binds all auto-bound methods in a component.
	 *
	 * @param {object} component Component whose method is going to be bound.
	 */
	function bindAutoBindMethods(component) {
	  for (var autoBindKey in component.__reactAutoBindMap) {
	    if (component.__reactAutoBindMap.hasOwnProperty(autoBindKey)) {
	      var method = component.__reactAutoBindMap[autoBindKey];
	      component[autoBindKey] = bindAutoBindMethod(
	        component,
	        ReactErrorUtils.guard(
	          method,
	          component.constructor.displayName + '.' + autoBindKey
	        )
	      );
	    }
	  }
	}

	var typeDeprecationDescriptor = {
	  enumerable: false,
	  get: function() {
	    var displayName = this.displayName || this.name || 'Component';
	    ("production" !== process.env.NODE_ENV ? warning(
	      false,
	      '%s.type is deprecated. Use %s directly to access the class.',
	      displayName,
	      displayName
	    ) : null);
	    Object.defineProperty(this, 'type', {
	      value: this
	    });
	    return this;
	  }
	};

	/**
	 * Add more to the ReactClass base class. These are all legacy features and
	 * therefore not already part of the modern ReactComponent.
	 */
	var ReactClassMixin = {

	  /**
	   * TODO: This will be deprecated because state should always keep a consistent
	   * type signature and the only use case for this, is to avoid that.
	   */
	  replaceState: function(newState, callback) {
	    ReactUpdateQueue.enqueueReplaceState(this, newState);
	    if (callback) {
	      ReactUpdateQueue.enqueueCallback(this, callback);
	    }
	  },

	  /**
	   * Checks whether or not this composite component is mounted.
	   * @return {boolean} True if mounted, false otherwise.
	   * @protected
	   * @final
	   */
	  isMounted: function() {
	    if ("production" !== process.env.NODE_ENV) {
	      var owner = ReactCurrentOwner.current;
	      if (owner !== null) {
	        ("production" !== process.env.NODE_ENV ? warning(
	          owner._warnedAboutRefsInRender,
	          '%s is accessing isMounted inside its render() function. ' +
	          'render() should be a pure function of props and state. It should ' +
	          'never access something that requires stale data from the previous ' +
	          'render, such as refs. Move this logic to componentDidMount and ' +
	          'componentDidUpdate instead.',
	          owner.getName() || 'A component'
	        ) : null);
	        owner._warnedAboutRefsInRender = true;
	      }
	    }
	    var internalInstance = ReactInstanceMap.get(this);
	    return (
	      internalInstance &&
	      internalInstance !== ReactLifeCycle.currentlyMountingInstance
	    );
	  },

	  /**
	   * Sets a subset of the props.
	   *
	   * @param {object} partialProps Subset of the next props.
	   * @param {?function} callback Called after props are updated.
	   * @final
	   * @public
	   * @deprecated
	   */
	  setProps: function(partialProps, callback) {
	    ReactUpdateQueue.enqueueSetProps(this, partialProps);
	    if (callback) {
	      ReactUpdateQueue.enqueueCallback(this, callback);
	    }
	  },

	  /**
	   * Replace all the props.
	   *
	   * @param {object} newProps Subset of the next props.
	   * @param {?function} callback Called after props are updated.
	   * @final
	   * @public
	   * @deprecated
	   */
	  replaceProps: function(newProps, callback) {
	    ReactUpdateQueue.enqueueReplaceProps(this, newProps);
	    if (callback) {
	      ReactUpdateQueue.enqueueCallback(this, callback);
	    }
	  }
	};

	var ReactClassComponent = function() {};
	assign(
	  ReactClassComponent.prototype,
	  ReactComponent.prototype,
	  ReactClassMixin
	);

	/**
	 * Module for creating composite components.
	 *
	 * @class ReactClass
	 */
	var ReactClass = {

	  /**
	   * Creates a composite component class given a class specification.
	   *
	   * @param {object} spec Class specification (which must define `render`).
	   * @return {function} Component constructor function.
	   * @public
	   */
	  createClass: function(spec) {
	    var Constructor = function(props, context) {
	      // This constructor is overridden by mocks. The argument is used
	      // by mocks to assert on what gets mounted.

	      if ("production" !== process.env.NODE_ENV) {
	        ("production" !== process.env.NODE_ENV ? warning(
	          this instanceof Constructor,
	          'Something is calling a React component directly. Use a factory or ' +
	          'JSX instead. See: https://fb.me/react-legacyfactory'
	        ) : null);
	      }

	      // Wire up auto-binding
	      if (this.__reactAutoBindMap) {
	        bindAutoBindMethods(this);
	      }

	      this.props = props;
	      this.context = context;
	      this.state = null;

	      // ReactClasses doesn't have constructors. Instead, they use the
	      // getInitialState and componentWillMount methods for initialization.

	      var initialState = this.getInitialState ? this.getInitialState() : null;
	      if ("production" !== process.env.NODE_ENV) {
	        // We allow auto-mocks to proceed as if they're returning null.
	        if (typeof initialState === 'undefined' &&
	            this.getInitialState._isMockFunction) {
	          // This is probably bad practice. Consider warning here and
	          // deprecating this convenience.
	          initialState = null;
	        }
	      }
	      ("production" !== process.env.NODE_ENV ? invariant(
	        typeof initialState === 'object' && !Array.isArray(initialState),
	        '%s.getInitialState(): must return an object or null',
	        Constructor.displayName || 'ReactCompositeComponent'
	      ) : invariant(typeof initialState === 'object' && !Array.isArray(initialState)));

	      this.state = initialState;
	    };
	    Constructor.prototype = new ReactClassComponent();
	    Constructor.prototype.constructor = Constructor;

	    injectedMixins.forEach(
	      mixSpecIntoComponent.bind(null, Constructor)
	    );

	    mixSpecIntoComponent(Constructor, spec);

	    // Initialize the defaultProps property after all mixins have been merged
	    if (Constructor.getDefaultProps) {
	      Constructor.defaultProps = Constructor.getDefaultProps();
	    }

	    if ("production" !== process.env.NODE_ENV) {
	      // This is a tag to indicate that the use of these method names is ok,
	      // since it's used with createClass. If it's not, then it's likely a
	      // mistake so we'll warn you to use the static property, property
	      // initializer or constructor respectively.
	      if (Constructor.getDefaultProps) {
	        Constructor.getDefaultProps.isReactClassApproved = {};
	      }
	      if (Constructor.prototype.getInitialState) {
	        Constructor.prototype.getInitialState.isReactClassApproved = {};
	      }
	    }

	    ("production" !== process.env.NODE_ENV ? invariant(
	      Constructor.prototype.render,
	      'createClass(...): Class specification must implement a `render` method.'
	    ) : invariant(Constructor.prototype.render));

	    if ("production" !== process.env.NODE_ENV) {
	      ("production" !== process.env.NODE_ENV ? warning(
	        !Constructor.prototype.componentShouldUpdate,
	        '%s has a method called ' +
	        'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' +
	        'The name is phrased as a question because the function is ' +
	        'expected to return a value.',
	        spec.displayName || 'A component'
	      ) : null);
	    }

	    // Reduce time spent doing lookups by setting these on the prototype.
	    for (var methodName in ReactClassInterface) {
	      if (!Constructor.prototype[methodName]) {
	        Constructor.prototype[methodName] = null;
	      }
	    }

	    // Legacy hook
	    Constructor.type = Constructor;
	    if ("production" !== process.env.NODE_ENV) {
	      try {
	        Object.defineProperty(Constructor, 'type', typeDeprecationDescriptor);
	      } catch (x) {
	        // IE will fail on defineProperty (es5-shim/sham too)
	      }
	    }

	    return Constructor;
	  },

	  injection: {
	    injectMixin: function(mixin) {
	      injectedMixins.push(mixin);
	    }
	  }

	};

	module.exports = ReactClass;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 39 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactErrorUtils
	 * @typechecks
	 */

	"use strict";

	var ReactErrorUtils = {
	  /**
	   * Creates a guarded version of a function. This is supposed to make debugging
	   * of event handlers easier. To aid debugging with the browser's debugger,
	   * this currently simply returns the original function.
	   *
	   * @param {function} func Function to be executed
	   * @param {string} name The name of the guard
	   * @return {function}
	   */
	  guard: function(func, name) {
	    return func;
	  }
	};

	module.exports = ReactErrorUtils;


/***/ },
/* 40 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule keyOf
	 */

	/**
	 * Allows extraction of a minified key. Let's the build system minify keys
	 * without loosing the ability to dynamically use key strings as values
	 * themselves. Pass in an object with a single key/val pair and it will return
	 * you the string key of that single record. Suppose you want to grab the
	 * value for a key 'className' inside of an object. Key/val minification may
	 * have aliased that key to be 'xa12'. keyOf({className: null}) will return
	 * 'xa12' in that case. Resolve keys you want to use once at startup time, then
	 * reuse those resolutions.
	 */
	var keyOf = function(oneKeyObj) {
	  var key;
	  for (key in oneKeyObj) {
	    if (!oneKeyObj.hasOwnProperty(key)) {
	      continue;
	    }
	    return key;
	  }
	  return null;
	};


	module.exports = keyOf;


/***/ },
/* 41 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOM
	 * @typechecks static-only
	 */

	'use strict';

	var ReactElement = __webpack_require__(12);
	var ReactElementValidator = __webpack_require__(31);

	var mapObject = __webpack_require__(42);

	/**
	 * Create a factory that creates HTML tag elements.
	 *
	 * @param {string} tag Tag name (e.g. `div`).
	 * @private
	 */
	function createDOMFactory(tag) {
	  if ("production" !== process.env.NODE_ENV) {
	    return ReactElementValidator.createFactory(tag);
	  }
	  return ReactElement.createFactory(tag);
	}

	/**
	 * Creates a mapping from supported HTML tags to `ReactDOMComponent` classes.
	 * This is also accessible via `React.DOM`.
	 *
	 * @public
	 */
	var ReactDOM = mapObject({
	  a: 'a',
	  abbr: 'abbr',
	  address: 'address',
	  area: 'area',
	  article: 'article',
	  aside: 'aside',
	  audio: 'audio',
	  b: 'b',
	  base: 'base',
	  bdi: 'bdi',
	  bdo: 'bdo',
	  big: 'big',
	  blockquote: 'blockquote',
	  body: 'body',
	  br: 'br',
	  button: 'button',
	  canvas: 'canvas',
	  caption: 'caption',
	  cite: 'cite',
	  code: 'code',
	  col: 'col',
	  colgroup: 'colgroup',
	  data: 'data',
	  datalist: 'datalist',
	  dd: 'dd',
	  del: 'del',
	  details: 'details',
	  dfn: 'dfn',
	  dialog: 'dialog',
	  div: 'div',
	  dl: 'dl',
	  dt: 'dt',
	  em: 'em',
	  embed: 'embed',
	  fieldset: 'fieldset',
	  figcaption: 'figcaption',
	  figure: 'figure',
	  footer: 'footer',
	  form: 'form',
	  h1: 'h1',
	  h2: 'h2',
	  h3: 'h3',
	  h4: 'h4',
	  h5: 'h5',
	  h6: 'h6',
	  head: 'head',
	  header: 'header',
	  hr: 'hr',
	  html: 'html',
	  i: 'i',
	  iframe: 'iframe',
	  img: 'img',
	  input: 'input',
	  ins: 'ins',
	  kbd: 'kbd',
	  keygen: 'keygen',
	  label: 'label',
	  legend: 'legend',
	  li: 'li',
	  link: 'link',
	  main: 'main',
	  map: 'map',
	  mark: 'mark',
	  menu: 'menu',
	  menuitem: 'menuitem',
	  meta: 'meta',
	  meter: 'meter',
	  nav: 'nav',
	  noscript: 'noscript',
	  object: 'object',
	  ol: 'ol',
	  optgroup: 'optgroup',
	  option: 'option',
	  output: 'output',
	  p: 'p',
	  param: 'param',
	  picture: 'picture',
	  pre: 'pre',
	  progress: 'progress',
	  q: 'q',
	  rp: 'rp',
	  rt: 'rt',
	  ruby: 'ruby',
	  s: 's',
	  samp: 'samp',
	  script: 'script',
	  section: 'section',
	  select: 'select',
	  small: 'small',
	  source: 'source',
	  span: 'span',
	  strong: 'strong',
	  style: 'style',
	  sub: 'sub',
	  summary: 'summary',
	  sup: 'sup',
	  table: 'table',
	  tbody: 'tbody',
	  td: 'td',
	  textarea: 'textarea',
	  tfoot: 'tfoot',
	  th: 'th',
	  thead: 'thead',
	  time: 'time',
	  title: 'title',
	  tr: 'tr',
	  track: 'track',
	  u: 'u',
	  ul: 'ul',
	  'var': 'var',
	  video: 'video',
	  wbr: 'wbr',

	  // SVG
	  circle: 'circle',
	  clipPath: 'clipPath',
	  defs: 'defs',
	  ellipse: 'ellipse',
	  g: 'g',
	  line: 'line',
	  linearGradient: 'linearGradient',
	  mask: 'mask',
	  path: 'path',
	  pattern: 'pattern',
	  polygon: 'polygon',
	  polyline: 'polyline',
	  radialGradient: 'radialGradient',
	  rect: 'rect',
	  stop: 'stop',
	  svg: 'svg',
	  text: 'text',
	  tspan: 'tspan'

	}, createDOMFactory);

	module.exports = ReactDOM;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 42 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule mapObject
	 */

	'use strict';

	var hasOwnProperty = Object.prototype.hasOwnProperty;

	/**
	 * Executes the provided `callback` once for each enumerable own property in the
	 * object and constructs a new object from the results. The `callback` is
	 * invoked with three arguments:
	 *
	 *  - the property value
	 *  - the property name
	 *  - the object being traversed
	 *
	 * Properties that are added after the call to `mapObject` will not be visited
	 * by `callback`. If the values of existing properties are changed, the value
	 * passed to `callback` will be the value at the time `mapObject` visits them.
	 * Properties that are deleted before being visited are not visited.
	 *
	 * @grep function objectMap()
	 * @grep function objMap()
	 *
	 * @param {?object} object
	 * @param {function} callback
	 * @param {*} context
	 * @return {?object}
	 */
	function mapObject(object, callback, context) {
	  if (!object) {
	    return null;
	  }
	  var result = {};
	  for (var name in object) {
	    if (hasOwnProperty.call(object, name)) {
	      result[name] = callback.call(context, object[name], name, object);
	    }
	  }
	  return result;
	}

	module.exports = mapObject;


/***/ },
/* 43 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMTextComponent
	 * @typechecks static-only
	 */

	'use strict';

	var DOMPropertyOperations = __webpack_require__(44);
	var ReactComponentBrowserEnvironment =
	  __webpack_require__(48);
	var ReactDOMComponent = __webpack_require__(88);

	var assign = __webpack_require__(14);
	var escapeTextContentForBrowser = __webpack_require__(47);

	/**
	 * Text nodes violate a couple assumptions that React makes about components:
	 *
	 *  - When mounting text into the DOM, adjacent text nodes are merged.
	 *  - Text nodes cannot be assigned a React root ID.
	 *
	 * This component is used to wrap strings in elements so that they can undergo
	 * the same reconciliation that is applied to elements.
	 *
	 * TODO: Investigate representing React components in the DOM with text nodes.
	 *
	 * @class ReactDOMTextComponent
	 * @extends ReactComponent
	 * @internal
	 */
	var ReactDOMTextComponent = function(props) {
	  // This constructor and its argument is currently used by mocks.
	};

	assign(ReactDOMTextComponent.prototype, {

	  /**
	   * @param {ReactText} text
	   * @internal
	   */
	  construct: function(text) {
	    // TODO: This is really a ReactText (ReactNode), not a ReactElement
	    this._currentElement = text;
	    this._stringText = '' + text;

	    // Properties
	    this._rootNodeID = null;
	    this._mountIndex = 0;
	  },

	  /**
	   * Creates the markup for this text node. This node is not intended to have
	   * any features besides containing text content.
	   *
	   * @param {string} rootID DOM ID of the root node.
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @return {string} Markup for this text node.
	   * @internal
	   */
	  mountComponent: function(rootID, transaction, context) {
	    this._rootNodeID = rootID;
	    var escapedText = escapeTextContentForBrowser(this._stringText);

	    if (transaction.renderToStaticMarkup) {
	      // Normally we'd wrap this in a `span` for the reasons stated above, but
	      // since this is a situation where React won't take over (static pages),
	      // we can simply return the text as it is.
	      return escapedText;
	    }

	    return (
	      '<span ' + DOMPropertyOperations.createMarkupForID(rootID) + '>' +
	        escapedText +
	      '</span>'
	    );
	  },

	  /**
	   * Updates this component by updating the text content.
	   *
	   * @param {ReactText} nextText The next text content
	   * @param {ReactReconcileTransaction} transaction
	   * @internal
	   */
	  receiveComponent: function(nextText, transaction) {
	    if (nextText !== this._currentElement) {
	      this._currentElement = nextText;
	      var nextStringText = '' + nextText;
	      if (nextStringText !== this._stringText) {
	        // TODO: Save this as pending props and use performUpdateIfNecessary
	        // and/or updateComponent to do the actual update for consistency with
	        // other component types?
	        this._stringText = nextStringText;
	        ReactDOMComponent.BackendIDOperations.updateTextContentByID(
	          this._rootNodeID,
	          nextStringText
	        );
	      }
	    }
	  },

	  unmountComponent: function() {
	    ReactComponentBrowserEnvironment.unmountIDFromEnvironment(this._rootNodeID);
	  }

	});

	module.exports = ReactDOMTextComponent;


/***/ },
/* 44 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule DOMPropertyOperations
	 * @typechecks static-only
	 */

	'use strict';

	var DOMProperty = __webpack_require__(45);

	var quoteAttributeValueForBrowser = __webpack_require__(46);
	var warning = __webpack_require__(16);

	function shouldIgnoreValue(name, value) {
	  return value == null ||
	    (DOMProperty.hasBooleanValue[name] && !value) ||
	    (DOMProperty.hasNumericValue[name] && isNaN(value)) ||
	    (DOMProperty.hasPositiveNumericValue[name] && (value < 1)) ||
	    (DOMProperty.hasOverloadedBooleanValue[name] && value === false);
	}

	if ("production" !== process.env.NODE_ENV) {
	  var reactProps = {
	    children: true,
	    dangerouslySetInnerHTML: true,
	    key: true,
	    ref: true
	  };
	  var warnedProperties = {};

	  var warnUnknownProperty = function(name) {
	    if (reactProps.hasOwnProperty(name) && reactProps[name] ||
	        warnedProperties.hasOwnProperty(name) && warnedProperties[name]) {
	      return;
	    }

	    warnedProperties[name] = true;
	    var lowerCasedName = name.toLowerCase();

	    // data-* attributes should be lowercase; suggest the lowercase version
	    var standardName = (
	      DOMProperty.isCustomAttribute(lowerCasedName) ?
	        lowerCasedName :
	      DOMProperty.getPossibleStandardName.hasOwnProperty(lowerCasedName) ?
	        DOMProperty.getPossibleStandardName[lowerCasedName] :
	        null
	    );

	    // For now, only warn when we have a suggested correction. This prevents
	    // logging too much when using transferPropsTo.
	    ("production" !== process.env.NODE_ENV ? warning(
	      standardName == null,
	      'Unknown DOM property %s. Did you mean %s?',
	      name,
	      standardName
	    ) : null);

	  };
	}

	/**
	 * Operations for dealing with DOM properties.
	 */
	var DOMPropertyOperations = {

	  /**
	   * Creates markup for the ID property.
	   *
	   * @param {string} id Unescaped ID.
	   * @return {string} Markup string.
	   */
	  createMarkupForID: function(id) {
	    return DOMProperty.ID_ATTRIBUTE_NAME + '=' +
	      quoteAttributeValueForBrowser(id);
	  },

	  /**
	   * Creates markup for a property.
	   *
	   * @param {string} name
	   * @param {*} value
	   * @return {?string} Markup string, or null if the property was invalid.
	   */
	  createMarkupForProperty: function(name, value) {
	    if (DOMProperty.isStandardName.hasOwnProperty(name) &&
	        DOMProperty.isStandardName[name]) {
	      if (shouldIgnoreValue(name, value)) {
	        return '';
	      }
	      var attributeName = DOMProperty.getAttributeName[name];
	      if (DOMProperty.hasBooleanValue[name] ||
	          (DOMProperty.hasOverloadedBooleanValue[name] && value === true)) {
	        return attributeName;
	      }
	      return attributeName + '=' + quoteAttributeValueForBrowser(value);
	    } else if (DOMProperty.isCustomAttribute(name)) {
	      if (value == null) {
	        return '';
	      }
	      return name + '=' + quoteAttributeValueForBrowser(value);
	    } else if ("production" !== process.env.NODE_ENV) {
	      warnUnknownProperty(name);
	    }
	    return null;
	  },

	  /**
	   * Sets the value for a property on a node.
	   *
	   * @param {DOMElement} node
	   * @param {string} name
	   * @param {*} value
	   */
	  setValueForProperty: function(node, name, value) {
	    if (DOMProperty.isStandardName.hasOwnProperty(name) &&
	        DOMProperty.isStandardName[name]) {
	      var mutationMethod = DOMProperty.getMutationMethod[name];
	      if (mutationMethod) {
	        mutationMethod(node, value);
	      } else if (shouldIgnoreValue(name, value)) {
	        this.deleteValueForProperty(node, name);
	      } else if (DOMProperty.mustUseAttribute[name]) {
	        // `setAttribute` with objects becomes only `[object]` in IE8/9,
	        // ('' + value) makes it output the correct toString()-value.
	        node.setAttribute(DOMProperty.getAttributeName[name], '' + value);
	      } else {
	        var propName = DOMProperty.getPropertyName[name];
	        // Must explicitly cast values for HAS_SIDE_EFFECTS-properties to the
	        // property type before comparing; only `value` does and is string.
	        if (!DOMProperty.hasSideEffects[name] ||
	            ('' + node[propName]) !== ('' + value)) {
	          // Contrary to `setAttribute`, object properties are properly
	          // `toString`ed by IE8/9.
	          node[propName] = value;
	        }
	      }
	    } else if (DOMProperty.isCustomAttribute(name)) {
	      if (value == null) {
	        node.removeAttribute(name);
	      } else {
	        node.setAttribute(name, '' + value);
	      }
	    } else if ("production" !== process.env.NODE_ENV) {
	      warnUnknownProperty(name);
	    }
	  },

	  /**
	   * Deletes the value for a property on a node.
	   *
	   * @param {DOMElement} node
	   * @param {string} name
	   */
	  deleteValueForProperty: function(node, name) {
	    if (DOMProperty.isStandardName.hasOwnProperty(name) &&
	        DOMProperty.isStandardName[name]) {
	      var mutationMethod = DOMProperty.getMutationMethod[name];
	      if (mutationMethod) {
	        mutationMethod(node, undefined);
	      } else if (DOMProperty.mustUseAttribute[name]) {
	        node.removeAttribute(DOMProperty.getAttributeName[name]);
	      } else {
	        var propName = DOMProperty.getPropertyName[name];
	        var defaultValue = DOMProperty.getDefaultValueForProperty(
	          node.nodeName,
	          propName
	        );
	        if (!DOMProperty.hasSideEffects[name] ||
	            ('' + node[propName]) !== defaultValue) {
	          node[propName] = defaultValue;
	        }
	      }
	    } else if (DOMProperty.isCustomAttribute(name)) {
	      node.removeAttribute(name);
	    } else if ("production" !== process.env.NODE_ENV) {
	      warnUnknownProperty(name);
	    }
	  }

	};

	module.exports = DOMPropertyOperations;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 45 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule DOMProperty
	 * @typechecks static-only
	 */

	/*jslint bitwise: true */

	'use strict';

	var invariant = __webpack_require__(8);

	function checkMask(value, bitmask) {
	  return (value & bitmask) === bitmask;
	}

	var DOMPropertyInjection = {
	  /**
	   * Mapping from normalized, camelcased property names to a configuration that
	   * specifies how the associated DOM property should be accessed or rendered.
	   */
	  MUST_USE_ATTRIBUTE: 0x1,
	  MUST_USE_PROPERTY: 0x2,
	  HAS_SIDE_EFFECTS: 0x4,
	  HAS_BOOLEAN_VALUE: 0x8,
	  HAS_NUMERIC_VALUE: 0x10,
	  HAS_POSITIVE_NUMERIC_VALUE: 0x20 | 0x10,
	  HAS_OVERLOADED_BOOLEAN_VALUE: 0x40,

	  /**
	   * Inject some specialized knowledge about the DOM. This takes a config object
	   * with the following properties:
	   *
	   * isCustomAttribute: function that given an attribute name will return true
	   * if it can be inserted into the DOM verbatim. Useful for data-* or aria-*
	   * attributes where it's impossible to enumerate all of the possible
	   * attribute names,
	   *
	   * Properties: object mapping DOM property name to one of the
	   * DOMPropertyInjection constants or null. If your attribute isn't in here,
	   * it won't get written to the DOM.
	   *
	   * DOMAttributeNames: object mapping React attribute name to the DOM
	   * attribute name. Attribute names not specified use the **lowercase**
	   * normalized name.
	   *
	   * DOMPropertyNames: similar to DOMAttributeNames but for DOM properties.
	   * Property names not specified use the normalized name.
	   *
	   * DOMMutationMethods: Properties that require special mutation methods. If
	   * `value` is undefined, the mutation method should unset the property.
	   *
	   * @param {object} domPropertyConfig the config as described above.
	   */
	  injectDOMPropertyConfig: function(domPropertyConfig) {
	    var Properties = domPropertyConfig.Properties || {};
	    var DOMAttributeNames = domPropertyConfig.DOMAttributeNames || {};
	    var DOMPropertyNames = domPropertyConfig.DOMPropertyNames || {};
	    var DOMMutationMethods = domPropertyConfig.DOMMutationMethods || {};

	    if (domPropertyConfig.isCustomAttribute) {
	      DOMProperty._isCustomAttributeFunctions.push(
	        domPropertyConfig.isCustomAttribute
	      );
	    }

	    for (var propName in Properties) {
	      ("production" !== process.env.NODE_ENV ? invariant(
	        !DOMProperty.isStandardName.hasOwnProperty(propName),
	        'injectDOMPropertyConfig(...): You\'re trying to inject DOM property ' +
	        '\'%s\' which has already been injected. You may be accidentally ' +
	        'injecting the same DOM property config twice, or you may be ' +
	        'injecting two configs that have conflicting property names.',
	        propName
	      ) : invariant(!DOMProperty.isStandardName.hasOwnProperty(propName)));

	      DOMProperty.isStandardName[propName] = true;

	      var lowerCased = propName.toLowerCase();
	      DOMProperty.getPossibleStandardName[lowerCased] = propName;

	      if (DOMAttributeNames.hasOwnProperty(propName)) {
	        var attributeName = DOMAttributeNames[propName];
	        DOMProperty.getPossibleStandardName[attributeName] = propName;
	        DOMProperty.getAttributeName[propName] = attributeName;
	      } else {
	        DOMProperty.getAttributeName[propName] = lowerCased;
	      }

	      DOMProperty.getPropertyName[propName] =
	        DOMPropertyNames.hasOwnProperty(propName) ?
	          DOMPropertyNames[propName] :
	          propName;

	      if (DOMMutationMethods.hasOwnProperty(propName)) {
	        DOMProperty.getMutationMethod[propName] = DOMMutationMethods[propName];
	      } else {
	        DOMProperty.getMutationMethod[propName] = null;
	      }

	      var propConfig = Properties[propName];
	      DOMProperty.mustUseAttribute[propName] =
	        checkMask(propConfig, DOMPropertyInjection.MUST_USE_ATTRIBUTE);
	      DOMProperty.mustUseProperty[propName] =
	        checkMask(propConfig, DOMPropertyInjection.MUST_USE_PROPERTY);
	      DOMProperty.hasSideEffects[propName] =
	        checkMask(propConfig, DOMPropertyInjection.HAS_SIDE_EFFECTS);
	      DOMProperty.hasBooleanValue[propName] =
	        checkMask(propConfig, DOMPropertyInjection.HAS_BOOLEAN_VALUE);
	      DOMProperty.hasNumericValue[propName] =
	        checkMask(propConfig, DOMPropertyInjection.HAS_NUMERIC_VALUE);
	      DOMProperty.hasPositiveNumericValue[propName] =
	        checkMask(propConfig, DOMPropertyInjection.HAS_POSITIVE_NUMERIC_VALUE);
	      DOMProperty.hasOverloadedBooleanValue[propName] =
	        checkMask(propConfig, DOMPropertyInjection.HAS_OVERLOADED_BOOLEAN_VALUE);

	      ("production" !== process.env.NODE_ENV ? invariant(
	        !DOMProperty.mustUseAttribute[propName] ||
	          !DOMProperty.mustUseProperty[propName],
	        'DOMProperty: Cannot require using both attribute and property: %s',
	        propName
	      ) : invariant(!DOMProperty.mustUseAttribute[propName] ||
	        !DOMProperty.mustUseProperty[propName]));
	      ("production" !== process.env.NODE_ENV ? invariant(
	        DOMProperty.mustUseProperty[propName] ||
	          !DOMProperty.hasSideEffects[propName],
	        'DOMProperty: Properties that have side effects must use property: %s',
	        propName
	      ) : invariant(DOMProperty.mustUseProperty[propName] ||
	        !DOMProperty.hasSideEffects[propName]));
	      ("production" !== process.env.NODE_ENV ? invariant(
	        !!DOMProperty.hasBooleanValue[propName] +
	          !!DOMProperty.hasNumericValue[propName] +
	          !!DOMProperty.hasOverloadedBooleanValue[propName] <= 1,
	        'DOMProperty: Value can be one of boolean, overloaded boolean, or ' +
	        'numeric value, but not a combination: %s',
	        propName
	      ) : invariant(!!DOMProperty.hasBooleanValue[propName] +
	        !!DOMProperty.hasNumericValue[propName] +
	        !!DOMProperty.hasOverloadedBooleanValue[propName] <= 1));
	    }
	  }
	};
	var defaultValueCache = {};

	/**
	 * DOMProperty exports lookup objects that can be used like functions:
	 *
	 *   > DOMProperty.isValid['id']
	 *   true
	 *   > DOMProperty.isValid['foobar']
	 *   undefined
	 *
	 * Although this may be confusing, it performs better in general.
	 *
	 * @see http://jsperf.com/key-exists
	 * @see http://jsperf.com/key-missing
	 */
	var DOMProperty = {

	  ID_ATTRIBUTE_NAME: 'data-reactid',

	  /**
	   * Checks whether a property name is a standard property.
	   * @type {Object}
	   */
	  isStandardName: {},

	  /**
	   * Mapping from lowercase property names to the properly cased version, used
	   * to warn in the case of missing properties.
	   * @type {Object}
	   */
	  getPossibleStandardName: {},

	  /**
	   * Mapping from normalized names to attribute names that differ. Attribute
	   * names are used when rendering markup or with `*Attribute()`.
	   * @type {Object}
	   */
	  getAttributeName: {},

	  /**
	   * Mapping from normalized names to properties on DOM node instances.
	   * (This includes properties that mutate due to external factors.)
	   * @type {Object}
	   */
	  getPropertyName: {},

	  /**
	   * Mapping from normalized names to mutation methods. This will only exist if
	   * mutation cannot be set simply by the property or `setAttribute()`.
	   * @type {Object}
	   */
	  getMutationMethod: {},

	  /**
	   * Whether the property must be accessed and mutated as an object property.
	   * @type {Object}
	   */
	  mustUseAttribute: {},

	  /**
	   * Whether the property must be accessed and mutated using `*Attribute()`.
	   * (This includes anything that fails `<propName> in <element>`.)
	   * @type {Object}
	   */
	  mustUseProperty: {},

	  /**
	   * Whether or not setting a value causes side effects such as triggering
	   * resources to be loaded or text selection changes. We must ensure that
	   * the value is only set if it has changed.
	   * @type {Object}
	   */
	  hasSideEffects: {},

	  /**
	   * Whether the property should be removed when set to a falsey value.
	   * @type {Object}
	   */
	  hasBooleanValue: {},

	  /**
	   * Whether the property must be numeric or parse as a
	   * numeric and should be removed when set to a falsey value.
	   * @type {Object}
	   */
	  hasNumericValue: {},

	  /**
	   * Whether the property must be positive numeric or parse as a positive
	   * numeric and should be removed when set to a falsey value.
	   * @type {Object}
	   */
	  hasPositiveNumericValue: {},

	  /**
	   * Whether the property can be used as a flag as well as with a value. Removed
	   * when strictly equal to false; present without a value when strictly equal
	   * to true; present with a value otherwise.
	   * @type {Object}
	   */
	  hasOverloadedBooleanValue: {},

	  /**
	   * All of the isCustomAttribute() functions that have been injected.
	   */
	  _isCustomAttributeFunctions: [],

	  /**
	   * Checks whether a property name is a custom attribute.
	   * @method
	   */
	  isCustomAttribute: function(attributeName) {
	    for (var i = 0; i < DOMProperty._isCustomAttributeFunctions.length; i++) {
	      var isCustomAttributeFn = DOMProperty._isCustomAttributeFunctions[i];
	      if (isCustomAttributeFn(attributeName)) {
	        return true;
	      }
	    }
	    return false;
	  },

	  /**
	   * Returns the default property value for a DOM property (i.e., not an
	   * attribute). Most default values are '' or false, but not all. Worse yet,
	   * some (in particular, `type`) vary depending on the type of element.
	   *
	   * TODO: Is it better to grab all the possible properties when creating an
	   * element to avoid having to create the same element twice?
	   */
	  getDefaultValueForProperty: function(nodeName, prop) {
	    var nodeDefaults = defaultValueCache[nodeName];
	    var testElement;
	    if (!nodeDefaults) {
	      defaultValueCache[nodeName] = nodeDefaults = {};
	    }
	    if (!(prop in nodeDefaults)) {
	      testElement = document.createElement(nodeName);
	      nodeDefaults[prop] = testElement[prop];
	    }
	    return nodeDefaults[prop];
	  },

	  injection: DOMPropertyInjection
	};

	module.exports = DOMProperty;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 46 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule quoteAttributeValueForBrowser
	 */

	'use strict';

	var escapeTextContentForBrowser = __webpack_require__(47);

	/**
	 * Escapes attribute value to prevent scripting attacks.
	 *
	 * @param {*} value Value to escape.
	 * @return {string} An escaped string.
	 */
	function quoteAttributeValueForBrowser(value) {
	  return '"' + escapeTextContentForBrowser(value) + '"';
	}

	module.exports = quoteAttributeValueForBrowser;


/***/ },
/* 47 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule escapeTextContentForBrowser
	 */

	'use strict';

	var ESCAPE_LOOKUP = {
	  '&': '&amp;',
	  '>': '&gt;',
	  '<': '&lt;',
	  '"': '&quot;',
	  '\'': '&#x27;'
	};

	var ESCAPE_REGEX = /[&><"']/g;

	function escaper(match) {
	  return ESCAPE_LOOKUP[match];
	}

	/**
	 * Escapes text to prevent scripting attacks.
	 *
	 * @param {*} text Text value to escape.
	 * @return {string} An escaped string.
	 */
	function escapeTextContentForBrowser(text) {
	  return ('' + text).replace(ESCAPE_REGEX, escaper);
	}

	module.exports = escapeTextContentForBrowser;


/***/ },
/* 48 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactComponentBrowserEnvironment
	 */

	/*jslint evil: true */

	'use strict';

	var ReactDOMIDOperations = __webpack_require__(49);
	var ReactMount = __webpack_require__(68);

	/**
	 * Abstracts away all functionality of the reconciler that requires knowledge of
	 * the browser context. TODO: These callers should be refactored to avoid the
	 * need for this injection.
	 */
	var ReactComponentBrowserEnvironment = {

	  processChildrenUpdates:
	    ReactDOMIDOperations.dangerouslyProcessChildrenUpdates,

	  replaceNodeWithMarkupByID:
	    ReactDOMIDOperations.dangerouslyReplaceNodeWithMarkupByID,

	  /**
	   * If a particular environment requires that some resources be cleaned up,
	   * specify this in the injected Mixin. In the DOM, we would likely want to
	   * purge any cached node ID lookups.
	   *
	   * @private
	   */
	  unmountIDFromEnvironment: function(rootNodeID) {
	    ReactMount.purgeID(rootNodeID);
	  }

	};

	module.exports = ReactComponentBrowserEnvironment;


/***/ },
/* 49 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMIDOperations
	 * @typechecks static-only
	 */

	/*jslint evil: true */

	'use strict';

	var CSSPropertyOperations = __webpack_require__(50);
	var DOMChildrenOperations = __webpack_require__(59);
	var DOMPropertyOperations = __webpack_require__(44);
	var ReactMount = __webpack_require__(68);
	var ReactPerf = __webpack_require__(27);

	var invariant = __webpack_require__(8);
	var setInnerHTML = __webpack_require__(67);

	/**
	 * Errors for properties that should not be updated with `updatePropertyById()`.
	 *
	 * @type {object}
	 * @private
	 */
	var INVALID_PROPERTY_ERRORS = {
	  dangerouslySetInnerHTML:
	    '`dangerouslySetInnerHTML` must be set using `updateInnerHTMLByID()`.',
	  style: '`style` must be set using `updateStylesByID()`.'
	};

	/**
	 * Operations used to process updates to DOM nodes. This is made injectable via
	 * `ReactDOMComponent.BackendIDOperations`.
	 */
	var ReactDOMIDOperations = {

	  /**
	   * Updates a DOM node with new property values. This should only be used to
	   * update DOM properties in `DOMProperty`.
	   *
	   * @param {string} id ID of the node to update.
	   * @param {string} name A valid property name, see `DOMProperty`.
	   * @param {*} value New value of the property.
	   * @internal
	   */
	  updatePropertyByID: function(id, name, value) {
	    var node = ReactMount.getNode(id);
	    ("production" !== process.env.NODE_ENV ? invariant(
	      !INVALID_PROPERTY_ERRORS.hasOwnProperty(name),
	      'updatePropertyByID(...): %s',
	      INVALID_PROPERTY_ERRORS[name]
	    ) : invariant(!INVALID_PROPERTY_ERRORS.hasOwnProperty(name)));

	    // If we're updating to null or undefined, we should remove the property
	    // from the DOM node instead of inadvertantly setting to a string. This
	    // brings us in line with the same behavior we have on initial render.
	    if (value != null) {
	      DOMPropertyOperations.setValueForProperty(node, name, value);
	    } else {
	      DOMPropertyOperations.deleteValueForProperty(node, name);
	    }
	  },

	  /**
	   * Updates a DOM node to remove a property. This should only be used to remove
	   * DOM properties in `DOMProperty`.
	   *
	   * @param {string} id ID of the node to update.
	   * @param {string} name A property name to remove, see `DOMProperty`.
	   * @internal
	   */
	  deletePropertyByID: function(id, name, value) {
	    var node = ReactMount.getNode(id);
	    ("production" !== process.env.NODE_ENV ? invariant(
	      !INVALID_PROPERTY_ERRORS.hasOwnProperty(name),
	      'updatePropertyByID(...): %s',
	      INVALID_PROPERTY_ERRORS[name]
	    ) : invariant(!INVALID_PROPERTY_ERRORS.hasOwnProperty(name)));
	    DOMPropertyOperations.deleteValueForProperty(node, name, value);
	  },

	  /**
	   * Updates a DOM node with new style values. If a value is specified as '',
	   * the corresponding style property will be unset.
	   *
	   * @param {string} id ID of the node to update.
	   * @param {object} styles Mapping from styles to values.
	   * @internal
	   */
	  updateStylesByID: function(id, styles) {
	    var node = ReactMount.getNode(id);
	    CSSPropertyOperations.setValueForStyles(node, styles);
	  },

	  /**
	   * Updates a DOM node's innerHTML.
	   *
	   * @param {string} id ID of the node to update.
	   * @param {string} html An HTML string.
	   * @internal
	   */
	  updateInnerHTMLByID: function(id, html) {
	    var node = ReactMount.getNode(id);
	    setInnerHTML(node, html);
	  },

	  /**
	   * Updates a DOM node's text content set by `props.content`.
	   *
	   * @param {string} id ID of the node to update.
	   * @param {string} content Text content.
	   * @internal
	   */
	  updateTextContentByID: function(id, content) {
	    var node = ReactMount.getNode(id);
	    DOMChildrenOperations.updateTextContent(node, content);
	  },

	  /**
	   * Replaces a DOM node that exists in the document with markup.
	   *
	   * @param {string} id ID of child to be replaced.
	   * @param {string} markup Dangerous markup to inject in place of child.
	   * @internal
	   * @see {Danger.dangerouslyReplaceNodeWithMarkup}
	   */
	  dangerouslyReplaceNodeWithMarkupByID: function(id, markup) {
	    var node = ReactMount.getNode(id);
	    DOMChildrenOperations.dangerouslyReplaceNodeWithMarkup(node, markup);
	  },

	  /**
	   * Updates a component's children by processing a series of updates.
	   *
	   * @param {array<object>} updates List of update configurations.
	   * @param {array<string>} markup List of markup strings.
	   * @internal
	   */
	  dangerouslyProcessChildrenUpdates: function(updates, markup) {
	    for (var i = 0; i < updates.length; i++) {
	      updates[i].parentNode = ReactMount.getNode(updates[i].parentID);
	    }
	    DOMChildrenOperations.processUpdates(updates, markup);
	  }
	};

	ReactPerf.measureMethods(ReactDOMIDOperations, 'ReactDOMIDOperations', {
	  updatePropertyByID: 'updatePropertyByID',
	  deletePropertyByID: 'deletePropertyByID',
	  updateStylesByID: 'updateStylesByID',
	  updateInnerHTMLByID: 'updateInnerHTMLByID',
	  updateTextContentByID: 'updateTextContentByID',
	  dangerouslyReplaceNodeWithMarkupByID: 'dangerouslyReplaceNodeWithMarkupByID',
	  dangerouslyProcessChildrenUpdates: 'dangerouslyProcessChildrenUpdates'
	});

	module.exports = ReactDOMIDOperations;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 50 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule CSSPropertyOperations
	 * @typechecks static-only
	 */

	'use strict';

	var CSSProperty = __webpack_require__(51);
	var ExecutionEnvironment = __webpack_require__(52);

	var camelizeStyleName = __webpack_require__(53);
	var dangerousStyleValue = __webpack_require__(55);
	var hyphenateStyleName = __webpack_require__(56);
	var memoizeStringOnly = __webpack_require__(58);
	var warning = __webpack_require__(16);

	var processStyleName = memoizeStringOnly(function(styleName) {
	  return hyphenateStyleName(styleName);
	});

	var styleFloatAccessor = 'cssFloat';
	if (ExecutionEnvironment.canUseDOM) {
	  // IE8 only supports accessing cssFloat (standard) as styleFloat
	  if (document.documentElement.style.cssFloat === undefined) {
	    styleFloatAccessor = 'styleFloat';
	  }
	}

	if ("production" !== process.env.NODE_ENV) {
	  // 'msTransform' is correct, but the other prefixes should be capitalized
	  var badVendoredStyleNamePattern = /^(?:webkit|moz|o)[A-Z]/;

	  // style values shouldn't contain a semicolon
	  var badStyleValueWithSemicolonPattern = /;\s*$/;

	  var warnedStyleNames = {};
	  var warnedStyleValues = {};

	  var warnHyphenatedStyleName = function(name) {
	    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
	      return;
	    }

	    warnedStyleNames[name] = true;
	    ("production" !== process.env.NODE_ENV ? warning(
	      false,
	      'Unsupported style property %s. Did you mean %s?',
	      name,
	      camelizeStyleName(name)
	    ) : null);
	  };

	  var warnBadVendoredStyleName = function(name) {
	    if (warnedStyleNames.hasOwnProperty(name) && warnedStyleNames[name]) {
	      return;
	    }

	    warnedStyleNames[name] = true;
	    ("production" !== process.env.NODE_ENV ? warning(
	      false,
	      'Unsupported vendor-prefixed style property %s. Did you mean %s?',
	      name,
	      name.charAt(0).toUpperCase() + name.slice(1)
	    ) : null);
	  };

	  var warnStyleValueWithSemicolon = function(name, value) {
	    if (warnedStyleValues.hasOwnProperty(value) && warnedStyleValues[value]) {
	      return;
	    }

	    warnedStyleValues[value] = true;
	    ("production" !== process.env.NODE_ENV ? warning(
	      false,
	      'Style property values shouldn\'t contain a semicolon. ' +
	      'Try "%s: %s" instead.',
	      name,
	      value.replace(badStyleValueWithSemicolonPattern, '')
	    ) : null);
	  };

	  /**
	   * @param {string} name
	   * @param {*} value
	   */
	  var warnValidStyle = function(name, value) {
	    if (name.indexOf('-') > -1) {
	      warnHyphenatedStyleName(name);
	    } else if (badVendoredStyleNamePattern.test(name)) {
	      warnBadVendoredStyleName(name);
	    } else if (badStyleValueWithSemicolonPattern.test(value)) {
	      warnStyleValueWithSemicolon(name, value);
	    }
	  };
	}

	/**
	 * Operations for dealing with CSS properties.
	 */
	var CSSPropertyOperations = {

	  /**
	   * Serializes a mapping of style properties for use as inline styles:
	   *
	   *   > createMarkupForStyles({width: '200px', height: 0})
	   *   "width:200px;height:0;"
	   *
	   * Undefined values are ignored so that declarative programming is easier.
	   * The result should be HTML-escaped before insertion into the DOM.
	   *
	   * @param {object} styles
	   * @return {?string}
	   */
	  createMarkupForStyles: function(styles) {
	    var serialized = '';
	    for (var styleName in styles) {
	      if (!styles.hasOwnProperty(styleName)) {
	        continue;
	      }
	      var styleValue = styles[styleName];
	      if ("production" !== process.env.NODE_ENV) {
	        warnValidStyle(styleName, styleValue);
	      }
	      if (styleValue != null) {
	        serialized += processStyleName(styleName) + ':';
	        serialized += dangerousStyleValue(styleName, styleValue) + ';';
	      }
	    }
	    return serialized || null;
	  },

	  /**
	   * Sets the value for multiple styles on a node.  If a value is specified as
	   * '' (empty string), the corresponding style property will be unset.
	   *
	   * @param {DOMElement} node
	   * @param {object} styles
	   */
	  setValueForStyles: function(node, styles) {
	    var style = node.style;
	    for (var styleName in styles) {
	      if (!styles.hasOwnProperty(styleName)) {
	        continue;
	      }
	      if ("production" !== process.env.NODE_ENV) {
	        warnValidStyle(styleName, styles[styleName]);
	      }
	      var styleValue = dangerousStyleValue(styleName, styles[styleName]);
	      if (styleName === 'float') {
	        styleName = styleFloatAccessor;
	      }
	      if (styleValue) {
	        style[styleName] = styleValue;
	      } else {
	        var expansion = CSSProperty.shorthandPropertyExpansions[styleName];
	        if (expansion) {
	          // Shorthand property that IE8 won't like unsetting, so unset each
	          // component to placate it
	          for (var individualStyleName in expansion) {
	            style[individualStyleName] = '';
	          }
	        } else {
	          style[styleName] = '';
	        }
	      }
	    }
	  }

	};

	module.exports = CSSPropertyOperations;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 51 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule CSSProperty
	 */

	'use strict';

	/**
	 * CSS properties which accept numbers but are not in units of "px".
	 */
	var isUnitlessNumber = {
	  boxFlex: true,
	  boxFlexGroup: true,
	  columnCount: true,
	  flex: true,
	  flexGrow: true,
	  flexPositive: true,
	  flexShrink: true,
	  flexNegative: true,
	  fontWeight: true,
	  lineClamp: true,
	  lineHeight: true,
	  opacity: true,
	  order: true,
	  orphans: true,
	  widows: true,
	  zIndex: true,
	  zoom: true,

	  // SVG-related properties
	  fillOpacity: true,
	  strokeDashoffset: true,
	  strokeOpacity: true,
	  strokeWidth: true
	};

	/**
	 * @param {string} prefix vendor-specific prefix, eg: Webkit
	 * @param {string} key style name, eg: transitionDuration
	 * @return {string} style name prefixed with `prefix`, properly camelCased, eg:
	 * WebkitTransitionDuration
	 */
	function prefixKey(prefix, key) {
	  return prefix + key.charAt(0).toUpperCase() + key.substring(1);
	}

	/**
	 * Support style names that may come passed in prefixed by adding permutations
	 * of vendor prefixes.
	 */
	var prefixes = ['Webkit', 'ms', 'Moz', 'O'];

	// Using Object.keys here, or else the vanilla for-in loop makes IE8 go into an
	// infinite loop, because it iterates over the newly added props too.
	Object.keys(isUnitlessNumber).forEach(function(prop) {
	  prefixes.forEach(function(prefix) {
	    isUnitlessNumber[prefixKey(prefix, prop)] = isUnitlessNumber[prop];
	  });
	});

	/**
	 * Most style properties can be unset by doing .style[prop] = '' but IE8
	 * doesn't like doing that with shorthand properties so for the properties that
	 * IE8 breaks on, which are listed here, we instead unset each of the
	 * individual properties. See http://bugs.jquery.com/ticket/12385.
	 * The 4-value 'clock' properties like margin, padding, border-width seem to
	 * behave without any problems. Curiously, list-style works too without any
	 * special prodding.
	 */
	var shorthandPropertyExpansions = {
	  background: {
	    backgroundImage: true,
	    backgroundPosition: true,
	    backgroundRepeat: true,
	    backgroundColor: true
	  },
	  border: {
	    borderWidth: true,
	    borderStyle: true,
	    borderColor: true
	  },
	  borderBottom: {
	    borderBottomWidth: true,
	    borderBottomStyle: true,
	    borderBottomColor: true
	  },
	  borderLeft: {
	    borderLeftWidth: true,
	    borderLeftStyle: true,
	    borderLeftColor: true
	  },
	  borderRight: {
	    borderRightWidth: true,
	    borderRightStyle: true,
	    borderRightColor: true
	  },
	  borderTop: {
	    borderTopWidth: true,
	    borderTopStyle: true,
	    borderTopColor: true
	  },
	  font: {
	    fontStyle: true,
	    fontVariant: true,
	    fontWeight: true,
	    fontSize: true,
	    lineHeight: true,
	    fontFamily: true
	  }
	};

	var CSSProperty = {
	  isUnitlessNumber: isUnitlessNumber,
	  shorthandPropertyExpansions: shorthandPropertyExpansions
	};

	module.exports = CSSProperty;


/***/ },
/* 52 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ExecutionEnvironment
	 */

	/*jslint evil: true */

	"use strict";

	var canUseDOM = !!(
	  (typeof window !== 'undefined' &&
	  window.document && window.document.createElement)
	);

	/**
	 * Simple, lightweight module assisting with the detection and context of
	 * Worker. Helps avoid circular dependencies and allows code to reason about
	 * whether or not they are in a Worker, even if they never include the main
	 * `ReactWorker` dependency.
	 */
	var ExecutionEnvironment = {

	  canUseDOM: canUseDOM,

	  canUseWorkers: typeof Worker !== 'undefined',

	  canUseEventListeners:
	    canUseDOM && !!(window.addEventListener || window.attachEvent),

	  canUseViewport: canUseDOM && !!window.screen,

	  isInWorker: !canUseDOM // For now, this is true - might change in the future.

	};

	module.exports = ExecutionEnvironment;


/***/ },
/* 53 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule camelizeStyleName
	 * @typechecks
	 */

	"use strict";

	var camelize = __webpack_require__(54);

	var msPattern = /^-ms-/;

	/**
	 * Camelcases a hyphenated CSS property name, for example:
	 *
	 *   > camelizeStyleName('background-color')
	 *   < "backgroundColor"
	 *   > camelizeStyleName('-moz-transition')
	 *   < "MozTransition"
	 *   > camelizeStyleName('-ms-transition')
	 *   < "msTransition"
	 *
	 * As Andi Smith suggests
	 * (http://www.andismith.com/blog/2012/02/modernizr-prefixed/), an `-ms` prefix
	 * is converted to lowercase `ms`.
	 *
	 * @param {string} string
	 * @return {string}
	 */
	function camelizeStyleName(string) {
	  return camelize(string.replace(msPattern, 'ms-'));
	}

	module.exports = camelizeStyleName;


/***/ },
/* 54 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule camelize
	 * @typechecks
	 */

	var _hyphenPattern = /-(.)/g;

	/**
	 * Camelcases a hyphenated string, for example:
	 *
	 *   > camelize('background-color')
	 *   < "backgroundColor"
	 *
	 * @param {string} string
	 * @return {string}
	 */
	function camelize(string) {
	  return string.replace(_hyphenPattern, function(_, character) {
	    return character.toUpperCase();
	  });
	}

	module.exports = camelize;


/***/ },
/* 55 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule dangerousStyleValue
	 * @typechecks static-only
	 */

	'use strict';

	var CSSProperty = __webpack_require__(51);

	var isUnitlessNumber = CSSProperty.isUnitlessNumber;

	/**
	 * Convert a value into the proper css writable value. The style name `name`
	 * should be logical (no hyphens), as specified
	 * in `CSSProperty.isUnitlessNumber`.
	 *
	 * @param {string} name CSS property name such as `topMargin`.
	 * @param {*} value CSS property value such as `10px`.
	 * @return {string} Normalized style value with dimensions applied.
	 */
	function dangerousStyleValue(name, value) {
	  // Note that we've removed escapeTextForBrowser() calls here since the
	  // whole string will be escaped when the attribute is injected into
	  // the markup. If you provide unsafe user data here they can inject
	  // arbitrary CSS which may be problematic (I couldn't repro this):
	  // https://www.owasp.org/index.php/XSS_Filter_Evasion_Cheat_Sheet
	  // http://www.thespanner.co.uk/2007/11/26/ultimate-xss-css-injection/
	  // This is not an XSS hole but instead a potential CSS injection issue
	  // which has lead to a greater discussion about how we're going to
	  // trust URLs moving forward. See #2115901

	  var isEmpty = value == null || typeof value === 'boolean' || value === '';
	  if (isEmpty) {
	    return '';
	  }

	  var isNonNumeric = isNaN(value);
	  if (isNonNumeric || value === 0 ||
	      isUnitlessNumber.hasOwnProperty(name) && isUnitlessNumber[name]) {
	    return '' + value; // cast to string
	  }

	  if (typeof value === 'string') {
	    value = value.trim();
	  }
	  return value + 'px';
	}

	module.exports = dangerousStyleValue;


/***/ },
/* 56 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule hyphenateStyleName
	 * @typechecks
	 */

	"use strict";

	var hyphenate = __webpack_require__(57);

	var msPattern = /^ms-/;

	/**
	 * Hyphenates a camelcased CSS property name, for example:
	 *
	 *   > hyphenateStyleName('backgroundColor')
	 *   < "background-color"
	 *   > hyphenateStyleName('MozTransition')
	 *   < "-moz-transition"
	 *   > hyphenateStyleName('msTransition')
	 *   < "-ms-transition"
	 *
	 * As Modernizr suggests (http://modernizr.com/docs/#prefixed), an `ms` prefix
	 * is converted to `-ms-`.
	 *
	 * @param {string} string
	 * @return {string}
	 */
	function hyphenateStyleName(string) {
	  return hyphenate(string).replace(msPattern, '-ms-');
	}

	module.exports = hyphenateStyleName;


/***/ },
/* 57 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule hyphenate
	 * @typechecks
	 */

	var _uppercasePattern = /([A-Z])/g;

	/**
	 * Hyphenates a camelcased string, for example:
	 *
	 *   > hyphenate('backgroundColor')
	 *   < "background-color"
	 *
	 * For CSS style names, use `hyphenateStyleName` instead which works properly
	 * with all vendor prefixes, including `ms`.
	 *
	 * @param {string} string
	 * @return {string}
	 */
	function hyphenate(string) {
	  return string.replace(_uppercasePattern, '-$1').toLowerCase();
	}

	module.exports = hyphenate;


/***/ },
/* 58 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule memoizeStringOnly
	 * @typechecks static-only
	 */

	'use strict';

	/**
	 * Memoizes the return value of a function that accepts one string argument.
	 *
	 * @param {function} callback
	 * @return {function}
	 */
	function memoizeStringOnly(callback) {
	  var cache = {};
	  return function(string) {
	    if (!cache.hasOwnProperty(string)) {
	      cache[string] = callback.call(this, string);
	    }
	    return cache[string];
	  };
	}

	module.exports = memoizeStringOnly;


/***/ },
/* 59 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule DOMChildrenOperations
	 * @typechecks static-only
	 */

	'use strict';

	var Danger = __webpack_require__(60);
	var ReactMultiChildUpdateTypes = __webpack_require__(65);

	var setTextContent = __webpack_require__(66);
	var invariant = __webpack_require__(8);

	/**
	 * Inserts `childNode` as a child of `parentNode` at the `index`.
	 *
	 * @param {DOMElement} parentNode Parent node in which to insert.
	 * @param {DOMElement} childNode Child node to insert.
	 * @param {number} index Index at which to insert the child.
	 * @internal
	 */
	function insertChildAt(parentNode, childNode, index) {
	  // By exploiting arrays returning `undefined` for an undefined index, we can
	  // rely exclusively on `insertBefore(node, null)` instead of also using
	  // `appendChild(node)`. However, using `undefined` is not allowed by all
	  // browsers so we must replace it with `null`.
	  parentNode.insertBefore(
	    childNode,
	    parentNode.childNodes[index] || null
	  );
	}

	/**
	 * Operations for updating with DOM children.
	 */
	var DOMChildrenOperations = {

	  dangerouslyReplaceNodeWithMarkup: Danger.dangerouslyReplaceNodeWithMarkup,

	  updateTextContent: setTextContent,

	  /**
	   * Updates a component's children by processing a series of updates. The
	   * update configurations are each expected to have a `parentNode` property.
	   *
	   * @param {array<object>} updates List of update configurations.
	   * @param {array<string>} markupList List of markup strings.
	   * @internal
	   */
	  processUpdates: function(updates, markupList) {
	    var update;
	    // Mapping from parent IDs to initial child orderings.
	    var initialChildren = null;
	    // List of children that will be moved or removed.
	    var updatedChildren = null;

	    for (var i = 0; i < updates.length; i++) {
	      update = updates[i];
	      if (update.type === ReactMultiChildUpdateTypes.MOVE_EXISTING ||
	          update.type === ReactMultiChildUpdateTypes.REMOVE_NODE) {
	        var updatedIndex = update.fromIndex;
	        var updatedChild = update.parentNode.childNodes[updatedIndex];
	        var parentID = update.parentID;

	        ("production" !== process.env.NODE_ENV ? invariant(
	          updatedChild,
	          'processUpdates(): Unable to find child %s of element. This ' +
	          'probably means the DOM was unexpectedly mutated (e.g., by the ' +
	          'browser), usually due to forgetting a <tbody> when using tables, ' +
	          'nesting tags like <form>, <p>, or <a>, or using non-SVG elements ' +
	          'in an <svg> parent. Try inspecting the child nodes of the element ' +
	          'with React ID `%s`.',
	          updatedIndex,
	          parentID
	        ) : invariant(updatedChild));

	        initialChildren = initialChildren || {};
	        initialChildren[parentID] = initialChildren[parentID] || [];
	        initialChildren[parentID][updatedIndex] = updatedChild;

	        updatedChildren = updatedChildren || [];
	        updatedChildren.push(updatedChild);
	      }
	    }

	    var renderedMarkup = Danger.dangerouslyRenderMarkup(markupList);

	    // Remove updated children first so that `toIndex` is consistent.
	    if (updatedChildren) {
	      for (var j = 0; j < updatedChildren.length; j++) {
	        updatedChildren[j].parentNode.removeChild(updatedChildren[j]);
	      }
	    }

	    for (var k = 0; k < updates.length; k++) {
	      update = updates[k];
	      switch (update.type) {
	        case ReactMultiChildUpdateTypes.INSERT_MARKUP:
	          insertChildAt(
	            update.parentNode,
	            renderedMarkup[update.markupIndex],
	            update.toIndex
	          );
	          break;
	        case ReactMultiChildUpdateTypes.MOVE_EXISTING:
	          insertChildAt(
	            update.parentNode,
	            initialChildren[update.parentID][update.fromIndex],
	            update.toIndex
	          );
	          break;
	        case ReactMultiChildUpdateTypes.TEXT_CONTENT:
	          setTextContent(
	            update.parentNode,
	            update.textContent
	          );
	          break;
	        case ReactMultiChildUpdateTypes.REMOVE_NODE:
	          // Already removed by the for-loop above.
	          break;
	      }
	    }
	  }

	};

	module.exports = DOMChildrenOperations;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 60 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule Danger
	 * @typechecks static-only
	 */

	/*jslint evil: true, sub: true */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(52);

	var createNodesFromMarkup = __webpack_require__(61);
	var emptyFunction = __webpack_require__(17);
	var getMarkupWrap = __webpack_require__(64);
	var invariant = __webpack_require__(8);

	var OPEN_TAG_NAME_EXP = /^(<[^ \/>]+)/;
	var RESULT_INDEX_ATTR = 'data-danger-index';

	/**
	 * Extracts the `nodeName` from a string of markup.
	 *
	 * NOTE: Extracting the `nodeName` does not require a regular expression match
	 * because we make assumptions about React-generated markup (i.e. there are no
	 * spaces surrounding the opening tag and there is at least one attribute).
	 *
	 * @param {string} markup String of markup.
	 * @return {string} Node name of the supplied markup.
	 * @see http://jsperf.com/extract-nodename
	 */
	function getNodeName(markup) {
	  return markup.substring(1, markup.indexOf(' '));
	}

	var Danger = {

	  /**
	   * Renders markup into an array of nodes. The markup is expected to render
	   * into a list of root nodes. Also, the length of `resultList` and
	   * `markupList` should be the same.
	   *
	   * @param {array<string>} markupList List of markup strings to render.
	   * @return {array<DOMElement>} List of rendered nodes.
	   * @internal
	   */
	  dangerouslyRenderMarkup: function(markupList) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      ExecutionEnvironment.canUseDOM,
	      'dangerouslyRenderMarkup(...): Cannot render markup in a worker ' +
	      'thread. Make sure `window` and `document` are available globally ' +
	      'before requiring React when unit testing or use ' +
	      'React.renderToString for server rendering.'
	    ) : invariant(ExecutionEnvironment.canUseDOM));
	    var nodeName;
	    var markupByNodeName = {};
	    // Group markup by `nodeName` if a wrap is necessary, else by '*'.
	    for (var i = 0; i < markupList.length; i++) {
	      ("production" !== process.env.NODE_ENV ? invariant(
	        markupList[i],
	        'dangerouslyRenderMarkup(...): Missing markup.'
	      ) : invariant(markupList[i]));
	      nodeName = getNodeName(markupList[i]);
	      nodeName = getMarkupWrap(nodeName) ? nodeName : '*';
	      markupByNodeName[nodeName] = markupByNodeName[nodeName] || [];
	      markupByNodeName[nodeName][i] = markupList[i];
	    }
	    var resultList = [];
	    var resultListAssignmentCount = 0;
	    for (nodeName in markupByNodeName) {
	      if (!markupByNodeName.hasOwnProperty(nodeName)) {
	        continue;
	      }
	      var markupListByNodeName = markupByNodeName[nodeName];

	      // This for-in loop skips the holes of the sparse array. The order of
	      // iteration should follow the order of assignment, which happens to match
	      // numerical index order, but we don't rely on that.
	      var resultIndex;
	      for (resultIndex in markupListByNodeName) {
	        if (markupListByNodeName.hasOwnProperty(resultIndex)) {
	          var markup = markupListByNodeName[resultIndex];

	          // Push the requested markup with an additional RESULT_INDEX_ATTR
	          // attribute.  If the markup does not start with a < character, it
	          // will be discarded below (with an appropriate console.error).
	          markupListByNodeName[resultIndex] = markup.replace(
	            OPEN_TAG_NAME_EXP,
	            // This index will be parsed back out below.
	            '$1 ' + RESULT_INDEX_ATTR + '="' + resultIndex + '" '
	          );
	        }
	      }

	      // Render each group of markup with similar wrapping `nodeName`.
	      var renderNodes = createNodesFromMarkup(
	        markupListByNodeName.join(''),
	        emptyFunction // Do nothing special with <script> tags.
	      );

	      for (var j = 0; j < renderNodes.length; ++j) {
	        var renderNode = renderNodes[j];
	        if (renderNode.hasAttribute &&
	            renderNode.hasAttribute(RESULT_INDEX_ATTR)) {

	          resultIndex = +renderNode.getAttribute(RESULT_INDEX_ATTR);
	          renderNode.removeAttribute(RESULT_INDEX_ATTR);

	          ("production" !== process.env.NODE_ENV ? invariant(
	            !resultList.hasOwnProperty(resultIndex),
	            'Danger: Assigning to an already-occupied result index.'
	          ) : invariant(!resultList.hasOwnProperty(resultIndex)));

	          resultList[resultIndex] = renderNode;

	          // This should match resultList.length and markupList.length when
	          // we're done.
	          resultListAssignmentCount += 1;

	        } else if ("production" !== process.env.NODE_ENV) {
	          console.error(
	            'Danger: Discarding unexpected node:',
	            renderNode
	          );
	        }
	      }
	    }

	    // Although resultList was populated out of order, it should now be a dense
	    // array.
	    ("production" !== process.env.NODE_ENV ? invariant(
	      resultListAssignmentCount === resultList.length,
	      'Danger: Did not assign to every index of resultList.'
	    ) : invariant(resultListAssignmentCount === resultList.length));

	    ("production" !== process.env.NODE_ENV ? invariant(
	      resultList.length === markupList.length,
	      'Danger: Expected markup to render %s nodes, but rendered %s.',
	      markupList.length,
	      resultList.length
	    ) : invariant(resultList.length === markupList.length));

	    return resultList;
	  },

	  /**
	   * Replaces a node with a string of markup at its current position within its
	   * parent. The markup must render into a single root node.
	   *
	   * @param {DOMElement} oldChild Child node to replace.
	   * @param {string} markup Markup to render in place of the child node.
	   * @internal
	   */
	  dangerouslyReplaceNodeWithMarkup: function(oldChild, markup) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      ExecutionEnvironment.canUseDOM,
	      'dangerouslyReplaceNodeWithMarkup(...): Cannot render markup in a ' +
	      'worker thread. Make sure `window` and `document` are available ' +
	      'globally before requiring React when unit testing or use ' +
	      'React.renderToString for server rendering.'
	    ) : invariant(ExecutionEnvironment.canUseDOM));
	    ("production" !== process.env.NODE_ENV ? invariant(markup, 'dangerouslyReplaceNodeWithMarkup(...): Missing markup.') : invariant(markup));
	    ("production" !== process.env.NODE_ENV ? invariant(
	      oldChild.tagName.toLowerCase() !== 'html',
	      'dangerouslyReplaceNodeWithMarkup(...): Cannot replace markup of the ' +
	      '<html> node. This is because browser quirks make this unreliable ' +
	      'and/or slow. If you want to render to the root you must use ' +
	      'server rendering. See React.renderToString().'
	    ) : invariant(oldChild.tagName.toLowerCase() !== 'html'));

	    var newChild = createNodesFromMarkup(markup, emptyFunction)[0];
	    oldChild.parentNode.replaceChild(newChild, oldChild);
	  }

	};

	module.exports = Danger;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 61 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule createNodesFromMarkup
	 * @typechecks
	 */

	/*jslint evil: true, sub: true */

	var ExecutionEnvironment = __webpack_require__(52);

	var createArrayFromMixed = __webpack_require__(62);
	var getMarkupWrap = __webpack_require__(64);
	var invariant = __webpack_require__(8);

	/**
	 * Dummy container used to render all markup.
	 */
	var dummyNode =
	  ExecutionEnvironment.canUseDOM ? document.createElement('div') : null;

	/**
	 * Pattern used by `getNodeName`.
	 */
	var nodeNamePattern = /^\s*<(\w+)/;

	/**
	 * Extracts the `nodeName` of the first element in a string of markup.
	 *
	 * @param {string} markup String of markup.
	 * @return {?string} Node name of the supplied markup.
	 */
	function getNodeName(markup) {
	  var nodeNameMatch = markup.match(nodeNamePattern);
	  return nodeNameMatch && nodeNameMatch[1].toLowerCase();
	}

	/**
	 * Creates an array containing the nodes rendered from the supplied markup. The
	 * optionally supplied `handleScript` function will be invoked once for each
	 * <script> element that is rendered. If no `handleScript` function is supplied,
	 * an exception is thrown if any <script> elements are rendered.
	 *
	 * @param {string} markup A string of valid HTML markup.
	 * @param {?function} handleScript Invoked once for each rendered <script>.
	 * @return {array<DOMElement|DOMTextNode>} An array of rendered nodes.
	 */
	function createNodesFromMarkup(markup, handleScript) {
	  var node = dummyNode;
	  ("production" !== process.env.NODE_ENV ? invariant(!!dummyNode, 'createNodesFromMarkup dummy not initialized') : invariant(!!dummyNode));
	  var nodeName = getNodeName(markup);

	  var wrap = nodeName && getMarkupWrap(nodeName);
	  if (wrap) {
	    node.innerHTML = wrap[1] + markup + wrap[2];

	    var wrapDepth = wrap[0];
	    while (wrapDepth--) {
	      node = node.lastChild;
	    }
	  } else {
	    node.innerHTML = markup;
	  }

	  var scripts = node.getElementsByTagName('script');
	  if (scripts.length) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      handleScript,
	      'createNodesFromMarkup(...): Unexpected <script> element rendered.'
	    ) : invariant(handleScript));
	    createArrayFromMixed(scripts).forEach(handleScript);
	  }

	  var nodes = createArrayFromMixed(node.childNodes);
	  while (node.lastChild) {
	    node.removeChild(node.lastChild);
	  }
	  return nodes;
	}

	module.exports = createNodesFromMarkup;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 62 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule createArrayFromMixed
	 * @typechecks
	 */

	var toArray = __webpack_require__(63);

	/**
	 * Perform a heuristic test to determine if an object is "array-like".
	 *
	 *   A monk asked Joshu, a Zen master, "Has a dog Buddha nature?"
	 *   Joshu replied: "Mu."
	 *
	 * This function determines if its argument has "array nature": it returns
	 * true if the argument is an actual array, an `arguments' object, or an
	 * HTMLCollection (e.g. node.childNodes or node.getElementsByTagName()).
	 *
	 * It will return false for other array-like objects like Filelist.
	 *
	 * @param {*} obj
	 * @return {boolean}
	 */
	function hasArrayNature(obj) {
	  return (
	    // not null/false
	    !!obj &&
	    // arrays are objects, NodeLists are functions in Safari
	    (typeof obj == 'object' || typeof obj == 'function') &&
	    // quacks like an array
	    ('length' in obj) &&
	    // not window
	    !('setInterval' in obj) &&
	    // no DOM node should be considered an array-like
	    // a 'select' element has 'length' and 'item' properties on IE8
	    (typeof obj.nodeType != 'number') &&
	    (
	      // a real array
	      (// HTMLCollection/NodeList
	      (Array.isArray(obj) ||
	      // arguments
	      ('callee' in obj) || 'item' in obj))
	    )
	  );
	}

	/**
	 * Ensure that the argument is an array by wrapping it in an array if it is not.
	 * Creates a copy of the argument if it is already an array.
	 *
	 * This is mostly useful idiomatically:
	 *
	 *   var createArrayFromMixed = require('createArrayFromMixed');
	 *
	 *   function takesOneOrMoreThings(things) {
	 *     things = createArrayFromMixed(things);
	 *     ...
	 *   }
	 *
	 * This allows you to treat `things' as an array, but accept scalars in the API.
	 *
	 * If you need to convert an array-like object, like `arguments`, into an array
	 * use toArray instead.
	 *
	 * @param {*} obj
	 * @return {array}
	 */
	function createArrayFromMixed(obj) {
	  if (!hasArrayNature(obj)) {
	    return [obj];
	  } else if (Array.isArray(obj)) {
	    return obj.slice();
	  } else {
	    return toArray(obj);
	  }
	}

	module.exports = createArrayFromMixed;


/***/ },
/* 63 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule toArray
	 * @typechecks
	 */

	var invariant = __webpack_require__(8);

	/**
	 * Convert array-like objects to arrays.
	 *
	 * This API assumes the caller knows the contents of the data type. For less
	 * well defined inputs use createArrayFromMixed.
	 *
	 * @param {object|function|filelist} obj
	 * @return {array}
	 */
	function toArray(obj) {
	  var length = obj.length;

	  // Some browse builtin objects can report typeof 'function' (e.g. NodeList in
	  // old versions of Safari).
	  ("production" !== process.env.NODE_ENV ? invariant(
	    !Array.isArray(obj) &&
	    (typeof obj === 'object' || typeof obj === 'function'),
	    'toArray: Array-like object expected'
	  ) : invariant(!Array.isArray(obj) &&
	  (typeof obj === 'object' || typeof obj === 'function')));

	  ("production" !== process.env.NODE_ENV ? invariant(
	    typeof length === 'number',
	    'toArray: Object needs a length property'
	  ) : invariant(typeof length === 'number'));

	  ("production" !== process.env.NODE_ENV ? invariant(
	    length === 0 ||
	    (length - 1) in obj,
	    'toArray: Object should have keys for indices'
	  ) : invariant(length === 0 ||
	  (length - 1) in obj));

	  // Old IE doesn't give collections access to hasOwnProperty. Assume inputs
	  // without method will throw during the slice call and skip straight to the
	  // fallback.
	  if (obj.hasOwnProperty) {
	    try {
	      return Array.prototype.slice.call(obj);
	    } catch (e) {
	      // IE < 9 does not support Array#slice on collections objects
	    }
	  }

	  // Fall back to copying key by key. This assumes all keys have a value,
	  // so will not preserve sparsely populated inputs.
	  var ret = Array(length);
	  for (var ii = 0; ii < length; ii++) {
	    ret[ii] = obj[ii];
	  }
	  return ret;
	}

	module.exports = toArray;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 64 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getMarkupWrap
	 */

	var ExecutionEnvironment = __webpack_require__(52);

	var invariant = __webpack_require__(8);

	/**
	 * Dummy container used to detect which wraps are necessary.
	 */
	var dummyNode =
	  ExecutionEnvironment.canUseDOM ? document.createElement('div') : null;

	/**
	 * Some browsers cannot use `innerHTML` to render certain elements standalone,
	 * so we wrap them, render the wrapped nodes, then extract the desired node.
	 *
	 * In IE8, certain elements cannot render alone, so wrap all elements ('*').
	 */
	var shouldWrap = {
	  // Force wrapping for SVG elements because if they get created inside a <div>,
	  // they will be initialized in the wrong namespace (and will not display).
	  'circle': true,
	  'clipPath': true,
	  'defs': true,
	  'ellipse': true,
	  'g': true,
	  'line': true,
	  'linearGradient': true,
	  'path': true,
	  'polygon': true,
	  'polyline': true,
	  'radialGradient': true,
	  'rect': true,
	  'stop': true,
	  'text': true
	};

	var selectWrap = [1, '<select multiple="true">', '</select>'];
	var tableWrap = [1, '<table>', '</table>'];
	var trWrap = [3, '<table><tbody><tr>', '</tr></tbody></table>'];

	var svgWrap = [1, '<svg>', '</svg>'];

	var markupWrap = {
	  '*': [1, '?<div>', '</div>'],

	  'area': [1, '<map>', '</map>'],
	  'col': [2, '<table><tbody></tbody><colgroup>', '</colgroup></table>'],
	  'legend': [1, '<fieldset>', '</fieldset>'],
	  'param': [1, '<object>', '</object>'],
	  'tr': [2, '<table><tbody>', '</tbody></table>'],

	  'optgroup': selectWrap,
	  'option': selectWrap,

	  'caption': tableWrap,
	  'colgroup': tableWrap,
	  'tbody': tableWrap,
	  'tfoot': tableWrap,
	  'thead': tableWrap,

	  'td': trWrap,
	  'th': trWrap,

	  'circle': svgWrap,
	  'clipPath': svgWrap,
	  'defs': svgWrap,
	  'ellipse': svgWrap,
	  'g': svgWrap,
	  'line': svgWrap,
	  'linearGradient': svgWrap,
	  'path': svgWrap,
	  'polygon': svgWrap,
	  'polyline': svgWrap,
	  'radialGradient': svgWrap,
	  'rect': svgWrap,
	  'stop': svgWrap,
	  'text': svgWrap
	};

	/**
	 * Gets the markup wrap configuration for the supplied `nodeName`.
	 *
	 * NOTE: This lazily detects which wraps are necessary for the current browser.
	 *
	 * @param {string} nodeName Lowercase `nodeName`.
	 * @return {?array} Markup wrap configuration, if applicable.
	 */
	function getMarkupWrap(nodeName) {
	  ("production" !== process.env.NODE_ENV ? invariant(!!dummyNode, 'Markup wrapping node not initialized') : invariant(!!dummyNode));
	  if (!markupWrap.hasOwnProperty(nodeName)) {
	    nodeName = '*';
	  }
	  if (!shouldWrap.hasOwnProperty(nodeName)) {
	    if (nodeName === '*') {
	      dummyNode.innerHTML = '<link />';
	    } else {
	      dummyNode.innerHTML = '<' + nodeName + '></' + nodeName + '>';
	    }
	    shouldWrap[nodeName] = !dummyNode.firstChild;
	  }
	  return shouldWrap[nodeName] ? markupWrap[nodeName] : null;
	}


	module.exports = getMarkupWrap;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 65 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactMultiChildUpdateTypes
	 */

	'use strict';

	var keyMirror = __webpack_require__(7);

	/**
	 * When a component's children are updated, a series of update configuration
	 * objects are created in order to batch and serialize the required changes.
	 *
	 * Enumerates all the possible types of update configurations.
	 *
	 * @internal
	 */
	var ReactMultiChildUpdateTypes = keyMirror({
	  INSERT_MARKUP: null,
	  MOVE_EXISTING: null,
	  REMOVE_NODE: null,
	  TEXT_CONTENT: null
	});

	module.exports = ReactMultiChildUpdateTypes;


/***/ },
/* 66 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule setTextContent
	 */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(52);
	var escapeTextContentForBrowser = __webpack_require__(47);
	var setInnerHTML = __webpack_require__(67);

	/**
	 * Set the textContent property of a node, ensuring that whitespace is preserved
	 * even in IE8. innerText is a poor substitute for textContent and, among many
	 * issues, inserts <br> instead of the literal newline chars. innerHTML behaves
	 * as it should.
	 *
	 * @param {DOMElement} node
	 * @param {string} text
	 * @internal
	 */
	var setTextContent = function(node, text) {
	  node.textContent = text;
	};

	if (ExecutionEnvironment.canUseDOM) {
	  if (!('textContent' in document.documentElement)) {
	    setTextContent = function(node, text) {
	      setInnerHTML(node, escapeTextContentForBrowser(text));
	    };
	  }
	}

	module.exports = setTextContent;


/***/ },
/* 67 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule setInnerHTML
	 */

	/* globals MSApp */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(52);

	var WHITESPACE_TEST = /^[ \r\n\t\f]/;
	var NONVISIBLE_TEST = /<(!--|link|noscript|meta|script|style)[ \r\n\t\f\/>]/;

	/**
	 * Set the innerHTML property of a node, ensuring that whitespace is preserved
	 * even in IE8.
	 *
	 * @param {DOMElement} node
	 * @param {string} html
	 * @internal
	 */
	var setInnerHTML = function(node, html) {
	  node.innerHTML = html;
	};

	// Win8 apps: Allow all html to be inserted
	if (typeof MSApp !== 'undefined' && MSApp.execUnsafeLocalFunction) {
	  setInnerHTML = function(node, html) {
	    MSApp.execUnsafeLocalFunction(function() {
	      node.innerHTML = html;
	    });
	  };
	}

	if (ExecutionEnvironment.canUseDOM) {
	  // IE8: When updating a just created node with innerHTML only leading
	  // whitespace is removed. When updating an existing node with innerHTML
	  // whitespace in root TextNodes is also collapsed.
	  // @see quirksmode.org/bugreports/archives/2004/11/innerhtml_and_t.html

	  // Feature detection; only IE8 is known to behave improperly like this.
	  var testElement = document.createElement('div');
	  testElement.innerHTML = ' ';
	  if (testElement.innerHTML === '') {
	    setInnerHTML = function(node, html) {
	      // Magic theory: IE8 supposedly differentiates between added and updated
	      // nodes when processing innerHTML, innerHTML on updated nodes suffers
	      // from worse whitespace behavior. Re-adding a node like this triggers
	      // the initial and more favorable whitespace behavior.
	      // TODO: What to do on a detached node?
	      if (node.parentNode) {
	        node.parentNode.replaceChild(node, node);
	      }

	      // We also implement a workaround for non-visible tags disappearing into
	      // thin air on IE8, this only happens if there is no visible text
	      // in-front of the non-visible tags. Piggyback on the whitespace fix
	      // and simply check if any non-visible tags appear in the source.
	      if (WHITESPACE_TEST.test(html) ||
	          html[0] === '<' && NONVISIBLE_TEST.test(html)) {
	        // Recover leading whitespace by temporarily prepending any character.
	        // \uFEFF has the potential advantage of being zero-width/invisible.
	        node.innerHTML = '\uFEFF' + html;

	        // deleteData leaves an empty `TextNode` which offsets the index of all
	        // children. Definitely want to avoid this.
	        var textNode = node.firstChild;
	        if (textNode.data.length === 1) {
	          node.removeChild(textNode);
	        } else {
	          textNode.deleteData(0, 1);
	        }
	      } else {
	        node.innerHTML = html;
	      }
	    };
	  }
	}

	module.exports = setInnerHTML;


/***/ },
/* 68 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactMount
	 */

	'use strict';

	var DOMProperty = __webpack_require__(45);
	var ReactBrowserEventEmitter = __webpack_require__(69);
	var ReactCurrentOwner = __webpack_require__(18);
	var ReactElement = __webpack_require__(12);
	var ReactElementValidator = __webpack_require__(31);
	var ReactEmptyComponent = __webpack_require__(77);
	var ReactInstanceHandles = __webpack_require__(21);
	var ReactInstanceMap = __webpack_require__(37);
	var ReactMarkupChecksum = __webpack_require__(78);
	var ReactPerf = __webpack_require__(27);
	var ReactReconciler = __webpack_require__(28);
	var ReactUpdateQueue = __webpack_require__(24);
	var ReactUpdates = __webpack_require__(25);

	var emptyObject = __webpack_require__(15);
	var containsNode = __webpack_require__(80);
	var getReactRootElementInContainer = __webpack_require__(83);
	var instantiateReactComponent = __webpack_require__(84);
	var invariant = __webpack_require__(8);
	var setInnerHTML = __webpack_require__(67);
	var shouldUpdateReactComponent = __webpack_require__(87);
	var warning = __webpack_require__(16);

	var SEPARATOR = ReactInstanceHandles.SEPARATOR;

	var ATTR_NAME = DOMProperty.ID_ATTRIBUTE_NAME;
	var nodeCache = {};

	var ELEMENT_NODE_TYPE = 1;
	var DOC_NODE_TYPE = 9;

	/** Mapping from reactRootID to React component instance. */
	var instancesByReactRootID = {};

	/** Mapping from reactRootID to `container` nodes. */
	var containersByReactRootID = {};

	if ("production" !== process.env.NODE_ENV) {
	  /** __DEV__-only mapping from reactRootID to root elements. */
	  var rootElementsByReactRootID = {};
	}

	// Used to store breadth-first search state in findComponentRoot.
	var findComponentRootReusableArray = [];

	/**
	 * Finds the index of the first character
	 * that's not common between the two given strings.
	 *
	 * @return {number} the index of the character where the strings diverge
	 */
	function firstDifferenceIndex(string1, string2) {
	  var minLen = Math.min(string1.length, string2.length);
	  for (var i = 0; i < minLen; i++) {
	    if (string1.charAt(i) !== string2.charAt(i)) {
	      return i;
	    }
	  }
	  return string1.length === string2.length ? -1 : minLen;
	}

	/**
	 * @param {DOMElement} container DOM element that may contain a React component.
	 * @return {?string} A "reactRoot" ID, if a React component is rendered.
	 */
	function getReactRootID(container) {
	  var rootElement = getReactRootElementInContainer(container);
	  return rootElement && ReactMount.getID(rootElement);
	}

	/**
	 * Accessing node[ATTR_NAME] or calling getAttribute(ATTR_NAME) on a form
	 * element can return its control whose name or ID equals ATTR_NAME. All
	 * DOM nodes support `getAttributeNode` but this can also get called on
	 * other objects so just return '' if we're given something other than a
	 * DOM node (such as window).
	 *
	 * @param {?DOMElement|DOMWindow|DOMDocument|DOMTextNode} node DOM node.
	 * @return {string} ID of the supplied `domNode`.
	 */
	function getID(node) {
	  var id = internalGetID(node);
	  if (id) {
	    if (nodeCache.hasOwnProperty(id)) {
	      var cached = nodeCache[id];
	      if (cached !== node) {
	        ("production" !== process.env.NODE_ENV ? invariant(
	          !isValid(cached, id),
	          'ReactMount: Two valid but unequal nodes with the same `%s`: %s',
	          ATTR_NAME, id
	        ) : invariant(!isValid(cached, id)));

	        nodeCache[id] = node;
	      }
	    } else {
	      nodeCache[id] = node;
	    }
	  }

	  return id;
	}

	function internalGetID(node) {
	  // If node is something like a window, document, or text node, none of
	  // which support attributes or a .getAttribute method, gracefully return
	  // the empty string, as if the attribute were missing.
	  return node && node.getAttribute && node.getAttribute(ATTR_NAME) || '';
	}

	/**
	 * Sets the React-specific ID of the given node.
	 *
	 * @param {DOMElement} node The DOM node whose ID will be set.
	 * @param {string} id The value of the ID attribute.
	 */
	function setID(node, id) {
	  var oldID = internalGetID(node);
	  if (oldID !== id) {
	    delete nodeCache[oldID];
	  }
	  node.setAttribute(ATTR_NAME, id);
	  nodeCache[id] = node;
	}

	/**
	 * Finds the node with the supplied React-generated DOM ID.
	 *
	 * @param {string} id A React-generated DOM ID.
	 * @return {DOMElement} DOM node with the suppled `id`.
	 * @internal
	 */
	function getNode(id) {
	  if (!nodeCache.hasOwnProperty(id) || !isValid(nodeCache[id], id)) {
	    nodeCache[id] = ReactMount.findReactNodeByID(id);
	  }
	  return nodeCache[id];
	}

	/**
	 * Finds the node with the supplied public React instance.
	 *
	 * @param {*} instance A public React instance.
	 * @return {?DOMElement} DOM node with the suppled `id`.
	 * @internal
	 */
	function getNodeFromInstance(instance) {
	  var id = ReactInstanceMap.get(instance)._rootNodeID;
	  if (ReactEmptyComponent.isNullComponentID(id)) {
	    return null;
	  }
	  if (!nodeCache.hasOwnProperty(id) || !isValid(nodeCache[id], id)) {
	    nodeCache[id] = ReactMount.findReactNodeByID(id);
	  }
	  return nodeCache[id];
	}

	/**
	 * A node is "valid" if it is contained by a currently mounted container.
	 *
	 * This means that the node does not have to be contained by a document in
	 * order to be considered valid.
	 *
	 * @param {?DOMElement} node The candidate DOM node.
	 * @param {string} id The expected ID of the node.
	 * @return {boolean} Whether the node is contained by a mounted container.
	 */
	function isValid(node, id) {
	  if (node) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      internalGetID(node) === id,
	      'ReactMount: Unexpected modification of `%s`',
	      ATTR_NAME
	    ) : invariant(internalGetID(node) === id));

	    var container = ReactMount.findReactContainerForID(id);
	    if (container && containsNode(container, node)) {
	      return true;
	    }
	  }

	  return false;
	}

	/**
	 * Causes the cache to forget about one React-specific ID.
	 *
	 * @param {string} id The ID to forget.
	 */
	function purgeID(id) {
	  delete nodeCache[id];
	}

	var deepestNodeSoFar = null;
	function findDeepestCachedAncestorImpl(ancestorID) {
	  var ancestor = nodeCache[ancestorID];
	  if (ancestor && isValid(ancestor, ancestorID)) {
	    deepestNodeSoFar = ancestor;
	  } else {
	    // This node isn't populated in the cache, so presumably none of its
	    // descendants are. Break out of the loop.
	    return false;
	  }
	}

	/**
	 * Return the deepest cached node whose ID is a prefix of `targetID`.
	 */
	function findDeepestCachedAncestor(targetID) {
	  deepestNodeSoFar = null;
	  ReactInstanceHandles.traverseAncestors(
	    targetID,
	    findDeepestCachedAncestorImpl
	  );

	  var foundNode = deepestNodeSoFar;
	  deepestNodeSoFar = null;
	  return foundNode;
	}

	/**
	 * Mounts this component and inserts it into the DOM.
	 *
	 * @param {ReactComponent} componentInstance The instance to mount.
	 * @param {string} rootID DOM ID of the root node.
	 * @param {DOMElement} container DOM element to mount into.
	 * @param {ReactReconcileTransaction} transaction
	 * @param {boolean} shouldReuseMarkup If true, do not insert markup
	 */
	function mountComponentIntoNode(
	    componentInstance,
	    rootID,
	    container,
	    transaction,
	    shouldReuseMarkup) {
	  var markup = ReactReconciler.mountComponent(
	    componentInstance, rootID, transaction, emptyObject
	  );
	  componentInstance._isTopLevel = true;
	  ReactMount._mountImageIntoNode(markup, container, shouldReuseMarkup);
	}

	/**
	 * Batched mount.
	 *
	 * @param {ReactComponent} componentInstance The instance to mount.
	 * @param {string} rootID DOM ID of the root node.
	 * @param {DOMElement} container DOM element to mount into.
	 * @param {boolean} shouldReuseMarkup If true, do not insert markup
	 */
	function batchedMountComponentIntoNode(
	    componentInstance,
	    rootID,
	    container,
	    shouldReuseMarkup) {
	  var transaction = ReactUpdates.ReactReconcileTransaction.getPooled();
	  transaction.perform(
	    mountComponentIntoNode,
	    null,
	    componentInstance,
	    rootID,
	    container,
	    transaction,
	    shouldReuseMarkup
	  );
	  ReactUpdates.ReactReconcileTransaction.release(transaction);
	}

	/**
	 * Mounting is the process of initializing a React component by creating its
	 * representative DOM elements and inserting them into a supplied `container`.
	 * Any prior content inside `container` is destroyed in the process.
	 *
	 *   ReactMount.render(
	 *     component,
	 *     document.getElementById('container')
	 *   );
	 *
	 *   <div id="container">                   <-- Supplied `container`.
	 *     <div data-reactid=".3">              <-- Rendered reactRoot of React
	 *       // ...                                 component.
	 *     </div>
	 *   </div>
	 *
	 * Inside of `container`, the first element rendered is the "reactRoot".
	 */
	var ReactMount = {
	  /** Exposed for debugging purposes **/
	  _instancesByReactRootID: instancesByReactRootID,

	  /**
	   * This is a hook provided to support rendering React components while
	   * ensuring that the apparent scroll position of its `container` does not
	   * change.
	   *
	   * @param {DOMElement} container The `container` being rendered into.
	   * @param {function} renderCallback This must be called once to do the render.
	   */
	  scrollMonitor: function(container, renderCallback) {
	    renderCallback();
	  },

	  /**
	   * Take a component that's already mounted into the DOM and replace its props
	   * @param {ReactComponent} prevComponent component instance already in the DOM
	   * @param {ReactElement} nextElement component instance to render
	   * @param {DOMElement} container container to render into
	   * @param {?function} callback function triggered on completion
	   */
	  _updateRootComponent: function(
	      prevComponent,
	      nextElement,
	      container,
	      callback) {
	    if ("production" !== process.env.NODE_ENV) {
	      ReactElementValidator.checkAndWarnForMutatedProps(nextElement);
	    }

	    ReactMount.scrollMonitor(container, function() {
	      ReactUpdateQueue.enqueueElementInternal(prevComponent, nextElement);
	      if (callback) {
	        ReactUpdateQueue.enqueueCallbackInternal(prevComponent, callback);
	      }
	    });

	    if ("production" !== process.env.NODE_ENV) {
	      // Record the root element in case it later gets transplanted.
	      rootElementsByReactRootID[getReactRootID(container)] =
	        getReactRootElementInContainer(container);
	    }

	    return prevComponent;
	  },

	  /**
	   * Register a component into the instance map and starts scroll value
	   * monitoring
	   * @param {ReactComponent} nextComponent component instance to render
	   * @param {DOMElement} container container to render into
	   * @return {string} reactRoot ID prefix
	   */
	  _registerComponent: function(nextComponent, container) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      container && (
	        (container.nodeType === ELEMENT_NODE_TYPE || container.nodeType === DOC_NODE_TYPE)
	      ),
	      '_registerComponent(...): Target container is not a DOM element.'
	    ) : invariant(container && (
	      (container.nodeType === ELEMENT_NODE_TYPE || container.nodeType === DOC_NODE_TYPE)
	    )));

	    ReactBrowserEventEmitter.ensureScrollValueMonitoring();

	    var reactRootID = ReactMount.registerContainer(container);
	    instancesByReactRootID[reactRootID] = nextComponent;
	    return reactRootID;
	  },

	  /**
	   * Render a new component into the DOM.
	   * @param {ReactElement} nextElement element to render
	   * @param {DOMElement} container container to render into
	   * @param {boolean} shouldReuseMarkup if we should skip the markup insertion
	   * @return {ReactComponent} nextComponent
	   */
	  _renderNewRootComponent: function(
	    nextElement,
	    container,
	    shouldReuseMarkup
	  ) {
	    // Various parts of our code (such as ReactCompositeComponent's
	    // _renderValidatedComponent) assume that calls to render aren't nested;
	    // verify that that's the case.
	    ("production" !== process.env.NODE_ENV ? warning(
	      ReactCurrentOwner.current == null,
	      '_renderNewRootComponent(): Render methods should be a pure function ' +
	      'of props and state; triggering nested component updates from ' +
	      'render is not allowed. If necessary, trigger nested updates in ' +
	      'componentDidUpdate.'
	    ) : null);

	    var componentInstance = instantiateReactComponent(nextElement, null);
	    var reactRootID = ReactMount._registerComponent(
	      componentInstance,
	      container
	    );

	    // The initial render is synchronous but any updates that happen during
	    // rendering, in componentWillMount or componentDidMount, will be batched
	    // according to the current batching strategy.

	    ReactUpdates.batchedUpdates(
	      batchedMountComponentIntoNode,
	      componentInstance,
	      reactRootID,
	      container,
	      shouldReuseMarkup
	    );

	    if ("production" !== process.env.NODE_ENV) {
	      // Record the root element in case it later gets transplanted.
	      rootElementsByReactRootID[reactRootID] =
	        getReactRootElementInContainer(container);
	    }

	    return componentInstance;
	  },

	  /**
	   * Renders a React component into the DOM in the supplied `container`.
	   *
	   * If the React component was previously rendered into `container`, this will
	   * perform an update on it and only mutate the DOM as necessary to reflect the
	   * latest React component.
	   *
	   * @param {ReactElement} nextElement Component element to render.
	   * @param {DOMElement} container DOM element to render into.
	   * @param {?function} callback function triggered on completion
	   * @return {ReactComponent} Component instance rendered in `container`.
	   */
	  render: function(nextElement, container, callback) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      ReactElement.isValidElement(nextElement),
	      'React.render(): Invalid component element.%s',
	      (
	        typeof nextElement === 'string' ?
	          ' Instead of passing an element string, make sure to instantiate ' +
	          'it by passing it to React.createElement.' :
	        typeof nextElement === 'function' ?
	          ' Instead of passing a component class, make sure to instantiate ' +
	          'it by passing it to React.createElement.' :
	        // Check if it quacks like an element
	        nextElement != null && nextElement.props !== undefined ?
	          ' This may be caused by unintentionally loading two independent ' +
	          'copies of React.' :
	          ''
	      )
	    ) : invariant(ReactElement.isValidElement(nextElement)));

	    var prevComponent = instancesByReactRootID[getReactRootID(container)];

	    if (prevComponent) {
	      var prevElement = prevComponent._currentElement;
	      if (shouldUpdateReactComponent(prevElement, nextElement)) {
	        return ReactMount._updateRootComponent(
	          prevComponent,
	          nextElement,
	          container,
	          callback
	        ).getPublicInstance();
	      } else {
	        ReactMount.unmountComponentAtNode(container);
	      }
	    }

	    var reactRootElement = getReactRootElementInContainer(container);
	    var containerHasReactMarkup =
	      reactRootElement && ReactMount.isRenderedByReact(reactRootElement);

	    if ("production" !== process.env.NODE_ENV) {
	      if (!containerHasReactMarkup || reactRootElement.nextSibling) {
	        var rootElementSibling = reactRootElement;
	        while (rootElementSibling) {
	          if (ReactMount.isRenderedByReact(rootElementSibling)) {
	            ("production" !== process.env.NODE_ENV ? warning(
	              false,
	              'render(): Target node has markup rendered by React, but there ' +
	              'are unrelated nodes as well. This is most commonly caused by ' +
	              'white-space inserted around server-rendered markup.'
	            ) : null);
	            break;
	          }

	          rootElementSibling = rootElementSibling.nextSibling;
	        }
	      }
	    }

	    var shouldReuseMarkup = containerHasReactMarkup && !prevComponent;

	    var component = ReactMount._renderNewRootComponent(
	      nextElement,
	      container,
	      shouldReuseMarkup
	    ).getPublicInstance();
	    if (callback) {
	      callback.call(component);
	    }
	    return component;
	  },

	  /**
	   * Constructs a component instance of `constructor` with `initialProps` and
	   * renders it into the supplied `container`.
	   *
	   * @param {function} constructor React component constructor.
	   * @param {?object} props Initial props of the component instance.
	   * @param {DOMElement} container DOM element to render into.
	   * @return {ReactComponent} Component instance rendered in `container`.
	   */
	  constructAndRenderComponent: function(constructor, props, container) {
	    var element = ReactElement.createElement(constructor, props);
	    return ReactMount.render(element, container);
	  },

	  /**
	   * Constructs a component instance of `constructor` with `initialProps` and
	   * renders it into a container node identified by supplied `id`.
	   *
	   * @param {function} componentConstructor React component constructor
	   * @param {?object} props Initial props of the component instance.
	   * @param {string} id ID of the DOM element to render into.
	   * @return {ReactComponent} Component instance rendered in the container node.
	   */
	  constructAndRenderComponentByID: function(constructor, props, id) {
	    var domNode = document.getElementById(id);
	    ("production" !== process.env.NODE_ENV ? invariant(
	      domNode,
	      'Tried to get element with id of "%s" but it is not present on the page.',
	      id
	    ) : invariant(domNode));
	    return ReactMount.constructAndRenderComponent(constructor, props, domNode);
	  },

	  /**
	   * Registers a container node into which React components will be rendered.
	   * This also creates the "reactRoot" ID that will be assigned to the element
	   * rendered within.
	   *
	   * @param {DOMElement} container DOM element to register as a container.
	   * @return {string} The "reactRoot" ID of elements rendered within.
	   */
	  registerContainer: function(container) {
	    var reactRootID = getReactRootID(container);
	    if (reactRootID) {
	      // If one exists, make sure it is a valid "reactRoot" ID.
	      reactRootID = ReactInstanceHandles.getReactRootIDFromNodeID(reactRootID);
	    }
	    if (!reactRootID) {
	      // No valid "reactRoot" ID found, create one.
	      reactRootID = ReactInstanceHandles.createReactRootID();
	    }
	    containersByReactRootID[reactRootID] = container;
	    return reactRootID;
	  },

	  /**
	   * Unmounts and destroys the React component rendered in the `container`.
	   *
	   * @param {DOMElement} container DOM element containing a React component.
	   * @return {boolean} True if a component was found in and unmounted from
	   *                   `container`
	   */
	  unmountComponentAtNode: function(container) {
	    // Various parts of our code (such as ReactCompositeComponent's
	    // _renderValidatedComponent) assume that calls to render aren't nested;
	    // verify that that's the case. (Strictly speaking, unmounting won't cause a
	    // render but we still don't expect to be in a render call here.)
	    ("production" !== process.env.NODE_ENV ? warning(
	      ReactCurrentOwner.current == null,
	      'unmountComponentAtNode(): Render methods should be a pure function of ' +
	      'props and state; triggering nested component updates from render is ' +
	      'not allowed. If necessary, trigger nested updates in ' +
	      'componentDidUpdate.'
	    ) : null);

	    ("production" !== process.env.NODE_ENV ? invariant(
	      container && (
	        (container.nodeType === ELEMENT_NODE_TYPE || container.nodeType === DOC_NODE_TYPE)
	      ),
	      'unmountComponentAtNode(...): Target container is not a DOM element.'
	    ) : invariant(container && (
	      (container.nodeType === ELEMENT_NODE_TYPE || container.nodeType === DOC_NODE_TYPE)
	    )));

	    var reactRootID = getReactRootID(container);
	    var component = instancesByReactRootID[reactRootID];
	    if (!component) {
	      return false;
	    }
	    ReactMount.unmountComponentFromNode(component, container);
	    delete instancesByReactRootID[reactRootID];
	    delete containersByReactRootID[reactRootID];
	    if ("production" !== process.env.NODE_ENV) {
	      delete rootElementsByReactRootID[reactRootID];
	    }
	    return true;
	  },

	  /**
	   * Unmounts a component and removes it from the DOM.
	   *
	   * @param {ReactComponent} instance React component instance.
	   * @param {DOMElement} container DOM element to unmount from.
	   * @final
	   * @internal
	   * @see {ReactMount.unmountComponentAtNode}
	   */
	  unmountComponentFromNode: function(instance, container) {
	    ReactReconciler.unmountComponent(instance);

	    if (container.nodeType === DOC_NODE_TYPE) {
	      container = container.documentElement;
	    }

	    // http://jsperf.com/emptying-a-node
	    while (container.lastChild) {
	      container.removeChild(container.lastChild);
	    }
	  },

	  /**
	   * Finds the container DOM element that contains React component to which the
	   * supplied DOM `id` belongs.
	   *
	   * @param {string} id The ID of an element rendered by a React component.
	   * @return {?DOMElement} DOM element that contains the `id`.
	   */
	  findReactContainerForID: function(id) {
	    var reactRootID = ReactInstanceHandles.getReactRootIDFromNodeID(id);
	    var container = containersByReactRootID[reactRootID];

	    if ("production" !== process.env.NODE_ENV) {
	      var rootElement = rootElementsByReactRootID[reactRootID];
	      if (rootElement && rootElement.parentNode !== container) {
	        ("production" !== process.env.NODE_ENV ? invariant(
	          // Call internalGetID here because getID calls isValid which calls
	          // findReactContainerForID (this function).
	          internalGetID(rootElement) === reactRootID,
	          'ReactMount: Root element ID differed from reactRootID.'
	        ) : invariant(// Call internalGetID here because getID calls isValid which calls
	        // findReactContainerForID (this function).
	        internalGetID(rootElement) === reactRootID));

	        var containerChild = container.firstChild;
	        if (containerChild &&
	            reactRootID === internalGetID(containerChild)) {
	          // If the container has a new child with the same ID as the old
	          // root element, then rootElementsByReactRootID[reactRootID] is
	          // just stale and needs to be updated. The case that deserves a
	          // warning is when the container is empty.
	          rootElementsByReactRootID[reactRootID] = containerChild;
	        } else {
	          ("production" !== process.env.NODE_ENV ? warning(
	            false,
	            'ReactMount: Root element has been removed from its original ' +
	            'container. New container:', rootElement.parentNode
	          ) : null);
	        }
	      }
	    }

	    return container;
	  },

	  /**
	   * Finds an element rendered by React with the supplied ID.
	   *
	   * @param {string} id ID of a DOM node in the React component.
	   * @return {DOMElement} Root DOM node of the React component.
	   */
	  findReactNodeByID: function(id) {
	    var reactRoot = ReactMount.findReactContainerForID(id);
	    return ReactMount.findComponentRoot(reactRoot, id);
	  },

	  /**
	   * True if the supplied `node` is rendered by React.
	   *
	   * @param {*} node DOM Element to check.
	   * @return {boolean} True if the DOM Element appears to be rendered by React.
	   * @internal
	   */
	  isRenderedByReact: function(node) {
	    if (node.nodeType !== 1) {
	      // Not a DOMElement, therefore not a React component
	      return false;
	    }
	    var id = ReactMount.getID(node);
	    return id ? id.charAt(0) === SEPARATOR : false;
	  },

	  /**
	   * Traverses up the ancestors of the supplied node to find a node that is a
	   * DOM representation of a React component.
	   *
	   * @param {*} node
	   * @return {?DOMEventTarget}
	   * @internal
	   */
	  getFirstReactDOM: function(node) {
	    var current = node;
	    while (current && current.parentNode !== current) {
	      if (ReactMount.isRenderedByReact(current)) {
	        return current;
	      }
	      current = current.parentNode;
	    }
	    return null;
	  },

	  /**
	   * Finds a node with the supplied `targetID` inside of the supplied
	   * `ancestorNode`.  Exploits the ID naming scheme to perform the search
	   * quickly.
	   *
	   * @param {DOMEventTarget} ancestorNode Search from this root.
	   * @pararm {string} targetID ID of the DOM representation of the component.
	   * @return {DOMEventTarget} DOM node with the supplied `targetID`.
	   * @internal
	   */
	  findComponentRoot: function(ancestorNode, targetID) {
	    var firstChildren = findComponentRootReusableArray;
	    var childIndex = 0;

	    var deepestAncestor = findDeepestCachedAncestor(targetID) || ancestorNode;

	    firstChildren[0] = deepestAncestor.firstChild;
	    firstChildren.length = 1;

	    while (childIndex < firstChildren.length) {
	      var child = firstChildren[childIndex++];
	      var targetChild;

	      while (child) {
	        var childID = ReactMount.getID(child);
	        if (childID) {
	          // Even if we find the node we're looking for, we finish looping
	          // through its siblings to ensure they're cached so that we don't have
	          // to revisit this node again. Otherwise, we make n^2 calls to getID
	          // when visiting the many children of a single node in order.

	          if (targetID === childID) {
	            targetChild = child;
	          } else if (ReactInstanceHandles.isAncestorIDOf(childID, targetID)) {
	            // If we find a child whose ID is an ancestor of the given ID,
	            // then we can be sure that we only want to search the subtree
	            // rooted at this child, so we can throw out the rest of the
	            // search state.
	            firstChildren.length = childIndex = 0;
	            firstChildren.push(child.firstChild);
	          }

	        } else {
	          // If this child had no ID, then there's a chance that it was
	          // injected automatically by the browser, as when a `<table>`
	          // element sprouts an extra `<tbody>` child as a side effect of
	          // `.innerHTML` parsing. Optimistically continue down this
	          // branch, but not before examining the other siblings.
	          firstChildren.push(child.firstChild);
	        }

	        child = child.nextSibling;
	      }

	      if (targetChild) {
	        // Emptying firstChildren/findComponentRootReusableArray is
	        // not necessary for correctness, but it helps the GC reclaim
	        // any nodes that were left at the end of the search.
	        firstChildren.length = 0;

	        return targetChild;
	      }
	    }

	    firstChildren.length = 0;

	    ("production" !== process.env.NODE_ENV ? invariant(
	      false,
	      'findComponentRoot(..., %s): Unable to find element. This probably ' +
	      'means the DOM was unexpectedly mutated (e.g., by the browser), ' +
	      'usually due to forgetting a <tbody> when using tables, nesting tags ' +
	      'like <form>, <p>, or <a>, or using non-SVG elements in an <svg> ' +
	      'parent. ' +
	      'Try inspecting the child nodes of the element with React ID `%s`.',
	      targetID,
	      ReactMount.getID(ancestorNode)
	    ) : invariant(false));
	  },

	  _mountImageIntoNode: function(markup, container, shouldReuseMarkup) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      container && (
	        (container.nodeType === ELEMENT_NODE_TYPE || container.nodeType === DOC_NODE_TYPE)
	      ),
	      'mountComponentIntoNode(...): Target container is not valid.'
	    ) : invariant(container && (
	      (container.nodeType === ELEMENT_NODE_TYPE || container.nodeType === DOC_NODE_TYPE)
	    )));

	    if (shouldReuseMarkup) {
	      var rootElement = getReactRootElementInContainer(container);
	      if (ReactMarkupChecksum.canReuseMarkup(markup, rootElement)) {
	        return;
	      } else {
	        var checksum = rootElement.getAttribute(
	          ReactMarkupChecksum.CHECKSUM_ATTR_NAME
	        );
	        rootElement.removeAttribute(ReactMarkupChecksum.CHECKSUM_ATTR_NAME);

	        var rootMarkup = rootElement.outerHTML;
	        rootElement.setAttribute(
	          ReactMarkupChecksum.CHECKSUM_ATTR_NAME,
	          checksum
	        );

	        var diffIndex = firstDifferenceIndex(markup, rootMarkup);
	        var difference = ' (client) ' +
	          markup.substring(diffIndex - 20, diffIndex + 20) +
	          '\n (server) ' + rootMarkup.substring(diffIndex - 20, diffIndex + 20);

	        ("production" !== process.env.NODE_ENV ? invariant(
	          container.nodeType !== DOC_NODE_TYPE,
	          'You\'re trying to render a component to the document using ' +
	          'server rendering but the checksum was invalid. This usually ' +
	          'means you rendered a different component type or props on ' +
	          'the client from the one on the server, or your render() ' +
	          'methods are impure. React cannot handle this case due to ' +
	          'cross-browser quirks by rendering at the document root. You ' +
	          'should look for environment dependent code in your components ' +
	          'and ensure the props are the same client and server side:\n%s',
	          difference
	        ) : invariant(container.nodeType !== DOC_NODE_TYPE));

	        if ("production" !== process.env.NODE_ENV) {
	          ("production" !== process.env.NODE_ENV ? warning(
	            false,
	            'React attempted to reuse markup in a container but the ' +
	            'checksum was invalid. This generally means that you are ' +
	            'using server rendering and the markup generated on the ' +
	            'server was not what the client was expecting. React injected ' +
	            'new markup to compensate which works but you have lost many ' +
	            'of the benefits of server rendering. Instead, figure out ' +
	            'why the markup being generated is different on the client ' +
	            'or server:\n%s',
	            difference
	          ) : null);
	        }
	      }
	    }

	    ("production" !== process.env.NODE_ENV ? invariant(
	      container.nodeType !== DOC_NODE_TYPE,
	      'You\'re trying to render a component to the document but ' +
	        'you didn\'t use server rendering. We can\'t do this ' +
	        'without using server rendering due to cross-browser quirks. ' +
	        'See React.renderToString() for server rendering.'
	    ) : invariant(container.nodeType !== DOC_NODE_TYPE));

	    setInnerHTML(container, markup);
	  },

	  /**
	   * React ID utilities.
	   */

	  getReactRootID: getReactRootID,

	  getID: getID,

	  setID: setID,

	  getNode: getNode,

	  getNodeFromInstance: getNodeFromInstance,

	  purgeID: purgeID
	};

	ReactPerf.measureMethods(ReactMount, 'ReactMount', {
	  _renderNewRootComponent: '_renderNewRootComponent',
	  _mountImageIntoNode: '_mountImageIntoNode'
	});

	module.exports = ReactMount;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 69 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactBrowserEventEmitter
	 * @typechecks static-only
	 */

	'use strict';

	var EventConstants = __webpack_require__(6);
	var EventPluginHub = __webpack_require__(70);
	var EventPluginRegistry = __webpack_require__(71);
	var ReactEventEmitterMixin = __webpack_require__(74);
	var ViewportMetrics = __webpack_require__(75);

	var assign = __webpack_require__(14);
	var isEventSupported = __webpack_require__(76);

	/**
	 * Summary of `ReactBrowserEventEmitter` event handling:
	 *
	 *  - Top-level delegation is used to trap most native browser events. This
	 *    may only occur in the main thread and is the responsibility of
	 *    ReactEventListener, which is injected and can therefore support pluggable
	 *    event sources. This is the only work that occurs in the main thread.
	 *
	 *  - We normalize and de-duplicate events to account for browser quirks. This
	 *    may be done in the worker thread.
	 *
	 *  - Forward these native events (with the associated top-level type used to
	 *    trap it) to `EventPluginHub`, which in turn will ask plugins if they want
	 *    to extract any synthetic events.
	 *
	 *  - The `EventPluginHub` will then process each event by annotating them with
	 *    "dispatches", a sequence of listeners and IDs that care about that event.
	 *
	 *  - The `EventPluginHub` then dispatches the events.
	 *
	 * Overview of React and the event system:
	 *
	 * +------------+    .
	 * |    DOM     |    .
	 * +------------+    .
	 *       |           .
	 *       v           .
	 * +------------+    .
	 * | ReactEvent |    .
	 * |  Listener  |    .
	 * +------------+    .                         +-----------+
	 *       |           .               +--------+|SimpleEvent|
	 *       |           .               |         |Plugin     |
	 * +-----|------+    .               v         +-----------+
	 * |     |      |    .    +--------------+                    +------------+
	 * |     +-----------.--->|EventPluginHub|                    |    Event   |
	 * |            |    .    |              |     +-----------+  | Propagators|
	 * | ReactEvent |    .    |              |     |TapEvent   |  |------------|
	 * |  Emitter   |    .    |              |<---+|Plugin     |  |other plugin|
	 * |            |    .    |              |     +-----------+  |  utilities |
	 * |     +-----------.--->|              |                    +------------+
	 * |     |      |    .    +--------------+
	 * +-----|------+    .                ^        +-----------+
	 *       |           .                |        |Enter/Leave|
	 *       +           .                +-------+|Plugin     |
	 * +-------------+   .                         +-----------+
	 * | application |   .
	 * |-------------|   .
	 * |             |   .
	 * |             |   .
	 * +-------------+   .
	 *                   .
	 *    React Core     .  General Purpose Event Plugin System
	 */

	var alreadyListeningTo = {};
	var isMonitoringScrollValue = false;
	var reactTopListenersCounter = 0;

	// For events like 'submit' which don't consistently bubble (which we trap at a
	// lower node than `document`), binding at `document` would cause duplicate
	// events so we don't include them here
	var topEventMapping = {
	  topBlur: 'blur',
	  topChange: 'change',
	  topClick: 'click',
	  topCompositionEnd: 'compositionend',
	  topCompositionStart: 'compositionstart',
	  topCompositionUpdate: 'compositionupdate',
	  topContextMenu: 'contextmenu',
	  topCopy: 'copy',
	  topCut: 'cut',
	  topDoubleClick: 'dblclick',
	  topDrag: 'drag',
	  topDragEnd: 'dragend',
	  topDragEnter: 'dragenter',
	  topDragExit: 'dragexit',
	  topDragLeave: 'dragleave',
	  topDragOver: 'dragover',
	  topDragStart: 'dragstart',
	  topDrop: 'drop',
	  topFocus: 'focus',
	  topInput: 'input',
	  topKeyDown: 'keydown',
	  topKeyPress: 'keypress',
	  topKeyUp: 'keyup',
	  topMouseDown: 'mousedown',
	  topMouseMove: 'mousemove',
	  topMouseOut: 'mouseout',
	  topMouseOver: 'mouseover',
	  topMouseUp: 'mouseup',
	  topPaste: 'paste',
	  topScroll: 'scroll',
	  topSelectionChange: 'selectionchange',
	  topTextInput: 'textInput',
	  topTouchCancel: 'touchcancel',
	  topTouchEnd: 'touchend',
	  topTouchMove: 'touchmove',
	  topTouchStart: 'touchstart',
	  topWheel: 'wheel'
	};

	/**
	 * To ensure no conflicts with other potential React instances on the page
	 */
	var topListenersIDKey = '_reactListenersID' + String(Math.random()).slice(2);

	function getListeningForDocument(mountAt) {
	  // In IE8, `mountAt` is a host object and doesn't have `hasOwnProperty`
	  // directly.
	  if (!Object.prototype.hasOwnProperty.call(mountAt, topListenersIDKey)) {
	    mountAt[topListenersIDKey] = reactTopListenersCounter++;
	    alreadyListeningTo[mountAt[topListenersIDKey]] = {};
	  }
	  return alreadyListeningTo[mountAt[topListenersIDKey]];
	}

	/**
	 * `ReactBrowserEventEmitter` is used to attach top-level event listeners. For
	 * example:
	 *
	 *   ReactBrowserEventEmitter.putListener('myID', 'onClick', myFunction);
	 *
	 * This would allocate a "registration" of `('onClick', myFunction)` on 'myID'.
	 *
	 * @internal
	 */
	var ReactBrowserEventEmitter = assign({}, ReactEventEmitterMixin, {

	  /**
	   * Injectable event backend
	   */
	  ReactEventListener: null,

	  injection: {
	    /**
	     * @param {object} ReactEventListener
	     */
	    injectReactEventListener: function(ReactEventListener) {
	      ReactEventListener.setHandleTopLevel(
	        ReactBrowserEventEmitter.handleTopLevel
	      );
	      ReactBrowserEventEmitter.ReactEventListener = ReactEventListener;
	    }
	  },

	  /**
	   * Sets whether or not any created callbacks should be enabled.
	   *
	   * @param {boolean} enabled True if callbacks should be enabled.
	   */
	  setEnabled: function(enabled) {
	    if (ReactBrowserEventEmitter.ReactEventListener) {
	      ReactBrowserEventEmitter.ReactEventListener.setEnabled(enabled);
	    }
	  },

	  /**
	   * @return {boolean} True if callbacks are enabled.
	   */
	  isEnabled: function() {
	    return !!(
	      (ReactBrowserEventEmitter.ReactEventListener && ReactBrowserEventEmitter.ReactEventListener.isEnabled())
	    );
	  },

	  /**
	   * We listen for bubbled touch events on the document object.
	   *
	   * Firefox v8.01 (and possibly others) exhibited strange behavior when
	   * mounting `onmousemove` events at some node that was not the document
	   * element. The symptoms were that if your mouse is not moving over something
	   * contained within that mount point (for example on the background) the
	   * top-level listeners for `onmousemove` won't be called. However, if you
	   * register the `mousemove` on the document object, then it will of course
	   * catch all `mousemove`s. This along with iOS quirks, justifies restricting
	   * top-level listeners to the document object only, at least for these
	   * movement types of events and possibly all events.
	   *
	   * @see http://www.quirksmode.org/blog/archives/2010/09/click_event_del.html
	   *
	   * Also, `keyup`/`keypress`/`keydown` do not bubble to the window on IE, but
	   * they bubble to document.
	   *
	   * @param {string} registrationName Name of listener (e.g. `onClick`).
	   * @param {object} contentDocumentHandle Document which owns the container
	   */
	  listenTo: function(registrationName, contentDocumentHandle) {
	    var mountAt = contentDocumentHandle;
	    var isListening = getListeningForDocument(mountAt);
	    var dependencies = EventPluginRegistry.
	      registrationNameDependencies[registrationName];

	    var topLevelTypes = EventConstants.topLevelTypes;
	    for (var i = 0, l = dependencies.length; i < l; i++) {
	      var dependency = dependencies[i];
	      if (!(
	            (isListening.hasOwnProperty(dependency) && isListening[dependency])
	          )) {
	        if (dependency === topLevelTypes.topWheel) {
	          if (isEventSupported('wheel')) {
	            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(
	              topLevelTypes.topWheel,
	              'wheel',
	              mountAt
	            );
	          } else if (isEventSupported('mousewheel')) {
	            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(
	              topLevelTypes.topWheel,
	              'mousewheel',
	              mountAt
	            );
	          } else {
	            // Firefox needs to capture a different mouse scroll event.
	            // @see http://www.quirksmode.org/dom/events/tests/scroll.html
	            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(
	              topLevelTypes.topWheel,
	              'DOMMouseScroll',
	              mountAt
	            );
	          }
	        } else if (dependency === topLevelTypes.topScroll) {

	          if (isEventSupported('scroll', true)) {
	            ReactBrowserEventEmitter.ReactEventListener.trapCapturedEvent(
	              topLevelTypes.topScroll,
	              'scroll',
	              mountAt
	            );
	          } else {
	            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(
	              topLevelTypes.topScroll,
	              'scroll',
	              ReactBrowserEventEmitter.ReactEventListener.WINDOW_HANDLE
	            );
	          }
	        } else if (dependency === topLevelTypes.topFocus ||
	            dependency === topLevelTypes.topBlur) {

	          if (isEventSupported('focus', true)) {
	            ReactBrowserEventEmitter.ReactEventListener.trapCapturedEvent(
	              topLevelTypes.topFocus,
	              'focus',
	              mountAt
	            );
	            ReactBrowserEventEmitter.ReactEventListener.trapCapturedEvent(
	              topLevelTypes.topBlur,
	              'blur',
	              mountAt
	            );
	          } else if (isEventSupported('focusin')) {
	            // IE has `focusin` and `focusout` events which bubble.
	            // @see http://www.quirksmode.org/blog/archives/2008/04/delegating_the.html
	            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(
	              topLevelTypes.topFocus,
	              'focusin',
	              mountAt
	            );
	            ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(
	              topLevelTypes.topBlur,
	              'focusout',
	              mountAt
	            );
	          }

	          // to make sure blur and focus event listeners are only attached once
	          isListening[topLevelTypes.topBlur] = true;
	          isListening[topLevelTypes.topFocus] = true;
	        } else if (topEventMapping.hasOwnProperty(dependency)) {
	          ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(
	            dependency,
	            topEventMapping[dependency],
	            mountAt
	          );
	        }

	        isListening[dependency] = true;
	      }
	    }
	  },

	  trapBubbledEvent: function(topLevelType, handlerBaseName, handle) {
	    return ReactBrowserEventEmitter.ReactEventListener.trapBubbledEvent(
	      topLevelType,
	      handlerBaseName,
	      handle
	    );
	  },

	  trapCapturedEvent: function(topLevelType, handlerBaseName, handle) {
	    return ReactBrowserEventEmitter.ReactEventListener.trapCapturedEvent(
	      topLevelType,
	      handlerBaseName,
	      handle
	    );
	  },

	  /**
	   * Listens to window scroll and resize events. We cache scroll values so that
	   * application code can access them without triggering reflows.
	   *
	   * NOTE: Scroll events do not bubble.
	   *
	   * @see http://www.quirksmode.org/dom/events/scroll.html
	   */
	  ensureScrollValueMonitoring: function() {
	    if (!isMonitoringScrollValue) {
	      var refresh = ViewportMetrics.refreshScrollValues;
	      ReactBrowserEventEmitter.ReactEventListener.monitorScrollValue(refresh);
	      isMonitoringScrollValue = true;
	    }
	  },

	  eventNameDispatchConfigs: EventPluginHub.eventNameDispatchConfigs,

	  registrationNameModules: EventPluginHub.registrationNameModules,

	  putListener: EventPluginHub.putListener,

	  getListener: EventPluginHub.getListener,

	  deleteListener: EventPluginHub.deleteListener,

	  deleteAllListeners: EventPluginHub.deleteAllListeners

	});

	module.exports = ReactBrowserEventEmitter;


/***/ },
/* 70 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule EventPluginHub
	 */

	'use strict';

	var EventPluginRegistry = __webpack_require__(71);
	var EventPluginUtils = __webpack_require__(5);

	var accumulateInto = __webpack_require__(72);
	var forEachAccumulated = __webpack_require__(73);
	var invariant = __webpack_require__(8);

	/**
	 * Internal store for event listeners
	 */
	var listenerBank = {};

	/**
	 * Internal queue of events that have accumulated their dispatches and are
	 * waiting to have their dispatches executed.
	 */
	var eventQueue = null;

	/**
	 * Dispatches an event and releases it back into the pool, unless persistent.
	 *
	 * @param {?object} event Synthetic event to be dispatched.
	 * @private
	 */
	var executeDispatchesAndRelease = function(event) {
	  if (event) {
	    var executeDispatch = EventPluginUtils.executeDispatch;
	    // Plugins can provide custom behavior when dispatching events.
	    var PluginModule = EventPluginRegistry.getPluginModuleForEvent(event);
	    if (PluginModule && PluginModule.executeDispatch) {
	      executeDispatch = PluginModule.executeDispatch;
	    }
	    EventPluginUtils.executeDispatchesInOrder(event, executeDispatch);

	    if (!event.isPersistent()) {
	      event.constructor.release(event);
	    }
	  }
	};

	/**
	 * - `InstanceHandle`: [required] Module that performs logical traversals of DOM
	 *   hierarchy given ids of the logical DOM elements involved.
	 */
	var InstanceHandle = null;

	function validateInstanceHandle() {
	  var valid =
	    InstanceHandle &&
	    InstanceHandle.traverseTwoPhase &&
	    InstanceHandle.traverseEnterLeave;
	  ("production" !== process.env.NODE_ENV ? invariant(
	    valid,
	    'InstanceHandle not injected before use!'
	  ) : invariant(valid));
	}

	/**
	 * This is a unified interface for event plugins to be installed and configured.
	 *
	 * Event plugins can implement the following properties:
	 *
	 *   `extractEvents` {function(string, DOMEventTarget, string, object): *}
	 *     Required. When a top-level event is fired, this method is expected to
	 *     extract synthetic events that will in turn be queued and dispatched.
	 *
	 *   `eventTypes` {object}
	 *     Optional, plugins that fire events must publish a mapping of registration
	 *     names that are used to register listeners. Values of this mapping must
	 *     be objects that contain `registrationName` or `phasedRegistrationNames`.
	 *
	 *   `executeDispatch` {function(object, function, string)}
	 *     Optional, allows plugins to override how an event gets dispatched. By
	 *     default, the listener is simply invoked.
	 *
	 * Each plugin that is injected into `EventsPluginHub` is immediately operable.
	 *
	 * @public
	 */
	var EventPluginHub = {

	  /**
	   * Methods for injecting dependencies.
	   */
	  injection: {

	    /**
	     * @param {object} InjectedMount
	     * @public
	     */
	    injectMount: EventPluginUtils.injection.injectMount,

	    /**
	     * @param {object} InjectedInstanceHandle
	     * @public
	     */
	    injectInstanceHandle: function(InjectedInstanceHandle) {
	      InstanceHandle = InjectedInstanceHandle;
	      if ("production" !== process.env.NODE_ENV) {
	        validateInstanceHandle();
	      }
	    },

	    getInstanceHandle: function() {
	      if ("production" !== process.env.NODE_ENV) {
	        validateInstanceHandle();
	      }
	      return InstanceHandle;
	    },

	    /**
	     * @param {array} InjectedEventPluginOrder
	     * @public
	     */
	    injectEventPluginOrder: EventPluginRegistry.injectEventPluginOrder,

	    /**
	     * @param {object} injectedNamesToPlugins Map from names to plugin modules.
	     */
	    injectEventPluginsByName: EventPluginRegistry.injectEventPluginsByName

	  },

	  eventNameDispatchConfigs: EventPluginRegistry.eventNameDispatchConfigs,

	  registrationNameModules: EventPluginRegistry.registrationNameModules,

	  /**
	   * Stores `listener` at `listenerBank[registrationName][id]`. Is idempotent.
	   *
	   * @param {string} id ID of the DOM element.
	   * @param {string} registrationName Name of listener (e.g. `onClick`).
	   * @param {?function} listener The callback to store.
	   */
	  putListener: function(id, registrationName, listener) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      !listener || typeof listener === 'function',
	      'Expected %s listener to be a function, instead got type %s',
	      registrationName, typeof listener
	    ) : invariant(!listener || typeof listener === 'function'));

	    var bankForRegistrationName =
	      listenerBank[registrationName] || (listenerBank[registrationName] = {});
	    bankForRegistrationName[id] = listener;
	  },

	  /**
	   * @param {string} id ID of the DOM element.
	   * @param {string} registrationName Name of listener (e.g. `onClick`).
	   * @return {?function} The stored callback.
	   */
	  getListener: function(id, registrationName) {
	    var bankForRegistrationName = listenerBank[registrationName];
	    return bankForRegistrationName && bankForRegistrationName[id];
	  },

	  /**
	   * Deletes a listener from the registration bank.
	   *
	   * @param {string} id ID of the DOM element.
	   * @param {string} registrationName Name of listener (e.g. `onClick`).
	   */
	  deleteListener: function(id, registrationName) {
	    var bankForRegistrationName = listenerBank[registrationName];
	    if (bankForRegistrationName) {
	      delete bankForRegistrationName[id];
	    }
	  },

	  /**
	   * Deletes all listeners for the DOM element with the supplied ID.
	   *
	   * @param {string} id ID of the DOM element.
	   */
	  deleteAllListeners: function(id) {
	    for (var registrationName in listenerBank) {
	      delete listenerBank[registrationName][id];
	    }
	  },

	  /**
	   * Allows registered plugins an opportunity to extract events from top-level
	   * native browser events.
	   *
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @internal
	   */
	  extractEvents: function(
	      topLevelType,
	      topLevelTarget,
	      topLevelTargetID,
	      nativeEvent) {
	    var events;
	    var plugins = EventPluginRegistry.plugins;
	    for (var i = 0, l = plugins.length; i < l; i++) {
	      // Not every plugin in the ordering may be loaded at runtime.
	      var possiblePlugin = plugins[i];
	      if (possiblePlugin) {
	        var extractedEvents = possiblePlugin.extractEvents(
	          topLevelType,
	          topLevelTarget,
	          topLevelTargetID,
	          nativeEvent
	        );
	        if (extractedEvents) {
	          events = accumulateInto(events, extractedEvents);
	        }
	      }
	    }
	    return events;
	  },

	  /**
	   * Enqueues a synthetic event that should be dispatched when
	   * `processEventQueue` is invoked.
	   *
	   * @param {*} events An accumulation of synthetic events.
	   * @internal
	   */
	  enqueueEvents: function(events) {
	    if (events) {
	      eventQueue = accumulateInto(eventQueue, events);
	    }
	  },

	  /**
	   * Dispatches all synthetic events on the event queue.
	   *
	   * @internal
	   */
	  processEventQueue: function() {
	    // Set `eventQueue` to null before processing it so that we can tell if more
	    // events get enqueued while processing.
	    var processingEventQueue = eventQueue;
	    eventQueue = null;
	    forEachAccumulated(processingEventQueue, executeDispatchesAndRelease);
	    ("production" !== process.env.NODE_ENV ? invariant(
	      !eventQueue,
	      'processEventQueue(): Additional events were enqueued while processing ' +
	      'an event queue. Support for this has not yet been implemented.'
	    ) : invariant(!eventQueue));
	  },

	  /**
	   * These are needed for tests only. Do not use!
	   */
	  __purge: function() {
	    listenerBank = {};
	  },

	  __getListenerBank: function() {
	    return listenerBank;
	  }

	};

	module.exports = EventPluginHub;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 71 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule EventPluginRegistry
	 * @typechecks static-only
	 */

	'use strict';

	var invariant = __webpack_require__(8);

	/**
	 * Injectable ordering of event plugins.
	 */
	var EventPluginOrder = null;

	/**
	 * Injectable mapping from names to event plugin modules.
	 */
	var namesToPlugins = {};

	/**
	 * Recomputes the plugin list using the injected plugins and plugin ordering.
	 *
	 * @private
	 */
	function recomputePluginOrdering() {
	  if (!EventPluginOrder) {
	    // Wait until an `EventPluginOrder` is injected.
	    return;
	  }
	  for (var pluginName in namesToPlugins) {
	    var PluginModule = namesToPlugins[pluginName];
	    var pluginIndex = EventPluginOrder.indexOf(pluginName);
	    ("production" !== process.env.NODE_ENV ? invariant(
	      pluginIndex > -1,
	      'EventPluginRegistry: Cannot inject event plugins that do not exist in ' +
	      'the plugin ordering, `%s`.',
	      pluginName
	    ) : invariant(pluginIndex > -1));
	    if (EventPluginRegistry.plugins[pluginIndex]) {
	      continue;
	    }
	    ("production" !== process.env.NODE_ENV ? invariant(
	      PluginModule.extractEvents,
	      'EventPluginRegistry: Event plugins must implement an `extractEvents` ' +
	      'method, but `%s` does not.',
	      pluginName
	    ) : invariant(PluginModule.extractEvents));
	    EventPluginRegistry.plugins[pluginIndex] = PluginModule;
	    var publishedEvents = PluginModule.eventTypes;
	    for (var eventName in publishedEvents) {
	      ("production" !== process.env.NODE_ENV ? invariant(
	        publishEventForPlugin(
	          publishedEvents[eventName],
	          PluginModule,
	          eventName
	        ),
	        'EventPluginRegistry: Failed to publish event `%s` for plugin `%s`.',
	        eventName,
	        pluginName
	      ) : invariant(publishEventForPlugin(
	        publishedEvents[eventName],
	        PluginModule,
	        eventName
	      )));
	    }
	  }
	}

	/**
	 * Publishes an event so that it can be dispatched by the supplied plugin.
	 *
	 * @param {object} dispatchConfig Dispatch configuration for the event.
	 * @param {object} PluginModule Plugin publishing the event.
	 * @return {boolean} True if the event was successfully published.
	 * @private
	 */
	function publishEventForPlugin(dispatchConfig, PluginModule, eventName) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    !EventPluginRegistry.eventNameDispatchConfigs.hasOwnProperty(eventName),
	    'EventPluginHub: More than one plugin attempted to publish the same ' +
	    'event name, `%s`.',
	    eventName
	  ) : invariant(!EventPluginRegistry.eventNameDispatchConfigs.hasOwnProperty(eventName)));
	  EventPluginRegistry.eventNameDispatchConfigs[eventName] = dispatchConfig;

	  var phasedRegistrationNames = dispatchConfig.phasedRegistrationNames;
	  if (phasedRegistrationNames) {
	    for (var phaseName in phasedRegistrationNames) {
	      if (phasedRegistrationNames.hasOwnProperty(phaseName)) {
	        var phasedRegistrationName = phasedRegistrationNames[phaseName];
	        publishRegistrationName(
	          phasedRegistrationName,
	          PluginModule,
	          eventName
	        );
	      }
	    }
	    return true;
	  } else if (dispatchConfig.registrationName) {
	    publishRegistrationName(
	      dispatchConfig.registrationName,
	      PluginModule,
	      eventName
	    );
	    return true;
	  }
	  return false;
	}

	/**
	 * Publishes a registration name that is used to identify dispatched events and
	 * can be used with `EventPluginHub.putListener` to register listeners.
	 *
	 * @param {string} registrationName Registration name to add.
	 * @param {object} PluginModule Plugin publishing the event.
	 * @private
	 */
	function publishRegistrationName(registrationName, PluginModule, eventName) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    !EventPluginRegistry.registrationNameModules[registrationName],
	    'EventPluginHub: More than one plugin attempted to publish the same ' +
	    'registration name, `%s`.',
	    registrationName
	  ) : invariant(!EventPluginRegistry.registrationNameModules[registrationName]));
	  EventPluginRegistry.registrationNameModules[registrationName] = PluginModule;
	  EventPluginRegistry.registrationNameDependencies[registrationName] =
	    PluginModule.eventTypes[eventName].dependencies;
	}

	/**
	 * Registers plugins so that they can extract and dispatch events.
	 *
	 * @see {EventPluginHub}
	 */
	var EventPluginRegistry = {

	  /**
	   * Ordered list of injected plugins.
	   */
	  plugins: [],

	  /**
	   * Mapping from event name to dispatch config
	   */
	  eventNameDispatchConfigs: {},

	  /**
	   * Mapping from registration name to plugin module
	   */
	  registrationNameModules: {},

	  /**
	   * Mapping from registration name to event name
	   */
	  registrationNameDependencies: {},

	  /**
	   * Injects an ordering of plugins (by plugin name). This allows the ordering
	   * to be decoupled from injection of the actual plugins so that ordering is
	   * always deterministic regardless of packaging, on-the-fly injection, etc.
	   *
	   * @param {array} InjectedEventPluginOrder
	   * @internal
	   * @see {EventPluginHub.injection.injectEventPluginOrder}
	   */
	  injectEventPluginOrder: function(InjectedEventPluginOrder) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      !EventPluginOrder,
	      'EventPluginRegistry: Cannot inject event plugin ordering more than ' +
	      'once. You are likely trying to load more than one copy of React.'
	    ) : invariant(!EventPluginOrder));
	    // Clone the ordering so it cannot be dynamically mutated.
	    EventPluginOrder = Array.prototype.slice.call(InjectedEventPluginOrder);
	    recomputePluginOrdering();
	  },

	  /**
	   * Injects plugins to be used by `EventPluginHub`. The plugin names must be
	   * in the ordering injected by `injectEventPluginOrder`.
	   *
	   * Plugins can be injected as part of page initialization or on-the-fly.
	   *
	   * @param {object} injectedNamesToPlugins Map from names to plugin modules.
	   * @internal
	   * @see {EventPluginHub.injection.injectEventPluginsByName}
	   */
	  injectEventPluginsByName: function(injectedNamesToPlugins) {
	    var isOrderingDirty = false;
	    for (var pluginName in injectedNamesToPlugins) {
	      if (!injectedNamesToPlugins.hasOwnProperty(pluginName)) {
	        continue;
	      }
	      var PluginModule = injectedNamesToPlugins[pluginName];
	      if (!namesToPlugins.hasOwnProperty(pluginName) ||
	          namesToPlugins[pluginName] !== PluginModule) {
	        ("production" !== process.env.NODE_ENV ? invariant(
	          !namesToPlugins[pluginName],
	          'EventPluginRegistry: Cannot inject two different event plugins ' +
	          'using the same name, `%s`.',
	          pluginName
	        ) : invariant(!namesToPlugins[pluginName]));
	        namesToPlugins[pluginName] = PluginModule;
	        isOrderingDirty = true;
	      }
	    }
	    if (isOrderingDirty) {
	      recomputePluginOrdering();
	    }
	  },

	  /**
	   * Looks up the plugin for the supplied event.
	   *
	   * @param {object} event A synthetic event.
	   * @return {?object} The plugin that created the supplied event.
	   * @internal
	   */
	  getPluginModuleForEvent: function(event) {
	    var dispatchConfig = event.dispatchConfig;
	    if (dispatchConfig.registrationName) {
	      return EventPluginRegistry.registrationNameModules[
	        dispatchConfig.registrationName
	      ] || null;
	    }
	    for (var phase in dispatchConfig.phasedRegistrationNames) {
	      if (!dispatchConfig.phasedRegistrationNames.hasOwnProperty(phase)) {
	        continue;
	      }
	      var PluginModule = EventPluginRegistry.registrationNameModules[
	        dispatchConfig.phasedRegistrationNames[phase]
	      ];
	      if (PluginModule) {
	        return PluginModule;
	      }
	    }
	    return null;
	  },

	  /**
	   * Exposed for unit testing.
	   * @private
	   */
	  _resetEventPlugins: function() {
	    EventPluginOrder = null;
	    for (var pluginName in namesToPlugins) {
	      if (namesToPlugins.hasOwnProperty(pluginName)) {
	        delete namesToPlugins[pluginName];
	      }
	    }
	    EventPluginRegistry.plugins.length = 0;

	    var eventNameDispatchConfigs = EventPluginRegistry.eventNameDispatchConfigs;
	    for (var eventName in eventNameDispatchConfigs) {
	      if (eventNameDispatchConfigs.hasOwnProperty(eventName)) {
	        delete eventNameDispatchConfigs[eventName];
	      }
	    }

	    var registrationNameModules = EventPluginRegistry.registrationNameModules;
	    for (var registrationName in registrationNameModules) {
	      if (registrationNameModules.hasOwnProperty(registrationName)) {
	        delete registrationNameModules[registrationName];
	      }
	    }
	  }

	};

	module.exports = EventPluginRegistry;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 72 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule accumulateInto
	 */

	'use strict';

	var invariant = __webpack_require__(8);

	/**
	 *
	 * Accumulates items that must not be null or undefined into the first one. This
	 * is used to conserve memory by avoiding array allocations, and thus sacrifices
	 * API cleanness. Since `current` can be null before being passed in and not
	 * null after this function, make sure to assign it back to `current`:
	 *
	 * `a = accumulateInto(a, b);`
	 *
	 * This API should be sparingly used. Try `accumulate` for something cleaner.
	 *
	 * @return {*|array<*>} An accumulation of items.
	 */

	function accumulateInto(current, next) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    next != null,
	    'accumulateInto(...): Accumulated items must not be null or undefined.'
	  ) : invariant(next != null));
	  if (current == null) {
	    return next;
	  }

	  // Both are not empty. Warning: Never call x.concat(y) when you are not
	  // certain that x is an Array (x could be a string with concat method).
	  var currentIsArray = Array.isArray(current);
	  var nextIsArray = Array.isArray(next);

	  if (currentIsArray && nextIsArray) {
	    current.push.apply(current, next);
	    return current;
	  }

	  if (currentIsArray) {
	    current.push(next);
	    return current;
	  }

	  if (nextIsArray) {
	    // A bit too dangerous to mutate `next`.
	    return [current].concat(next);
	  }

	  return [current, next];
	}

	module.exports = accumulateInto;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 73 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule forEachAccumulated
	 */

	'use strict';

	/**
	 * @param {array} an "accumulation" of items which is either an Array or
	 * a single item. Useful when paired with the `accumulate` module. This is a
	 * simple utility that allows us to reason about a collection of items, but
	 * handling the case when there is exactly one item (and we do not need to
	 * allocate an array).
	 */
	var forEachAccumulated = function(arr, cb, scope) {
	  if (Array.isArray(arr)) {
	    arr.forEach(cb, scope);
	  } else if (arr) {
	    cb.call(scope, arr);
	  }
	};

	module.exports = forEachAccumulated;


/***/ },
/* 74 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactEventEmitterMixin
	 */

	'use strict';

	var EventPluginHub = __webpack_require__(70);

	function runEventQueueInBatch(events) {
	  EventPluginHub.enqueueEvents(events);
	  EventPluginHub.processEventQueue();
	}

	var ReactEventEmitterMixin = {

	  /**
	   * Streams a fired top-level event to `EventPluginHub` where plugins have the
	   * opportunity to create `ReactEvent`s to be dispatched.
	   *
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {object} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native environment event.
	   */
	  handleTopLevel: function(
	      topLevelType,
	      topLevelTarget,
	      topLevelTargetID,
	      nativeEvent) {
	    var events = EventPluginHub.extractEvents(
	      topLevelType,
	      topLevelTarget,
	      topLevelTargetID,
	      nativeEvent
	    );

	    runEventQueueInBatch(events);
	  }
	};

	module.exports = ReactEventEmitterMixin;


/***/ },
/* 75 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ViewportMetrics
	 */

	'use strict';

	var ViewportMetrics = {

	  currentScrollLeft: 0,

	  currentScrollTop: 0,

	  refreshScrollValues: function(scrollPosition) {
	    ViewportMetrics.currentScrollLeft = scrollPosition.x;
	    ViewportMetrics.currentScrollTop = scrollPosition.y;
	  }

	};

	module.exports = ViewportMetrics;


/***/ },
/* 76 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule isEventSupported
	 */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(52);

	var useHasFeature;
	if (ExecutionEnvironment.canUseDOM) {
	  useHasFeature =
	    document.implementation &&
	    document.implementation.hasFeature &&
	    // always returns true in newer browsers as per the standard.
	    // @see http://dom.spec.whatwg.org/#dom-domimplementation-hasfeature
	    document.implementation.hasFeature('', '') !== true;
	}

	/**
	 * Checks if an event is supported in the current execution environment.
	 *
	 * NOTE: This will not work correctly for non-generic events such as `change`,
	 * `reset`, `load`, `error`, and `select`.
	 *
	 * Borrows from Modernizr.
	 *
	 * @param {string} eventNameSuffix Event name, e.g. "click".
	 * @param {?boolean} capture Check if the capture phase is supported.
	 * @return {boolean} True if the event is supported.
	 * @internal
	 * @license Modernizr 3.0.0pre (Custom Build) | MIT
	 */
	function isEventSupported(eventNameSuffix, capture) {
	  if (!ExecutionEnvironment.canUseDOM ||
	      capture && !('addEventListener' in document)) {
	    return false;
	  }

	  var eventName = 'on' + eventNameSuffix;
	  var isSupported = eventName in document;

	  if (!isSupported) {
	    var element = document.createElement('div');
	    element.setAttribute(eventName, 'return;');
	    isSupported = typeof element[eventName] === 'function';
	  }

	  if (!isSupported && useHasFeature && eventNameSuffix === 'wheel') {
	    // This is the only way to test support for the `wheel` event in IE9+.
	    isSupported = document.implementation.hasFeature('Events.wheel', '3.0');
	  }

	  return isSupported;
	}

	module.exports = isEventSupported;


/***/ },
/* 77 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactEmptyComponent
	 */

	'use strict';

	var ReactElement = __webpack_require__(12);
	var ReactInstanceMap = __webpack_require__(37);

	var invariant = __webpack_require__(8);

	var component;
	// This registry keeps track of the React IDs of the components that rendered to
	// `null` (in reality a placeholder such as `noscript`)
	var nullComponentIDsRegistry = {};

	var ReactEmptyComponentInjection = {
	  injectEmptyComponent: function(emptyComponent) {
	    component = ReactElement.createFactory(emptyComponent);
	  }
	};

	var ReactEmptyComponentType = function() {};
	ReactEmptyComponentType.prototype.componentDidMount = function() {
	  var internalInstance = ReactInstanceMap.get(this);
	  // TODO: Make sure we run these methods in the correct order, we shouldn't
	  // need this check. We're going to assume if we're here it means we ran
	  // componentWillUnmount already so there is no internal instance (it gets
	  // removed as part of the unmounting process).
	  if (!internalInstance) {
	    return;
	  }
	  registerNullComponentID(internalInstance._rootNodeID);
	};
	ReactEmptyComponentType.prototype.componentWillUnmount = function() {
	  var internalInstance = ReactInstanceMap.get(this);
	  // TODO: Get rid of this check. See TODO in componentDidMount.
	  if (!internalInstance) {
	    return;
	  }
	  deregisterNullComponentID(internalInstance._rootNodeID);
	};
	ReactEmptyComponentType.prototype.render = function() {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    component,
	    'Trying to return null from a render, but no null placeholder component ' +
	    'was injected.'
	  ) : invariant(component));
	  return component();
	};

	var emptyElement = ReactElement.createElement(ReactEmptyComponentType);

	/**
	 * Mark the component as having rendered to null.
	 * @param {string} id Component's `_rootNodeID`.
	 */
	function registerNullComponentID(id) {
	  nullComponentIDsRegistry[id] = true;
	}

	/**
	 * Unmark the component as having rendered to null: it renders to something now.
	 * @param {string} id Component's `_rootNodeID`.
	 */
	function deregisterNullComponentID(id) {
	  delete nullComponentIDsRegistry[id];
	}

	/**
	 * @param {string} id Component's `_rootNodeID`.
	 * @return {boolean} True if the component is rendered to null.
	 */
	function isNullComponentID(id) {
	  return !!nullComponentIDsRegistry[id];
	}

	var ReactEmptyComponent = {
	  emptyElement: emptyElement,
	  injection: ReactEmptyComponentInjection,
	  isNullComponentID: isNullComponentID
	};

	module.exports = ReactEmptyComponent;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 78 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactMarkupChecksum
	 */

	'use strict';

	var adler32 = __webpack_require__(79);

	var ReactMarkupChecksum = {
	  CHECKSUM_ATTR_NAME: 'data-react-checksum',

	  /**
	   * @param {string} markup Markup string
	   * @return {string} Markup string with checksum attribute attached
	   */
	  addChecksumToMarkup: function(markup) {
	    var checksum = adler32(markup);
	    return markup.replace(
	      '>',
	      ' ' + ReactMarkupChecksum.CHECKSUM_ATTR_NAME + '="' + checksum + '">'
	    );
	  },

	  /**
	   * @param {string} markup to use
	   * @param {DOMElement} element root React element
	   * @returns {boolean} whether or not the markup is the same
	   */
	  canReuseMarkup: function(markup, element) {
	    var existingChecksum = element.getAttribute(
	      ReactMarkupChecksum.CHECKSUM_ATTR_NAME
	    );
	    existingChecksum = existingChecksum && parseInt(existingChecksum, 10);
	    var markupChecksum = adler32(markup);
	    return markupChecksum === existingChecksum;
	  }
	};

	module.exports = ReactMarkupChecksum;


/***/ },
/* 79 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule adler32
	 */

	/* jslint bitwise:true */

	'use strict';

	var MOD = 65521;

	// This is a clean-room implementation of adler32 designed for detecting
	// if markup is not what we expect it to be. It does not need to be
	// cryptographically strong, only reasonably good at detecting if markup
	// generated on the server is different than that on the client.
	function adler32(data) {
	  var a = 1;
	  var b = 0;
	  for (var i = 0; i < data.length; i++) {
	    a = (a + data.charCodeAt(i)) % MOD;
	    b = (b + a) % MOD;
	  }
	  return a | (b << 16);
	}

	module.exports = adler32;


/***/ },
/* 80 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule containsNode
	 * @typechecks
	 */

	var isTextNode = __webpack_require__(81);

	/*jslint bitwise:true */

	/**
	 * Checks if a given DOM node contains or is another DOM node.
	 *
	 * @param {?DOMNode} outerNode Outer DOM node.
	 * @param {?DOMNode} innerNode Inner DOM node.
	 * @return {boolean} True if `outerNode` contains or is `innerNode`.
	 */
	function containsNode(outerNode, innerNode) {
	  if (!outerNode || !innerNode) {
	    return false;
	  } else if (outerNode === innerNode) {
	    return true;
	  } else if (isTextNode(outerNode)) {
	    return false;
	  } else if (isTextNode(innerNode)) {
	    return containsNode(outerNode, innerNode.parentNode);
	  } else if (outerNode.contains) {
	    return outerNode.contains(innerNode);
	  } else if (outerNode.compareDocumentPosition) {
	    return !!(outerNode.compareDocumentPosition(innerNode) & 16);
	  } else {
	    return false;
	  }
	}

	module.exports = containsNode;


/***/ },
/* 81 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule isTextNode
	 * @typechecks
	 */

	var isNode = __webpack_require__(82);

	/**
	 * @param {*} object The object to check.
	 * @return {boolean} Whether or not the object is a DOM text node.
	 */
	function isTextNode(object) {
	  return isNode(object) && object.nodeType == 3;
	}

	module.exports = isTextNode;


/***/ },
/* 82 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule isNode
	 * @typechecks
	 */

	/**
	 * @param {*} object The object to check.
	 * @return {boolean} Whether or not the object is a DOM node.
	 */
	function isNode(object) {
	  return !!(object && (
	    ((typeof Node === 'function' ? object instanceof Node : typeof object === 'object' &&
	    typeof object.nodeType === 'number' &&
	    typeof object.nodeName === 'string'))
	  ));
	}

	module.exports = isNode;


/***/ },
/* 83 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getReactRootElementInContainer
	 */

	'use strict';

	var DOC_NODE_TYPE = 9;

	/**
	 * @param {DOMElement|DOMDocument} container DOM element that may contain
	 *                                           a React component
	 * @return {?*} DOM element that may have the reactRoot ID, or null.
	 */
	function getReactRootElementInContainer(container) {
	  if (!container) {
	    return null;
	  }

	  if (container.nodeType === DOC_NODE_TYPE) {
	    return container.documentElement;
	  } else {
	    return container.firstChild;
	  }
	}

	module.exports = getReactRootElementInContainer;


/***/ },
/* 84 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule instantiateReactComponent
	 * @typechecks static-only
	 */

	'use strict';

	var ReactCompositeComponent = __webpack_require__(85);
	var ReactEmptyComponent = __webpack_require__(77);
	var ReactNativeComponent = __webpack_require__(34);

	var assign = __webpack_require__(14);
	var invariant = __webpack_require__(8);
	var warning = __webpack_require__(16);

	// To avoid a cyclic dependency, we create the final class in this module
	var ReactCompositeComponentWrapper = function() { };
	assign(
	  ReactCompositeComponentWrapper.prototype,
	  ReactCompositeComponent.Mixin,
	  {
	    _instantiateReactComponent: instantiateReactComponent
	  }
	);

	/**
	 * Check if the type reference is a known internal type. I.e. not a user
	 * provided composite type.
	 *
	 * @param {function} type
	 * @return {boolean} Returns true if this is a valid internal type.
	 */
	function isInternalComponentType(type) {
	  return (
	    typeof type === 'function' &&
	    typeof type.prototype !== 'undefined' &&
	    typeof type.prototype.mountComponent === 'function' &&
	    typeof type.prototype.receiveComponent === 'function'
	  );
	}

	/**
	 * Given a ReactNode, create an instance that will actually be mounted.
	 *
	 * @param {ReactNode} node
	 * @param {*} parentCompositeType The composite type that resolved this.
	 * @return {object} A new instance of the element's constructor.
	 * @protected
	 */
	function instantiateReactComponent(node, parentCompositeType) {
	  var instance;

	  if (node === null || node === false) {
	    node = ReactEmptyComponent.emptyElement;
	  }

	  if (typeof node === 'object') {
	    var element = node;
	    if ("production" !== process.env.NODE_ENV) {
	      ("production" !== process.env.NODE_ENV ? warning(
	        element && (typeof element.type === 'function' ||
	                    typeof element.type === 'string'),
	        'Only functions or strings can be mounted as React components.'
	      ) : null);
	    }

	    // Special case string values
	    if (parentCompositeType === element.type &&
	        typeof element.type === 'string') {
	      // Avoid recursion if the wrapper renders itself.
	      instance = ReactNativeComponent.createInternalComponent(element);
	      // All native components are currently wrapped in a composite so we're
	      // safe to assume that this is what we should instantiate.
	    } else if (isInternalComponentType(element.type)) {
	      // This is temporarily available for custom components that are not string
	      // represenations. I.e. ART. Once those are updated to use the string
	      // representation, we can drop this code path.
	      instance = new element.type(element);
	    } else {
	      instance = new ReactCompositeComponentWrapper();
	    }
	  } else if (typeof node === 'string' || typeof node === 'number') {
	    instance = ReactNativeComponent.createInstanceForText(node);
	  } else {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      false,
	      'Encountered invalid React node of type %s',
	      typeof node
	    ) : invariant(false));
	  }

	  if ("production" !== process.env.NODE_ENV) {
	    ("production" !== process.env.NODE_ENV ? warning(
	      typeof instance.construct === 'function' &&
	      typeof instance.mountComponent === 'function' &&
	      typeof instance.receiveComponent === 'function' &&
	      typeof instance.unmountComponent === 'function',
	      'Only React Components can be mounted.'
	    ) : null);
	  }

	  // Sets up the instance. This can probably just move into the constructor now.
	  instance.construct(node);

	  // These two fields are used by the DOM and ART diffing algorithms
	  // respectively. Instead of using expandos on components, we should be
	  // storing the state needed by the diffing algorithms elsewhere.
	  instance._mountIndex = 0;
	  instance._mountImage = null;

	  if ("production" !== process.env.NODE_ENV) {
	    instance._isOwnerNecessary = false;
	    instance._warnedAboutRefsInRender = false;
	  }

	  // Internal instances should fully constructed at this point, so they should
	  // not get any new fields added to them at this point.
	  if ("production" !== process.env.NODE_ENV) {
	    if (Object.preventExtensions) {
	      Object.preventExtensions(instance);
	    }
	  }

	  return instance;
	}

	module.exports = instantiateReactComponent;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 85 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactCompositeComponent
	 */

	'use strict';

	var ReactComponentEnvironment = __webpack_require__(86);
	var ReactContext = __webpack_require__(13);
	var ReactCurrentOwner = __webpack_require__(18);
	var ReactElement = __webpack_require__(12);
	var ReactElementValidator = __webpack_require__(31);
	var ReactInstanceMap = __webpack_require__(37);
	var ReactLifeCycle = __webpack_require__(36);
	var ReactNativeComponent = __webpack_require__(34);
	var ReactPerf = __webpack_require__(27);
	var ReactPropTypeLocations = __webpack_require__(32);
	var ReactPropTypeLocationNames = __webpack_require__(33);
	var ReactReconciler = __webpack_require__(28);
	var ReactUpdates = __webpack_require__(25);

	var assign = __webpack_require__(14);
	var emptyObject = __webpack_require__(15);
	var invariant = __webpack_require__(8);
	var shouldUpdateReactComponent = __webpack_require__(87);
	var warning = __webpack_require__(16);

	function getDeclarationErrorAddendum(component) {
	  var owner = component._currentElement._owner || null;
	  if (owner) {
	    var name = owner.getName();
	    if (name) {
	      return ' Check the render method of `' + name + '`.';
	    }
	  }
	  return '';
	}

	/**
	 * ------------------ The Life-Cycle of a Composite Component ------------------
	 *
	 * - constructor: Initialization of state. The instance is now retained.
	 *   - componentWillMount
	 *   - render
	 *   - [children's constructors]
	 *     - [children's componentWillMount and render]
	 *     - [children's componentDidMount]
	 *     - componentDidMount
	 *
	 *       Update Phases:
	 *       - componentWillReceiveProps (only called if parent updated)
	 *       - shouldComponentUpdate
	 *         - componentWillUpdate
	 *           - render
	 *           - [children's constructors or receive props phases]
	 *         - componentDidUpdate
	 *
	 *     - componentWillUnmount
	 *     - [children's componentWillUnmount]
	 *   - [children destroyed]
	 * - (destroyed): The instance is now blank, released by React and ready for GC.
	 *
	 * -----------------------------------------------------------------------------
	 */

	/**
	 * An incrementing ID assigned to each component when it is mounted. This is
	 * used to enforce the order in which `ReactUpdates` updates dirty components.
	 *
	 * @private
	 */
	var nextMountID = 1;

	/**
	 * @lends {ReactCompositeComponent.prototype}
	 */
	var ReactCompositeComponentMixin = {

	  /**
	   * Base constructor for all composite component.
	   *
	   * @param {ReactElement} element
	   * @final
	   * @internal
	   */
	  construct: function(element) {
	    this._currentElement = element;
	    this._rootNodeID = null;
	    this._instance = null;

	    // See ReactUpdateQueue
	    this._pendingElement = null;
	    this._pendingStateQueue = null;
	    this._pendingReplaceState = false;
	    this._pendingForceUpdate = false;

	    this._renderedComponent = null;

	    this._context = null;
	    this._mountOrder = 0;
	    this._isTopLevel = false;

	    // See ReactUpdates and ReactUpdateQueue.
	    this._pendingCallbacks = null;
	  },

	  /**
	   * Initializes the component, renders markup, and registers event listeners.
	   *
	   * @param {string} rootID DOM ID of the root node.
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @return {?string} Rendered markup to be inserted into the DOM.
	   * @final
	   * @internal
	   */
	  mountComponent: function(rootID, transaction, context) {
	    this._context = context;
	    this._mountOrder = nextMountID++;
	    this._rootNodeID = rootID;

	    var publicProps = this._processProps(this._currentElement.props);
	    var publicContext = this._processContext(this._currentElement._context);

	    var Component = ReactNativeComponent.getComponentClassForElement(
	      this._currentElement
	    );

	    // Initialize the public class
	    var inst = new Component(publicProps, publicContext);

	    if ("production" !== process.env.NODE_ENV) {
	      // This will throw later in _renderValidatedComponent, but add an early
	      // warning now to help debugging
	      ("production" !== process.env.NODE_ENV ? warning(
	        inst.render != null,
	        '%s(...): No `render` method found on the returned component ' +
	        'instance: you may have forgotten to define `render` in your ' +
	        'component or you may have accidentally tried to render an element ' +
	        'whose type is a function that isn\'t a React component.',
	        Component.displayName || Component.name || 'Component'
	      ) : null);
	    }

	    // These should be set up in the constructor, but as a convenience for
	    // simpler class abstractions, we set them up after the fact.
	    inst.props = publicProps;
	    inst.context = publicContext;
	    inst.refs = emptyObject;

	    this._instance = inst;

	    // Store a reference from the instance back to the internal representation
	    ReactInstanceMap.set(inst, this);

	    if ("production" !== process.env.NODE_ENV) {
	      this._warnIfContextsDiffer(this._currentElement._context, context);
	    }

	    if ("production" !== process.env.NODE_ENV) {
	      // Since plain JS classes are defined without any special initialization
	      // logic, we can not catch common errors early. Therefore, we have to
	      // catch them here, at initialization time, instead.
	      ("production" !== process.env.NODE_ENV ? warning(
	        !inst.getInitialState ||
	        inst.getInitialState.isReactClassApproved,
	        'getInitialState was defined on %s, a plain JavaScript class. ' +
	        'This is only supported for classes created using React.createClass. ' +
	        'Did you mean to define a state property instead?',
	        this.getName() || 'a component'
	      ) : null);
	      ("production" !== process.env.NODE_ENV ? warning(
	        !inst.getDefaultProps ||
	        inst.getDefaultProps.isReactClassApproved,
	        'getDefaultProps was defined on %s, a plain JavaScript class. ' +
	        'This is only supported for classes created using React.createClass. ' +
	        'Use a static property to define defaultProps instead.',
	        this.getName() || 'a component'
	      ) : null);
	      ("production" !== process.env.NODE_ENV ? warning(
	        !inst.propTypes,
	        'propTypes was defined as an instance property on %s. Use a static ' +
	        'property to define propTypes instead.',
	        this.getName() || 'a component'
	      ) : null);
	      ("production" !== process.env.NODE_ENV ? warning(
	        !inst.contextTypes,
	        'contextTypes was defined as an instance property on %s. Use a ' +
	        'static property to define contextTypes instead.',
	        this.getName() || 'a component'
	      ) : null);
	      ("production" !== process.env.NODE_ENV ? warning(
	        typeof inst.componentShouldUpdate !== 'function',
	        '%s has a method called ' +
	        'componentShouldUpdate(). Did you mean shouldComponentUpdate()? ' +
	        'The name is phrased as a question because the function is ' +
	        'expected to return a value.',
	        (this.getName() || 'A component')
	      ) : null);
	    }

	    var initialState = inst.state;
	    if (initialState === undefined) {
	      inst.state = initialState = null;
	    }
	    ("production" !== process.env.NODE_ENV ? invariant(
	      typeof initialState === 'object' && !Array.isArray(initialState),
	      '%s.state: must be set to an object or null',
	      this.getName() || 'ReactCompositeComponent'
	    ) : invariant(typeof initialState === 'object' && !Array.isArray(initialState)));

	    this._pendingStateQueue = null;
	    this._pendingReplaceState = false;
	    this._pendingForceUpdate = false;

	    var childContext;
	    var renderedElement;

	    var previouslyMounting = ReactLifeCycle.currentlyMountingInstance;
	    ReactLifeCycle.currentlyMountingInstance = this;
	    try {
	      if (inst.componentWillMount) {
	        inst.componentWillMount();
	        // When mounting, calls to `setState` by `componentWillMount` will set
	        // `this._pendingStateQueue` without triggering a re-render.
	        if (this._pendingStateQueue) {
	          inst.state = this._processPendingState(inst.props, inst.context);
	        }
	      }

	      childContext = this._getValidatedChildContext(context);
	      renderedElement = this._renderValidatedComponent(childContext);
	    } finally {
	      ReactLifeCycle.currentlyMountingInstance = previouslyMounting;
	    }

	    this._renderedComponent = this._instantiateReactComponent(
	      renderedElement,
	      this._currentElement.type // The wrapping type
	    );

	    var markup = ReactReconciler.mountComponent(
	      this._renderedComponent,
	      rootID,
	      transaction,
	      this._mergeChildContext(context, childContext)
	    );
	    if (inst.componentDidMount) {
	      transaction.getReactMountReady().enqueue(inst.componentDidMount, inst);
	    }

	    return markup;
	  },

	  /**
	   * Releases any resources allocated by `mountComponent`.
	   *
	   * @final
	   * @internal
	   */
	  unmountComponent: function() {
	    var inst = this._instance;

	    if (inst.componentWillUnmount) {
	      var previouslyUnmounting = ReactLifeCycle.currentlyUnmountingInstance;
	      ReactLifeCycle.currentlyUnmountingInstance = this;
	      try {
	        inst.componentWillUnmount();
	      } finally {
	        ReactLifeCycle.currentlyUnmountingInstance = previouslyUnmounting;
	      }
	    }

	    ReactReconciler.unmountComponent(this._renderedComponent);
	    this._renderedComponent = null;

	    // Reset pending fields
	    this._pendingStateQueue = null;
	    this._pendingReplaceState = false;
	    this._pendingForceUpdate = false;
	    this._pendingCallbacks = null;
	    this._pendingElement = null;

	    // These fields do not really need to be reset since this object is no
	    // longer accessible.
	    this._context = null;
	    this._rootNodeID = null;

	    // Delete the reference from the instance to this internal representation
	    // which allow the internals to be properly cleaned up even if the user
	    // leaks a reference to the public instance.
	    ReactInstanceMap.remove(inst);

	    // Some existing components rely on inst.props even after they've been
	    // destroyed (in event handlers).
	    // TODO: inst.props = null;
	    // TODO: inst.state = null;
	    // TODO: inst.context = null;
	  },

	  /**
	   * Schedule a partial update to the props. Only used for internal testing.
	   *
	   * @param {object} partialProps Subset of the next props.
	   * @param {?function} callback Called after props are updated.
	   * @final
	   * @internal
	   */
	  _setPropsInternal: function(partialProps, callback) {
	    // This is a deoptimized path. We optimize for always having an element.
	    // This creates an extra internal element.
	    var element = this._pendingElement || this._currentElement;
	    this._pendingElement = ReactElement.cloneAndReplaceProps(
	      element,
	      assign({}, element.props, partialProps)
	    );
	    ReactUpdates.enqueueUpdate(this, callback);
	  },

	  /**
	   * Filters the context object to only contain keys specified in
	   * `contextTypes`
	   *
	   * @param {object} context
	   * @return {?object}
	   * @private
	   */
	  _maskContext: function(context) {
	    var maskedContext = null;
	    // This really should be getting the component class for the element,
	    // but we know that we're not going to need it for built-ins.
	    if (typeof this._currentElement.type === 'string') {
	      return emptyObject;
	    }
	    var contextTypes = this._currentElement.type.contextTypes;
	    if (!contextTypes) {
	      return emptyObject;
	    }
	    maskedContext = {};
	    for (var contextName in contextTypes) {
	      maskedContext[contextName] = context[contextName];
	    }
	    return maskedContext;
	  },

	  /**
	   * Filters the context object to only contain keys specified in
	   * `contextTypes`, and asserts that they are valid.
	   *
	   * @param {object} context
	   * @return {?object}
	   * @private
	   */
	  _processContext: function(context) {
	    var maskedContext = this._maskContext(context);
	    if ("production" !== process.env.NODE_ENV) {
	      var Component = ReactNativeComponent.getComponentClassForElement(
	        this._currentElement
	      );
	      if (Component.contextTypes) {
	        this._checkPropTypes(
	          Component.contextTypes,
	          maskedContext,
	          ReactPropTypeLocations.context
	        );
	      }
	    }
	    return maskedContext;
	  },

	  /**
	   * @param {object} currentContext
	   * @return {object}
	   * @private
	   */
	  _getValidatedChildContext: function(currentContext) {
	    var inst = this._instance;
	    var childContext = inst.getChildContext && inst.getChildContext();
	    if (childContext) {
	      ("production" !== process.env.NODE_ENV ? invariant(
	        typeof inst.constructor.childContextTypes === 'object',
	        '%s.getChildContext(): childContextTypes must be defined in order to ' +
	        'use getChildContext().',
	        this.getName() || 'ReactCompositeComponent'
	      ) : invariant(typeof inst.constructor.childContextTypes === 'object'));
	      if ("production" !== process.env.NODE_ENV) {
	        this._checkPropTypes(
	          inst.constructor.childContextTypes,
	          childContext,
	          ReactPropTypeLocations.childContext
	        );
	      }
	      for (var name in childContext) {
	        ("production" !== process.env.NODE_ENV ? invariant(
	          name in inst.constructor.childContextTypes,
	          '%s.getChildContext(): key "%s" is not defined in childContextTypes.',
	          this.getName() || 'ReactCompositeComponent',
	          name
	        ) : invariant(name in inst.constructor.childContextTypes));
	      }
	      return childContext;
	    }
	    return null;
	  },

	  _mergeChildContext: function(currentContext, childContext) {
	    if (childContext) {
	      return assign({}, currentContext, childContext);
	    }
	    return currentContext;
	  },

	  /**
	   * Processes props by setting default values for unspecified props and
	   * asserting that the props are valid. Does not mutate its argument; returns
	   * a new props object with defaults merged in.
	   *
	   * @param {object} newProps
	   * @return {object}
	   * @private
	   */
	  _processProps: function(newProps) {
	    if ("production" !== process.env.NODE_ENV) {
	      var Component = ReactNativeComponent.getComponentClassForElement(
	        this._currentElement
	      );
	      if (Component.propTypes) {
	        this._checkPropTypes(
	          Component.propTypes,
	          newProps,
	          ReactPropTypeLocations.prop
	        );
	      }
	    }
	    return newProps;
	  },

	  /**
	   * Assert that the props are valid
	   *
	   * @param {object} propTypes Map of prop name to a ReactPropType
	   * @param {object} props
	   * @param {string} location e.g. "prop", "context", "child context"
	   * @private
	   */
	  _checkPropTypes: function(propTypes, props, location) {
	    // TODO: Stop validating prop types here and only use the element
	    // validation.
	    var componentName = this.getName();
	    for (var propName in propTypes) {
	      if (propTypes.hasOwnProperty(propName)) {
	        var error;
	        try {
	          // This is intentionally an invariant that gets caught. It's the same
	          // behavior as without this statement except with a better message.
	          ("production" !== process.env.NODE_ENV ? invariant(
	            typeof propTypes[propName] === 'function',
	            '%s: %s type `%s` is invalid; it must be a function, usually ' +
	            'from React.PropTypes.',
	            componentName || 'React class',
	            ReactPropTypeLocationNames[location],
	            propName
	          ) : invariant(typeof propTypes[propName] === 'function'));
	          error = propTypes[propName](props, propName, componentName, location);
	        } catch (ex) {
	          error = ex;
	        }
	        if (error instanceof Error) {
	          // We may want to extend this logic for similar errors in
	          // React.render calls, so I'm abstracting it away into
	          // a function to minimize refactoring in the future
	          var addendum = getDeclarationErrorAddendum(this);

	          if (location === ReactPropTypeLocations.prop) {
	            // Preface gives us something to blacklist in warning module
	            ("production" !== process.env.NODE_ENV ? warning(
	              false,
	              'Failed Composite propType: %s%s',
	              error.message,
	              addendum
	            ) : null);
	          } else {
	            ("production" !== process.env.NODE_ENV ? warning(
	              false,
	              'Failed Context Types: %s%s',
	              error.message,
	              addendum
	            ) : null);
	          }
	        }
	      }
	    }
	  },

	  receiveComponent: function(nextElement, transaction, nextContext) {
	    var prevElement = this._currentElement;
	    var prevContext = this._context;

	    this._pendingElement = null;

	    this.updateComponent(
	      transaction,
	      prevElement,
	      nextElement,
	      prevContext,
	      nextContext
	    );
	  },

	  /**
	   * If any of `_pendingElement`, `_pendingStateQueue`, or `_pendingForceUpdate`
	   * is set, update the component.
	   *
	   * @param {ReactReconcileTransaction} transaction
	   * @internal
	   */
	  performUpdateIfNecessary: function(transaction) {
	    if (this._pendingElement != null) {
	      ReactReconciler.receiveComponent(
	        this,
	        this._pendingElement || this._currentElement,
	        transaction,
	        this._context
	      );
	    }

	    if (this._pendingStateQueue !== null || this._pendingForceUpdate) {
	      if ("production" !== process.env.NODE_ENV) {
	        ReactElementValidator.checkAndWarnForMutatedProps(
	          this._currentElement
	        );
	      }

	      this.updateComponent(
	        transaction,
	        this._currentElement,
	        this._currentElement,
	        this._context,
	        this._context
	      );
	    }
	  },

	  /**
	   * Compare two contexts, warning if they are different
	   * TODO: Remove this check when owner-context is removed
	   */
	   _warnIfContextsDiffer: function(ownerBasedContext, parentBasedContext) {
	    ownerBasedContext = this._maskContext(ownerBasedContext);
	    parentBasedContext = this._maskContext(parentBasedContext);
	    var parentKeys = Object.keys(parentBasedContext).sort();
	    var displayName = this.getName() || 'ReactCompositeComponent';
	    for (var i = 0; i < parentKeys.length; i++) {
	      var key = parentKeys[i];
	      ("production" !== process.env.NODE_ENV ? warning(
	        ownerBasedContext[key] === parentBasedContext[key],
	        'owner-based and parent-based contexts differ '  +
	        '(values: `%s` vs `%s`) for key (%s) while mounting %s ' +
	        '(see: http://fb.me/react-context-by-parent)',
	        ownerBasedContext[key],
	        parentBasedContext[key],
	        key,
	        displayName
	      ) : null);
	    }
	  },

	  /**
	   * Perform an update to a mounted component. The componentWillReceiveProps and
	   * shouldComponentUpdate methods are called, then (assuming the update isn't
	   * skipped) the remaining update lifecycle methods are called and the DOM
	   * representation is updated.
	   *
	   * By default, this implements React's rendering and reconciliation algorithm.
	   * Sophisticated clients may wish to override this.
	   *
	   * @param {ReactReconcileTransaction} transaction
	   * @param {ReactElement} prevParentElement
	   * @param {ReactElement} nextParentElement
	   * @internal
	   * @overridable
	   */
	  updateComponent: function(
	    transaction,
	    prevParentElement,
	    nextParentElement,
	    prevUnmaskedContext,
	    nextUnmaskedContext
	  ) {
	    var inst = this._instance;

	    var nextContext = inst.context;
	    var nextProps = inst.props;

	    // Distinguish between a props update versus a simple state update
	    if (prevParentElement !== nextParentElement) {
	      nextContext = this._processContext(nextParentElement._context);
	      nextProps = this._processProps(nextParentElement.props);

	      if ("production" !== process.env.NODE_ENV) {
	        if (nextUnmaskedContext != null) {
	          this._warnIfContextsDiffer(
	            nextParentElement._context,
	            nextUnmaskedContext
	          );
	        }
	      }

	      // An update here will schedule an update but immediately set
	      // _pendingStateQueue which will ensure that any state updates gets
	      // immediately reconciled instead of waiting for the next batch.

	      if (inst.componentWillReceiveProps) {
	        inst.componentWillReceiveProps(nextProps, nextContext);
	      }
	    }

	    var nextState = this._processPendingState(nextProps, nextContext);

	    var shouldUpdate =
	      this._pendingForceUpdate ||
	      !inst.shouldComponentUpdate ||
	      inst.shouldComponentUpdate(nextProps, nextState, nextContext);

	    if ("production" !== process.env.NODE_ENV) {
	      ("production" !== process.env.NODE_ENV ? warning(
	        typeof shouldUpdate !== 'undefined',
	        '%s.shouldComponentUpdate(): Returned undefined instead of a ' +
	        'boolean value. Make sure to return true or false.',
	        this.getName() || 'ReactCompositeComponent'
	      ) : null);
	    }

	    if (shouldUpdate) {
	      this._pendingForceUpdate = false;
	      // Will set `this.props`, `this.state` and `this.context`.
	      this._performComponentUpdate(
	        nextParentElement,
	        nextProps,
	        nextState,
	        nextContext,
	        transaction,
	        nextUnmaskedContext
	      );
	    } else {
	      // If it's determined that a component should not update, we still want
	      // to set props and state but we shortcut the rest of the update.
	      this._currentElement = nextParentElement;
	      this._context = nextUnmaskedContext;
	      inst.props = nextProps;
	      inst.state = nextState;
	      inst.context = nextContext;
	    }
	  },

	  _processPendingState: function(props, context) {
	    var inst = this._instance;
	    var queue = this._pendingStateQueue;
	    var replace = this._pendingReplaceState;
	    this._pendingReplaceState = false;
	    this._pendingStateQueue = null;

	    if (!queue) {
	      return inst.state;
	    }

	    if (replace && queue.length === 1) {
	      return queue[0];
	    }

	    var nextState = assign({}, replace ? queue[0] : inst.state);
	    for (var i = replace ? 1 : 0; i < queue.length; i++) {
	      var partial = queue[i];
	      assign(
	        nextState,
	        typeof partial === 'function' ?
	          partial.call(inst, nextState, props, context) :
	          partial
	      );
	    }

	    return nextState;
	  },

	  /**
	   * Merges new props and state, notifies delegate methods of update and
	   * performs update.
	   *
	   * @param {ReactElement} nextElement Next element
	   * @param {object} nextProps Next public object to set as properties.
	   * @param {?object} nextState Next object to set as state.
	   * @param {?object} nextContext Next public object to set as context.
	   * @param {ReactReconcileTransaction} transaction
	   * @param {?object} unmaskedContext
	   * @private
	   */
	  _performComponentUpdate: function(
	    nextElement,
	    nextProps,
	    nextState,
	    nextContext,
	    transaction,
	    unmaskedContext
	  ) {
	    var inst = this._instance;

	    var prevProps = inst.props;
	    var prevState = inst.state;
	    var prevContext = inst.context;

	    if (inst.componentWillUpdate) {
	      inst.componentWillUpdate(nextProps, nextState, nextContext);
	    }

	    this._currentElement = nextElement;
	    this._context = unmaskedContext;
	    inst.props = nextProps;
	    inst.state = nextState;
	    inst.context = nextContext;

	    this._updateRenderedComponent(transaction, unmaskedContext);

	    if (inst.componentDidUpdate) {
	      transaction.getReactMountReady().enqueue(
	        inst.componentDidUpdate.bind(inst, prevProps, prevState, prevContext),
	        inst
	      );
	    }
	  },

	  /**
	   * Call the component's `render` method and update the DOM accordingly.
	   *
	   * @param {ReactReconcileTransaction} transaction
	   * @internal
	   */
	  _updateRenderedComponent: function(transaction, context) {
	    var prevComponentInstance = this._renderedComponent;
	    var prevRenderedElement = prevComponentInstance._currentElement;
	    var childContext = this._getValidatedChildContext();
	    var nextRenderedElement = this._renderValidatedComponent(childContext);
	    if (shouldUpdateReactComponent(prevRenderedElement, nextRenderedElement)) {
	      ReactReconciler.receiveComponent(
	        prevComponentInstance,
	        nextRenderedElement,
	        transaction,
	        this._mergeChildContext(context, childContext)
	      );
	    } else {
	      // These two IDs are actually the same! But nothing should rely on that.
	      var thisID = this._rootNodeID;
	      var prevComponentID = prevComponentInstance._rootNodeID;
	      ReactReconciler.unmountComponent(prevComponentInstance);

	      this._renderedComponent = this._instantiateReactComponent(
	        nextRenderedElement,
	        this._currentElement.type
	      );
	      var nextMarkup = ReactReconciler.mountComponent(
	        this._renderedComponent,
	        thisID,
	        transaction,
	        this._mergeChildContext(context, childContext)
	      );
	      this._replaceNodeWithMarkupByID(prevComponentID, nextMarkup);
	    }
	  },

	  /**
	   * @protected
	   */
	  _replaceNodeWithMarkupByID: function(prevComponentID, nextMarkup) {
	    ReactComponentEnvironment.replaceNodeWithMarkupByID(
	      prevComponentID,
	      nextMarkup
	    );
	  },

	  /**
	   * @protected
	   */
	  _renderValidatedComponentWithoutOwnerOrContext: function() {
	    var inst = this._instance;
	    var renderedComponent = inst.render();
	    if ("production" !== process.env.NODE_ENV) {
	      // We allow auto-mocks to proceed as if they're returning null.
	      if (typeof renderedComponent === 'undefined' &&
	          inst.render._isMockFunction) {
	        // This is probably bad practice. Consider warning here and
	        // deprecating this convenience.
	        renderedComponent = null;
	      }
	    }

	    return renderedComponent;
	  },

	  /**
	   * @private
	   */
	  _renderValidatedComponent: function(childContext) {
	    var renderedComponent;
	    var previousContext = ReactContext.current;
	    ReactContext.current = this._mergeChildContext(
	      this._currentElement._context,
	      childContext
	    );
	    ReactCurrentOwner.current = this;
	    try {
	      renderedComponent =
	        this._renderValidatedComponentWithoutOwnerOrContext();
	    } finally {
	      ReactContext.current = previousContext;
	      ReactCurrentOwner.current = null;
	    }
	    ("production" !== process.env.NODE_ENV ? invariant(
	      // TODO: An `isValidNode` function would probably be more appropriate
	      renderedComponent === null || renderedComponent === false ||
	      ReactElement.isValidElement(renderedComponent),
	      '%s.render(): A valid ReactComponent must be returned. You may have ' +
	        'returned undefined, an array or some other invalid object.',
	      this.getName() || 'ReactCompositeComponent'
	    ) : invariant(// TODO: An `isValidNode` function would probably be more appropriate
	    renderedComponent === null || renderedComponent === false ||
	    ReactElement.isValidElement(renderedComponent)));
	    return renderedComponent;
	  },

	  /**
	   * Lazily allocates the refs object and stores `component` as `ref`.
	   *
	   * @param {string} ref Reference name.
	   * @param {component} component Component to store as `ref`.
	   * @final
	   * @private
	   */
	  attachRef: function(ref, component) {
	    var inst = this.getPublicInstance();
	    var refs = inst.refs === emptyObject ? (inst.refs = {}) : inst.refs;
	    refs[ref] = component.getPublicInstance();
	  },

	  /**
	   * Detaches a reference name.
	   *
	   * @param {string} ref Name to dereference.
	   * @final
	   * @private
	   */
	  detachRef: function(ref) {
	    var refs = this.getPublicInstance().refs;
	    delete refs[ref];
	  },

	  /**
	   * Get a text description of the component that can be used to identify it
	   * in error messages.
	   * @return {string} The name or null.
	   * @internal
	   */
	  getName: function() {
	    var type = this._currentElement.type;
	    var constructor = this._instance && this._instance.constructor;
	    return (
	      type.displayName || (constructor && constructor.displayName) ||
	      type.name || (constructor && constructor.name) ||
	      null
	    );
	  },

	  /**
	   * Get the publicly accessible representation of this component - i.e. what
	   * is exposed by refs and returned by React.render. Can be null for stateless
	   * components.
	   *
	   * @return {ReactComponent} the public component instance.
	   * @internal
	   */
	  getPublicInstance: function() {
	    return this._instance;
	  },

	  // Stub
	  _instantiateReactComponent: null

	};

	ReactPerf.measureMethods(
	  ReactCompositeComponentMixin,
	  'ReactCompositeComponent',
	  {
	    mountComponent: 'mountComponent',
	    updateComponent: 'updateComponent',
	    _renderValidatedComponent: '_renderValidatedComponent'
	  }
	);

	var ReactCompositeComponent = {

	  Mixin: ReactCompositeComponentMixin

	};

	module.exports = ReactCompositeComponent;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 86 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactComponentEnvironment
	 */

	'use strict';

	var invariant = __webpack_require__(8);

	var injected = false;

	var ReactComponentEnvironment = {

	  /**
	   * Optionally injectable environment dependent cleanup hook. (server vs.
	   * browser etc). Example: A browser system caches DOM nodes based on component
	   * ID and must remove that cache entry when this instance is unmounted.
	   */
	  unmountIDFromEnvironment: null,

	  /**
	   * Optionally injectable hook for swapping out mount images in the middle of
	   * the tree.
	   */
	  replaceNodeWithMarkupByID: null,

	  /**
	   * Optionally injectable hook for processing a queue of child updates. Will
	   * later move into MultiChildComponents.
	   */
	  processChildrenUpdates: null,

	  injection: {
	    injectEnvironment: function(environment) {
	      ("production" !== process.env.NODE_ENV ? invariant(
	        !injected,
	        'ReactCompositeComponent: injectEnvironment() can only be called once.'
	      ) : invariant(!injected));
	      ReactComponentEnvironment.unmountIDFromEnvironment =
	        environment.unmountIDFromEnvironment;
	      ReactComponentEnvironment.replaceNodeWithMarkupByID =
	        environment.replaceNodeWithMarkupByID;
	      ReactComponentEnvironment.processChildrenUpdates =
	        environment.processChildrenUpdates;
	      injected = true;
	    }
	  }

	};

	module.exports = ReactComponentEnvironment;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 87 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule shouldUpdateReactComponent
	 * @typechecks static-only
	 */

	'use strict';

	var warning = __webpack_require__(16);

	/**
	 * Given a `prevElement` and `nextElement`, determines if the existing
	 * instance should be updated as opposed to being destroyed or replaced by a new
	 * instance. Both arguments are elements. This ensures that this logic can
	 * operate on stateless trees without any backing instance.
	 *
	 * @param {?object} prevElement
	 * @param {?object} nextElement
	 * @return {boolean} True if the existing instance should be updated.
	 * @protected
	 */
	function shouldUpdateReactComponent(prevElement, nextElement) {
	  if (prevElement != null && nextElement != null) {
	    var prevType = typeof prevElement;
	    var nextType = typeof nextElement;
	    if (prevType === 'string' || prevType === 'number') {
	      return (nextType === 'string' || nextType === 'number');
	    } else {
	      if (nextType === 'object' &&
	          prevElement.type === nextElement.type &&
	          prevElement.key === nextElement.key) {
	        var ownersMatch = prevElement._owner === nextElement._owner;
	        var prevName = null;
	        var nextName = null;
	        var nextDisplayName = null;
	        if ("production" !== process.env.NODE_ENV) {
	          if (!ownersMatch) {
	            if (prevElement._owner != null &&
	                prevElement._owner.getPublicInstance() != null &&
	                prevElement._owner.getPublicInstance().constructor != null) {
	              prevName =
	                prevElement._owner.getPublicInstance().constructor.displayName;
	            }
	            if (nextElement._owner != null &&
	                nextElement._owner.getPublicInstance() != null &&
	                nextElement._owner.getPublicInstance().constructor != null) {
	              nextName =
	                nextElement._owner.getPublicInstance().constructor.displayName;
	            }
	            if (nextElement.type != null &&
	                nextElement.type.displayName != null) {
	              nextDisplayName = nextElement.type.displayName;
	            }
	            if (nextElement.type != null && typeof nextElement.type === 'string') {
	              nextDisplayName = nextElement.type;
	            }
	            if (typeof nextElement.type !== 'string' ||
	                nextElement.type === 'input' ||
	                nextElement.type === 'textarea') {
	              if ((prevElement._owner != null &&
	                  prevElement._owner._isOwnerNecessary === false) ||
	                  (nextElement._owner != null &&
	                  nextElement._owner._isOwnerNecessary === false)) {
	                if (prevElement._owner != null) {
	                  prevElement._owner._isOwnerNecessary = true;
	                }
	                if (nextElement._owner != null) {
	                  nextElement._owner._isOwnerNecessary = true;
	                }
	                ("production" !== process.env.NODE_ENV ? warning(
	                  false,
	                  '<%s /> is being rendered by both %s and %s using the same ' +
	                  'key (%s) in the same place. Currently, this means that ' +
	                  'they don\'t preserve state. This behavior should be very ' +
	                  'rare so we\'re considering deprecating it. Please contact ' +
	                  'the React team and explain your use case so that we can ' +
	                  'take that into consideration.',
	                  nextDisplayName || 'Unknown Component',
	                  prevName || '[Unknown]',
	                  nextName || '[Unknown]',
	                  prevElement.key
	                ) : null);
	              }
	            }
	          }
	        }
	        return ownersMatch;
	      }
	    }
	  }
	  return false;
	}

	module.exports = shouldUpdateReactComponent;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 88 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMComponent
	 * @typechecks static-only
	 */

	/* global hasOwnProperty:true */

	'use strict';

	var CSSPropertyOperations = __webpack_require__(50);
	var DOMProperty = __webpack_require__(45);
	var DOMPropertyOperations = __webpack_require__(44);
	var ReactBrowserEventEmitter = __webpack_require__(69);
	var ReactComponentBrowserEnvironment =
	  __webpack_require__(48);
	var ReactMount = __webpack_require__(68);
	var ReactMultiChild = __webpack_require__(89);
	var ReactPerf = __webpack_require__(27);

	var assign = __webpack_require__(14);
	var escapeTextContentForBrowser = __webpack_require__(47);
	var invariant = __webpack_require__(8);
	var isEventSupported = __webpack_require__(76);
	var keyOf = __webpack_require__(40);
	var warning = __webpack_require__(16);

	var deleteListener = ReactBrowserEventEmitter.deleteListener;
	var listenTo = ReactBrowserEventEmitter.listenTo;
	var registrationNameModules = ReactBrowserEventEmitter.registrationNameModules;

	// For quickly matching children type, to test if can be treated as content.
	var CONTENT_TYPES = {'string': true, 'number': true};

	var STYLE = keyOf({style: null});

	var ELEMENT_NODE_TYPE = 1;

	/**
	 * Optionally injectable operations for mutating the DOM
	 */
	var BackendIDOperations = null;

	/**
	 * @param {?object} props
	 */
	function assertValidProps(props) {
	  if (!props) {
	    return;
	  }
	  // Note the use of `==` which checks for null or undefined.
	  if (props.dangerouslySetInnerHTML != null) {
	    ("production" !== process.env.NODE_ENV ? invariant(
	      props.children == null,
	      'Can only set one of `children` or `props.dangerouslySetInnerHTML`.'
	    ) : invariant(props.children == null));
	    ("production" !== process.env.NODE_ENV ? invariant(
	      typeof props.dangerouslySetInnerHTML === 'object' &&
	      '__html' in props.dangerouslySetInnerHTML,
	      '`props.dangerouslySetInnerHTML` must be in the form `{__html: ...}`. ' +
	      'Please visit https://fb.me/react-invariant-dangerously-set-inner-html ' +
	      'for more information.'
	    ) : invariant(typeof props.dangerouslySetInnerHTML === 'object' &&
	    '__html' in props.dangerouslySetInnerHTML));
	  }
	  if ("production" !== process.env.NODE_ENV) {
	    ("production" !== process.env.NODE_ENV ? warning(
	      props.innerHTML == null,
	      'Directly setting property `innerHTML` is not permitted. ' +
	      'For more information, lookup documentation on `dangerouslySetInnerHTML`.'
	    ) : null);
	    ("production" !== process.env.NODE_ENV ? warning(
	      !props.contentEditable || props.children == null,
	      'A component is `contentEditable` and contains `children` managed by ' +
	      'React. It is now your responsibility to guarantee that none of ' +
	      'those nodes are unexpectedly modified or duplicated. This is ' +
	      'probably not intentional.'
	    ) : null);
	  }
	  ("production" !== process.env.NODE_ENV ? invariant(
	    props.style == null || typeof props.style === 'object',
	    'The `style` prop expects a mapping from style properties to values, ' +
	    'not a string. For example, style={{marginRight: spacing + \'em\'}} when ' +
	    'using JSX.'
	  ) : invariant(props.style == null || typeof props.style === 'object'));
	}

	function putListener(id, registrationName, listener, transaction) {
	  if ("production" !== process.env.NODE_ENV) {
	    // IE8 has no API for event capturing and the `onScroll` event doesn't
	    // bubble.
	    ("production" !== process.env.NODE_ENV ? warning(
	      registrationName !== 'onScroll' || isEventSupported('scroll', true),
	      'This browser doesn\'t support the `onScroll` event'
	    ) : null);
	  }
	  var container = ReactMount.findReactContainerForID(id);
	  if (container) {
	    var doc = container.nodeType === ELEMENT_NODE_TYPE ?
	      container.ownerDocument :
	      container;
	    listenTo(registrationName, doc);
	  }
	  transaction.getPutListenerQueue().enqueuePutListener(
	    id,
	    registrationName,
	    listener
	  );
	}

	// For HTML, certain tags should omit their close tag. We keep a whitelist for
	// those special cased tags.

	var omittedCloseTags = {
	  'area': true,
	  'base': true,
	  'br': true,
	  'col': true,
	  'embed': true,
	  'hr': true,
	  'img': true,
	  'input': true,
	  'keygen': true,
	  'link': true,
	  'meta': true,
	  'param': true,
	  'source': true,
	  'track': true,
	  'wbr': true
	  // NOTE: menuitem's close tag should be omitted, but that causes problems.
	};

	// We accept any tag to be rendered but since this gets injected into abitrary
	// HTML, we want to make sure that it's a safe tag.
	// http://www.w3.org/TR/REC-xml/#NT-Name

	var VALID_TAG_REGEX = /^[a-zA-Z][a-zA-Z:_\.\-\d]*$/; // Simplified subset
	var validatedTagCache = {};
	var hasOwnProperty = {}.hasOwnProperty;

	function validateDangerousTag(tag) {
	  if (!hasOwnProperty.call(validatedTagCache, tag)) {
	    ("production" !== process.env.NODE_ENV ? invariant(VALID_TAG_REGEX.test(tag), 'Invalid tag: %s', tag) : invariant(VALID_TAG_REGEX.test(tag)));
	    validatedTagCache[tag] = true;
	  }
	}

	/**
	 * Creates a new React class that is idempotent and capable of containing other
	 * React components. It accepts event listeners and DOM properties that are
	 * valid according to `DOMProperty`.
	 *
	 *  - Event listeners: `onClick`, `onMouseDown`, etc.
	 *  - DOM properties: `className`, `name`, `title`, etc.
	 *
	 * The `style` property functions differently from the DOM API. It accepts an
	 * object mapping of style properties to values.
	 *
	 * @constructor ReactDOMComponent
	 * @extends ReactMultiChild
	 */
	function ReactDOMComponent(tag) {
	  validateDangerousTag(tag);
	  this._tag = tag;
	  this._renderedChildren = null;
	  this._previousStyleCopy = null;
	  this._rootNodeID = null;
	}

	ReactDOMComponent.displayName = 'ReactDOMComponent';

	ReactDOMComponent.Mixin = {

	  construct: function(element) {
	    this._currentElement = element;
	  },

	  /**
	   * Generates root tag markup then recurses. This method has side effects and
	   * is not idempotent.
	   *
	   * @internal
	   * @param {string} rootID The root DOM ID for this node.
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @return {string} The computed markup.
	   */
	  mountComponent: function(rootID, transaction, context) {
	    this._rootNodeID = rootID;
	    assertValidProps(this._currentElement.props);
	    var closeTag = omittedCloseTags[this._tag] ? '' : '</' + this._tag + '>';
	    return (
	      this._createOpenTagMarkupAndPutListeners(transaction) +
	      this._createContentMarkup(transaction, context) +
	      closeTag
	    );
	  },

	  /**
	   * Creates markup for the open tag and all attributes.
	   *
	   * This method has side effects because events get registered.
	   *
	   * Iterating over object properties is faster than iterating over arrays.
	   * @see http://jsperf.com/obj-vs-arr-iteration
	   *
	   * @private
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @return {string} Markup of opening tag.
	   */
	  _createOpenTagMarkupAndPutListeners: function(transaction) {
	    var props = this._currentElement.props;
	    var ret = '<' + this._tag;

	    for (var propKey in props) {
	      if (!props.hasOwnProperty(propKey)) {
	        continue;
	      }
	      var propValue = props[propKey];
	      if (propValue == null) {
	        continue;
	      }
	      if (registrationNameModules.hasOwnProperty(propKey)) {
	        putListener(this._rootNodeID, propKey, propValue, transaction);
	      } else {
	        if (propKey === STYLE) {
	          if (propValue) {
	            propValue = this._previousStyleCopy = assign({}, props.style);
	          }
	          propValue = CSSPropertyOperations.createMarkupForStyles(propValue);
	        }
	        var markup =
	          DOMPropertyOperations.createMarkupForProperty(propKey, propValue);
	        if (markup) {
	          ret += ' ' + markup;
	        }
	      }
	    }

	    // For static pages, no need to put React ID and checksum. Saves lots of
	    // bytes.
	    if (transaction.renderToStaticMarkup) {
	      return ret + '>';
	    }

	    var markupForID = DOMPropertyOperations.createMarkupForID(this._rootNodeID);
	    return ret + ' ' + markupForID + '>';
	  },

	  /**
	   * Creates markup for the content between the tags.
	   *
	   * @private
	   * @param {ReactReconcileTransaction|ReactServerRenderingTransaction} transaction
	   * @param {object} context
	   * @return {string} Content markup.
	   */
	  _createContentMarkup: function(transaction, context) {
	    var prefix = '';
	    if (this._tag === 'listing' ||
	        this._tag === 'pre' ||
	        this._tag === 'textarea') {
	      // Add an initial newline because browsers ignore the first newline in
	      // a <listing>, <pre>, or <textarea> as an "authoring convenience" -- see
	      // https://html.spec.whatwg.org/multipage/syntax.html#parsing-main-inbody.
	      prefix = '\n';
	    }

	    var props = this._currentElement.props;

	    // Intentional use of != to avoid catching zero/false.
	    var innerHTML = props.dangerouslySetInnerHTML;
	    if (innerHTML != null) {
	      if (innerHTML.__html != null) {
	        return prefix + innerHTML.__html;
	      }
	    } else {
	      var contentToUse =
	        CONTENT_TYPES[typeof props.children] ? props.children : null;
	      var childrenToUse = contentToUse != null ? null : props.children;
	      if (contentToUse != null) {
	        return prefix + escapeTextContentForBrowser(contentToUse);
	      } else if (childrenToUse != null) {
	        var mountImages = this.mountChildren(
	          childrenToUse,
	          transaction,
	          context
	        );
	        return prefix + mountImages.join('');
	      }
	    }
	    return prefix;
	  },

	  receiveComponent: function(nextElement, transaction, context) {
	    var prevElement = this._currentElement;
	    this._currentElement = nextElement;
	    this.updateComponent(transaction, prevElement, nextElement, context);
	  },

	  /**
	   * Updates a native DOM component after it has already been allocated and
	   * attached to the DOM. Reconciles the root DOM node, then recurses.
	   *
	   * @param {ReactReconcileTransaction} transaction
	   * @param {ReactElement} prevElement
	   * @param {ReactElement} nextElement
	   * @internal
	   * @overridable
	   */
	  updateComponent: function(transaction, prevElement, nextElement, context) {
	    assertValidProps(this._currentElement.props);
	    this._updateDOMProperties(prevElement.props, transaction);
	    this._updateDOMChildren(prevElement.props, transaction, context);
	  },

	  /**
	   * Reconciles the properties by detecting differences in property values and
	   * updating the DOM as necessary. This function is probably the single most
	   * critical path for performance optimization.
	   *
	   * TODO: Benchmark whether checking for changed values in memory actually
	   *       improves performance (especially statically positioned elements).
	   * TODO: Benchmark the effects of putting this at the top since 99% of props
	   *       do not change for a given reconciliation.
	   * TODO: Benchmark areas that can be improved with caching.
	   *
	   * @private
	   * @param {object} lastProps
	   * @param {ReactReconcileTransaction} transaction
	   */
	  _updateDOMProperties: function(lastProps, transaction) {
	    var nextProps = this._currentElement.props;
	    var propKey;
	    var styleName;
	    var styleUpdates;
	    for (propKey in lastProps) {
	      if (nextProps.hasOwnProperty(propKey) ||
	         !lastProps.hasOwnProperty(propKey)) {
	        continue;
	      }
	      if (propKey === STYLE) {
	        var lastStyle = this._previousStyleCopy;
	        for (styleName in lastStyle) {
	          if (lastStyle.hasOwnProperty(styleName)) {
	            styleUpdates = styleUpdates || {};
	            styleUpdates[styleName] = '';
	          }
	        }
	        this._previousStyleCopy = null;
	      } else if (registrationNameModules.hasOwnProperty(propKey)) {
	        deleteListener(this._rootNodeID, propKey);
	      } else if (
	          DOMProperty.isStandardName[propKey] ||
	          DOMProperty.isCustomAttribute(propKey)) {
	        BackendIDOperations.deletePropertyByID(
	          this._rootNodeID,
	          propKey
	        );
	      }
	    }
	    for (propKey in nextProps) {
	      var nextProp = nextProps[propKey];
	      var lastProp = propKey === STYLE ?
	        this._previousStyleCopy :
	        lastProps[propKey];
	      if (!nextProps.hasOwnProperty(propKey) || nextProp === lastProp) {
	        continue;
	      }
	      if (propKey === STYLE) {
	        if (nextProp) {
	          nextProp = this._previousStyleCopy = assign({}, nextProp);
	        } else {
	          this._previousStyleCopy = null;
	        }
	        if (lastProp) {
	          // Unset styles on `lastProp` but not on `nextProp`.
	          for (styleName in lastProp) {
	            if (lastProp.hasOwnProperty(styleName) &&
	                (!nextProp || !nextProp.hasOwnProperty(styleName))) {
	              styleUpdates = styleUpdates || {};
	              styleUpdates[styleName] = '';
	            }
	          }
	          // Update styles that changed since `lastProp`.
	          for (styleName in nextProp) {
	            if (nextProp.hasOwnProperty(styleName) &&
	                lastProp[styleName] !== nextProp[styleName]) {
	              styleUpdates = styleUpdates || {};
	              styleUpdates[styleName] = nextProp[styleName];
	            }
	          }
	        } else {
	          // Relies on `updateStylesByID` not mutating `styleUpdates`.
	          styleUpdates = nextProp;
	        }
	      } else if (registrationNameModules.hasOwnProperty(propKey)) {
	        putListener(this._rootNodeID, propKey, nextProp, transaction);
	      } else if (
	          DOMProperty.isStandardName[propKey] ||
	          DOMProperty.isCustomAttribute(propKey)) {
	        BackendIDOperations.updatePropertyByID(
	          this._rootNodeID,
	          propKey,
	          nextProp
	        );
	      }
	    }
	    if (styleUpdates) {
	      BackendIDOperations.updateStylesByID(
	        this._rootNodeID,
	        styleUpdates
	      );
	    }
	  },

	  /**
	   * Reconciles the children with the various properties that affect the
	   * children content.
	   *
	   * @param {object} lastProps
	   * @param {ReactReconcileTransaction} transaction
	   */
	  _updateDOMChildren: function(lastProps, transaction, context) {
	    var nextProps = this._currentElement.props;

	    var lastContent =
	      CONTENT_TYPES[typeof lastProps.children] ? lastProps.children : null;
	    var nextContent =
	      CONTENT_TYPES[typeof nextProps.children] ? nextProps.children : null;

	    var lastHtml =
	      lastProps.dangerouslySetInnerHTML &&
	      lastProps.dangerouslySetInnerHTML.__html;
	    var nextHtml =
	      nextProps.dangerouslySetInnerHTML &&
	      nextProps.dangerouslySetInnerHTML.__html;

	    // Note the use of `!=` which checks for null or undefined.
	    var lastChildren = lastContent != null ? null : lastProps.children;
	    var nextChildren = nextContent != null ? null : nextProps.children;

	    // If we're switching from children to content/html or vice versa, remove
	    // the old content
	    var lastHasContentOrHtml = lastContent != null || lastHtml != null;
	    var nextHasContentOrHtml = nextContent != null || nextHtml != null;
	    if (lastChildren != null && nextChildren == null) {
	      this.updateChildren(null, transaction, context);
	    } else if (lastHasContentOrHtml && !nextHasContentOrHtml) {
	      this.updateTextContent('');
	    }

	    if (nextContent != null) {
	      if (lastContent !== nextContent) {
	        this.updateTextContent('' + nextContent);
	      }
	    } else if (nextHtml != null) {
	      if (lastHtml !== nextHtml) {
	        BackendIDOperations.updateInnerHTMLByID(
	          this._rootNodeID,
	          nextHtml
	        );
	      }
	    } else if (nextChildren != null) {
	      this.updateChildren(nextChildren, transaction, context);
	    }
	  },

	  /**
	   * Destroys all event registrations for this instance. Does not remove from
	   * the DOM. That must be done by the parent.
	   *
	   * @internal
	   */
	  unmountComponent: function() {
	    this.unmountChildren();
	    ReactBrowserEventEmitter.deleteAllListeners(this._rootNodeID);
	    ReactComponentBrowserEnvironment.unmountIDFromEnvironment(this._rootNodeID);
	    this._rootNodeID = null;
	  }

	};

	ReactPerf.measureMethods(ReactDOMComponent, 'ReactDOMComponent', {
	  mountComponent: 'mountComponent',
	  updateComponent: 'updateComponent'
	});

	assign(
	  ReactDOMComponent.prototype,
	  ReactDOMComponent.Mixin,
	  ReactMultiChild.Mixin
	);

	ReactDOMComponent.injection = {
	  injectIDOperations: function(IDOperations) {
	    ReactDOMComponent.BackendIDOperations = BackendIDOperations = IDOperations;
	  }
	};

	module.exports = ReactDOMComponent;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 89 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactMultiChild
	 * @typechecks static-only
	 */

	'use strict';

	var ReactComponentEnvironment = __webpack_require__(86);
	var ReactMultiChildUpdateTypes = __webpack_require__(65);

	var ReactReconciler = __webpack_require__(28);
	var ReactChildReconciler = __webpack_require__(90);

	/**
	 * Updating children of a component may trigger recursive updates. The depth is
	 * used to batch recursive updates to render markup more efficiently.
	 *
	 * @type {number}
	 * @private
	 */
	var updateDepth = 0;

	/**
	 * Queue of update configuration objects.
	 *
	 * Each object has a `type` property that is in `ReactMultiChildUpdateTypes`.
	 *
	 * @type {array<object>}
	 * @private
	 */
	var updateQueue = [];

	/**
	 * Queue of markup to be rendered.
	 *
	 * @type {array<string>}
	 * @private
	 */
	var markupQueue = [];

	/**
	 * Enqueues markup to be rendered and inserted at a supplied index.
	 *
	 * @param {string} parentID ID of the parent component.
	 * @param {string} markup Markup that renders into an element.
	 * @param {number} toIndex Destination index.
	 * @private
	 */
	function enqueueMarkup(parentID, markup, toIndex) {
	  // NOTE: Null values reduce hidden classes.
	  updateQueue.push({
	    parentID: parentID,
	    parentNode: null,
	    type: ReactMultiChildUpdateTypes.INSERT_MARKUP,
	    markupIndex: markupQueue.push(markup) - 1,
	    textContent: null,
	    fromIndex: null,
	    toIndex: toIndex
	  });
	}

	/**
	 * Enqueues moving an existing element to another index.
	 *
	 * @param {string} parentID ID of the parent component.
	 * @param {number} fromIndex Source index of the existing element.
	 * @param {number} toIndex Destination index of the element.
	 * @private
	 */
	function enqueueMove(parentID, fromIndex, toIndex) {
	  // NOTE: Null values reduce hidden classes.
	  updateQueue.push({
	    parentID: parentID,
	    parentNode: null,
	    type: ReactMultiChildUpdateTypes.MOVE_EXISTING,
	    markupIndex: null,
	    textContent: null,
	    fromIndex: fromIndex,
	    toIndex: toIndex
	  });
	}

	/**
	 * Enqueues removing an element at an index.
	 *
	 * @param {string} parentID ID of the parent component.
	 * @param {number} fromIndex Index of the element to remove.
	 * @private
	 */
	function enqueueRemove(parentID, fromIndex) {
	  // NOTE: Null values reduce hidden classes.
	  updateQueue.push({
	    parentID: parentID,
	    parentNode: null,
	    type: ReactMultiChildUpdateTypes.REMOVE_NODE,
	    markupIndex: null,
	    textContent: null,
	    fromIndex: fromIndex,
	    toIndex: null
	  });
	}

	/**
	 * Enqueues setting the text content.
	 *
	 * @param {string} parentID ID of the parent component.
	 * @param {string} textContent Text content to set.
	 * @private
	 */
	function enqueueTextContent(parentID, textContent) {
	  // NOTE: Null values reduce hidden classes.
	  updateQueue.push({
	    parentID: parentID,
	    parentNode: null,
	    type: ReactMultiChildUpdateTypes.TEXT_CONTENT,
	    markupIndex: null,
	    textContent: textContent,
	    fromIndex: null,
	    toIndex: null
	  });
	}

	/**
	 * Processes any enqueued updates.
	 *
	 * @private
	 */
	function processQueue() {
	  if (updateQueue.length) {
	    ReactComponentEnvironment.processChildrenUpdates(
	      updateQueue,
	      markupQueue
	    );
	    clearQueue();
	  }
	}

	/**
	 * Clears any enqueued updates.
	 *
	 * @private
	 */
	function clearQueue() {
	  updateQueue.length = 0;
	  markupQueue.length = 0;
	}

	/**
	 * ReactMultiChild are capable of reconciling multiple children.
	 *
	 * @class ReactMultiChild
	 * @internal
	 */
	var ReactMultiChild = {

	  /**
	   * Provides common functionality for components that must reconcile multiple
	   * children. This is used by `ReactDOMComponent` to mount, update, and
	   * unmount child components.
	   *
	   * @lends {ReactMultiChild.prototype}
	   */
	  Mixin: {

	    /**
	     * Generates a "mount image" for each of the supplied children. In the case
	     * of `ReactDOMComponent`, a mount image is a string of markup.
	     *
	     * @param {?object} nestedChildren Nested child maps.
	     * @return {array} An array of mounted representations.
	     * @internal
	     */
	    mountChildren: function(nestedChildren, transaction, context) {
	      var children = ReactChildReconciler.instantiateChildren(
	        nestedChildren, transaction, context
	      );
	      this._renderedChildren = children;
	      var mountImages = [];
	      var index = 0;
	      for (var name in children) {
	        if (children.hasOwnProperty(name)) {
	          var child = children[name];
	          // Inlined for performance, see `ReactInstanceHandles.createReactID`.
	          var rootID = this._rootNodeID + name;
	          var mountImage = ReactReconciler.mountComponent(
	            child,
	            rootID,
	            transaction,
	            context
	          );
	          child._mountIndex = index;
	          mountImages.push(mountImage);
	          index++;
	        }
	      }
	      return mountImages;
	    },

	    /**
	     * Replaces any rendered children with a text content string.
	     *
	     * @param {string} nextContent String of content.
	     * @internal
	     */
	    updateTextContent: function(nextContent) {
	      updateDepth++;
	      var errorThrown = true;
	      try {
	        var prevChildren = this._renderedChildren;
	        // Remove any rendered children.
	        ReactChildReconciler.unmountChildren(prevChildren);
	        // TODO: The setTextContent operation should be enough
	        for (var name in prevChildren) {
	          if (prevChildren.hasOwnProperty(name)) {
	            this._unmountChildByName(prevChildren[name], name);
	          }
	        }
	        // Set new text content.
	        this.setTextContent(nextContent);
	        errorThrown = false;
	      } finally {
	        updateDepth--;
	        if (!updateDepth) {
	          if (errorThrown) {
	            clearQueue();
	          } else {
	            processQueue();
	          }
	        }
	      }
	    },

	    /**
	     * Updates the rendered children with new children.
	     *
	     * @param {?object} nextNestedChildren Nested child maps.
	     * @param {ReactReconcileTransaction} transaction
	     * @internal
	     */
	    updateChildren: function(nextNestedChildren, transaction, context) {
	      updateDepth++;
	      var errorThrown = true;
	      try {
	        this._updateChildren(nextNestedChildren, transaction, context);
	        errorThrown = false;
	      } finally {
	        updateDepth--;
	        if (!updateDepth) {
	          if (errorThrown) {
	            clearQueue();
	          } else {
	            processQueue();
	          }
	        }

	      }
	    },

	    /**
	     * Improve performance by isolating this hot code path from the try/catch
	     * block in `updateChildren`.
	     *
	     * @param {?object} nextNestedChildren Nested child maps.
	     * @param {ReactReconcileTransaction} transaction
	     * @final
	     * @protected
	     */
	    _updateChildren: function(nextNestedChildren, transaction, context) {
	      var prevChildren = this._renderedChildren;
	      var nextChildren = ReactChildReconciler.updateChildren(
	        prevChildren, nextNestedChildren, transaction, context
	      );
	      this._renderedChildren = nextChildren;
	      if (!nextChildren && !prevChildren) {
	        return;
	      }
	      var name;
	      // `nextIndex` will increment for each child in `nextChildren`, but
	      // `lastIndex` will be the last index visited in `prevChildren`.
	      var lastIndex = 0;
	      var nextIndex = 0;
	      for (name in nextChildren) {
	        if (!nextChildren.hasOwnProperty(name)) {
	          continue;
	        }
	        var prevChild = prevChildren && prevChildren[name];
	        var nextChild = nextChildren[name];
	        if (prevChild === nextChild) {
	          this.moveChild(prevChild, nextIndex, lastIndex);
	          lastIndex = Math.max(prevChild._mountIndex, lastIndex);
	          prevChild._mountIndex = nextIndex;
	        } else {
	          if (prevChild) {
	            // Update `lastIndex` before `_mountIndex` gets unset by unmounting.
	            lastIndex = Math.max(prevChild._mountIndex, lastIndex);
	            this._unmountChildByName(prevChild, name);
	          }
	          // The child must be instantiated before it's mounted.
	          this._mountChildByNameAtIndex(
	            nextChild, name, nextIndex, transaction, context
	          );
	        }
	        nextIndex++;
	      }
	      // Remove children that are no longer present.
	      for (name in prevChildren) {
	        if (prevChildren.hasOwnProperty(name) &&
	            !(nextChildren && nextChildren.hasOwnProperty(name))) {
	          this._unmountChildByName(prevChildren[name], name);
	        }
	      }
	    },

	    /**
	     * Unmounts all rendered children. This should be used to clean up children
	     * when this component is unmounted.
	     *
	     * @internal
	     */
	    unmountChildren: function() {
	      var renderedChildren = this._renderedChildren;
	      ReactChildReconciler.unmountChildren(renderedChildren);
	      this._renderedChildren = null;
	    },

	    /**
	     * Moves a child component to the supplied index.
	     *
	     * @param {ReactComponent} child Component to move.
	     * @param {number} toIndex Destination index of the element.
	     * @param {number} lastIndex Last index visited of the siblings of `child`.
	     * @protected
	     */
	    moveChild: function(child, toIndex, lastIndex) {
	      // If the index of `child` is less than `lastIndex`, then it needs to
	      // be moved. Otherwise, we do not need to move it because a child will be
	      // inserted or moved before `child`.
	      if (child._mountIndex < lastIndex) {
	        enqueueMove(this._rootNodeID, child._mountIndex, toIndex);
	      }
	    },

	    /**
	     * Creates a child component.
	     *
	     * @param {ReactComponent} child Component to create.
	     * @param {string} mountImage Markup to insert.
	     * @protected
	     */
	    createChild: function(child, mountImage) {
	      enqueueMarkup(this._rootNodeID, mountImage, child._mountIndex);
	    },

	    /**
	     * Removes a child component.
	     *
	     * @param {ReactComponent} child Child to remove.
	     * @protected
	     */
	    removeChild: function(child) {
	      enqueueRemove(this._rootNodeID, child._mountIndex);
	    },

	    /**
	     * Sets this text content string.
	     *
	     * @param {string} textContent Text content to set.
	     * @protected
	     */
	    setTextContent: function(textContent) {
	      enqueueTextContent(this._rootNodeID, textContent);
	    },

	    /**
	     * Mounts a child with the supplied name.
	     *
	     * NOTE: This is part of `updateChildren` and is here for readability.
	     *
	     * @param {ReactComponent} child Component to mount.
	     * @param {string} name Name of the child.
	     * @param {number} index Index at which to insert the child.
	     * @param {ReactReconcileTransaction} transaction
	     * @private
	     */
	    _mountChildByNameAtIndex: function(
	      child,
	      name,
	      index,
	      transaction,
	      context) {
	      // Inlined for performance, see `ReactInstanceHandles.createReactID`.
	      var rootID = this._rootNodeID + name;
	      var mountImage = ReactReconciler.mountComponent(
	        child,
	        rootID,
	        transaction,
	        context
	      );
	      child._mountIndex = index;
	      this.createChild(child, mountImage);
	    },

	    /**
	     * Unmounts a rendered child by name.
	     *
	     * NOTE: This is part of `updateChildren` and is here for readability.
	     *
	     * @param {ReactComponent} child Component to unmount.
	     * @param {string} name Name of the child in `this._renderedChildren`.
	     * @private
	     */
	    _unmountChildByName: function(child, name) {
	      this.removeChild(child);
	      child._mountIndex = null;
	    }

	  }

	};

	module.exports = ReactMultiChild;


/***/ },
/* 90 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactChildReconciler
	 * @typechecks static-only
	 */

	'use strict';

	var ReactReconciler = __webpack_require__(28);

	var flattenChildren = __webpack_require__(91);
	var instantiateReactComponent = __webpack_require__(84);
	var shouldUpdateReactComponent = __webpack_require__(87);

	/**
	 * ReactChildReconciler provides helpers for initializing or updating a set of
	 * children. Its output is suitable for passing it onto ReactMultiChild which
	 * does diffed reordering and insertion.
	 */
	var ReactChildReconciler = {

	  /**
	   * Generates a "mount image" for each of the supplied children. In the case
	   * of `ReactDOMComponent`, a mount image is a string of markup.
	   *
	   * @param {?object} nestedChildNodes Nested child maps.
	   * @return {?object} A set of child instances.
	   * @internal
	   */
	  instantiateChildren: function(nestedChildNodes, transaction, context) {
	    var children = flattenChildren(nestedChildNodes);
	    for (var name in children) {
	      if (children.hasOwnProperty(name)) {
	        var child = children[name];
	        // The rendered children must be turned into instances as they're
	        // mounted.
	        var childInstance = instantiateReactComponent(child, null);
	        children[name] = childInstance;
	      }
	    }
	    return children;
	  },

	  /**
	   * Updates the rendered children and returns a new set of children.
	   *
	   * @param {?object} prevChildren Previously initialized set of children.
	   * @param {?object} nextNestedChildNodes Nested child maps.
	   * @param {ReactReconcileTransaction} transaction
	   * @param {object} context
	   * @return {?object} A new set of child instances.
	   * @internal
	   */
	  updateChildren: function(
	    prevChildren,
	    nextNestedChildNodes,
	    transaction,
	    context) {
	    // We currently don't have a way to track moves here but if we use iterators
	    // instead of for..in we can zip the iterators and check if an item has
	    // moved.
	    // TODO: If nothing has changed, return the prevChildren object so that we
	    // can quickly bailout if nothing has changed.
	    var nextChildren = flattenChildren(nextNestedChildNodes);
	    if (!nextChildren && !prevChildren) {
	      return null;
	    }
	    var name;
	    for (name in nextChildren) {
	      if (!nextChildren.hasOwnProperty(name)) {
	        continue;
	      }
	      var prevChild = prevChildren && prevChildren[name];
	      var prevElement = prevChild && prevChild._currentElement;
	      var nextElement = nextChildren[name];
	      if (shouldUpdateReactComponent(prevElement, nextElement)) {
	        ReactReconciler.receiveComponent(
	          prevChild, nextElement, transaction, context
	        );
	        nextChildren[name] = prevChild;
	      } else {
	        if (prevChild) {
	          ReactReconciler.unmountComponent(prevChild, name);
	        }
	        // The child must be instantiated before it's mounted.
	        var nextChildInstance = instantiateReactComponent(
	          nextElement,
	          null
	        );
	        nextChildren[name] = nextChildInstance;
	      }
	    }
	    // Unmount children that are no longer present.
	    for (name in prevChildren) {
	      if (prevChildren.hasOwnProperty(name) &&
	          !(nextChildren && nextChildren.hasOwnProperty(name))) {
	        ReactReconciler.unmountComponent(prevChildren[name]);
	      }
	    }
	    return nextChildren;
	  },

	  /**
	   * Unmounts all rendered children. This should be used to clean up children
	   * when this component is unmounted.
	   *
	   * @param {?object} renderedChildren Previously initialized set of children.
	   * @internal
	   */
	  unmountChildren: function(renderedChildren) {
	    for (var name in renderedChildren) {
	      var renderedChild = renderedChildren[name];
	      ReactReconciler.unmountComponent(renderedChild);
	    }
	  }

	};

	module.exports = ReactChildReconciler;


/***/ },
/* 91 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule flattenChildren
	 */

	'use strict';

	var traverseAllChildren = __webpack_require__(19);
	var warning = __webpack_require__(16);

	/**
	 * @param {function} traverseContext Context passed through traversal.
	 * @param {?ReactComponent} child React child component.
	 * @param {!string} name String name of key path to child.
	 */
	function flattenSingleChildIntoContext(traverseContext, child, name) {
	  // We found a component instance.
	  var result = traverseContext;
	  var keyUnique = !result.hasOwnProperty(name);
	  if ("production" !== process.env.NODE_ENV) {
	    ("production" !== process.env.NODE_ENV ? warning(
	      keyUnique,
	      'flattenChildren(...): Encountered two children with the same key, ' +
	      '`%s`. Child keys must be unique; when two children share a key, only ' +
	      'the first child will be used.',
	      name
	    ) : null);
	  }
	  if (keyUnique && child != null) {
	    result[name] = child;
	  }
	}

	/**
	 * Flattens children that are typically specified as `props.children`. Any null
	 * children will not be included in the resulting object.
	 * @return {!object} flattened children keyed by name.
	 */
	function flattenChildren(children) {
	  if (children == null) {
	    return children;
	  }
	  var result = {};
	  traverseAllChildren(children, flattenSingleChildIntoContext, result);
	  return result;
	}

	module.exports = flattenChildren;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 92 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDefaultInjection
	 */

	'use strict';

	var BeforeInputEventPlugin = __webpack_require__(93);
	var ChangeEventPlugin = __webpack_require__(101);
	var ClientReactRootIndex = __webpack_require__(103);
	var DefaultEventPluginOrder = __webpack_require__(104);
	var EnterLeaveEventPlugin = __webpack_require__(105);
	var ExecutionEnvironment = __webpack_require__(52);
	var HTMLDOMPropertyConfig = __webpack_require__(109);
	var MobileSafariClickEventPlugin = __webpack_require__(110);
	var ReactBrowserComponentMixin = __webpack_require__(111);
	var ReactClass = __webpack_require__(38);
	var ReactComponentBrowserEnvironment =
	  __webpack_require__(48);
	var ReactDefaultBatchingStrategy = __webpack_require__(113);
	var ReactDOMComponent = __webpack_require__(88);
	var ReactDOMButton = __webpack_require__(114);
	var ReactDOMForm = __webpack_require__(117);
	var ReactDOMImg = __webpack_require__(119);
	var ReactDOMIDOperations = __webpack_require__(49);
	var ReactDOMIframe = __webpack_require__(120);
	var ReactDOMInput = __webpack_require__(121);
	var ReactDOMOption = __webpack_require__(124);
	var ReactDOMSelect = __webpack_require__(125);
	var ReactDOMTextarea = __webpack_require__(126);
	var ReactDOMTextComponent = __webpack_require__(43);
	var ReactElement = __webpack_require__(12);
	var ReactEventListener = __webpack_require__(127);
	var ReactInjection = __webpack_require__(130);
	var ReactInstanceHandles = __webpack_require__(21);
	var ReactMount = __webpack_require__(68);
	var ReactReconcileTransaction = __webpack_require__(131);
	var SelectEventPlugin = __webpack_require__(137);
	var ServerReactRootIndex = __webpack_require__(139);
	var SimpleEventPlugin = __webpack_require__(140);
	var SVGDOMPropertyConfig = __webpack_require__(149);

	var createFullPageComponent = __webpack_require__(150);

	function autoGenerateWrapperClass(type) {
	  return ReactClass.createClass({
	    tagName: type.toUpperCase(),
	    render: function() {
	      return new ReactElement(
	        type,
	        null,
	        null,
	        null,
	        null,
	        this.props
	      );
	    }
	  });
	}

	function inject() {
	  ReactInjection.EventEmitter.injectReactEventListener(
	    ReactEventListener
	  );

	  /**
	   * Inject modules for resolving DOM hierarchy and plugin ordering.
	   */
	  ReactInjection.EventPluginHub.injectEventPluginOrder(DefaultEventPluginOrder);
	  ReactInjection.EventPluginHub.injectInstanceHandle(ReactInstanceHandles);
	  ReactInjection.EventPluginHub.injectMount(ReactMount);

	  /**
	   * Some important event plugins included by default (without having to require
	   * them).
	   */
	  ReactInjection.EventPluginHub.injectEventPluginsByName({
	    SimpleEventPlugin: SimpleEventPlugin,
	    EnterLeaveEventPlugin: EnterLeaveEventPlugin,
	    ChangeEventPlugin: ChangeEventPlugin,
	    MobileSafariClickEventPlugin: MobileSafariClickEventPlugin,
	    SelectEventPlugin: SelectEventPlugin,
	    BeforeInputEventPlugin: BeforeInputEventPlugin
	  });

	  ReactInjection.NativeComponent.injectGenericComponentClass(
	    ReactDOMComponent
	  );

	  ReactInjection.NativeComponent.injectTextComponentClass(
	    ReactDOMTextComponent
	  );

	  ReactInjection.NativeComponent.injectAutoWrapper(
	    autoGenerateWrapperClass
	  );

	  // This needs to happen before createFullPageComponent() otherwise the mixin
	  // won't be included.
	  ReactInjection.Class.injectMixin(ReactBrowserComponentMixin);

	  ReactInjection.NativeComponent.injectComponentClasses({
	    'button': ReactDOMButton,
	    'form': ReactDOMForm,
	    'iframe': ReactDOMIframe,
	    'img': ReactDOMImg,
	    'input': ReactDOMInput,
	    'option': ReactDOMOption,
	    'select': ReactDOMSelect,
	    'textarea': ReactDOMTextarea,

	    'html': createFullPageComponent('html'),
	    'head': createFullPageComponent('head'),
	    'body': createFullPageComponent('body')
	  });

	  ReactInjection.DOMProperty.injectDOMPropertyConfig(HTMLDOMPropertyConfig);
	  ReactInjection.DOMProperty.injectDOMPropertyConfig(SVGDOMPropertyConfig);

	  ReactInjection.EmptyComponent.injectEmptyComponent('noscript');

	  ReactInjection.Updates.injectReconcileTransaction(
	    ReactReconcileTransaction
	  );
	  ReactInjection.Updates.injectBatchingStrategy(
	    ReactDefaultBatchingStrategy
	  );

	  ReactInjection.RootIndex.injectCreateReactRootIndex(
	    ExecutionEnvironment.canUseDOM ?
	      ClientReactRootIndex.createReactRootIndex :
	      ServerReactRootIndex.createReactRootIndex
	  );

	  ReactInjection.Component.injectEnvironment(ReactComponentBrowserEnvironment);
	  ReactInjection.DOMComponent.injectIDOperations(ReactDOMIDOperations);

	  if ("production" !== process.env.NODE_ENV) {
	    var url = (ExecutionEnvironment.canUseDOM && window.location.href) || '';
	    if ((/[?&]react_perf\b/).test(url)) {
	      var ReactDefaultPerf = __webpack_require__(151);
	      ReactDefaultPerf.start();
	    }
	  }
	}

	module.exports = {
	  inject: inject
	};

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 93 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015 Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule BeforeInputEventPlugin
	 * @typechecks static-only
	 */

	'use strict';

	var EventConstants = __webpack_require__(6);
	var EventPropagators = __webpack_require__(94);
	var ExecutionEnvironment = __webpack_require__(52);
	var FallbackCompositionState = __webpack_require__(95);
	var SyntheticCompositionEvent = __webpack_require__(97);
	var SyntheticInputEvent = __webpack_require__(100);

	var keyOf = __webpack_require__(40);

	var END_KEYCODES = [9, 13, 27, 32]; // Tab, Return, Esc, Space
	var START_KEYCODE = 229;

	var canUseCompositionEvent = (
	  ExecutionEnvironment.canUseDOM &&
	  'CompositionEvent' in window
	);

	var documentMode = null;
	if (ExecutionEnvironment.canUseDOM && 'documentMode' in document) {
	  documentMode = document.documentMode;
	}

	// Webkit offers a very useful `textInput` event that can be used to
	// directly represent `beforeInput`. The IE `textinput` event is not as
	// useful, so we don't use it.
	var canUseTextInputEvent = (
	  ExecutionEnvironment.canUseDOM &&
	  'TextEvent' in window &&
	  !documentMode &&
	  !isPresto()
	);

	// In IE9+, we have access to composition events, but the data supplied
	// by the native compositionend event may be incorrect. Japanese ideographic
	// spaces, for instance (\u3000) are not recorded correctly.
	var useFallbackCompositionData = (
	  ExecutionEnvironment.canUseDOM &&
	  (
	    (!canUseCompositionEvent || documentMode && documentMode > 8 && documentMode <= 11)
	  )
	);

	/**
	 * Opera <= 12 includes TextEvent in window, but does not fire
	 * text input events. Rely on keypress instead.
	 */
	function isPresto() {
	  var opera = window.opera;
	  return (
	    typeof opera === 'object' &&
	    typeof opera.version === 'function' &&
	    parseInt(opera.version(), 10) <= 12
	  );
	}

	var SPACEBAR_CODE = 32;
	var SPACEBAR_CHAR = String.fromCharCode(SPACEBAR_CODE);

	var topLevelTypes = EventConstants.topLevelTypes;

	// Events and their corresponding property names.
	var eventTypes = {
	  beforeInput: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onBeforeInput: null}),
	      captured: keyOf({onBeforeInputCapture: null})
	    },
	    dependencies: [
	      topLevelTypes.topCompositionEnd,
	      topLevelTypes.topKeyPress,
	      topLevelTypes.topTextInput,
	      topLevelTypes.topPaste
	    ]
	  },
	  compositionEnd: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onCompositionEnd: null}),
	      captured: keyOf({onCompositionEndCapture: null})
	    },
	    dependencies: [
	      topLevelTypes.topBlur,
	      topLevelTypes.topCompositionEnd,
	      topLevelTypes.topKeyDown,
	      topLevelTypes.topKeyPress,
	      topLevelTypes.topKeyUp,
	      topLevelTypes.topMouseDown
	    ]
	  },
	  compositionStart: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onCompositionStart: null}),
	      captured: keyOf({onCompositionStartCapture: null})
	    },
	    dependencies: [
	      topLevelTypes.topBlur,
	      topLevelTypes.topCompositionStart,
	      topLevelTypes.topKeyDown,
	      topLevelTypes.topKeyPress,
	      topLevelTypes.topKeyUp,
	      topLevelTypes.topMouseDown
	    ]
	  },
	  compositionUpdate: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onCompositionUpdate: null}),
	      captured: keyOf({onCompositionUpdateCapture: null})
	    },
	    dependencies: [
	      topLevelTypes.topBlur,
	      topLevelTypes.topCompositionUpdate,
	      topLevelTypes.topKeyDown,
	      topLevelTypes.topKeyPress,
	      topLevelTypes.topKeyUp,
	      topLevelTypes.topMouseDown
	    ]
	  }
	};

	// Track whether we've ever handled a keypress on the space key.
	var hasSpaceKeypress = false;

	/**
	 * Return whether a native keypress event is assumed to be a command.
	 * This is required because Firefox fires `keypress` events for key commands
	 * (cut, copy, select-all, etc.) even though no character is inserted.
	 */
	function isKeypressCommand(nativeEvent) {
	  return (
	    (nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) &&
	    // ctrlKey && altKey is equivalent to AltGr, and is not a command.
	    !(nativeEvent.ctrlKey && nativeEvent.altKey)
	  );
	}


	/**
	 * Translate native top level events into event types.
	 *
	 * @param {string} topLevelType
	 * @return {object}
	 */
	function getCompositionEventType(topLevelType) {
	  switch (topLevelType) {
	    case topLevelTypes.topCompositionStart:
	      return eventTypes.compositionStart;
	    case topLevelTypes.topCompositionEnd:
	      return eventTypes.compositionEnd;
	    case topLevelTypes.topCompositionUpdate:
	      return eventTypes.compositionUpdate;
	  }
	}

	/**
	 * Does our fallback best-guess model think this event signifies that
	 * composition has begun?
	 *
	 * @param {string} topLevelType
	 * @param {object} nativeEvent
	 * @return {boolean}
	 */
	function isFallbackCompositionStart(topLevelType, nativeEvent) {
	  return (
	    topLevelType === topLevelTypes.topKeyDown &&
	    nativeEvent.keyCode === START_KEYCODE
	  );
	}

	/**
	 * Does our fallback mode think that this event is the end of composition?
	 *
	 * @param {string} topLevelType
	 * @param {object} nativeEvent
	 * @return {boolean}
	 */
	function isFallbackCompositionEnd(topLevelType, nativeEvent) {
	  switch (topLevelType) {
	    case topLevelTypes.topKeyUp:
	      // Command keys insert or clear IME input.
	      return (END_KEYCODES.indexOf(nativeEvent.keyCode) !== -1);
	    case topLevelTypes.topKeyDown:
	      // Expect IME keyCode on each keydown. If we get any other
	      // code we must have exited earlier.
	      return (nativeEvent.keyCode !== START_KEYCODE);
	    case topLevelTypes.topKeyPress:
	    case topLevelTypes.topMouseDown:
	    case topLevelTypes.topBlur:
	      // Events are not possible without cancelling IME.
	      return true;
	    default:
	      return false;
	  }
	}

	/**
	 * Google Input Tools provides composition data via a CustomEvent,
	 * with the `data` property populated in the `detail` object. If this
	 * is available on the event object, use it. If not, this is a plain
	 * composition event and we have nothing special to extract.
	 *
	 * @param {object} nativeEvent
	 * @return {?string}
	 */
	function getDataFromCustomEvent(nativeEvent) {
	  var detail = nativeEvent.detail;
	  if (typeof detail === 'object' && 'data' in detail) {
	    return detail.data;
	  }
	  return null;
	}

	// Track the current IME composition fallback object, if any.
	var currentComposition = null;

	/**
	 * @param {string} topLevelType Record from `EventConstants`.
	 * @param {DOMEventTarget} topLevelTarget The listening component root node.
	 * @param {string} topLevelTargetID ID of `topLevelTarget`.
	 * @param {object} nativeEvent Native browser event.
	 * @return {?object} A SyntheticCompositionEvent.
	 */
	function extractCompositionEvent(
	  topLevelType,
	  topLevelTarget,
	  topLevelTargetID,
	  nativeEvent
	) {
	  var eventType;
	  var fallbackData;

	  if (canUseCompositionEvent) {
	    eventType = getCompositionEventType(topLevelType);
	  } else if (!currentComposition) {
	    if (isFallbackCompositionStart(topLevelType, nativeEvent)) {
	      eventType = eventTypes.compositionStart;
	    }
	  } else if (isFallbackCompositionEnd(topLevelType, nativeEvent)) {
	    eventType = eventTypes.compositionEnd;
	  }

	  if (!eventType) {
	    return null;
	  }

	  if (useFallbackCompositionData) {
	    // The current composition is stored statically and must not be
	    // overwritten while composition continues.
	    if (!currentComposition && eventType === eventTypes.compositionStart) {
	      currentComposition = FallbackCompositionState.getPooled(topLevelTarget);
	    } else if (eventType === eventTypes.compositionEnd) {
	      if (currentComposition) {
	        fallbackData = currentComposition.getData();
	      }
	    }
	  }

	  var event = SyntheticCompositionEvent.getPooled(
	    eventType,
	    topLevelTargetID,
	    nativeEvent
	  );

	  if (fallbackData) {
	    // Inject data generated from fallback path into the synthetic event.
	    // This matches the property of native CompositionEventInterface.
	    event.data = fallbackData;
	  } else {
	    var customData = getDataFromCustomEvent(nativeEvent);
	    if (customData !== null) {
	      event.data = customData;
	    }
	  }

	  EventPropagators.accumulateTwoPhaseDispatches(event);
	  return event;
	}

	/**
	 * @param {string} topLevelType Record from `EventConstants`.
	 * @param {object} nativeEvent Native browser event.
	 * @return {?string} The string corresponding to this `beforeInput` event.
	 */
	function getNativeBeforeInputChars(topLevelType, nativeEvent) {
	  switch (topLevelType) {
	    case topLevelTypes.topCompositionEnd:
	      return getDataFromCustomEvent(nativeEvent);
	    case topLevelTypes.topKeyPress:
	      /**
	       * If native `textInput` events are available, our goal is to make
	       * use of them. However, there is a special case: the spacebar key.
	       * In Webkit, preventing default on a spacebar `textInput` event
	       * cancels character insertion, but it *also* causes the browser
	       * to fall back to its default spacebar behavior of scrolling the
	       * page.
	       *
	       * Tracking at:
	       * https://code.google.com/p/chromium/issues/detail?id=355103
	       *
	       * To avoid this issue, use the keypress event as if no `textInput`
	       * event is available.
	       */
	      var which = nativeEvent.which;
	      if (which !== SPACEBAR_CODE) {
	        return null;
	      }

	      hasSpaceKeypress = true;
	      return SPACEBAR_CHAR;

	    case topLevelTypes.topTextInput:
	      // Record the characters to be added to the DOM.
	      var chars = nativeEvent.data;

	      // If it's a spacebar character, assume that we have already handled
	      // it at the keypress level and bail immediately. Android Chrome
	      // doesn't give us keycodes, so we need to blacklist it.
	      if (chars === SPACEBAR_CHAR && hasSpaceKeypress) {
	        return null;
	      }

	      return chars;

	    default:
	      // For other native event types, do nothing.
	      return null;
	  }
	}

	/**
	 * For browsers that do not provide the `textInput` event, extract the
	 * appropriate string to use for SyntheticInputEvent.
	 *
	 * @param {string} topLevelType Record from `EventConstants`.
	 * @param {object} nativeEvent Native browser event.
	 * @return {?string} The fallback string for this `beforeInput` event.
	 */
	function getFallbackBeforeInputChars(topLevelType, nativeEvent) {
	  // If we are currently composing (IME) and using a fallback to do so,
	  // try to extract the composed characters from the fallback object.
	  if (currentComposition) {
	    if (
	      topLevelType === topLevelTypes.topCompositionEnd ||
	      isFallbackCompositionEnd(topLevelType, nativeEvent)
	    ) {
	      var chars = currentComposition.getData();
	      FallbackCompositionState.release(currentComposition);
	      currentComposition = null;
	      return chars;
	    }
	    return null;
	  }

	  switch (topLevelType) {
	    case topLevelTypes.topPaste:
	      // If a paste event occurs after a keypress, throw out the input
	      // chars. Paste events should not lead to BeforeInput events.
	      return null;
	    case topLevelTypes.topKeyPress:
	      /**
	       * As of v27, Firefox may fire keypress events even when no character
	       * will be inserted. A few possibilities:
	       *
	       * - `which` is `0`. Arrow keys, Esc key, etc.
	       *
	       * - `which` is the pressed key code, but no char is available.
	       *   Ex: 'AltGr + d` in Polish. There is no modified character for
	       *   this key combination and no character is inserted into the
	       *   document, but FF fires the keypress for char code `100` anyway.
	       *   No `input` event will occur.
	       *
	       * - `which` is the pressed key code, but a command combination is
	       *   being used. Ex: `Cmd+C`. No character is inserted, and no
	       *   `input` event will occur.
	       */
	      if (nativeEvent.which && !isKeypressCommand(nativeEvent)) {
	        return String.fromCharCode(nativeEvent.which);
	      }
	      return null;
	    case topLevelTypes.topCompositionEnd:
	      return useFallbackCompositionData ? null : nativeEvent.data;
	    default:
	      return null;
	  }
	}

	/**
	 * Extract a SyntheticInputEvent for `beforeInput`, based on either native
	 * `textInput` or fallback behavior.
	 *
	 * @param {string} topLevelType Record from `EventConstants`.
	 * @param {DOMEventTarget} topLevelTarget The listening component root node.
	 * @param {string} topLevelTargetID ID of `topLevelTarget`.
	 * @param {object} nativeEvent Native browser event.
	 * @return {?object} A SyntheticInputEvent.
	 */
	function extractBeforeInputEvent(
	  topLevelType,
	  topLevelTarget,
	  topLevelTargetID,
	  nativeEvent
	) {
	  var chars;

	  if (canUseTextInputEvent) {
	    chars = getNativeBeforeInputChars(topLevelType, nativeEvent);
	  } else {
	    chars = getFallbackBeforeInputChars(topLevelType, nativeEvent);
	  }

	  // If no characters are being inserted, no BeforeInput event should
	  // be fired.
	  if (!chars) {
	    return null;
	  }

	  var event = SyntheticInputEvent.getPooled(
	    eventTypes.beforeInput,
	    topLevelTargetID,
	    nativeEvent
	  );

	  event.data = chars;
	  EventPropagators.accumulateTwoPhaseDispatches(event);
	  return event;
	}

	/**
	 * Create an `onBeforeInput` event to match
	 * http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105/#events-inputevents.
	 *
	 * This event plugin is based on the native `textInput` event
	 * available in Chrome, Safari, Opera, and IE. This event fires after
	 * `onKeyPress` and `onCompositionEnd`, but before `onInput`.
	 *
	 * `beforeInput` is spec'd but not implemented in any browsers, and
	 * the `input` event does not provide any useful information about what has
	 * actually been added, contrary to the spec. Thus, `textInput` is the best
	 * available event to identify the characters that have actually been inserted
	 * into the target node.
	 *
	 * This plugin is also responsible for emitting `composition` events, thus
	 * allowing us to share composition fallback code for both `beforeInput` and
	 * `composition` event types.
	 */
	var BeforeInputEventPlugin = {

	  eventTypes: eventTypes,

	  /**
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @see {EventPluginHub.extractEvents}
	   */
	  extractEvents: function(
	    topLevelType,
	    topLevelTarget,
	    topLevelTargetID,
	    nativeEvent
	  ) {
	    return [
	      extractCompositionEvent(
	        topLevelType,
	        topLevelTarget,
	        topLevelTargetID,
	        nativeEvent
	      ),
	      extractBeforeInputEvent(
	        topLevelType,
	        topLevelTarget,
	        topLevelTargetID,
	        nativeEvent
	      )
	    ];
	  }
	};

	module.exports = BeforeInputEventPlugin;


/***/ },
/* 94 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule EventPropagators
	 */

	'use strict';

	var EventConstants = __webpack_require__(6);
	var EventPluginHub = __webpack_require__(70);

	var accumulateInto = __webpack_require__(72);
	var forEachAccumulated = __webpack_require__(73);

	var PropagationPhases = EventConstants.PropagationPhases;
	var getListener = EventPluginHub.getListener;

	/**
	 * Some event types have a notion of different registration names for different
	 * "phases" of propagation. This finds listeners by a given phase.
	 */
	function listenerAtPhase(id, event, propagationPhase) {
	  var registrationName =
	    event.dispatchConfig.phasedRegistrationNames[propagationPhase];
	  return getListener(id, registrationName);
	}

	/**
	 * Tags a `SyntheticEvent` with dispatched listeners. Creating this function
	 * here, allows us to not have to bind or create functions for each event.
	 * Mutating the event's members allows us to not have to create a wrapping
	 * "dispatch" object that pairs the event with the listener.
	 */
	function accumulateDirectionalDispatches(domID, upwards, event) {
	  if ("production" !== process.env.NODE_ENV) {
	    if (!domID) {
	      throw new Error('Dispatching id must not be null');
	    }
	  }
	  var phase = upwards ? PropagationPhases.bubbled : PropagationPhases.captured;
	  var listener = listenerAtPhase(domID, event, phase);
	  if (listener) {
	    event._dispatchListeners =
	      accumulateInto(event._dispatchListeners, listener);
	    event._dispatchIDs = accumulateInto(event._dispatchIDs, domID);
	  }
	}

	/**
	 * Collect dispatches (must be entirely collected before dispatching - see unit
	 * tests). Lazily allocate the array to conserve memory.  We must loop through
	 * each event and perform the traversal for each one. We can not perform a
	 * single traversal for the entire collection of events because each event may
	 * have a different target.
	 */
	function accumulateTwoPhaseDispatchesSingle(event) {
	  if (event && event.dispatchConfig.phasedRegistrationNames) {
	    EventPluginHub.injection.getInstanceHandle().traverseTwoPhase(
	      event.dispatchMarker,
	      accumulateDirectionalDispatches,
	      event
	    );
	  }
	}


	/**
	 * Accumulates without regard to direction, does not look for phased
	 * registration names. Same as `accumulateDirectDispatchesSingle` but without
	 * requiring that the `dispatchMarker` be the same as the dispatched ID.
	 */
	function accumulateDispatches(id, ignoredDirection, event) {
	  if (event && event.dispatchConfig.registrationName) {
	    var registrationName = event.dispatchConfig.registrationName;
	    var listener = getListener(id, registrationName);
	    if (listener) {
	      event._dispatchListeners =
	        accumulateInto(event._dispatchListeners, listener);
	      event._dispatchIDs = accumulateInto(event._dispatchIDs, id);
	    }
	  }
	}

	/**
	 * Accumulates dispatches on an `SyntheticEvent`, but only for the
	 * `dispatchMarker`.
	 * @param {SyntheticEvent} event
	 */
	function accumulateDirectDispatchesSingle(event) {
	  if (event && event.dispatchConfig.registrationName) {
	    accumulateDispatches(event.dispatchMarker, null, event);
	  }
	}

	function accumulateTwoPhaseDispatches(events) {
	  forEachAccumulated(events, accumulateTwoPhaseDispatchesSingle);
	}

	function accumulateEnterLeaveDispatches(leave, enter, fromID, toID) {
	  EventPluginHub.injection.getInstanceHandle().traverseEnterLeave(
	    fromID,
	    toID,
	    accumulateDispatches,
	    leave,
	    enter
	  );
	}


	function accumulateDirectDispatches(events) {
	  forEachAccumulated(events, accumulateDirectDispatchesSingle);
	}



	/**
	 * A small set of propagation patterns, each of which will accept a small amount
	 * of information, and generate a set of "dispatch ready event objects" - which
	 * are sets of events that have already been annotated with a set of dispatched
	 * listener functions/ids. The API is designed this way to discourage these
	 * propagation strategies from actually executing the dispatches, since we
	 * always want to collect the entire set of dispatches before executing event a
	 * single one.
	 *
	 * @constructor EventPropagators
	 */
	var EventPropagators = {
	  accumulateTwoPhaseDispatches: accumulateTwoPhaseDispatches,
	  accumulateDirectDispatches: accumulateDirectDispatches,
	  accumulateEnterLeaveDispatches: accumulateEnterLeaveDispatches
	};

	module.exports = EventPropagators;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 95 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule FallbackCompositionState
	 * @typechecks static-only
	 */

	'use strict';

	var PooledClass = __webpack_require__(10);

	var assign = __webpack_require__(14);
	var getTextContentAccessor = __webpack_require__(96);

	/**
	 * This helper class stores information about text content of a target node,
	 * allowing comparison of content before and after a given event.
	 *
	 * Identify the node where selection currently begins, then observe
	 * both its text content and its current position in the DOM. Since the
	 * browser may natively replace the target node during composition, we can
	 * use its position to find its replacement.
	 *
	 * @param {DOMEventTarget} root
	 */
	function FallbackCompositionState(root) {
	  this._root = root;
	  this._startText = this.getText();
	  this._fallbackText = null;
	}

	assign(FallbackCompositionState.prototype, {
	  /**
	   * Get current text of input.
	   *
	   * @return {string}
	   */
	  getText: function() {
	    if ('value' in this._root) {
	      return this._root.value;
	    }
	    return this._root[getTextContentAccessor()];
	  },

	  /**
	   * Determine the differing substring between the initially stored
	   * text content and the current content.
	   *
	   * @return {string}
	   */
	  getData: function() {
	    if (this._fallbackText) {
	      return this._fallbackText;
	    }

	    var start;
	    var startValue = this._startText;
	    var startLength = startValue.length;
	    var end;
	    var endValue = this.getText();
	    var endLength = endValue.length;

	    for (start = 0; start < startLength; start++) {
	      if (startValue[start] !== endValue[start]) {
	        break;
	      }
	    }

	    var minEnd = startLength - start;
	    for (end = 1; end <= minEnd; end++) {
	      if (startValue[startLength - end] !== endValue[endLength - end]) {
	        break;
	      }
	    }

	    var sliceTail = end > 1 ? 1 - end : undefined;
	    this._fallbackText = endValue.slice(start, sliceTail);
	    return this._fallbackText;
	  }
	});

	PooledClass.addPoolingTo(FallbackCompositionState);

	module.exports = FallbackCompositionState;


/***/ },
/* 96 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getTextContentAccessor
	 */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(52);

	var contentKey = null;

	/**
	 * Gets the key used to access text content on a DOM node.
	 *
	 * @return {?string} Key used to access text content.
	 * @internal
	 */
	function getTextContentAccessor() {
	  if (!contentKey && ExecutionEnvironment.canUseDOM) {
	    // Prefer textContent to innerText because many browsers support both but
	    // SVG <text> elements don't support innerText even when <div> does.
	    contentKey = 'textContent' in document.documentElement ?
	      'textContent' :
	      'innerText';
	  }
	  return contentKey;
	}

	module.exports = getTextContentAccessor;


/***/ },
/* 97 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticCompositionEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticEvent = __webpack_require__(98);

	/**
	 * @interface Event
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/#events-compositionevents
	 */
	var CompositionEventInterface = {
	  data: null
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticCompositionEvent(
	  dispatchConfig,
	  dispatchMarker,
	  nativeEvent) {
	  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
	}

	SyntheticEvent.augmentClass(
	  SyntheticCompositionEvent,
	  CompositionEventInterface
	);

	module.exports = SyntheticCompositionEvent;


/***/ },
/* 98 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticEvent
	 * @typechecks static-only
	 */

	'use strict';

	var PooledClass = __webpack_require__(10);

	var assign = __webpack_require__(14);
	var emptyFunction = __webpack_require__(17);
	var getEventTarget = __webpack_require__(99);

	/**
	 * @interface Event
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var EventInterface = {
	  type: null,
	  target: getEventTarget,
	  // currentTarget is set when dispatching; no use in copying it here
	  currentTarget: emptyFunction.thatReturnsNull,
	  eventPhase: null,
	  bubbles: null,
	  cancelable: null,
	  timeStamp: function(event) {
	    return event.timeStamp || Date.now();
	  },
	  defaultPrevented: null,
	  isTrusted: null
	};

	/**
	 * Synthetic events are dispatched by event plugins, typically in response to a
	 * top-level event delegation handler.
	 *
	 * These systems should generally use pooling to reduce the frequency of garbage
	 * collection. The system should check `isPersistent` to determine whether the
	 * event should be released into the pool after being dispatched. Users that
	 * need a persisted event should invoke `persist`.
	 *
	 * Synthetic events (and subclasses) implement the DOM Level 3 Events API by
	 * normalizing browser quirks. Subclasses do not necessarily have to implement a
	 * DOM interface; custom application-specific events can also subclass this.
	 *
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 */
	function SyntheticEvent(dispatchConfig, dispatchMarker, nativeEvent) {
	  this.dispatchConfig = dispatchConfig;
	  this.dispatchMarker = dispatchMarker;
	  this.nativeEvent = nativeEvent;

	  var Interface = this.constructor.Interface;
	  for (var propName in Interface) {
	    if (!Interface.hasOwnProperty(propName)) {
	      continue;
	    }
	    var normalize = Interface[propName];
	    if (normalize) {
	      this[propName] = normalize(nativeEvent);
	    } else {
	      this[propName] = nativeEvent[propName];
	    }
	  }

	  var defaultPrevented = nativeEvent.defaultPrevented != null ?
	    nativeEvent.defaultPrevented :
	    nativeEvent.returnValue === false;
	  if (defaultPrevented) {
	    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
	  } else {
	    this.isDefaultPrevented = emptyFunction.thatReturnsFalse;
	  }
	  this.isPropagationStopped = emptyFunction.thatReturnsFalse;
	}

	assign(SyntheticEvent.prototype, {

	  preventDefault: function() {
	    this.defaultPrevented = true;
	    var event = this.nativeEvent;
	    if (event.preventDefault) {
	      event.preventDefault();
	    } else {
	      event.returnValue = false;
	    }
	    this.isDefaultPrevented = emptyFunction.thatReturnsTrue;
	  },

	  stopPropagation: function() {
	    var event = this.nativeEvent;
	    if (event.stopPropagation) {
	      event.stopPropagation();
	    } else {
	      event.cancelBubble = true;
	    }
	    this.isPropagationStopped = emptyFunction.thatReturnsTrue;
	  },

	  /**
	   * We release all dispatched `SyntheticEvent`s after each event loop, adding
	   * them back into the pool. This allows a way to hold onto a reference that
	   * won't be added back into the pool.
	   */
	  persist: function() {
	    this.isPersistent = emptyFunction.thatReturnsTrue;
	  },

	  /**
	   * Checks if this event should be released back into the pool.
	   *
	   * @return {boolean} True if this should not be released, false otherwise.
	   */
	  isPersistent: emptyFunction.thatReturnsFalse,

	  /**
	   * `PooledClass` looks for `destructor` on each instance it releases.
	   */
	  destructor: function() {
	    var Interface = this.constructor.Interface;
	    for (var propName in Interface) {
	      this[propName] = null;
	    }
	    this.dispatchConfig = null;
	    this.dispatchMarker = null;
	    this.nativeEvent = null;
	  }

	});

	SyntheticEvent.Interface = EventInterface;

	/**
	 * Helper to reduce boilerplate when creating subclasses.
	 *
	 * @param {function} Class
	 * @param {?object} Interface
	 */
	SyntheticEvent.augmentClass = function(Class, Interface) {
	  var Super = this;

	  var prototype = Object.create(Super.prototype);
	  assign(prototype, Class.prototype);
	  Class.prototype = prototype;
	  Class.prototype.constructor = Class;

	  Class.Interface = assign({}, Super.Interface, Interface);
	  Class.augmentClass = Super.augmentClass;

	  PooledClass.addPoolingTo(Class, PooledClass.threeArgumentPooler);
	};

	PooledClass.addPoolingTo(SyntheticEvent, PooledClass.threeArgumentPooler);

	module.exports = SyntheticEvent;


/***/ },
/* 99 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getEventTarget
	 * @typechecks static-only
	 */

	'use strict';

	/**
	 * Gets the target node from a native browser event by accounting for
	 * inconsistencies in browser DOM APIs.
	 *
	 * @param {object} nativeEvent Native browser event.
	 * @return {DOMEventTarget} Target node.
	 */
	function getEventTarget(nativeEvent) {
	  var target = nativeEvent.target || nativeEvent.srcElement || window;
	  // Safari may fire events on text nodes (Node.TEXT_NODE is 3).
	  // @see http://www.quirksmode.org/js/events_properties.html
	  return target.nodeType === 3 ? target.parentNode : target;
	}

	module.exports = getEventTarget;


/***/ },
/* 100 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticInputEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticEvent = __webpack_require__(98);

	/**
	 * @interface Event
	 * @see http://www.w3.org/TR/2013/WD-DOM-Level-3-Events-20131105
	 *      /#events-inputevents
	 */
	var InputEventInterface = {
	  data: null
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticInputEvent(
	  dispatchConfig,
	  dispatchMarker,
	  nativeEvent) {
	  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
	}

	SyntheticEvent.augmentClass(
	  SyntheticInputEvent,
	  InputEventInterface
	);

	module.exports = SyntheticInputEvent;


/***/ },
/* 101 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ChangeEventPlugin
	 */

	'use strict';

	var EventConstants = __webpack_require__(6);
	var EventPluginHub = __webpack_require__(70);
	var EventPropagators = __webpack_require__(94);
	var ExecutionEnvironment = __webpack_require__(52);
	var ReactUpdates = __webpack_require__(25);
	var SyntheticEvent = __webpack_require__(98);

	var isEventSupported = __webpack_require__(76);
	var isTextInputElement = __webpack_require__(102);
	var keyOf = __webpack_require__(40);

	var topLevelTypes = EventConstants.topLevelTypes;

	var eventTypes = {
	  change: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onChange: null}),
	      captured: keyOf({onChangeCapture: null})
	    },
	    dependencies: [
	      topLevelTypes.topBlur,
	      topLevelTypes.topChange,
	      topLevelTypes.topClick,
	      topLevelTypes.topFocus,
	      topLevelTypes.topInput,
	      topLevelTypes.topKeyDown,
	      topLevelTypes.topKeyUp,
	      topLevelTypes.topSelectionChange
	    ]
	  }
	};

	/**
	 * For IE shims
	 */
	var activeElement = null;
	var activeElementID = null;
	var activeElementValue = null;
	var activeElementValueProp = null;

	/**
	 * SECTION: handle `change` event
	 */
	function shouldUseChangeEvent(elem) {
	  return (
	    elem.nodeName === 'SELECT' ||
	    (elem.nodeName === 'INPUT' && elem.type === 'file')
	  );
	}

	var doesChangeEventBubble = false;
	if (ExecutionEnvironment.canUseDOM) {
	  // See `handleChange` comment below
	  doesChangeEventBubble = isEventSupported('change') && (
	    (!('documentMode' in document) || document.documentMode > 8)
	  );
	}

	function manualDispatchChangeEvent(nativeEvent) {
	  var event = SyntheticEvent.getPooled(
	    eventTypes.change,
	    activeElementID,
	    nativeEvent
	  );
	  EventPropagators.accumulateTwoPhaseDispatches(event);

	  // If change and propertychange bubbled, we'd just bind to it like all the
	  // other events and have it go through ReactBrowserEventEmitter. Since it
	  // doesn't, we manually listen for the events and so we have to enqueue and
	  // process the abstract event manually.
	  //
	  // Batching is necessary here in order to ensure that all event handlers run
	  // before the next rerender (including event handlers attached to ancestor
	  // elements instead of directly on the input). Without this, controlled
	  // components don't work properly in conjunction with event bubbling because
	  // the component is rerendered and the value reverted before all the event
	  // handlers can run. See https://github.com/facebook/react/issues/708.
	  ReactUpdates.batchedUpdates(runEventInBatch, event);
	}

	function runEventInBatch(event) {
	  EventPluginHub.enqueueEvents(event);
	  EventPluginHub.processEventQueue();
	}

	function startWatchingForChangeEventIE8(target, targetID) {
	  activeElement = target;
	  activeElementID = targetID;
	  activeElement.attachEvent('onchange', manualDispatchChangeEvent);
	}

	function stopWatchingForChangeEventIE8() {
	  if (!activeElement) {
	    return;
	  }
	  activeElement.detachEvent('onchange', manualDispatchChangeEvent);
	  activeElement = null;
	  activeElementID = null;
	}

	function getTargetIDForChangeEvent(
	    topLevelType,
	    topLevelTarget,
	    topLevelTargetID) {
	  if (topLevelType === topLevelTypes.topChange) {
	    return topLevelTargetID;
	  }
	}
	function handleEventsForChangeEventIE8(
	    topLevelType,
	    topLevelTarget,
	    topLevelTargetID) {
	  if (topLevelType === topLevelTypes.topFocus) {
	    // stopWatching() should be a noop here but we call it just in case we
	    // missed a blur event somehow.
	    stopWatchingForChangeEventIE8();
	    startWatchingForChangeEventIE8(topLevelTarget, topLevelTargetID);
	  } else if (topLevelType === topLevelTypes.topBlur) {
	    stopWatchingForChangeEventIE8();
	  }
	}


	/**
	 * SECTION: handle `input` event
	 */
	var isInputEventSupported = false;
	if (ExecutionEnvironment.canUseDOM) {
	  // IE9 claims to support the input event but fails to trigger it when
	  // deleting text, so we ignore its input events
	  isInputEventSupported = isEventSupported('input') && (
	    (!('documentMode' in document) || document.documentMode > 9)
	  );
	}

	/**
	 * (For old IE.) Replacement getter/setter for the `value` property that gets
	 * set on the active element.
	 */
	var newValueProp =  {
	  get: function() {
	    return activeElementValueProp.get.call(this);
	  },
	  set: function(val) {
	    // Cast to a string so we can do equality checks.
	    activeElementValue = '' + val;
	    activeElementValueProp.set.call(this, val);
	  }
	};

	/**
	 * (For old IE.) Starts tracking propertychange events on the passed-in element
	 * and override the value property so that we can distinguish user events from
	 * value changes in JS.
	 */
	function startWatchingForValueChange(target, targetID) {
	  activeElement = target;
	  activeElementID = targetID;
	  activeElementValue = target.value;
	  activeElementValueProp = Object.getOwnPropertyDescriptor(
	    target.constructor.prototype,
	    'value'
	  );

	  Object.defineProperty(activeElement, 'value', newValueProp);
	  activeElement.attachEvent('onpropertychange', handlePropertyChange);
	}

	/**
	 * (For old IE.) Removes the event listeners from the currently-tracked element,
	 * if any exists.
	 */
	function stopWatchingForValueChange() {
	  if (!activeElement) {
	    return;
	  }

	  // delete restores the original property definition
	  delete activeElement.value;
	  activeElement.detachEvent('onpropertychange', handlePropertyChange);

	  activeElement = null;
	  activeElementID = null;
	  activeElementValue = null;
	  activeElementValueProp = null;
	}

	/**
	 * (For old IE.) Handles a propertychange event, sending a `change` event if
	 * the value of the active element has changed.
	 */
	function handlePropertyChange(nativeEvent) {
	  if (nativeEvent.propertyName !== 'value') {
	    return;
	  }
	  var value = nativeEvent.srcElement.value;
	  if (value === activeElementValue) {
	    return;
	  }
	  activeElementValue = value;

	  manualDispatchChangeEvent(nativeEvent);
	}

	/**
	 * If a `change` event should be fired, returns the target's ID.
	 */
	function getTargetIDForInputEvent(
	    topLevelType,
	    topLevelTarget,
	    topLevelTargetID) {
	  if (topLevelType === topLevelTypes.topInput) {
	    // In modern browsers (i.e., not IE8 or IE9), the input event is exactly
	    // what we want so fall through here and trigger an abstract event
	    return topLevelTargetID;
	  }
	}

	// For IE8 and IE9.
	function handleEventsForInputEventIE(
	    topLevelType,
	    topLevelTarget,
	    topLevelTargetID) {
	  if (topLevelType === topLevelTypes.topFocus) {
	    // In IE8, we can capture almost all .value changes by adding a
	    // propertychange handler and looking for events with propertyName
	    // equal to 'value'
	    // In IE9, propertychange fires for most input events but is buggy and
	    // doesn't fire when text is deleted, but conveniently, selectionchange
	    // appears to fire in all of the remaining cases so we catch those and
	    // forward the event if the value has changed
	    // In either case, we don't want to call the event handler if the value
	    // is changed from JS so we redefine a setter for `.value` that updates
	    // our activeElementValue variable, allowing us to ignore those changes
	    //
	    // stopWatching() should be a noop here but we call it just in case we
	    // missed a blur event somehow.
	    stopWatchingForValueChange();
	    startWatchingForValueChange(topLevelTarget, topLevelTargetID);
	  } else if (topLevelType === topLevelTypes.topBlur) {
	    stopWatchingForValueChange();
	  }
	}

	// For IE8 and IE9.
	function getTargetIDForInputEventIE(
	    topLevelType,
	    topLevelTarget,
	    topLevelTargetID) {
	  if (topLevelType === topLevelTypes.topSelectionChange ||
	      topLevelType === topLevelTypes.topKeyUp ||
	      topLevelType === topLevelTypes.topKeyDown) {
	    // On the selectionchange event, the target is just document which isn't
	    // helpful for us so just check activeElement instead.
	    //
	    // 99% of the time, keydown and keyup aren't necessary. IE8 fails to fire
	    // propertychange on the first input event after setting `value` from a
	    // script and fires only keydown, keypress, keyup. Catching keyup usually
	    // gets it and catching keydown lets us fire an event for the first
	    // keystroke if user does a key repeat (it'll be a little delayed: right
	    // before the second keystroke). Other input methods (e.g., paste) seem to
	    // fire selectionchange normally.
	    if (activeElement && activeElement.value !== activeElementValue) {
	      activeElementValue = activeElement.value;
	      return activeElementID;
	    }
	  }
	}


	/**
	 * SECTION: handle `click` event
	 */
	function shouldUseClickEvent(elem) {
	  // Use the `click` event to detect changes to checkbox and radio inputs.
	  // This approach works across all browsers, whereas `change` does not fire
	  // until `blur` in IE8.
	  return (
	    elem.nodeName === 'INPUT' &&
	    (elem.type === 'checkbox' || elem.type === 'radio')
	  );
	}

	function getTargetIDForClickEvent(
	    topLevelType,
	    topLevelTarget,
	    topLevelTargetID) {
	  if (topLevelType === topLevelTypes.topClick) {
	    return topLevelTargetID;
	  }
	}

	/**
	 * This plugin creates an `onChange` event that normalizes change events
	 * across form elements. This event fires at a time when it's possible to
	 * change the element's value without seeing a flicker.
	 *
	 * Supported elements are:
	 * - input (see `isTextInputElement`)
	 * - textarea
	 * - select
	 */
	var ChangeEventPlugin = {

	  eventTypes: eventTypes,

	  /**
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @see {EventPluginHub.extractEvents}
	   */
	  extractEvents: function(
	      topLevelType,
	      topLevelTarget,
	      topLevelTargetID,
	      nativeEvent) {

	    var getTargetIDFunc, handleEventFunc;
	    if (shouldUseChangeEvent(topLevelTarget)) {
	      if (doesChangeEventBubble) {
	        getTargetIDFunc = getTargetIDForChangeEvent;
	      } else {
	        handleEventFunc = handleEventsForChangeEventIE8;
	      }
	    } else if (isTextInputElement(topLevelTarget)) {
	      if (isInputEventSupported) {
	        getTargetIDFunc = getTargetIDForInputEvent;
	      } else {
	        getTargetIDFunc = getTargetIDForInputEventIE;
	        handleEventFunc = handleEventsForInputEventIE;
	      }
	    } else if (shouldUseClickEvent(topLevelTarget)) {
	      getTargetIDFunc = getTargetIDForClickEvent;
	    }

	    if (getTargetIDFunc) {
	      var targetID = getTargetIDFunc(
	        topLevelType,
	        topLevelTarget,
	        topLevelTargetID
	      );
	      if (targetID) {
	        var event = SyntheticEvent.getPooled(
	          eventTypes.change,
	          targetID,
	          nativeEvent
	        );
	        EventPropagators.accumulateTwoPhaseDispatches(event);
	        return event;
	      }
	    }

	    if (handleEventFunc) {
	      handleEventFunc(
	        topLevelType,
	        topLevelTarget,
	        topLevelTargetID
	      );
	    }
	  }

	};

	module.exports = ChangeEventPlugin;


/***/ },
/* 102 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule isTextInputElement
	 */

	'use strict';

	/**
	 * @see http://www.whatwg.org/specs/web-apps/current-work/multipage/the-input-element.html#input-type-attr-summary
	 */
	var supportedInputTypes = {
	  'color': true,
	  'date': true,
	  'datetime': true,
	  'datetime-local': true,
	  'email': true,
	  'month': true,
	  'number': true,
	  'password': true,
	  'range': true,
	  'search': true,
	  'tel': true,
	  'text': true,
	  'time': true,
	  'url': true,
	  'week': true
	};

	function isTextInputElement(elem) {
	  return elem && (
	    (elem.nodeName === 'INPUT' && supportedInputTypes[elem.type] || elem.nodeName === 'TEXTAREA')
	  );
	}

	module.exports = isTextInputElement;


/***/ },
/* 103 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ClientReactRootIndex
	 * @typechecks
	 */

	'use strict';

	var nextReactRootIndex = 0;

	var ClientReactRootIndex = {
	  createReactRootIndex: function() {
	    return nextReactRootIndex++;
	  }
	};

	module.exports = ClientReactRootIndex;


/***/ },
/* 104 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule DefaultEventPluginOrder
	 */

	'use strict';

	var keyOf = __webpack_require__(40);

	/**
	 * Module that is injectable into `EventPluginHub`, that specifies a
	 * deterministic ordering of `EventPlugin`s. A convenient way to reason about
	 * plugins, without having to package every one of them. This is better than
	 * having plugins be ordered in the same order that they are injected because
	 * that ordering would be influenced by the packaging order.
	 * `ResponderEventPlugin` must occur before `SimpleEventPlugin` so that
	 * preventing default on events is convenient in `SimpleEventPlugin` handlers.
	 */
	var DefaultEventPluginOrder = [
	  keyOf({ResponderEventPlugin: null}),
	  keyOf({SimpleEventPlugin: null}),
	  keyOf({TapEventPlugin: null}),
	  keyOf({EnterLeaveEventPlugin: null}),
	  keyOf({ChangeEventPlugin: null}),
	  keyOf({SelectEventPlugin: null}),
	  keyOf({BeforeInputEventPlugin: null}),
	  keyOf({AnalyticsEventPlugin: null}),
	  keyOf({MobileSafariClickEventPlugin: null})
	];

	module.exports = DefaultEventPluginOrder;


/***/ },
/* 105 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule EnterLeaveEventPlugin
	 * @typechecks static-only
	 */

	'use strict';

	var EventConstants = __webpack_require__(6);
	var EventPropagators = __webpack_require__(94);
	var SyntheticMouseEvent = __webpack_require__(106);

	var ReactMount = __webpack_require__(68);
	var keyOf = __webpack_require__(40);

	var topLevelTypes = EventConstants.topLevelTypes;
	var getFirstReactDOM = ReactMount.getFirstReactDOM;

	var eventTypes = {
	  mouseEnter: {
	    registrationName: keyOf({onMouseEnter: null}),
	    dependencies: [
	      topLevelTypes.topMouseOut,
	      topLevelTypes.topMouseOver
	    ]
	  },
	  mouseLeave: {
	    registrationName: keyOf({onMouseLeave: null}),
	    dependencies: [
	      topLevelTypes.topMouseOut,
	      topLevelTypes.topMouseOver
	    ]
	  }
	};

	var extractedEvents = [null, null];

	var EnterLeaveEventPlugin = {

	  eventTypes: eventTypes,

	  /**
	   * For almost every interaction we care about, there will be both a top-level
	   * `mouseover` and `mouseout` event that occurs. Only use `mouseout` so that
	   * we do not extract duplicate events. However, moving the mouse into the
	   * browser from outside will not fire a `mouseout` event. In this case, we use
	   * the `mouseover` top-level event.
	   *
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @see {EventPluginHub.extractEvents}
	   */
	  extractEvents: function(
	      topLevelType,
	      topLevelTarget,
	      topLevelTargetID,
	      nativeEvent) {
	    if (topLevelType === topLevelTypes.topMouseOver &&
	        (nativeEvent.relatedTarget || nativeEvent.fromElement)) {
	      return null;
	    }
	    if (topLevelType !== topLevelTypes.topMouseOut &&
	        topLevelType !== topLevelTypes.topMouseOver) {
	      // Must not be a mouse in or mouse out - ignoring.
	      return null;
	    }

	    var win;
	    if (topLevelTarget.window === topLevelTarget) {
	      // `topLevelTarget` is probably a window object.
	      win = topLevelTarget;
	    } else {
	      // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
	      var doc = topLevelTarget.ownerDocument;
	      if (doc) {
	        win = doc.defaultView || doc.parentWindow;
	      } else {
	        win = window;
	      }
	    }

	    var from, to;
	    if (topLevelType === topLevelTypes.topMouseOut) {
	      from = topLevelTarget;
	      to =
	        getFirstReactDOM(nativeEvent.relatedTarget || nativeEvent.toElement) ||
	        win;
	    } else {
	      from = win;
	      to = topLevelTarget;
	    }

	    if (from === to) {
	      // Nothing pertains to our managed components.
	      return null;
	    }

	    var fromID = from ? ReactMount.getID(from) : '';
	    var toID = to ? ReactMount.getID(to) : '';

	    var leave = SyntheticMouseEvent.getPooled(
	      eventTypes.mouseLeave,
	      fromID,
	      nativeEvent
	    );
	    leave.type = 'mouseleave';
	    leave.target = from;
	    leave.relatedTarget = to;

	    var enter = SyntheticMouseEvent.getPooled(
	      eventTypes.mouseEnter,
	      toID,
	      nativeEvent
	    );
	    enter.type = 'mouseenter';
	    enter.target = to;
	    enter.relatedTarget = from;

	    EventPropagators.accumulateEnterLeaveDispatches(leave, enter, fromID, toID);

	    extractedEvents[0] = leave;
	    extractedEvents[1] = enter;

	    return extractedEvents;
	  }

	};

	module.exports = EnterLeaveEventPlugin;


/***/ },
/* 106 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticMouseEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticUIEvent = __webpack_require__(107);
	var ViewportMetrics = __webpack_require__(75);

	var getEventModifierState = __webpack_require__(108);

	/**
	 * @interface MouseEvent
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var MouseEventInterface = {
	  screenX: null,
	  screenY: null,
	  clientX: null,
	  clientY: null,
	  ctrlKey: null,
	  shiftKey: null,
	  altKey: null,
	  metaKey: null,
	  getModifierState: getEventModifierState,
	  button: function(event) {
	    // Webkit, Firefox, IE9+
	    // which:  1 2 3
	    // button: 0 1 2 (standard)
	    var button = event.button;
	    if ('which' in event) {
	      return button;
	    }
	    // IE<9
	    // which:  undefined
	    // button: 0 0 0
	    // button: 1 4 2 (onmouseup)
	    return button === 2 ? 2 : button === 4 ? 1 : 0;
	  },
	  buttons: null,
	  relatedTarget: function(event) {
	    return event.relatedTarget || (
	      ((event.fromElement === event.srcElement ? event.toElement : event.fromElement))
	    );
	  },
	  // "Proprietary" Interface.
	  pageX: function(event) {
	    return 'pageX' in event ?
	      event.pageX :
	      event.clientX + ViewportMetrics.currentScrollLeft;
	  },
	  pageY: function(event) {
	    return 'pageY' in event ?
	      event.pageY :
	      event.clientY + ViewportMetrics.currentScrollTop;
	  }
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticMouseEvent(dispatchConfig, dispatchMarker, nativeEvent) {
	  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
	}

	SyntheticUIEvent.augmentClass(SyntheticMouseEvent, MouseEventInterface);

	module.exports = SyntheticMouseEvent;


/***/ },
/* 107 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticUIEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticEvent = __webpack_require__(98);

	var getEventTarget = __webpack_require__(99);

	/**
	 * @interface UIEvent
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var UIEventInterface = {
	  view: function(event) {
	    if (event.view) {
	      return event.view;
	    }

	    var target = getEventTarget(event);
	    if (target != null && target.window === target) {
	      // target is a window object
	      return target;
	    }

	    var doc = target.ownerDocument;
	    // TODO: Figure out why `ownerDocument` is sometimes undefined in IE8.
	    if (doc) {
	      return doc.defaultView || doc.parentWindow;
	    } else {
	      return window;
	    }
	  },
	  detail: function(event) {
	    return event.detail || 0;
	  }
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticEvent}
	 */
	function SyntheticUIEvent(dispatchConfig, dispatchMarker, nativeEvent) {
	  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
	}

	SyntheticEvent.augmentClass(SyntheticUIEvent, UIEventInterface);

	module.exports = SyntheticUIEvent;


/***/ },
/* 108 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getEventModifierState
	 * @typechecks static-only
	 */

	'use strict';

	/**
	 * Translation from modifier key to the associated property in the event.
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/#keys-Modifiers
	 */

	var modifierKeyToProp = {
	  'Alt': 'altKey',
	  'Control': 'ctrlKey',
	  'Meta': 'metaKey',
	  'Shift': 'shiftKey'
	};

	// IE8 does not implement getModifierState so we simply map it to the only
	// modifier keys exposed by the event itself, does not support Lock-keys.
	// Currently, all major browsers except Chrome seems to support Lock-keys.
	function modifierStateGetter(keyArg) {
	  /*jshint validthis:true */
	  var syntheticEvent = this;
	  var nativeEvent = syntheticEvent.nativeEvent;
	  if (nativeEvent.getModifierState) {
	    return nativeEvent.getModifierState(keyArg);
	  }
	  var keyProp = modifierKeyToProp[keyArg];
	  return keyProp ? !!nativeEvent[keyProp] : false;
	}

	function getEventModifierState(nativeEvent) {
	  return modifierStateGetter;
	}

	module.exports = getEventModifierState;


/***/ },
/* 109 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule HTMLDOMPropertyConfig
	 */

	/*jslint bitwise: true*/

	'use strict';

	var DOMProperty = __webpack_require__(45);
	var ExecutionEnvironment = __webpack_require__(52);

	var MUST_USE_ATTRIBUTE = DOMProperty.injection.MUST_USE_ATTRIBUTE;
	var MUST_USE_PROPERTY = DOMProperty.injection.MUST_USE_PROPERTY;
	var HAS_BOOLEAN_VALUE = DOMProperty.injection.HAS_BOOLEAN_VALUE;
	var HAS_SIDE_EFFECTS = DOMProperty.injection.HAS_SIDE_EFFECTS;
	var HAS_NUMERIC_VALUE = DOMProperty.injection.HAS_NUMERIC_VALUE;
	var HAS_POSITIVE_NUMERIC_VALUE =
	  DOMProperty.injection.HAS_POSITIVE_NUMERIC_VALUE;
	var HAS_OVERLOADED_BOOLEAN_VALUE =
	  DOMProperty.injection.HAS_OVERLOADED_BOOLEAN_VALUE;

	var hasSVG;
	if (ExecutionEnvironment.canUseDOM) {
	  var implementation = document.implementation;
	  hasSVG = (
	    implementation &&
	    implementation.hasFeature &&
	    implementation.hasFeature(
	      'http://www.w3.org/TR/SVG11/feature#BasicStructure',
	      '1.1'
	    )
	  );
	}


	var HTMLDOMPropertyConfig = {
	  isCustomAttribute: RegExp.prototype.test.bind(
	    /^(data|aria)-[a-z_][a-z\d_.\-]*$/
	  ),
	  Properties: {
	    /**
	     * Standard Properties
	     */
	    accept: null,
	    acceptCharset: null,
	    accessKey: null,
	    action: null,
	    allowFullScreen: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
	    allowTransparency: MUST_USE_ATTRIBUTE,
	    alt: null,
	    async: HAS_BOOLEAN_VALUE,
	    autoComplete: null,
	    // autoFocus is polyfilled/normalized by AutoFocusMixin
	    // autoFocus: HAS_BOOLEAN_VALUE,
	    autoPlay: HAS_BOOLEAN_VALUE,
	    cellPadding: null,
	    cellSpacing: null,
	    charSet: MUST_USE_ATTRIBUTE,
	    checked: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    classID: MUST_USE_ATTRIBUTE,
	    // To set className on SVG elements, it's necessary to use .setAttribute;
	    // this works on HTML elements too in all browsers except IE8. Conveniently,
	    // IE8 doesn't support SVG and so we can simply use the attribute in
	    // browsers that support SVG and the property in browsers that don't,
	    // regardless of whether the element is HTML or SVG.
	    className: hasSVG ? MUST_USE_ATTRIBUTE : MUST_USE_PROPERTY,
	    cols: MUST_USE_ATTRIBUTE | HAS_POSITIVE_NUMERIC_VALUE,
	    colSpan: null,
	    content: null,
	    contentEditable: null,
	    contextMenu: MUST_USE_ATTRIBUTE,
	    controls: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    coords: null,
	    crossOrigin: null,
	    data: null, // For `<object />` acts as `src`.
	    dateTime: MUST_USE_ATTRIBUTE,
	    defer: HAS_BOOLEAN_VALUE,
	    dir: null,
	    disabled: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
	    download: HAS_OVERLOADED_BOOLEAN_VALUE,
	    draggable: null,
	    encType: null,
	    form: MUST_USE_ATTRIBUTE,
	    formAction: MUST_USE_ATTRIBUTE,
	    formEncType: MUST_USE_ATTRIBUTE,
	    formMethod: MUST_USE_ATTRIBUTE,
	    formNoValidate: HAS_BOOLEAN_VALUE,
	    formTarget: MUST_USE_ATTRIBUTE,
	    frameBorder: MUST_USE_ATTRIBUTE,
	    headers: null,
	    height: MUST_USE_ATTRIBUTE,
	    hidden: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
	    high: null,
	    href: null,
	    hrefLang: null,
	    htmlFor: null,
	    httpEquiv: null,
	    icon: null,
	    id: MUST_USE_PROPERTY,
	    label: null,
	    lang: null,
	    list: MUST_USE_ATTRIBUTE,
	    loop: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    low: null,
	    manifest: MUST_USE_ATTRIBUTE,
	    marginHeight: null,
	    marginWidth: null,
	    max: null,
	    maxLength: MUST_USE_ATTRIBUTE,
	    media: MUST_USE_ATTRIBUTE,
	    mediaGroup: null,
	    method: null,
	    min: null,
	    multiple: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    muted: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    name: null,
	    noValidate: HAS_BOOLEAN_VALUE,
	    open: HAS_BOOLEAN_VALUE,
	    optimum: null,
	    pattern: null,
	    placeholder: null,
	    poster: null,
	    preload: null,
	    radioGroup: null,
	    readOnly: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    rel: null,
	    required: HAS_BOOLEAN_VALUE,
	    role: MUST_USE_ATTRIBUTE,
	    rows: MUST_USE_ATTRIBUTE | HAS_POSITIVE_NUMERIC_VALUE,
	    rowSpan: null,
	    sandbox: null,
	    scope: null,
	    scoped: HAS_BOOLEAN_VALUE,
	    scrolling: null,
	    seamless: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
	    selected: MUST_USE_PROPERTY | HAS_BOOLEAN_VALUE,
	    shape: null,
	    size: MUST_USE_ATTRIBUTE | HAS_POSITIVE_NUMERIC_VALUE,
	    sizes: MUST_USE_ATTRIBUTE,
	    span: HAS_POSITIVE_NUMERIC_VALUE,
	    spellCheck: null,
	    src: null,
	    srcDoc: MUST_USE_PROPERTY,
	    srcSet: MUST_USE_ATTRIBUTE,
	    start: HAS_NUMERIC_VALUE,
	    step: null,
	    style: null,
	    tabIndex: null,
	    target: null,
	    title: null,
	    type: null,
	    useMap: null,
	    value: MUST_USE_PROPERTY | HAS_SIDE_EFFECTS,
	    width: MUST_USE_ATTRIBUTE,
	    wmode: MUST_USE_ATTRIBUTE,

	    /**
	     * Non-standard Properties
	     */
	    // autoCapitalize and autoCorrect are supported in Mobile Safari for
	    // keyboard hints.
	    autoCapitalize: null,
	    autoCorrect: null,
	    // itemProp, itemScope, itemType are for
	    // Microdata support. See http://schema.org/docs/gs.html
	    itemProp: MUST_USE_ATTRIBUTE,
	    itemScope: MUST_USE_ATTRIBUTE | HAS_BOOLEAN_VALUE,
	    itemType: MUST_USE_ATTRIBUTE,
	    // itemID and itemRef are for Microdata support as well but
	    // only specified in the the WHATWG spec document. See
	    // https://html.spec.whatwg.org/multipage/microdata.html#microdata-dom-api
	    itemID: MUST_USE_ATTRIBUTE,
	    itemRef: MUST_USE_ATTRIBUTE,
	    // property is supported for OpenGraph in meta tags.
	    property: null,
	    // IE-only attribute that controls focus behavior
	    unselectable: MUST_USE_ATTRIBUTE
	  },
	  DOMAttributeNames: {
	    acceptCharset: 'accept-charset',
	    className: 'class',
	    htmlFor: 'for',
	    httpEquiv: 'http-equiv'
	  },
	  DOMPropertyNames: {
	    autoCapitalize: 'autocapitalize',
	    autoComplete: 'autocomplete',
	    autoCorrect: 'autocorrect',
	    autoFocus: 'autofocus',
	    autoPlay: 'autoplay',
	    // `encoding` is equivalent to `enctype`, IE8 lacks an `enctype` setter.
	    // http://www.w3.org/TR/html5/forms.html#dom-fs-encoding
	    encType: 'encoding',
	    hrefLang: 'hreflang',
	    radioGroup: 'radiogroup',
	    spellCheck: 'spellcheck',
	    srcDoc: 'srcdoc',
	    srcSet: 'srcset'
	  }
	};

	module.exports = HTMLDOMPropertyConfig;


/***/ },
/* 110 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule MobileSafariClickEventPlugin
	 * @typechecks static-only
	 */

	'use strict';

	var EventConstants = __webpack_require__(6);

	var emptyFunction = __webpack_require__(17);

	var topLevelTypes = EventConstants.topLevelTypes;

	/**
	 * Mobile Safari does not fire properly bubble click events on non-interactive
	 * elements, which means delegated click listeners do not fire. The workaround
	 * for this bug involves attaching an empty click listener on the target node.
	 *
	 * This particular plugin works around the bug by attaching an empty click
	 * listener on `touchstart` (which does fire on every element).
	 */
	var MobileSafariClickEventPlugin = {

	  eventTypes: null,

	  /**
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @see {EventPluginHub.extractEvents}
	   */
	  extractEvents: function(
	      topLevelType,
	      topLevelTarget,
	      topLevelTargetID,
	      nativeEvent) {
	    if (topLevelType === topLevelTypes.topTouchStart) {
	      var target = nativeEvent.target;
	      if (target && !target.onclick) {
	        target.onclick = emptyFunction;
	      }
	    }
	  }

	};

	module.exports = MobileSafariClickEventPlugin;


/***/ },
/* 111 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactBrowserComponentMixin
	 */

	'use strict';

	var findDOMNode = __webpack_require__(112);

	var ReactBrowserComponentMixin = {
	  /**
	   * Returns the DOM node rendered by this component.
	   *
	   * @return {DOMElement} The root node of this component.
	   * @final
	   * @protected
	   */
	  getDOMNode: function() {
	    return findDOMNode(this);
	  }
	};

	module.exports = ReactBrowserComponentMixin;


/***/ },
/* 112 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule findDOMNode
	 * @typechecks static-only
	 */

	'use strict';

	var ReactCurrentOwner = __webpack_require__(18);
	var ReactInstanceMap = __webpack_require__(37);
	var ReactMount = __webpack_require__(68);

	var invariant = __webpack_require__(8);
	var isNode = __webpack_require__(82);
	var warning = __webpack_require__(16);

	/**
	 * Returns the DOM node rendered by this element.
	 *
	 * @param {ReactComponent|DOMElement} componentOrElement
	 * @return {DOMElement} The root node of this element.
	 */
	function findDOMNode(componentOrElement) {
	  if ("production" !== process.env.NODE_ENV) {
	    var owner = ReactCurrentOwner.current;
	    if (owner !== null) {
	      ("production" !== process.env.NODE_ENV ? warning(
	        owner._warnedAboutRefsInRender,
	        '%s is accessing getDOMNode or findDOMNode inside its render(). ' +
	        'render() should be a pure function of props and state. It should ' +
	        'never access something that requires stale data from the previous ' +
	        'render, such as refs. Move this logic to componentDidMount and ' +
	        'componentDidUpdate instead.',
	        owner.getName() || 'A component'
	      ) : null);
	      owner._warnedAboutRefsInRender = true;
	    }
	  }
	  if (componentOrElement == null) {
	    return null;
	  }
	  if (isNode(componentOrElement)) {
	    return componentOrElement;
	  }
	  if (ReactInstanceMap.has(componentOrElement)) {
	    return ReactMount.getNodeFromInstance(componentOrElement);
	  }
	  ("production" !== process.env.NODE_ENV ? invariant(
	    componentOrElement.render == null ||
	    typeof componentOrElement.render !== 'function',
	    'Component (with keys: %s) contains `render` method ' +
	    'but is not mounted in the DOM',
	    Object.keys(componentOrElement)
	  ) : invariant(componentOrElement.render == null ||
	  typeof componentOrElement.render !== 'function'));
	  ("production" !== process.env.NODE_ENV ? invariant(
	    false,
	    'Element appears to be neither ReactComponent nor DOMNode (keys: %s)',
	    Object.keys(componentOrElement)
	  ) : invariant(false));
	}

	module.exports = findDOMNode;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 113 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDefaultBatchingStrategy
	 */

	'use strict';

	var ReactUpdates = __webpack_require__(25);
	var Transaction = __webpack_require__(35);

	var assign = __webpack_require__(14);
	var emptyFunction = __webpack_require__(17);

	var RESET_BATCHED_UPDATES = {
	  initialize: emptyFunction,
	  close: function() {
	    ReactDefaultBatchingStrategy.isBatchingUpdates = false;
	  }
	};

	var FLUSH_BATCHED_UPDATES = {
	  initialize: emptyFunction,
	  close: ReactUpdates.flushBatchedUpdates.bind(ReactUpdates)
	};

	var TRANSACTION_WRAPPERS = [FLUSH_BATCHED_UPDATES, RESET_BATCHED_UPDATES];

	function ReactDefaultBatchingStrategyTransaction() {
	  this.reinitializeTransaction();
	}

	assign(
	  ReactDefaultBatchingStrategyTransaction.prototype,
	  Transaction.Mixin,
	  {
	    getTransactionWrappers: function() {
	      return TRANSACTION_WRAPPERS;
	    }
	  }
	);

	var transaction = new ReactDefaultBatchingStrategyTransaction();

	var ReactDefaultBatchingStrategy = {
	  isBatchingUpdates: false,

	  /**
	   * Call the provided function in a context within which calls to `setState`
	   * and friends are batched such that components aren't updated unnecessarily.
	   */
	  batchedUpdates: function(callback, a, b, c, d) {
	    var alreadyBatchingUpdates = ReactDefaultBatchingStrategy.isBatchingUpdates;

	    ReactDefaultBatchingStrategy.isBatchingUpdates = true;

	    // The code is written this way to avoid extra allocations
	    if (alreadyBatchingUpdates) {
	      callback(a, b, c, d);
	    } else {
	      transaction.perform(callback, null, a, b, c, d);
	    }
	  }
	};

	module.exports = ReactDefaultBatchingStrategy;


/***/ },
/* 114 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMButton
	 */

	'use strict';

	var AutoFocusMixin = __webpack_require__(115);
	var ReactBrowserComponentMixin = __webpack_require__(111);
	var ReactClass = __webpack_require__(38);
	var ReactElement = __webpack_require__(12);

	var keyMirror = __webpack_require__(7);

	var button = ReactElement.createFactory('button');

	var mouseListenerNames = keyMirror({
	  onClick: true,
	  onDoubleClick: true,
	  onMouseDown: true,
	  onMouseMove: true,
	  onMouseUp: true,
	  onClickCapture: true,
	  onDoubleClickCapture: true,
	  onMouseDownCapture: true,
	  onMouseMoveCapture: true,
	  onMouseUpCapture: true
	});

	/**
	 * Implements a <button> native component that does not receive mouse events
	 * when `disabled` is set.
	 */
	var ReactDOMButton = ReactClass.createClass({
	  displayName: 'ReactDOMButton',
	  tagName: 'BUTTON',

	  mixins: [AutoFocusMixin, ReactBrowserComponentMixin],

	  render: function() {
	    var props = {};

	    // Copy the props; except the mouse listeners if we're disabled
	    for (var key in this.props) {
	      if (this.props.hasOwnProperty(key) &&
	          (!this.props.disabled || !mouseListenerNames[key])) {
	        props[key] = this.props[key];
	      }
	    }

	    return button(props, this.props.children);
	  }

	});

	module.exports = ReactDOMButton;


/***/ },
/* 115 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule AutoFocusMixin
	 * @typechecks static-only
	 */

	'use strict';

	var focusNode = __webpack_require__(116);

	var AutoFocusMixin = {
	  componentDidMount: function() {
	    if (this.props.autoFocus) {
	      focusNode(this.getDOMNode());
	    }
	  }
	};

	module.exports = AutoFocusMixin;


/***/ },
/* 116 */
/***/ function(module, exports) {

	/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule focusNode
	 */

	"use strict";

	/**
	 * @param {DOMElement} node input/textarea to focus
	 */
	function focusNode(node) {
	  // IE8 can throw "Can't move focus to the control because it is invisible,
	  // not enabled, or of a type that does not accept the focus." for all kinds of
	  // reasons that are too expensive and fragile to test.
	  try {
	    node.focus();
	  } catch(e) {
	  }
	}

	module.exports = focusNode;


/***/ },
/* 117 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMForm
	 */

	'use strict';

	var EventConstants = __webpack_require__(6);
	var LocalEventTrapMixin = __webpack_require__(118);
	var ReactBrowserComponentMixin = __webpack_require__(111);
	var ReactClass = __webpack_require__(38);
	var ReactElement = __webpack_require__(12);

	var form = ReactElement.createFactory('form');

	/**
	 * Since onSubmit doesn't bubble OR capture on the top level in IE8, we need
	 * to capture it on the <form> element itself. There are lots of hacks we could
	 * do to accomplish this, but the most reliable is to make <form> a
	 * composite component and use `componentDidMount` to attach the event handlers.
	 */
	var ReactDOMForm = ReactClass.createClass({
	  displayName: 'ReactDOMForm',
	  tagName: 'FORM',

	  mixins: [ReactBrowserComponentMixin, LocalEventTrapMixin],

	  render: function() {
	    // TODO: Instead of using `ReactDOM` directly, we should use JSX. However,
	    // `jshint` fails to parse JSX so in order for linting to work in the open
	    // source repo, we need to just use `ReactDOM.form`.
	    return form(this.props);
	  },

	  componentDidMount: function() {
	    this.trapBubbledEvent(EventConstants.topLevelTypes.topReset, 'reset');
	    this.trapBubbledEvent(EventConstants.topLevelTypes.topSubmit, 'submit');
	  }
	});

	module.exports = ReactDOMForm;


/***/ },
/* 118 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule LocalEventTrapMixin
	 */

	'use strict';

	var ReactBrowserEventEmitter = __webpack_require__(69);

	var accumulateInto = __webpack_require__(72);
	var forEachAccumulated = __webpack_require__(73);
	var invariant = __webpack_require__(8);

	function remove(event) {
	  event.remove();
	}

	var LocalEventTrapMixin = {
	  trapBubbledEvent:function(topLevelType, handlerBaseName) {
	    ("production" !== process.env.NODE_ENV ? invariant(this.isMounted(), 'Must be mounted to trap events') : invariant(this.isMounted()));
	    // If a component renders to null or if another component fatals and causes
	    // the state of the tree to be corrupted, `node` here can be null.
	    var node = this.getDOMNode();
	    ("production" !== process.env.NODE_ENV ? invariant(
	      node,
	      'LocalEventTrapMixin.trapBubbledEvent(...): Requires node to be rendered.'
	    ) : invariant(node));
	    var listener = ReactBrowserEventEmitter.trapBubbledEvent(
	      topLevelType,
	      handlerBaseName,
	      node
	    );
	    this._localEventListeners =
	      accumulateInto(this._localEventListeners, listener);
	  },

	  // trapCapturedEvent would look nearly identical. We don't implement that
	  // method because it isn't currently needed.

	  componentWillUnmount:function() {
	    if (this._localEventListeners) {
	      forEachAccumulated(this._localEventListeners, remove);
	    }
	  }
	};

	module.exports = LocalEventTrapMixin;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 119 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMImg
	 */

	'use strict';

	var EventConstants = __webpack_require__(6);
	var LocalEventTrapMixin = __webpack_require__(118);
	var ReactBrowserComponentMixin = __webpack_require__(111);
	var ReactClass = __webpack_require__(38);
	var ReactElement = __webpack_require__(12);

	var img = ReactElement.createFactory('img');

	/**
	 * Since onLoad doesn't bubble OR capture on the top level in IE8, we need to
	 * capture it on the <img> element itself. There are lots of hacks we could do
	 * to accomplish this, but the most reliable is to make <img> a composite
	 * component and use `componentDidMount` to attach the event handlers.
	 */
	var ReactDOMImg = ReactClass.createClass({
	  displayName: 'ReactDOMImg',
	  tagName: 'IMG',

	  mixins: [ReactBrowserComponentMixin, LocalEventTrapMixin],

	  render: function() {
	    return img(this.props);
	  },

	  componentDidMount: function() {
	    this.trapBubbledEvent(EventConstants.topLevelTypes.topLoad, 'load');
	    this.trapBubbledEvent(EventConstants.topLevelTypes.topError, 'error');
	  }
	});

	module.exports = ReactDOMImg;


/***/ },
/* 120 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMIframe
	 */

	'use strict';

	var EventConstants = __webpack_require__(6);
	var LocalEventTrapMixin = __webpack_require__(118);
	var ReactBrowserComponentMixin = __webpack_require__(111);
	var ReactClass = __webpack_require__(38);
	var ReactElement = __webpack_require__(12);

	var iframe = ReactElement.createFactory('iframe');

	/**
	 * Since onLoad doesn't bubble OR capture on the top level in IE8, we need to
	 * capture it on the <iframe> element itself. There are lots of hacks we could
	 * do to accomplish this, but the most reliable is to make <iframe> a composite
	 * component and use `componentDidMount` to attach the event handlers.
	 */
	var ReactDOMIframe = ReactClass.createClass({
	  displayName: 'ReactDOMIframe',
	  tagName: 'IFRAME',

	  mixins: [ReactBrowserComponentMixin, LocalEventTrapMixin],

	  render: function() {
	    return iframe(this.props);
	  },

	  componentDidMount: function() {
	    this.trapBubbledEvent(EventConstants.topLevelTypes.topLoad, 'load');
	  }
	});

	module.exports = ReactDOMIframe;


/***/ },
/* 121 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMInput
	 */

	'use strict';

	var AutoFocusMixin = __webpack_require__(115);
	var DOMPropertyOperations = __webpack_require__(44);
	var LinkedValueUtils = __webpack_require__(122);
	var ReactBrowserComponentMixin = __webpack_require__(111);
	var ReactClass = __webpack_require__(38);
	var ReactElement = __webpack_require__(12);
	var ReactMount = __webpack_require__(68);
	var ReactUpdates = __webpack_require__(25);

	var assign = __webpack_require__(14);
	var invariant = __webpack_require__(8);

	var input = ReactElement.createFactory('input');

	var instancesByReactID = {};

	function forceUpdateIfMounted() {
	  /*jshint validthis:true */
	  if (this.isMounted()) {
	    this.forceUpdate();
	  }
	}

	/**
	 * Implements an <input> native component that allows setting these optional
	 * props: `checked`, `value`, `defaultChecked`, and `defaultValue`.
	 *
	 * If `checked` or `value` are not supplied (or null/undefined), user actions
	 * that affect the checked state or value will trigger updates to the element.
	 *
	 * If they are supplied (and not null/undefined), the rendered element will not
	 * trigger updates to the element. Instead, the props must change in order for
	 * the rendered element to be updated.
	 *
	 * The rendered element will be initialized as unchecked (or `defaultChecked`)
	 * with an empty value (or `defaultValue`).
	 *
	 * @see http://www.w3.org/TR/2012/WD-html5-20121025/the-input-element.html
	 */
	var ReactDOMInput = ReactClass.createClass({
	  displayName: 'ReactDOMInput',
	  tagName: 'INPUT',

	  mixins: [AutoFocusMixin, LinkedValueUtils.Mixin, ReactBrowserComponentMixin],

	  getInitialState: function() {
	    var defaultValue = this.props.defaultValue;
	    return {
	      initialChecked: this.props.defaultChecked || false,
	      initialValue: defaultValue != null ? defaultValue : null
	    };
	  },

	  render: function() {
	    // Clone `this.props` so we don't mutate the input.
	    var props = assign({}, this.props);

	    props.defaultChecked = null;
	    props.defaultValue = null;

	    var value = LinkedValueUtils.getValue(this);
	    props.value = value != null ? value : this.state.initialValue;

	    var checked = LinkedValueUtils.getChecked(this);
	    props.checked = checked != null ? checked : this.state.initialChecked;

	    props.onChange = this._handleChange;

	    return input(props, this.props.children);
	  },

	  componentDidMount: function() {
	    var id = ReactMount.getID(this.getDOMNode());
	    instancesByReactID[id] = this;
	  },

	  componentWillUnmount: function() {
	    var rootNode = this.getDOMNode();
	    var id = ReactMount.getID(rootNode);
	    delete instancesByReactID[id];
	  },

	  componentDidUpdate: function(prevProps, prevState, prevContext) {
	    var rootNode = this.getDOMNode();
	    if (this.props.checked != null) {
	      DOMPropertyOperations.setValueForProperty(
	        rootNode,
	        'checked',
	        this.props.checked || false
	      );
	    }

	    var value = LinkedValueUtils.getValue(this);
	    if (value != null) {
	      // Cast `value` to a string to ensure the value is set correctly. While
	      // browsers typically do this as necessary, jsdom doesn't.
	      DOMPropertyOperations.setValueForProperty(rootNode, 'value', '' + value);
	    }
	  },

	  _handleChange: function(event) {
	    var returnValue;
	    var onChange = LinkedValueUtils.getOnChange(this);
	    if (onChange) {
	      returnValue = onChange.call(this, event);
	    }
	    // Here we use asap to wait until all updates have propagated, which
	    // is important when using controlled components within layers:
	    // https://github.com/facebook/react/issues/1698
	    ReactUpdates.asap(forceUpdateIfMounted, this);

	    var name = this.props.name;
	    if (this.props.type === 'radio' && name != null) {
	      var rootNode = this.getDOMNode();
	      var queryRoot = rootNode;

	      while (queryRoot.parentNode) {
	        queryRoot = queryRoot.parentNode;
	      }

	      // If `rootNode.form` was non-null, then we could try `form.elements`,
	      // but that sometimes behaves strangely in IE8. We could also try using
	      // `form.getElementsByName`, but that will only return direct children
	      // and won't include inputs that use the HTML5 `form=` attribute. Since
	      // the input might not even be in a form, let's just use the global
	      // `querySelectorAll` to ensure we don't miss anything.
	      var group = queryRoot.querySelectorAll(
	        'input[name=' + JSON.stringify('' + name) + '][type="radio"]');

	      for (var i = 0, groupLen = group.length; i < groupLen; i++) {
	        var otherNode = group[i];
	        if (otherNode === rootNode ||
	            otherNode.form !== rootNode.form) {
	          continue;
	        }
	        var otherID = ReactMount.getID(otherNode);
	        ("production" !== process.env.NODE_ENV ? invariant(
	          otherID,
	          'ReactDOMInput: Mixing React and non-React radio inputs with the ' +
	          'same `name` is not supported.'
	        ) : invariant(otherID));
	        var otherInstance = instancesByReactID[otherID];
	        ("production" !== process.env.NODE_ENV ? invariant(
	          otherInstance,
	          'ReactDOMInput: Unknown radio button ID %s.',
	          otherID
	        ) : invariant(otherInstance));
	        // If this is a controlled radio button group, forcing the input that
	        // was previously checked to update will cause it to be come re-checked
	        // as appropriate.
	        ReactUpdates.asap(forceUpdateIfMounted, otherInstance);
	      }
	    }

	    return returnValue;
	  }

	});

	module.exports = ReactDOMInput;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 122 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule LinkedValueUtils
	 * @typechecks static-only
	 */

	'use strict';

	var ReactPropTypes = __webpack_require__(123);

	var invariant = __webpack_require__(8);

	var hasReadOnlyValue = {
	  'button': true,
	  'checkbox': true,
	  'image': true,
	  'hidden': true,
	  'radio': true,
	  'reset': true,
	  'submit': true
	};

	function _assertSingleLink(input) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    input.props.checkedLink == null || input.props.valueLink == null,
	    'Cannot provide a checkedLink and a valueLink. If you want to use ' +
	    'checkedLink, you probably don\'t want to use valueLink and vice versa.'
	  ) : invariant(input.props.checkedLink == null || input.props.valueLink == null));
	}
	function _assertValueLink(input) {
	  _assertSingleLink(input);
	  ("production" !== process.env.NODE_ENV ? invariant(
	    input.props.value == null && input.props.onChange == null,
	    'Cannot provide a valueLink and a value or onChange event. If you want ' +
	    'to use value or onChange, you probably don\'t want to use valueLink.'
	  ) : invariant(input.props.value == null && input.props.onChange == null));
	}

	function _assertCheckedLink(input) {
	  _assertSingleLink(input);
	  ("production" !== process.env.NODE_ENV ? invariant(
	    input.props.checked == null && input.props.onChange == null,
	    'Cannot provide a checkedLink and a checked property or onChange event. ' +
	    'If you want to use checked or onChange, you probably don\'t want to ' +
	    'use checkedLink'
	  ) : invariant(input.props.checked == null && input.props.onChange == null));
	}

	/**
	 * @param {SyntheticEvent} e change event to handle
	 */
	function _handleLinkedValueChange(e) {
	  /*jshint validthis:true */
	  this.props.valueLink.requestChange(e.target.value);
	}

	/**
	  * @param {SyntheticEvent} e change event to handle
	  */
	function _handleLinkedCheckChange(e) {
	  /*jshint validthis:true */
	  this.props.checkedLink.requestChange(e.target.checked);
	}

	/**
	 * Provide a linked `value` attribute for controlled forms. You should not use
	 * this outside of the ReactDOM controlled form components.
	 */
	var LinkedValueUtils = {
	  Mixin: {
	    propTypes: {
	      value: function(props, propName, componentName) {
	        if (!props[propName] ||
	            hasReadOnlyValue[props.type] ||
	            props.onChange ||
	            props.readOnly ||
	            props.disabled) {
	          return null;
	        }
	        return new Error(
	          'You provided a `value` prop to a form field without an ' +
	          '`onChange` handler. This will render a read-only field. If ' +
	          'the field should be mutable use `defaultValue`. Otherwise, ' +
	          'set either `onChange` or `readOnly`.'
	        );
	      },
	      checked: function(props, propName, componentName) {
	        if (!props[propName] ||
	            props.onChange ||
	            props.readOnly ||
	            props.disabled) {
	          return null;
	        }
	        return new Error(
	          'You provided a `checked` prop to a form field without an ' +
	          '`onChange` handler. This will render a read-only field. If ' +
	          'the field should be mutable use `defaultChecked`. Otherwise, ' +
	          'set either `onChange` or `readOnly`.'
	        );
	      },
	      onChange: ReactPropTypes.func
	    }
	  },

	  /**
	   * @param {ReactComponent} input Form component
	   * @return {*} current value of the input either from value prop or link.
	   */
	  getValue: function(input) {
	    if (input.props.valueLink) {
	      _assertValueLink(input);
	      return input.props.valueLink.value;
	    }
	    return input.props.value;
	  },

	  /**
	   * @param {ReactComponent} input Form component
	   * @return {*} current checked status of the input either from checked prop
	   *             or link.
	   */
	  getChecked: function(input) {
	    if (input.props.checkedLink) {
	      _assertCheckedLink(input);
	      return input.props.checkedLink.value;
	    }
	    return input.props.checked;
	  },

	  /**
	   * @param {ReactComponent} input Form component
	   * @return {function} change callback either from onChange prop or link.
	   */
	  getOnChange: function(input) {
	    if (input.props.valueLink) {
	      _assertValueLink(input);
	      return _handleLinkedValueChange;
	    } else if (input.props.checkedLink) {
	      _assertCheckedLink(input);
	      return _handleLinkedCheckChange;
	    }
	    return input.props.onChange;
	  }
	};

	module.exports = LinkedValueUtils;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 123 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactPropTypes
	 */

	'use strict';

	var ReactElement = __webpack_require__(12);
	var ReactFragment = __webpack_require__(11);
	var ReactPropTypeLocationNames = __webpack_require__(33);

	var emptyFunction = __webpack_require__(17);

	/**
	 * Collection of methods that allow declaration and validation of props that are
	 * supplied to React components. Example usage:
	 *
	 *   var Props = require('ReactPropTypes');
	 *   var MyArticle = React.createClass({
	 *     propTypes: {
	 *       // An optional string prop named "description".
	 *       description: Props.string,
	 *
	 *       // A required enum prop named "category".
	 *       category: Props.oneOf(['News','Photos']).isRequired,
	 *
	 *       // A prop named "dialog" that requires an instance of Dialog.
	 *       dialog: Props.instanceOf(Dialog).isRequired
	 *     },
	 *     render: function() { ... }
	 *   });
	 *
	 * A more formal specification of how these methods are used:
	 *
	 *   type := array|bool|func|object|number|string|oneOf([...])|instanceOf(...)
	 *   decl := ReactPropTypes.{type}(.isRequired)?
	 *
	 * Each and every declaration produces a function with the same signature. This
	 * allows the creation of custom validation functions. For example:
	 *
	 *  var MyLink = React.createClass({
	 *    propTypes: {
	 *      // An optional string or URI prop named "href".
	 *      href: function(props, propName, componentName) {
	 *        var propValue = props[propName];
	 *        if (propValue != null && typeof propValue !== 'string' &&
	 *            !(propValue instanceof URI)) {
	 *          return new Error(
	 *            'Expected a string or an URI for ' + propName + ' in ' +
	 *            componentName
	 *          );
	 *        }
	 *      }
	 *    },
	 *    render: function() {...}
	 *  });
	 *
	 * @internal
	 */

	var ANONYMOUS = '<<anonymous>>';

	var elementTypeChecker = createElementTypeChecker();
	var nodeTypeChecker = createNodeChecker();

	var ReactPropTypes = {
	  array: createPrimitiveTypeChecker('array'),
	  bool: createPrimitiveTypeChecker('boolean'),
	  func: createPrimitiveTypeChecker('function'),
	  number: createPrimitiveTypeChecker('number'),
	  object: createPrimitiveTypeChecker('object'),
	  string: createPrimitiveTypeChecker('string'),

	  any: createAnyTypeChecker(),
	  arrayOf: createArrayOfTypeChecker,
	  element: elementTypeChecker,
	  instanceOf: createInstanceTypeChecker,
	  node: nodeTypeChecker,
	  objectOf: createObjectOfTypeChecker,
	  oneOf: createEnumTypeChecker,
	  oneOfType: createUnionTypeChecker,
	  shape: createShapeTypeChecker
	};

	function createChainableTypeChecker(validate) {
	  function checkType(isRequired, props, propName, componentName, location) {
	    componentName = componentName || ANONYMOUS;
	    if (props[propName] == null) {
	      var locationName = ReactPropTypeLocationNames[location];
	      if (isRequired) {
	        return new Error(
	          ("Required " + locationName + " `" + propName + "` was not specified in ") +
	          ("`" + componentName + "`.")
	        );
	      }
	      return null;
	    } else {
	      return validate(props, propName, componentName, location);
	    }
	  }

	  var chainedCheckType = checkType.bind(null, false);
	  chainedCheckType.isRequired = checkType.bind(null, true);

	  return chainedCheckType;
	}

	function createPrimitiveTypeChecker(expectedType) {
	  function validate(props, propName, componentName, location) {
	    var propValue = props[propName];
	    var propType = getPropType(propValue);
	    if (propType !== expectedType) {
	      var locationName = ReactPropTypeLocationNames[location];
	      // `propValue` being instance of, say, date/regexp, pass the 'object'
	      // check, but we can offer a more precise error message here rather than
	      // 'of type `object`'.
	      var preciseType = getPreciseType(propValue);

	      return new Error(
	        ("Invalid " + locationName + " `" + propName + "` of type `" + preciseType + "` ") +
	        ("supplied to `" + componentName + "`, expected `" + expectedType + "`.")
	      );
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function createAnyTypeChecker() {
	  return createChainableTypeChecker(emptyFunction.thatReturns(null));
	}

	function createArrayOfTypeChecker(typeChecker) {
	  function validate(props, propName, componentName, location) {
	    var propValue = props[propName];
	    if (!Array.isArray(propValue)) {
	      var locationName = ReactPropTypeLocationNames[location];
	      var propType = getPropType(propValue);
	      return new Error(
	        ("Invalid " + locationName + " `" + propName + "` of type ") +
	        ("`" + propType + "` supplied to `" + componentName + "`, expected an array.")
	      );
	    }
	    for (var i = 0; i < propValue.length; i++) {
	      var error = typeChecker(propValue, i, componentName, location);
	      if (error instanceof Error) {
	        return error;
	      }
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function createElementTypeChecker() {
	  function validate(props, propName, componentName, location) {
	    if (!ReactElement.isValidElement(props[propName])) {
	      var locationName = ReactPropTypeLocationNames[location];
	      return new Error(
	        ("Invalid " + locationName + " `" + propName + "` supplied to ") +
	        ("`" + componentName + "`, expected a ReactElement.")
	      );
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function createInstanceTypeChecker(expectedClass) {
	  function validate(props, propName, componentName, location) {
	    if (!(props[propName] instanceof expectedClass)) {
	      var locationName = ReactPropTypeLocationNames[location];
	      var expectedClassName = expectedClass.name || ANONYMOUS;
	      return new Error(
	        ("Invalid " + locationName + " `" + propName + "` supplied to ") +
	        ("`" + componentName + "`, expected instance of `" + expectedClassName + "`.")
	      );
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function createEnumTypeChecker(expectedValues) {
	  function validate(props, propName, componentName, location) {
	    var propValue = props[propName];
	    for (var i = 0; i < expectedValues.length; i++) {
	      if (propValue === expectedValues[i]) {
	        return null;
	      }
	    }

	    var locationName = ReactPropTypeLocationNames[location];
	    var valuesString = JSON.stringify(expectedValues);
	    return new Error(
	      ("Invalid " + locationName + " `" + propName + "` of value `" + propValue + "` ") +
	      ("supplied to `" + componentName + "`, expected one of " + valuesString + ".")
	    );
	  }
	  return createChainableTypeChecker(validate);
	}

	function createObjectOfTypeChecker(typeChecker) {
	  function validate(props, propName, componentName, location) {
	    var propValue = props[propName];
	    var propType = getPropType(propValue);
	    if (propType !== 'object') {
	      var locationName = ReactPropTypeLocationNames[location];
	      return new Error(
	        ("Invalid " + locationName + " `" + propName + "` of type ") +
	        ("`" + propType + "` supplied to `" + componentName + "`, expected an object.")
	      );
	    }
	    for (var key in propValue) {
	      if (propValue.hasOwnProperty(key)) {
	        var error = typeChecker(propValue, key, componentName, location);
	        if (error instanceof Error) {
	          return error;
	        }
	      }
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function createUnionTypeChecker(arrayOfTypeCheckers) {
	  function validate(props, propName, componentName, location) {
	    for (var i = 0; i < arrayOfTypeCheckers.length; i++) {
	      var checker = arrayOfTypeCheckers[i];
	      if (checker(props, propName, componentName, location) == null) {
	        return null;
	      }
	    }

	    var locationName = ReactPropTypeLocationNames[location];
	    return new Error(
	      ("Invalid " + locationName + " `" + propName + "` supplied to ") +
	      ("`" + componentName + "`.")
	    );
	  }
	  return createChainableTypeChecker(validate);
	}

	function createNodeChecker() {
	  function validate(props, propName, componentName, location) {
	    if (!isNode(props[propName])) {
	      var locationName = ReactPropTypeLocationNames[location];
	      return new Error(
	        ("Invalid " + locationName + " `" + propName + "` supplied to ") +
	        ("`" + componentName + "`, expected a ReactNode.")
	      );
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function createShapeTypeChecker(shapeTypes) {
	  function validate(props, propName, componentName, location) {
	    var propValue = props[propName];
	    var propType = getPropType(propValue);
	    if (propType !== 'object') {
	      var locationName = ReactPropTypeLocationNames[location];
	      return new Error(
	        ("Invalid " + locationName + " `" + propName + "` of type `" + propType + "` ") +
	        ("supplied to `" + componentName + "`, expected `object`.")
	      );
	    }
	    for (var key in shapeTypes) {
	      var checker = shapeTypes[key];
	      if (!checker) {
	        continue;
	      }
	      var error = checker(propValue, key, componentName, location);
	      if (error) {
	        return error;
	      }
	    }
	    return null;
	  }
	  return createChainableTypeChecker(validate);
	}

	function isNode(propValue) {
	  switch (typeof propValue) {
	    case 'number':
	    case 'string':
	    case 'undefined':
	      return true;
	    case 'boolean':
	      return !propValue;
	    case 'object':
	      if (Array.isArray(propValue)) {
	        return propValue.every(isNode);
	      }
	      if (propValue === null || ReactElement.isValidElement(propValue)) {
	        return true;
	      }
	      propValue = ReactFragment.extractIfFragment(propValue);
	      for (var k in propValue) {
	        if (!isNode(propValue[k])) {
	          return false;
	        }
	      }
	      return true;
	    default:
	      return false;
	  }
	}

	// Equivalent of `typeof` but with special handling for array and regexp.
	function getPropType(propValue) {
	  var propType = typeof propValue;
	  if (Array.isArray(propValue)) {
	    return 'array';
	  }
	  if (propValue instanceof RegExp) {
	    // Old webkits (at least until Android 4.0) return 'function' rather than
	    // 'object' for typeof a RegExp. We'll normalize this here so that /bla/
	    // passes PropTypes.object.
	    return 'object';
	  }
	  return propType;
	}

	// This handles more types than `getPropType`. Only used for error messages.
	// See `createPrimitiveTypeChecker`.
	function getPreciseType(propValue) {
	  var propType = getPropType(propValue);
	  if (propType === 'object') {
	    if (propValue instanceof Date) {
	      return 'date';
	    } else if (propValue instanceof RegExp) {
	      return 'regexp';
	    }
	  }
	  return propType;
	}

	module.exports = ReactPropTypes;


/***/ },
/* 124 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMOption
	 */

	'use strict';

	var ReactBrowserComponentMixin = __webpack_require__(111);
	var ReactClass = __webpack_require__(38);
	var ReactElement = __webpack_require__(12);

	var warning = __webpack_require__(16);

	var option = ReactElement.createFactory('option');

	/**
	 * Implements an <option> native component that warns when `selected` is set.
	 */
	var ReactDOMOption = ReactClass.createClass({
	  displayName: 'ReactDOMOption',
	  tagName: 'OPTION',

	  mixins: [ReactBrowserComponentMixin],

	  componentWillMount: function() {
	    // TODO (yungsters): Remove support for `selected` in <option>.
	    if ("production" !== process.env.NODE_ENV) {
	      ("production" !== process.env.NODE_ENV ? warning(
	        this.props.selected == null,
	        'Use the `defaultValue` or `value` props on <select> instead of ' +
	        'setting `selected` on <option>.'
	      ) : null);
	    }
	  },

	  render: function() {
	    return option(this.props, this.props.children);
	  }

	});

	module.exports = ReactDOMOption;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 125 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMSelect
	 */

	'use strict';

	var AutoFocusMixin = __webpack_require__(115);
	var LinkedValueUtils = __webpack_require__(122);
	var ReactBrowserComponentMixin = __webpack_require__(111);
	var ReactClass = __webpack_require__(38);
	var ReactElement = __webpack_require__(12);
	var ReactUpdates = __webpack_require__(25);

	var assign = __webpack_require__(14);

	var select = ReactElement.createFactory('select');

	function updateOptionsIfPendingUpdateAndMounted() {
	  /*jshint validthis:true */
	  if (this._pendingUpdate) {
	    this._pendingUpdate = false;
	    var value = LinkedValueUtils.getValue(this);
	    if (value != null && this.isMounted()) {
	      updateOptions(this, value);
	    }
	  }
	}

	/**
	 * Validation function for `value` and `defaultValue`.
	 * @private
	 */
	function selectValueType(props, propName, componentName) {
	  if (props[propName] == null) {
	    return null;
	  }
	  if (props.multiple) {
	    if (!Array.isArray(props[propName])) {
	      return new Error(
	        ("The `" + propName + "` prop supplied to <select> must be an array if ") +
	        ("`multiple` is true.")
	      );
	    }
	  } else {
	    if (Array.isArray(props[propName])) {
	      return new Error(
	        ("The `" + propName + "` prop supplied to <select> must be a scalar ") +
	        ("value if `multiple` is false.")
	      );
	    }
	  }
	}

	/**
	 * @param {ReactComponent} component Instance of ReactDOMSelect
	 * @param {*} propValue A stringable (with `multiple`, a list of stringables).
	 * @private
	 */
	function updateOptions(component, propValue) {
	  var selectedValue, i, l;
	  var options = component.getDOMNode().options;

	  if (component.props.multiple) {
	    selectedValue = {};
	    for (i = 0, l = propValue.length; i < l; i++) {
	      selectedValue['' + propValue[i]] = true;
	    }
	    for (i = 0, l = options.length; i < l; i++) {
	      var selected = selectedValue.hasOwnProperty(options[i].value);
	      if (options[i].selected !== selected) {
	        options[i].selected = selected;
	      }
	    }
	  } else {
	    // Do not set `select.value` as exact behavior isn't consistent across all
	    // browsers for all cases.
	    selectedValue = '' + propValue;
	    for (i = 0, l = options.length; i < l; i++) {
	      if (options[i].value === selectedValue) {
	        options[i].selected = true;
	        return;
	      }
	    }
	    if (options.length) {
	      options[0].selected = true;
	    }
	  }
	}

	/**
	 * Implements a <select> native component that allows optionally setting the
	 * props `value` and `defaultValue`. If `multiple` is false, the prop must be a
	 * stringable. If `multiple` is true, the prop must be an array of stringables.
	 *
	 * If `value` is not supplied (or null/undefined), user actions that change the
	 * selected option will trigger updates to the rendered options.
	 *
	 * If it is supplied (and not null/undefined), the rendered options will not
	 * update in response to user actions. Instead, the `value` prop must change in
	 * order for the rendered options to update.
	 *
	 * If `defaultValue` is provided, any options with the supplied values will be
	 * selected.
	 */
	var ReactDOMSelect = ReactClass.createClass({
	  displayName: 'ReactDOMSelect',
	  tagName: 'SELECT',

	  mixins: [AutoFocusMixin, LinkedValueUtils.Mixin, ReactBrowserComponentMixin],

	  propTypes: {
	    defaultValue: selectValueType,
	    value: selectValueType
	  },

	  render: function() {
	    // Clone `this.props` so we don't mutate the input.
	    var props = assign({}, this.props);

	    props.onChange = this._handleChange;
	    props.value = null;

	    return select(props, this.props.children);
	  },

	  componentWillMount: function() {
	    this._pendingUpdate = false;
	  },

	  componentDidMount: function() {
	    var value = LinkedValueUtils.getValue(this);
	    if (value != null) {
	      updateOptions(this, value);
	    } else if (this.props.defaultValue != null) {
	      updateOptions(this, this.props.defaultValue);
	    }
	  },

	  componentDidUpdate: function(prevProps) {
	    var value = LinkedValueUtils.getValue(this);
	    if (value != null) {
	      this._pendingUpdate = false;
	      updateOptions(this, value);
	    } else if (!prevProps.multiple !== !this.props.multiple) {
	      // For simplicity, reapply `defaultValue` if `multiple` is toggled.
	      if (this.props.defaultValue != null) {
	        updateOptions(this, this.props.defaultValue);
	      } else {
	        // Revert the select back to its default unselected state.
	        updateOptions(this, this.props.multiple ? [] : '');
	      }
	    }
	  },

	  _handleChange: function(event) {
	    var returnValue;
	    var onChange = LinkedValueUtils.getOnChange(this);
	    if (onChange) {
	      returnValue = onChange.call(this, event);
	    }

	    this._pendingUpdate = true;
	    ReactUpdates.asap(updateOptionsIfPendingUpdateAndMounted, this);
	    return returnValue;
	  }

	});

	module.exports = ReactDOMSelect;


/***/ },
/* 126 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMTextarea
	 */

	'use strict';

	var AutoFocusMixin = __webpack_require__(115);
	var DOMPropertyOperations = __webpack_require__(44);
	var LinkedValueUtils = __webpack_require__(122);
	var ReactBrowserComponentMixin = __webpack_require__(111);
	var ReactClass = __webpack_require__(38);
	var ReactElement = __webpack_require__(12);
	var ReactUpdates = __webpack_require__(25);

	var assign = __webpack_require__(14);
	var invariant = __webpack_require__(8);

	var warning = __webpack_require__(16);

	var textarea = ReactElement.createFactory('textarea');

	function forceUpdateIfMounted() {
	  /*jshint validthis:true */
	  if (this.isMounted()) {
	    this.forceUpdate();
	  }
	}

	/**
	 * Implements a <textarea> native component that allows setting `value`, and
	 * `defaultValue`. This differs from the traditional DOM API because value is
	 * usually set as PCDATA children.
	 *
	 * If `value` is not supplied (or null/undefined), user actions that affect the
	 * value will trigger updates to the element.
	 *
	 * If `value` is supplied (and not null/undefined), the rendered element will
	 * not trigger updates to the element. Instead, the `value` prop must change in
	 * order for the rendered element to be updated.
	 *
	 * The rendered element will be initialized with an empty value, the prop
	 * `defaultValue` if specified, or the children content (deprecated).
	 */
	var ReactDOMTextarea = ReactClass.createClass({
	  displayName: 'ReactDOMTextarea',
	  tagName: 'TEXTAREA',

	  mixins: [AutoFocusMixin, LinkedValueUtils.Mixin, ReactBrowserComponentMixin],

	  getInitialState: function() {
	    var defaultValue = this.props.defaultValue;
	    // TODO (yungsters): Remove support for children content in <textarea>.
	    var children = this.props.children;
	    if (children != null) {
	      if ("production" !== process.env.NODE_ENV) {
	        ("production" !== process.env.NODE_ENV ? warning(
	          false,
	          'Use the `defaultValue` or `value` props instead of setting ' +
	          'children on <textarea>.'
	        ) : null);
	      }
	      ("production" !== process.env.NODE_ENV ? invariant(
	        defaultValue == null,
	        'If you supply `defaultValue` on a <textarea>, do not pass children.'
	      ) : invariant(defaultValue == null));
	      if (Array.isArray(children)) {
	        ("production" !== process.env.NODE_ENV ? invariant(
	          children.length <= 1,
	          '<textarea> can only have at most one child.'
	        ) : invariant(children.length <= 1));
	        children = children[0];
	      }

	      defaultValue = '' + children;
	    }
	    if (defaultValue == null) {
	      defaultValue = '';
	    }
	    var value = LinkedValueUtils.getValue(this);
	    return {
	      // We save the initial value so that `ReactDOMComponent` doesn't update
	      // `textContent` (unnecessary since we update value).
	      // The initial value can be a boolean or object so that's why it's
	      // forced to be a string.
	      initialValue: '' + (value != null ? value : defaultValue)
	    };
	  },

	  render: function() {
	    // Clone `this.props` so we don't mutate the input.
	    var props = assign({}, this.props);

	    ("production" !== process.env.NODE_ENV ? invariant(
	      props.dangerouslySetInnerHTML == null,
	      '`dangerouslySetInnerHTML` does not make sense on <textarea>.'
	    ) : invariant(props.dangerouslySetInnerHTML == null));

	    props.defaultValue = null;
	    props.value = null;
	    props.onChange = this._handleChange;

	    // Always set children to the same thing. In IE9, the selection range will
	    // get reset if `textContent` is mutated.
	    return textarea(props, this.state.initialValue);
	  },

	  componentDidUpdate: function(prevProps, prevState, prevContext) {
	    var value = LinkedValueUtils.getValue(this);
	    if (value != null) {
	      var rootNode = this.getDOMNode();
	      // Cast `value` to a string to ensure the value is set correctly. While
	      // browsers typically do this as necessary, jsdom doesn't.
	      DOMPropertyOperations.setValueForProperty(rootNode, 'value', '' + value);
	    }
	  },

	  _handleChange: function(event) {
	    var returnValue;
	    var onChange = LinkedValueUtils.getOnChange(this);
	    if (onChange) {
	      returnValue = onChange.call(this, event);
	    }
	    ReactUpdates.asap(forceUpdateIfMounted, this);
	    return returnValue;
	  }

	});

	module.exports = ReactDOMTextarea;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 127 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactEventListener
	 * @typechecks static-only
	 */

	'use strict';

	var EventListener = __webpack_require__(128);
	var ExecutionEnvironment = __webpack_require__(52);
	var PooledClass = __webpack_require__(10);
	var ReactInstanceHandles = __webpack_require__(21);
	var ReactMount = __webpack_require__(68);
	var ReactUpdates = __webpack_require__(25);

	var assign = __webpack_require__(14);
	var getEventTarget = __webpack_require__(99);
	var getUnboundedScrollPosition = __webpack_require__(129);

	/**
	 * Finds the parent React component of `node`.
	 *
	 * @param {*} node
	 * @return {?DOMEventTarget} Parent container, or `null` if the specified node
	 *                           is not nested.
	 */
	function findParent(node) {
	  // TODO: It may be a good idea to cache this to prevent unnecessary DOM
	  // traversal, but caching is difficult to do correctly without using a
	  // mutation observer to listen for all DOM changes.
	  var nodeID = ReactMount.getID(node);
	  var rootID = ReactInstanceHandles.getReactRootIDFromNodeID(nodeID);
	  var container = ReactMount.findReactContainerForID(rootID);
	  var parent = ReactMount.getFirstReactDOM(container);
	  return parent;
	}

	// Used to store ancestor hierarchy in top level callback
	function TopLevelCallbackBookKeeping(topLevelType, nativeEvent) {
	  this.topLevelType = topLevelType;
	  this.nativeEvent = nativeEvent;
	  this.ancestors = [];
	}
	assign(TopLevelCallbackBookKeeping.prototype, {
	  destructor: function() {
	    this.topLevelType = null;
	    this.nativeEvent = null;
	    this.ancestors.length = 0;
	  }
	});
	PooledClass.addPoolingTo(
	  TopLevelCallbackBookKeeping,
	  PooledClass.twoArgumentPooler
	);

	function handleTopLevelImpl(bookKeeping) {
	  var topLevelTarget = ReactMount.getFirstReactDOM(
	    getEventTarget(bookKeeping.nativeEvent)
	  ) || window;

	  // Loop through the hierarchy, in case there's any nested components.
	  // It's important that we build the array of ancestors before calling any
	  // event handlers, because event handlers can modify the DOM, leading to
	  // inconsistencies with ReactMount's node cache. See #1105.
	  var ancestor = topLevelTarget;
	  while (ancestor) {
	    bookKeeping.ancestors.push(ancestor);
	    ancestor = findParent(ancestor);
	  }

	  for (var i = 0, l = bookKeeping.ancestors.length; i < l; i++) {
	    topLevelTarget = bookKeeping.ancestors[i];
	    var topLevelTargetID = ReactMount.getID(topLevelTarget) || '';
	    ReactEventListener._handleTopLevel(
	      bookKeeping.topLevelType,
	      topLevelTarget,
	      topLevelTargetID,
	      bookKeeping.nativeEvent
	    );
	  }
	}

	function scrollValueMonitor(cb) {
	  var scrollPosition = getUnboundedScrollPosition(window);
	  cb(scrollPosition);
	}

	var ReactEventListener = {
	  _enabled: true,
	  _handleTopLevel: null,

	  WINDOW_HANDLE: ExecutionEnvironment.canUseDOM ? window : null,

	  setHandleTopLevel: function(handleTopLevel) {
	    ReactEventListener._handleTopLevel = handleTopLevel;
	  },

	  setEnabled: function(enabled) {
	    ReactEventListener._enabled = !!enabled;
	  },

	  isEnabled: function() {
	    return ReactEventListener._enabled;
	  },


	  /**
	   * Traps top-level events by using event bubbling.
	   *
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {string} handlerBaseName Event name (e.g. "click").
	   * @param {object} handle Element on which to attach listener.
	   * @return {object} An object with a remove function which will forcefully
	   *                  remove the listener.
	   * @internal
	   */
	  trapBubbledEvent: function(topLevelType, handlerBaseName, handle) {
	    var element = handle;
	    if (!element) {
	      return null;
	    }
	    return EventListener.listen(
	      element,
	      handlerBaseName,
	      ReactEventListener.dispatchEvent.bind(null, topLevelType)
	    );
	  },

	  /**
	   * Traps a top-level event by using event capturing.
	   *
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {string} handlerBaseName Event name (e.g. "click").
	   * @param {object} handle Element on which to attach listener.
	   * @return {object} An object with a remove function which will forcefully
	   *                  remove the listener.
	   * @internal
	   */
	  trapCapturedEvent: function(topLevelType, handlerBaseName, handle) {
	    var element = handle;
	    if (!element) {
	      return null;
	    }
	    return EventListener.capture(
	      element,
	      handlerBaseName,
	      ReactEventListener.dispatchEvent.bind(null, topLevelType)
	    );
	  },

	  monitorScrollValue: function(refresh) {
	    var callback = scrollValueMonitor.bind(null, refresh);
	    EventListener.listen(window, 'scroll', callback);
	  },

	  dispatchEvent: function(topLevelType, nativeEvent) {
	    if (!ReactEventListener._enabled) {
	      return;
	    }

	    var bookKeeping = TopLevelCallbackBookKeeping.getPooled(
	      topLevelType,
	      nativeEvent
	    );
	    try {
	      // Event queue being processed in the same cycle allows
	      // `preventDefault`.
	      ReactUpdates.batchedUpdates(handleTopLevelImpl, bookKeeping);
	    } finally {
	      TopLevelCallbackBookKeeping.release(bookKeeping);
	    }
	  }
	};

	module.exports = ReactEventListener;


/***/ },
/* 128 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 *
	 * Licensed under the Apache License, Version 2.0 (the "License");
	 * you may not use this file except in compliance with the License.
	 * You may obtain a copy of the License at
	 *
	 * http://www.apache.org/licenses/LICENSE-2.0
	 *
	 * Unless required by applicable law or agreed to in writing, software
	 * distributed under the License is distributed on an "AS IS" BASIS,
	 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
	 * See the License for the specific language governing permissions and
	 * limitations under the License.
	 *
	 * @providesModule EventListener
	 * @typechecks
	 */

	var emptyFunction = __webpack_require__(17);

	/**
	 * Upstream version of event listener. Does not take into account specific
	 * nature of platform.
	 */
	var EventListener = {
	  /**
	   * Listen to DOM events during the bubble phase.
	   *
	   * @param {DOMEventTarget} target DOM element to register listener on.
	   * @param {string} eventType Event type, e.g. 'click' or 'mouseover'.
	   * @param {function} callback Callback function.
	   * @return {object} Object with a `remove` method.
	   */
	  listen: function(target, eventType, callback) {
	    if (target.addEventListener) {
	      target.addEventListener(eventType, callback, false);
	      return {
	        remove: function() {
	          target.removeEventListener(eventType, callback, false);
	        }
	      };
	    } else if (target.attachEvent) {
	      target.attachEvent('on' + eventType, callback);
	      return {
	        remove: function() {
	          target.detachEvent('on' + eventType, callback);
	        }
	      };
	    }
	  },

	  /**
	   * Listen to DOM events during the capture phase.
	   *
	   * @param {DOMEventTarget} target DOM element to register listener on.
	   * @param {string} eventType Event type, e.g. 'click' or 'mouseover'.
	   * @param {function} callback Callback function.
	   * @return {object} Object with a `remove` method.
	   */
	  capture: function(target, eventType, callback) {
	    if (!target.addEventListener) {
	      if ("production" !== process.env.NODE_ENV) {
	        console.error(
	          'Attempted to listen to events during the capture phase on a ' +
	          'browser that does not support the capture phase. Your application ' +
	          'will not receive some events.'
	        );
	      }
	      return {
	        remove: emptyFunction
	      };
	    } else {
	      target.addEventListener(eventType, callback, true);
	      return {
	        remove: function() {
	          target.removeEventListener(eventType, callback, true);
	        }
	      };
	    }
	  },

	  registerDefault: function() {}
	};

	module.exports = EventListener;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 129 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getUnboundedScrollPosition
	 * @typechecks
	 */

	"use strict";

	/**
	 * Gets the scroll position of the supplied element or window.
	 *
	 * The return values are unbounded, unlike `getScrollPosition`. This means they
	 * may be negative or exceed the element boundaries (which is possible using
	 * inertial scrolling).
	 *
	 * @param {DOMWindow|DOMElement} scrollable
	 * @return {object} Map with `x` and `y` keys.
	 */
	function getUnboundedScrollPosition(scrollable) {
	  if (scrollable === window) {
	    return {
	      x: window.pageXOffset || document.documentElement.scrollLeft,
	      y: window.pageYOffset || document.documentElement.scrollTop
	    };
	  }
	  return {
	    x: scrollable.scrollLeft,
	    y: scrollable.scrollTop
	  };
	}

	module.exports = getUnboundedScrollPosition;


/***/ },
/* 130 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactInjection
	 */

	'use strict';

	var DOMProperty = __webpack_require__(45);
	var EventPluginHub = __webpack_require__(70);
	var ReactComponentEnvironment = __webpack_require__(86);
	var ReactClass = __webpack_require__(38);
	var ReactEmptyComponent = __webpack_require__(77);
	var ReactBrowserEventEmitter = __webpack_require__(69);
	var ReactNativeComponent = __webpack_require__(34);
	var ReactDOMComponent = __webpack_require__(88);
	var ReactPerf = __webpack_require__(27);
	var ReactRootIndex = __webpack_require__(22);
	var ReactUpdates = __webpack_require__(25);

	var ReactInjection = {
	  Component: ReactComponentEnvironment.injection,
	  Class: ReactClass.injection,
	  DOMComponent: ReactDOMComponent.injection,
	  DOMProperty: DOMProperty.injection,
	  EmptyComponent: ReactEmptyComponent.injection,
	  EventPluginHub: EventPluginHub.injection,
	  EventEmitter: ReactBrowserEventEmitter.injection,
	  NativeComponent: ReactNativeComponent.injection,
	  Perf: ReactPerf.injection,
	  RootIndex: ReactRootIndex.injection,
	  Updates: ReactUpdates.injection
	};

	module.exports = ReactInjection;


/***/ },
/* 131 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactReconcileTransaction
	 * @typechecks static-only
	 */

	'use strict';

	var CallbackQueue = __webpack_require__(26);
	var PooledClass = __webpack_require__(10);
	var ReactBrowserEventEmitter = __webpack_require__(69);
	var ReactInputSelection = __webpack_require__(132);
	var ReactPutListenerQueue = __webpack_require__(136);
	var Transaction = __webpack_require__(35);

	var assign = __webpack_require__(14);

	/**
	 * Ensures that, when possible, the selection range (currently selected text
	 * input) is not disturbed by performing the transaction.
	 */
	var SELECTION_RESTORATION = {
	  /**
	   * @return {Selection} Selection information.
	   */
	  initialize: ReactInputSelection.getSelectionInformation,
	  /**
	   * @param {Selection} sel Selection information returned from `initialize`.
	   */
	  close: ReactInputSelection.restoreSelection
	};

	/**
	 * Suppresses events (blur/focus) that could be inadvertently dispatched due to
	 * high level DOM manipulations (like temporarily removing a text input from the
	 * DOM).
	 */
	var EVENT_SUPPRESSION = {
	  /**
	   * @return {boolean} The enabled status of `ReactBrowserEventEmitter` before
	   * the reconciliation.
	   */
	  initialize: function() {
	    var currentlyEnabled = ReactBrowserEventEmitter.isEnabled();
	    ReactBrowserEventEmitter.setEnabled(false);
	    return currentlyEnabled;
	  },

	  /**
	   * @param {boolean} previouslyEnabled Enabled status of
	   *   `ReactBrowserEventEmitter` before the reconciliation occured. `close`
	   *   restores the previous value.
	   */
	  close: function(previouslyEnabled) {
	    ReactBrowserEventEmitter.setEnabled(previouslyEnabled);
	  }
	};

	/**
	 * Provides a queue for collecting `componentDidMount` and
	 * `componentDidUpdate` callbacks during the the transaction.
	 */
	var ON_DOM_READY_QUEUEING = {
	  /**
	   * Initializes the internal `onDOMReady` queue.
	   */
	  initialize: function() {
	    this.reactMountReady.reset();
	  },

	  /**
	   * After DOM is flushed, invoke all registered `onDOMReady` callbacks.
	   */
	  close: function() {
	    this.reactMountReady.notifyAll();
	  }
	};

	var PUT_LISTENER_QUEUEING = {
	  initialize: function() {
	    this.putListenerQueue.reset();
	  },

	  close: function() {
	    this.putListenerQueue.putListeners();
	  }
	};

	/**
	 * Executed within the scope of the `Transaction` instance. Consider these as
	 * being member methods, but with an implied ordering while being isolated from
	 * each other.
	 */
	var TRANSACTION_WRAPPERS = [
	  PUT_LISTENER_QUEUEING,
	  SELECTION_RESTORATION,
	  EVENT_SUPPRESSION,
	  ON_DOM_READY_QUEUEING
	];

	/**
	 * Currently:
	 * - The order that these are listed in the transaction is critical:
	 * - Suppresses events.
	 * - Restores selection range.
	 *
	 * Future:
	 * - Restore document/overflow scroll positions that were unintentionally
	 *   modified via DOM insertions above the top viewport boundary.
	 * - Implement/integrate with customized constraint based layout system and keep
	 *   track of which dimensions must be remeasured.
	 *
	 * @class ReactReconcileTransaction
	 */
	function ReactReconcileTransaction() {
	  this.reinitializeTransaction();
	  // Only server-side rendering really needs this option (see
	  // `ReactServerRendering`), but server-side uses
	  // `ReactServerRenderingTransaction` instead. This option is here so that it's
	  // accessible and defaults to false when `ReactDOMComponent` and
	  // `ReactTextComponent` checks it in `mountComponent`.`
	  this.renderToStaticMarkup = false;
	  this.reactMountReady = CallbackQueue.getPooled(null);
	  this.putListenerQueue = ReactPutListenerQueue.getPooled();
	}

	var Mixin = {
	  /**
	   * @see Transaction
	   * @abstract
	   * @final
	   * @return {array<object>} List of operation wrap proceedures.
	   *   TODO: convert to array<TransactionWrapper>
	   */
	  getTransactionWrappers: function() {
	    return TRANSACTION_WRAPPERS;
	  },

	  /**
	   * @return {object} The queue to collect `onDOMReady` callbacks with.
	   */
	  getReactMountReady: function() {
	    return this.reactMountReady;
	  },

	  getPutListenerQueue: function() {
	    return this.putListenerQueue;
	  },

	  /**
	   * `PooledClass` looks for this, and will invoke this before allowing this
	   * instance to be resused.
	   */
	  destructor: function() {
	    CallbackQueue.release(this.reactMountReady);
	    this.reactMountReady = null;

	    ReactPutListenerQueue.release(this.putListenerQueue);
	    this.putListenerQueue = null;
	  }
	};


	assign(ReactReconcileTransaction.prototype, Transaction.Mixin, Mixin);

	PooledClass.addPoolingTo(ReactReconcileTransaction);

	module.exports = ReactReconcileTransaction;


/***/ },
/* 132 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactInputSelection
	 */

	'use strict';

	var ReactDOMSelection = __webpack_require__(133);

	var containsNode = __webpack_require__(80);
	var focusNode = __webpack_require__(116);
	var getActiveElement = __webpack_require__(135);

	function isInDocument(node) {
	  return containsNode(document.documentElement, node);
	}

	/**
	 * @ReactInputSelection: React input selection module. Based on Selection.js,
	 * but modified to be suitable for react and has a couple of bug fixes (doesn't
	 * assume buttons have range selections allowed).
	 * Input selection module for React.
	 */
	var ReactInputSelection = {

	  hasSelectionCapabilities: function(elem) {
	    return elem && (
	      ((elem.nodeName === 'INPUT' && elem.type === 'text') ||
	      elem.nodeName === 'TEXTAREA' || elem.contentEditable === 'true')
	    );
	  },

	  getSelectionInformation: function() {
	    var focusedElem = getActiveElement();
	    return {
	      focusedElem: focusedElem,
	      selectionRange:
	          ReactInputSelection.hasSelectionCapabilities(focusedElem) ?
	          ReactInputSelection.getSelection(focusedElem) :
	          null
	    };
	  },

	  /**
	   * @restoreSelection: If any selection information was potentially lost,
	   * restore it. This is useful when performing operations that could remove dom
	   * nodes and place them back in, resulting in focus being lost.
	   */
	  restoreSelection: function(priorSelectionInformation) {
	    var curFocusedElem = getActiveElement();
	    var priorFocusedElem = priorSelectionInformation.focusedElem;
	    var priorSelectionRange = priorSelectionInformation.selectionRange;
	    if (curFocusedElem !== priorFocusedElem &&
	        isInDocument(priorFocusedElem)) {
	      if (ReactInputSelection.hasSelectionCapabilities(priorFocusedElem)) {
	        ReactInputSelection.setSelection(
	          priorFocusedElem,
	          priorSelectionRange
	        );
	      }
	      focusNode(priorFocusedElem);
	    }
	  },

	  /**
	   * @getSelection: Gets the selection bounds of a focused textarea, input or
	   * contentEditable node.
	   * -@input: Look up selection bounds of this input
	   * -@return {start: selectionStart, end: selectionEnd}
	   */
	  getSelection: function(input) {
	    var selection;

	    if ('selectionStart' in input) {
	      // Modern browser with input or textarea.
	      selection = {
	        start: input.selectionStart,
	        end: input.selectionEnd
	      };
	    } else if (document.selection && input.nodeName === 'INPUT') {
	      // IE8 input.
	      var range = document.selection.createRange();
	      // There can only be one selection per document in IE, so it must
	      // be in our element.
	      if (range.parentElement() === input) {
	        selection = {
	          start: -range.moveStart('character', -input.value.length),
	          end: -range.moveEnd('character', -input.value.length)
	        };
	      }
	    } else {
	      // Content editable or old IE textarea.
	      selection = ReactDOMSelection.getOffsets(input);
	    }

	    return selection || {start: 0, end: 0};
	  },

	  /**
	   * @setSelection: Sets the selection bounds of a textarea or input and focuses
	   * the input.
	   * -@input     Set selection bounds of this input or textarea
	   * -@offsets   Object of same form that is returned from get*
	   */
	  setSelection: function(input, offsets) {
	    var start = offsets.start;
	    var end = offsets.end;
	    if (typeof end === 'undefined') {
	      end = start;
	    }

	    if ('selectionStart' in input) {
	      input.selectionStart = start;
	      input.selectionEnd = Math.min(end, input.value.length);
	    } else if (document.selection && input.nodeName === 'INPUT') {
	      var range = input.createTextRange();
	      range.collapse(true);
	      range.moveStart('character', start);
	      range.moveEnd('character', end - start);
	      range.select();
	    } else {
	      ReactDOMSelection.setOffsets(input, offsets);
	    }
	  }
	};

	module.exports = ReactInputSelection;


/***/ },
/* 133 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDOMSelection
	 */

	'use strict';

	var ExecutionEnvironment = __webpack_require__(52);

	var getNodeForCharacterOffset = __webpack_require__(134);
	var getTextContentAccessor = __webpack_require__(96);

	/**
	 * While `isCollapsed` is available on the Selection object and `collapsed`
	 * is available on the Range object, IE11 sometimes gets them wrong.
	 * If the anchor/focus nodes and offsets are the same, the range is collapsed.
	 */
	function isCollapsed(anchorNode, anchorOffset, focusNode, focusOffset) {
	  return anchorNode === focusNode && anchorOffset === focusOffset;
	}

	/**
	 * Get the appropriate anchor and focus node/offset pairs for IE.
	 *
	 * The catch here is that IE's selection API doesn't provide information
	 * about whether the selection is forward or backward, so we have to
	 * behave as though it's always forward.
	 *
	 * IE text differs from modern selection in that it behaves as though
	 * block elements end with a new line. This means character offsets will
	 * differ between the two APIs.
	 *
	 * @param {DOMElement} node
	 * @return {object}
	 */
	function getIEOffsets(node) {
	  var selection = document.selection;
	  var selectedRange = selection.createRange();
	  var selectedLength = selectedRange.text.length;

	  // Duplicate selection so we can move range without breaking user selection.
	  var fromStart = selectedRange.duplicate();
	  fromStart.moveToElementText(node);
	  fromStart.setEndPoint('EndToStart', selectedRange);

	  var startOffset = fromStart.text.length;
	  var endOffset = startOffset + selectedLength;

	  return {
	    start: startOffset,
	    end: endOffset
	  };
	}

	/**
	 * @param {DOMElement} node
	 * @return {?object}
	 */
	function getModernOffsets(node) {
	  var selection = window.getSelection && window.getSelection();

	  if (!selection || selection.rangeCount === 0) {
	    return null;
	  }

	  var anchorNode = selection.anchorNode;
	  var anchorOffset = selection.anchorOffset;
	  var focusNode = selection.focusNode;
	  var focusOffset = selection.focusOffset;

	  var currentRange = selection.getRangeAt(0);

	  // If the node and offset values are the same, the selection is collapsed.
	  // `Selection.isCollapsed` is available natively, but IE sometimes gets
	  // this value wrong.
	  var isSelectionCollapsed = isCollapsed(
	    selection.anchorNode,
	    selection.anchorOffset,
	    selection.focusNode,
	    selection.focusOffset
	  );

	  var rangeLength = isSelectionCollapsed ? 0 : currentRange.toString().length;

	  var tempRange = currentRange.cloneRange();
	  tempRange.selectNodeContents(node);
	  tempRange.setEnd(currentRange.startContainer, currentRange.startOffset);

	  var isTempRangeCollapsed = isCollapsed(
	    tempRange.startContainer,
	    tempRange.startOffset,
	    tempRange.endContainer,
	    tempRange.endOffset
	  );

	  var start = isTempRangeCollapsed ? 0 : tempRange.toString().length;
	  var end = start + rangeLength;

	  // Detect whether the selection is backward.
	  var detectionRange = document.createRange();
	  detectionRange.setStart(anchorNode, anchorOffset);
	  detectionRange.setEnd(focusNode, focusOffset);
	  var isBackward = detectionRange.collapsed;

	  return {
	    start: isBackward ? end : start,
	    end: isBackward ? start : end
	  };
	}

	/**
	 * @param {DOMElement|DOMTextNode} node
	 * @param {object} offsets
	 */
	function setIEOffsets(node, offsets) {
	  var range = document.selection.createRange().duplicate();
	  var start, end;

	  if (typeof offsets.end === 'undefined') {
	    start = offsets.start;
	    end = start;
	  } else if (offsets.start > offsets.end) {
	    start = offsets.end;
	    end = offsets.start;
	  } else {
	    start = offsets.start;
	    end = offsets.end;
	  }

	  range.moveToElementText(node);
	  range.moveStart('character', start);
	  range.setEndPoint('EndToStart', range);
	  range.moveEnd('character', end - start);
	  range.select();
	}

	/**
	 * In modern non-IE browsers, we can support both forward and backward
	 * selections.
	 *
	 * Note: IE10+ supports the Selection object, but it does not support
	 * the `extend` method, which means that even in modern IE, it's not possible
	 * to programatically create a backward selection. Thus, for all IE
	 * versions, we use the old IE API to create our selections.
	 *
	 * @param {DOMElement|DOMTextNode} node
	 * @param {object} offsets
	 */
	function setModernOffsets(node, offsets) {
	  if (!window.getSelection) {
	    return;
	  }

	  var selection = window.getSelection();
	  var length = node[getTextContentAccessor()].length;
	  var start = Math.min(offsets.start, length);
	  var end = typeof offsets.end === 'undefined' ?
	            start : Math.min(offsets.end, length);

	  // IE 11 uses modern selection, but doesn't support the extend method.
	  // Flip backward selections, so we can set with a single range.
	  if (!selection.extend && start > end) {
	    var temp = end;
	    end = start;
	    start = temp;
	  }

	  var startMarker = getNodeForCharacterOffset(node, start);
	  var endMarker = getNodeForCharacterOffset(node, end);

	  if (startMarker && endMarker) {
	    var range = document.createRange();
	    range.setStart(startMarker.node, startMarker.offset);
	    selection.removeAllRanges();

	    if (start > end) {
	      selection.addRange(range);
	      selection.extend(endMarker.node, endMarker.offset);
	    } else {
	      range.setEnd(endMarker.node, endMarker.offset);
	      selection.addRange(range);
	    }
	  }
	}

	var useIEOffsets = (
	  ExecutionEnvironment.canUseDOM &&
	  'selection' in document &&
	  !('getSelection' in window)
	);

	var ReactDOMSelection = {
	  /**
	   * @param {DOMElement} node
	   */
	  getOffsets: useIEOffsets ? getIEOffsets : getModernOffsets,

	  /**
	   * @param {DOMElement|DOMTextNode} node
	   * @param {object} offsets
	   */
	  setOffsets: useIEOffsets ? setIEOffsets : setModernOffsets
	};

	module.exports = ReactDOMSelection;


/***/ },
/* 134 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getNodeForCharacterOffset
	 */

	'use strict';

	/**
	 * Given any node return the first leaf node without children.
	 *
	 * @param {DOMElement|DOMTextNode} node
	 * @return {DOMElement|DOMTextNode}
	 */
	function getLeafNode(node) {
	  while (node && node.firstChild) {
	    node = node.firstChild;
	  }
	  return node;
	}

	/**
	 * Get the next sibling within a container. This will walk up the
	 * DOM if a node's siblings have been exhausted.
	 *
	 * @param {DOMElement|DOMTextNode} node
	 * @return {?DOMElement|DOMTextNode}
	 */
	function getSiblingNode(node) {
	  while (node) {
	    if (node.nextSibling) {
	      return node.nextSibling;
	    }
	    node = node.parentNode;
	  }
	}

	/**
	 * Get object describing the nodes which contain characters at offset.
	 *
	 * @param {DOMElement|DOMTextNode} root
	 * @param {number} offset
	 * @return {?object}
	 */
	function getNodeForCharacterOffset(root, offset) {
	  var node = getLeafNode(root);
	  var nodeStart = 0;
	  var nodeEnd = 0;

	  while (node) {
	    if (node.nodeType === 3) {
	      nodeEnd = nodeStart + node.textContent.length;

	      if (nodeStart <= offset && nodeEnd >= offset) {
	        return {
	          node: node,
	          offset: offset - nodeStart
	        };
	      }

	      nodeStart = nodeEnd;
	    }

	    node = getLeafNode(getSiblingNode(node));
	  }
	}

	module.exports = getNodeForCharacterOffset;


/***/ },
/* 135 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getActiveElement
	 * @typechecks
	 */

	/**
	 * Same as document.activeElement but wraps in a try-catch block. In IE it is
	 * not safe to call document.activeElement if there is nothing focused.
	 *
	 * The activeElement will be null only if the document body is not yet defined.
	 */
	function getActiveElement() /*?DOMElement*/ {
	  try {
	    return document.activeElement || document.body;
	  } catch (e) {
	    return document.body;
	  }
	}

	module.exports = getActiveElement;


/***/ },
/* 136 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactPutListenerQueue
	 */

	'use strict';

	var PooledClass = __webpack_require__(10);
	var ReactBrowserEventEmitter = __webpack_require__(69);

	var assign = __webpack_require__(14);

	function ReactPutListenerQueue() {
	  this.listenersToPut = [];
	}

	assign(ReactPutListenerQueue.prototype, {
	  enqueuePutListener: function(rootNodeID, propKey, propValue) {
	    this.listenersToPut.push({
	      rootNodeID: rootNodeID,
	      propKey: propKey,
	      propValue: propValue
	    });
	  },

	  putListeners: function() {
	    for (var i = 0; i < this.listenersToPut.length; i++) {
	      var listenerToPut = this.listenersToPut[i];
	      ReactBrowserEventEmitter.putListener(
	        listenerToPut.rootNodeID,
	        listenerToPut.propKey,
	        listenerToPut.propValue
	      );
	    }
	  },

	  reset: function() {
	    this.listenersToPut.length = 0;
	  },

	  destructor: function() {
	    this.reset();
	  }
	});

	PooledClass.addPoolingTo(ReactPutListenerQueue);

	module.exports = ReactPutListenerQueue;


/***/ },
/* 137 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SelectEventPlugin
	 */

	'use strict';

	var EventConstants = __webpack_require__(6);
	var EventPropagators = __webpack_require__(94);
	var ReactInputSelection = __webpack_require__(132);
	var SyntheticEvent = __webpack_require__(98);

	var getActiveElement = __webpack_require__(135);
	var isTextInputElement = __webpack_require__(102);
	var keyOf = __webpack_require__(40);
	var shallowEqual = __webpack_require__(138);

	var topLevelTypes = EventConstants.topLevelTypes;

	var eventTypes = {
	  select: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onSelect: null}),
	      captured: keyOf({onSelectCapture: null})
	    },
	    dependencies: [
	      topLevelTypes.topBlur,
	      topLevelTypes.topContextMenu,
	      topLevelTypes.topFocus,
	      topLevelTypes.topKeyDown,
	      topLevelTypes.topMouseDown,
	      topLevelTypes.topMouseUp,
	      topLevelTypes.topSelectionChange
	    ]
	  }
	};

	var activeElement = null;
	var activeElementID = null;
	var lastSelection = null;
	var mouseDown = false;

	/**
	 * Get an object which is a unique representation of the current selection.
	 *
	 * The return value will not be consistent across nodes or browsers, but
	 * two identical selections on the same node will return identical objects.
	 *
	 * @param {DOMElement} node
	 * @param {object}
	 */
	function getSelection(node) {
	  if ('selectionStart' in node &&
	      ReactInputSelection.hasSelectionCapabilities(node)) {
	    return {
	      start: node.selectionStart,
	      end: node.selectionEnd
	    };
	  } else if (window.getSelection) {
	    var selection = window.getSelection();
	    return {
	      anchorNode: selection.anchorNode,
	      anchorOffset: selection.anchorOffset,
	      focusNode: selection.focusNode,
	      focusOffset: selection.focusOffset
	    };
	  } else if (document.selection) {
	    var range = document.selection.createRange();
	    return {
	      parentElement: range.parentElement(),
	      text: range.text,
	      top: range.boundingTop,
	      left: range.boundingLeft
	    };
	  }
	}

	/**
	 * Poll selection to see whether it's changed.
	 *
	 * @param {object} nativeEvent
	 * @return {?SyntheticEvent}
	 */
	function constructSelectEvent(nativeEvent) {
	  // Ensure we have the right element, and that the user is not dragging a
	  // selection (this matches native `select` event behavior). In HTML5, select
	  // fires only on input and textarea thus if there's no focused element we
	  // won't dispatch.
	  if (mouseDown ||
	      activeElement == null ||
	      activeElement !== getActiveElement()) {
	    return null;
	  }

	  // Only fire when selection has actually changed.
	  var currentSelection = getSelection(activeElement);
	  if (!lastSelection || !shallowEqual(lastSelection, currentSelection)) {
	    lastSelection = currentSelection;

	    var syntheticEvent = SyntheticEvent.getPooled(
	      eventTypes.select,
	      activeElementID,
	      nativeEvent
	    );

	    syntheticEvent.type = 'select';
	    syntheticEvent.target = activeElement;

	    EventPropagators.accumulateTwoPhaseDispatches(syntheticEvent);

	    return syntheticEvent;
	  }
	}

	/**
	 * This plugin creates an `onSelect` event that normalizes select events
	 * across form elements.
	 *
	 * Supported elements are:
	 * - input (see `isTextInputElement`)
	 * - textarea
	 * - contentEditable
	 *
	 * This differs from native browser implementations in the following ways:
	 * - Fires on contentEditable fields as well as inputs.
	 * - Fires for collapsed selection.
	 * - Fires after user input.
	 */
	var SelectEventPlugin = {

	  eventTypes: eventTypes,

	  /**
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @see {EventPluginHub.extractEvents}
	   */
	  extractEvents: function(
	      topLevelType,
	      topLevelTarget,
	      topLevelTargetID,
	      nativeEvent) {

	    switch (topLevelType) {
	      // Track the input node that has focus.
	      case topLevelTypes.topFocus:
	        if (isTextInputElement(topLevelTarget) ||
	            topLevelTarget.contentEditable === 'true') {
	          activeElement = topLevelTarget;
	          activeElementID = topLevelTargetID;
	          lastSelection = null;
	        }
	        break;
	      case topLevelTypes.topBlur:
	        activeElement = null;
	        activeElementID = null;
	        lastSelection = null;
	        break;

	      // Don't fire the event while the user is dragging. This matches the
	      // semantics of the native select event.
	      case topLevelTypes.topMouseDown:
	        mouseDown = true;
	        break;
	      case topLevelTypes.topContextMenu:
	      case topLevelTypes.topMouseUp:
	        mouseDown = false;
	        return constructSelectEvent(nativeEvent);

	      // Chrome and IE fire non-standard event when selection is changed (and
	      // sometimes when it hasn't).
	      // Firefox doesn't support selectionchange, so check selection status
	      // after each key entry. The selection changes after keydown and before
	      // keyup, but we check on keydown as well in the case of holding down a
	      // key, when multiple keydown events are fired but only one keyup is.
	      case topLevelTypes.topSelectionChange:
	      case topLevelTypes.topKeyDown:
	      case topLevelTypes.topKeyUp:
	        return constructSelectEvent(nativeEvent);
	    }
	  }
	};

	module.exports = SelectEventPlugin;


/***/ },
/* 138 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule shallowEqual
	 */

	'use strict';

	/**
	 * Performs equality by iterating through keys on an object and returning
	 * false when any key has values which are not strictly equal between
	 * objA and objB. Returns true when the values of all keys are strictly equal.
	 *
	 * @return {boolean}
	 */
	function shallowEqual(objA, objB) {
	  if (objA === objB) {
	    return true;
	  }
	  var key;
	  // Test for A's keys different from B.
	  for (key in objA) {
	    if (objA.hasOwnProperty(key) &&
	        (!objB.hasOwnProperty(key) || objA[key] !== objB[key])) {
	      return false;
	    }
	  }
	  // Test for B's keys missing from A.
	  for (key in objB) {
	    if (objB.hasOwnProperty(key) && !objA.hasOwnProperty(key)) {
	      return false;
	    }
	  }
	  return true;
	}

	module.exports = shallowEqual;


/***/ },
/* 139 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ServerReactRootIndex
	 * @typechecks
	 */

	'use strict';

	/**
	 * Size of the reactRoot ID space. We generate random numbers for React root
	 * IDs and if there's a collision the events and DOM update system will
	 * get confused. In the future we need a way to generate GUIDs but for
	 * now this will work on a smaller scale.
	 */
	var GLOBAL_MOUNT_POINT_MAX = Math.pow(2, 53);

	var ServerReactRootIndex = {
	  createReactRootIndex: function() {
	    return Math.ceil(Math.random() * GLOBAL_MOUNT_POINT_MAX);
	  }
	};

	module.exports = ServerReactRootIndex;


/***/ },
/* 140 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SimpleEventPlugin
	 */

	'use strict';

	var EventConstants = __webpack_require__(6);
	var EventPluginUtils = __webpack_require__(5);
	var EventPropagators = __webpack_require__(94);
	var SyntheticClipboardEvent = __webpack_require__(142);
	var SyntheticEvent = __webpack_require__(98);
	var SyntheticFocusEvent = __webpack_require__(143);
	var SyntheticKeyboardEvent = __webpack_require__(144);
	var SyntheticMouseEvent = __webpack_require__(106);
	var SyntheticDragEvent = __webpack_require__(141);
	var SyntheticTouchEvent = __webpack_require__(147);
	var SyntheticUIEvent = __webpack_require__(107);
	var SyntheticWheelEvent = __webpack_require__(148);

	var getEventCharCode = __webpack_require__(145);

	var invariant = __webpack_require__(8);
	var keyOf = __webpack_require__(40);
	var warning = __webpack_require__(16);

	var topLevelTypes = EventConstants.topLevelTypes;

	var eventTypes = {
	  blur: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onBlur: true}),
	      captured: keyOf({onBlurCapture: true})
	    }
	  },
	  click: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onClick: true}),
	      captured: keyOf({onClickCapture: true})
	    }
	  },
	  contextMenu: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onContextMenu: true}),
	      captured: keyOf({onContextMenuCapture: true})
	    }
	  },
	  copy: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onCopy: true}),
	      captured: keyOf({onCopyCapture: true})
	    }
	  },
	  cut: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onCut: true}),
	      captured: keyOf({onCutCapture: true})
	    }
	  },
	  doubleClick: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onDoubleClick: true}),
	      captured: keyOf({onDoubleClickCapture: true})
	    }
	  },
	  drag: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onDrag: true}),
	      captured: keyOf({onDragCapture: true})
	    }
	  },
	  dragEnd: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onDragEnd: true}),
	      captured: keyOf({onDragEndCapture: true})
	    }
	  },
	  dragEnter: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onDragEnter: true}),
	      captured: keyOf({onDragEnterCapture: true})
	    }
	  },
	  dragExit: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onDragExit: true}),
	      captured: keyOf({onDragExitCapture: true})
	    }
	  },
	  dragLeave: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onDragLeave: true}),
	      captured: keyOf({onDragLeaveCapture: true})
	    }
	  },
	  dragOver: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onDragOver: true}),
	      captured: keyOf({onDragOverCapture: true})
	    }
	  },
	  dragStart: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onDragStart: true}),
	      captured: keyOf({onDragStartCapture: true})
	    }
	  },
	  drop: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onDrop: true}),
	      captured: keyOf({onDropCapture: true})
	    }
	  },
	  focus: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onFocus: true}),
	      captured: keyOf({onFocusCapture: true})
	    }
	  },
	  input: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onInput: true}),
	      captured: keyOf({onInputCapture: true})
	    }
	  },
	  keyDown: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onKeyDown: true}),
	      captured: keyOf({onKeyDownCapture: true})
	    }
	  },
	  keyPress: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onKeyPress: true}),
	      captured: keyOf({onKeyPressCapture: true})
	    }
	  },
	  keyUp: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onKeyUp: true}),
	      captured: keyOf({onKeyUpCapture: true})
	    }
	  },
	  load: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onLoad: true}),
	      captured: keyOf({onLoadCapture: true})
	    }
	  },
	  error: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onError: true}),
	      captured: keyOf({onErrorCapture: true})
	    }
	  },
	  // Note: We do not allow listening to mouseOver events. Instead, use the
	  // onMouseEnter/onMouseLeave created by `EnterLeaveEventPlugin`.
	  mouseDown: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onMouseDown: true}),
	      captured: keyOf({onMouseDownCapture: true})
	    }
	  },
	  mouseMove: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onMouseMove: true}),
	      captured: keyOf({onMouseMoveCapture: true})
	    }
	  },
	  mouseOut: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onMouseOut: true}),
	      captured: keyOf({onMouseOutCapture: true})
	    }
	  },
	  mouseOver: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onMouseOver: true}),
	      captured: keyOf({onMouseOverCapture: true})
	    }
	  },
	  mouseUp: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onMouseUp: true}),
	      captured: keyOf({onMouseUpCapture: true})
	    }
	  },
	  paste: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onPaste: true}),
	      captured: keyOf({onPasteCapture: true})
	    }
	  },
	  reset: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onReset: true}),
	      captured: keyOf({onResetCapture: true})
	    }
	  },
	  scroll: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onScroll: true}),
	      captured: keyOf({onScrollCapture: true})
	    }
	  },
	  submit: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onSubmit: true}),
	      captured: keyOf({onSubmitCapture: true})
	    }
	  },
	  touchCancel: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onTouchCancel: true}),
	      captured: keyOf({onTouchCancelCapture: true})
	    }
	  },
	  touchEnd: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onTouchEnd: true}),
	      captured: keyOf({onTouchEndCapture: true})
	    }
	  },
	  touchMove: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onTouchMove: true}),
	      captured: keyOf({onTouchMoveCapture: true})
	    }
	  },
	  touchStart: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onTouchStart: true}),
	      captured: keyOf({onTouchStartCapture: true})
	    }
	  },
	  wheel: {
	    phasedRegistrationNames: {
	      bubbled: keyOf({onWheel: true}),
	      captured: keyOf({onWheelCapture: true})
	    }
	  }
	};

	var topLevelEventsToDispatchConfig = {
	  topBlur:        eventTypes.blur,
	  topClick:       eventTypes.click,
	  topContextMenu: eventTypes.contextMenu,
	  topCopy:        eventTypes.copy,
	  topCut:         eventTypes.cut,
	  topDoubleClick: eventTypes.doubleClick,
	  topDrag:        eventTypes.drag,
	  topDragEnd:     eventTypes.dragEnd,
	  topDragEnter:   eventTypes.dragEnter,
	  topDragExit:    eventTypes.dragExit,
	  topDragLeave:   eventTypes.dragLeave,
	  topDragOver:    eventTypes.dragOver,
	  topDragStart:   eventTypes.dragStart,
	  topDrop:        eventTypes.drop,
	  topError:       eventTypes.error,
	  topFocus:       eventTypes.focus,
	  topInput:       eventTypes.input,
	  topKeyDown:     eventTypes.keyDown,
	  topKeyPress:    eventTypes.keyPress,
	  topKeyUp:       eventTypes.keyUp,
	  topLoad:        eventTypes.load,
	  topMouseDown:   eventTypes.mouseDown,
	  topMouseMove:   eventTypes.mouseMove,
	  topMouseOut:    eventTypes.mouseOut,
	  topMouseOver:   eventTypes.mouseOver,
	  topMouseUp:     eventTypes.mouseUp,
	  topPaste:       eventTypes.paste,
	  topReset:       eventTypes.reset,
	  topScroll:      eventTypes.scroll,
	  topSubmit:      eventTypes.submit,
	  topTouchCancel: eventTypes.touchCancel,
	  topTouchEnd:    eventTypes.touchEnd,
	  topTouchMove:   eventTypes.touchMove,
	  topTouchStart:  eventTypes.touchStart,
	  topWheel:       eventTypes.wheel
	};

	for (var type in topLevelEventsToDispatchConfig) {
	  topLevelEventsToDispatchConfig[type].dependencies = [type];
	}

	var SimpleEventPlugin = {

	  eventTypes: eventTypes,

	  /**
	   * Same as the default implementation, except cancels the event when return
	   * value is false. This behavior will be disabled in a future release.
	   *
	   * @param {object} Event to be dispatched.
	   * @param {function} Application-level callback.
	   * @param {string} domID DOM ID to pass to the callback.
	   */
	  executeDispatch: function(event, listener, domID) {
	    var returnValue = EventPluginUtils.executeDispatch(event, listener, domID);

	    ("production" !== process.env.NODE_ENV ? warning(
	      typeof returnValue !== 'boolean',
	      'Returning `false` from an event handler is deprecated and will be ' +
	      'ignored in a future release. Instead, manually call ' +
	      'e.stopPropagation() or e.preventDefault(), as appropriate.'
	    ) : null);

	    if (returnValue === false) {
	      event.stopPropagation();
	      event.preventDefault();
	    }
	  },

	  /**
	   * @param {string} topLevelType Record from `EventConstants`.
	   * @param {DOMEventTarget} topLevelTarget The listening component root node.
	   * @param {string} topLevelTargetID ID of `topLevelTarget`.
	   * @param {object} nativeEvent Native browser event.
	   * @return {*} An accumulation of synthetic events.
	   * @see {EventPluginHub.extractEvents}
	   */
	  extractEvents: function(
	      topLevelType,
	      topLevelTarget,
	      topLevelTargetID,
	      nativeEvent) {
	    var dispatchConfig = topLevelEventsToDispatchConfig[topLevelType];
	    if (!dispatchConfig) {
	      return null;
	    }
	    var EventConstructor;
	    switch (topLevelType) {
	      case topLevelTypes.topInput:
	      case topLevelTypes.topLoad:
	      case topLevelTypes.topError:
	      case topLevelTypes.topReset:
	      case topLevelTypes.topSubmit:
	        // HTML Events
	        // @see http://www.w3.org/TR/html5/index.html#events-0
	        EventConstructor = SyntheticEvent;
	        break;
	      case topLevelTypes.topKeyPress:
	        // FireFox creates a keypress event for function keys too. This removes
	        // the unwanted keypress events. Enter is however both printable and
	        // non-printable. One would expect Tab to be as well (but it isn't).
	        if (getEventCharCode(nativeEvent) === 0) {
	          return null;
	        }
	        /* falls through */
	      case topLevelTypes.topKeyDown:
	      case topLevelTypes.topKeyUp:
	        EventConstructor = SyntheticKeyboardEvent;
	        break;
	      case topLevelTypes.topBlur:
	      case topLevelTypes.topFocus:
	        EventConstructor = SyntheticFocusEvent;
	        break;
	      case topLevelTypes.topClick:
	        // Firefox creates a click event on right mouse clicks. This removes the
	        // unwanted click events.
	        if (nativeEvent.button === 2) {
	          return null;
	        }
	        /* falls through */
	      case topLevelTypes.topContextMenu:
	      case topLevelTypes.topDoubleClick:
	      case topLevelTypes.topMouseDown:
	      case topLevelTypes.topMouseMove:
	      case topLevelTypes.topMouseOut:
	      case topLevelTypes.topMouseOver:
	      case topLevelTypes.topMouseUp:
	        EventConstructor = SyntheticMouseEvent;
	        break;
	      case topLevelTypes.topDrag:
	      case topLevelTypes.topDragEnd:
	      case topLevelTypes.topDragEnter:
	      case topLevelTypes.topDragExit:
	      case topLevelTypes.topDragLeave:
	      case topLevelTypes.topDragOver:
	      case topLevelTypes.topDragStart:
	      case topLevelTypes.topDrop:
	        EventConstructor = SyntheticDragEvent;
	        break;
	      case topLevelTypes.topTouchCancel:
	      case topLevelTypes.topTouchEnd:
	      case topLevelTypes.topTouchMove:
	      case topLevelTypes.topTouchStart:
	        EventConstructor = SyntheticTouchEvent;
	        break;
	      case topLevelTypes.topScroll:
	        EventConstructor = SyntheticUIEvent;
	        break;
	      case topLevelTypes.topWheel:
	        EventConstructor = SyntheticWheelEvent;
	        break;
	      case topLevelTypes.topCopy:
	      case topLevelTypes.topCut:
	      case topLevelTypes.topPaste:
	        EventConstructor = SyntheticClipboardEvent;
	        break;
	    }
	    ("production" !== process.env.NODE_ENV ? invariant(
	      EventConstructor,
	      'SimpleEventPlugin: Unhandled event type, `%s`.',
	      topLevelType
	    ) : invariant(EventConstructor));
	    var event = EventConstructor.getPooled(
	      dispatchConfig,
	      topLevelTargetID,
	      nativeEvent
	    );
	    EventPropagators.accumulateTwoPhaseDispatches(event);
	    return event;
	  }

	};

	module.exports = SimpleEventPlugin;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 141 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticDragEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticMouseEvent = __webpack_require__(106);

	/**
	 * @interface DragEvent
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var DragEventInterface = {
	  dataTransfer: null
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticDragEvent(dispatchConfig, dispatchMarker, nativeEvent) {
	  SyntheticMouseEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
	}

	SyntheticMouseEvent.augmentClass(SyntheticDragEvent, DragEventInterface);

	module.exports = SyntheticDragEvent;


/***/ },
/* 142 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticClipboardEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticEvent = __webpack_require__(98);

	/**
	 * @interface Event
	 * @see http://www.w3.org/TR/clipboard-apis/
	 */
	var ClipboardEventInterface = {
	  clipboardData: function(event) {
	    return (
	      'clipboardData' in event ?
	        event.clipboardData :
	        window.clipboardData
	    );
	  }
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticClipboardEvent(dispatchConfig, dispatchMarker, nativeEvent) {
	  SyntheticEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
	}

	SyntheticEvent.augmentClass(SyntheticClipboardEvent, ClipboardEventInterface);

	module.exports = SyntheticClipboardEvent;


/***/ },
/* 143 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticFocusEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticUIEvent = __webpack_require__(107);

	/**
	 * @interface FocusEvent
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var FocusEventInterface = {
	  relatedTarget: null
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticFocusEvent(dispatchConfig, dispatchMarker, nativeEvent) {
	  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
	}

	SyntheticUIEvent.augmentClass(SyntheticFocusEvent, FocusEventInterface);

	module.exports = SyntheticFocusEvent;


/***/ },
/* 144 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticKeyboardEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticUIEvent = __webpack_require__(107);

	var getEventCharCode = __webpack_require__(145);
	var getEventKey = __webpack_require__(146);
	var getEventModifierState = __webpack_require__(108);

	/**
	 * @interface KeyboardEvent
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var KeyboardEventInterface = {
	  key: getEventKey,
	  location: null,
	  ctrlKey: null,
	  shiftKey: null,
	  altKey: null,
	  metaKey: null,
	  repeat: null,
	  locale: null,
	  getModifierState: getEventModifierState,
	  // Legacy Interface
	  charCode: function(event) {
	    // `charCode` is the result of a KeyPress event and represents the value of
	    // the actual printable character.

	    // KeyPress is deprecated, but its replacement is not yet final and not
	    // implemented in any major browser. Only KeyPress has charCode.
	    if (event.type === 'keypress') {
	      return getEventCharCode(event);
	    }
	    return 0;
	  },
	  keyCode: function(event) {
	    // `keyCode` is the result of a KeyDown/Up event and represents the value of
	    // physical keyboard key.

	    // The actual meaning of the value depends on the users' keyboard layout
	    // which cannot be detected. Assuming that it is a US keyboard layout
	    // provides a surprisingly accurate mapping for US and European users.
	    // Due to this, it is left to the user to implement at this time.
	    if (event.type === 'keydown' || event.type === 'keyup') {
	      return event.keyCode;
	    }
	    return 0;
	  },
	  which: function(event) {
	    // `which` is an alias for either `keyCode` or `charCode` depending on the
	    // type of the event.
	    if (event.type === 'keypress') {
	      return getEventCharCode(event);
	    }
	    if (event.type === 'keydown' || event.type === 'keyup') {
	      return event.keyCode;
	    }
	    return 0;
	  }
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticKeyboardEvent(dispatchConfig, dispatchMarker, nativeEvent) {
	  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
	}

	SyntheticUIEvent.augmentClass(SyntheticKeyboardEvent, KeyboardEventInterface);

	module.exports = SyntheticKeyboardEvent;


/***/ },
/* 145 */
/***/ function(module, exports) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getEventCharCode
	 * @typechecks static-only
	 */

	'use strict';

	/**
	 * `charCode` represents the actual "character code" and is safe to use with
	 * `String.fromCharCode`. As such, only keys that correspond to printable
	 * characters produce a valid `charCode`, the only exception to this is Enter.
	 * The Tab-key is considered non-printable and does not have a `charCode`,
	 * presumably because it does not produce a tab-character in browsers.
	 *
	 * @param {object} nativeEvent Native browser event.
	 * @return {string} Normalized `charCode` property.
	 */
	function getEventCharCode(nativeEvent) {
	  var charCode;
	  var keyCode = nativeEvent.keyCode;

	  if ('charCode' in nativeEvent) {
	    charCode = nativeEvent.charCode;

	    // FF does not set `charCode` for the Enter-key, check against `keyCode`.
	    if (charCode === 0 && keyCode === 13) {
	      charCode = 13;
	    }
	  } else {
	    // IE8 does not implement `charCode`, but `keyCode` has the correct value.
	    charCode = keyCode;
	  }

	  // Some non-printable keys are reported in `charCode`/`keyCode`, discard them.
	  // Must not discard the (non-)printable Enter-key.
	  if (charCode >= 32 || charCode === 13) {
	    return charCode;
	  }

	  return 0;
	}

	module.exports = getEventCharCode;


/***/ },
/* 146 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule getEventKey
	 * @typechecks static-only
	 */

	'use strict';

	var getEventCharCode = __webpack_require__(145);

	/**
	 * Normalization of deprecated HTML5 `key` values
	 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
	 */
	var normalizeKey = {
	  'Esc': 'Escape',
	  'Spacebar': ' ',
	  'Left': 'ArrowLeft',
	  'Up': 'ArrowUp',
	  'Right': 'ArrowRight',
	  'Down': 'ArrowDown',
	  'Del': 'Delete',
	  'Win': 'OS',
	  'Menu': 'ContextMenu',
	  'Apps': 'ContextMenu',
	  'Scroll': 'ScrollLock',
	  'MozPrintableKey': 'Unidentified'
	};

	/**
	 * Translation from legacy `keyCode` to HTML5 `key`
	 * Only special keys supported, all others depend on keyboard layout or browser
	 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent#Key_names
	 */
	var translateToKey = {
	  8: 'Backspace',
	  9: 'Tab',
	  12: 'Clear',
	  13: 'Enter',
	  16: 'Shift',
	  17: 'Control',
	  18: 'Alt',
	  19: 'Pause',
	  20: 'CapsLock',
	  27: 'Escape',
	  32: ' ',
	  33: 'PageUp',
	  34: 'PageDown',
	  35: 'End',
	  36: 'Home',
	  37: 'ArrowLeft',
	  38: 'ArrowUp',
	  39: 'ArrowRight',
	  40: 'ArrowDown',
	  45: 'Insert',
	  46: 'Delete',
	  112: 'F1', 113: 'F2', 114: 'F3', 115: 'F4', 116: 'F5', 117: 'F6',
	  118: 'F7', 119: 'F8', 120: 'F9', 121: 'F10', 122: 'F11', 123: 'F12',
	  144: 'NumLock',
	  145: 'ScrollLock',
	  224: 'Meta'
	};

	/**
	 * @param {object} nativeEvent Native browser event.
	 * @return {string} Normalized `key` property.
	 */
	function getEventKey(nativeEvent) {
	  if (nativeEvent.key) {
	    // Normalize inconsistent values reported by browsers due to
	    // implementations of a working draft specification.

	    // FireFox implements `key` but returns `MozPrintableKey` for all
	    // printable characters (normalized to `Unidentified`), ignore it.
	    var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
	    if (key !== 'Unidentified') {
	      return key;
	    }
	  }

	  // Browser does not implement `key`, polyfill as much of it as we can.
	  if (nativeEvent.type === 'keypress') {
	    var charCode = getEventCharCode(nativeEvent);

	    // The enter-key is technically both printable and non-printable and can
	    // thus be captured by `keypress`, no other non-printable key should.
	    return charCode === 13 ? 'Enter' : String.fromCharCode(charCode);
	  }
	  if (nativeEvent.type === 'keydown' || nativeEvent.type === 'keyup') {
	    // While user keyboard layout determines the actual meaning of each
	    // `keyCode` value, almost all function keys have a universal value.
	    return translateToKey[nativeEvent.keyCode] || 'Unidentified';
	  }
	  return '';
	}

	module.exports = getEventKey;


/***/ },
/* 147 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticTouchEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticUIEvent = __webpack_require__(107);

	var getEventModifierState = __webpack_require__(108);

	/**
	 * @interface TouchEvent
	 * @see http://www.w3.org/TR/touch-events/
	 */
	var TouchEventInterface = {
	  touches: null,
	  targetTouches: null,
	  changedTouches: null,
	  altKey: null,
	  metaKey: null,
	  ctrlKey: null,
	  shiftKey: null,
	  getModifierState: getEventModifierState
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticUIEvent}
	 */
	function SyntheticTouchEvent(dispatchConfig, dispatchMarker, nativeEvent) {
	  SyntheticUIEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
	}

	SyntheticUIEvent.augmentClass(SyntheticTouchEvent, TouchEventInterface);

	module.exports = SyntheticTouchEvent;


/***/ },
/* 148 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SyntheticWheelEvent
	 * @typechecks static-only
	 */

	'use strict';

	var SyntheticMouseEvent = __webpack_require__(106);

	/**
	 * @interface WheelEvent
	 * @see http://www.w3.org/TR/DOM-Level-3-Events/
	 */
	var WheelEventInterface = {
	  deltaX: function(event) {
	    return (
	      'deltaX' in event ? event.deltaX :
	      // Fallback to `wheelDeltaX` for Webkit and normalize (right is positive).
	      'wheelDeltaX' in event ? -event.wheelDeltaX : 0
	    );
	  },
	  deltaY: function(event) {
	    return (
	      'deltaY' in event ? event.deltaY :
	      // Fallback to `wheelDeltaY` for Webkit and normalize (down is positive).
	      'wheelDeltaY' in event ? -event.wheelDeltaY :
	      // Fallback to `wheelDelta` for IE<9 and normalize (down is positive).
	      'wheelDelta' in event ? -event.wheelDelta : 0
	    );
	  },
	  deltaZ: null,

	  // Browsers without "deltaMode" is reporting in raw wheel delta where one
	  // notch on the scroll is always +/- 120, roughly equivalent to pixels.
	  // A good approximation of DOM_DELTA_LINE (1) is 5% of viewport size or
	  // ~40 pixels, for DOM_DELTA_SCREEN (2) it is 87.5% of viewport size.
	  deltaMode: null
	};

	/**
	 * @param {object} dispatchConfig Configuration used to dispatch this event.
	 * @param {string} dispatchMarker Marker identifying the event target.
	 * @param {object} nativeEvent Native browser event.
	 * @extends {SyntheticMouseEvent}
	 */
	function SyntheticWheelEvent(dispatchConfig, dispatchMarker, nativeEvent) {
	  SyntheticMouseEvent.call(this, dispatchConfig, dispatchMarker, nativeEvent);
	}

	SyntheticMouseEvent.augmentClass(SyntheticWheelEvent, WheelEventInterface);

	module.exports = SyntheticWheelEvent;


/***/ },
/* 149 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule SVGDOMPropertyConfig
	 */

	/*jslint bitwise: true*/

	'use strict';

	var DOMProperty = __webpack_require__(45);

	var MUST_USE_ATTRIBUTE = DOMProperty.injection.MUST_USE_ATTRIBUTE;

	var SVGDOMPropertyConfig = {
	  Properties: {
	    clipPath: MUST_USE_ATTRIBUTE,
	    cx: MUST_USE_ATTRIBUTE,
	    cy: MUST_USE_ATTRIBUTE,
	    d: MUST_USE_ATTRIBUTE,
	    dx: MUST_USE_ATTRIBUTE,
	    dy: MUST_USE_ATTRIBUTE,
	    fill: MUST_USE_ATTRIBUTE,
	    fillOpacity: MUST_USE_ATTRIBUTE,
	    fontFamily: MUST_USE_ATTRIBUTE,
	    fontSize: MUST_USE_ATTRIBUTE,
	    fx: MUST_USE_ATTRIBUTE,
	    fy: MUST_USE_ATTRIBUTE,
	    gradientTransform: MUST_USE_ATTRIBUTE,
	    gradientUnits: MUST_USE_ATTRIBUTE,
	    markerEnd: MUST_USE_ATTRIBUTE,
	    markerMid: MUST_USE_ATTRIBUTE,
	    markerStart: MUST_USE_ATTRIBUTE,
	    offset: MUST_USE_ATTRIBUTE,
	    opacity: MUST_USE_ATTRIBUTE,
	    patternContentUnits: MUST_USE_ATTRIBUTE,
	    patternUnits: MUST_USE_ATTRIBUTE,
	    points: MUST_USE_ATTRIBUTE,
	    preserveAspectRatio: MUST_USE_ATTRIBUTE,
	    r: MUST_USE_ATTRIBUTE,
	    rx: MUST_USE_ATTRIBUTE,
	    ry: MUST_USE_ATTRIBUTE,
	    spreadMethod: MUST_USE_ATTRIBUTE,
	    stopColor: MUST_USE_ATTRIBUTE,
	    stopOpacity: MUST_USE_ATTRIBUTE,
	    stroke: MUST_USE_ATTRIBUTE,
	    strokeDasharray: MUST_USE_ATTRIBUTE,
	    strokeLinecap: MUST_USE_ATTRIBUTE,
	    strokeOpacity: MUST_USE_ATTRIBUTE,
	    strokeWidth: MUST_USE_ATTRIBUTE,
	    textAnchor: MUST_USE_ATTRIBUTE,
	    transform: MUST_USE_ATTRIBUTE,
	    version: MUST_USE_ATTRIBUTE,
	    viewBox: MUST_USE_ATTRIBUTE,
	    x1: MUST_USE_ATTRIBUTE,
	    x2: MUST_USE_ATTRIBUTE,
	    x: MUST_USE_ATTRIBUTE,
	    y1: MUST_USE_ATTRIBUTE,
	    y2: MUST_USE_ATTRIBUTE,
	    y: MUST_USE_ATTRIBUTE
	  },
	  DOMAttributeNames: {
	    clipPath: 'clip-path',
	    fillOpacity: 'fill-opacity',
	    fontFamily: 'font-family',
	    fontSize: 'font-size',
	    gradientTransform: 'gradientTransform',
	    gradientUnits: 'gradientUnits',
	    markerEnd: 'marker-end',
	    markerMid: 'marker-mid',
	    markerStart: 'marker-start',
	    patternContentUnits: 'patternContentUnits',
	    patternUnits: 'patternUnits',
	    preserveAspectRatio: 'preserveAspectRatio',
	    spreadMethod: 'spreadMethod',
	    stopColor: 'stop-color',
	    stopOpacity: 'stop-opacity',
	    strokeDasharray: 'stroke-dasharray',
	    strokeLinecap: 'stroke-linecap',
	    strokeOpacity: 'stroke-opacity',
	    strokeWidth: 'stroke-width',
	    textAnchor: 'text-anchor',
	    viewBox: 'viewBox'
	  }
	};

	module.exports = SVGDOMPropertyConfig;


/***/ },
/* 150 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule createFullPageComponent
	 * @typechecks
	 */

	'use strict';

	// Defeat circular references by requiring this directly.
	var ReactClass = __webpack_require__(38);
	var ReactElement = __webpack_require__(12);

	var invariant = __webpack_require__(8);

	/**
	 * Create a component that will throw an exception when unmounted.
	 *
	 * Components like <html> <head> and <body> can't be removed or added
	 * easily in a cross-browser way, however it's valuable to be able to
	 * take advantage of React's reconciliation for styling and <title>
	 * management. So we just document it and throw in dangerous cases.
	 *
	 * @param {string} tag The tag to wrap
	 * @return {function} convenience constructor of new component
	 */
	function createFullPageComponent(tag) {
	  var elementFactory = ReactElement.createFactory(tag);

	  var FullPageComponent = ReactClass.createClass({
	    tagName: tag.toUpperCase(),
	    displayName: 'ReactFullPageComponent' + tag,

	    componentWillUnmount: function() {
	      ("production" !== process.env.NODE_ENV ? invariant(
	        false,
	        '%s tried to unmount. Because of cross-browser quirks it is ' +
	        'impossible to unmount some top-level components (eg <html>, <head>, ' +
	        'and <body>) reliably and efficiently. To fix this, have a single ' +
	        'top-level component that never unmounts render these elements.',
	        this.constructor.displayName
	      ) : invariant(false));
	    },

	    render: function() {
	      return elementFactory(this.props);
	    }
	  });

	  return FullPageComponent;
	}

	module.exports = createFullPageComponent;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 151 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDefaultPerf
	 * @typechecks static-only
	 */

	'use strict';

	var DOMProperty = __webpack_require__(45);
	var ReactDefaultPerfAnalysis = __webpack_require__(152);
	var ReactMount = __webpack_require__(68);
	var ReactPerf = __webpack_require__(27);

	var performanceNow = __webpack_require__(153);

	function roundFloat(val) {
	  return Math.floor(val * 100) / 100;
	}

	function addValue(obj, key, val) {
	  obj[key] = (obj[key] || 0) + val;
	}

	var ReactDefaultPerf = {
	  _allMeasurements: [], // last item in the list is the current one
	  _mountStack: [0],
	  _injected: false,

	  start: function() {
	    if (!ReactDefaultPerf._injected) {
	      ReactPerf.injection.injectMeasure(ReactDefaultPerf.measure);
	    }

	    ReactDefaultPerf._allMeasurements.length = 0;
	    ReactPerf.enableMeasure = true;
	  },

	  stop: function() {
	    ReactPerf.enableMeasure = false;
	  },

	  getLastMeasurements: function() {
	    return ReactDefaultPerf._allMeasurements;
	  },

	  printExclusive: function(measurements) {
	    measurements = measurements || ReactDefaultPerf._allMeasurements;
	    var summary = ReactDefaultPerfAnalysis.getExclusiveSummary(measurements);
	    console.table(summary.map(function(item) {
	      return {
	        'Component class name': item.componentName,
	        'Total inclusive time (ms)': roundFloat(item.inclusive),
	        'Exclusive mount time (ms)': roundFloat(item.exclusive),
	        'Exclusive render time (ms)': roundFloat(item.render),
	        'Mount time per instance (ms)': roundFloat(item.exclusive / item.count),
	        'Render time per instance (ms)': roundFloat(item.render / item.count),
	        'Instances': item.count
	      };
	    }));
	    // TODO: ReactDefaultPerfAnalysis.getTotalTime() does not return the correct
	    // number.
	  },

	  printInclusive: function(measurements) {
	    measurements = measurements || ReactDefaultPerf._allMeasurements;
	    var summary = ReactDefaultPerfAnalysis.getInclusiveSummary(measurements);
	    console.table(summary.map(function(item) {
	      return {
	        'Owner > component': item.componentName,
	        'Inclusive time (ms)': roundFloat(item.time),
	        'Instances': item.count
	      };
	    }));
	    console.log(
	      'Total time:',
	      ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms'
	    );
	  },

	  getMeasurementsSummaryMap: function(measurements) {
	    var summary = ReactDefaultPerfAnalysis.getInclusiveSummary(
	      measurements,
	      true
	    );
	    return summary.map(function(item) {
	      return {
	        'Owner > component': item.componentName,
	        'Wasted time (ms)': item.time,
	        'Instances': item.count
	      };
	    });
	  },

	  printWasted: function(measurements) {
	    measurements = measurements || ReactDefaultPerf._allMeasurements;
	    console.table(ReactDefaultPerf.getMeasurementsSummaryMap(measurements));
	    console.log(
	      'Total time:',
	      ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms'
	    );
	  },

	  printDOM: function(measurements) {
	    measurements = measurements || ReactDefaultPerf._allMeasurements;
	    var summary = ReactDefaultPerfAnalysis.getDOMSummary(measurements);
	    console.table(summary.map(function(item) {
	      var result = {};
	      result[DOMProperty.ID_ATTRIBUTE_NAME] = item.id;
	      result['type'] = item.type;
	      result['args'] = JSON.stringify(item.args);
	      return result;
	    }));
	    console.log(
	      'Total time:',
	      ReactDefaultPerfAnalysis.getTotalTime(measurements).toFixed(2) + ' ms'
	    );
	  },

	  _recordWrite: function(id, fnName, totalTime, args) {
	    // TODO: totalTime isn't that useful since it doesn't count paints/reflows
	    var writes =
	      ReactDefaultPerf
	        ._allMeasurements[ReactDefaultPerf._allMeasurements.length - 1]
	        .writes;
	    writes[id] = writes[id] || [];
	    writes[id].push({
	      type: fnName,
	      time: totalTime,
	      args: args
	    });
	  },

	  measure: function(moduleName, fnName, func) {
	    return function() {for (var args=[],$__0=0,$__1=arguments.length;$__0<$__1;$__0++) args.push(arguments[$__0]);
	      var totalTime;
	      var rv;
	      var start;

	      if (fnName === '_renderNewRootComponent' ||
	          fnName === 'flushBatchedUpdates') {
	        // A "measurement" is a set of metrics recorded for each flush. We want
	        // to group the metrics for a given flush together so we can look at the
	        // components that rendered and the DOM operations that actually
	        // happened to determine the amount of "wasted work" performed.
	        ReactDefaultPerf._allMeasurements.push({
	          exclusive: {},
	          inclusive: {},
	          render: {},
	          counts: {},
	          writes: {},
	          displayNames: {},
	          totalTime: 0
	        });
	        start = performanceNow();
	        rv = func.apply(this, args);
	        ReactDefaultPerf._allMeasurements[
	          ReactDefaultPerf._allMeasurements.length - 1
	        ].totalTime = performanceNow() - start;
	        return rv;
	      } else if (fnName === '_mountImageIntoNode' ||
	          moduleName === 'ReactDOMIDOperations') {
	        start = performanceNow();
	        rv = func.apply(this, args);
	        totalTime = performanceNow() - start;

	        if (fnName === '_mountImageIntoNode') {
	          var mountID = ReactMount.getID(args[1]);
	          ReactDefaultPerf._recordWrite(mountID, fnName, totalTime, args[0]);
	        } else if (fnName === 'dangerouslyProcessChildrenUpdates') {
	          // special format
	          args[0].forEach(function(update) {
	            var writeArgs = {};
	            if (update.fromIndex !== null) {
	              writeArgs.fromIndex = update.fromIndex;
	            }
	            if (update.toIndex !== null) {
	              writeArgs.toIndex = update.toIndex;
	            }
	            if (update.textContent !== null) {
	              writeArgs.textContent = update.textContent;
	            }
	            if (update.markupIndex !== null) {
	              writeArgs.markup = args[1][update.markupIndex];
	            }
	            ReactDefaultPerf._recordWrite(
	              update.parentID,
	              update.type,
	              totalTime,
	              writeArgs
	            );
	          });
	        } else {
	          // basic format
	          ReactDefaultPerf._recordWrite(
	            args[0],
	            fnName,
	            totalTime,
	            Array.prototype.slice.call(args, 1)
	          );
	        }
	        return rv;
	      } else if (moduleName === 'ReactCompositeComponent' && (
	        (// TODO: receiveComponent()?
	        (fnName === 'mountComponent' ||
	        fnName === 'updateComponent' || fnName === '_renderValidatedComponent')))) {

	        if (typeof this._currentElement.type === 'string') {
	          return func.apply(this, args);
	        }

	        var rootNodeID = fnName === 'mountComponent' ?
	          args[0] :
	          this._rootNodeID;
	        var isRender = fnName === '_renderValidatedComponent';
	        var isMount = fnName === 'mountComponent';

	        var mountStack = ReactDefaultPerf._mountStack;
	        var entry = ReactDefaultPerf._allMeasurements[
	          ReactDefaultPerf._allMeasurements.length - 1
	        ];

	        if (isRender) {
	          addValue(entry.counts, rootNodeID, 1);
	        } else if (isMount) {
	          mountStack.push(0);
	        }

	        start = performanceNow();
	        rv = func.apply(this, args);
	        totalTime = performanceNow() - start;

	        if (isRender) {
	          addValue(entry.render, rootNodeID, totalTime);
	        } else if (isMount) {
	          var subMountTime = mountStack.pop();
	          mountStack[mountStack.length - 1] += totalTime;
	          addValue(entry.exclusive, rootNodeID, totalTime - subMountTime);
	          addValue(entry.inclusive, rootNodeID, totalTime);
	        } else {
	          addValue(entry.inclusive, rootNodeID, totalTime);
	        }

	        entry.displayNames[rootNodeID] = {
	          current: this.getName(),
	          owner: this._currentElement._owner ?
	            this._currentElement._owner.getName() :
	            '<root>'
	        };

	        return rv;
	      } else {
	        return func.apply(this, args);
	      }
	    };
	  }
	};

	module.exports = ReactDefaultPerf;


/***/ },
/* 152 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactDefaultPerfAnalysis
	 */

	var assign = __webpack_require__(14);

	// Don't try to save users less than 1.2ms (a number I made up)
	var DONT_CARE_THRESHOLD = 1.2;
	var DOM_OPERATION_TYPES = {
	  '_mountImageIntoNode': 'set innerHTML',
	  INSERT_MARKUP: 'set innerHTML',
	  MOVE_EXISTING: 'move',
	  REMOVE_NODE: 'remove',
	  TEXT_CONTENT: 'set textContent',
	  'updatePropertyByID': 'update attribute',
	  'deletePropertyByID': 'delete attribute',
	  'updateStylesByID': 'update styles',
	  'updateInnerHTMLByID': 'set innerHTML',
	  'dangerouslyReplaceNodeWithMarkupByID': 'replace'
	};

	function getTotalTime(measurements) {
	  // TODO: return number of DOM ops? could be misleading.
	  // TODO: measure dropped frames after reconcile?
	  // TODO: log total time of each reconcile and the top-level component
	  // class that triggered it.
	  var totalTime = 0;
	  for (var i = 0; i < measurements.length; i++) {
	    var measurement = measurements[i];
	    totalTime += measurement.totalTime;
	  }
	  return totalTime;
	}

	function getDOMSummary(measurements) {
	  var items = [];
	  for (var i = 0; i < measurements.length; i++) {
	    var measurement = measurements[i];
	    var id;

	    for (id in measurement.writes) {
	      measurement.writes[id].forEach(function(write) {
	        items.push({
	          id: id,
	          type: DOM_OPERATION_TYPES[write.type] || write.type,
	          args: write.args
	        });
	      });
	    }
	  }
	  return items;
	}

	function getExclusiveSummary(measurements) {
	  var candidates = {};
	  var displayName;

	  for (var i = 0; i < measurements.length; i++) {
	    var measurement = measurements[i];
	    var allIDs = assign(
	      {},
	      measurement.exclusive,
	      measurement.inclusive
	    );

	    for (var id in allIDs) {
	      displayName = measurement.displayNames[id].current;

	      candidates[displayName] = candidates[displayName] || {
	        componentName: displayName,
	        inclusive: 0,
	        exclusive: 0,
	        render: 0,
	        count: 0
	      };
	      if (measurement.render[id]) {
	        candidates[displayName].render += measurement.render[id];
	      }
	      if (measurement.exclusive[id]) {
	        candidates[displayName].exclusive += measurement.exclusive[id];
	      }
	      if (measurement.inclusive[id]) {
	        candidates[displayName].inclusive += measurement.inclusive[id];
	      }
	      if (measurement.counts[id]) {
	        candidates[displayName].count += measurement.counts[id];
	      }
	    }
	  }

	  // Now make a sorted array with the results.
	  var arr = [];
	  for (displayName in candidates) {
	    if (candidates[displayName].exclusive >= DONT_CARE_THRESHOLD) {
	      arr.push(candidates[displayName]);
	    }
	  }

	  arr.sort(function(a, b) {
	    return b.exclusive - a.exclusive;
	  });

	  return arr;
	}

	function getInclusiveSummary(measurements, onlyClean) {
	  var candidates = {};
	  var inclusiveKey;

	  for (var i = 0; i < measurements.length; i++) {
	    var measurement = measurements[i];
	    var allIDs = assign(
	      {},
	      measurement.exclusive,
	      measurement.inclusive
	    );
	    var cleanComponents;

	    if (onlyClean) {
	      cleanComponents = getUnchangedComponents(measurement);
	    }

	    for (var id in allIDs) {
	      if (onlyClean && !cleanComponents[id]) {
	        continue;
	      }

	      var displayName = measurement.displayNames[id];

	      // Inclusive time is not useful for many components without knowing where
	      // they are instantiated. So we aggregate inclusive time with both the
	      // owner and current displayName as the key.
	      inclusiveKey = displayName.owner + ' > ' + displayName.current;

	      candidates[inclusiveKey] = candidates[inclusiveKey] || {
	        componentName: inclusiveKey,
	        time: 0,
	        count: 0
	      };

	      if (measurement.inclusive[id]) {
	        candidates[inclusiveKey].time += measurement.inclusive[id];
	      }
	      if (measurement.counts[id]) {
	        candidates[inclusiveKey].count += measurement.counts[id];
	      }
	    }
	  }

	  // Now make a sorted array with the results.
	  var arr = [];
	  for (inclusiveKey in candidates) {
	    if (candidates[inclusiveKey].time >= DONT_CARE_THRESHOLD) {
	      arr.push(candidates[inclusiveKey]);
	    }
	  }

	  arr.sort(function(a, b) {
	    return b.time - a.time;
	  });

	  return arr;
	}

	function getUnchangedComponents(measurement) {
	  // For a given reconcile, look at which components did not actually
	  // render anything to the DOM and return a mapping of their ID to
	  // the amount of time it took to render the entire subtree.
	  var cleanComponents = {};
	  var dirtyLeafIDs = Object.keys(measurement.writes);
	  var allIDs = assign({}, measurement.exclusive, measurement.inclusive);

	  for (var id in allIDs) {
	    var isDirty = false;
	    // For each component that rendered, see if a component that triggered
	    // a DOM op is in its subtree.
	    for (var i = 0; i < dirtyLeafIDs.length; i++) {
	      if (dirtyLeafIDs[i].indexOf(id) === 0) {
	        isDirty = true;
	        break;
	      }
	    }
	    if (!isDirty && measurement.counts[id] > 0) {
	      cleanComponents[id] = true;
	    }
	  }
	  return cleanComponents;
	}

	var ReactDefaultPerfAnalysis = {
	  getExclusiveSummary: getExclusiveSummary,
	  getInclusiveSummary: getInclusiveSummary,
	  getDOMSummary: getDOMSummary,
	  getTotalTime: getTotalTime
	};

	module.exports = ReactDefaultPerfAnalysis;


/***/ },
/* 153 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule performanceNow
	 * @typechecks
	 */

	var performance = __webpack_require__(154);

	/**
	 * Detect if we can use `window.performance.now()` and gracefully fallback to
	 * `Date.now()` if it doesn't exist. We need to support Firefox < 15 for now
	 * because of Facebook's testing infrastructure.
	 */
	if (!performance || !performance.now) {
	  performance = Date;
	}

	var performanceNow = performance.now.bind(performance);

	module.exports = performanceNow;


/***/ },
/* 154 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule performance
	 * @typechecks
	 */

	"use strict";

	var ExecutionEnvironment = __webpack_require__(52);

	var performance;

	if (ExecutionEnvironment.canUseDOM) {
	  performance =
	    window.performance ||
	    window.msPerformance ||
	    window.webkitPerformance;
	}

	module.exports = performance || {};


/***/ },
/* 155 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @typechecks static-only
	 * @providesModule ReactServerRendering
	 */
	'use strict';

	var ReactElement = __webpack_require__(12);
	var ReactInstanceHandles = __webpack_require__(21);
	var ReactMarkupChecksum = __webpack_require__(78);
	var ReactServerRenderingTransaction =
	  __webpack_require__(156);

	var emptyObject = __webpack_require__(15);
	var instantiateReactComponent = __webpack_require__(84);
	var invariant = __webpack_require__(8);

	/**
	 * @param {ReactElement} element
	 * @return {string} the HTML markup
	 */
	function renderToString(element) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    ReactElement.isValidElement(element),
	    'renderToString(): You must pass a valid ReactElement.'
	  ) : invariant(ReactElement.isValidElement(element)));

	  var transaction;
	  try {
	    var id = ReactInstanceHandles.createReactRootID();
	    transaction = ReactServerRenderingTransaction.getPooled(false);

	    return transaction.perform(function() {
	      var componentInstance = instantiateReactComponent(element, null);
	      var markup =
	        componentInstance.mountComponent(id, transaction, emptyObject);
	      return ReactMarkupChecksum.addChecksumToMarkup(markup);
	    }, null);
	  } finally {
	    ReactServerRenderingTransaction.release(transaction);
	  }
	}

	/**
	 * @param {ReactElement} element
	 * @return {string} the HTML markup, without the extra React ID and checksum
	 * (for generating static pages)
	 */
	function renderToStaticMarkup(element) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    ReactElement.isValidElement(element),
	    'renderToStaticMarkup(): You must pass a valid ReactElement.'
	  ) : invariant(ReactElement.isValidElement(element)));

	  var transaction;
	  try {
	    var id = ReactInstanceHandles.createReactRootID();
	    transaction = ReactServerRenderingTransaction.getPooled(true);

	    return transaction.perform(function() {
	      var componentInstance = instantiateReactComponent(element, null);
	      return componentInstance.mountComponent(id, transaction, emptyObject);
	    }, null);
	  } finally {
	    ReactServerRenderingTransaction.release(transaction);
	  }
	}

	module.exports = {
	  renderToString: renderToString,
	  renderToStaticMarkup: renderToStaticMarkup
	};

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 156 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Copyright 2014-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule ReactServerRenderingTransaction
	 * @typechecks
	 */

	'use strict';

	var PooledClass = __webpack_require__(10);
	var CallbackQueue = __webpack_require__(26);
	var ReactPutListenerQueue = __webpack_require__(136);
	var Transaction = __webpack_require__(35);

	var assign = __webpack_require__(14);
	var emptyFunction = __webpack_require__(17);

	/**
	 * Provides a `CallbackQueue` queue for collecting `onDOMReady` callbacks
	 * during the performing of the transaction.
	 */
	var ON_DOM_READY_QUEUEING = {
	  /**
	   * Initializes the internal `onDOMReady` queue.
	   */
	  initialize: function() {
	    this.reactMountReady.reset();
	  },

	  close: emptyFunction
	};

	var PUT_LISTENER_QUEUEING = {
	  initialize: function() {
	    this.putListenerQueue.reset();
	  },

	  close: emptyFunction
	};

	/**
	 * Executed within the scope of the `Transaction` instance. Consider these as
	 * being member methods, but with an implied ordering while being isolated from
	 * each other.
	 */
	var TRANSACTION_WRAPPERS = [
	  PUT_LISTENER_QUEUEING,
	  ON_DOM_READY_QUEUEING
	];

	/**
	 * @class ReactServerRenderingTransaction
	 * @param {boolean} renderToStaticMarkup
	 */
	function ReactServerRenderingTransaction(renderToStaticMarkup) {
	  this.reinitializeTransaction();
	  this.renderToStaticMarkup = renderToStaticMarkup;
	  this.reactMountReady = CallbackQueue.getPooled(null);
	  this.putListenerQueue = ReactPutListenerQueue.getPooled();
	}

	var Mixin = {
	  /**
	   * @see Transaction
	   * @abstract
	   * @final
	   * @return {array} Empty list of operation wrap proceedures.
	   */
	  getTransactionWrappers: function() {
	    return TRANSACTION_WRAPPERS;
	  },

	  /**
	   * @return {object} The queue to collect `onDOMReady` callbacks with.
	   */
	  getReactMountReady: function() {
	    return this.reactMountReady;
	  },

	  getPutListenerQueue: function() {
	    return this.putListenerQueue;
	  },

	  /**
	   * `PooledClass` looks for this, and will invoke this before allowing this
	   * instance to be resused.
	   */
	  destructor: function() {
	    CallbackQueue.release(this.reactMountReady);
	    this.reactMountReady = null;

	    ReactPutListenerQueue.release(this.putListenerQueue);
	    this.putListenerQueue = null;
	  }
	};


	assign(
	  ReactServerRenderingTransaction.prototype,
	  Transaction.Mixin,
	  Mixin
	);

	PooledClass.addPoolingTo(ReactServerRenderingTransaction);

	module.exports = ReactServerRenderingTransaction;


/***/ },
/* 157 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule onlyChild
	 */
	'use strict';

	var ReactElement = __webpack_require__(12);

	var invariant = __webpack_require__(8);

	/**
	 * Returns the first child in a collection of children and verifies that there
	 * is only one child in the collection. The current implementation of this
	 * function assumes that a single child gets passed without a wrapper, but the
	 * purpose of this helper function is to abstract away the particular structure
	 * of children.
	 *
	 * @param {?object} children Child collection structure.
	 * @return {ReactComponent} The first and only `ReactComponent` contained in the
	 * structure.
	 */
	function onlyChild(children) {
	  ("production" !== process.env.NODE_ENV ? invariant(
	    ReactElement.isValidElement(children),
	    'onlyChild must be passed a children with exactly one child.'
	  ) : invariant(ReactElement.isValidElement(children)));
	  return children;
	}

	module.exports = onlyChild;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 158 */,
/* 159 */,
/* 160 */,
/* 161 */,
/* 162 */,
/* 163 */,
/* 164 */,
/* 165 */,
/* 166 */,
/* 167 */,
/* 168 */,
/* 169 */,
/* 170 */,
/* 171 */,
/* 172 */,
/* 173 */,
/* 174 */,
/* 175 */,
/* 176 */,
/* 177 */,
/* 178 */,
/* 179 */,
/* 180 */,
/* 181 */,
/* 182 */,
/* 183 */,
/* 184 */,
/* 185 */,
/* 186 */,
/* 187 */,
/* 188 */,
/* 189 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	exports.__esModule = true;

	function _interopRequireDefault(obj) {
	  return obj && obj.__esModule ? obj : { 'default': obj };
	}

	var _createStore = __webpack_require__(192);

	var _createStore2 = _interopRequireDefault(_createStore);

	var _utilsCombineReducers = __webpack_require__(195);

	var _utilsCombineReducers2 = _interopRequireDefault(_utilsCombineReducers);

	var _utilsBindActionCreators = __webpack_require__(198);

	var _utilsBindActionCreators2 = _interopRequireDefault(_utilsBindActionCreators);

	var _utilsApplyMiddleware = __webpack_require__(190);

	var _utilsApplyMiddleware2 = _interopRequireDefault(_utilsApplyMiddleware);

	var _utilsCompose = __webpack_require__(191);

	var _utilsCompose2 = _interopRequireDefault(_utilsCompose);

	var _utilsSideEffects = __webpack_require__(194);

	exports.createStore = _createStore2['default'];
	exports.combineReducers = _utilsCombineReducers2['default'];
	exports.bindActionCreators = _utilsBindActionCreators2['default'];
	exports.applyMiddleware = _utilsApplyMiddleware2['default'];
	exports.compose = _utilsCompose2['default'];
	exports.withSideEffect = _utilsSideEffects.withSideEffect;

/***/ },
/* 190 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	exports.__esModule = true;

	var _extends = Object.assign || function (target) {
	  for (var i = 1; i < arguments.length; i++) {
	    var source = arguments[i];for (var key in source) {
	      if (Object.prototype.hasOwnProperty.call(source, key)) {
	        target[key] = source[key];
	      }
	    }
	  }return target;
	};

	exports['default'] = applyMiddleware;

	function _interopRequireDefault(obj) {
	  return obj && obj.__esModule ? obj : { 'default': obj };
	}

	var _compose = __webpack_require__(191);

	var _compose2 = _interopRequireDefault(_compose);

	/**
	 * Creates a store enhancer that applies middleware to the dispatch method
	 * of the Redux store. This is handy for a variety of tasks, such as expressing
	 * asynchronous actions in a concise manner, or logging every action payload.
	 *
	 * See `redux-thunk` package as an example of the Redux middleware.
	 *
	 * Because middleware is potentially asynchronous, this should be the first
	 * store enhancer in the composition chain.
	 *
	 * Note that each middleware will be given the `dispatch` and `getState` functions
	 * as named arguments.
	 *
	 * @param {...Function} middlewares The middleware chain to be applied.
	 * @returns {Function} A store enhancer applying the middleware.
	 */

	function applyMiddleware() {
	  for (var _len = arguments.length, middlewares = Array(_len), _key = 0; _key < _len; _key++) {
	    middlewares[_key] = arguments[_key];
	  }

	  return function (next) {
	    return function (reducer, initialState) {
	      var store = next(reducer, initialState);
	      var _dispatch = store.dispatch;
	      var chain = [];

	      var middlewareAPI = {
	        getState: store.getState,
	        dispatch: function dispatch(action) {
	          return _dispatch(action);
	        }
	      };
	      chain = middlewares.map(function (middleware) {
	        return middleware(middlewareAPI);
	      });
	      _dispatch = _compose2['default'].apply(undefined, chain.concat([store.dispatch]));

	      return _extends({}, store, {
	        dispatch: _dispatch
	      });
	    };
	  };
	}

	module.exports = exports['default'];

/***/ },
/* 191 */
/***/ function(module, exports) {

	/**
	 * Composes functions from left to right.
	 *
	 * @param {...Function} funcs - The functions to compose. Each is expected to
	 * accept a function as an argument and to return a function.
	 * @returns {Function} A function obtained by composing functions from left to
	 * right.
	 */
	"use strict";

	exports.__esModule = true;
	exports["default"] = compose;

	function compose() {
	  for (var _len = arguments.length, funcs = Array(_len), _key = 0; _key < _len; _key++) {
	    funcs[_key] = arguments[_key];
	  }

	  return funcs.reduceRight(function (composed, f) {
	    return f(composed);
	  });
	}

	module.exports = exports["default"];

/***/ },
/* 192 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	exports.__esModule = true;
	exports['default'] = createStore;

	function _interopRequireDefault(obj) {
	  return obj && obj.__esModule ? obj : { 'default': obj };
	}

	var _utilsIsPlainObject = __webpack_require__(193);

	var _utilsIsPlainObject2 = _interopRequireDefault(_utilsIsPlainObject);

	var _utilsSideEffects = __webpack_require__(194);

	/**
	 * These are private action types reserved by Redux.
	 * For any unknown actions, you must return the current state.
	 * If the current state is undefined, you must return the initial state.
	 * Do not reference these action types directly in your code.
	 */
	var ActionTypes = {
	  INIT: '@@redux/INIT'
	};

	exports.ActionTypes = ActionTypes;
	/**
	 * Creates a Redux store that holds the state tree.
	 * The only way to change the data in the store is to call `dispatch()` on it.
	 *
	 * There should only be a single store in your app. To specify how different
	 * parts of the state tree respond to actions, you may combine several reducers
	 * into a single reducer function by using `combineReducers`.
	 *
	 * @param {Function} reducer A function that returns the next state tree, given
	 * the current state tree and the action to handle.
	 *
	 * @param {any} [initialState] The initial state. You may optionally specify it
	 * to hydrate the state from the server in universal apps, or to restore a
	 * previously serialized user session.
	 * If you use `combineReducers` to produce the root reducer function, this must be
	 * an object with the same shape as `combineReducers` keys.
	 *
	 * @returns {Store} A Redux store that lets you read the state, dispatch actions
	 * and subscribe to changes.
	 */

	function createStore(reducer, initialState) {
	  if (typeof reducer !== 'function') {
	    throw new Error('Expected the reducer to be a function.');
	  }

	  var currentReducer = reducer;
	  var currentState = initialState;
	  var listeners = [];
	  var isDispatching = false;

	  /**
	   * Reads the state tree managed by the store.
	   *
	   * @returns {any} The current state tree of your application.
	   */
	  function getState() {
	    return currentState;
	  }

	  /**
	   * Adds a change listener. It will be called any time an action is dispatched,
	   * and some part of the state tree may potentially have changed. You may then
	   * call `getState()` to read the current state tree inside the callback.
	   *
	   * @param {Function} listener A callback to be invoked on every dispatch.
	   * @returns {Function} A function to remove this change listener.
	   */
	  function subscribe(listener) {
	    listeners.push(listener);

	    return function unsubscribe() {
	      var index = listeners.indexOf(listener);
	      listeners.splice(index, 1);
	    };
	  }

	  /**
	   * Dispatches an action. It is the only way to trigger a state change.
	   *
	   * The `reducer` function, used to create the store, will be called with the
	   * current state tree and the given `action`. Its return value will
	   * be considered the **next** state of the tree, and the change listeners
	   * will be notified.
	   *
	   * The base implementation only supports plain object actions. If you want to
	   * dispatch a Promise, an Observable, a thunk, or something else, you need to
	   * wrap your store creating function into the corresponding middleware. For
	   * example, see the documentation for the `redux-thunk` package. Even the
	   * middleware will eventually dispatch plain object actions using this method.
	   *
	   * @param {Object} action A plain object representing “what changed”. It is
	   * a good idea to keep actions serializable so you can record and replay user
	   * sessions, or use the time travelling `redux-devtools`.
	   *
	   * @returns {Object} For convenience, the same action object you dispatched.
	   *
	   * Note that, if you use a custom middleware, it may wrap `dispatch()` to
	   * return something else (for example, a Promise you can await).
	   */
	  function dispatch(action) {
	    if (!_utilsIsPlainObject2['default'](action)) {
	      throw new Error('Actions must be plain objects. Use custom middleware for async actions.');
	    }

	    if (isDispatching) {
	      throw new Error('Reducers may not dispatch actions.');
	    }

	    try {
	      isDispatching = true;
	      currentState = currentReducer(currentState, action);

	      if (currentState instanceof _utilsSideEffects.StateAndEffect) {
	        var effect = currentState.effect;
	        // Since side effects may dispatch at any time, don't run them
	        // immediately since we don't want cascading dispatches
	        setTimeout(function () {
	          return effect(dispatch, getState);
	        }, 0);

	        currentState = currentState.state;
	      }
	    } finally {
	      isDispatching = false;
	    }

	    listeners.slice().forEach(function (listener) {
	      return listener();
	    });
	    return action;
	  }

	  /**
	   * Returns the reducer currently used by the store to calculate the state.
	   *
	   * It is likely that you will only need this function if you implement a hot
	   * reloading mechanism for Redux.
	   *
	   * @returns {Function} The reducer used by the current store.
	   */
	  function getReducer() {
	    return currentReducer;
	  }

	  /**
	   * Replaces the reducer currently used by the store to calculate the state.
	   *
	   * You might need this if your app implements code splitting and you want to
	   * load some of the reducers dynamically. You might also need this if you
	   * implement a hot reloading mechanism for Redux.
	   *
	   * @param {Function} nextReducer The reducer for the store to use instead.
	   * @returns {void}
	   */
	  function replaceReducer(nextReducer) {
	    currentReducer = nextReducer;
	    dispatch({ type: ActionTypes.INIT });
	  }

	  // When a store is created, an "INIT" action is dispatched so that every
	  // reducer returns their initial state. This effectively populates
	  // the initial state tree.
	  dispatch({ type: ActionTypes.INIT });

	  return {
	    dispatch: dispatch,
	    subscribe: subscribe,
	    getState: getState,
	    getReducer: getReducer,
	    replaceReducer: replaceReducer
	  };
	}

/***/ },
/* 193 */
/***/ function(module, exports) {

	'use strict';

	exports.__esModule = true;
	exports['default'] = isPlainObject;
	var fnToString = function fnToString(fn) {
	  return Function.prototype.toString.call(fn);
	};

	/**
	 * @param {any} obj The object to inspect.
	 * @returns {boolean} True if the argument appears to be a plain object.
	 */

	function isPlainObject(obj) {
	  if (!obj || typeof obj !== 'object') {
	    return false;
	  }

	  var proto = typeof obj.constructor === 'function' ? Object.getPrototypeOf(obj) : Object.prototype;

	  if (proto === null) {
	    return true;
	  }

	  var constructor = proto.constructor;

	  return typeof constructor === 'function' && constructor instanceof constructor && fnToString(constructor) === fnToString(Object);
	}

	module.exports = exports['default'];

/***/ },
/* 194 */
/***/ function(module, exports) {

	"use strict";

	exports.__esModule = true;
	exports.StateAndEffect = StateAndEffect;
	exports.withSideEffect = withSideEffect;

	function StateAndEffect(state, effect) {
	  this.effect = effect;
	  this.state = state;
	}

	function withSideEffect(state, effect) {
	  if (state instanceof StateAndEffect) {
	    return new StateAndEffect(state.state, function () {
	      state.effect.apply(null, arguments);
	      effect.apply(null, arguments);
	    });
	  }
	  return new StateAndEffect(state, effect);
	}

/***/ },
/* 195 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {'use strict';

	exports.__esModule = true;
	exports['default'] = combineReducers;

	function _interopRequireDefault(obj) {
	  return obj && obj.__esModule ? obj : { 'default': obj };
	}

	var _createStore = __webpack_require__(192);

	var _utilsIsPlainObject = __webpack_require__(193);

	var _utilsIsPlainObject2 = _interopRequireDefault(_utilsIsPlainObject);

	var _utilsMapValues = __webpack_require__(196);

	var _utilsMapValues2 = _interopRequireDefault(_utilsMapValues);

	var _utilsPick = __webpack_require__(197);

	var _utilsPick2 = _interopRequireDefault(_utilsPick);

	var _utilsSideEffects = __webpack_require__(194);

	function getErrorMessage(key, action) {
	  var actionType = action && action.type;
	  var actionName = actionType && '"' + actionType.toString() + '"' || 'an action';

	  return 'Reducer "' + key + '" returned undefined handling ' + actionName + '. ' + 'To ignore an action, you must explicitly return the previous state.';
	}

	function verifyStateShape(initialState, currentState) {
	  var reducerKeys = Object.keys(currentState);

	  if (reducerKeys.length === 0) {
	    console.error('Store does not have a valid reducer. Make sure the argument passed ' + 'to combineReducers is an object whose values are reducers.');
	    return;
	  }

	  if (!_utilsIsPlainObject2['default'](initialState)) {
	    console.error('initialState has unexpected type of "' + ({}).toString.call(initialState).match(/\s([a-z|A-Z]+)/)[1] + '". Expected initialState to be an object with the following ' + ('keys: "' + reducerKeys.join('", "') + '"'));
	    return;
	  }

	  var unexpectedKeys = Object.keys(initialState).filter(function (key) {
	    return reducerKeys.indexOf(key) < 0;
	  });

	  if (unexpectedKeys.length > 0) {
	    console.error('Unexpected ' + (unexpectedKeys.length > 1 ? 'keys' : 'key') + ' ' + ('"' + unexpectedKeys.join('", "') + '" in initialState will be ignored. ') + ('Expected to find one of the known reducer keys instead: "' + reducerKeys.join('", "') + '"'));
	  }
	}

	/**
	 * Turns an object whose values are different reducer functions, into a single
	 * reducer function. It will call every child reducer, and gather their results
	 * into a single state object, whose keys correspond to the keys of the passed
	 * reducer functions.
	 *
	 * @param {Object} reducers An object whose values correspond to different
	 * reducer functions that need to be combined into one. One handy way to obtain
	 * it is to use ES6 `import * as reducers` syntax. The reducers may never return
	 * undefined for any action. Instead, they should return their initial state
	 * if the state passed to them was undefined, and the current state for any
	 * unrecognized action.
	 *
	 * @returns {Function} A reducer function that invokes every reducer inside the
	 * passed object, and builds a state object with the same shape.
	 */

	function combineReducers(reducers) {
	  var finalReducers = _utilsPick2['default'](reducers, function (val) {
	    return typeof val === 'function';
	  });

	  Object.keys(finalReducers).forEach(function (key) {
	    var reducer = finalReducers[key];
	    if (typeof reducer(undefined, { type: _createStore.ActionTypes.INIT }) === 'undefined') {
	      throw new Error('Reducer "' + key + '" returned undefined during initialization. ' + 'If the state passed to the reducer is undefined, you must ' + 'explicitly return the initial state. The initial state may ' + 'not be undefined.');
	    }

	    var type = Math.random().toString(36).substring(7).split('').join('.');
	    if (typeof reducer(undefined, { type: type }) === 'undefined') {
	      throw new Error('Reducer "' + key + '" returned undefined when probed with a random type. ' + ('Don\'t try to handle ' + _createStore.ActionTypes.INIT + ' or other actions in "redux/*" ') + 'namespace. They are considered private. Instead, you must return the ' + 'current state for any unknown actions, unless it is undefined, ' + 'in which case you must return the initial state, regardless of the ' + 'action type. The initial state may not be undefined.');
	    }
	  });

	  var defaultState = _utilsMapValues2['default'](finalReducers, function () {
	    return undefined;
	  });
	  var stateShapeVerified;

	  return function combination(state, action) {
	    if (state === undefined) state = defaultState;

	    var sideEffects = [];
	    var finalState = _utilsMapValues2['default'](finalReducers, function (reducer, key) {
	      var newState = reducer(state[key], action);
	      if (typeof newState === 'undefined') {
	        throw new Error(getErrorMessage(key, action));
	      } else if (newState instanceof _utilsSideEffects.StateAndEffect) {
	        sideEffects.push(newState.effect);
	        return newState.state;
	      }
	      return newState;
	    });

	    if (
	    // Node-like CommonJS environments (Browserify, Webpack)
	    typeof process !== 'undefined' && typeof process.env !== 'undefined' && process.env.NODE_ENV !== 'production' ||
	    // React Native
	    typeof __DEV__ !== 'undefined' && __DEV__ //eslint-disable-line no-undef
	    ) {
	        if (!stateShapeVerified) {
	          verifyStateShape(state, finalState);
	          stateShapeVerified = true;
	        }
	      }

	    if (sideEffects.length) {
	      return _utilsSideEffects.withSideEffect(finalState, function (dispatch, getState) {
	        sideEffects.forEach(function (e) {
	          return e(dispatch, getState);
	        });
	      });
	    }
	    return finalState;
	  };
	}

	module.exports = exports['default'];
	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 196 */
/***/ function(module, exports) {

	/**
	 * Applies a function to every key-value pair inside an object.
	 *
	 * @param {Object} obj The source object.
	 * @param {Function} fn The mapper function that receives the value and the key.
	 * @returns {Object} A new object that contains the mapped values for the keys.
	 */
	"use strict";

	exports.__esModule = true;
	exports["default"] = mapValues;

	function mapValues(obj, fn) {
	  return Object.keys(obj).reduce(function (result, key) {
	    result[key] = fn(obj[key], key);
	    return result;
	  }, {});
	}

	module.exports = exports["default"];

/***/ },
/* 197 */
/***/ function(module, exports) {

	/**
	 * Picks key-value pairs from an object where values satisfy a predicate.
	 *
	 * @param {Object} obj The object to pick from.
	 * @param {Function} fn The predicate the values must satisfy to be copied.
	 * @returns {Object} The object with the values that satisfied the predicate.
	 */
	"use strict";

	exports.__esModule = true;
	exports["default"] = pick;

	function pick(obj, fn) {
	  return Object.keys(obj).reduce(function (result, key) {
	    if (fn(obj[key])) {
	      result[key] = obj[key];
	    }
	    return result;
	  }, {});
	}

	module.exports = exports["default"];

/***/ },
/* 198 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	exports.__esModule = true;
	exports['default'] = bindActionCreators;

	function _interopRequireDefault(obj) {
	  return obj && obj.__esModule ? obj : { 'default': obj };
	}

	var _utilsMapValues = __webpack_require__(196);

	var _utilsMapValues2 = _interopRequireDefault(_utilsMapValues);

	function bindActionCreator(actionCreator, dispatch) {
	  return function () {
	    return dispatch(actionCreator.apply(undefined, arguments));
	  };
	}

	/**
	 * Turns an object whose values are action creators, into an object with the
	 * same keys, but with every function wrapped into a `dispatch` call so they
	 * may be invoked directly. This is just a convenience method, as you can call
	 * `store.dispatch(MyActionCreators.doSomething())` yourself just fine.
	 *
	 * For convenience, you can also pass a single function as the first argument,
	 * and get a function in return.
	 *
	 * @param {Function|Object} actionCreators An object whose values are action
	 * creator functions. One handy way to obtain it is to use ES6 `import * as`
	 * syntax. You may also pass a single function.
	 *
	 * @param {Function} dispatch The `dispatch` function available on your Redux
	 * store.
	 *
	 * @returns {Function|Object} The object mimicking the original object, but with
	 * every action creator wrapped into the `dispatch` call. If you passed a
	 * function as `actionCreators`, the return value will also be a single
	 * function.
	 */

	function bindActionCreators(actionCreators, dispatch) {
	  if (typeof actionCreators === 'function') {
	    return bindActionCreator(actionCreators, dispatch);
	  }

	  if (typeof actionCreators !== 'object' || actionCreators == null) {
	    throw new Error('bindActionCreators expected an object or a function, instead received ' + typeof actionCreators + '. ' + 'Did you write "import ActionCreators from" instead of "import * as ActionCreators from"?');
	  }

	  return _utilsMapValues2['default'](actionCreators, function (actionCreator) {
	    return bindActionCreator(actionCreator, dispatch);
	  });
	}

	module.exports = exports['default'];

/***/ },
/* 199 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	exports.__esModule = true;

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

	var _react = __webpack_require__(2);

	var _react2 = _interopRequireDefault(_react);

	var _componentsCreateAll = __webpack_require__(200);

	var _componentsCreateAll2 = _interopRequireDefault(_componentsCreateAll);

	var _createAll = _componentsCreateAll2['default'](_react2['default']);

	var Provider = _createAll.Provider;
	var Connector = _createAll.Connector;
	var provide = _createAll.provide;
	var connect = _createAll.connect;
	exports.Provider = Provider;
	exports.Connector = Connector;
	exports.provide = provide;
	exports.connect = connect;

/***/ },
/* 200 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	exports.__esModule = true;
	exports['default'] = createAll;

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

	var _createProvider = __webpack_require__(201);

	var _createProvider2 = _interopRequireDefault(_createProvider);

	var _createProvideDecorator = __webpack_require__(203);

	var _createProvideDecorator2 = _interopRequireDefault(_createProvideDecorator);

	var _createConnector = __webpack_require__(205);

	var _createConnector2 = _interopRequireDefault(_createConnector);

	var _createConnectDecorator = __webpack_require__(209);

	var _createConnectDecorator2 = _interopRequireDefault(_createConnectDecorator);

	function createAll(React) {
	  // Wrapper components
	  var Provider = _createProvider2['default'](React);
	  var Connector = _createConnector2['default'](React);

	  // Higher-order components (decorators)
	  var provide = _createProvideDecorator2['default'](React, Provider);
	  var connect = _createConnectDecorator2['default'](React, Connector);

	  return { Provider: Provider, Connector: Connector, provide: provide, connect: connect };
	}

	module.exports = exports['default'];

/***/ },
/* 201 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	exports.__esModule = true;

	var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ('value' in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

	exports['default'] = createProvider;

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	function _inherits(subClass, superClass) { if (typeof superClass !== 'function' && superClass !== null) { throw new TypeError('Super expression must either be null or a function, not ' + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) subClass.__proto__ = superClass; }

	var _utilsCreateStoreShape = __webpack_require__(202);

	var _utilsCreateStoreShape2 = _interopRequireDefault(_utilsCreateStoreShape);

	function createProvider(React) {
	  var Component = React.Component;
	  var PropTypes = React.PropTypes;

	  var storeShape = _utilsCreateStoreShape2['default'](PropTypes);

	  return (function (_Component) {
	    function Provider(props, context) {
	      _classCallCheck(this, Provider);

	      _Component.call(this, props, context);
	      this.state = { store: props.store };
	    }

	    _inherits(Provider, _Component);

	    Provider.prototype.getChildContext = function getChildContext() {
	      return { store: this.state.store };
	    };

	    Provider.prototype.componentWillReceiveProps = function componentWillReceiveProps(nextProps) {
	      var store = this.state.store;
	      var nextStore = nextProps.store;

	      if (store !== nextStore) {
	        var nextReducer = nextStore.getReducer();
	        store.replaceReducer(nextReducer);
	      }
	    };

	    Provider.prototype.render = function render() {
	      var children = this.props.children;

	      return children();
	    };

	    _createClass(Provider, null, [{
	      key: 'childContextTypes',
	      value: {
	        store: storeShape.isRequired
	      },
	      enumerable: true
	    }, {
	      key: 'propTypes',
	      value: {
	        children: PropTypes.func.isRequired
	      },
	      enumerable: true
	    }]);

	    return Provider;
	  })(Component);
	}

	module.exports = exports['default'];

/***/ },
/* 202 */
/***/ function(module, exports) {

	"use strict";

	exports.__esModule = true;
	exports["default"] = createStoreShape;

	function createStoreShape(PropTypes) {
	  return PropTypes.shape({
	    subscribe: PropTypes.func.isRequired,
	    dispatch: PropTypes.func.isRequired,
	    getState: PropTypes.func.isRequired
	  });
	}

	module.exports = exports["default"];

/***/ },
/* 203 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	exports.__esModule = true;

	var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ('value' in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

	exports['default'] = createProvideDecorator;

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	function _inherits(subClass, superClass) { if (typeof superClass !== 'function' && superClass !== null) { throw new TypeError('Super expression must either be null or a function, not ' + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) subClass.__proto__ = superClass; }

	var _utilsGetDisplayName = __webpack_require__(204);

	var _utilsGetDisplayName2 = _interopRequireDefault(_utilsGetDisplayName);

	function createProvideDecorator(React, Provider) {
	  var Component = React.Component;

	  return function provide(store) {
	    return function (DecoratedComponent) {
	      return (function (_Component) {
	        function ProviderDecorator() {
	          _classCallCheck(this, ProviderDecorator);

	          _Component.apply(this, arguments);
	        }

	        _inherits(ProviderDecorator, _Component);

	        ProviderDecorator.prototype.render = function render() {
	          var _this = this;

	          return React.createElement(
	            Provider,
	            { store: store },
	            function () {
	              return React.createElement(DecoratedComponent, _this.props);
	            }
	          );
	        };

	        _createClass(ProviderDecorator, null, [{
	          key: 'displayName',
	          value: 'Provider(' + _utilsGetDisplayName2['default'](DecoratedComponent) + ')',
	          enumerable: true
	        }, {
	          key: 'DecoratedComponent',
	          value: DecoratedComponent,
	          enumerable: true
	        }]);

	        return ProviderDecorator;
	      })(Component);
	    };
	  };
	}

	module.exports = exports['default'];

/***/ },
/* 204 */
/***/ function(module, exports) {

	'use strict';

	exports.__esModule = true;
	exports['default'] = getDisplayName;

	function getDisplayName(Component) {
	  return Component.displayName || Component.name || 'Component';
	}

	module.exports = exports['default'];

/***/ },
/* 205 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	exports.__esModule = true;

	var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

	var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ('value' in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

	exports['default'] = createConnector;

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	function _inherits(subClass, superClass) { if (typeof superClass !== 'function' && superClass !== null) { throw new TypeError('Super expression must either be null or a function, not ' + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) subClass.__proto__ = superClass; }

	var _utilsCreateStoreShape = __webpack_require__(202);

	var _utilsCreateStoreShape2 = _interopRequireDefault(_utilsCreateStoreShape);

	var _utilsShallowEqual = __webpack_require__(206);

	var _utilsShallowEqual2 = _interopRequireDefault(_utilsShallowEqual);

	var _utilsIsPlainObject = __webpack_require__(207);

	var _utilsIsPlainObject2 = _interopRequireDefault(_utilsIsPlainObject);

	var _invariant = __webpack_require__(208);

	var _invariant2 = _interopRequireDefault(_invariant);

	function createConnector(React) {
	  var Component = React.Component;
	  var PropTypes = React.PropTypes;

	  var storeShape = _utilsCreateStoreShape2['default'](PropTypes);

	  return (function (_Component) {
	    function Connector(props, context) {
	      _classCallCheck(this, Connector);

	      _Component.call(this, props, context);
	      this.state = this.selectState(props, context);
	    }

	    _inherits(Connector, _Component);

	    Connector.prototype.shouldComponentUpdate = function shouldComponentUpdate(nextProps, nextState) {
	      return !this.isSliceEqual(this.state.slice, nextState.slice) || !_utilsShallowEqual2['default'](this.props, nextProps);
	    };

	    Connector.prototype.isSliceEqual = function isSliceEqual(slice, nextSlice) {
	      var isRefEqual = slice === nextSlice;
	      if (isRefEqual) {
	        return true;
	      } else if (typeof slice !== 'object' || typeof nextSlice !== 'object') {
	        return isRefEqual;
	      }
	      return _utilsShallowEqual2['default'](slice, nextSlice);
	    };

	    Connector.prototype.componentDidMount = function componentDidMount() {
	      this.unsubscribe = this.context.store.subscribe(this.handleChange.bind(this));
	      this.handleChange();
	    };

	    Connector.prototype.componentWillReceiveProps = function componentWillReceiveProps(nextProps) {
	      if (nextProps.select !== this.props.select) {
	        // Force the state slice recalculation
	        this.handleChange(nextProps);
	      }
	    };

	    Connector.prototype.componentWillUnmount = function componentWillUnmount() {
	      this.unsubscribe();
	    };

	    Connector.prototype.handleChange = function handleChange() {
	      var props = arguments[0] === undefined ? this.props : arguments[0];

	      var nextState = this.selectState(props, this.context);
	      if (!this.isSliceEqual(this.state.slice, nextState.slice)) {
	        this.setState(nextState);
	      }
	    };

	    Connector.prototype.selectState = function selectState(props, context) {
	      var state = context.store.getState();
	      var slice = props.select(state);

	      _invariant2['default'](_utilsIsPlainObject2['default'](slice), 'The return value of `select` prop must be an object. Instead received %s.', slice);

	      return { slice: slice };
	    };

	    Connector.prototype.render = function render() {
	      var children = this.props.children;
	      var slice = this.state.slice;
	      var dispatch = this.context.store.dispatch;

	      return children(_extends({ dispatch: dispatch }, slice));
	    };

	    _createClass(Connector, null, [{
	      key: 'contextTypes',
	      value: {
	        store: storeShape.isRequired
	      },
	      enumerable: true
	    }, {
	      key: 'propTypes',
	      value: {
	        children: PropTypes.func.isRequired,
	        select: PropTypes.func.isRequired
	      },
	      enumerable: true
	    }, {
	      key: 'defaultProps',
	      value: {
	        select: function select(state) {
	          return state;
	        }
	      },
	      enumerable: true
	    }]);

	    return Connector;
	  })(Component);
	}

	module.exports = exports['default'];


/***/ },
/* 206 */
/***/ function(module, exports) {

	"use strict";

	exports.__esModule = true;
	exports["default"] = shallowEqual;

	function shallowEqual(objA, objB) {
	  if (objA === objB) {
	    return true;
	  }

	  var keysA = Object.keys(objA);
	  var keysB = Object.keys(objB);

	  if (keysA.length !== keysB.length) {
	    return false;
	  }

	  // Test for A's keys different from B.
	  var hasOwn = Object.prototype.hasOwnProperty;
	  for (var i = 0; i < keysA.length; i++) {
	    if (!hasOwn.call(objB, keysA[i]) || objA[keysA[i]] !== objB[keysA[i]]) {
	      return false;
	    }
	  }

	  return true;
	}

	module.exports = exports["default"];

/***/ },
/* 207 */
/***/ function(module, exports) {

	'use strict';

	exports.__esModule = true;
	exports['default'] = isPlainObject;

	function isPlainObject(obj) {
	  return obj ? typeof obj === 'object' && Object.getPrototypeOf(obj) === Object.prototype : false;
	}

	module.exports = exports['default'];

/***/ },
/* 208 */
/***/ function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(process) {/**
	 * Copyright 2013-2015, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * LICENSE file in the root directory of this source tree. An additional grant
	 * of patent rights can be found in the PATENTS file in the same directory.
	 *
	 * @providesModule invariant
	 */

	'use strict';

	/**
	 * Use invariant() to assert state which your program assumes to be true.
	 *
	 * Provide sprintf-style format (only %s is supported) and arguments
	 * to provide information about what broke and what you were
	 * expecting.
	 *
	 * The invariant message will be stripped in production, but the invariant
	 * will remain to ensure logic does not differ in production.
	 */

	var invariant = function(condition, format, a, b, c, d, e, f) {
	  if (process.env.NODE_ENV !== 'production') {
	    if (format === undefined) {
	      throw new Error('invariant requires an error message argument');
	    }
	  }

	  if (!condition) {
	    var error;
	    if (format === undefined) {
	      error = new Error(
	        'Minified exception occurred; use the non-minified dev environment ' +
	        'for the full error message and additional helpful warnings.'
	      );
	    } else {
	      var args = [a, b, c, d, e, f];
	      var argIndex = 0;
	      error = new Error(
	        'Invariant Violation: ' +
	        format.replace(/%s/g, function() { return args[argIndex++]; })
	      );
	    }

	    error.framesToPop = 1; // we don't care about invariant's own frame
	    throw error;
	  }
	};

	module.exports = invariant;

	/* WEBPACK VAR INJECTION */}.call(exports, __webpack_require__(4)))

/***/ },
/* 209 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	exports.__esModule = true;

	var _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; };

	var _createClass = (function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ('value' in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; })();

	exports['default'] = createConnectDecorator;

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { 'default': obj }; }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError('Cannot call a class as a function'); } }

	function _inherits(subClass, superClass) { if (typeof superClass !== 'function' && superClass !== null) { throw new TypeError('Super expression must either be null or a function, not ' + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) subClass.__proto__ = superClass; }

	var _utilsGetDisplayName = __webpack_require__(204);

	var _utilsGetDisplayName2 = _interopRequireDefault(_utilsGetDisplayName);

	var _utilsShallowEqualScalar = __webpack_require__(210);

	var _utilsShallowEqualScalar2 = _interopRequireDefault(_utilsShallowEqualScalar);

	function createConnectDecorator(React, Connector) {
	  var Component = React.Component;

	  return function connect(select) {
	    return function (DecoratedComponent) {
	      return (function (_Component) {
	        function ConnectorDecorator() {
	          _classCallCheck(this, ConnectorDecorator);

	          _Component.apply(this, arguments);
	        }

	        _inherits(ConnectorDecorator, _Component);

	        ConnectorDecorator.prototype.shouldComponentUpdate = function shouldComponentUpdate(nextProps) {
	          return !_utilsShallowEqualScalar2['default'](this.props, nextProps);
	        };

	        ConnectorDecorator.prototype.render = function render() {
	          var _this = this;

	          return React.createElement(
	            Connector,
	            { select: function (state) {
	                return select(state, _this.props);
	              } },
	            function (stuff) {
	              return React.createElement(DecoratedComponent, _extends({}, stuff, _this.props));
	            }
	          );
	        };

	        _createClass(ConnectorDecorator, null, [{
	          key: 'displayName',
	          value: 'Connector(' + _utilsGetDisplayName2['default'](DecoratedComponent) + ')',
	          enumerable: true
	        }, {
	          key: 'DecoratedComponent',
	          value: DecoratedComponent,
	          enumerable: true
	        }]);

	        return ConnectorDecorator;
	      })(Component);
	    };
	  };
	}

	module.exports = exports['default'];

/***/ },
/* 210 */
/***/ function(module, exports) {

	'use strict';

	exports.__esModule = true;
	exports['default'] = shallowEqualScalar;

	function shallowEqualScalar(objA, objB) {
	  if (objA === objB) {
	    return true;
	  }

	  if (typeof objA !== 'object' || objA === null || typeof objB !== 'object' || objB === null) {
	    return false;
	  }

	  var keysA = Object.keys(objA);
	  var keysB = Object.keys(objB);

	  if (keysA.length !== keysB.length) {
	    return false;
	  }

	  // Test for A's keys different from B.
	  var hasOwn = Object.prototype.hasOwnProperty;
	  for (var i = 0; i < keysA.length; i++) {
	    if (!hasOwn.call(objB, keysA[i])) {
	      return false;
	    }

	    var valA = objA[keysA[i]];
	    var valB = objB[keysA[i]];

	    if (valA !== valB || typeof valA === 'object' || typeof valB === 'object') {
	      return false;
	    }
	  }

	  return true;
	}

	module.exports = exports['default'];

/***/ },
/* 211 */,
/* 212 */,
/* 213 */,
/* 214 */,
/* 215 */,
/* 216 */,
/* 217 */,
/* 218 */,
/* 219 */,
/* 220 */,
/* 221 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var React = __webpack_require__(2);

	var _require = __webpack_require__(189);

	var createStore = _require.createStore;

	var _require2 = __webpack_require__(199);

	var Provider = _require2.Provider;

	var connect = __webpack_require__(222);
	var actions = __webpack_require__(225);
	var reducers = __webpack_require__(227);
	var ds = __webpack_require__(223);
	var dom = React.DOM;

	// Specify a static DataScript query, and `this.props.location` is
	// automatically bound! And this component is automatically rerendered
	// when it changes!
	var App = React.createClass({
	  displayName: 'App',

	  statics: {
	    localQueries: {
	      location: '[:find ?l :where [?e "name" "James"] [?e "location" ?l]]'
	    },
	    actions: actions
	  },

	  render: function render() {
	    var actions = this.props.actions;

	    return dom.div({ className: 'app' }, dom.div(null, "User location: " + this.props.location), 'Change it: ', dom.input({ onChange: function onChange(e) {
	        return actions.updateUserLocation(e.target.value);
	      },
	      value: this.props.location }));
	  }
	});

	App = connect(App);

	// Create a basic DataScript database
	var db = ds.db_with(ds.empty_db(), [[":db/add", 1, "name", "James"], [":db/add", 1, "height", 6], [":db/add", 1, "location", "Richmond"]]);
	var store = createStore(reducers, db);

	React.render(React.createElement(Provider, { store: store }, function () {
	  return React.createElement(App);
	}), document.querySelector("#mount"));

/***/ },
/* 222 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var React = __webpack_require__(2);
	var PropTypes = React.PropTypes;

	var _require = __webpack_require__(189);

	var bindActionCreators = _require.bindActionCreators;

	var ds = __webpack_require__(223);
	var mergeObj = __webpack_require__(224);

	var storeShape = PropTypes.shape({
	  subscribe: PropTypes.func.isRequired,
	  dispatch: PropTypes.func.isRequired,
	  getState: PropTypes.func.isRequired
	});

	function connect(component) {
	  return React.createClass({
	    statics: {
	      dumb: component
	    },

	    contextTypes: {
	      store: storeShape.isRequired
	    },

	    getInitialState: function getInitialState() {
	      var store = this.context.store;

	      return {
	        actions: component.actions ? { actions: bindActionCreators(component.actions, store.dispatch) } : null,
	        namedActions: component.namedActions ? bindActionCreators(component.namedActions, store.dispatch) : null,
	        queryResults: this.queryState()
	      };
	    },

	    componentDidMount: function componentDidMount() {
	      this.unsubscribe = this.context.store.subscribe(this.handleChange);
	    },

	    componentWillUnmount: function componentWillUnmount() {
	      this.unsubscribe();
	    },

	    handleChange: function handleChange() {
	      var _this = this;

	      var results = this.queryState();
	      // This actually won't work with DataScript queries anymore, the
	      // results will always be fresh. We would need a different way
	      // to track how changes occur in the DB to optimize rendering.
	      var changed = Object.keys(this.state.queryResults).some(function (k) {
	        return results[k] !== _this.state.queryResults[k];
	      });

	      if (changed) {
	        this.setState({ queryResults: results });
	      }
	    },

	    queryState: function queryState() {
	      var _this2 = this;

	      if (component.localQueries) {
	        var _ret = (function () {
	          var db = _this2.context.store.getState();
	          var queries = {};
	          Object.keys(component.localQueries).forEach(function (name) {
	            var query = component.localQueries[name];
	            queries[name] = ds.q(query, db);
	          });
	          return {
	            v: queries
	          };
	        })();

	        if (typeof _ret === 'object') return _ret.v;
	      }

	      return null;
	    },

	    render: function render() {
	      return React.createElement(component, mergeObj(this.props, this.state.actions || {}, this.state.namedActions || {}, this.state.queryResults || {}));
	    }
	  });
	}

	module.exports = connect;

/***/ },
/* 223 */
/***/ function(module, exports, __webpack_require__) {

	/**
	 * Datascript v0.11.5
	 *
	 * Copyright 2014-2015 Nikita Prokopov
	 *
	 * Licensed under Eclipse Public License;
	 * you may not use this file except in compliance with the License.
	 * You may obtain a copy of the License at
	 *
	 * https://github.com/tonsky/datascript/blob/master/LICENSE
	 *
	 * Unless required by applicable law or agreed to in writing, software
	 * distributed under the License is distributed on an "AS IS" BASIS,
	 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
	 * See the License for the specific language governing permissions and
	 * limitations under the License.
	 */

	(function (definition) {
	    // This file will function properly as a <script> tag, or a module
	    // using CommonJS and NodeJS or RequireJS module formats. In
	    // Common/Node/RequireJS, the module exports the mori API and when
	    // executed as a simple <script>, it creates a mori global
	    // instead.

	    // Wrapper gratefully adapted from:
	    // https://github.com/kriskowal/q/blob/v1/q.js
	    // https://github.com/swannodette/mori/blob/master/support/wrapper.js

	    // CommonJS
	    if (true) {
	        module.exports = definition();

	    // RequireJS
	    } else if (typeof define === "function" && define.amd) {
	        define(definition);

	    // <script>
	    } else {
	        datascript = definition();
	    }
	})(function () {
	    return function () {
	if(typeof Math.imul == "undefined" || (Math.imul(0xffffffff,5) == 0)) {
	    Math.imul = function (a, b) {
	        var ah  = (a >>> 16) & 0xffff;
	        var al = a & 0xffff;
	        var bh  = (b >>> 16) & 0xffff;
	        var bl = b & 0xffff;
	        // the shift by 0 fixes the sign on the high part
	        // the final |0 converts the unsigned value into a signed value
	        return ((al * bl) + (((ah * bl + al * bh) << 16) >>> 0)|0);
	    }
	}


	var g,ba=this;
	function ca(b){var a=typeof b;if("object"==a)if(b){if(b instanceof Array)return"array";if(b instanceof Object)return a;var c=Object.prototype.toString.call(b);if("[object Window]"==c)return"object";if("[object Array]"==c||"number"==typeof b.length&&"undefined"!=typeof b.splice&&"undefined"!=typeof b.propertyIsEnumerable&&!b.propertyIsEnumerable("splice"))return"array";if("[object Function]"==c||"undefined"!=typeof b.call&&"undefined"!=typeof b.propertyIsEnumerable&&!b.propertyIsEnumerable("call"))return"function"}else return"null";else if("function"==
	a&&"undefined"==typeof b.call)return"object";return a}var da="closure_uid_"+(1E9*Math.random()>>>0),ea=0;function fa(b,a){var c=b.split("."),d=ba;c[0]in d||!d.execScript||d.execScript("var "+c[0]);for(var e;c.length&&(e=c.shift());)c.length||void 0===a?d=d[e]?d[e]:d[e]={}:d[e]=a};function ga(b){return Array.prototype.join.call(arguments,"")};function ha(b,a){for(var c in b)a.call(void 0,b[c],c,b)};function ia(b,a){null!=b&&this.append.apply(this,arguments)}g=ia.prototype;g.Bb="";g.set=function(b){this.Bb=""+b};g.append=function(b,a,c){this.Bb+=b;if(null!=a)for(var d=1;d<arguments.length;d++)this.Bb+=arguments[d];return this};g.clear=function(){this.Bb=""};g.toString=function(){return this.Bb};function ja(b,a){b.sort(a||ka)}function la(b,a){for(var c=0;c<b.length;c++)b[c]={index:c,value:b[c]};var d=a||ka;ja(b,function(a,b){return d(a.value,b.value)||a.index-b.index});for(c=0;c<b.length;c++)b[c]=b[c].value}function ka(b,a){return b>a?1:b<a?-1:0};if("undefined"===typeof ma)var ma=function(){throw Error("No *print-fn* fn set for evaluation environment");};var na=!0,oa=null;if("undefined"===typeof pa)var pa=null;function qa(){return new m(null,5,[ra,!0,sa,!0,ta,!1,ua,!1,wa,null],null)}
	function ya(){na=!1;ma=function(){function b(b){var d=null;if(0<arguments.length){for(var d=0,e=Array(arguments.length-0);d<e.length;)e[d]=arguments[d+0],++d;d=new za(e,0)}return a.call(this,d)}function a(a){return console.log.apply(console,Aa?Ba(a):Ca.call(null,a))}b.H=0;b.F=function(b){b=q(b);return a(b)};b.j=a;return b}()}function r(b){return null!=b&&!1!==b}function Da(b){return null==b}function Ea(b){return b instanceof Array}function Fa(b){return r(b)?!1:!0}
	function Ga(b,a){return b[ca(null==a?null:a)]?!0:b._?!0:!1}function Ha(b){return null==b?null:b.constructor}function Ia(b,a){var c=Ha(a),c=r(r(c)?c.Jd:c)?c.Id:ca(a);return Error(["No protocol method ",b," defined for type ",c,": ",a].join(""))}function Ka(b){var a=b.Id;return r(a)?a:""+u(b)}var La="undefined"!==typeof Symbol&&"function"===ca(Symbol)?Symbol.iterator:"@@iterator";
	function Ma(){switch(arguments.length){case 1:return Array(arguments[0]);case 2:return Array(arguments[1]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function Oa(b){return Array(b)}function Qa(b){for(var a=b.length,c=Array(a),d=0;;)if(d<a)c[d]=b[d],d+=1;else break;return c}function Ca(){switch(arguments.length){case 1:return Ba(arguments[0]);case 2:return Ba(arguments[1]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}
	function Aa(b){return Ba(b)}function Ba(b){function a(a,b){a.push(b);return a}var c=[];return v?v(a,c,b):Ra.call(null,a,c,b)}
	var Sa={},Ta={},Va=function Va(a){if(a?a.L:a)return a.L(a);var c;c=Va[ca(null==a?null:a)];if(!c&&(c=Va._,!c))throw Ia("ICounted.-count",a);return c.call(null,a)},Wa=function Wa(a){if(a?a.ca:a)return a.ca(a);var c;c=Wa[ca(null==a?null:a)];if(!c&&(c=Wa._,!c))throw Ia("IEmptyableCollection.-empty",a);return c.call(null,a)},Xa={},Ya=function Ya(a,c){if(a?a.O:a)return a.O(a,c);var d;d=Ya[ca(null==a?null:a)];if(!d&&(d=Ya._,!d))throw Ia("ICollection.-conj",a);return d.call(null,a,c)},Za={},w=function w(){switch(arguments.length){case 2:return w.b(arguments[0],
	arguments[1]);case 3:return w.g(arguments[0],arguments[1],arguments[2]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}};w.b=function(b,a){if(b?b.M:b)return b.M(b,a);var c;c=w[ca(null==b?null:b)];if(!c&&(c=w._,!c))throw Ia("IIndexed.-nth",b);return c.call(null,b,a)};w.g=function(b,a,c){if(b?b.xa:b)return b.xa(b,a,c);var d;d=w[ca(null==b?null:b)];if(!d&&(d=w._,!d))throw Ia("IIndexed.-nth",b);return d.call(null,b,a,c)};w.H=3;
	var ab={},bb=function bb(a){if(a?a.ia:a)return a.ia(a);var c;c=bb[ca(null==a?null:a)];if(!c&&(c=bb._,!c))throw Ia("ISeq.-first",a);return c.call(null,a)},cb=function cb(a){if(a?a.ma:a)return a.ma(a);var c;c=cb[ca(null==a?null:a)];if(!c&&(c=cb._,!c))throw Ia("ISeq.-rest",a);return c.call(null,a)},db={},fb={},hb=function hb(){switch(arguments.length){case 2:return hb.b(arguments[0],arguments[1]);case 3:return hb.g(arguments[0],arguments[1],arguments[2]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));
	}};hb.b=function(b,a){if(b?b.C:b)return b.C(b,a);var c;c=hb[ca(null==b?null:b)];if(!c&&(c=hb._,!c))throw Ia("ILookup.-lookup",b);return c.call(null,b,a)};hb.g=function(b,a,c){if(b?b.B:b)return b.B(b,a,c);var d;d=hb[ca(null==b?null:b)];if(!d&&(d=hb._,!d))throw Ia("ILookup.-lookup",b);return d.call(null,b,a,c)};hb.H=3;
	var ib=function ib(a,c){if(a?a.Nb:a)return a.Nb(a,c);var d;d=ib[ca(null==a?null:a)];if(!d&&(d=ib._,!d))throw Ia("IAssociative.-contains-key?",a);return d.call(null,a,c)},kb=function kb(a,c,d){if(a?a.T:a)return a.T(a,c,d);var e;e=kb[ca(null==a?null:a)];if(!e&&(e=kb._,!e))throw Ia("IAssociative.-assoc",a);return e.call(null,a,c,d)},lb={},mb=function mb(a,c){if(a?a.W:a)return a.W(a,c);var d;d=mb[ca(null==a?null:a)];if(!d&&(d=mb._,!d))throw Ia("IMap.-dissoc",a);return d.call(null,a,c)},nb={},ob=function ob(a){if(a?
	a.gc:a)return a.gc(a);var c;c=ob[ca(null==a?null:a)];if(!c&&(c=ob._,!c))throw Ia("IMapEntry.-key",a);return c.call(null,a)},pb=function pb(a){if(a?a.hc:a)return a.hc(a);var c;c=pb[ca(null==a?null:a)];if(!c&&(c=pb._,!c))throw Ia("IMapEntry.-val",a);return c.call(null,a)},qb={},rb=function rb(a,c){if(a?a.zc:a)return a.zc(a,c);var d;d=rb[ca(null==a?null:a)];if(!d&&(d=rb._,!d))throw Ia("ISet.-disjoin",a);return d.call(null,a,c)},sb=function sb(a){if(a?a.Db:a)return a.Db(a);var c;c=sb[ca(null==a?null:
	a)];if(!c&&(c=sb._,!c))throw Ia("IStack.-peek",a);return c.call(null,a)},tb={},ub=function ub(a,c,d){if(a?a.Qb:a)return a.Qb(a,c,d);var e;e=ub[ca(null==a?null:a)];if(!e&&(e=ub._,!e))throw Ia("IVector.-assoc-n",a);return e.call(null,a,c,d)},vb=function vb(a){if(a?a.wc:a)return a.wc(a);var c;c=vb[ca(null==a?null:a)];if(!c&&(c=vb._,!c))throw Ia("IDeref.-deref",a);return c.call(null,a)},wb={},yb=function yb(a){if(a?a.N:a)return a.N(a);var c;c=yb[ca(null==a?null:a)];if(!c&&(c=yb._,!c))throw Ia("IMeta.-meta",
	a);return c.call(null,a)},zb={},Ab=function Ab(a,c){if(a?a.P:a)return a.P(a,c);var d;d=Ab[ca(null==a?null:a)];if(!d&&(d=Ab._,!d))throw Ia("IWithMeta.-with-meta",a);return d.call(null,a,c)},Cb={},Db=function Db(){switch(arguments.length){case 2:return Db.b(arguments[0],arguments[1]);case 3:return Db.g(arguments[0],arguments[1],arguments[2]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}};
	Db.b=function(b,a){if(b?b.ja:b)return b.ja(b,a);var c;c=Db[ca(null==b?null:b)];if(!c&&(c=Db._,!c))throw Ia("IReduce.-reduce",b);return c.call(null,b,a)};Db.g=function(b,a,c){if(b?b.ka:b)return b.ka(b,a,c);var d;d=Db[ca(null==b?null:b)];if(!d&&(d=Db._,!d))throw Ia("IReduce.-reduce",b);return d.call(null,b,a,c)};Db.H=3;
	var Eb=function Eb(a,c,d){if(a?a.ac:a)return a.ac(a,c,d);var e;e=Eb[ca(null==a?null:a)];if(!e&&(e=Eb._,!e))throw Ia("IKVReduce.-kv-reduce",a);return e.call(null,a,c,d)},Fb=function Fb(a,c){if(a?a.u:a)return a.u(a,c);var d;d=Fb[ca(null==a?null:a)];if(!d&&(d=Fb._,!d))throw Ia("IEquiv.-equiv",a);return d.call(null,a,c)},Ib=function Ib(a){if(a?a.I:a)return a.I(a);var c;c=Ib[ca(null==a?null:a)];if(!c&&(c=Ib._,!c))throw Ia("IHash.-hash",a);return c.call(null,a)},Jb={},Kb=function Kb(a){if(a?a.J:a)return a.J(a);
	var c;c=Kb[ca(null==a?null:a)];if(!c&&(c=Kb._,!c))throw Ia("ISeqable.-seq",a);return c.call(null,a)},Lb={},Mb={},Nb=function Nb(a){if(a?a.Ua:a)return a.Ua(a);var c;c=Nb[ca(null==a?null:a)];if(!c&&(c=Nb._,!c))throw Ia("IReversible.-rseq",a);return c.call(null,a)},Ob=function Ob(a,c){if(a?a.gd:a)return a.gd(0,c);var d;d=Ob[ca(null==a?null:a)];if(!d&&(d=Ob._,!d))throw Ia("IWriter.-write",a);return d.call(null,a,c)},Pb={},Qb=function Qb(a,c,d){if(a?a.D:a)return a.D(a,c,d);var e;e=Qb[ca(null==a?null:a)];
	if(!e&&(e=Qb._,!e))throw Ia("IPrintWithWriter.-pr-writer",a);return e.call(null,a,c,d)},Rb=function Rb(a,c,d){if(a?a.fd:a)return a.fd(0,c,d);var e;e=Rb[ca(null==a?null:a)];if(!e&&(e=Rb._,!e))throw Ia("IWatchable.-notify-watches",a);return e.call(null,a,c,d)},Sb=function Sb(a){if(a?a.$b:a)return a.$b(a);var c;c=Sb[ca(null==a?null:a)];if(!c&&(c=Sb._,!c))throw Ia("IEditableCollection.-as-transient",a);return c.call(null,a)},Ub=function Ub(a,c){if(a?a.Pb:a)return a.Pb(a,c);var d;d=Ub[ca(null==a?null:
	a)];if(!d&&(d=Ub._,!d))throw Ia("ITransientCollection.-conj!",a);return d.call(null,a,c)},Vb=function Vb(a){if(a?a.bc:a)return a.bc(a);var c;c=Vb[ca(null==a?null:a)];if(!c&&(c=Vb._,!c))throw Ia("ITransientCollection.-persistent!",a);return c.call(null,a)},Wb=function Wb(a,c,d){if(a?a.jc:a)return a.jc(a,c,d);var e;e=Wb[ca(null==a?null:a)];if(!e&&(e=Wb._,!e))throw Ia("ITransientAssociative.-assoc!",a);return e.call(null,a,c,d)},Xb=function Xb(a,c,d){if(a?a.dd:a)return a.dd(0,c,d);var e;e=Xb[ca(null==
	a?null:a)];if(!e&&(e=Xb._,!e))throw Ia("ITransientVector.-assoc-n!",a);return e.call(null,a,c,d)},Yb={},Zb=function Zb(a,c){if(a?a.Ob:a)return a.Ob(a,c);var d;d=Zb[ca(null==a?null:a)];if(!d&&(d=Zb._,!d))throw Ia("IComparable.-compare",a);return d.call(null,a,c)},$b=function $b(a){if(a?a.ad:a)return a.ad();var c;c=$b[ca(null==a?null:a)];if(!c&&(c=$b._,!c))throw Ia("IChunk.-drop-first",a);return c.call(null,a)},y=function y(a){if(a?a.uc:a)return a.uc(a);var c;c=y[ca(null==a?null:a)];if(!c&&(c=y._,!c))throw Ia("IChunkedSeq.-chunked-first",
	a);return c.call(null,a)},A=function A(a){if(a?a.vc:a)return a.vc(a);var c;c=A[ca(null==a?null:a)];if(!c&&(c=A._,!c))throw Ia("IChunkedSeq.-chunked-rest",a);return c.call(null,a)},ac=function ac(a){if(a?a.tc:a)return a.tc(a);var c;c=ac[ca(null==a?null:a)];if(!c&&(c=ac._,!c))throw Ia("IChunkedNext.-chunked-next",a);return c.call(null,a)},cc=function cc(a,c){if(a?a.Bd:a)return a.Bd(a,c);var d;d=cc[ca(null==a?null:a)];if(!d&&(d=cc._,!d))throw Ia("IReset.-reset!",a);return d.call(null,a,c)},dc=function dc(){switch(arguments.length){case 2:return dc.b(arguments[0],
	arguments[1]);case 3:return dc.g(arguments[0],arguments[1],arguments[2]);case 4:return dc.S(arguments[0],arguments[1],arguments[2],arguments[3]);case 5:return dc.Z(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}};dc.b=function(b,a){if(b?b.Dd:b)return b.Dd(b,a);var c;c=dc[ca(null==b?null:b)];if(!c&&(c=dc._,!c))throw Ia("ISwap.-swap!",b);return c.call(null,b,a)};
	dc.g=function(b,a,c){if(b?b.Ed:b)return b.Ed(b,a,c);var d;d=dc[ca(null==b?null:b)];if(!d&&(d=dc._,!d))throw Ia("ISwap.-swap!",b);return d.call(null,b,a,c)};dc.S=function(b,a,c,d){if(b?b.Fd:b)return b.Fd(b,a,c,d);var e;e=dc[ca(null==b?null:b)];if(!e&&(e=dc._,!e))throw Ia("ISwap.-swap!",b);return e.call(null,b,a,c,d)};dc.Z=function(b,a,c,d,e){if(b?b.Gd:b)return b.Gd(b,a,c,d,e);var f;f=dc[ca(null==b?null:b)];if(!f&&(f=dc._,!f))throw Ia("ISwap.-swap!",b);return f.call(null,b,a,c,d,e)};dc.H=5;
	var ec=function ec(a,c){if(a?a.ed:a)return a.ed(0,c);var d;d=ec[ca(null==a?null:a)];if(!d&&(d=ec._,!d))throw Ia("IVolatile.-vreset!",a);return d.call(null,a,c)},fc=function fc(a){if(a?a.fc:a)return a.fc(a);var c;c=fc[ca(null==a?null:a)];if(!c&&(c=fc._,!c))throw Ia("IIterable.-iterator",a);return c.call(null,a)};function gc(b){this.Ld=b;this.m=1073741824;this.A=0}gc.prototype.gd=function(b,a){return this.Ld.append(a)};function hc(b){var a=new ia;b.D(null,new gc(a),qa());return""+u(a)}
	var ic="undefined"!==typeof Math.imul&&0!==Math.imul(4294967295,5)?function(b,a){return Math.imul(b,a)}:function(b,a){var c=b&65535,d=a&65535;return c*d+((b>>>16&65535)*d+c*(a>>>16&65535)<<16>>>0)|0};function jc(b){b=ic(b|0,-862048943);return ic(b<<15|b>>>-15,461845907)}function kc(b,a){var c=(b|0)^(a|0);return ic(c<<13|c>>>-13,5)+-430675100|0}function mc(b,a){var c=(b|0)^a,c=ic(c^c>>>16,-2048144789),c=ic(c^c>>>13,-1028477387);return c^c>>>16}
	function nc(b){var a;a:{a=1;for(var c=0;;)if(a<b.length){var d=a+2,c=kc(c,jc(b.charCodeAt(a-1)|b.charCodeAt(a)<<16));a=d}else{a=c;break a}}a=1===(b.length&1)?a^jc(b.charCodeAt(b.length-1)):a;return mc(a,ic(2,b.length))}var oc={},qc=0;function rc(b){255<qc&&(oc={},qc=0);var a=oc[b];if("number"!==typeof a){a:if(null!=b)if(a=b.length,0<a)for(var c=0,d=0;;)if(c<a)var e=c+1,d=ic(31,d)+b.charCodeAt(c),c=e;else{a=d;break a}else a=0;else a=0;oc[b]=a;qc+=1}return b=a}
	function sc(b){b&&(b.m&4194304||b.bd)?b=b.I(null):"number"===typeof b?b=Math.floor(b)%2147483647:!0===b?b=1:!1===b?b=0:"string"===typeof b?(b=rc(b),0!==b&&(b=jc(b),b=kc(0,b),b=mc(b,4))):b=b instanceof Date?b.valueOf():null==b?0:Ib(b);return b}function tc(b,a){return b^a+2654435769+(b<<6)+(b>>2)}function vc(b){return b instanceof C}
	function wc(b,a){if(b.Za===a.Za)return 0;var c=Fa(b.ua);if(r(c?a.ua:c))return-1;if(r(b.ua)){if(Fa(a.ua))return 1;c=ka(b.ua,a.ua);return 0===c?ka(b.name,a.name):c}return ka(b.name,a.name)}function C(b,a,c,d,e){this.ua=b;this.name=a;this.Za=c;this.Zb=d;this.va=e;this.m=2154168321;this.A=4096}g=C.prototype;g.toString=function(){return this.Za};g.equiv=function(b){return this.u(null,b)};g.u=function(b,a){return a instanceof C?this.Za===a.Za:!1};
	g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return hb.g(b,this,null);case 3:return hb.g(b,this,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return hb.g(b,this,null)};b.g=function(a,b,d){return hb.g(b,this,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return hb.g(b,this,null)};g.b=function(b,a){return hb.g(b,this,a)};g.N=function(){return this.va};
	g.P=function(b,a){return new C(this.ua,this.name,this.Za,this.Zb,a)};g.I=function(){var b=this.Zb;return null!=b?b:this.Zb=b=tc(nc(this.name),rc(this.ua))};g.D=function(b,a){return Ob(a,this.Za)};function xc(b){return b instanceof C?b:yc(null,b)}function yc(b,a){var c=null!=b?[u(b),u("/"),u(a)].join(""):a;return new C(b,a,c,null,null)}
	function q(b){if(null==b)return null;if(b&&(b.m&8388608||b.Hc))return b.J(null);if(Ea(b)||"string"===typeof b)return 0===b.length?null:new za(b,0);if(Ga(Jb,b))return Kb(b);throw Error([u(b),u(" is not ISeqable")].join(""));}function D(b){if(null==b)return null;if(b&&(b.m&64||b.ic))return b.ia(null);b=q(b);return null==b?null:bb(b)}function Ac(b){return null!=b?b&&(b.m&64||b.ic)?b.ma(null):(b=q(b))?cb(b):Bc:Bc}function E(b){return null==b?null:b&&(b.m&128||b.yc)?b.qa(null):q(Ac(b))}
	var G=function G(){switch(arguments.length){case 1:return G.c(arguments[0]);case 2:return G.b(arguments[0],arguments[1]);default:return G.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};G.c=function(){return!0};G.b=function(b,a){return null==b?null==a:b===a||Fb(b,a)};G.j=function(b,a,c){for(;;)if(G.b(b,a))if(E(c))b=a,a=D(c),c=E(c);else return G.b(a,D(c));else return!1};G.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return G.j(a,b,c)};G.H=2;
	function Cc(b){this.Y=b}Cc.prototype.next=function(){if(null!=this.Y){var b=D(this.Y);this.Y=E(this.Y);return{value:b,done:!1}}return{value:null,done:!0}};function Dc(b){return new Cc(q(b))}function Ec(b,a){var c=jc(b),c=kc(0,c);return mc(c,a)}function Fc(b){var a=0,c=1;for(b=q(b);;)if(null!=b)a+=1,c=ic(31,c)+sc(D(b))|0,b=E(b);else return Ec(c,a)}var Gc=Ec(1,0);function Hc(b){var a=0,c=0;for(b=q(b);;)if(null!=b)a+=1,c=c+sc(D(b))|0,b=E(b);else return Ec(c,a)}var Ic=Ec(0,0);Ta["null"]=!0;
	Va["null"]=function(){return 0};Date.prototype.u=function(b,a){return a instanceof Date&&this.valueOf()===a.valueOf()};Date.prototype.ec=!0;Date.prototype.Ob=function(b,a){if(a instanceof Date)return ka(this.valueOf(),a.valueOf());throw Error([u("Cannot compare "),u(this),u(" to "),u(a)].join(""));};Fb.number=function(b,a){return b===a};Sa["function"]=!0;wb["function"]=!0;yb["function"]=function(){return null};Ib._=function(b){return b[da]||(b[da]=++ea)};function Jc(b){return b+1}
	function Kc(b){this.G=b;this.m=32768;this.A=0}Kc.prototype.wc=function(){return this.G};function Lc(b){return b instanceof Kc}function Pc(b){return vb(b)}function Qc(b,a){var c=Va(b);if(0===c)return a.R?a.R():a.call(null);for(var d=w.b(b,0),e=1;;)if(e<c){var f=w.b(b,e),d=a.b?a.b(d,f):a.call(null,d,f);if(Lc(d))return vb(d);e+=1}else return d}function Rc(b,a,c){var d=Va(b),e=c;for(c=0;;)if(c<d){var f=w.b(b,c),e=a.b?a.b(e,f):a.call(null,e,f);if(Lc(e))return vb(e);c+=1}else return e}
	function Sc(b,a){var c=b.length;if(0===b.length)return a.R?a.R():a.call(null);for(var d=b[0],e=1;;)if(e<c){var f=b[e],d=a.b?a.b(d,f):a.call(null,d,f);if(Lc(d))return vb(d);e+=1}else return d}function Tc(b,a,c){var d=b.length,e=c;for(c=0;;)if(c<d){var f=b[c],e=a.b?a.b(e,f):a.call(null,e,f);if(Lc(e))return vb(e);c+=1}else return e}function Uc(b,a,c,d){for(var e=b.length;;)if(d<e){var f=b[d];c=a.b?a.b(c,f):a.call(null,c,f);if(Lc(c))return vb(c);d+=1}else return c}
	function Vc(b){return b?b.m&2||b.Fc?!0:b.m?!1:Ga(Ta,b):Ga(Ta,b)}function Wc(b){return b?b.m&16||b.cd?!0:b.m?!1:Ga(Za,b):Ga(Za,b)}function Xc(b,a){this.l=b;this.K=a}Xc.prototype.Bc=function(){return this.K<this.l.length};Xc.prototype.next=function(){var b=this.l[this.K];this.K+=1;return b};function za(b,a){this.l=b;this.K=a;this.m=166199550;this.A=8192}g=za.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};
	g.M=function(b,a){var c=a+this.K;return c<this.l.length?this.l[c]:null};g.xa=function(b,a,c){b=a+this.K;return b<this.l.length?this.l[b]:c};g.fc=function(){return new Xc(this.l,this.K)};g.qa=function(){return this.K+1<this.l.length?new za(this.l,this.K+1):null};g.L=function(){var b=this.l.length-this.K;return 0>b?0:b};g.Ua=function(){var b=Va(this);return 0<b?new Yc(this,b-1,null):null};g.I=function(){return Fc(this)};g.u=function(b,a){return ad.b?ad.b(this,a):ad.call(null,this,a)};g.ca=function(){return Bc};
	g.ja=function(b,a){return Uc(this.l,a,this.l[this.K],this.K+1)};g.ka=function(b,a,c){return Uc(this.l,a,c,this.K)};g.ia=function(){return this.l[this.K]};g.ma=function(){return this.K+1<this.l.length?new za(this.l,this.K+1):Bc};g.J=function(){return this.K<this.l.length?this:null};g.O=function(b,a){return bd.b?bd.b(a,this):bd.call(null,a,this)};za.prototype[La]=function(){return Dc(this)};function cd(b,a){return a<b.length?new za(b,a):null}
	function H(){switch(arguments.length){case 1:return cd(arguments[0],0);case 2:return cd(arguments[0],arguments[1]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function Yc(b,a,c){this.sc=b;this.K=a;this.w=c;this.m=32374990;this.A=8192}g=Yc.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.w};g.qa=function(){return 0<this.K?new Yc(this.sc,this.K-1,null):null};g.L=function(){return this.K+1};g.I=function(){return Fc(this)};
	g.u=function(b,a){return ad.b?ad.b(this,a):ad.call(null,this,a)};g.ca=function(){var b=this.w;return dd.b?dd.b(Bc,b):dd.call(null,Bc,b)};g.ja=function(b,a){return ed?ed(a,this):fd.call(null,a,this)};g.ka=function(b,a,c){return gd?gd(a,c,this):fd.call(null,a,c,this)};g.ia=function(){return w.b(this.sc,this.K)};g.ma=function(){return 0<this.K?new Yc(this.sc,this.K-1,null):Bc};g.J=function(){return this};g.P=function(b,a){return new Yc(this.sc,this.K,a)};
	g.O=function(b,a){return bd.b?bd.b(a,this):bd.call(null,a,this)};Yc.prototype[La]=function(){return Dc(this)};function hd(b){return D(E(b))}function id(b){return D(D(b))}function jd(b){for(;;){var a=E(b);if(null!=a)b=a;else return D(b)}}Fb._=function(b,a){return b===a};
	var kd=function kd(){switch(arguments.length){case 0:return kd.R();case 1:return kd.c(arguments[0]);case 2:return kd.b(arguments[0],arguments[1]);default:return kd.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};kd.R=function(){return I};kd.c=function(b){return b};kd.b=function(b,a){return null!=b?Ya(b,a):Ya(Bc,a)};kd.j=function(b,a,c){for(;;)if(r(c))b=kd.b(b,a),a=D(c),c=E(c);else return kd.b(b,a)};kd.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return kd.j(a,b,c)};
	kd.H=2;function J(b){if(null!=b)if(b&&(b.m&2||b.Fc))b=b.L(null);else if(Ea(b))b=b.length;else if("string"===typeof b)b=b.length;else if(Ga(Ta,b))b=Va(b);else a:{b=q(b);for(var a=0;;){if(Vc(b)){b=a+Va(b);break a}b=E(b);a+=1}}else b=0;return b}function ld(b,a){for(var c=null;;){if(null==b)return c;if(0===a)return q(b)?D(b):c;if(Wc(b))return w.g(b,a,c);if(q(b)){var d=E(b),e=a-1;b=d;a=e}else return c}}
	function md(b,a){if("number"!==typeof a)throw Error("index argument to nth must be a number");if(null==b)return b;if(b&&(b.m&16||b.cd))return b.M(null,a);if(Ea(b)||"string"===typeof b)return a<b.length?b[a]:null;if(Ga(Za,b))return w.b(b,a);if(b?b.m&64||b.ic||(b.m?0:Ga(ab,b)):Ga(ab,b)){var c;a:{c=b;for(var d=a;;){if(null==c)throw Error("Index out of bounds");if(0===d){if(q(c)){c=D(c);break a}throw Error("Index out of bounds");}if(Wc(c)){c=w.b(c,d);break a}if(q(c))c=E(c),--d;else throw Error("Index out of bounds");
	}}return c}throw Error([u("nth not supported on this type "),u(Ka(Ha(b)))].join(""));}function L(b,a){if("number"!==typeof a)throw Error("index argument to nth must be a number.");if(null==b)return null;if(b&&(b.m&16||b.cd))return b.xa(null,a,null);if(Ea(b)||"string"===typeof b)return a<b.length?b[a]:null;if(Ga(Za,b))return w.b(b,a);if(b?b.m&64||b.ic||(b.m?0:Ga(ab,b)):Ga(ab,b))return ld(b,a);throw Error([u("nth not supported on this type "),u(Ka(Ha(b)))].join(""));}
	function nd(b,a){return null==b?null:b&&(b.m&256||b.Gc)?b.C(null,a):Ea(b)?a<b.length?b[a|0]:null:"string"===typeof b?a<b.length?b[a|0]:null:Ga(fb,b)?hb.b(b,a):null}function od(b,a,c){return null!=b?b&&(b.m&256||b.Gc)?b.B(null,a,c):Ea(b)?a<b.length?b[a]:c:"string"===typeof b?a<b.length?b[a]:c:Ga(fb,b)?hb.g(b,a,c):c:c}
	var M=function M(){switch(arguments.length){case 3:return M.g(arguments[0],arguments[1],arguments[2]);default:return M.j(arguments[0],arguments[1],arguments[2],new za(Array.prototype.slice.call(arguments,3),0))}};M.g=function(b,a,c){return null!=b?kb(b,a,c):pd([a],[c])};M.j=function(b,a,c,d){for(;;)if(b=M.g(b,a,c),r(d))a=D(d),c=hd(d),d=E(E(d));else return b};M.F=function(b){var a=D(b),c=E(b);b=D(c);var d=E(c),c=D(d),d=E(d);return M.j(a,b,c,d)};M.H=3;
	var O=function O(){switch(arguments.length){case 1:return O.c(arguments[0]);case 2:return O.b(arguments[0],arguments[1]);default:return O.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};O.c=function(b){return b};O.b=function(b,a){return null==b?null:mb(b,a)};O.j=function(b,a,c){for(;;){if(null==b)return null;b=O.b(b,a);if(r(c))a=D(c),c=E(c);else return b}};O.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return O.j(a,b,c)};O.H=2;
	function sd(b){var a="function"==ca(b);return r(a)?a:b?r(r(null)?null:b.qd)?!0:b.Eb?!1:Ga(Sa,b):Ga(Sa,b)}function td(b,a){this.o=b;this.w=a;this.m=393217;this.A=0}g=td.prototype;g.N=function(){return this.w};g.P=function(b,a){return new td(this.o,a)};g.qd=!0;
	g.call=function(){function b(a,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N,W,Od,xa,Ua){a=this.o;return ud.xc?ud.xc(a,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N,W,Od,xa,Ua):ud.call(null,a,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N,W,Od,xa,Ua)}function a(a,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N,W,Od,xa){a=this;return a.o.qb?a.o.qb(b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N,W,Od,xa):a.o.call(null,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N,W,Od,xa)}function c(a,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N,W,xa){a=this;return a.o.pb?a.o.pb(b,c,d,e,f,h,k,n,l,p,t,
	z,x,B,K,F,N,W,xa):a.o.call(null,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N,W,xa)}function d(a,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N,W){a=this;return a.o.ob?a.o.ob(b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N,W):a.o.call(null,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N,W)}function e(a,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N){a=this;return a.o.nb?a.o.nb(b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N):a.o.call(null,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F,N)}function f(a,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F){a=this;return a.o.mb?a.o.mb(b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,
	F):a.o.call(null,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K,F)}function h(a,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K){a=this;return a.o.lb?a.o.lb(b,c,d,e,f,h,k,n,l,p,t,z,x,B,K):a.o.call(null,b,c,d,e,f,h,k,n,l,p,t,z,x,B,K)}function k(a,b,c,d,e,f,h,k,n,l,p,t,z,x,B){a=this;return a.o.kb?a.o.kb(b,c,d,e,f,h,k,n,l,p,t,z,x,B):a.o.call(null,b,c,d,e,f,h,k,n,l,p,t,z,x,B)}function l(a,b,c,d,e,f,h,k,n,l,p,t,z,x){a=this;return a.o.jb?a.o.jb(b,c,d,e,f,h,k,n,l,p,t,z,x):a.o.call(null,b,c,d,e,f,h,k,n,l,p,t,z,x)}function n(a,b,c,d,e,f,h,
	k,n,l,p,t,z){a=this;return a.o.ib?a.o.ib(b,c,d,e,f,h,k,n,l,p,t,z):a.o.call(null,b,c,d,e,f,h,k,n,l,p,t,z)}function p(a,b,c,d,e,f,h,k,n,l,p,t){a=this;return a.o.hb?a.o.hb(b,c,d,e,f,h,k,n,l,p,t):a.o.call(null,b,c,d,e,f,h,k,n,l,p,t)}function t(a,b,c,d,e,f,h,k,n,l,p){a=this;return a.o.gb?a.o.gb(b,c,d,e,f,h,k,n,l,p):a.o.call(null,b,c,d,e,f,h,k,n,l,p)}function x(a,b,c,d,e,f,h,k,n,l){a=this;return a.o.tb?a.o.tb(b,c,d,e,f,h,k,n,l):a.o.call(null,b,c,d,e,f,h,k,n,l)}function z(a,b,c,d,e,f,h,k,n){a=this;return a.o.sb?
	a.o.sb(b,c,d,e,f,h,k,n):a.o.call(null,b,c,d,e,f,h,k,n)}function B(a,b,c,d,e,f,h,k){a=this;return a.o.rb?a.o.rb(b,c,d,e,f,h,k):a.o.call(null,b,c,d,e,f,h,k)}function F(a,b,c,d,e,f,h){a=this;return a.o.wa?a.o.wa(b,c,d,e,f,h):a.o.call(null,b,c,d,e,f,h)}function K(a,b,c,d,e,f){a=this;return a.o.Z?a.o.Z(b,c,d,e,f):a.o.call(null,b,c,d,e,f)}function N(a,b,c,d,e){a=this;return a.o.S?a.o.S(b,c,d,e):a.o.call(null,b,c,d,e)}function W(a,b,c,d){a=this;return a.o.g?a.o.g(b,c,d):a.o.call(null,b,c,d)}function xa(a,
	b,c){a=this;return a.o.b?a.o.b(b,c):a.o.call(null,b,c)}function Ua(a,b){a=this;return a.o.c?a.o.c(b):a.o.call(null,b)}function gb(a){a=this;return a.o.R?a.o.R():a.o.call(null)}var aa=null,aa=function(aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb,Hb,bc,Tb,pc,lc,zc,ee,ff,fe,Od,jj,Bg){switch(arguments.length){case 1:return gb.call(this,aa);case 2:return Ua.call(this,aa,Pa);case 3:return xa.call(this,aa,Pa,va);case 4:return W.call(this,aa,Pa,va,Na);case 5:return N.call(this,aa,Pa,va,Na,eb);case 6:return K.call(this,
	aa,Pa,va,Na,eb,Ja);case 7:return F.call(this,aa,Pa,va,Na,eb,Ja,$a);case 8:return B.call(this,aa,Pa,va,Na,eb,Ja,$a,jb);case 9:return z.call(this,aa,Pa,va,Na,eb,Ja,$a,jb,Bb);case 10:return x.call(this,aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb);case 11:return t.call(this,aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb,Hb);case 12:return p.call(this,aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb,Hb,bc);case 13:return n.call(this,aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb,Hb,bc,Tb);case 14:return l.call(this,aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb,Hb,bc,Tb,pc);case 15:return k.call(this,
	aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb,Hb,bc,Tb,pc,lc);case 16:return h.call(this,aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb,Hb,bc,Tb,pc,lc,zc);case 17:return f.call(this,aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb,Hb,bc,Tb,pc,lc,zc,ee);case 18:return e.call(this,aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb,Hb,bc,Tb,pc,lc,zc,ee,ff);case 19:return d.call(this,aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb,Hb,bc,Tb,pc,lc,zc,ee,ff,fe);case 20:return c.call(this,aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb,Hb,bc,Tb,pc,lc,zc,ee,ff,fe,Od);case 21:return a.call(this,aa,Pa,va,Na,eb,Ja,
	$a,jb,Bb,Gb,Hb,bc,Tb,pc,lc,zc,ee,ff,fe,Od,jj);case 22:return b.call(this,aa,Pa,va,Na,eb,Ja,$a,jb,Bb,Gb,Hb,bc,Tb,pc,lc,zc,ee,ff,fe,Od,jj,Bg)}throw Error("Invalid arity: "+arguments.length);};aa.c=gb;aa.b=Ua;aa.g=xa;aa.S=W;aa.Z=N;aa.wa=K;aa.rb=F;aa.sb=B;aa.tb=z;aa.gb=x;aa.hb=t;aa.ib=p;aa.jb=n;aa.kb=l;aa.lb=k;aa.mb=h;aa.nb=f;aa.ob=e;aa.pb=d;aa.qb=c;aa.vd=a;aa.xc=b;return aa}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.R=function(){return this.o.R?this.o.R():this.o.call(null)};
	g.c=function(b){return this.o.c?this.o.c(b):this.o.call(null,b)};g.b=function(b,a){return this.o.b?this.o.b(b,a):this.o.call(null,b,a)};g.g=function(b,a,c){return this.o.g?this.o.g(b,a,c):this.o.call(null,b,a,c)};g.S=function(b,a,c,d){return this.o.S?this.o.S(b,a,c,d):this.o.call(null,b,a,c,d)};g.Z=function(b,a,c,d,e){return this.o.Z?this.o.Z(b,a,c,d,e):this.o.call(null,b,a,c,d,e)};g.wa=function(b,a,c,d,e,f){return this.o.wa?this.o.wa(b,a,c,d,e,f):this.o.call(null,b,a,c,d,e,f)};
	g.rb=function(b,a,c,d,e,f,h){return this.o.rb?this.o.rb(b,a,c,d,e,f,h):this.o.call(null,b,a,c,d,e,f,h)};g.sb=function(b,a,c,d,e,f,h,k){return this.o.sb?this.o.sb(b,a,c,d,e,f,h,k):this.o.call(null,b,a,c,d,e,f,h,k)};g.tb=function(b,a,c,d,e,f,h,k,l){return this.o.tb?this.o.tb(b,a,c,d,e,f,h,k,l):this.o.call(null,b,a,c,d,e,f,h,k,l)};g.gb=function(b,a,c,d,e,f,h,k,l,n){return this.o.gb?this.o.gb(b,a,c,d,e,f,h,k,l,n):this.o.call(null,b,a,c,d,e,f,h,k,l,n)};
	g.hb=function(b,a,c,d,e,f,h,k,l,n,p){return this.o.hb?this.o.hb(b,a,c,d,e,f,h,k,l,n,p):this.o.call(null,b,a,c,d,e,f,h,k,l,n,p)};g.ib=function(b,a,c,d,e,f,h,k,l,n,p,t){return this.o.ib?this.o.ib(b,a,c,d,e,f,h,k,l,n,p,t):this.o.call(null,b,a,c,d,e,f,h,k,l,n,p,t)};g.jb=function(b,a,c,d,e,f,h,k,l,n,p,t,x){return this.o.jb?this.o.jb(b,a,c,d,e,f,h,k,l,n,p,t,x):this.o.call(null,b,a,c,d,e,f,h,k,l,n,p,t,x)};
	g.kb=function(b,a,c,d,e,f,h,k,l,n,p,t,x,z){return this.o.kb?this.o.kb(b,a,c,d,e,f,h,k,l,n,p,t,x,z):this.o.call(null,b,a,c,d,e,f,h,k,l,n,p,t,x,z)};g.lb=function(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B){return this.o.lb?this.o.lb(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B):this.o.call(null,b,a,c,d,e,f,h,k,l,n,p,t,x,z,B)};g.mb=function(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F){return this.o.mb?this.o.mb(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F):this.o.call(null,b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F)};
	g.nb=function(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K){return this.o.nb?this.o.nb(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K):this.o.call(null,b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K)};g.ob=function(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N){return this.o.ob?this.o.ob(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N):this.o.call(null,b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N)};
	g.pb=function(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return this.o.pb?this.o.pb(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W):this.o.call(null,b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W)};g.qb=function(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa){return this.o.qb?this.o.qb(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa):this.o.call(null,b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa)};
	g.vd=function(b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa,Ua){var gb=this.o;return ud.xc?ud.xc(gb,b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa,Ua):ud.call(null,gb,b,a,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa,Ua)};function dd(b,a){return sd(b)&&!(b?b.m&262144||b.Hd||(b.m?0:Ga(zb,b)):Ga(zb,b))?new td(b,a):null==b?null:Ab(b,a)}function vd(b){var a=null!=b;return(a?b?b.m&131072||b.yd||(b.m?0:Ga(wb,b)):Ga(wb,b):a)?yb(b):null}
	var wd=function wd(){switch(arguments.length){case 1:return wd.c(arguments[0]);case 2:return wd.b(arguments[0],arguments[1]);default:return wd.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};wd.c=function(b){return b};wd.b=function(b,a){return null==b?null:rb(b,a)};wd.j=function(b,a,c){for(;;){if(null==b)return null;b=wd.b(b,a);if(r(c))a=D(c),c=E(c);else return b}};wd.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return wd.j(a,b,c)};wd.H=2;
	function xd(b){return null==b||Fa(q(b))}function yd(b){return null==b?!1:b?b.m&8||b.Nd?!0:b.m?!1:Ga(Xa,b):Ga(Xa,b)}function zd(b){return null==b?!1:b?b.m&4096||b.Qd?!0:b.m?!1:Ga(qb,b):Ga(qb,b)}function Ad(b){return b?b.m&16777216||b.Pd?!0:b.m?!1:Ga(Lb,b):Ga(Lb,b)}function Bd(b){return null==b?!1:b?b.m&1024||b.wd?!0:b.m?!1:Ga(lb,b):Ga(lb,b)}function Cd(b){return b?b.m&16384||b.Rd?!0:b.m?!1:Ga(tb,b):Ga(tb,b)}function Ed(b){return b?b.A&512||b.Md?!0:!1:!1}
	function Fd(b){var a=[];ha(b,function(a,b){return function(a,c){return b.push(c)}}(b,a));return a}function Gd(b,a,c,d,e){for(;0!==e;)c[d]=b[a],d+=1,--e,a+=1}var Hd={};function Id(b){return null==b?!1:b?b.m&64||b.ic?!0:b.m?!1:Ga(ab,b):Ga(ab,b)}function Md(b){return b?b.m&8388608||b.Hc?!0:b.m?!1:Ga(Jb,b):Ga(Jb,b)}function Nd(b){return r(b)?!0:!1}function Pd(b,a){return od(b,a,Hd)===Hd?!1:!0}
	var Qd=function Qd(){switch(arguments.length){case 1:return Qd.c(arguments[0]);case 2:return Qd.b(arguments[0],arguments[1]);default:return Qd.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};Qd.c=function(){return!0};Qd.b=function(b,a){return!G.b(b,a)};
	Qd.j=function(b,a,c){if(G.b(b,a))return!1;a:if(b=[b,a],a=b.length,a<=Rd)for(var d=0,e=Sb(Sd);;)if(d<a)var f=d+1,e=Wb(e,b[d],null),d=f;else{b=new Td(null,Vb(e),null);break a}else for(d=0,e=Sb(Ud);;)if(d<a)f=d+1,e=Ub(e,b[d]),d=f;else{b=Vb(e);break a}for(a=c;;)if(d=D(a),c=E(a),r(a)){if(Pd(b,d))return!1;b=kd.b(b,d);a=c}else return!0};Qd.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return Qd.j(a,b,c)};Qd.H=2;
	function Vd(b,a){if(b===a)return 0;if(null==b)return-1;if(null==a)return 1;if("number"===typeof b){if("number"===typeof a)return ka(b,a);throw Error([u("Cannot compare "),u(b),u(" to "),u(a)].join(""));}if(b?b.A&2048||b.ec||(b.A?0:Ga(Yb,b)):Ga(Yb,b))return Zb(b,a);if("string"!==typeof b&&!Ea(b)&&!0!==b&&!1!==b||Ha(b)!==Ha(a))throw Error([u("Cannot compare "),u(b),u(" to "),u(a)].join(""));return ka(b,a)}
	function Wd(b,a){var c=J(b),d=J(a);if(c<d)c=-1;else if(c>d)c=1;else if(0===c)c=0;else a:for(d=0;;){var e=Vd(md(b,d),md(a,d));if(0===e&&d+1<c)d+=1;else{c=e;break a}}return c}function Xd(b){return G.b(b,Vd)?Vd:function(a,c){var d=b.b?b.b(a,c):b.call(null,a,c);return"number"===typeof d?d:r(d)?-1:r(b.b?b.b(c,a):b.call(null,c,a))?1:0}}function Yd(b,a){if(q(a)){var c=Zd.c?Zd.c(a):Zd.call(null,a),d=Xd(b);la(c,d);return q(c)}return Bc}
	function fd(){switch(arguments.length){case 2:return ed(arguments[0],arguments[1]);case 3:return gd(arguments[0],arguments[1],arguments[2]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function ed(b,a){var c=q(a);if(c){var d=D(c),c=E(c);return v?v(b,d,c):Ra.call(null,b,d,c)}return b.R?b.R():b.call(null)}function gd(b,a,c){for(c=q(c);;)if(c){var d=D(c);a=b.b?b.b(a,d):b.call(null,a,d);if(Lc(a))return vb(a);c=E(c)}else return a}
	function $d(b){b=Zd.c?Zd.c(b):Zd.call(null,b);for(var a=Math.random,c=b.length-1;0<c;c--){var d=Math.floor(a()*(c+1)),e=b[c];b[c]=b[d];b[d]=e}return ce.c?ce.c(b):ce.call(null,b)}function Ra(){switch(arguments.length){case 2:return de(arguments[0],arguments[1]);case 3:return v(arguments[0],arguments[1],arguments[2]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}
	function de(b,a){return a&&(a.m&524288||a.Ad)?a.ja(null,b):Ea(a)?Sc(a,b):"string"===typeof a?Sc(a,b):Ga(Cb,a)?Db.b(a,b):ed(b,a)}function v(b,a,c){return c&&(c.m&524288||c.Ad)?c.ka(null,b,a):Ea(c)?Tc(c,b,a):"string"===typeof c?Tc(c,b,a):Ga(Cb,c)?Db.g(c,b,a):gd(b,a,c)}function ge(b,a,c){return null!=c?Eb(c,b,a):a}function he(b){return b}function ie(b,a,c,d){b=b.c?b.c(a):b.call(null,a);c=v(b,c,d);return b.c?b.c(c):b.call(null,c)}
	var je=function je(){switch(arguments.length){case 0:return je.R();case 1:return je.c(arguments[0]);case 2:return je.b(arguments[0],arguments[1]);default:return je.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};je.R=function(){return 0};je.c=function(b){return b};je.b=function(b,a){return b+a};je.j=function(b,a,c){return v(je,b+a,c)};je.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return je.j(a,b,c)};je.H=2;
	var ke=function ke(){switch(arguments.length){case 1:return ke.c(arguments[0]);case 2:return ke.b(arguments[0],arguments[1]);default:return ke.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};ke.c=function(b){return-b};ke.b=function(b,a){return b-a};ke.j=function(b,a,c){return v(ke,b-a,c)};ke.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return ke.j(a,b,c)};ke.H=2;
	var le=function le(){switch(arguments.length){case 0:return le.R();case 1:return le.c(arguments[0]);case 2:return le.b(arguments[0],arguments[1]);default:return le.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};le.R=function(){return 1};le.c=function(b){return b};le.b=function(b,a){return b*a};le.j=function(b,a,c){return v(le,b*a,c)};le.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return le.j(a,b,c)};le.H=2;
	var me=function me(){switch(arguments.length){case 1:return me.c(arguments[0]);case 2:return me.b(arguments[0],arguments[1]);default:return me.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};me.c=function(b){return 1/b};me.b=function(b,a){return b/a};me.j=function(b,a,c){return v(me,b/a,c)};me.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return me.j(a,b,c)};me.H=2;
	var ne=function ne(){switch(arguments.length){case 1:return ne.c(arguments[0]);case 2:return ne.b(arguments[0],arguments[1]);default:return ne.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};ne.c=function(){return!0};ne.b=function(b,a){return b<a};ne.j=function(b,a,c){for(;;)if(b<a)if(E(c))b=a,a=D(c),c=E(c);else return a<D(c);else return!1};ne.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return ne.j(a,b,c)};ne.H=2;
	var oe=function oe(){switch(arguments.length){case 1:return oe.c(arguments[0]);case 2:return oe.b(arguments[0],arguments[1]);default:return oe.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};oe.c=function(){return!0};oe.b=function(b,a){return b<=a};oe.j=function(b,a,c){for(;;)if(b<=a)if(E(c))b=a,a=D(c),c=E(c);else return a<=D(c);else return!1};oe.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return oe.j(a,b,c)};oe.H=2;
	var pe=function pe(){switch(arguments.length){case 1:return pe.c(arguments[0]);case 2:return pe.b(arguments[0],arguments[1]);default:return pe.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};pe.c=function(){return!0};pe.b=function(b,a){return b>a};pe.j=function(b,a,c){for(;;)if(b>a)if(E(c))b=a,a=D(c),c=E(c);else return a>D(c);else return!1};pe.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return pe.j(a,b,c)};pe.H=2;
	var qe=function qe(){switch(arguments.length){case 1:return qe.c(arguments[0]);case 2:return qe.b(arguments[0],arguments[1]);default:return qe.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};qe.c=function(){return!0};qe.b=function(b,a){return b>=a};qe.j=function(b,a,c){for(;;)if(b>=a)if(E(c))b=a,a=D(c),c=E(c);else return a>=D(c);else return!1};qe.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return qe.j(a,b,c)};qe.H=2;
	var re=function re(){switch(arguments.length){case 1:return re.c(arguments[0]);case 2:return re.b(arguments[0],arguments[1]);default:return re.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};re.c=function(b){return b};re.b=function(b,a){return b>a?b:a};re.j=function(b,a,c){return v(re,b>a?b:a,c)};re.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return re.j(a,b,c)};re.H=2;
	var se=function se(){switch(arguments.length){case 1:return se.c(arguments[0]);case 2:return se.b(arguments[0],arguments[1]);default:return se.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};se.c=function(b){return b};se.b=function(b,a){return b<a?b:a};se.j=function(b,a,c){return v(se,b<a?b:a,c)};se.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return se.j(a,b,c)};se.H=2;function te(b){return 0<=b?Math.floor(b):Math.ceil(b)}function ue(b,a){return(b%a+a)%a}
	function ve(b,a){return te((b-b%a)/a)}function we(b){b-=b>>1&1431655765;b=(b&858993459)+(b>>2&858993459);return 16843009*(b+(b>>4)&252645135)>>24}var xe=function xe(){switch(arguments.length){case 1:return xe.c(arguments[0]);case 2:return xe.b(arguments[0],arguments[1]);default:return xe.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};xe.c=function(){return!0};xe.b=function(b,a){return Fb(b,a)};
	xe.j=function(b,a,c){for(;;)if(b===a)if(E(c))b=a,a=D(c),c=E(c);else return a===D(c);else return!1};xe.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return xe.j(a,b,c)};xe.H=2;function ye(b){return 0>b}function ze(b,a){for(var c=a,d=q(b);;)if(d&&0<c)--c,d=E(d);else return d}var u=function u(){switch(arguments.length){case 0:return u.R();case 1:return u.c(arguments[0]);default:return u.j(arguments[0],new za(Array.prototype.slice.call(arguments,1),0))}};u.R=function(){return""};
	u.c=function(b){return null==b?"":ga(b)};u.j=function(b,a){for(var c=new ia(""+u(b)),d=a;;)if(r(d))c=c.append(""+u(D(d))),d=E(d);else return c.toString()};u.F=function(b){var a=D(b);b=E(b);return u.j(a,b)};u.H=1;function Ae(b,a){return b.substring(a)}function ad(b,a){var c;if(Ad(a))if(Vc(b)&&Vc(a)&&J(b)!==J(a))c=!1;else a:{c=q(b);for(var d=q(a);;){if(null==c){c=null==d;break a}if(null!=d&&G.b(D(c),D(d)))c=E(c),d=E(d);else{c=!1;break a}}}else c=null;return Nd(c)}
	function Be(b){var a=0;for(b=q(b);;)if(b){var c=D(b),a=(a+(sc(function(){var a=c;return Ce.c?Ce.c(a):Ce.call(null,a)}())^sc(function(){var a=c;return De.c?De.c(a):De.call(null,a)}())))%4503599627370496;b=E(b)}else return a}function Ee(b,a,c,d,e){this.w=b;this.first=a;this.wb=c;this.count=d;this.i=e;this.m=65937646;this.A=8192}g=Ee.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.w};g.qa=function(){return 1===this.count?null:this.wb};
	g.L=function(){return this.count};g.Db=function(){return this.first};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return Ab(Bc,this.w)};g.ja=function(b,a){return ed(a,this)};g.ka=function(b,a,c){return gd(a,c,this)};g.ia=function(){return this.first};g.ma=function(){return 1===this.count?Bc:this.wb};g.J=function(){return this};g.P=function(b,a){return new Ee(a,this.first,this.wb,this.count,this.i)};
	g.O=function(b,a){return new Ee(this.w,a,this,this.count+1,null)};Ee.prototype[La]=function(){return Dc(this)};function Fe(b){this.w=b;this.m=65937614;this.A=8192}g=Fe.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.w};g.qa=function(){return null};g.L=function(){return 0};g.Db=function(){return null};g.I=function(){return Gc};g.u=function(b,a){return ad(this,a)};g.ca=function(){return this};g.ja=function(b,a){return ed(a,this)};
	g.ka=function(b,a,c){return gd(a,c,this)};g.ia=function(){return null};g.ma=function(){return Bc};g.J=function(){return null};g.P=function(b,a){return new Fe(a)};g.O=function(b,a){return new Ee(this.w,a,null,1,null)};var Bc=new Fe(null);Fe.prototype[La]=function(){return Dc(this)};function Ge(b){return(b?b.m&134217728||b.Cd||(b.m?0:Ga(Mb,b)):Ga(Mb,b))?Nb(b):v(kd,Bc,b)}var He=function He(){return He.j(0<arguments.length?new za(Array.prototype.slice.call(arguments,0),0):null)};
	He.j=function(b){var a;if(b instanceof za&&0===b.K)a=b.l;else a:for(a=[];;)if(null!=b)a.push(b.ia(null)),b=b.qa(null);else break a;b=a.length;for(var c=Bc;;)if(0<b){var d=b-1,c=c.O(null,a[b-1]);b=d}else return c};He.H=0;He.F=function(b){return He.j(q(b))};function Ie(b,a,c,d){this.w=b;this.first=a;this.wb=c;this.i=d;this.m=65929452;this.A=8192}g=Ie.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.w};
	g.qa=function(){return null==this.wb?null:q(this.wb)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(Bc,this.w)};g.ja=function(b,a){return ed(a,this)};g.ka=function(b,a,c){return gd(a,c,this)};g.ia=function(){return this.first};g.ma=function(){return null==this.wb?Bc:this.wb};g.J=function(){return this};g.P=function(b,a){return new Ie(a,this.first,this.wb,this.i)};g.O=function(b,a){return new Ie(null,a,this,this.i)};
	Ie.prototype[La]=function(){return Dc(this)};function bd(b,a){var c=null==a;return(c?c:a&&(a.m&64||a.ic))?new Ie(null,b,a,null):new Ie(null,b,q(a),null)}function Je(b,a){if(b.U===a.U)return 0;var c=Fa(b.ua);if(r(c?a.ua:c))return-1;if(r(b.ua)){if(Fa(a.ua))return 1;c=ka(b.ua,a.ua);return 0===c?ka(b.name,a.name):c}return ka(b.name,a.name)}function P(b,a,c,d){this.ua=b;this.name=a;this.U=c;this.Zb=d;this.m=2153775105;this.A=4096}g=P.prototype;g.toString=function(){return[u(":"),u(this.U)].join("")};
	g.equiv=function(b){return this.u(null,b)};g.u=function(b,a){return a instanceof P?this.U===a.U:!1};g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return nd(b,this);case 3:return od(b,this,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return nd(b,this)};b.g=function(a,b,d){return od(b,this,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return nd(b,this)};
	g.b=function(b,a){return od(b,this,a)};g.I=function(){var b=this.Zb;return null!=b?b:this.Zb=b=tc(nc(this.name),rc(this.ua))+2654435769|0};g.D=function(b,a){return Ob(a,[u(":"),u(this.U)].join(""))};function Q(b,a){return b===a?!0:b instanceof P&&a instanceof P?b.U===a.U:!1}function Ke(b){if(b&&(b.A&4096||b.zd))return b.ua;throw Error([u("Doesn't support namespace: "),u(b)].join(""));}
	var Le=function Le(){switch(arguments.length){case 1:return Le.c(arguments[0]);case 2:return Le.b(arguments[0],arguments[1]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}};Le.c=function(b){if(b instanceof P)return b;if(b instanceof C)return new P(Ke(b),Me.c?Me.c(b):Me.call(null,b),b.Za,null);if("string"===typeof b){var a=b.split("/");return 2===a.length?new P(a[0],a[1],b,null):new P(null,a[0],b,null)}return null};
	Le.b=function(b,a){return new P(b,a,[u(r(b)?[u(b),u("/")].join(""):null),u(a)].join(""),null)};Le.H=2;function Ne(b,a,c,d){this.w=b;this.$=a;this.Y=c;this.i=d;this.m=32374988;this.A=0}g=Ne.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};function Oe(b){null!=b.$&&(b.Y=b.$.R?b.$.R():b.$.call(null),b.$=null);return b.Y}g.N=function(){return this.w};g.qa=function(){Kb(this);return null==this.Y?null:E(this.Y)};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(Bc,this.w)};g.ja=function(b,a){return ed(a,this)};g.ka=function(b,a,c){return gd(a,c,this)};g.ia=function(){Kb(this);return null==this.Y?null:D(this.Y)};g.ma=function(){Kb(this);return null!=this.Y?Ac(this.Y):Bc};g.J=function(){Oe(this);if(null==this.Y)return null;for(var b=this.Y;;)if(b instanceof Ne)b=Oe(b);else return this.Y=b,q(this.Y)};
	g.P=function(b,a){return new Ne(a,this.$,this.Y,this.i)};g.O=function(b,a){return bd(a,this)};Ne.prototype[La]=function(){return Dc(this)};function Pe(b,a){this.Ec=b;this.end=a;this.m=2;this.A=0}Pe.prototype.add=function(b){this.Ec[this.end]=b;return this.end+=1};Pe.prototype.fb=function(){var b=new Qe(this.Ec,0,this.end);this.Ec=null;return b};Pe.prototype.L=function(){return this.end};function Re(b){return new Pe(Array(b),0)}
	function Qe(b,a,c){this.l=b;this.ra=a;this.end=c;this.m=524306;this.A=0}g=Qe.prototype;g.L=function(){return this.end-this.ra};g.M=function(b,a){return this.l[this.ra+a]};g.xa=function(b,a,c){return 0<=a&&a<this.end-this.ra?this.l[this.ra+a]:c};g.ad=function(){if(this.ra===this.end)throw Error("-drop-first of empty chunk");return new Qe(this.l,this.ra+1,this.end)};g.ja=function(b,a){return Uc(this.l,a,this.l[this.ra],this.ra+1)};g.ka=function(b,a,c){return Uc(this.l,a,c,this.ra)};
	function Se(b,a,c,d){this.fb=b;this.Xa=a;this.w=c;this.i=d;this.m=31850732;this.A=1536}g=Se.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.w};g.qa=function(){if(1<Va(this.fb))return new Se($b(this.fb),this.Xa,this.w,null);var b=Kb(this.Xa);return null==b?null:b};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(Bc,this.w)};
	g.ia=function(){return w.b(this.fb,0)};g.ma=function(){return 1<Va(this.fb)?new Se($b(this.fb),this.Xa,this.w,null):null==this.Xa?Bc:this.Xa};g.J=function(){return this};g.uc=function(){return this.fb};g.vc=function(){return null==this.Xa?Bc:this.Xa};g.P=function(b,a){return new Se(this.fb,this.Xa,a,this.i)};g.O=function(b,a){return bd(a,this)};g.tc=function(){return null==this.Xa?null:this.Xa};Se.prototype[La]=function(){return Dc(this)};
	function Te(b,a){return 0===Va(b)?a:new Se(b,a,null,null)}function We(b,a){b.add(a)}function Xe(b){return b.fb()}function Zd(b){for(var a=[];;)if(q(b))a.push(D(b)),b=E(b);else return a}function Ye(b,a){if(Vc(b))return J(b);for(var c=b,d=a,e=0;;)if(0<d&&q(c))c=E(c),--d,e+=1;else return e}
	var Ze=function Ze(a){return null==a?null:null==E(a)?q(D(a)):bd(D(a),Ze(E(a)))},R=function R(){switch(arguments.length){case 0:return R.R();case 1:return R.c(arguments[0]);case 2:return R.b(arguments[0],arguments[1]);default:return R.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};R.R=function(){return new Ne(null,function(){return null},null,null)};R.c=function(b){return new Ne(null,function(){return b},null,null)};
	R.b=function(b,a){return new Ne(null,function(){var c=q(b);return c?Ed(c)?Te(y(c),R.b(A(c),a)):bd(D(c),R.b(Ac(c),a)):a},null,null)};R.j=function(b,a,c){return function e(a,b){return new Ne(null,function(){var c=q(a);return c?Ed(c)?Te(y(c),e(A(c),b)):bd(D(c),e(Ac(c),b)):r(b)?e(D(b),E(b)):null},null,null)}(R.b(b,a),c)};R.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return R.j(a,b,c)};R.H=2;function $e(b){return q(b)}function af(b){return Sb(b)}function ef(b){return Vb(b)}
	var gf=function gf(){switch(arguments.length){case 0:return gf.R();case 1:return gf.c(arguments[0]);case 2:return gf.b(arguments[0],arguments[1]);default:return gf.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};gf.R=function(){return Sb(I)};gf.c=function(b){return b};gf.b=function(b,a){return Ub(b,a)};gf.j=function(b,a,c){for(;;)if(b=Ub(b,a),r(c))a=D(c),c=E(c);else return b};gf.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return gf.j(a,b,c)};gf.H=2;
	var hf=function hf(){switch(arguments.length){case 3:return hf.g(arguments[0],arguments[1],arguments[2]);default:return hf.j(arguments[0],arguments[1],arguments[2],new za(Array.prototype.slice.call(arguments,3),0))}};hf.g=function(b,a,c){return Wb(b,a,c)};hf.j=function(b,a,c,d){for(;;)if(b=Wb(b,a,c),r(d))a=D(d),c=hd(d),d=E(E(d));else return b};hf.F=function(b){var a=D(b),c=E(b);b=D(c);var d=E(c),c=D(d),d=E(d);return hf.j(a,b,c,d)};hf.H=3;
	function jf(b,a,c){var d=q(c);if(0===a)return b.R?b.R():b.call(null);c=bb(d);var e=cb(d);if(1===a)return b.c?b.c(c):b.c?b.c(c):b.call(null,c);var d=bb(e),f=cb(e);if(2===a)return b.b?b.b(c,d):b.b?b.b(c,d):b.call(null,c,d);var e=bb(f),h=cb(f);if(3===a)return b.g?b.g(c,d,e):b.g?b.g(c,d,e):b.call(null,c,d,e);var f=bb(h),k=cb(h);if(4===a)return b.S?b.S(c,d,e,f):b.S?b.S(c,d,e,f):b.call(null,c,d,e,f);var h=bb(k),l=cb(k);if(5===a)return b.Z?b.Z(c,d,e,f,h):b.Z?b.Z(c,d,e,f,h):b.call(null,c,d,e,f,h);var k=bb(l),
	n=cb(l);if(6===a)return b.wa?b.wa(c,d,e,f,h,k):b.wa?b.wa(c,d,e,f,h,k):b.call(null,c,d,e,f,h,k);var l=bb(n),p=cb(n);if(7===a)return b.rb?b.rb(c,d,e,f,h,k,l):b.rb?b.rb(c,d,e,f,h,k,l):b.call(null,c,d,e,f,h,k,l);var n=bb(p),t=cb(p);if(8===a)return b.sb?b.sb(c,d,e,f,h,k,l,n):b.sb?b.sb(c,d,e,f,h,k,l,n):b.call(null,c,d,e,f,h,k,l,n);var p=bb(t),x=cb(t);if(9===a)return b.tb?b.tb(c,d,e,f,h,k,l,n,p):b.tb?b.tb(c,d,e,f,h,k,l,n,p):b.call(null,c,d,e,f,h,k,l,n,p);var t=bb(x),z=cb(x);if(10===a)return b.gb?b.gb(c,
	d,e,f,h,k,l,n,p,t):b.gb?b.gb(c,d,e,f,h,k,l,n,p,t):b.call(null,c,d,e,f,h,k,l,n,p,t);var x=bb(z),B=cb(z);if(11===a)return b.hb?b.hb(c,d,e,f,h,k,l,n,p,t,x):b.hb?b.hb(c,d,e,f,h,k,l,n,p,t,x):b.call(null,c,d,e,f,h,k,l,n,p,t,x);var z=bb(B),F=cb(B);if(12===a)return b.ib?b.ib(c,d,e,f,h,k,l,n,p,t,x,z):b.ib?b.ib(c,d,e,f,h,k,l,n,p,t,x,z):b.call(null,c,d,e,f,h,k,l,n,p,t,x,z);var B=bb(F),K=cb(F);if(13===a)return b.jb?b.jb(c,d,e,f,h,k,l,n,p,t,x,z,B):b.jb?b.jb(c,d,e,f,h,k,l,n,p,t,x,z,B):b.call(null,c,d,e,f,h,k,l,
	n,p,t,x,z,B);var F=bb(K),N=cb(K);if(14===a)return b.kb?b.kb(c,d,e,f,h,k,l,n,p,t,x,z,B,F):b.kb?b.kb(c,d,e,f,h,k,l,n,p,t,x,z,B,F):b.call(null,c,d,e,f,h,k,l,n,p,t,x,z,B,F);var K=bb(N),W=cb(N);if(15===a)return b.lb?b.lb(c,d,e,f,h,k,l,n,p,t,x,z,B,F,K):b.lb?b.lb(c,d,e,f,h,k,l,n,p,t,x,z,B,F,K):b.call(null,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K);var N=bb(W),xa=cb(W);if(16===a)return b.mb?b.mb(c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N):b.mb?b.mb(c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N):b.call(null,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N);var W=
	bb(xa),Ua=cb(xa);if(17===a)return b.nb?b.nb(c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W):b.nb?b.nb(c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W):b.call(null,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W);var xa=bb(Ua),gb=cb(Ua);if(18===a)return b.ob?b.ob(c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa):b.ob?b.ob(c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa):b.call(null,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa);Ua=bb(gb);gb=cb(gb);if(19===a)return b.pb?b.pb(c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa,Ua):b.pb?b.pb(c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa,Ua):b.call(null,
	c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa,Ua);var aa=bb(gb);cb(gb);if(20===a)return b.qb?b.qb(c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa,Ua,aa):b.qb?b.qb(c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa,Ua,aa):b.call(null,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,xa,Ua,aa);throw Error("Only up to 20 arguments supported on functions");}
	function ud(){switch(arguments.length){case 2:return kf(arguments[0],arguments[1]);case 3:return lf(arguments[0],arguments[1],arguments[2]);case 4:return mf(arguments[0],arguments[1],arguments[2],arguments[3]);case 5:return nf(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4]);default:return of(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4],new za(Array.prototype.slice.call(arguments,5),0))}}
	function kf(b,a){var c=b.H;if(b.F){var d=Ye(a,c+1);return d<=c?jf(b,d,a):b.F(a)}return b.apply(b,Zd(a))}function lf(b,a,c){a=bd(a,c);c=b.H;if(b.F){var d=Ye(a,c+1);return d<=c?jf(b,d,a):b.F(a)}return b.apply(b,Zd(a))}function mf(b,a,c,d){a=bd(a,bd(c,d));c=b.H;return b.F?(d=Ye(a,c+1),d<=c?jf(b,d,a):b.F(a)):b.apply(b,Zd(a))}function nf(b,a,c,d,e){a=bd(a,bd(c,bd(d,e)));c=b.H;return b.F?(d=Ye(a,c+1),d<=c?jf(b,d,a):b.F(a)):b.apply(b,Zd(a))}
	function of(b,a,c,d,e,f){a=bd(a,bd(c,bd(d,bd(e,Ze(f)))));c=b.H;return b.F?(d=Ye(a,c+1),d<=c?jf(b,d,a):b.F(a)):b.apply(b,Zd(a))}var pf=function pf(){switch(arguments.length){case 1:return pf.c(arguments[0]);case 2:return pf.b(arguments[0],arguments[1]);default:return pf.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};pf.c=function(){return!1};pf.b=function(b,a){return!G.b(b,a)};pf.j=function(b,a,c){return Fa(mf(G,b,a,c))};
	pf.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return pf.j(a,b,c)};pf.H=2;function qf(b){return q(b)?b:null}function rf(b,a){for(;;){if(null==q(a))return!0;var c;c=D(a);c=b.c?b.c(c):b.call(null,c);if(r(c)){c=b;var d=E(a);b=c;a=d}else return!1}}function sf(b,a){for(;;)if(q(a)){var c;c=D(a);c=b.c?b.c(c):b.call(null,c);if(r(c))return c;c=b;var d=E(a);b=c;a=d}else return null}
	function tf(b){if("number"===typeof b&&Fa(isNaN(b))&&Infinity!==b&&parseFloat(b)===parseInt(b,10))return 0===(b&1);throw Error([u("Argument must be an integer: "),u(b)].join(""));}function uf(b){return!tf(b)}
	function vf(b){return function(){function a(a,c){return Fa(b.b?b.b(a,c):b.call(null,a,c))}function c(a){return Fa(b.c?b.c(a):b.call(null,a))}function d(){return Fa(b.R?b.R():b.call(null))}var e=null,f=function(){function a(b,d,e){var f=null;if(2<arguments.length){for(var f=0,h=Array(arguments.length-2);f<h.length;)h[f]=arguments[f+2],++f;f=new za(h,0)}return c.call(this,b,d,f)}function c(a,d,e){return Fa(mf(b,a,d,e))}a.H=2;a.F=function(a){var b=D(a);a=E(a);var d=D(a);a=Ac(a);return c(b,d,a)};a.j=
	c;return a}(),e=function(b,e,l){switch(arguments.length){case 0:return d.call(this);case 1:return c.call(this,b);case 2:return a.call(this,b,e);default:var n=null;if(2<arguments.length){for(var n=0,p=Array(arguments.length-2);n<p.length;)p[n]=arguments[n+2],++n;n=new za(p,0)}return f.j(b,e,n)}throw Error("Invalid arity: "+arguments.length);};e.H=2;e.F=f.F;e.R=d;e.c=c;e.b=a;e.j=f.j;return e}()}
	var wf=function wf(){switch(arguments.length){case 0:return wf.R();case 1:return wf.c(arguments[0]);case 2:return wf.b(arguments[0],arguments[1]);case 3:return wf.g(arguments[0],arguments[1],arguments[2]);default:return wf.j(arguments[0],arguments[1],arguments[2],new za(Array.prototype.slice.call(arguments,3),0))}};wf.R=function(){return he};wf.c=function(b){return b};
	wf.b=function(b,a){return function(){function c(c,d,e){c=a.g?a.g(c,d,e):a.call(null,c,d,e);return b.c?b.c(c):b.call(null,c)}function d(c,d){var e=a.b?a.b(c,d):a.call(null,c,d);return b.c?b.c(e):b.call(null,e)}function e(c){c=a.c?a.c(c):a.call(null,c);return b.c?b.c(c):b.call(null,c)}function f(){var c=a.R?a.R():a.call(null);return b.c?b.c(c):b.call(null,c)}var h=null,k=function(){function c(a,b,e,f){var h=null;if(3<arguments.length){for(var h=0,k=Array(arguments.length-3);h<k.length;)k[h]=arguments[h+
	3],++h;h=new za(k,0)}return d.call(this,a,b,e,h)}function d(c,e,f,h){c=nf(a,c,e,f,h);return b.c?b.c(c):b.call(null,c)}c.H=3;c.F=function(a){var b=D(a);a=E(a);var c=D(a);a=E(a);var e=D(a);a=Ac(a);return d(b,c,e,a)};c.j=d;return c}(),h=function(a,b,h,t){switch(arguments.length){case 0:return f.call(this);case 1:return e.call(this,a);case 2:return d.call(this,a,b);case 3:return c.call(this,a,b,h);default:var x=null;if(3<arguments.length){for(var x=0,z=Array(arguments.length-3);x<z.length;)z[x]=arguments[x+
	3],++x;x=new za(z,0)}return k.j(a,b,h,x)}throw Error("Invalid arity: "+arguments.length);};h.H=3;h.F=k.F;h.R=f;h.c=e;h.b=d;h.g=c;h.j=k.j;return h}()};
	wf.g=function(b,a,c){return function(){function d(d,e,f){d=c.g?c.g(d,e,f):c.call(null,d,e,f);d=a.c?a.c(d):a.call(null,d);return b.c?b.c(d):b.call(null,d)}function e(d,e){var f;f=c.b?c.b(d,e):c.call(null,d,e);f=a.c?a.c(f):a.call(null,f);return b.c?b.c(f):b.call(null,f)}function f(d){d=c.c?c.c(d):c.call(null,d);d=a.c?a.c(d):a.call(null,d);return b.c?b.c(d):b.call(null,d)}function h(){var d;d=c.R?c.R():c.call(null);d=a.c?a.c(d):a.call(null,d);return b.c?b.c(d):b.call(null,d)}var k=null,l=function(){function d(a,
	b,c,f){var h=null;if(3<arguments.length){for(var h=0,k=Array(arguments.length-3);h<k.length;)k[h]=arguments[h+3],++h;h=new za(k,0)}return e.call(this,a,b,c,h)}function e(d,f,h,k){d=nf(c,d,f,h,k);d=a.c?a.c(d):a.call(null,d);return b.c?b.c(d):b.call(null,d)}d.H=3;d.F=function(a){var b=D(a);a=E(a);var c=D(a);a=E(a);var d=D(a);a=Ac(a);return e(b,c,d,a)};d.j=e;return d}(),k=function(a,b,c,k){switch(arguments.length){case 0:return h.call(this);case 1:return f.call(this,a);case 2:return e.call(this,a,b);
	case 3:return d.call(this,a,b,c);default:var z=null;if(3<arguments.length){for(var z=0,B=Array(arguments.length-3);z<B.length;)B[z]=arguments[z+3],++z;z=new za(B,0)}return l.j(a,b,c,z)}throw Error("Invalid arity: "+arguments.length);};k.H=3;k.F=l.F;k.R=h;k.c=f;k.b=e;k.g=d;k.j=l.j;return k}()};
	wf.j=function(b,a,c,d){return function(a){return function(){function b(a){var d=null;if(0<arguments.length){for(var d=0,e=Array(arguments.length-0);d<e.length;)e[d]=arguments[d+0],++d;d=new za(e,0)}return c.call(this,d)}function c(b){b=kf(D(a),b);for(var d=E(a);;)if(d)b=D(d).call(null,b),d=E(d);else return b}b.H=0;b.F=function(a){a=q(a);return c(a)};b.j=c;return b}()}(Ge(bd(b,bd(a,bd(c,d)))))};wf.F=function(b){var a=D(b),c=E(b);b=D(c);var d=E(c),c=D(d),d=E(d);return wf.j(a,b,c,d)};wf.H=3;
	function xf(b,a){return function(){function c(c,d,e){return b.S?b.S(a,c,d,e):b.call(null,a,c,d,e)}function d(c,d){return b.g?b.g(a,c,d):b.call(null,a,c,d)}function e(c){return b.b?b.b(a,c):b.call(null,a,c)}function f(){return b.c?b.c(a):b.call(null,a)}var h=null,k=function(){function c(a,b,e,f){var h=null;if(3<arguments.length){for(var h=0,k=Array(arguments.length-3);h<k.length;)k[h]=arguments[h+3],++h;h=new za(k,0)}return d.call(this,a,b,e,h)}function d(c,e,f,h){return of(b,a,c,e,f,H([h],0))}c.H=
	3;c.F=function(a){var b=D(a);a=E(a);var c=D(a);a=E(a);var e=D(a);a=Ac(a);return d(b,c,e,a)};c.j=d;return c}(),h=function(a,b,h,t){switch(arguments.length){case 0:return f.call(this);case 1:return e.call(this,a);case 2:return d.call(this,a,b);case 3:return c.call(this,a,b,h);default:var x=null;if(3<arguments.length){for(var x=0,z=Array(arguments.length-3);x<z.length;)z[x]=arguments[x+3],++x;x=new za(z,0)}return k.j(a,b,h,x)}throw Error("Invalid arity: "+arguments.length);};h.H=3;h.F=k.F;h.R=f;h.c=
	e;h.b=d;h.g=c;h.j=k.j;return h}()}
	function yf(b,a){return function(){function c(c,d,e){c=null==c?a:c;return b.g?b.g(c,d,e):b.call(null,c,d,e)}function d(c,d){var e=null==c?a:c;return b.b?b.b(e,d):b.call(null,e,d)}function e(c){c=null==c?a:c;return b.c?b.c(c):b.call(null,c)}var f=null,h=function(){function c(a,b,e,f){var h=null;if(3<arguments.length){for(var h=0,k=Array(arguments.length-3);h<k.length;)k[h]=arguments[h+3],++h;h=new za(k,0)}return d.call(this,a,b,e,h)}function d(c,e,f,h){return nf(b,null==c?a:c,e,f,h)}c.H=3;c.F=function(a){var b=
	D(a);a=E(a);var c=D(a);a=E(a);var e=D(a);a=Ac(a);return d(b,c,e,a)};c.j=d;return c}(),f=function(a,b,f,p){switch(arguments.length){case 1:return e.call(this,a);case 2:return d.call(this,a,b);case 3:return c.call(this,a,b,f);default:var t=null;if(3<arguments.length){for(var t=0,x=Array(arguments.length-3);t<x.length;)x[t]=arguments[t+3],++t;t=new za(x,0)}return h.j(a,b,f,t)}throw Error("Invalid arity: "+arguments.length);};f.H=3;f.F=h.F;f.c=e;f.b=d;f.g=c;f.j=h.j;return f}()}
	function zf(b,a,c,d){this.state=b;this.w=a;this.pd=d;this.A=16386;this.m=6455296}g=zf.prototype;g.equiv=function(b){return this.u(null,b)};g.u=function(b,a){return this===a};g.wc=function(){return this.state};g.N=function(){return this.w};
	g.fd=function(b,a,c){for(var d=q(this.pd),e=null,f=0,h=0;;)if(h<f){b=e.M(null,h);var k=L(b,0);b=L(b,1);var l=a,n=c;b.S?b.S(k,this,l,n):b.call(null,k,this,l,n);h+=1}else if(b=q(d))d=b,Ed(d)?(e=y(d),d=A(d),b=e,f=J(e),e=b):(b=D(d),k=L(b,0),b=L(b,1),e=k,f=a,h=c,b.S?b.S(e,this,f,h):b.call(null,e,this,f,h),d=E(d),e=null,f=0),h=0;else return null};g.I=function(){return this[da]||(this[da]=++ea)};
	function Af(){switch(arguments.length){case 1:return Bf(arguments[0]);default:var b=arguments[0],a=new za(Array.prototype.slice.call(arguments,1),0),a=Id(a)?kf(Cf,a):a,c=nd(a,ta);nd(a,Df);return new zf(b,c,0,null)}}function Bf(b){return new zf(b,null,0,null)}function Ef(b,a){if(b instanceof zf){var c=b.state;b.state=a;null!=b.pd&&Rb(b,c,a);return a}return cc(b,a)}
	var Ff=function Ff(){switch(arguments.length){case 2:return Ff.b(arguments[0],arguments[1]);case 3:return Ff.g(arguments[0],arguments[1],arguments[2]);case 4:return Ff.S(arguments[0],arguments[1],arguments[2],arguments[3]);default:return Ff.j(arguments[0],arguments[1],arguments[2],arguments[3],new za(Array.prototype.slice.call(arguments,4),0))}};Ff.b=function(b,a){var c;b instanceof zf?(c=b.state,c=a.c?a.c(c):a.call(null,c),c=Ef(b,c)):c=dc.b(b,a);return c};
	Ff.g=function(b,a,c){if(b instanceof zf){var d=b.state;a=a.b?a.b(d,c):a.call(null,d,c);b=Ef(b,a)}else b=dc.g(b,a,c);return b};Ff.S=function(b,a,c,d){if(b instanceof zf){var e=b.state;a=a.g?a.g(e,c,d):a.call(null,e,c,d);b=Ef(b,a)}else b=dc.S(b,a,c,d);return b};Ff.j=function(b,a,c,d,e){return b instanceof zf?Ef(b,nf(a,b.state,c,d,e)):dc.Z(b,a,c,d,e)};Ff.F=function(b){var a=D(b),c=E(b);b=D(c);var d=E(c),c=D(d),e=E(d),d=D(e),e=E(e);return Ff.j(a,b,c,d,e)};Ff.H=4;
	function Gf(b){this.state=b;this.m=32768;this.A=0}Gf.prototype.ed=function(b,a){return this.state=a};Gf.prototype.wc=function(){return this.state};function Hf(b){return new Gf(b)}function If(b,a){ec(b,a)}
	function Jf(){var b=Kf,a=Lf;return function(){function c(c,d,e){var f=b.c?b.c(c):b.call(null,c);if(r(f))return f;f=b.c?b.c(d):b.call(null,d);if(r(f))return f;f=b.c?b.c(e):b.call(null,e);if(r(f))return f;c=a.c?a.c(c):a.call(null,c);if(r(c))return c;d=a.c?a.c(d):a.call(null,d);return r(d)?d:a.c?a.c(e):a.call(null,e)}function d(c,d){var e=b.c?b.c(c):b.call(null,c);if(r(e))return e;e=b.c?b.c(d):b.call(null,d);if(r(e))return e;e=a.c?a.c(c):a.call(null,c);return r(e)?e:a.c?a.c(d):a.call(null,d)}function e(c){var d=
	b.c?b.c(c):b.call(null,c);return r(d)?d:a.c?a.c(c):a.call(null,c)}var f=null,h=function(){function c(a,b,e,f){var h=null;if(3<arguments.length){for(var h=0,k=Array(arguments.length-3);h<k.length;)k[h]=arguments[h+3],++h;h=new za(k,0)}return d.call(this,a,b,e,h)}function d(c,e,h,k){c=f.g(c,e,h);return r(c)?c:sf(function(){return function(c){var d=b.c?b.c(c):b.call(null,c);return r(d)?d:a.c?a.c(c):a.call(null,c)}}(c),k)}c.H=3;c.F=function(a){var b=D(a);a=E(a);var c=D(a);a=E(a);var e=D(a);a=Ac(a);return d(b,
	c,e,a)};c.j=d;return c}(),f=function(a,b,f,p){switch(arguments.length){case 0:return null;case 1:return e.call(this,a);case 2:return d.call(this,a,b);case 3:return c.call(this,a,b,f);default:var t=null;if(3<arguments.length){for(var t=0,x=Array(arguments.length-3);t<x.length;)x[t]=arguments[t+3],++t;t=new za(x,0)}return h.j(a,b,f,t)}throw Error("Invalid arity: "+arguments.length);};f.H=3;f.F=h.F;f.R=function(){return null};f.c=e;f.b=d;f.g=c;f.j=h.j;return f}()}
	var Mf=function Mf(){switch(arguments.length){case 1:return Mf.c(arguments[0]);case 2:return Mf.b(arguments[0],arguments[1]);case 3:return Mf.g(arguments[0],arguments[1],arguments[2]);case 4:return Mf.S(arguments[0],arguments[1],arguments[2],arguments[3]);default:return Mf.j(arguments[0],arguments[1],arguments[2],arguments[3],new za(Array.prototype.slice.call(arguments,4),0))}};
	Mf.c=function(b){return function(a){return function(){function c(c,d){var e=b.c?b.c(d):b.call(null,d);return a.b?a.b(c,e):a.call(null,c,e)}function d(b){return a.c?a.c(b):a.call(null,b)}function e(){return a.R?a.R():a.call(null)}var f=null,h=function(){function c(a,b,e){var f=null;if(2<arguments.length){for(var f=0,h=Array(arguments.length-2);f<h.length;)h[f]=arguments[f+2],++f;f=new za(h,0)}return d.call(this,a,b,f)}function d(c,e,f){e=lf(b,e,f);return a.b?a.b(c,e):a.call(null,c,e)}c.H=2;c.F=function(a){var b=
	D(a);a=E(a);var c=D(a);a=Ac(a);return d(b,c,a)};c.j=d;return c}(),f=function(a,b,f){switch(arguments.length){case 0:return e.call(this);case 1:return d.call(this,a);case 2:return c.call(this,a,b);default:var p=null;if(2<arguments.length){for(var p=0,t=Array(arguments.length-2);p<t.length;)t[p]=arguments[p+2],++p;p=new za(t,0)}return h.j(a,b,p)}throw Error("Invalid arity: "+arguments.length);};f.H=2;f.F=h.F;f.R=e;f.c=d;f.b=c;f.j=h.j;return f}()}};
	Mf.b=function(b,a){return new Ne(null,function(){var c=q(a);if(c){if(Ed(c)){for(var d=y(c),e=J(d),f=Re(e),h=0;;)if(h<e)We(f,function(){var a=w.b(d,h);return b.c?b.c(a):b.call(null,a)}()),h+=1;else break;return Te(Xe(f),Mf.b(b,A(c)))}return bd(function(){var a=D(c);return b.c?b.c(a):b.call(null,a)}(),Mf.b(b,Ac(c)))}return null},null,null)};
	Mf.g=function(b,a,c){return new Ne(null,function(){var d=q(a),e=q(c);if(d&&e){var f=bd,h;h=D(d);var k=D(e);h=b.b?b.b(h,k):b.call(null,h,k);d=f(h,Mf.g(b,Ac(d),Ac(e)))}else d=null;return d},null,null)};Mf.S=function(b,a,c,d){return new Ne(null,function(){var e=q(a),f=q(c),h=q(d);if(e&&f&&h){var k=bd,l;l=D(e);var n=D(f),p=D(h);l=b.g?b.g(l,n,p):b.call(null,l,n,p);e=k(l,Mf.S(b,Ac(e),Ac(f),Ac(h)))}else e=null;return e},null,null)};
	Mf.j=function(b,a,c,d,e){var f=function k(a){return new Ne(null,function(){var b=Mf.b(q,a);return rf(he,b)?bd(Mf.b(D,b),k(Mf.b(Ac,b))):null},null,null)};return Mf.b(function(){return function(a){return kf(b,a)}}(f),f(kd.j(e,d,H([c,a],0))))};Mf.F=function(b){var a=D(b),c=E(b);b=D(c);var d=E(c),c=D(d),e=E(d),d=D(e),e=E(e);return Mf.j(a,b,c,d,e)};Mf.H=4;
	function Nf(b){return function(a){return function(b){return function(){function d(d,e){var f=vb(b),h=ec(b,vb(b)-1),f=0<f?a.b?a.b(d,e):a.call(null,d,e):d;return 0<h?f:Lc(f)?f:new Kc(f)}function e(b){return a.c?a.c(b):a.call(null,b)}function f(){return a.R?a.R():a.call(null)}var h=null,h=function(a,b){switch(arguments.length){case 0:return f.call(this);case 1:return e.call(this,a);case 2:return d.call(this,a,b)}throw Error("Invalid arity: "+arguments.length);};h.R=f;h.c=e;h.b=d;return h}()}(Hf(b))}}
	function Of(b,a){return new Ne(null,function(){if(0<b){var c=q(a);return c?bd(D(c),Of(b-1,Ac(c))):null}return null},null,null)}function Pf(b,a){return new Ne(null,function(c){return function(){return c(b,a)}}(function(a,b){for(;;){var e=q(b);if(0<a&&e){var f=a-1,e=Ac(e);a=f;b=e}else return e}}),null,null)}
	function Qf(b,a){return new Ne(null,function(c){return function(){return c(b,a)}}(function(a,b){for(;;){var e=q(b),f;if(f=e)f=D(e),f=a.c?a.c(f):a.call(null,f);if(r(f))f=a,e=Ac(e),a=f,b=e;else return e}}),null,null)}function Rf(b){return new Ne(null,function(){return bd(b,Rf(b))},null,null)}function Sf(b,a){return Of(b,Rf(a))}function Tf(b){return new Ne(null,function(){return bd(b.R?b.R():b.call(null),Tf(b))},null,null)}function Uf(b,a){return Of(b,Tf(a))}
	function Vf(b,a){return kf(R,lf(Mf,b,a))}function Wf(b){return function(a){return function(){function c(c,d){return r(b.c?b.c(d):b.call(null,d))?a.b?a.b(c,d):a.call(null,c,d):c}function d(b){return a.c?a.c(b):a.call(null,b)}function e(){return a.R?a.R():a.call(null)}var f=null,f=function(a,b){switch(arguments.length){case 0:return e.call(this);case 1:return d.call(this,a);case 2:return c.call(this,a,b)}throw Error("Invalid arity: "+arguments.length);};f.R=e;f.c=d;f.b=c;return f}()}}
	function Xf(b,a){return new Ne(null,function(){var c=q(a);if(c){if(Ed(c)){for(var d=y(c),e=J(d),f=Re(e),h=0;;)if(h<e){var k;k=w.b(d,h);k=b.c?b.c(k):b.call(null,k);r(k)&&(k=w.b(d,h),f.add(k));h+=1}else break;return Te(Xe(f),Xf(b,A(c)))}d=D(c);c=Ac(c);return r(b.c?b.c(d):b.call(null,d))?bd(d,Xf(b,c)):Xf(b,c)}return null},null,null)}function Yf(b,a){return Xf(vf(b),a)}
	var S=function S(){switch(arguments.length){case 2:return S.b(arguments[0],arguments[1]);case 3:return S.g(arguments[0],arguments[1],arguments[2]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}};S.b=function(b,a){return null!=b?b&&(b.A&4||b.rd)?dd(ef(v(Ub,Sb(b),a)),vd(b)):v(Ya,b,a):v(kd,Bc,a)};S.g=function(b,a,c){return b&&(b.A&4||b.rd)?dd(ef(ie(a,gf,Sb(b),c)),vd(b)):ie(a,kd,b,c)};S.H=3;
	function Zf(b,a){return ef(v(function(a,d){return gf.b(a,b.c?b.c(d):b.call(null,d))},Sb(I),a))}function $f(b,a,c){return S.b(I,Mf.g(b,a,c))}function ag(b,a,c){return S.b(I,Mf.S(b,a,c,bg()))}function cg(b,a){return dg(b,a,null)}function dg(b,a,c){var d=Hd;for(a=q(a);;)if(a){var e=b;if(e?e.m&256||e.Gc||(e.m?0:Ga(fb,e)):Ga(fb,e)){b=od(b,D(a),d);if(d===b)return c;a=E(a)}else return c}else return b}
	var eg=function eg(a,c,d){var e=L(c,0);c=ze(c,1);return r(c)?M.g(a,e,eg(nd(a,e),c,d)):M.g(a,e,d)},fg=function fg(){switch(arguments.length){case 3:return fg.g(arguments[0],arguments[1],arguments[2]);case 4:return fg.S(arguments[0],arguments[1],arguments[2],arguments[3]);case 5:return fg.Z(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4]);case 6:return fg.wa(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4],arguments[5]);default:return fg.j(arguments[0],arguments[1],
	arguments[2],arguments[3],arguments[4],arguments[5],new za(Array.prototype.slice.call(arguments,6),0))}};fg.g=function(b,a,c){var d=L(a,0);a=ze(a,1);return r(a)?M.g(b,d,fg.g(nd(b,d),a,c)):M.g(b,d,function(){var a=nd(b,d);return c.c?c.c(a):c.call(null,a)}())};fg.S=function(b,a,c,d){var e=L(a,0);a=ze(a,1);return r(a)?M.g(b,e,fg.S(nd(b,e),a,c,d)):M.g(b,e,function(){var a=nd(b,e);return c.b?c.b(a,d):c.call(null,a,d)}())};
	fg.Z=function(b,a,c,d,e){var f=L(a,0);a=ze(a,1);return r(a)?M.g(b,f,fg.Z(nd(b,f),a,c,d,e)):M.g(b,f,function(){var a=nd(b,f);return c.g?c.g(a,d,e):c.call(null,a,d,e)}())};fg.wa=function(b,a,c,d,e,f){var h=L(a,0);a=ze(a,1);return r(a)?M.g(b,h,fg.wa(nd(b,h),a,c,d,e,f)):M.g(b,h,function(){var a=nd(b,h);return c.S?c.S(a,d,e,f):c.call(null,a,d,e,f)}())};fg.j=function(b,a,c,d,e,f,h){var k=L(a,0);a=ze(a,1);return r(a)?M.g(b,k,of(fg,nd(b,k),a,c,d,H([e,f,h],0))):M.g(b,k,of(c,nd(b,k),d,e,f,H([h],0)))};
	fg.F=function(b){var a=D(b),c=E(b);b=D(c);var d=E(c),c=D(d),e=E(d),d=D(e),f=E(e),e=D(f),h=E(f),f=D(h),h=E(h);return fg.j(a,b,c,d,e,f,h)};fg.H=6;function gg(b,a,c){return M.g(b,a,function(){var d=nd(b,a);return c.c?c.c(d):c.call(null,d)}())}function hg(b,a,c,d){return M.g(b,a,function(){var e=nd(b,a);return c.b?c.b(e,d):c.call(null,e,d)}())}function ig(b,a,c,d,e){return M.g(b,a,function(){var f=nd(b,a);return c.g?c.g(f,d,e):c.call(null,f,d,e)}())}function jg(b,a){this.da=b;this.l=a}
	function mg(b){return new jg(b,[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null])}function ng(b){b=b.s;return 32>b?0:b-1>>>5<<5}function og(b,a,c){for(;;){if(0===a)return c;var d=mg(b);d.l[0]=c;c=d;a-=5}}var pg=function pg(a,c,d,e){var f=new jg(d.da,Qa(d.l)),h=a.s-1>>>c&31;5===c?f.l[h]=e:(d=d.l[h],a=null!=d?pg(a,c-5,d,e):og(null,c-5,e),f.l[h]=a);return f};
	function qg(b,a){throw Error([u("No item "),u(b),u(" in vector of length "),u(a)].join(""));}function rg(b,a){if(a>=ng(b))return b.za;for(var c=b.root,d=b.shift;;)if(0<d)var e=d-5,c=c.l[a>>>d&31],d=e;else return c.l}function sg(b,a){return 0<=a&&a<b.s?rg(b,a):qg(a,b.s)}var tg=function tg(a,c,d,e,f){var h=new jg(d.da,Qa(d.l));if(0===c)h.l[e&31]=f;else{var k=e>>>c&31;a=tg(a,c-5,d.l[k],e,f);h.l[k]=a}return h};function ug(b,a,c,d,e,f){this.K=b;this.Dc=a;this.l=c;this.v=d;this.start=e;this.end=f}
	ug.prototype.Bc=function(){return this.K<this.end};ug.prototype.next=function(){32===this.K-this.Dc&&(this.l=rg(this.v,this.K),this.Dc+=32);var b=this.l[this.K&31];this.K+=1;return b};function T(b,a,c,d,e,f){this.w=b;this.s=a;this.shift=c;this.root=d;this.za=e;this.i=f;this.m=167668511;this.A=8196}g=T.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){return"number"===typeof a?w.g(this,a,c):c};
	g.ac=function(b,a,c){b=0;for(var d=c;;)if(b<this.s){var e=rg(this,b);c=e.length;a:for(var f=0;;)if(f<c){var h=f+b,k=e[f],d=a.g?a.g(d,h,k):a.call(null,d,h,k);if(Lc(d)){e=d;break a}f+=1}else{e=d;break a}if(Lc(e))return a=e,Pc.c?Pc.c(a):Pc.call(null,a);b+=c;d=e}else return d};g.M=function(b,a){return sg(this,a)[a&31]};g.xa=function(b,a,c){return 0<=a&&a<this.s?rg(this,a)[a&31]:c};
	g.Qb=function(b,a,c){if(0<=a&&a<this.s)return ng(this)<=a?(b=Qa(this.za),b[a&31]=c,new T(this.w,this.s,this.shift,this.root,b,null)):new T(this.w,this.s,this.shift,tg(this,this.shift,this.root,a,c),this.za,null);if(a===this.s)return Ya(this,c);throw Error([u("Index "),u(a),u(" out of bounds  [0,"),u(this.s),u("]")].join(""));};g.fc=function(){var b=this.s;return new ug(0,0,0<J(this)?rg(this,0):null,this,0,b)};g.N=function(){return this.w};g.L=function(){return this.s};
	g.gc=function(){return w.b(this,0)};g.hc=function(){return w.b(this,1)};g.Db=function(){return 0<this.s?w.b(this,this.s-1):null};g.Ua=function(){return 0<this.s?new Yc(this,this.s-1,null):null};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){if(a instanceof T)if(this.s===J(a))for(var c=fc(this),d=fc(a);;)if(r(c.Bc())){var e=c.next(),f=d.next();if(!G.b(e,f))return!1}else return!0;else return!1;else return ad(this,a)};
	g.$b=function(){var b=this;return new vg(b.s,b.shift,function(){var a=b.root;return yg.c?yg.c(a):yg.call(null,a)}(),function(){var a=b.za;return zg.c?zg.c(a):zg.call(null,a)}())};g.ca=function(){return dd(I,this.w)};g.ja=function(b,a){return Qc(this,a)};
	g.ka=function(b,a,c){b=0;for(var d=c;;)if(b<this.s){var e=rg(this,b);c=e.length;a:for(var f=0;;)if(f<c){var h=e[f],d=a.b?a.b(d,h):a.call(null,d,h);if(Lc(d)){e=d;break a}f+=1}else{e=d;break a}if(Lc(e))return a=e,Pc.c?Pc.c(a):Pc.call(null,a);b+=c;d=e}else return d};g.T=function(b,a,c){if("number"===typeof a)return ub(this,a,c);throw Error("Vector's key for assoc must be a number.");};
	g.J=function(){if(0===this.s)return null;if(32>=this.s)return new za(this.za,0);var b;a:{b=this.root;for(var a=this.shift;;)if(0<a)a-=5,b=b.l[0];else{b=b.l;break a}}return Ag?Ag(this,b,0,0):Cg.call(null,this,b,0,0)};g.P=function(b,a){return new T(a,this.s,this.shift,this.root,this.za,this.i)};
	g.O=function(b,a){if(32>this.s-ng(this)){for(var c=this.za.length,d=Array(c+1),e=0;;)if(e<c)d[e]=this.za[e],e+=1;else break;d[c]=a;return new T(this.w,this.s+1,this.shift,this.root,d,null)}c=(d=this.s>>>5>1<<this.shift)?this.shift+5:this.shift;d?(d=mg(null),d.l[0]=this.root,e=og(null,this.shift,new jg(null,this.za)),d.l[1]=e):d=pg(this,this.shift,this.root,new jg(null,this.za));return new T(this.w,this.s+1,c,d,[a],null)};
	g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return this.M(null,b);case 3:return this.xa(null,b,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return this.M(null,b)};b.g=function(a,b,d){return this.xa(null,b,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return this.M(null,b)};g.b=function(b,a){return this.xa(null,b,a)};
	var U=new jg(null,[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null]),I=new T(null,0,5,U,[],Gc);function Dg(b){var a=b.length;if(32>a)return new T(null,a,5,U,b,null);for(var c=32,d=(new T(null,32,5,U,b.slice(0,32),null)).$b(null);;)if(c<a)var e=c+1,d=gf.b(d,b[c]),c=e;else return Vb(d)}T.prototype[La]=function(){return Dc(this)};function ce(b){return Ea(b)?Dg(b):Vb(v(Ub,Sb(I),b))}
	var Eg=function Eg(){return Eg.j(0<arguments.length?new za(Array.prototype.slice.call(arguments,0),0):null)};Eg.j=function(b){return b instanceof za&&0===b.K?Dg(b.l):ce(b)};Eg.H=0;Eg.F=function(b){return Eg.j(q(b))};function Fg(b,a,c,d,e,f){this.Ha=b;this.node=a;this.K=c;this.ra=d;this.w=e;this.i=f;this.m=32375020;this.A=1536}g=Fg.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.w};
	g.qa=function(){if(this.ra+1<this.node.length){var b;b=this.Ha;var a=this.node,c=this.K,d=this.ra+1;b=Ag?Ag(b,a,c,d):Cg.call(null,b,a,c,d);return null==b?null:b}return ac(this)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(I,this.w)};g.ja=function(b,a){var c;c=this.Ha;var d=this.K+this.ra,e=J(this.Ha);c=Gg?Gg(c,d,e):Hg.call(null,c,d,e);return Qc(c,a)};
	g.ka=function(b,a,c){b=this.Ha;var d=this.K+this.ra,e=J(this.Ha);b=Gg?Gg(b,d,e):Hg.call(null,b,d,e);return Rc(b,a,c)};g.ia=function(){return this.node[this.ra]};g.ma=function(){if(this.ra+1<this.node.length){var b;b=this.Ha;var a=this.node,c=this.K,d=this.ra+1;b=Ag?Ag(b,a,c,d):Cg.call(null,b,a,c,d);return null==b?Bc:b}return A(this)};g.J=function(){return this};g.uc=function(){var b=this.node;return new Qe(b,this.ra,b.length)};
	g.vc=function(){var b=this.K+this.node.length;if(b<Va(this.Ha)){var a=this.Ha,c=rg(this.Ha,b);return Ag?Ag(a,c,b,0):Cg.call(null,a,c,b,0)}return Bc};g.P=function(b,a){var c=this.Ha,d=this.node,e=this.K,f=this.ra;return Ig?Ig(c,d,e,f,a):Cg.call(null,c,d,e,f,a)};g.O=function(b,a){return bd(a,this)};g.tc=function(){var b=this.K+this.node.length;if(b<Va(this.Ha)){var a=this.Ha,c=rg(this.Ha,b);return Ag?Ag(a,c,b,0):Cg.call(null,a,c,b,0)}return null};Fg.prototype[La]=function(){return Dc(this)};
	function Cg(){switch(arguments.length){case 3:var b=arguments[0],a=arguments[1],c=arguments[2];return new Fg(b,sg(b,a),a,c,null,null);case 4:return Ag(arguments[0],arguments[1],arguments[2],arguments[3]);case 5:return Ig(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function Ag(b,a,c,d){return new Fg(b,a,c,d,null,null)}function Ig(b,a,c,d,e){return new Fg(b,a,c,d,e,null)}
	function Jg(b,a,c,d,e){this.w=b;this.v=a;this.start=c;this.end=d;this.i=e;this.m=167666463;this.A=8192}g=Jg.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){return"number"===typeof a?w.g(this,a,c):c};g.ac=function(b,a,c){b=this.start;for(var d=0;;)if(b<this.end){var e=d,f=w.b(this.v,b);c=a.g?a.g(c,e,f):a.call(null,c,e,f);if(Lc(c))return a=c,Pc.c?Pc.c(a):Pc.call(null,a);d+=1;b+=1}else return c};
	g.M=function(b,a){return 0>a||this.end<=this.start+a?qg(a,this.end-this.start):w.b(this.v,this.start+a)};g.xa=function(b,a,c){return 0>a||this.end<=this.start+a?c:w.g(this.v,this.start+a,c)};g.Qb=function(b,a,c){var d=this.start+a;b=this.w;c=M.g(this.v,d,c);a=this.start;var e=this.end,d=d+1,d=e>d?e:d;return Kg.Z?Kg.Z(b,c,a,d,null):Kg.call(null,b,c,a,d,null)};g.N=function(){return this.w};g.L=function(){return this.end-this.start};g.Db=function(){return w.b(this.v,this.end-1)};
	g.Ua=function(){return this.start!==this.end?new Yc(this,this.end-this.start-1,null):null};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(I,this.w)};g.ja=function(b,a){return Qc(this,a)};g.ka=function(b,a,c){return Rc(this,a,c)};g.T=function(b,a,c){if("number"===typeof a)return ub(this,a,c);throw Error("Subvec's key for assoc must be a number.");};
	g.J=function(){var b=this;return function(a){return function d(e){return e===b.end?null:bd(w.b(b.v,e),new Ne(null,function(){return function(){return d(e+1)}}(a),null,null))}}(this)(b.start)};g.P=function(b,a){var c=this.v,d=this.start,e=this.end,f=this.i;return Kg.Z?Kg.Z(a,c,d,e,f):Kg.call(null,a,c,d,e,f)};g.O=function(b,a){var c=this.w,d=ub(this.v,this.end,a),e=this.start,f=this.end+1;return Kg.Z?Kg.Z(c,d,e,f,null):Kg.call(null,c,d,e,f,null)};
	g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return this.M(null,b);case 3:return this.xa(null,b,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return this.M(null,b)};b.g=function(a,b,d){return this.xa(null,b,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return this.M(null,b)};g.b=function(b,a){return this.xa(null,b,a)};Jg.prototype[La]=function(){return Dc(this)};
	function Kg(b,a,c,d,e){for(;;)if(a instanceof Jg)c=a.start+c,d=a.start+d,a=a.v;else{var f=J(a);if(0>c||0>d||c>f||d>f)throw Error("Index out of bounds");return new Jg(b,a,c,d,e)}}function Hg(){switch(arguments.length){case 2:var b=arguments[0];return Gg(b,arguments[1],J(b));case 3:return Gg(arguments[0],arguments[1],arguments[2]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function Gg(b,a,c){return Kg(null,b,a,c,null)}
	function Lg(b,a){return b===a.da?a:new jg(b,Qa(a.l))}function yg(b){return new jg({},Qa(b.l))}function zg(b){var a=[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null];Gd(b,0,a,0,b.length);return a}var Mg=function Mg(a,c,d,e){d=Lg(a.root.da,d);var f=a.s-1>>>c&31;if(5===c)a=e;else{var h=d.l[f];a=null!=h?Mg(a,c-5,h,e):og(a.root.da,c-5,e)}d.l[f]=a;return d};
	function vg(b,a,c,d){this.s=b;this.shift=a;this.root=c;this.za=d;this.A=88;this.m=275}g=vg.prototype;
	g.Pb=function(b,a){if(this.root.da){if(32>this.s-ng(this))this.za[this.s&31]=a;else{var c=new jg(this.root.da,this.za),d=[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null];d[0]=a;this.za=d;if(this.s>>>5>1<<this.shift){var d=[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null],e=this.shift+
	5;d[0]=this.root;d[1]=og(this.root.da,this.shift,c);this.root=new jg(this.root.da,d);this.shift=e}else this.root=Mg(this,this.shift,this.root,c)}this.s+=1;return this}throw Error("conj! after persistent!");};g.bc=function(){if(this.root.da){this.root.da=null;var b=this.s-ng(this),a=Array(b);Gd(this.za,0,a,0,b);return new T(null,this.s,this.shift,this.root,a,null)}throw Error("persistent! called twice");};
	g.jc=function(b,a,c){if("number"===typeof a)return Xb(this,a,c);throw Error("TransientVector's key for assoc! must be a number.");};
	g.dd=function(b,a,c){var d=this;if(d.root.da){if(0<=a&&a<d.s)return ng(this)<=a?d.za[a&31]=c:(b=function(){return function f(b,k){var l=Lg(d.root.da,k);if(0===b)l.l[a&31]=c;else{var n=a>>>b&31,p=f(b-5,l.l[n]);l.l[n]=p}return l}}(this).call(null,d.shift,d.root),d.root=b),this;if(a===d.s)return Ub(this,c);throw Error([u("Index "),u(a),u(" out of bounds for TransientVector of length"),u(d.s)].join(""));}throw Error("assoc! after persistent!");};
	g.L=function(){if(this.root.da)return this.s;throw Error("count after persistent!");};g.M=function(b,a){if(this.root.da)return sg(this,a)[a&31];throw Error("nth after persistent!");};g.xa=function(b,a,c){return 0<=a&&a<this.s?w.b(this,a):c};g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){return"number"===typeof a?w.g(this,a,c):c};
	g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return this.C(null,b);case 3:return this.B(null,b,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return this.C(null,b)};b.g=function(a,b,d){return this.B(null,b,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return this.C(null,b)};g.b=function(b,a){return this.B(null,b,a)};
	function Ng(b,a,c,d){this.w=b;this.Ja=a;this.Kb=c;this.i=d;this.m=31850572;this.A=0}g=Ng.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.w};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(Bc,this.w)};g.ia=function(){return D(this.Ja)};
	g.ma=function(){var b=E(this.Ja);return b?new Ng(this.w,b,this.Kb,null):null==this.Kb?Wa(this):new Ng(this.w,this.Kb,null,null)};g.J=function(){return this};g.P=function(b,a){return new Ng(a,this.Ja,this.Kb,this.i)};g.O=function(b,a){return bd(a,this)};Ng.prototype[La]=function(){return Dc(this)};function Og(b,a,c,d,e){this.w=b;this.count=a;this.Ja=c;this.Kb=d;this.i=e;this.m=31858766;this.A=8192}g=Og.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};
	g.N=function(){return this.w};g.L=function(){return this.count};g.Db=function(){return D(this.Ja)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(Pg,this.w)};g.ia=function(){return D(this.Ja)};g.ma=function(){return Ac(q(this))};g.J=function(){var b=q(this.Kb),a=this.Ja;return r(r(a)?a:b)?new Ng(null,this.Ja,q(b),null):null};g.P=function(b,a){return new Og(a,this.count,this.Ja,this.Kb,this.i)};
	g.O=function(b,a){var c;r(this.Ja)?(c=this.Kb,c=new Og(this.w,this.count+1,this.Ja,kd.b(r(c)?c:I,a),null)):c=new Og(this.w,this.count+1,kd.b(this.Ja,a),I,null);return c};var Pg=new Og(null,0,null,I,Gc);Og.prototype[La]=function(){return Dc(this)};function Qg(){this.m=2097152;this.A=0}Qg.prototype.equiv=function(b){return this.u(null,b)};Qg.prototype.u=function(){return!1};var Rg=new Qg;
	function Sg(b,a){return Nd(Bd(a)?J(b)===J(a)?rf(he,Mf.b(function(b){return G.b(od(a,D(b),Rg),hd(b))},b)):null:null)}function Tg(b){this.Y=b}Tg.prototype.next=function(){if(null!=this.Y){var b=D(this.Y),a=L(b,0),b=L(b,1);this.Y=E(this.Y);return{value:[a,b],done:!1}}return{value:null,done:!0}};function Ug(b){return new Tg(q(b))}function Vg(b){this.Y=b}Vg.prototype.next=function(){if(null!=this.Y){var b=D(this.Y);this.Y=E(this.Y);return{value:[b,b],done:!1}}return{value:null,done:!0}};
	function Wg(b){return new Vg(q(b))}
	function Xg(b,a){var c;if(a instanceof P)a:{c=b.length;for(var d=a.U,e=0;;){if(c<=e){c=-1;break a}var f=b[e];if(f instanceof P&&d===f.U){c=e;break a}e+=2}}else if(c="string"==typeof a,r(r(c)?c:"number"===typeof a))a:for(c=b.length,d=0;;){if(c<=d){c=-1;break a}if(a===b[d]){c=d;break a}d+=2}else if(a instanceof C)a:for(c=b.length,d=a.Za,e=0;;){if(c<=e){c=-1;break a}f=b[e];if(f instanceof C&&d===f.Za){c=e;break a}e+=2}else if(null==a)a:for(c=b.length,d=0;;){if(c<=d){c=-1;break a}if(null==b[d]){c=d;break a}d+=
	2}else a:for(c=b.length,d=0;;){if(c<=d){c=-1;break a}if(G.b(a,b[d])){c=d;break a}d+=2}return c}function Yg(b,a,c){this.l=b;this.K=a;this.va=c;this.m=32374990;this.A=0}g=Yg.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.va};g.qa=function(){return this.K<this.l.length-2?new Yg(this.l,this.K+2,this.va):null};g.L=function(){return(this.l.length-this.K)/2};g.I=function(){return Fc(this)};g.u=function(b,a){return ad(this,a)};
	g.ca=function(){return dd(Bc,this.va)};g.ja=function(b,a){return ed(a,this)};g.ka=function(b,a,c){return gd(a,c,this)};g.ia=function(){return new T(null,2,5,U,[this.l[this.K],this.l[this.K+1]],null)};g.ma=function(){return this.K<this.l.length-2?new Yg(this.l,this.K+2,this.va):Bc};g.J=function(){return this};g.P=function(b,a){return new Yg(this.l,this.K,a)};g.O=function(b,a){return bd(a,this)};Yg.prototype[La]=function(){return Dc(this)};function Zg(b,a,c){this.l=b;this.K=a;this.s=c}
	Zg.prototype.Bc=function(){return this.K<this.s};Zg.prototype.next=function(){var b=new T(null,2,5,U,[this.l[this.K],this.l[this.K+1]],null);this.K+=2;return b};function m(b,a,c,d){this.w=b;this.s=a;this.l=c;this.i=d;this.m=16647951;this.A=8196}g=m.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.keys=function(){return Dc($g.c?$g.c(this):$g.call(null,this))};g.entries=function(){return Ug(q(this))};
	g.values=function(){return Dc(ah.c?ah.c(this):ah.call(null,this))};g.has=function(b){return Pd(this,b)};g.get=function(b,a){return this.B(null,b,a)};g.forEach=function(b){for(var a=q(this),c=null,d=0,e=0;;)if(e<d){var f=c.M(null,e),h=L(f,0),f=L(f,1);b.b?b.b(f,h):b.call(null,f,h);e+=1}else if(a=q(a))Ed(a)?(c=y(a),a=A(a),h=c,d=J(c),c=h):(c=D(a),h=L(c,0),c=f=L(c,1),b.b?b.b(c,h):b.call(null,c,h),a=E(a),c=null,d=0),e=0;else return null};g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){b=Xg(this.l,a);return-1===b?c:this.l[b+1]};g.ac=function(b,a,c){b=this.l.length;for(var d=0;;)if(d<b){var e=this.l[d],f=this.l[d+1];c=a.g?a.g(c,e,f):a.call(null,c,e,f);if(Lc(c))return a=c,Pc.c?Pc.c(a):Pc.call(null,a);d+=2}else return c};g.fc=function(){return new Zg(this.l,0,2*this.s)};g.N=function(){return this.w};g.L=function(){return this.s};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Hc(this)};
	g.u=function(b,a){if(a&&(a.m&1024||a.wd)){var c=this.l.length;if(this.s===a.L(null))for(var d=0;;)if(d<c){var e=a.B(null,this.l[d],Hd);if(e!==Hd)if(G.b(this.l[d+1],e))d+=2;else return!1;else return!1}else return!0;else return!1}else return Sg(this,a)};g.$b=function(){return new eh({},this.l.length,Qa(this.l))};g.ca=function(){return Ab(Sd,this.w)};g.ja=function(b,a){return ed(a,this)};g.ka=function(b,a,c){return gd(a,c,this)};
	g.W=function(b,a){if(0<=Xg(this.l,a)){var c=this.l.length,d=c-2;if(0===d)return Wa(this);for(var d=Array(d),e=0,f=0;;){if(e>=c)return new m(this.w,this.s-1,d,null);G.b(a,this.l[e])||(d[f]=this.l[e],d[f+1]=this.l[e+1],f+=2);e+=2}}else return this};
	g.T=function(b,a,c){b=Xg(this.l,a);if(-1===b){if(this.s<Rd){b=this.l;for(var d=b.length,e=Array(d+2),f=0;;)if(f<d)e[f]=b[f],f+=1;else break;e[d]=a;e[d+1]=c;return new m(this.w,this.s+1,e,null)}return Ab(kb(S.b(fh,this),a,c),this.w)}if(c===this.l[b+1])return this;a=Qa(this.l);a[b+1]=c;return new m(this.w,this.s,a,null)};g.Nb=function(b,a){return-1!==Xg(this.l,a)};g.J=function(){var b=this.l;return 0<=b.length-2?new Yg(b,0,null):null};g.P=function(b,a){return new m(a,this.s,this.l,this.i)};
	g.O=function(b,a){if(Cd(a))return kb(this,w.b(a,0),w.b(a,1));for(var c=this,d=q(a);;){if(null==d)return c;var e=D(d);if(Cd(e))c=kb(c,w.b(e,0),w.b(e,1)),d=E(d);else throw Error("conj on a map takes map entries or seqables of map entries");}};
	g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return this.C(null,b);case 3:return this.B(null,b,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return this.C(null,b)};b.g=function(a,b,d){return this.B(null,b,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return this.C(null,b)};g.b=function(b,a){return this.B(null,b,a)};var Sd=new m(null,0,[],Ic),Rd=8;
	function gh(b){for(var a=[],c=0;;)if(c<b.length){var d=b[c],e=b[c+1];-1===Xg(a,d)&&(a.push(d),a.push(e));c+=2}else break;return new m(null,a.length/2,a,null)}m.prototype[La]=function(){return Dc(this)};function eh(b,a,c){this.cc=b;this.dc=a;this.l=c;this.m=258;this.A=56}g=eh.prototype;g.L=function(){if(r(this.cc))return ve(this.dc,2);throw Error("count after persistent!");};g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){if(r(this.cc))return b=Xg(this.l,a),-1===b?c:this.l[b+1];throw Error("lookup after persistent!");};
	g.Pb=function(b,a){if(r(this.cc)){if(a?a.m&2048||a.xd||(a.m?0:Ga(nb,a)):Ga(nb,a))return Wb(this,Ce.c?Ce.c(a):Ce.call(null,a),De.c?De.c(a):De.call(null,a));for(var c=q(a),d=this;;){var e=D(c);if(r(e))var f=e,c=E(c),d=Wb(d,function(){var a=f;return Ce.c?Ce.c(a):Ce.call(null,a)}(),function(){var a=f;return De.c?De.c(a):De.call(null,a)}());else return d}}else throw Error("conj! after persistent!");};
	g.bc=function(){if(r(this.cc))return this.cc=!1,new m(null,ve(this.dc,2),this.l,null);throw Error("persistent! called twice");};g.jc=function(b,a,c){var d=this;if(r(d.cc)){b=Xg(this.l,a);if(-1===b)return d.dc+2<=2*Rd?(d.dc+=2,d.l.push(a),d.l.push(c),this):hf.g(function(){var a=d.dc,b=d.l;return hh.b?hh.b(a,b):hh.call(null,a,b)}(),a,c);c!==d.l[b+1]&&(d.l[b+1]=c);return this}throw Error("assoc! after persistent!");};
	function hh(b,a){for(var c=Sb(fh),d=0;;)if(d<b)c=hf.g(c,a[d],a[d+1]),d+=2;else return c}function ih(){this.G=!1}function jh(b,a){return b===a?!0:Q(b,a)?!0:G.b(b,a)}function kh(b,a,c){b=Qa(b);b[a]=c;return b}function lh(b,a){var c=Array(b.length-2);Gd(b,0,c,0,2*a);Gd(b,2*(a+1),c,2*a,c.length-2*a);return c}function mh(b,a,c,d){b=b.Rb(a);b.l[c]=d;return b}
	function nh(b,a,c){for(var d=b.length,e=0,f=c;;)if(e<d){c=b[e];if(null!=c){var h=b[e+1];c=a.g?a.g(f,c,h):a.call(null,f,c,h)}else c=b[e+1],c=null!=c?c.Ub(a,f):f;if(Lc(c))return b=c,Pc.c?Pc.c(b):Pc.call(null,b);e+=2;f=c}else return f}function oh(b,a,c){this.da=b;this.ha=a;this.l=c}g=oh.prototype;g.Rb=function(b){if(b===this.da)return this;var a=we(this.ha),c=Array(0>a?4:2*(a+1));Gd(this.l,0,c,0,2*a);return new oh(b,this.ha,c)};g.oc=function(){var b=this.l;return ph?ph(b):qh.call(null,b)};
	g.Ub=function(b,a){return nh(this.l,b,a)};g.Gb=function(b,a,c,d){var e=1<<(a>>>b&31);if(0===(this.ha&e))return d;var f=we(this.ha&e-1),e=this.l[2*f],f=this.l[2*f+1];return null==e?f.Gb(b+5,a,c,d):jh(c,e)?f:d};
	g.Ra=function(b,a,c,d,e,f){var h=1<<(c>>>a&31),k=we(this.ha&h-1);if(0===(this.ha&h)){var l=we(this.ha);if(2*l<this.l.length){b=this.Rb(b);a=b.l;f.G=!0;a:for(c=2*(l-k),f=2*k+(c-1),l=2*(k+1)+(c-1);;){if(0===c)break a;a[l]=a[f];--l;--c;--f}a[2*k]=d;a[2*k+1]=e;b.ha|=h;return b}if(16<=l){k=[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null];k[c>>>a&31]=rh.Ra(b,a+5,c,d,e,f);for(e=d=0;;)if(32>d)0!==
	(this.ha>>>d&1)&&(k[d]=null!=this.l[e]?rh.Ra(b,a+5,sc(this.l[e]),this.l[e],this.l[e+1],f):this.l[e+1],e+=2),d+=1;else break;return new sh(b,l+1,k)}a=Array(2*(l+4));Gd(this.l,0,a,0,2*k);a[2*k]=d;a[2*k+1]=e;Gd(this.l,2*k,a,2*(k+1),2*(l-k));f.G=!0;b=this.Rb(b);b.l=a;b.ha|=h;return b}l=this.l[2*k];h=this.l[2*k+1];if(null==l)return l=h.Ra(b,a+5,c,d,e,f),l===h?this:mh(this,b,2*k+1,l);if(jh(d,l))return e===h?this:mh(this,b,2*k+1,e);f.G=!0;f=a+5;d=th?th(b,f,l,h,c,d,e):uh.call(null,b,f,l,h,c,d,e);e=2*k;k=
	2*k+1;b=this.Rb(b);b.l[e]=null;b.l[k]=d;return b};
	g.Qa=function(b,a,c,d,e){var f=1<<(a>>>b&31),h=we(this.ha&f-1);if(0===(this.ha&f)){var k=we(this.ha);if(16<=k){h=[null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null,null];h[a>>>b&31]=rh.Qa(b+5,a,c,d,e);for(d=c=0;;)if(32>c)0!==(this.ha>>>c&1)&&(h[c]=null!=this.l[d]?rh.Qa(b+5,sc(this.l[d]),this.l[d],this.l[d+1],e):this.l[d+1],d+=2),c+=1;else break;return new sh(null,k+1,h)}b=Array(2*(k+1));Gd(this.l,
	0,b,0,2*h);b[2*h]=c;b[2*h+1]=d;Gd(this.l,2*h,b,2*(h+1),2*(k-h));e.G=!0;return new oh(null,this.ha|f,b)}var l=this.l[2*h],f=this.l[2*h+1];if(null==l)return k=f.Qa(b+5,a,c,d,e),k===f?this:new oh(null,this.ha,kh(this.l,2*h+1,k));if(jh(c,l))return d===f?this:new oh(null,this.ha,kh(this.l,2*h+1,d));e.G=!0;e=this.ha;k=this.l;b+=5;b=vh?vh(b,l,f,a,c,d):uh.call(null,b,l,f,a,c,d);c=2*h;h=2*h+1;d=Qa(k);d[c]=null;d[h]=b;return new oh(null,e,d)};
	g.pc=function(b,a,c){var d=1<<(a>>>b&31);if(0===(this.ha&d))return this;var e=we(this.ha&d-1),f=this.l[2*e],h=this.l[2*e+1];return null==f?(b=h.pc(b+5,a,c),b===h?this:null!=b?new oh(null,this.ha,kh(this.l,2*e+1,b)):this.ha===d?null:new oh(null,this.ha^d,lh(this.l,e))):jh(c,f)?new oh(null,this.ha^d,lh(this.l,e)):this};var rh=new oh(null,0,[]);function sh(b,a,c){this.da=b;this.s=a;this.l=c}g=sh.prototype;g.Rb=function(b){return b===this.da?this:new sh(b,this.s,Qa(this.l))};
	g.oc=function(){var b=this.l;return wh?wh(b):xh.call(null,b)};g.Ub=function(b,a){for(var c=this.l.length,d=0,e=a;;)if(d<c){var f=this.l[d];if(null!=f&&(e=f.Ub(b,e),Lc(e)))return c=e,Pc.c?Pc.c(c):Pc.call(null,c);d+=1}else return e};g.Gb=function(b,a,c,d){var e=this.l[a>>>b&31];return null!=e?e.Gb(b+5,a,c,d):d};g.Ra=function(b,a,c,d,e,f){var h=c>>>a&31,k=this.l[h];if(null==k)return b=mh(this,b,h,rh.Ra(b,a+5,c,d,e,f)),b.s+=1,b;a=k.Ra(b,a+5,c,d,e,f);return a===k?this:mh(this,b,h,a)};
	g.Qa=function(b,a,c,d,e){var f=a>>>b&31,h=this.l[f];if(null==h)return new sh(null,this.s+1,kh(this.l,f,rh.Qa(b+5,a,c,d,e)));b=h.Qa(b+5,a,c,d,e);return b===h?this:new sh(null,this.s,kh(this.l,f,b))};
	g.pc=function(b,a,c){var d=a>>>b&31,e=this.l[d];if(null!=e){b=e.pc(b+5,a,c);if(b===e)d=this;else if(null==b)if(8>=this.s)a:{e=this.l;b=e.length;a=Array(2*(this.s-1));c=0;for(var f=1,h=0;;)if(c<b)c!==d&&null!=e[c]&&(a[f]=e[c],f+=2,h|=1<<c),c+=1;else{d=new oh(null,h,a);break a}}else d=new sh(null,this.s-1,kh(this.l,d,b));else d=new sh(null,this.s,kh(this.l,d,b));return d}return this};function yh(b,a,c){a*=2;for(var d=0;;)if(d<a){if(jh(c,b[d]))return d;d+=2}else return-1}
	function zh(b,a,c,d){this.da=b;this.ub=a;this.s=c;this.l=d}g=zh.prototype;g.Rb=function(b){if(b===this.da)return this;var a=Array(2*(this.s+1));Gd(this.l,0,a,0,2*this.s);return new zh(b,this.ub,this.s,a)};g.oc=function(){var b=this.l;return ph?ph(b):qh.call(null,b)};g.Ub=function(b,a){return nh(this.l,b,a)};g.Gb=function(b,a,c,d){b=yh(this.l,this.s,c);return 0>b?d:jh(c,this.l[b])?this.l[b+1]:d};
	g.Ra=function(b,a,c,d,e,f){if(c===this.ub){a=yh(this.l,this.s,d);if(-1===a){if(this.l.length>2*this.s)return a=2*this.s,c=2*this.s+1,b=this.Rb(b),b.l[a]=d,b.l[c]=e,f.G=!0,b.s+=1,b;c=this.l.length;a=Array(c+2);Gd(this.l,0,a,0,c);a[c]=d;a[c+1]=e;f.G=!0;d=this.s+1;b===this.da?(this.l=a,this.s=d,b=this):b=new zh(this.da,this.ub,d,a);return b}return this.l[a+1]===e?this:mh(this,b,a+1,e)}return(new oh(b,1<<(this.ub>>>a&31),[null,this,null,null])).Ra(b,a,c,d,e,f)};
	g.Qa=function(b,a,c,d,e){return a===this.ub?(b=yh(this.l,this.s,c),-1===b?(b=2*this.s,a=Array(b+2),Gd(this.l,0,a,0,b),a[b]=c,a[b+1]=d,e.G=!0,new zh(null,this.ub,this.s+1,a)):G.b(this.l[b],d)?this:new zh(null,this.ub,this.s,kh(this.l,b+1,d))):(new oh(null,1<<(this.ub>>>b&31),[null,this])).Qa(b,a,c,d,e)};g.pc=function(b,a,c){b=yh(this.l,this.s,c);return-1===b?this:1===this.s?null:new zh(null,this.ub,this.s-1,lh(this.l,ve(b,2)))};
	function uh(){switch(arguments.length){case 6:return vh(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4],arguments[5]);case 7:return th(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4],arguments[5],arguments[6]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function vh(b,a,c,d,e,f){var h=sc(a);if(h===d)return new zh(null,h,2,[a,c,e,f]);var k=new ih;return rh.Qa(b,h,a,c,k).Qa(b,d,e,f,k)}
	function th(b,a,c,d,e,f,h){var k=sc(c);if(k===e)return new zh(null,k,2,[c,d,f,h]);var l=new ih;return rh.Ra(b,a,k,c,d,l).Ra(b,a,e,f,h,l)}function Ah(b,a,c,d,e){this.w=b;this.Hb=a;this.K=c;this.Y=d;this.i=e;this.m=32374860;this.A=0}g=Ah.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.w};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(Bc,this.w)};
	g.ja=function(b,a){return ed(a,this)};g.ka=function(b,a,c){return gd(a,c,this)};g.ia=function(){return null==this.Y?new T(null,2,5,U,[this.Hb[this.K],this.Hb[this.K+1]],null):D(this.Y)};g.ma=function(){if(null==this.Y){var b=this.Hb,a=this.K+2;return Bh?Bh(b,a,null):qh.call(null,b,a,null)}var b=this.Hb,a=this.K,c=E(this.Y);return Bh?Bh(b,a,c):qh.call(null,b,a,c)};g.J=function(){return this};g.P=function(b,a){return new Ah(a,this.Hb,this.K,this.Y,this.i)};g.O=function(b,a){return bd(a,this)};
	Ah.prototype[La]=function(){return Dc(this)};function qh(){switch(arguments.length){case 1:return ph(arguments[0]);case 3:return Bh(arguments[0],arguments[1],arguments[2]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function ph(b){return Bh(b,0,null)}
	function Bh(b,a,c){if(null==c)for(c=b.length;;)if(a<c){if(null!=b[a])return new Ah(null,b,a,null,null);var d=b[a+1];if(r(d)&&(d=d.oc(),r(d)))return new Ah(null,b,a+2,d,null);a+=2}else return null;else return new Ah(null,b,a,c,null)}function Ch(b,a,c,d,e){this.w=b;this.Hb=a;this.K=c;this.Y=d;this.i=e;this.m=32374860;this.A=0}g=Ch.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.w};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(Bc,this.w)};g.ja=function(b,a){return ed(a,this)};g.ka=function(b,a,c){return gd(a,c,this)};g.ia=function(){return D(this.Y)};g.ma=function(){var b=this.Hb,a=this.K,c=E(this.Y);return Dh?Dh(null,b,a,c):xh.call(null,null,b,a,c)};g.J=function(){return this};g.P=function(b,a){return new Ch(a,this.Hb,this.K,this.Y,this.i)};g.O=function(b,a){return bd(a,this)};
	Ch.prototype[La]=function(){return Dc(this)};function xh(){switch(arguments.length){case 1:return wh(arguments[0]);case 4:return Dh(arguments[0],arguments[1],arguments[2],arguments[3]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function wh(b){return Dh(null,b,0,null)}function Dh(b,a,c,d){if(null==d)for(d=a.length;;)if(c<d){var e=a[c];if(r(e)&&(e=e.oc(),r(e)))return new Ch(b,a,c+1,e,null);c+=1}else return null;else return new Ch(b,a,c,d,null)}
	function Eh(b,a,c,d,e,f){this.w=b;this.s=a;this.root=c;this.sa=d;this.Ca=e;this.i=f;this.m=16123663;this.A=8196}g=Eh.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.keys=function(){return Dc($g.c?$g.c(this):$g.call(null,this))};g.entries=function(){return Ug(q(this))};g.values=function(){return Dc(ah.c?ah.c(this):ah.call(null,this))};g.has=function(b){return Pd(this,b)};g.get=function(b,a){return this.B(null,b,a)};
	g.forEach=function(b){for(var a=q(this),c=null,d=0,e=0;;)if(e<d){var f=c.M(null,e),h=L(f,0),f=L(f,1);b.b?b.b(f,h):b.call(null,f,h);e+=1}else if(a=q(a))Ed(a)?(c=y(a),a=A(a),h=c,d=J(c),c=h):(c=D(a),h=L(c,0),c=f=L(c,1),b.b?b.b(c,h):b.call(null,c,h),a=E(a),c=null,d=0),e=0;else return null};g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){return null==a?this.sa?this.Ca:c:null==this.root?c:this.root.Gb(0,sc(a),a,c)};
	g.ac=function(b,a,c){this.sa&&(b=this.Ca,c=a.g?a.g(c,null,b):a.call(null,c,null,b));return Lc(c)?Pc.c?Pc.c(c):Pc.call(null,c):null!=this.root?this.root.Ub(a,c):c};g.N=function(){return this.w};g.L=function(){return this.s};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Hc(this)};g.u=function(b,a){return Sg(this,a)};g.$b=function(){return new Fh({},this.root,this.s,this.sa,this.Ca)};g.ca=function(){return Ab(fh,this.w)};
	g.W=function(b,a){if(null==a)return this.sa?new Eh(this.w,this.s-1,this.root,!1,null,null):this;if(null==this.root)return this;var c=this.root.pc(0,sc(a),a);return c===this.root?this:new Eh(this.w,this.s-1,c,this.sa,this.Ca,null)};g.T=function(b,a,c){if(null==a)return this.sa&&c===this.Ca?this:new Eh(this.w,this.sa?this.s:this.s+1,this.root,!0,c,null);b=new ih;a=(null==this.root?rh:this.root).Qa(0,sc(a),a,c,b);return a===this.root?this:new Eh(this.w,b.G?this.s+1:this.s,a,this.sa,this.Ca,null)};
	g.Nb=function(b,a){return null==a?this.sa:null==this.root?!1:this.root.Gb(0,sc(a),a,Hd)!==Hd};g.J=function(){if(0<this.s){var b=null!=this.root?this.root.oc():null;return this.sa?bd(new T(null,2,5,U,[null,this.Ca],null),b):b}return null};g.P=function(b,a){return new Eh(a,this.s,this.root,this.sa,this.Ca,this.i)};
	g.O=function(b,a){if(Cd(a))return kb(this,w.b(a,0),w.b(a,1));for(var c=this,d=q(a);;){if(null==d)return c;var e=D(d);if(Cd(e))c=kb(c,w.b(e,0),w.b(e,1)),d=E(d);else throw Error("conj on a map takes map entries or seqables of map entries");}};
	g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return this.C(null,b);case 3:return this.B(null,b,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return this.C(null,b)};b.g=function(a,b,d){return this.B(null,b,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return this.C(null,b)};g.b=function(b,a){return this.B(null,b,a)};var fh=new Eh(null,0,null,!1,null,Ic);
	function pd(b,a){for(var c=b.length,d=0,e=Sb(fh);;)if(d<c)var f=d+1,e=e.jc(null,b[d],a[d]),d=f;else return Vb(e)}Eh.prototype[La]=function(){return Dc(this)};function Fh(b,a,c,d,e){this.da=b;this.root=a;this.count=c;this.sa=d;this.Ca=e;this.m=258;this.A=56}
	function Gh(b,a){if(b.da){if(a?a.m&2048||a.xd||(a.m?0:Ga(nb,a)):Ga(nb,a))return Hh(b,Ce.c?Ce.c(a):Ce.call(null,a),De.c?De.c(a):De.call(null,a));for(var c=q(a),d=b;;){var e=D(c);if(r(e))var f=e,c=E(c),d=Hh(d,function(){var a=f;return Ce.c?Ce.c(a):Ce.call(null,a)}(),function(){var a=f;return De.c?De.c(a):De.call(null,a)}());else return d}}else throw Error("conj! after persistent");}
	function Hh(b,a,c){if(b.da){if(null==a)b.Ca!==c&&(b.Ca=c),b.sa||(b.count+=1,b.sa=!0);else{var d=new ih;a=(null==b.root?rh:b.root).Ra(b.da,0,sc(a),a,c,d);a!==b.root&&(b.root=a);d.G&&(b.count+=1)}return b}throw Error("assoc! after persistent!");}g=Fh.prototype;g.L=function(){if(this.da)return this.count;throw Error("count after persistent!");};g.C=function(b,a){return null==a?this.sa?this.Ca:null:null==this.root?null:this.root.Gb(0,sc(a),a)};
	g.B=function(b,a,c){return null==a?this.sa?this.Ca:c:null==this.root?c:this.root.Gb(0,sc(a),a,c)};g.Pb=function(b,a){return Gh(this,a)};g.bc=function(){var b;if(this.da)this.da=null,b=new Eh(null,this.count,this.root,this.sa,this.Ca,null);else throw Error("persistent! called twice");return b};g.jc=function(b,a,c){return Hh(this,a,c)};function Lh(b,a,c){for(var d=a;;)if(null!=b)a=c?b.left:b.right,d=kd.b(d,b),b=a;else return d}
	function Mh(b,a,c,d,e){this.w=b;this.stack=a;this.rc=c;this.s=d;this.i=e;this.m=32374862;this.A=0}g=Mh.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.w};g.L=function(){return 0>this.s?J(E(this))+1:this.s};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(Bc,this.w)};g.ja=function(b,a){return ed(a,this)};g.ka=function(b,a,c){return gd(a,c,this)};
	g.ia=function(){var b=this.stack;return null==b?null:sb(b)};g.ma=function(){var b=D(this.stack),b=Lh(this.rc?b.right:b.left,E(this.stack),this.rc);return null!=b?new Mh(null,b,this.rc,this.s-1,null):Bc};g.J=function(){return this};g.P=function(b,a){return new Mh(a,this.stack,this.rc,this.s,this.i)};g.O=function(b,a){return bd(a,this)};Mh.prototype[La]=function(){return Dc(this)};function Nh(b,a,c){return new Mh(null,Lh(b,null,a),a,c,null)}
	function Oh(b,a,c,d){return c instanceof Ph?c.left instanceof Ph?new Ph(c.key,c.G,c.left.eb(),new Qh(b,a,c.right,d,null),null):c.right instanceof Ph?new Ph(c.right.key,c.right.G,new Qh(c.key,c.G,c.left,c.right.left,null),new Qh(b,a,c.right.right,d,null),null):new Qh(b,a,c,d,null):new Qh(b,a,c,d,null)}
	function Rh(b,a,c,d){return d instanceof Ph?d.right instanceof Ph?new Ph(d.key,d.G,new Qh(b,a,c,d.left,null),d.right.eb(),null):d.left instanceof Ph?new Ph(d.left.key,d.left.G,new Qh(b,a,c,d.left.left,null),new Qh(d.key,d.G,d.left.right,d.right,null),null):new Qh(b,a,c,d,null):new Qh(b,a,c,d,null)}
	function Sh(b,a,c,d){if(c instanceof Ph)return new Ph(b,a,c.eb(),d,null);if(d instanceof Qh)return Rh(b,a,c,d.qc());if(d instanceof Ph&&d.left instanceof Qh)return new Ph(d.left.key,d.left.G,new Qh(b,a,c,d.left.left,null),Rh(d.key,d.G,d.left.right,d.right.qc()),null);throw Error("red-black tree invariant violation");}
	var Th=function Th(a,c,d){d=null!=a.left?Th(a.left,c,d):d;if(Lc(d))return Pc.c?Pc.c(d):Pc.call(null,d);var e=a.key,f=a.G;d=c.g?c.g(d,e,f):c.call(null,d,e,f);if(Lc(d))return Pc.c?Pc.c(d):Pc.call(null,d);a=null!=a.right?Th(a.right,c,d):d;return Lc(a)?Pc.c?Pc.c(a):Pc.call(null,a):a};function Qh(b,a,c,d,e){this.key=b;this.G=a;this.left=c;this.right=d;this.i=e;this.m=32402207;this.A=0}g=Qh.prototype;g.Xc=function(b){return b.Zc(this)};
	g.qc=function(){return new Ph(this.key,this.G,this.left,this.right,null)};g.eb=function(){return this};g.Wc=function(b){return b.Yc(this)};g.replace=function(b,a,c,d){return new Qh(b,a,c,d,null)};g.Yc=function(b){return new Qh(b.key,b.G,this,b.right,null)};g.Zc=function(b){return new Qh(b.key,b.G,b.left,this,null)};g.Ub=function(b,a){return Th(this,b,a)};g.C=function(b,a){return w.g(this,a,null)};g.B=function(b,a,c){return w.g(this,a,c)};g.M=function(b,a){return 0===a?this.key:1===a?this.G:null};
	g.xa=function(b,a,c){return 0===a?this.key:1===a?this.G:c};g.Qb=function(b,a,c){return(new T(null,2,5,U,[this.key,this.G],null)).Qb(null,a,c)};g.N=function(){return null};g.L=function(){return 2};g.gc=function(){return this.key};g.hc=function(){return this.G};g.Db=function(){return this.G};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return I};g.ja=function(b,a){return Qc(this,a)};g.ka=function(b,a,c){return Rc(this,a,c)};
	g.T=function(b,a,c){return M.g(new T(null,2,5,U,[this.key,this.G],null),a,c)};g.J=function(){return Ya(Ya(Bc,this.G),this.key)};g.P=function(b,a){return dd(new T(null,2,5,U,[this.key,this.G],null),a)};g.O=function(b,a){return new T(null,3,5,U,[this.key,this.G,a],null)};
	g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return this.C(null,b);case 3:return this.B(null,b,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return this.C(null,b)};b.g=function(a,b,d){return this.B(null,b,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return this.C(null,b)};g.b=function(b,a){return this.B(null,b,a)};Qh.prototype[La]=function(){return Dc(this)};
	function Ph(b,a,c,d,e){this.key=b;this.G=a;this.left=c;this.right=d;this.i=e;this.m=32402207;this.A=0}g=Ph.prototype;g.Xc=function(b){return new Ph(this.key,this.G,this.left,b,null)};g.qc=function(){throw Error("red-black tree invariant violation");};g.eb=function(){return new Qh(this.key,this.G,this.left,this.right,null)};g.Wc=function(b){return new Ph(this.key,this.G,b,this.right,null)};g.replace=function(b,a,c,d){return new Ph(b,a,c,d,null)};
	g.Yc=function(b){return this.left instanceof Ph?new Ph(this.key,this.G,this.left.eb(),new Qh(b.key,b.G,this.right,b.right,null),null):this.right instanceof Ph?new Ph(this.right.key,this.right.G,new Qh(this.key,this.G,this.left,this.right.left,null),new Qh(b.key,b.G,this.right.right,b.right,null),null):new Qh(b.key,b.G,this,b.right,null)};
	g.Zc=function(b){return this.right instanceof Ph?new Ph(this.key,this.G,new Qh(b.key,b.G,b.left,this.left,null),this.right.eb(),null):this.left instanceof Ph?new Ph(this.left.key,this.left.G,new Qh(b.key,b.G,b.left,this.left.left,null),new Qh(this.key,this.G,this.left.right,this.right,null),null):new Qh(b.key,b.G,b.left,this,null)};g.Ub=function(b,a){return Th(this,b,a)};g.C=function(b,a){return w.g(this,a,null)};g.B=function(b,a,c){return w.g(this,a,c)};
	g.M=function(b,a){return 0===a?this.key:1===a?this.G:null};g.xa=function(b,a,c){return 0===a?this.key:1===a?this.G:c};g.Qb=function(b,a,c){return(new T(null,2,5,U,[this.key,this.G],null)).Qb(null,a,c)};g.N=function(){return null};g.L=function(){return 2};g.gc=function(){return this.key};g.hc=function(){return this.G};g.Db=function(){return this.G};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return I};
	g.ja=function(b,a){return Qc(this,a)};g.ka=function(b,a,c){return Rc(this,a,c)};g.T=function(b,a,c){return M.g(new T(null,2,5,U,[this.key,this.G],null),a,c)};g.J=function(){return Ya(Ya(Bc,this.G),this.key)};g.P=function(b,a){return dd(new T(null,2,5,U,[this.key,this.G],null),a)};g.O=function(b,a){return new T(null,3,5,U,[this.key,this.G,a],null)};
	g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return this.C(null,b);case 3:return this.B(null,b,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return this.C(null,b)};b.g=function(a,b,d){return this.B(null,b,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return this.C(null,b)};g.b=function(b,a){return this.B(null,b,a)};Ph.prototype[La]=function(){return Dc(this)};
	var Uh=function Uh(a,c,d,e,f){if(null==c)return new Ph(d,e,null,null,null);var h;h=c.key;h=a.b?a.b(d,h):a.call(null,d,h);if(0===h)return f[0]=c,null;if(0>h)return a=Uh(a,c.left,d,e,f),null!=a?c.Wc(a):null;a=Uh(a,c.right,d,e,f);return null!=a?c.Xc(a):null},Vh=function Vh(a,c){if(null==a)return c;if(null==c)return a;if(a instanceof Ph){if(c instanceof Ph){var d=Vh(a.right,c.left);return d instanceof Ph?new Ph(d.key,d.G,new Ph(a.key,a.G,a.left,d.left,null),new Ph(c.key,c.G,d.right,c.right,null),null):
	new Ph(a.key,a.G,a.left,new Ph(c.key,c.G,d,c.right,null),null)}return new Ph(a.key,a.G,a.left,Vh(a.right,c),null)}if(c instanceof Ph)return new Ph(c.key,c.G,Vh(a,c.left),c.right,null);d=Vh(a.right,c.left);return d instanceof Ph?new Ph(d.key,d.G,new Qh(a.key,a.G,a.left,d.left,null),new Qh(c.key,c.G,d.right,c.right,null),null):Sh(a.key,a.G,a.left,new Qh(c.key,c.G,d,c.right,null))},Wh=function Wh(a,c,d,e){if(null!=c){var f;f=c.key;f=a.b?a.b(d,f):a.call(null,d,f);if(0===f)return e[0]=c,Vh(c.left,c.right);
	if(0>f)return a=Wh(a,c.left,d,e),null!=a||null!=e[0]?c.left instanceof Qh?Sh(c.key,c.G,a,c.right):new Ph(c.key,c.G,a,c.right,null):null;a=Wh(a,c.right,d,e);if(null!=a||null!=e[0])if(c.right instanceof Qh)if(e=c.key,d=c.G,c=c.left,a instanceof Ph)c=new Ph(e,d,c,a.eb(),null);else if(c instanceof Qh)c=Oh(e,d,c.qc(),a);else if(c instanceof Ph&&c.right instanceof Qh)c=new Ph(c.right.key,c.right.G,Oh(c.key,c.G,c.left.qc(),c.right.left),new Qh(e,d,c.right.right,a,null),null);else throw Error("red-black tree invariant violation");
	else c=new Ph(c.key,c.G,c.left,a,null);else c=null;return c}return null},Xh=function Xh(a,c,d,e){var f=c.key,h=a.b?a.b(d,f):a.call(null,d,f);return 0===h?c.replace(f,e,c.left,c.right):0>h?c.replace(f,c.G,Xh(a,c.left,d,e),c.right):c.replace(f,c.G,c.left,Xh(a,c.right,d,e))};function Yh(b,a,c,d,e){this.Ia=b;this.yb=a;this.s=c;this.w=d;this.i=e;this.m=418776847;this.A=8192}g=Yh.prototype;
	g.forEach=function(b){for(var a=q(this),c=null,d=0,e=0;;)if(e<d){var f=c.M(null,e),h=L(f,0),f=L(f,1);b.b?b.b(f,h):b.call(null,f,h);e+=1}else if(a=q(a))Ed(a)?(c=y(a),a=A(a),h=c,d=J(c),c=h):(c=D(a),h=L(c,0),c=f=L(c,1),b.b?b.b(c,h):b.call(null,c,h),a=E(a),c=null,d=0),e=0;else return null};g.get=function(b,a){return this.B(null,b,a)};g.entries=function(){return Ug(q(this))};g.toString=function(){return hc(this)};g.keys=function(){return Dc($g.c?$g.c(this):$g.call(null,this))};
	g.values=function(){return Dc(ah.c?ah.c(this):ah.call(null,this))};g.equiv=function(b){return this.u(null,b)};function Zh(b,a){for(var c=b.yb;;)if(null!=c){var d;d=c.key;d=b.Ia.b?b.Ia.b(a,d):b.Ia.call(null,a,d);if(0===d)return c;c=0>d?c.left:c.right}else return null}g.has=function(b){return Pd(this,b)};g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){b=Zh(this,a);return null!=b?b.G:c};g.ac=function(b,a,c){return null!=this.yb?Th(this.yb,a,c):c};g.N=function(){return this.w};g.L=function(){return this.s};
	g.Ua=function(){return 0<this.s?Nh(this.yb,!1,this.s):null};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Hc(this)};g.u=function(b,a){return Sg(this,a)};g.ca=function(){return new Yh(this.Ia,null,0,this.w,0)};g.W=function(b,a){var c=[null],d=Wh(this.Ia,this.yb,a,c);return null==d?null==md(c,0)?this:new Yh(this.Ia,null,0,this.w,null):new Yh(this.Ia,d.eb(),this.s-1,this.w,null)};
	g.T=function(b,a,c){b=[null];var d=Uh(this.Ia,this.yb,a,c,b);return null==d?(b=md(b,0),G.b(c,b.G)?this:new Yh(this.Ia,Xh(this.Ia,this.yb,a,c),this.s,this.w,null)):new Yh(this.Ia,d.eb(),this.s+1,this.w,null)};g.Nb=function(b,a){return null!=Zh(this,a)};g.J=function(){return 0<this.s?Nh(this.yb,!0,this.s):null};g.P=function(b,a){return new Yh(this.Ia,this.yb,this.s,a,this.i)};
	g.O=function(b,a){if(Cd(a))return kb(this,w.b(a,0),w.b(a,1));for(var c=this,d=q(a);;){if(null==d)return c;var e=D(d);if(Cd(e))c=kb(c,w.b(e,0),w.b(e,1)),d=E(d);else throw Error("conj on a map takes map entries or seqables of map entries");}};
	g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return this.C(null,b);case 3:return this.B(null,b,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return this.C(null,b)};b.g=function(a,b,d){return this.B(null,b,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return this.C(null,b)};g.b=function(b,a){return this.B(null,b,a)};var $h=new Yh(Vd,null,0,null,Ic);Yh.prototype[La]=function(){return Dc(this)};
	var Cf=function Cf(){return Cf.j(0<arguments.length?new za(Array.prototype.slice.call(arguments,0),0):null)};Cf.j=function(b){b=q(b);for(var a=Sb(fh);;)if(b){var c=E(E(b)),a=hf.g(a,D(b),hd(b));b=c}else return Vb(a)};Cf.H=0;Cf.F=function(b){return Cf.j(q(b))};var ai=function ai(){return ai.j(0<arguments.length?new za(Array.prototype.slice.call(arguments,0),0):null)};ai.j=function(b){b=b instanceof za&&0===b.K?b.l:Ba(b);return gh(b)};ai.H=0;ai.F=function(b){return ai.j(q(b))};
	function bi(b,a){this.ta=b;this.va=a;this.m=32374988;this.A=0}g=bi.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.va};g.qa=function(){var b=this.ta,b=(b?b.m&128||b.yc||(b.m?0:Ga(db,b)):Ga(db,b))?this.ta.qa(null):E(this.ta);return null==b?null:new bi(b,this.va)};g.I=function(){return Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(Bc,this.va)};g.ja=function(b,a){return ed(a,this)};
	g.ka=function(b,a,c){return gd(a,c,this)};g.ia=function(){return this.ta.ia(null).gc(null)};g.ma=function(){var b=this.ta,b=(b?b.m&128||b.yc||(b.m?0:Ga(db,b)):Ga(db,b))?this.ta.qa(null):E(this.ta);return null!=b?new bi(b,this.va):Bc};g.J=function(){return this};g.P=function(b,a){return new bi(this.ta,a)};g.O=function(b,a){return bd(a,this)};bi.prototype[La]=function(){return Dc(this)};function $g(b){return(b=q(b))?new bi(b,null):null}function Ce(b){return ob(b)}
	function ci(b,a){this.ta=b;this.va=a;this.m=32374988;this.A=0}g=ci.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.N=function(){return this.va};g.qa=function(){var b=this.ta,b=(b?b.m&128||b.yc||(b.m?0:Ga(db,b)):Ga(db,b))?this.ta.qa(null):E(this.ta);return null==b?null:new ci(b,this.va)};g.I=function(){return Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(Bc,this.va)};g.ja=function(b,a){return ed(a,this)};
	g.ka=function(b,a,c){return gd(a,c,this)};g.ia=function(){return this.ta.ia(null).hc(null)};g.ma=function(){var b=this.ta,b=(b?b.m&128||b.yc||(b.m?0:Ga(db,b)):Ga(db,b))?this.ta.qa(null):E(this.ta);return null!=b?new ci(b,this.va):Bc};g.J=function(){return this};g.P=function(b,a){return new ci(this.ta,a)};g.O=function(b,a){return bd(a,this)};ci.prototype[La]=function(){return Dc(this)};function ah(b){return(b=q(b))?new ci(b,null):null}function De(b){return pb(b)}
	var di=function di(){return di.j(0<arguments.length?new za(Array.prototype.slice.call(arguments,0),0):null)};di.j=function(b){return r(sf(he,b))?de(function(a,b){return kd.b(r(a)?a:Sd,b)},b):null};di.H=0;di.F=function(b){return di.j(q(b))};function ei(b,a){for(var c=Sd,d=q(a);;)if(d)var e=D(d),f=od(b,e,fi),c=pf.b(f,fi)?M.g(c,e,f):c,d=E(d);else return dd(c,vd(b))}function Td(b,a,c){this.w=b;this.Sb=a;this.i=c;this.m=15077647;this.A=8196}g=Td.prototype;g.toString=function(){return hc(this)};
	g.equiv=function(b){return this.u(null,b)};g.keys=function(){return Dc(q(this))};g.entries=function(){return Wg(q(this))};g.values=function(){return Dc(q(this))};g.has=function(b){return Pd(this,b)};g.forEach=function(b){for(var a=q(this),c=null,d=0,e=0;;)if(e<d){var f=c.M(null,e),h=L(f,0),f=L(f,1);b.b?b.b(f,h):b.call(null,f,h);e+=1}else if(a=q(a))Ed(a)?(c=y(a),a=A(a),h=c,d=J(c),c=h):(c=D(a),h=L(c,0),c=f=L(c,1),b.b?b.b(c,h):b.call(null,c,h),a=E(a),c=null,d=0),e=0;else return null};
	g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){return ib(this.Sb,a)?a:c};g.N=function(){return this.w};g.L=function(){return Va(this.Sb)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Hc(this)};g.u=function(b,a){return zd(a)&&J(this)===J(a)&&rf(function(a){return function(b){return Pd(a,b)}}(this),a)};g.$b=function(){return new gi(Sb(this.Sb))};g.ca=function(){return dd(Ud,this.w)};g.zc=function(b,a){return new Td(this.w,mb(this.Sb,a),null)};g.J=function(){return $g(this.Sb)};
	g.P=function(b,a){return new Td(a,this.Sb,this.i)};g.O=function(b,a){return new Td(this.w,M.g(this.Sb,a,null),null)};g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return this.C(null,b);case 3:return this.B(null,b,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return this.C(null,b)};b.g=function(a,b,d){return this.B(null,b,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};
	g.c=function(b){return this.C(null,b)};g.b=function(b,a){return this.B(null,b,a)};var Ud=new Td(null,Sd,Ic);Td.prototype[La]=function(){return Dc(this)};function gi(b){this.xb=b;this.A=136;this.m=259}g=gi.prototype;g.Pb=function(b,a){this.xb=hf.g(this.xb,a,null);return this};g.bc=function(){return new Td(null,Vb(this.xb),null)};g.L=function(){return J(this.xb)};g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){return hb.g(this.xb,a,Hd)===Hd?c:a};
	g.call=function(){function b(a,b,c){return hb.g(this.xb,b,Hd)===Hd?c:b}function a(a,b){return hb.g(this.xb,b,Hd)===Hd?null:b}var c=null,c=function(c,e,f){switch(arguments.length){case 2:return a.call(this,c,e);case 3:return b.call(this,c,e,f)}throw Error("Invalid arity: "+arguments.length);};c.b=a;c.g=b;return c}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return hb.g(this.xb,b,Hd)===Hd?null:b};g.b=function(b,a){return hb.g(this.xb,b,Hd)===Hd?a:b};
	function hi(b,a,c){this.w=b;this.zb=a;this.i=c;this.m=417730831;this.A=8192}g=hi.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};g.keys=function(){return Dc(q(this))};g.entries=function(){return Wg(q(this))};g.values=function(){return Dc(q(this))};g.has=function(b){return Pd(this,b)};
	g.forEach=function(b){for(var a=q(this),c=null,d=0,e=0;;)if(e<d){var f=c.M(null,e),h=L(f,0),f=L(f,1);b.b?b.b(f,h):b.call(null,f,h);e+=1}else if(a=q(a))Ed(a)?(c=y(a),a=A(a),h=c,d=J(c),c=h):(c=D(a),h=L(c,0),c=f=L(c,1),b.b?b.b(c,h):b.call(null,c,h),a=E(a),c=null,d=0),e=0;else return null};g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){b=Zh(this.zb,a);return null!=b?b.key:c};g.N=function(){return this.w};g.L=function(){return J(this.zb)};
	g.Ua=function(){return 0<J(this.zb)?Mf.b(Ce,Nb(this.zb)):null};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Hc(this)};g.u=function(b,a){return zd(a)&&J(this)===J(a)&&rf(function(a){return function(b){return Pd(a,b)}}(this),a)};g.ca=function(){return new hi(this.w,Wa(this.zb),0)};g.zc=function(b,a){return new hi(this.w,O.b(this.zb,a),null)};g.J=function(){return $g(this.zb)};g.P=function(b,a){return new hi(a,this.zb,this.i)};g.O=function(b,a){return new hi(this.w,M.g(this.zb,a,null),null)};
	g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return this.C(null,b);case 3:return this.B(null,b,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return this.C(null,b)};b.g=function(a,b,d){return this.B(null,b,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return this.C(null,b)};g.b=function(b,a){return this.B(null,b,a)};var ii=new hi(null,$h,Ic);hi.prototype[La]=function(){return Dc(this)};
	function ji(b){b=q(b);if(null==b)return Ud;if(b instanceof za&&0===b.K){b=b.l;a:for(var a=0,c=Sb(Ud);;)if(a<b.length)var d=a+1,c=c.Pb(null,b[a]),a=d;else break a;return c.bc(null)}for(d=Sb(Ud);;)if(null!=b)a=E(b),d=d.Pb(null,b.ia(null)),b=a;else return Vb(d)}function ki(){return v(Ya,ii,0<arguments.length?new za(Array.prototype.slice.call(arguments,0),0):null)}
	var li=function li(){switch(arguments.length){case 0:return li.R();case 1:return li.c(arguments[0]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}};
	li.R=function(){return function(b){return function(a){return function(){function c(c,d){if(Pd(Pc.c?Pc.c(a):Pc.call(null,a),d))return c;ec(a,kd.b(vb(a),d));return b.b?b.b(c,d):b.call(null,c,d)}function d(a){return b.c?b.c(a):b.call(null,a)}function e(){return b.R?b.R():b.call(null)}var f=null,f=function(a,b){switch(arguments.length){case 0:return e.call(this);case 1:return d.call(this,a);case 2:return c.call(this,a,b)}throw Error("Invalid arity: "+arguments.length);};f.R=e;f.c=d;f.b=c;return f}()}(Hf(Ud))}};
	li.c=function(b){return function c(b,e){return new Ne(null,function(){return function(b,d){for(;;){var e=b,l=L(e,0);if(e=q(e))if(Pd(d,l))l=Ac(e),e=d,b=l,d=e;else return bd(l,c(Ac(e),kd.b(d,l)));else return null}}.call(null,b,e)},null,null)}(b,Ud)};li.H=1;function mi(b){for(var a=I;;)if(E(b))a=kd.b(a,D(b)),b=E(b);else return q(a)}function Me(b){if(b&&(b.A&4096||b.zd))return b.name;if("string"===typeof b)return b;throw Error([u("Doesn't support name: "),u(b)].join(""));}
	function ni(b,a){for(var c=Sb(Sd),d=q(b),e=q(a);;)if(d&&e)c=hf.g(c,D(d),D(e)),d=E(d),e=E(e);else return Vb(c)}var oi=function oi(){switch(arguments.length){case 2:return oi.b(arguments[0],arguments[1]);case 3:return oi.g(arguments[0],arguments[1],arguments[2]);default:return oi.j(arguments[0],arguments[1],arguments[2],new za(Array.prototype.slice.call(arguments,3),0))}};oi.b=function(b,a){return a};oi.g=function(b,a,c){return(b.c?b.c(a):b.call(null,a))>(b.c?b.c(c):b.call(null,c))?a:c};
	oi.j=function(b,a,c,d){return v(function(a,c){return oi.g(b,a,c)},oi.g(b,a,c),d)};oi.F=function(b){var a=D(b),c=E(b);b=D(c);var d=E(c),c=D(d),d=E(d);return oi.j(a,b,c,d)};oi.H=3;function pi(b,a,c){return new Ne(null,function(){var d=q(c);return d?bd(Of(b,d),pi(b,a,Pf(a,d))):null},null,null)}function qi(b,a){return new Ne(null,function(){var c=q(a);if(c){var d;d=D(c);d=b.c?b.c(d):b.call(null,d);c=r(d)?bd(D(c),qi(b,Ac(c))):null}else c=null;return c},null,null)}
	function ri(b,a,c){this.K=b;this.end=a;this.step=c}ri.prototype.Bc=function(){return 0<this.step?this.K<this.end:this.K>this.end};ri.prototype.next=function(){var b=this.K;this.K+=this.step;return b};function si(b,a,c,d,e){this.w=b;this.start=a;this.end=c;this.step=d;this.i=e;this.m=32375006;this.A=8192}g=si.prototype;g.toString=function(){return hc(this)};g.equiv=function(b){return this.u(null,b)};
	g.M=function(b,a){if(a<Va(this))return this.start+a*this.step;if(this.start>this.end&&0===this.step)return this.start;throw Error("Index out of bounds");};g.xa=function(b,a,c){return a<Va(this)?this.start+a*this.step:this.start>this.end&&0===this.step?this.start:c};g.fc=function(){return new ri(this.start,this.end,this.step)};g.N=function(){return this.w};
	g.qa=function(){return 0<this.step?this.start+this.step<this.end?new si(this.w,this.start+this.step,this.end,this.step,null):null:this.start+this.step>this.end?new si(this.w,this.start+this.step,this.end,this.step,null):null};g.L=function(){return Fa(Kb(this))?0:Math.ceil((this.end-this.start)/this.step)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Fc(this)};g.u=function(b,a){return ad(this,a)};g.ca=function(){return dd(Bc,this.w)};g.ja=function(b,a){return Qc(this,a)};
	g.ka=function(b,a,c){for(b=this.start;;)if(0<this.step?b<this.end:b>this.end){var d=b;c=a.b?a.b(c,d):a.call(null,c,d);if(Lc(c))return a=c,Pc.c?Pc.c(a):Pc.call(null,a);b+=this.step}else return c};g.ia=function(){return null==Kb(this)?null:this.start};g.ma=function(){return null!=Kb(this)?new si(this.w,this.start+this.step,this.end,this.step,null):Bc};g.J=function(){return 0<this.step?this.start<this.end?this:null:this.start>this.end?this:null};
	g.P=function(b,a){return new si(a,this.start,this.end,this.step,this.i)};g.O=function(b,a){return bd(a,this)};si.prototype[La]=function(){return Dc(this)};function bg(){return new si(null,0,Number.MAX_VALUE,1,null)}function ti(b){return new si(null,0,b,1,null)}function ui(b,a){return new T(null,2,5,U,[qi(b,a),Qf(b,a)],null)}
	function vi(b,a){return new Ne(null,function(){var c=q(a);if(c){var d=D(c),e=b.c?b.c(d):b.call(null,d),d=bd(d,qi(function(a,c){return function(a){return G.b(c,b.c?b.c(a):b.call(null,a))}}(d,e,c,c),E(c)));return bd(d,vi(b,q(Pf(J(d),c))))}return null},null,null)}function wi(b){a:for(var a=b;;)if(q(a))a=E(a);else break a;return b}function xi(b,a){if("string"===typeof a){var c=b.exec(a);return G.b(D(c),a)?1===J(c)?D(c):ce(c):null}throw new TypeError("re-matches must match against a string.");}
	function yi(b,a){if("string"===typeof a){var c=b.exec(a);return null==c?null:1===J(c)?D(c):ce(c)}throw new TypeError("re-find must match against a string.");}function zi(b){if(b instanceof RegExp)return b;var a=yi(/^\(\?([idmsux]*)\)/,b),c=L(a,0),a=L(a,1);b=Ae(b,J(c));return new RegExp(b,r(a)?a:"")}
	function V(b,a,c,d,e,f,h){var k=oa;oa=null==oa?null:oa-1;try{if(null!=oa&&0>oa)return Ob(b,"#");Ob(b,c);if(0===wa.c(f))q(h)&&Ob(b,function(){var a=Ai.c(f);return r(a)?a:"..."}());else{if(q(h)){var l=D(h);a.g?a.g(l,b,f):a.call(null,l,b,f)}for(var n=E(h),p=wa.c(f)-1;;)if(!n||null!=p&&0===p){q(n)&&0===p&&(Ob(b,d),Ob(b,function(){var a=Ai.c(f);return r(a)?a:"..."}()));break}else{Ob(b,d);var t=D(n);c=b;h=f;a.g?a.g(t,c,h):a.call(null,t,c,h);var x=E(n);c=p-1;n=x;p=c}}return Ob(b,e)}finally{oa=k}}
	function Bi(b,a){for(var c=q(a),d=null,e=0,f=0;;)if(f<e){var h=d.M(null,f);Ob(b,h);f+=1}else if(c=q(c))d=c,Ed(d)?(c=y(d),e=A(d),d=c,h=J(c),c=e,e=h):(h=D(d),Ob(b,h),c=E(d),d=null,e=0),f=0;else return null}var Ci={'"':'\\"',"\\":"\\\\","\b":"\\b","\f":"\\f","\n":"\\n","\r":"\\r","\t":"\\t"};function Di(b){return[u('"'),u(b.replace(RegExp('[\\\\"\b\f\n\r\t]',"g"),function(a){return Ci[a]})),u('"')].join("")}
	function Ei(b,a,c){if(null==b)return Ob(a,"nil");if(void 0===b)return Ob(a,"#\x3cundefined\x3e");if(r(function(){var a=nd(c,ta);return r(a)?(a=b?b.m&131072||b.yd?!0:b.m?!1:Ga(wb,b):Ga(wb,b))?vd(b):a:a}())){Ob(a,"^");var d=vd(b);Fi.g?Fi.g(d,a,c):Fi.call(null,d,a,c);Ob(a," ")}return null==b?Ob(a,"nil"):b.Jd?b.Sd(b,a,c):b&&(b.m&2147483648||b.aa)?b.D(null,a,c):Ha(b)===Boolean||"number"===typeof b?Ob(a,""+u(b)):null!=b&&b.constructor===Object?(Ob(a,"#js "),d=Mf.b(function(a){return new T(null,2,5,U,[Le.c(a),
	b[a]],null)},Fd(b)),Gi.S?Gi.S(d,Fi,a,c):Gi.call(null,d,Fi,a,c)):Ea(b)?V(a,Fi,"#js ["," ","]",c,b):r("string"==typeof b)?r(sa.c(c))?Ob(a,Di(b)):Ob(a,b):sd(b)?Bi(a,H(["#\x3c",""+u(b),"\x3e"],0)):b instanceof Date?(d=function(a,b){for(var c=""+u(a);;)if(J(c)<b)c=[u("0"),u(c)].join("");else return c},Bi(a,H(['#inst "',""+u(b.getUTCFullYear()),"-",d(b.getUTCMonth()+1,2),"-",d(b.getUTCDate(),2),"T",d(b.getUTCHours(),2),":",d(b.getUTCMinutes(),2),":",d(b.getUTCSeconds(),2),".",d(b.getUTCMilliseconds(),3),
	"-",'00:00"'],0))):r(b instanceof RegExp)?Bi(a,H(['#"',b.source,'"'],0)):(b?b.m&2147483648||b.aa||(b.m?0:Ga(Pb,b)):Ga(Pb,b))?Qb(b,a,c):Bi(a,H(["#\x3c",""+u(b),"\x3e"],0))}function Fi(b,a,c){var d=Hi.c(c);return r(d)?(c=M.g(c,Ii,Ei),d.g?d.g(b,a,c):d.call(null,b,a,c)):Ei(b,a,c)}
	function Ji(b,a){var c;if(xd(b))c="";else{c=u;var d=new ia;a:{var e=new gc(d);Fi(D(b),e,a);for(var f=q(E(b)),h=null,k=0,l=0;;)if(l<k){var n=h.M(null,l);Ob(e," ");Fi(n,e,a);l+=1}else if(f=q(f))h=f,Ed(h)?(f=y(h),k=A(h),h=f,n=J(f),f=k,k=n):(n=D(h),Ob(e," "),Fi(n,e,a),f=E(h),h=null,k=0),l=0;else break a}c=""+c(d)}return c}function X(b){return Ji(b,qa())}
	function Ki(b){var a=M.g(qa(),sa,!1);b=Ji(b,a);ma.c?ma.c(b):ma.call(null,b);r(na)?(b=qa(),ma.c?ma.c("\n"):ma.call(null,"\n"),b=(nd(b,ra),null)):b=null;return b}function Gi(b,a,c,d){return V(c,function(b,c,d){var k=ob(b);a.g?a.g(k,c,d):a.call(null,k,c,d);Ob(c," ");b=pb(b);return a.g?a.g(b,c,d):a.call(null,b,c,d)},"{",", ","}",d,q(b))}Gf.prototype.aa=!0;Gf.prototype.D=function(b,a,c){Ob(a,"#\x3cVolatile: ");Fi(this.state,a,c);return Ob(a,"\x3e")};za.prototype.aa=!0;
	za.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};Ne.prototype.aa=!0;Ne.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};Mh.prototype.aa=!0;Mh.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};Ah.prototype.aa=!0;Ah.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};Qh.prototype.aa=!0;Qh.prototype.D=function(b,a,c){return V(a,Fi,"["," ","]",c,this)};Yg.prototype.aa=!0;Yg.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};
	hi.prototype.aa=!0;hi.prototype.D=function(b,a,c){return V(a,Fi,"#{"," ","}",c,this)};Fg.prototype.aa=!0;Fg.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};Ie.prototype.aa=!0;Ie.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};Yc.prototype.aa=!0;Yc.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};Eh.prototype.aa=!0;Eh.prototype.D=function(b,a,c){return Gi(this,Fi,a,c)};Ch.prototype.aa=!0;Ch.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};
	Jg.prototype.aa=!0;Jg.prototype.D=function(b,a,c){return V(a,Fi,"["," ","]",c,this)};Yh.prototype.aa=!0;Yh.prototype.D=function(b,a,c){return Gi(this,Fi,a,c)};Td.prototype.aa=!0;Td.prototype.D=function(b,a,c){return V(a,Fi,"#{"," ","}",c,this)};Se.prototype.aa=!0;Se.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};zf.prototype.aa=!0;zf.prototype.D=function(b,a,c){Ob(a,"#\x3cAtom: ");Fi(this.state,a,c);return Ob(a,"\x3e")};ci.prototype.aa=!0;
	ci.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};Ph.prototype.aa=!0;Ph.prototype.D=function(b,a,c){return V(a,Fi,"["," ","]",c,this)};T.prototype.aa=!0;T.prototype.D=function(b,a,c){return V(a,Fi,"["," ","]",c,this)};Ng.prototype.aa=!0;Ng.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};Fe.prototype.aa=!0;Fe.prototype.D=function(b,a){return Ob(a,"()")};Og.prototype.aa=!0;Og.prototype.D=function(b,a,c){return V(a,Fi,"#queue ["," ","]",c,q(this))};m.prototype.aa=!0;
	m.prototype.D=function(b,a,c){return Gi(this,Fi,a,c)};si.prototype.aa=!0;si.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};bi.prototype.aa=!0;bi.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};Ee.prototype.aa=!0;Ee.prototype.D=function(b,a,c){return V(a,Fi,"("," ",")",c,this)};C.prototype.ec=!0;C.prototype.Ob=function(b,a){if(a instanceof C)return wc(this,a);throw Error([u("Cannot compare "),u(this),u(" to "),u(a)].join(""));};P.prototype.ec=!0;
	P.prototype.Ob=function(b,a){if(a instanceof P)return Je(this,a);throw Error([u("Cannot compare "),u(this),u(" to "),u(a)].join(""));};Jg.prototype.ec=!0;Jg.prototype.Ob=function(b,a){if(Cd(a))return Wd(this,a);throw Error([u("Cannot compare "),u(this),u(" to "),u(a)].join(""));};T.prototype.ec=!0;T.prototype.Ob=function(b,a){if(Cd(a))return Wd(this,a);throw Error([u("Cannot compare "),u(this),u(" to "),u(a)].join(""));};
	function Li(b){return function(a,c){var d=b.b?b.b(a,c):b.call(null,a,c);return Lc(d)?new Kc(d):d}}function Mi(b){return function(a){return function(){function c(b,c){return v(a,b,c)}function d(a){return b.c?b.c(a):b.call(null,a)}function e(){return b.R?b.R():b.call(null)}var f=null,f=function(a,b){switch(arguments.length){case 0:return e.call(this);case 1:return d.call(this,a);case 2:return c.call(this,a,b)}throw Error("Invalid arity: "+arguments.length);};f.R=e;f.c=d;f.b=c;return f}()}(Li(b))}
	var Ni={},Oi=function Oi(a){if(a?a.ud:a)return a.ud(a);var c;c=Oi[ca(null==a?null:a)];if(!c&&(c=Oi._,!c))throw Ia("IEncodeJS.-clj-\x3ejs",a);return c.call(null,a)};function Pi(b){return(b?r(r(null)?null:b.td)||(b.Eb?0:Ga(Ni,b)):Ga(Ni,b))?Oi(b):"string"===typeof b||"number"===typeof b||b instanceof P||b instanceof C?Qi.c?Qi.c(b):Qi.call(null,b):X(H([b],0))}
	var Qi=function Qi(a){if(null==a)return null;if(a?r(r(null)?null:a.td)||(a.Eb?0:Ga(Ni,a)):Ga(Ni,a))return Oi(a);if(a instanceof P)return Me(a);if(a instanceof C)return""+u(a);if(Bd(a)){var c={};a=q(a);for(var d=null,e=0,f=0;;)if(f<e){var h=d.M(null,f),k=L(h,0),h=L(h,1);c[Pi(k)]=Qi(h);f+=1}else if(a=q(a))Ed(a)?(e=y(a),a=A(a),d=e,e=J(e)):(e=D(a),d=L(e,0),e=L(e,1),c[Pi(d)]=Qi(e),a=E(a),d=null,e=0),f=0;else break;return c}if(yd(a)){c=[];a=q(Mf.b(Qi,a));d=null;for(f=e=0;;)if(f<e)k=d.M(null,f),c.push(k),
	f+=1;else if(a=q(a))d=a,Ed(d)?(a=y(d),f=A(d),d=a,e=J(a),a=f):(a=D(d),c.push(a),a=E(d),d=null,e=0),f=0;else break;return c}return a},Ri={},Si=function Si(a,c){if(a?a.sd:a)return a.sd(a,c);var d;d=Si[ca(null==a?null:a)];if(!d&&(d=Si._,!d))throw Ia("IEncodeClojure.-js-\x3eclj",a);return d.call(null,a,c)};
	function Ti(b){var a=H([new m(null,1,[Ui,!1],null)],0),c=Id(a)?kf(Cf,a):a,d=nd(c,Ui);return function(b,c,d,k){return function n(p){return(p?r(r(null)?null:p.Od)||(p.Eb?0:Ga(Ri,p)):Ga(Ri,p))?Si(p,kf(ai,a)):Id(p)?wi(Mf.b(n,p)):yd(p)?S.b(null==p?null:Wa(p),Mf.b(n,p)):Ea(p)?ce(Mf.b(n,p)):Ha(p)===Object?S.b(Sd,function(){return function(a,b,c,d){return function K(e){return new Ne(null,function(a,b,c,d){return function(){for(;;){var a=q(e);if(a){if(Ed(a)){var b=y(a),c=J(b),f=Re(c);return function(){for(var a=
	0;;)if(a<c){var e=w.b(b,a),h=f,k=U,t;t=e;t=d.c?d.c(t):d.call(null,t);e=new T(null,2,5,k,[t,n(p[e])],null);h.add(e);a+=1}else return!0}()?Te(Xe(f),K(A(a))):Te(Xe(f),null)}var h=D(a);return bd(new T(null,2,5,U,[function(){var a=h;return d.c?d.c(a):d.call(null,a)}(),n(p[h])],null),K(Ac(a)))}return null}}}(a,b,c,d),null,null)}}(b,c,d,k)(Fd(p))}()):p}}(a,c,d,r(d)?Le:u)(b)}function Vi(b){return Math.floor(Math.random()*b)}function Wi(b){return md(b,Vi(J(b)))}
	function Xi(b,a){return ef(v(function(a,d){var e=b.c?b.c(d):b.call(null,d);return hf.g(a,e,kd.b(od(a,e,I),d))},Sb(Sd),a))}function Yi(b,a){this.Xb=b;this.i=a;this.m=2153775104;this.A=2048}g=Yi.prototype;g.toString=function(){return this.Xb};g.equiv=function(b){return this.u(null,b)};g.u=function(b,a){return a instanceof Yi&&this.Xb===a.Xb};g.D=function(b,a){return Ob(a,[u('#uuid "'),u(this.Xb),u('"')].join(""))};
	g.I=function(){if(null==this.i){for(var b=this.Xb,a=0,c=0;c<b.length;++c)a=31*a+b.charCodeAt(c),a%=4294967296;this.i=a}return this.i};g.Ob=function(b,a){return ka(this.Xb,a.Xb)};function Zi(b,a,c){var d=Error();this.message=b;this.data=a;this.$c=c;this.name=d.name;this.description=d.description;this.Kd=d.Kd;this.fileName=d.fileName;this.lineNumber=d.lineNumber;this.columnNumber=d.columnNumber;this.stack=d.stack;return this}Zi.prototype.__proto__=Error.prototype;Zi.prototype.aa=!0;
	Zi.prototype.D=function(b,a,c){Ob(a,"#ExceptionInfo{:message ");Fi(this.message,a,c);r(this.data)&&(Ob(a,", :data "),Fi(this.data,a,c));r(this.$c)&&(Ob(a,", :cause "),Fi(this.$c,a,c));return Ob(a,"}")};Zi.prototype.toString=function(){return hc(this)};function $i(b,a){return new Zi(b,a,null)};var aj=new P("transact","unique","transact/unique",-940992320),bj=new P(null,"used-args","used-args",23596256),cj=new P(null,"args","args",1315556576),dj=new P("parser","in","parser/in",1617442048),ej=new P(null,"find","find",496279456),fj=new P(null,"tempids","tempids",1767509089),gj=new P(null,"schema","schema",-1582001791),hj=new P(null,"female","female",1810186049),ij=new P(null,"tuples","tuples",-676032639),kj=new P("parser","find","parser/find",-801023103),lj=new P(null,"attribute","attribute",
	-2074029119),mj=new P(null,"min","min",444991522),nj=new P("transact","cas","transact/cas",816687170),oj=new P("lookup-ref","unique","lookup-ref/unique",-960647710),pj=new P(null,"expand-kvps","expand-kvps",44141154),qj=new P("db.unique","identity","db.unique/identity",1675950722),rj=new P(null,"last-name","last-name",-1695738974),sj=new P(null,"*","*",-1294732318),tj=new P("datascript.pull-api","expand-rev","datascript.pull-api/expand-rev",2143627459),uj=new P(null,"max-tx","max-tx",1119558339),
	vj=new P(null,"multi?","multi?",-749311069),wj=new P(null,"where","where",-2044795965),xj=new P(null,"v","v",21465059),yj=new P(null,"fn","fn",-1175266204),zj=new P("db","unique","db/unique",329396388),Aj=new P(null,"wildcard","wildcard",-622473020),Bj=new P(null,"done","done",-889844188),ta=new P(null,"meta","meta",1499536964),Cj=new P(null,"variable","variable",-281346492),Dj=new P(null,"age","age",-604307804),Ej=new P(null,"sex","sex",-2056008571),ua=new P(null,"dup","dup",556298533),Fj=new P(null,
	"aevt","aevt",-585148059),Gj=new P(null,"pred","pred",1927423397),Hj=new P(null,"rattr","rattr",-1122345563),Ij=new P(null,"key","key",-1516042587),Jj=new P(null,"element","element",1974019749),Kj=new P(null,"limit","limit",-1355822363),Lj=new P("parser","where","parser/where",-966053850),Mj=new P(null,"datom","datom",-371556090),Nj=new P(null,"male","male",-560253338),Oj=new P(null,"db-after","db-after",-571884666),Rj=new P("parser","rule-vars","parser/rule-vars",-1493174969),Df=new P(null,"validator",
	"validator",-1966190681),Sj=new P(null,"fragment","fragment",826775688),Tj=new P(null,"raw","raw",1604651272),Uj=new P(null,"default","default",-1987822328),Vj=new P(null,"rule-vars","rule-vars",1665972520),Wj=new P(null,"free","free",801364328),Xj=new P(null,"added","added",2057651688),Yj=new P(null,"new","new",-2085437848),Zj=new P(null,"other","other",995793544),ak=new P(null,"bindings","bindings",1271397192),bk=new P("db","valueType","db/valueType",1827971944),ck=new P(null,"symbol","symbol",
	-1038572696),dk=new P(null,"avet","avet",1383857032),ek=new P(null,"sources","sources",-321166424),fk=new P(null,"name","name",1843675177),gk=new P("query","binding","query/binding",698240489),hk=new P("db.type","ref","db.type/ref",-1728373079),ik=new P(null,"value","value",305978217),jk=new P(null,"prefix-context","prefix-context",-1269613591),kk=new P("datascript.pull-api","recursion","datascript.pull-api/recursion",-1897884534),lk=new P(null,"expand-rev","expand-rev",1249112650),mk=new P(null,
	"operation","operation",-1267664310),nk=new P(null,"max-eid","max-eid",2134868075),ok=new P(null,"follows","follows",-757192277),pk=new P(null,"eids","eids",1546550700),qk=new P(null,"state","state",-1988618099),rk=new P(null,"entity-id","entity-id",1485898093),Ii=new P(null,"fallback-impl","fallback-impl",-1501286995),sk=new P(null,"source","source",-433931539),tk=new P("transaction","filtered","transaction/filtered",1699706605),ra=new P(null,"flush-on-newline","flush-on-newline",-151457939),uk=
	new P("db","isComponent","db/isComponent",423352398),vk=new P("db.fn","call","db.fn/call",-151594418),wk=new P("transact","upsert","transact/upsert",412688078),xk=new P(null,"e","e",1381269198),yk=new P(null,"rules","rules",1198912366),zk=new P(null,"elements","elements",657646735),Ak=new P(null,"size","size",1098693007),Bk=new P(null,"eavt","eavt",-666437073),Ck=new P("db.fn","retractEntity","db.fn/retractEntity",-1423535441),Dk=new P(null,"batch","batch",-662921200),Ek=new P("db.unique","value",
	"db.unique/value",276903088),Fk=new P(null,"ts","ts",1617209904),Gk=new P(null,"db-before","db-before",-553691536),sa=new P(null,"readably","readably",1129599760),Ai=new P(null,"more-marker","more-marker",-14717935),Hk=new P(null,"tx-data","tx-data",934159761),Ik=new P(null,"rels","rels",1770187185),Jk=new P("db","retract","db/retract",-1549825231),Kk=new P(null,"binding","binding",539932593),Lk=new P(null,"tx","tx",466630418),Mk=new P(null,"porrl","porrl",-1693905102),Nk=new P("parser","with","parser/with",
	-386255821),Ok=new P(null,"recursion","recursion",-749738765),Pk=new P(null,"seen","seen",-518999789),Qk=new P(null,"median","median",569566131),Rk=new P(null,"with","with",-1536296876),Sk=new P(null,"pending-guards","pending-guards",-1255527308),Tk=new P("db","current-tx","db/current-tx",1600722132),wa=new P(null,"print-length","print-length",1931866356),Uk=new P(null,"max","max",61366548),Vk=new P("db.fn","retractAttribute","db.fn/retractAttribute",937402164),Wk=new P("lookup-ref","syntax","lookup-ref/syntax",
	-317304012),Xk=new P("db.fn","cas","db.fn/cas",-379352172),Yk=new P("entity-id","syntax","entity-id/syntax",1921317045),Zk=new P(null,"specs","specs",1426570741),$k=new P("db","cardinality","db/cardinality",-104975659),al=new P(null,"expected","expected",1583670997),bl=new P("db","id","db/id",-1388397098),cl=new P("entity-id","missing","entity-id/missing",1234588374),dl=new P(null,"attrs","attrs",-2090668713),el=new P(null,"context","context",-830191113),fl=new P("datascript.impl.entity","nf","datascript.impl.entity/nf",
	-953741353),gl=new P(null,"subpattern","subpattern",45002743),hl=new P(null,"error","error",-978969032),il=new P(null,"depth","depth",1768663640),jl=new P("parser","binding","parser/binding",-346395752),kl=new P("schema","validation","schema/validation",1178447161),ll=new P("db","add","db/add",235286841),ml=new P(null,"clauses","clauses",1454841241),nl=new P(null,"form","form",-1624062471),ol=new P(null,"tag","tag",-1290361223),pl=new P(null,"unfiltered-db","unfiltered-db",-1363720391),ql=new P(null,
	"target","target",253001721),rl=new P(null,"eid","eid",559519930),sl=new P(null,"rschema","rschema",-1196134054),tl=new P(null,"tx-meta","tx-meta",1159283194),ul=new P("db.cardinality","many","db.cardinality/many",772806234),vl=new P("transact","syntax","transact/syntax",-299207078),wl=new P(null,"wildcard?","wildcard?",-686044101),Hi=new P(null,"alt-impl","alt-impl",670969595),xl=new P("parser","pull","parser/pull",-2147427204),yl=new P(null,"entity","entity",-450970276),zl=new P(null,"prefix-clauses",
	"prefix-clauses",1294180028),Ui=new P(null,"keywordize-keys","keywordize-keys",1310784252),Al=new P(null,"expand","expand",595248157),Bl=new P(null,"listeners","listeners",394544445),Cl=new P(null,"map","map",1371690461),Hl=new P(null,"kvps","kvps",65308317),Il=new P(null,"test","test",577538877),Jl=new P("db.cardinality","one","db.cardinality/one",1428352190),Kl=new P(null,"required","required",1807647006),Wl=new P(null,"a","a",-2123407586),Xl=new P(null,"datoms","datoms",-290874434),Yl=new P(null,
	"old","old",-1825222690),Zl=new P(null,"in","in",-1531184865),$l=new P(null,"vars","vars",-2046957217),am=new P(null,"pattern","pattern",242135423),fi=new P("cljs.core","not-found","cljs.core/not-found",-1572889185),bm=new P(null,"salary","salary",1283904063),cm=new P("parser","query","parser/query",1877320671),dm=new P(null,"attr","attr",-604132353),em=new P(null,"results","results",-1134170113);var fm=function(b,a){return function(){function c(c,d,e){return Nd(function(){var f=b.c?b.c(c):b.call(null,c);return r(f)&&(f=b.c?b.c(d):b.call(null,d),r(f)&&(f=b.c?b.c(e):b.call(null,e),r(f)&&(f=a.c?a.c(c):a.call(null,c),r(f))))?(f=a.c?a.c(d):a.call(null,d),r(f)?a.c?a.c(e):a.call(null,e):f):f}())}function d(c,d){return Nd(function(){var e=b.c?b.c(c):b.call(null,c);return r(e)&&(e=b.c?b.c(d):b.call(null,d),r(e))?(e=a.c?a.c(c):a.call(null,c),r(e)?a.c?a.c(d):a.call(null,d):e):e}())}function e(c){var d=
	b.c?b.c(c):b.call(null,c);c=r(d)?a.c?a.c(c):a.call(null,c):d;return Nd(c)}var f=null,h=function(){function c(a,b,e,f){var h=null;if(3<arguments.length){for(var h=0,k=Array(arguments.length-3);h<k.length;)k[h]=arguments[h+3],++h;h=new za(k,0)}return d.call(this,a,b,e,h)}function d(c,e,h,k){return Nd(function(){var d=f.g(c,e,h);return r(d)?rf(function(){return function(c){var d=b.c?b.c(c):b.call(null,c);return r(d)?a.c?a.c(c):a.call(null,c):d}}(d),k):d}())}c.H=3;c.F=function(a){var b=D(a);a=E(a);var c=
	D(a);a=E(a);var e=D(a);a=Ac(a);return d(b,c,e,a)};c.j=d;return c}(),f=function(a,b,f,p){switch(arguments.length){case 0:return!0;case 1:return e.call(this,a);case 2:return d.call(this,a,b);case 3:return c.call(this,a,b,f);default:var t=null;if(3<arguments.length){for(var t=0,x=Array(arguments.length-3);t<x.length;)x[t]=arguments[t+3],++t;t=new za(x,0)}return h.j(a,b,f,t)}throw Error("Invalid arity: "+arguments.length);};f.H=3;f.F=h.F;f.R=function(){return!0};f.c=e;f.b=d;f.g=c;f.j=h.j;return f}()}(function(b){return"number"===
	typeof b},ye);var gm={},hm=D(Xf(function(b){return 0!=(32&1<<b)},new si(null,31,-1,-1,null)))+1,im=(1<<hm)-1;function jm(b,a,c,d){for(var e=0,f=te(c);;)if(e<=f){c=e+f>>>1;var h=a[c];0>(b.b?b.b(h,d):b.call(null,h,d))?e=c+1:f=c-1}else return e}function km(b,a,c,d){for(var e=0,f=te(c);;)if(e<=f){c=e+f>>>1;var h=a[c];0<(b.b?b.b(h,d):b.call(null,h,d))?f=c-1:e=c+1}else return e}function lm(b,a,c){var d=a.length,e=jm(b,a,d-1,c);if(d=e<d)a=a[e],d=0===(b.b?b.b(a,c):b.call(null,a,c));return d?e:-1}
	function mm(b,a,c){var d=a.length;b=jm(b,a,d-1,c);return b===d?-1:b}function nm(b){return b[b.length-1]}function om(b,a,c,d,e,f){var h=f.length,k=d-a,l=k+h,n;n=k+h+(c-e);n=Oa?Array(n):Ma.call(null,n);d-=a;for(var p=0;;)if(p<d)n[p+0]=b[p+a],p+=1;else break;a=h-0;for(h=0;;)if(h<a)n[h+k]=f[h+0],h+=1;else break;c-=e;for(f=0;;)if(f<c)n[f+l]=b[f+e],f+=1;else break;return n}function pm(b,a,c,d){return om(b,0,b.length,a,c,d)}
	function qm(b,a){var c=b.length,d=a.length,e=c+d,f=e>>>1,h=e-f,e=Oa?Array(f):Ma.call(null,f),h=Oa?Array(h):Ma.call(null,h);if(c<=f){for(var k=c-0,l=0;;)if(l<k)e[l+0]=b[l+0],l+=1;else break;k=f-c-0;for(l=0;;)if(l<k)e[l+c]=a[l+0],l+=1;else break;d-=f-c;for(k=0;;)if(k<d)h[k+0]=a[k+(f-c)],k+=1;else break}else{k=f-0;for(l=0;;)if(l<k)e[l+0]=b[l+0],l+=1;else break;k=c-f;for(l=0;;)if(l<k)h[l+0]=b[l+f],l+=1;else break;d-=0;for(k=0;;)if(k<d)h[k+(c-f)]=a[k+0],k+=1;else break}return[e,h]}
	function rm(b,a,c,d,e,f){d-=c;if(f=d===f-0)for(f=0;;){if(f===d)return!0;var h=a[f+c],k=e[f+0];if(0!==(b.b?b.b(h,k):b.call(null,h,k)))return!1;f+=1}else return f}function sm(b,a){for(var c=a.length,d=0;;)if(d<c){var e=d,f;f=a[d];f=b.c?b.c(f):b.call(null,f);a[e]=f;d+=1}else break;return a}function tm(b){var a=b.length,c=Sb(I);if(0<a)for(var d=0;;){var e=a-d;if(32>=e){gf.b(c,b.slice(d));break}else 40<=e?(gf.b(c,b.slice(d,d+24)),d+=24):(e>>>=1,gf.b(c,b.slice(d,d+e)),d+=e)}return Zd(Vb(c))}
	function um(b,a){return r(b)?r(a)?[b,a]:[b]:[a]}function vm(b,a,c){return r(b)?r(a)?r(c)?[b,a,c]:[b,a]:r(c)?[b,c]:[b]:r(a)?r(c)?[a,c]:[a]:[c]}
	var wm=function wm(a){if(a?a.Kc:a)return a.Kc(a);var c;c=wm[ca(null==a?null:a)];if(!c&&(c=wm._,!c))throw Ia("INode.node-lim-key",a);return c.call(null,a)},xm=function xm(a){if(a?a.kd:a)return a.keys.length;var c;c=xm[ca(null==a?null:a)];if(!c&&(c=xm._,!c))throw Ia("INode.node-len",a);return c.call(null,a)},ym=function ym(a,c){if(a?a.Mc:a)return a.Mc(a,c);var d;d=ym[ca(null==a?null:a)];if(!d&&(d=ym._,!d))throw Ia("INode.node-merge",a);return d.call(null,a,c)},zm=function zm(a,c){if(a?a.Nc:a)return a.Nc(a,
	c);var d;d=zm[ca(null==a?null:a)];if(!d&&(d=zm._,!d))throw Ia("INode.node-merge-n-split",a);return d.call(null,a,c)},Am=function Am(a,c,d){if(a?a.Lc:a)return a.Lc(a,c,d);var e;e=Am[ca(null==a?null:a)];if(!e&&(e=Am._,!e))throw Ia("INode.node-lookup",a);return e.call(null,a,c,d)},Bm=function Bm(a,c,d){if(a?a.Ic:a)return a.Ic(a,c,d);var e;e=Bm[ca(null==a?null:a)];if(!e&&(e=Bm._,!e))throw Ia("INode.node-conj",a);return e.call(null,a,c,d)},Cm=function Cm(a,c,d,e,f,h){if(a?a.Jc:a)return a.Jc(a,c,d,e,f,
	h);var k;k=Cm[ca(null==a?null:a)];if(!k&&(k=Cm._,!k))throw Ia("INode.node-disj",a);return k.call(null,a,c,d,e,f,h)};function Dm(b,a,c,d){if(r(a))return[b];if(16<xm(b))return vm(c,b,d);if(r(r(c)?16>=xm(c):c))return um(ym(c,b),d);if(r(r(d)?16>=xm(d):d))return um(c,ym(b,d));if(r(r(c)?null==d||xm(c)<xm(d):c))return b=zm(c,b),vm(b[0],b[1],d);b=zm(b,d);return vm(c,b[0],b[1])}function Em(b,a){this.keys=b;this.la=a}g=Em.prototype;g.Kc=function(){return nm(this.keys)};g.kd=function(){return this.keys.length};
	g.Mc=function(b,a){return new Em(this.keys.concat(a.keys),this.la.concat(a.la))};g.Nc=function(b,a){var c=qm(this.keys,a.keys),d=qm(this.la,a.la);return um(new Em(c[0],d[0]),new Em(c[1],d[1]))};g.Lc=function(b,a,c){b=mm(a,this.keys,c);return-1===b?null:Am(this.la[b],a,c)};
	g.Ic=function(b,a,c){b=jm(a,this.keys,this.keys.length-2,c);c=Bm(this.la[b],a,c);if(r(c)){var d=this.keys,e=b+1,f=c.map(wm);a=rm(a,d,b,e,f,f.length)?d:pm(d,b,e,f);b=pm(this.la,b,b+1,c);if(32>=b.length)return[new Em(a,b)];c=b.length>>>1;return[new Em(a.slice(0,c),b.slice(0,c)),new Em(a.slice(c),b.slice(c))]}return null};
	g.Jc=function(b,a,c,d,e,f){var h=mm(a,this.keys,c);if(-1===h)return null;b=0<=h-1?this.la[h-1]:null;var k=h+1<this.la.length?this.la[h+1]:null;c=Cm(this.la[h],a,c,!1,b,k);if(r(c)){b=r(b)?h-1:h;var h=r(k)?2+h:1+h,k=this.keys,l=c.map(wm);a=rm(a,k,b,h,l,l.length)?k:pm(k,b,h,l);c=pm(this.la,b,h,c);return Dm(new Em(a,c),d,e,f)}return null};function Fm(b){this.keys=b}g=Fm.prototype;g.Kc=function(){return nm(this.keys)};g.kd=function(){return this.keys.length};g.Mc=function(b,a){return new Fm(this.keys.concat(a.keys))};
	g.Nc=function(b,a){var c=qm(this.keys,a.keys);return um(new Fm(c[0]),new Fm(c[1]))};g.Lc=function(b,a,c){b=lm(a,this.keys,c);return-1===b?null:this.keys[b]};g.Ic=function(b,a,c){b=jm(a,this.keys,this.keys.length-1,c);var d=this.keys.length,e;if(e=b<d)e=this.keys[b],e=0===(a.b?a.b(c,e):a.call(null,c,e));return e?null:32===d?(a=d+1>>>1,b>a?[new Fm(this.keys.slice(0,a)),new Fm(om(this.keys,a,d,b,b,[c]))]:[new Fm(om(this.keys,0,a,b,b,[c])),new Fm(this.keys.slice(a,d))]):[new Fm(pm(this.keys,b,b,[c]))]};
	g.Jc=function(b,a,c,d,e,f){b=lm(a,this.keys,c);if(-1===b)return null;b=pm(this.keys,b,b+1,[]);return Dm(new Fm(b),d,e,f)};function Gm(b,a,c,d,e,f){this.root=b;this.shift=a;this.s=c;this.Oa=d;this.w=e;this.i=f;this.m=2297303311;this.A=8192}g=Gm.prototype;g.toString=function(){return hc(this)};g.C=function(b,a){return Am(this.root,this.Oa,a)};g.B=function(b,a,c){b=Am(this.root,this.Oa,a);return r(b)?b:c};g.D=function(b,a,c){return V(a,Fi,"#{"," ","}",c,q(this))};g.N=function(){return this.w};g.L=function(){return this.s};
	g.Ua=function(){var b=Hm.c?Hm.c(this):Hm.call(null,this);return Nb(b)};g.I=function(){var b=this.i;if(null!=b)return b;a:for(var b=0,a=q(this);;)if(a)var c=D(a),b=(b+sc(c))%4503599627370496,a=E(a);else break a;return this.i=b};g.u=function(b,a){return zd(a)&&this.s===J(a)&&rf(function(a){return function(b){return Pd(a,b)}}(this),a)};g.ca=function(){return new Gm(new Fm([]),0,0,this.Oa,this.w,gm.Cc)};g.zc=function(b,a){var c=this.Oa;return Im.g?Im.g(this,a,c):Im.call(null,this,a,c)};
	g.ja=function(b,a){var c=Hm.c?Hm.c(this):Hm.call(null,this);return r(c)?Db.b(c,a):a.R?a.R():a.call(null)};g.ka=function(b,a,c){b=Hm.c?Hm.c(this):Hm.call(null,this);return r(b)?Db.g(b,a,c):c};g.J=function(){return Hm.c?Hm.c(this):Hm.call(null,this)};g.P=function(b,a){return new Gm(this.root,this.shift,this.s,this.Oa,a,this.i)};g.O=function(b,a){var c=this.Oa;return Jm.g?Jm.g(this,a,c):Jm.call(null,this,a,c)};
	g.call=function(){var b=null,b=function(a,b,d){switch(arguments.length){case 2:return this.C(null,b);case 3:return this.B(null,b,d)}throw Error("Invalid arity: "+arguments.length);};b.b=function(a,b){return this.C(null,b)};b.g=function(a,b,d){return this.B(null,b,d)};return b}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return this.C(null,b)};g.b=function(b,a){return this.B(null,b,a)};function Km(b,a,c,d){return new Gm(a,c,d,b.Oa,b.w,gm.Cc)}
	function Lm(b,a){for(var c=b.shift,d=b.root;;)if(0<c)d=d.la[im&a>>>c],c-=hm;else return d.keys}function Jm(b,a,c){a=Bm(b.root,c,a);return null==a?b:1===a.length?Km(b,a[0],b.shift,b.s+1):Km(b,new Em(a.map(wm),a),b.shift+hm,b.s+1)}function Im(b,a,c){a=Cm(b.root,c,a,!0,null,null);if(null==a)return b;a=a[0];return a instanceof Em&&1===a.la.length?Km(b,a.la[0],b.shift-hm,b.s-1):Km(b,a,b.shift,b.s-1)}
	var Mm=function Mm(a,c,d){var e=im&c>>>d;return 0<d?(c=Mm(a.la[e],c,d-hm),-1===c?e+1<a.la.length?0|e+1<<d:-1:c|e<<d):e+1<a.keys.length?0|e+1<<0:-1};function Nm(b,a){for(var c=b,d=0,e=a;;)if(0<e)d|=c.la.length-1<<e,e-=hm,c=nm(c.la);else return d|c.keys.length-1<<0}var Om=function Om(a,c,d){var e=im&c>>>d;if(0<d){var f=d-hm;c=Om(a.la[e],c,f);return-1===c?0<=e-1?(--e,Nm(a.la[e],f)|e<<d):-1:c|e<<d}return 0<=e-1?0|e-1<<0:-1};
	function Hm(b){if(0<xm(b.root)){var a=Nm(b.root,b.shift)+1;return Pm.g?Pm.g(b,0,a):Pm.call(null,b,0,a)}return null}function Qm(b,a,c,d,e){this.set=b;this.left=a;this.right=c;this.keys=d;this.Ka=e;this.m=143130816;this.A=1536}g=Qm.prototype;g.J=function(){return r(this.keys)?this:null};g.ia=function(){return Rm.c?Rm.c(this):Rm.call(null,this)};g.ma=function(){var b=Sm.c?Sm.c(this):Sm.call(null,this);return r(b)?b:Bc};g.qa=function(){return Sm.c?Sm.c(this):Sm.call(null,this)};
	g.uc=function(){return Tm.c?Tm.c(this):Tm.call(null,this)};g.vc=function(){var b=ac(this);return r(b)?b:Bc};g.tc=function(){return Um.c?Um.c(this):Um.call(null,this)};g.ja=function(b,a){return Vm?Vm(this,a):Wm.call(null,this,a)};g.ka=function(b,a,c){return Xm?Xm(this,a,c):Wm.call(null,this,a,c)};g.Ua=function(){return Ym.c?Ym.c(this):Ym.call(null,this)};function Pm(b,a,c){return new Qm(b,a,c,Lm(b,a),im&a>>>0)}function Rm(b){return r(b.keys)?b.keys[b.Ka]:null}
	function Sm(b){var a=b.set,c=b.left,d=b.right,e=b.keys;b=b.Ka;if(r(e)){if(b+1<e.length)return c+1<d?new Qm(a,c+1,d,e,b+1):null;c=Mm(a.root,c,a.shift);return pf.b(-1,c)&&c<d?Pm(a,c,d):null}return null}function Tm(b){var a=b.right,c=b.keys,d=b.Ka;b=G.b(b.left|im,a|im)?a&im:c.length;return new Qe(c,d,b)}function Um(b){var a=b.set,c=b.right;b=Mm(a.root,b.left+(b.keys.length-b.Ka-1),a.shift);return pf.b(-1,b)&&b<c?Pm(a,b,c):null}
	function Ym(b){var a=b.set,c=b.left,d=b.right;return r(b.keys)?(b=Om(a.root,c,a.shift),d=Om(a.root,d,a.shift),Zm.g?Zm.g(a,b,d):Zm.call(null,a,b,d)):null}function Wm(){switch(arguments.length){case 2:return Vm(arguments[0],arguments[1]);case 3:return Xm(arguments[0],arguments[1],arguments[2]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function Vm(b,a){if(null==b.keys)return a.R?a.R():a.call(null);var c=Rm(b),d=Sm(b);return r(d)?Xm(d,a,c):c}
	function Xm(b,a,c){var d=b.set,e=b.right,f=b.left,h=b.keys;for(b=b.Ka;;){if(Lc(c))return a=c,Pc.c?Pc.c(a):Pc.call(null,a);if(null==h)return c;var k=h[b];c=a.b?a.b(c,k):a.call(null,c,k);if(b+1<h.length)if(f+1<e)b+=1,f+=1;else return c;else if(f=Mm(d.root,f,d.shift),-1!==f&&f<e)h=Lm(d,f),b=im&f>>>0;else return c}}function $m(b,a,c,d,e){this.set=b;this.left=a;this.right=c;this.keys=d;this.Ka=e;this.m=142606528;this.A=0}g=$m.prototype;g.J=function(){return r(this.keys)?this:null};
	g.ia=function(){return an.c?an.c(this):an.call(null,this)};g.ma=function(){var b=bn.c?bn.c(this):bn.call(null,this);return r(b)?b:Bc};g.qa=function(){return bn.c?bn.c(this):bn.call(null,this)};g.Ua=function(){return cn.c?cn.c(this):cn.call(null,this)};function Zm(b,a,c){return new $m(b,a,c,Lm(b,c),im&c>>>0)}function an(b){return r(b.keys)?b.keys[b.Ka]:null}
	function bn(b){var a=b.set,c=b.left,d=b.right,e=b.keys;b=b.Ka;if(r(e)){if(0<=b-1)return d-1>c?new $m(a,c,d-1,e,b-1):null;d=Om(a.root,d,a.shift);return pf.b(-1,d)&&d>c?Zm(a,c,d):null}return null}function cn(b){var a=b.set,c=b.left;b=b.right;if(r($g)){var d=Mm(a.root,b,a.shift);return Pm(a,-1===c?0:Mm(a.root,c,a.shift),-1===d?b+1:d)}return null}
	function dn(b,a,c){a:for(var d=b.root,e=0,f=b.shift;;){var h=xm(d);if(0===f){var k=d.keys,k=jm(b.Oa,k,h-1,a);a=h===k?-1:e|k<<0;break a}k=d.keys;k=jm(b.Oa,k,h-2,a);e|=k<<f;f-=hm;d=d.la[k]}if(0>a)return null;a:for(d=b.root,e=0,f=b.shift;;){k=xm(d);if(0===f){h=d.keys;k=km(b.Oa,h,k-1,c);c=e|k<<0;break a}h=d.keys;k=km(b.Oa,h,k-2,c);e|=k<<f;f-=hm;d=d.la[k]}return c>a?new Qm(b,a,c,Lm(b,a),im&a>>>0):null}function en(b,a){return dn(b,a,a)}
	function fn(b,a){for(var c=sm(function(a){return new Fm(a)},tm(b)),d=c,e=0;;){var f=J(d);switch(f){case 0:return new Gm(new Fm([]),0,0,a,null,gm.Cc);case 1:return new Gm(D(d),e,b.length,a,null,gm.Cc);default:d=sm(function(){return function(a){return new Em(a.map(wm),a)}}(d,e,f,c),tm(d)),e+=hm}}}function gn(b){return new Gm(new Fm([]),0,0,b,null,gm.Cc)};var hn=function hn(a,c){var d;d=xf(hn,a);Id(c)?(d=wi(Mf.b(d,c)),d=a.c?a.c(d):a.call(null,d)):yd(c)?(d=S.b(null==c?null:Wa(c),Mf.b(d,c)),d=a.c?a.c(d):a.call(null,d)):d=a.c?a.c(c):a.call(null,c);return d};var jn=Error;function kn(b,a,c,d,e){this.e=b;this.a=a;this.v=c;this.tx=d;this.added=e;this.m=2162164480;this.A=0}g=kn.prototype;g.I=function(){var b=this.i;return r(b)?b:this.i=ln.c?ln.c(this):ln.call(null,this)};g.u=function(b,a){var c=a instanceof kn;return c?mn.b?mn.b(this,a):mn.call(null,this,a):c};g.J=function(){return nn.c?nn.c(this):nn.call(null,this)};g.C=function(b,a){return on.g?on.g(this,a,null):on.call(null,this,a,null)};
	g.B=function(b,a,c){return on.g?on.g(this,a,c):on.call(null,this,a,c)};g.T=function(b,a){return pn.c?pn.c(a):pn.call(null,a)};g.D=function(b,a,c){return V(a,Fi,"#datascript/Datom ["," ","]",c,new T(null,5,5,U,[this.e,this.a,this.v,this.tx,this.added],null))};
	var qn=function qn(){switch(arguments.length){case 3:return qn.g(arguments[0],arguments[1],arguments[2]);case 4:return qn.S(arguments[0],arguments[1],arguments[2],arguments[3]);case 5:return qn.Z(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}};qn.g=function(b,a,c){return new kn(b,a,c,536870912,!0)};qn.S=function(b,a,c,d){return new kn(b,a,c,d,!0)};qn.Z=function(b,a,c,d,e){return new kn(b,a,c,d,e)};qn.H=5;
	function ln(b){return tc(tc(sc(b.e),sc(b.a)),sc(b.v))}function mn(b,a){return G.b(b.e,a.e)&&G.b(b.a,a.a)&&G.b(b.v,a.v)}function nn(b){return Ya(Ya(Ya(Ya(Ya(Bc,b.added),b.tx),b.v),b.a),b.e)}function on(b,a,c){return G.b(xj,a)?b.v:G.b("e",a)?b.e:G.b(Xj,a)?b.added:G.b("v",a)?b.v:G.b(xk,a)?b.e:G.b("a",a)?b.a:G.b(Lk,a)?b.tx:G.b("added",a)?b.added:G.b("tx",a)?b.tx:G.b(Wl,a)?b.a:c}
	function pn(b,a,c){switch(a instanceof P?a.U:null){case "e":return new kn(c,b.a,b.v,b.tx,b.added);case "a":return new kn(b.e,c,b.v,b.tx,b.added);case "v":return new kn(b.e,b.a,c,b.tx,b.added);case "tx":return new kn(b.e,b.a,b.v,c,b.added);case "added":return new kn(b.e,b.a,b.v,b.tx,c);default:throw new jn([u("invalid key for #datascript/Datom: "),u(a)].join(""));}}function rn(b,a){return r(r(b)?a:b)?Vd(b,a):0}function sn(b,a){return r(r(b)?a:b)?b-a:0}
	function tn(b,a){if(null!=b&&null!=a){var c=Ha(b),d=Ha(a);return c===d?Vd(b,a):ka(c,d)}return 0}function un(b,a){var c=sn(b.e,a.e);return 0===c&&(c=rn(b.a,a.a),0===c&&(c=tn(b.v,a.v),0===c))?(c=sn(b.tx,a.tx),0===c?0:c):c}function vn(b,a){var c=rn(b.a,a.a);return 0===c&&(c=sn(b.e,a.e),0===c&&(c=tn(b.v,a.v),0===c))?(c=sn(b.tx,a.tx),0===c?0:c):c}function wn(b,a){var c=rn(b.a,a.a);return 0===c&&(c=tn(b.v,a.v),0===c&&(c=sn(b.e,a.e),0===c))?(c=sn(b.tx,a.tx),0===c?0:c):c}
	function xn(b,a){return b instanceof P?Zb(b,a):ka(b,a)}function yn(b,a){var c=Ha(b),d=Ha(a);return c===d?Vd(b,a):ka(c,d)}function zn(b,a){var c=b.e-a.e;return 0===c&&(c=xn(b.a,a.a),0===c&&(c=yn(b.v,a.v),0===c))?(c=b.tx-a.tx,0===c?0:c):c}function An(b,a){var c=xn(b.a,a.a);return 0===c&&(c=b.e-a.e,0===c&&(c=yn(b.v,a.v),0===c))?(c=b.tx-a.tx,0===c?0:c):c}function Bn(b,a){var c=xn(b.a,a.a);return 0===c&&(c=yn(b.v,a.v),0===c&&(c=b.e-a.e,0===c))?(c=b.tx-a.tx,0===c?0:c):c}
	var Cn={},Dn=function Dn(a,c){if(a?a.Uc:a)return a.Uc(a,c);var d;d=Dn[ca(null==a?null:a)];if(!d&&(d=Dn._,!d))throw Ia("ISearch.-search",a);return d.call(null,a,c)},En=function En(a,c,d){if(a?a.Rc:a)return a.Rc(a,c,d);var e;e=En[ca(null==a?null:a)];if(!e&&(e=En._,!e))throw Ia("IIndexAccess.-datoms",a);return e.call(null,a,c,d)},Fn=function Fn(a,c,d){if(a?a.Tc:a)return a.Tc(a,c,d);var e;e=Fn[ca(null==a?null:a)];if(!e&&(e=Fn._,!e))throw Ia("IIndexAccess.-seek-datoms",a);return e.call(null,a,c,d)},Gn=
	function Gn(a,c,d,e){if(a?a.Sc:a)return a.Sc(a,c,d,e);var f;f=Gn[ca(null==a?null:a)];if(!f&&(f=Gn._,!f))throw Ia("IIndexAccess.-index-range",a);return f.call(null,a,c,d,e)},Hn={},In=function In(a){if(a?a.Qc:a)return a.Qc(a);var c;c=In[ca(null==a?null:a)];if(!c&&(c=In._,!c))throw Ia("IDB.-schema",a);return c.call(null,a)},Jn=function Jn(a,c){if(a?a.Pc:a)return a.Pc(a,c);var d;d=Jn[ca(null==a?null:a)];if(!d&&(d=Jn._,!d))throw Ia("IDB.-attrs-by",a);return d.call(null,a,c)};
	function Kn(b,a,c,d,e,f,h,k,l,n){this.Da=b;this.ya=a;this.Fa=c;this.Ba=d;this.La=e;this.Ma=f;this.Ga=h;this.h=k;this.f=l;this.i=n;this.m=2229667594;this.A=8192}g=Kn.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "schema":return this.Da;case "eavt":return this.ya;case "aevt":return this.Fa;case "avet":return this.Ba;case "max-eid":return this.La;case "max-tx":return this.Ma;case "rschema":return this.Ga;default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.core.DB{",", ","}",c,R.b(new T(null,7,5,U,[new T(null,2,5,U,[gj,this.Da],null),new T(null,2,5,U,[Bk,this.ya],null),new T(null,2,5,U,[Fj,this.Fa],null),new T(null,2,5,U,[dk,this.Ba],null),new T(null,2,5,U,[nk,this.La],null),new T(null,2,5,U,[uj,this.Ma],null),new T(null,2,5,U,[sl,this.Ga],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 7+J(this.f)};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,7,[gj,null,uj,null,Fj,null,dk,null,nk,null,Bk,null,sl,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Kn(this.Da,this.ya,this.Fa,this.Ba,this.La,this.Ma,this.Ga,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(gj,a):Q.call(null,gj,a))?new Kn(c,this.ya,this.Fa,this.Ba,this.La,this.Ma,this.Ga,this.h,this.f,null):r(Q.b?Q.b(Bk,a):Q.call(null,Bk,a))?new Kn(this.Da,c,this.Fa,this.Ba,this.La,this.Ma,this.Ga,this.h,this.f,null):r(Q.b?Q.b(Fj,a):Q.call(null,Fj,a))?new Kn(this.Da,this.ya,c,this.Ba,this.La,this.Ma,this.Ga,this.h,this.f,null):r(Q.b?Q.b(dk,a):Q.call(null,dk,a))?new Kn(this.Da,this.ya,this.Fa,c,this.La,this.Ma,this.Ga,this.h,this.f,null):r(Q.b?Q.b(nk,a):Q.call(null,
	nk,a))?new Kn(this.Da,this.ya,this.Fa,this.Ba,c,this.Ma,this.Ga,this.h,this.f,null):r(Q.b?Q.b(uj,a):Q.call(null,uj,a))?new Kn(this.Da,this.ya,this.Fa,this.Ba,this.La,c,this.Ga,this.h,this.f,null):r(Q.b?Q.b(sl,a):Q.call(null,sl,a))?new Kn(this.Da,this.ya,this.Fa,this.Ba,this.La,this.Ma,c,this.h,this.f,null):new Kn(this.Da,this.ya,this.Fa,this.Ba,this.La,this.Ma,this.Ga,this.h,M.g(this.f,a,c),null)};
	g.J=function(){return q(R.b(new T(null,7,5,U,[new T(null,2,5,U,[gj,this.Da],null),new T(null,2,5,U,[Bk,this.ya],null),new T(null,2,5,U,[Fj,this.Fa],null),new T(null,2,5,U,[dk,this.Ba],null),new T(null,2,5,U,[nk,this.La],null),new T(null,2,5,U,[uj,this.Ma],null),new T(null,2,5,U,[sl,this.Ga],null)],null),this.f))};g.P=function(b,a){return new Kn(this.Da,this.ya,this.Fa,this.Ba,this.La,this.Ma,this.Ga,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};
	function Ln(b){return new Kn(gj.c(b),Bk.c(b),Fj.c(b),dk.c(b),nk.c(b),uj.c(b),sl.c(b),null,O.j(b,gj,H([Bk,Fj,dk,nk,uj,sl],0)),null)}g.aa=!0;g.D=function(b,a,c){return Mn.g?Mn.g(this,a,c):Mn.call(null,this,a,c)};g.Fc=!0;g.L=function(){return J(this.ya)};g.Cd=!0;g.Ua=function(){return Nb(this.ya)};g.bd=!0;g.I=function(){return Nn.c?Nn.c(this):Nn.call(null,this)};g.u=function(b,a){return On.b?On.b(this,a):On.call(null,this,a)};g.ca=function(){var b=this.Da;return Pn?Pn(b):Qn.call(null,b)};g.Hc=!0;
	g.J=function(){return Kb(this.ya)};g.Rc=function(b,a,c){return en(nd(this,a),Rn.g?Rn.g(this,a,c):Rn.call(null,this,a,c))};g.Tc=function(b,a,c){b=nd(this,a);a=Rn.g?Rn.g(this,a,c):Rn.call(null,this,a,c);return dn(b,a,new kn(null,null,null,null,null))};
	g.Sc=function(b,a,c,d){b=Ya(Ya(Ya(Ya(Ya(Bc,d),c),a),new C(null,"db","db",-1661185010,null)),new C(null,"-index-range","-index-range",898114142,null));Sn.b?Sn.b(a,b):Sn.call(null,a,b);c=Tn.Z?Tn.Z(this,null,a,c,null):Tn.call(null,this,null,a,c,null);a=Tn.Z?Tn.Z(this,null,a,d,null):Tn.call(null,this,null,a,d,null);return dn(this.Ba,c,a)};g.Oc=!0;g.Qc=function(){return this.Da};g.Pc=function(b,a){return this.Ga.call(null,a)};g.ld=!0;
	g.Uc=function(b,a){var c=L(a,0),d=L(a,1),e=L(a,2),f=L(a,3),h=this.ya,k=this.Fa,l=this.Ba;return r(c)?r(d)?null!=e?r(f)?en(h,new kn(c,d,e,f,null)):en(h,new kn(c,d,e,null,null)):r(f)?Xf(function(a,b,c,d,e){return function(a){return G.b(e,a.tx)}}(a,c,d,e,f,h,k,l,this),en(h,new kn(c,d,null,null,null))):en(h,new kn(c,d,null,null,null)):null!=e?r(f)?Xf(function(a,b,c,d,e){return function(a){return G.b(d,a.v)&&G.b(e,a.tx)}}(a,c,d,e,f,h,k,l,this),en(h,new kn(c,null,null,null,null))):Xf(function(a,b,c,d){return function(a){return G.b(d,
	a.v)}}(a,c,d,e,f,h,k,l,this),en(h,new kn(c,null,null,null,null))):r(f)?Xf(function(a,b,c,d,e){return function(a){return G.b(e,a.tx)}}(a,c,d,e,f,h,k,l,this),en(h,new kn(c,null,null,null,null))):en(h,new kn(c,null,null,null,null)):r(d)?null!=e?r(f)?Xf(function(a,b,c,d,e){return function(a){return G.b(e,a.tx)}}(a,c,d,e,f,h,k,l,this),en(l,new kn(null,d,e,null,null))):en(l,new kn(null,d,e,null,null)):r(f)?Xf(function(a,b,c,d,e){return function(a){return G.b(e,a.tx)}}(a,c,d,e,f,h,k,l,this),en(l,new kn(null,
	d,null,null,null))):en(l,new kn(null,d,null,null,null)):null!=e?r(f)?Xf(function(a,b,c,d,e){return function(a){return G.b(d,a.v)&&G.b(e,a.tx)}}(a,c,d,e,f,h,k,l,this),h):Xf(function(a,b,c,d){return function(a){return G.b(d,a.v)}}(a,c,d,e,f,h,k,l,this),h):r(f)?Xf(function(a,b,c,d,e){return function(a){return G.b(e,a.tx)}}(a,c,d,e,f,h,k,l,this),h):h};function Un(b,a,c,d,e){this.Ea=b;this.Na=a;this.h=c;this.f=d;this.i=e;this.m=2229667594;this.A=8192}g=Un.prototype;
	g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "unfiltered-db":return this.Ea;case "pred":return this.Na;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.core.FilteredDB{",", ","}",c,R.b(new T(null,2,5,U,[new T(null,2,5,U,[pl,this.Ea],null),new T(null,2,5,U,[Gj,this.Na],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 2+J(this.f)};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,2,[Gj,null,pl,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Un(this.Ea,this.Na,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(pl,a):Q.call(null,pl,a))?new Un(c,this.Na,this.h,this.f,null):r(Q.b?Q.b(Gj,a):Q.call(null,Gj,a))?new Un(this.Ea,c,this.h,this.f,null):new Un(this.Ea,this.Na,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,2,5,U,[new T(null,2,5,U,[pl,this.Ea],null),new T(null,2,5,U,[Gj,this.Na],null)],null),this.f))};g.P=function(b,a){return new Un(this.Ea,this.Na,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};
	g.Gc=!0;g.C=function(){throw Error("-lookup is not supported on FilteredDB");};g.B=function(){throw Error("-lookup is not supported on FilteredDB");};g.aa=!0;g.D=function(b,a,c){return Mn.g?Mn.g(this,a,c):Mn.call(null,this,a,c)};g.Fc=!0;g.L=function(){return J(En(this,Bk,I))};g.bd=!0;g.I=function(){return Vn.c?Vn.c(this):Vn.call(null,this)};g.u=function(b,a){return On.b?On.b(this,a):On.call(null,this,a)};g.ca=function(){throw Error("-empty is not supported on FilteredDB");};
	g.Nb=function(){throw Error("-contains-key? is not supported on FilteredDB");};g.T=function(){throw Error("-assoc is not supported on FilteredDB");};g.Hc=!0;g.J=function(){return En(this,Bk,I)};g.Rc=function(b,a,c){return Xf(this.Na,En(this.Ea,a,c))};g.Tc=function(b,a,c){return Xf(this.Na,Fn(this.Ea,a,c))};g.Sc=function(b,a,c,d){return Xf(this.Na,Gn(this.Ea,a,c,d))};g.Oc=!0;g.Qc=function(){return In(this.Ea)};g.Pc=function(b,a){return Jn(this.Ea,a)};g.ld=!0;
	g.Uc=function(b,a){return Xf(this.Na,Dn(this.Ea,a))};function Wn(b,a){return G.b(new T(null,2,5,U,[b,a],null),new T(null,2,5,U,[uk,!0],null))?new T(null,1,5,U,[uk],null):G.b(a,hk)?new T(null,1,5,U,[hk],null):G.b(a,ul)?new T(null,1,5,U,[ul],null):G.b(a,qj)?new T(null,2,5,U,[zj,qj],null):G.b(a,Ek)?new T(null,2,5,U,[zj,Ek],null):null}function Xn(b){var a=Ud;return v(function(b,d){var e=L(d,0),f=L(d,1);return fg.S(b,new T(null,1,5,U,[e],null),yf(kd,a),f)},Sd,b)}
	function Yn(b){return Xn(function(){return function c(b){return new Ne(null,function(){for(var e=b;;){var f=q(e);if(f){var h=f,k=D(h),l=L(k,0),n=L(k,1);if(f=q(function(b,c,d,e,f,h){return function N(k){return new Ne(null,function(b,c,d,e,f,h){return function(){for(var l=k;;){var n=q(l);if(n){var p=n,t=D(p),x=L(t,0),z=L(t,1);if(n=q(function(b,c,d,e,f,h,k,l,n,p,t,x){return function Bg(z){return new Ne(null,function(b,c,d,e,f,h,k,l,n){return function(){for(;;){var b=q(z);if(b){if(Ed(b)){var c=y(b),d=
	J(c),e=Re(d);a:for(var f=0;;)if(f<d){var h=w.b(c,f);e.add(new T(null,2,5,U,[h,n],null));f+=1}else{c=!0;break a}return c?Te(Xe(e),Bg(A(b))):Te(Xe(e),null)}e=D(b);return bd(new T(null,2,5,U,[e,n],null),Bg(Ac(b)))}return null}}}(b,c,d,e,f,h,k,l,n,p,t,x),null,null)}}(l,b,t,x,z,p,n,c,d,e,f,h)(Wn(x,z))))return R.b(n,N(Ac(l)));l=Ac(l)}else return null}}}(b,c,d,e,f,h),null,null)}}(e,k,l,n,h,f)(n)))return R.b(f,c(Ac(e)));e=Ac(e)}else return null}},null,null)}(b)}())}
	function Zn(b,a,c,d){if(null!=c&&!Pd(d,c))throw $i([u("Bad attribute specification for "),u(X(H([new gh([b,new gh([a,c])])],0))),u(", expected one of "),u(d)].join(""),new m(null,4,[hl,kl,lj,b,Ij,a,ik,c],null));}
	function $n(b){for(var a=q(b),c=null,d=0,e=0;;)if(e<d){var f=c.M(null,e),h=L(f,0),k=L(f,1),l=uk.b(k,!1);Zn(h,uk,uk.c(k),new Td(null,new m(null,2,[!0,null,!1,null],null),null));if(r(function(){var a=l;return r(a)?pf.b(bk.c(k),hk):a}()))throw $i([u("Bad attribute specification for "),u(h),u(": {:db/isComponent true} should also have {:db/valueType :db.type/ref}")].join(""),new m(null,3,[hl,kl,lj,h,Ij,uk],null));Zn(h,zj,zj.c(k),new Td(null,new m(null,2,[qj,null,Ek,null],null),null));Zn(h,bk,bk.c(k),
	new Td(null,new m(null,1,[hk,null],null),null));Zn(h,$k,$k.c(k),new Td(null,new m(null,2,[ul,null,Jl,null],null),null));e+=1}else if(a=q(a)){if(Ed(a))d=y(a),a=A(a),c=d,d=J(d);else{var c=D(a),d=L(c,0),n=L(c,1),p=uk.b(n,!1);Zn(d,uk,uk.c(n),new Td(null,new m(null,2,[!0,null,!1,null],null),null));if(r(function(){var a=p;return r(a)?pf.b(bk.c(n),hk):a}()))throw $i([u("Bad attribute specification for "),u(d),u(": {:db/isComponent true} should also have {:db/valueType :db.type/ref}")].join(""),new m(null,
	3,[hl,kl,lj,d,Ij,uk],null));Zn(d,zj,zj.c(n),new Td(null,new m(null,2,[qj,null,Ek,null],null),null));Zn(d,bk,bk.c(n),new Td(null,new m(null,1,[hk,null],null),null));Zn(d,$k,$k.c(n),new Td(null,new m(null,2,[ul,null,Jl,null],null),null));a=E(a);c=null;d=0}e=0}else break;return b}function Qn(){switch(arguments.length){case 0:return ao();case 1:return Pn(arguments[0]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function ao(){return Pn(null)}
	function Pn(b){return Ln(new m(null,7,[gj,$n(b),Bk,gn(un),Fj,gn(vn),dk,gn(wn),nk,0,uj,536870912,sl,Yn(b)],null))}function bo(){switch(arguments.length){case 1:return co(arguments[0]);case 2:return eo(arguments[0],arguments[1]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function co(b){return eo(b,null)}
	function eo(b,a){if(xd(b))return Pn(a);var c=$n(a),d=Aa?Ba(b):Ca.call(null,b),e=fn(d.sort(zn),un),f=fn(d.sort(An),vn),h=fn(d.sort(Bn),wn),k=xk.c(D(Nb(e))),c=ie(Mf.c(function(){return function(a){return a.tx}}(c,d,e,f,h,k)),re,536870912,e);return Ln(new m(null,7,[gj,a,Bk,e,Fj,f,dk,h,nk,k,uj,c,sl,Yn(a)],null))}function Nn(b){var a=b.i;return r(a)?a:b.i=Fc(En(b,Bk,I))}function Vn(b){var a=b.i;return r(a)?a:b.i=Fc(En(b,Bk,I))}
	function On(b,a){var c=a instanceof Kn||a instanceof Un;if(c&&(c=G.b(In(b),In(a))))a:{var d=En(b,Bk,I),c=En(a,Bk,I),e=G.b(J(d),J(c));if(e)for(d=q(d),c=q(c);;){if(null==d){c=!0;break a}if(G.b(D(d),D(c)))d=E(d),c=E(c);else{c=!1;break a}}else c=e}return c}function Mn(b,a,c){Ob(a,"#datascript/DB {");Ob(a,":schema ");Fi(In(b),a,c);Ob(a,", :datoms ");V(a,function(a,b,c){return V(b,Fi,"["," ","]",c,new T(null,4,5,U,[a.e,a.a,a.v,a.tx],null))},"["," ","]",c,En(b,Bk,I));return Ob(a,"}")}
	function Tn(b,a,c,d,e){if(r(c)){var f=Ya(Ya(Ya(Ya(Ya(Ya(Bc,e),d),c),a),new C(null,"db","db",-1661185010,null)),new C(null,"resolve-datom","resolve-datom",-294110827,null));Sn.b?Sn.b(c,f):Sn.call(null,c,f)}a=fo.b?fo.b(b,a):fo.call(null,b,a);f=(f=null!=d)?go.b?go.b(b,c):go.call(null,b,c):f;return new kn(a,c,r(f)?ho.b?ho.b(b,d):ho.call(null,b,d):d,fo.b?fo.b(b,e):fo.call(null,b,e),null)}
	function Rn(b,a,c){var d=L(c,0),e=L(c,1),f=L(c,2);c=L(c,3);switch(a instanceof P?a.U:null){case "eavt":return Tn(b,d,e,f,c);case "aevt":return Tn(b,e,d,f,c);case "avet":return Tn(b,f,d,e,c);default:throw Error([u("No matching clause: "),u(a)].join(""));}}function io(b,a,c,d,e,f,h,k){this.Wa=b;this.Va=a;this.ab=c;this.$a=d;this.bb=e;this.h=f;this.f=h;this.i=k;this.m=2229667594;this.A=8192}g=io.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "db-before":return this.Wa;case "db-after":return this.Va;case "tx-data":return this.ab;case "tempids":return this.$a;case "tx-meta":return this.bb;default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.core.TxReport{",", ","}",c,R.b(new T(null,5,5,U,[new T(null,2,5,U,[Gk,this.Wa],null),new T(null,2,5,U,[Oj,this.Va],null),new T(null,2,5,U,[Hk,this.ab],null),new T(null,2,5,U,[fj,this.$a],null),new T(null,2,5,U,[tl,this.bb],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 5+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,5,[fj,null,Oj,null,Gk,null,Hk,null,tl,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new io(this.Wa,this.Va,this.ab,this.$a,this.bb,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(Gk,a):Q.call(null,Gk,a))?new io(c,this.Va,this.ab,this.$a,this.bb,this.h,this.f,null):r(Q.b?Q.b(Oj,a):Q.call(null,Oj,a))?new io(this.Wa,c,this.ab,this.$a,this.bb,this.h,this.f,null):r(Q.b?Q.b(Hk,a):Q.call(null,Hk,a))?new io(this.Wa,this.Va,c,this.$a,this.bb,this.h,this.f,null):r(Q.b?Q.b(fj,a):Q.call(null,fj,a))?new io(this.Wa,this.Va,this.ab,c,this.bb,this.h,this.f,null):r(Q.b?Q.b(tl,a):Q.call(null,tl,a))?new io(this.Wa,this.Va,this.ab,this.$a,c,this.h,this.f,
	null):new io(this.Wa,this.Va,this.ab,this.$a,this.bb,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,5,5,U,[new T(null,2,5,U,[Gk,this.Wa],null),new T(null,2,5,U,[Oj,this.Va],null),new T(null,2,5,U,[Hk,this.ab],null),new T(null,2,5,U,[fj,this.$a],null),new T(null,2,5,U,[tl,this.bb],null)],null),this.f))};g.P=function(b,a){return new io(this.Wa,this.Va,this.ab,this.$a,this.bb,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};
	function jo(b,a,c){return Pd(Jn(b,c),a)}function go(b,a){return jo(b,a,hk)}
	function ko(b,a){if("number"===typeof a)return a;if(Ad(a)){if(pf.b(J(a),2))throw $i([u("Lookup ref should contain 2 elements: "),u(X(H([a],0)))].join(""),new m(null,2,[hl,Wk,rk,a],null));if(jo(b,D(a),qj))return xk.c(D(En(b,dk,a)));throw $i([u("Lookup ref attribute should be marked as :db.unique/identity: "),u(X(H([a],0)))].join(""),new m(null,2,[hl,oj,rk,a],null));}throw $i([u("Expected number or lookup ref for entity id, got "),u(X(H([a],0)))].join(""),new m(null,2,[hl,Yk,rk,a],null));}
	function ho(b,a){var c=ko(b,a);if(r(c))return c;throw $i([u("Nothing found for entity id "),u(X(H([a],0)))].join(""),new m(null,2,[hl,cl,rk,a],null));}function fo(b,a){return r(a)?ho(b,a):null}function Sn(b,a){if(b instanceof P||"string"===typeof b)return null;throw $i([u("Bad entity attribute "),u(X(H([b],0))),u(" at "),u(X(H([a],0))),u(", expected keyword or string")].join(""),new m(null,3,[hl,vl,lj,b,el,a],null));}
	function lo(b,a){if(null==b)throw $i([u("Cannot store nil as a value at "),u(X(H([a],0)))].join(""),new m(null,3,[hl,vl,ik,b,el,a],null));return null}function mo(b){return cg(b,new T(null,2,5,U,[Gk,uj],null))+1}function no(b,a){return a>nk.c(b)&&536870912>a?M.g(b,nk,a):b}function oo(b,a,c){return fg.S(eg(b,new T(null,2,5,U,[fj,a],null),c),new T(null,1,5,U,[Oj],null),no,c)}function po(b){return G.b(b,Tk)||G.b(b,":db/current-tx")}
	function ro(b,a){var c;c=a.added;c=r(c)?jo(b,a.a,zj):c;if(r(c)&&(c=qf(En(b,dk,new T(null,2,5,U,[a.a,a.v],null))),r(c)))throw $i([u("Cannot add "),u(X(H([a],0))),u(" because of unique constraint: "),u(X(H([c],0)))].join(""),new m(null,3,[hl,aj,lj,a.a,Mj,a],null));if(r(a.added))return no(fg.Z(fg.Z(fg.Z(b,new T(null,1,5,U,[Bk],null),Jm,a,zn),new T(null,1,5,U,[Fj],null),Jm,a,An),new T(null,1,5,U,[dk],null),Jm,a,Bn),a.e);c=D(Dn(b,new T(null,3,5,U,[a.e,a.a,a.v],null)));return fg.Z(fg.Z(fg.Z(b,new T(null,
	1,5,U,[Bk],null),Im,c,zn),new T(null,1,5,U,[Fj],null),Im,c,An),new T(null,1,5,U,[dk],null),Im,c,Bn)}function so(b,a){return fg.S(fg.S(b,new T(null,1,5,U,[Oj],null),ro,a),new T(null,1,5,U,[Hk],null),kd,a)}function to(b){if(b instanceof P)return G.b("_",md(Me(b),0));if("string"===typeof b)return Nd(xi(/(?:([^\/]+)\/)?_([^\/]+)/,b));throw $i([u("Bad attribute type: "),u(X(H([b],0))),u(", expected keyword or string")].join(""),new m(null,2,[hl,vl,lj,b],null));}
	function uo(b){if(b instanceof P)return to(b)?Le.b(Ke(b),Ae(Me(b),1)):Le.b(Ke(b),[u("_"),u(Me(b))].join(""));if("string"===typeof b){var a=xi(/(?:([^\/]+)\/)?([^\/]+)/,b);L(a,0);b=L(a,1);a=L(a,2);return G.b("_",md(a,0))?r(b)?[u(b),u("/"),u(a.substring(1))].join(""):a.substring(1):r(b)?[u(b),u("/_"),u(a)].join(""):[u("_"),u(a)].join("")}throw $i([u("Bad attribute type: "),u(X(H([b],0))),u(", expected keyword or string")].join(""),new m(null,2,[hl,vl,lj,b],null));}
	function vo(b,a){var c=qf(Jn(b,qj));return r(c)?ge(function(){return function(c,e,f){var h=D(En(b,dk,new T(null,2,5,U,[e,f],null)));if(r(h)){var k=bl.c(c),l=h.e;if(null==k)return M.g(O.b(c,e),bl,l);if(G.b(k,l))return O.b(c,e);throw $i([u("Cannot resolve upsert for "),u(X(H([a],0))),u(": "),u(X(H([new gh([bl,k,e,f])],0))),u(" conflicts with existing "),u(X(H([h],0)))].join(""),new m(null,4,[hl,wk,lj,e,yl,a,Mj,h],null));}return c}}(c,c),a,ei(a,c)):a}
	function wo(b,a,c){to(a)||jo(b,a,ul)?(a=Ea.c?Ea.c(c):Ea.call(null,c),a=r(a)?a:yd(c)&&!Bd(c),b=Fa(a)?new T(null,1,5,U,[c],null):G.b(J(c),2)&&jo(b,D(c),qj)?new T(null,1,5,U,[c],null):c):b=new T(null,1,5,U,[c],null);return b}
	function xo(b,a){return function(a){return function e(f){return new Ne(null,function(a){return function(){for(var c=f;;){var l=q(c);if(l){var n=l,p=D(n),t=L(p,0),x=L(p,1);if(pf.b(t,bl)){var z=Sn(t,new gh([bl,a,t,x])),B=to(t),F=B?uo(t):t;if(B&&!go(b,F))throw $i([u("Bad attribute "),u(X(H([t],0))),u(": reverse attribute name requires {:db/valueType :db.type/ref} in schema")].join(""),new m(null,3,[hl,vl,lj,t,el,new gh([bl,a,t,x])],null));if(l=q(function(a,c,e,f,h,k,l,n,p,t,x){return function Ja(z){return new Ne(null,
	function(a,c,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(z);if(a){if(Ed(a)){var c=y(a),h=J(c),k=Re(h);a:for(var n=0;;)if(n<h){var p=w.b(c,n),p=go(b,f)&&Bd(p)?M.g(p,uo(l),x):e?new T(null,4,5,U,[ll,p,f,x],null):new T(null,4,5,U,[ll,x,f,p],null);k.add(p);n+=1}else{c=!0;break a}return c?Te(Xe(k),Ja(A(a))):Te(Xe(k),null)}k=D(a);return bd(go(b,f)&&Bd(k)?M.g(k,uo(l),x):e?new T(null,4,5,U,[ll,k,f,x],null):new T(null,4,5,U,[ll,x,f,k],null),Ja(Ac(a)))}return null}}}(a,c,e,f,h,k,l,n,p,t,x),null,null)}}(c,
	z,B,F,null,p,t,x,n,l,a)(wo(b,t,x))))return R.b(l,e(Ac(c)))}c=Ac(c)}else return null}}}(a),null,null)}}(bl.c(a))(a)}function yo(b,a){L(a,0);var c=L(a,1),d=L(a,2),e=L(a,3);Sn(d,a);lo(e,a);var f=mo(b),h=Oj.c(b),c=ho(h,c),e=go(h,d)?ho(h,e):e,k=new kn(c,d,e,f,!0);if(jo(h,d,ul))return xd(Dn(h,new T(null,3,5,U,[c,d,e],null)))?so(b,k):b;h=D(Dn(h,new T(null,2,5,U,[c,d],null)));return r(h)?G.b(h.v,e)?b:so(so(b,new kn(c,d,h.v,f,!1)),k):so(b,k)}
	function zo(b,a){var c=mo(b);return so(b,new kn(a.e,a.a,a.v,c,!1))}function Ao(b,a){return S.g(Ud,wf.b(Wf(function(a){return jo(b,a.a,uk)}),Mf.c(function(a){return new T(null,2,5,U,[Ck,a.v],null)})),a)}
	function Bo(b,a){for(;;){if(null!=a&&!Ad(a))throw $i([u("Bad transaction data "),u(X(H([a],0))),u(", expected sequential collection")].join(""),new m(null,2,[hl,vl,Hk,a],null));var c=a,d=L(c,0),e=ze(c,1),f=Oj.c(b);if(null==d)return fg.g(eg(b,new T(null,2,5,U,[fj,Tk],null),mo(b)),new T(null,2,5,U,[Oj,uj],null),Jc);if(Bd(d)){var h=bl.c(d),k=fo(f,r(function(){var a=h;return fm.c?fm.c(a):fm.call(null,a)}())?cg(b,new T(null,2,5,U,[fj,h],null)):po(h)?mo(b):h),l=vo(f,M.g(d,bl,k)),d=function(){var a=bl.c(l);
	return r(a)?a:nk.c(f)+1}(),k=M.g(l,bl,d),d=null==h?fg.S(b,new T(null,1,5,U,[Oj],null),no,d):r(function(){var a=h;return fm.c?fm.c(a):fm.call(null,a)}())?oo(b,h,d):b,e=R.b(xo(f,k),e);b=d;a=e}else if(Ad(d)){var n=d,k=L(n,0),p=L(n,1),t=L(n,2),x=L(n,3);if(G.b(k,vk)){var z=L(d,0),k=L(d,1),B=ze(d,2),d=b,e=R.b(lf(k,f,B),e);b=d;a=e}else if(G.b(k,Xk)){var F=d,z=L(F,0),B=L(F,1),K=L(F,2),N=L(F,3),W=L(F,4),xa=ho(f,B),Ua=Sn(K,d),gb=go(f,K)?ho(f,N):N,aa=go(f,K)?ho(f,W):W,uc=lo(gb,d),Pa=lo(aa,d),va=Dn(f,new T(null,
	2,5,U,[xa,K],null));if(jo(f,K,ul))if(r(sf(function(a,b,c,d,e,f,h,k,l,n,p){return function(a){return G.b(a.v,p)}}(b,a,F,z,B,K,N,W,xa,Ua,gb,aa,uc,Pa,va,n,k,p,t,x,c,d,e,f),va)))b=k=yo(b,new T(null,4,5,U,[ll,xa,K,aa],null)),a=e;else throw $i([u(":db.fn/cas failed on datom ["),u(X(H([xa],0))),u(" "),u(X(H([K],0))),u(" "),u(X(H([Mf.b(xj,va)],0))),u("], expected "),u(X(H([gb],0)))].join(""),new m(null,4,[hl,nj,Yl,va,al,gb,Yj,aa],null));else if(k=xj.c(D(va)),G.b(k,gb))b=k=yo(b,new T(null,4,5,U,[ll,xa,K,aa],
	null)),a=e;else throw $i([u(":db.fn/cas failed on datom ["),u(X(H([xa],0))),u(" "),u(X(H([K],0))),u(" "),u(X(H([k],0))),u("], expected "),u(X(H([gb],0)))].join(""),new m(null,4,[hl,nj,Yl,D(va),al,gb,Yj,aa],null));}else if(po(p))d=b,e=R.b(new T(null,1,5,U,[new T(null,4,5,U,[k,mo(b),t,x],null)],null),e),b=d,a=e;else if(go(f,t)&&po(x))d=b,e=R.b(new T(null,1,5,U,[new T(null,4,5,U,[k,p,t,mo(b)],null)],null),e),b=d,a=e;else if(r(function(){var a=p;return fm.c?fm.c(a):fm.call(null,a)}()))d=cg(b,new T(null,
	2,5,U,[fj,p],null)),r(d)?(B=b,e=R.b(new T(null,1,5,U,[new T(null,4,5,U,[k,d,t,x],null)],null),e),b=B,a=e):(e=oo(b,p,nk.c(f)+1),k=a,b=e,a=k);else if(r(function(){var a=go(f,t);return a?(a=x,fm.c?fm.c(a):fm.call(null,a)):a}()))d=cg(b,new T(null,2,5,U,[fj,x],null)),r(d)?(B=b,e=R.b(new T(null,1,5,U,[new T(null,4,5,U,[k,p,t,d],null)],null),e),b=B,a=e):(e=oo(b,x,nk.c(f)+1),k=a,b=e,a=k);else if(G.b(k,ll))b=k=yo(b,d),a=e;else if(G.b(k,Jk))B=ho(f,p),k=go(f,t)?ho(f,x):x,Sn(t,d),lo(k,d),d=D(Dn(f,new T(null,
	3,5,U,[B,t,k],null))),r(d)&&(b=k=zo(b,d)),a=e;else if(G.b(k,Vk))B=ho(f,p),Sn(t,d),va=Dn(f,new T(null,2,5,U,[B,t],null)),k=v(zo,b,va),e=R.b(Ao(f,va),e),b=k,a=e;else if(G.b(k,Ck))B=ho(f,p),va=Dn(f,new T(null,1,5,U,[B],null)),k=Vf(function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(a){return Dn(x,new T(null,3,5,U,[null,a,c],null))}}(b,a,B,va,n,k,p,t,x,c,d,e,f),H([Jn(f,hk)],0)),k=v(zo,b,R.b(va,k)),e=R.b(Ao(f,va),e),b=k,a=e;else throw $i([u("Unknown operation at "),u(X(H([d],0))),u(", expected :db/add, :db/retract, :db.fn/call, :db.fn/retractAttribute or :db.fn/retractEntity")].join(""),
	new m(null,3,[hl,vl,mk,k,Hk,d],null));}else throw $i([u("Bad entity type at "),u(X(H([d],0))),u(", expected map or vector")].join(""),new m(null,2,[hl,vl,Hk,d],null));}};function Co(b,a){var c=lf(oi,b,a);return bd(c,Yf(function(a){return function(b){return a===b}}(c),a))}var Do=function Do(){switch(arguments.length){case 0:return Do.R();case 1:return Do.c(arguments[0]);case 2:return Do.b(arguments[0],arguments[1]);default:return Do.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};Do.R=function(){return Ud};Do.c=function(b){return b};Do.b=function(b,a){return J(b)<J(a)?v(kd,a,b):v(kd,b,a)};
	Do.j=function(b,a,c){b=Co(J,kd.j(c,a,H([b],0)));return v(S,D(b),Ac(b))};Do.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return Do.j(a,b,c)};Do.H=2;var Eo=function Eo(){switch(arguments.length){case 1:return Eo.c(arguments[0]);case 2:return Eo.b(arguments[0],arguments[1]);default:return Eo.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};Eo.c=function(b){return b};
	Eo.b=function(b,a){for(;;)if(J(a)<J(b)){var c=b;b=a;a=c}else return v(function(a,b){return function(a,c){return Pd(b,c)?a:wd.b(a,c)}}(b,a),b,b)};Eo.j=function(b,a,c){b=Co(function(a){return-J(a)},kd.j(c,a,H([b],0)));return v(Eo,D(b),Ac(b))};Eo.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return Eo.j(a,b,c)};Eo.H=2;
	var Fo=function Fo(){switch(arguments.length){case 1:return Fo.c(arguments[0]);case 2:return Fo.b(arguments[0],arguments[1]);default:return Fo.j(arguments[0],arguments[1],new za(Array.prototype.slice.call(arguments,2),0))}};Fo.c=function(b){return b};Fo.b=function(b,a){return J(b)<J(a)?v(function(b,d){return Pd(a,d)?wd.b(b,d):b},b,b):v(wd,b,a)};Fo.j=function(b,a,c){return v(Fo,b,kd.b(c,a))};Fo.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return Fo.j(a,b,c)};Fo.H=2;var Go={},Ho=function Ho(a,c,d){if(a?a.fa:a)return a.fa(a,c,d);var e;e=Ho[ca(null==a?null:a)];if(!e&&(e=Ho._,!e))throw Ia("ITraversable.-collect",a);return e.call(null,a,c,d)},Io=function Io(a,c){if(a?a.ga:a)return a.ga(a,c);var d;d=Io[ca(null==a?null:a)];if(!d&&(d=Io._,!d))throw Ia("ITraversable.-collect-vars",a);return d.call(null,a,c)};function Jo(b,a){return Ad(b)&&G.b(J(b),a)}
	function Ko(b,a){return Ad(a)?v(function(a,d){var e=b.c?b.c(d):b.call(null,d);return r(e)?kd.b(a,e):new Kc(null)},I,a):null}function Lo(){return Mo(arguments[0],arguments[1],2<arguments.length?new za(Array.prototype.slice.call(arguments,2),0):null)}
	function Mo(b,a,c){var d=L(c,0),e=r(d)?d:I;return r(b.c?b.c(a):b.call(null,a))?kd.b(e,a):(a?r(r(null)?null:a.ea)||(a.Eb?0:Ga(Go,a)):Ga(Go,a))?Ho(a,b,e):r(Md.c?Md.c(a):Md.call(null,a))?v(function(){return function(a,c){return Mo(b,c,H([a],0))}}(e,c,d),e,a):e}function No(b){var a=xd(b);return a?a:kf(Qd,b)}function Oo(b,a){return dd(b,new m(null,1,[sk,a],null))}function Po(b){var a=sk.c(vd(b));return r(a)?a:b}function Qo(b,a,c){this.h=b;this.f=a;this.i=c;this.m=2229667594;this.A=8192}g=Qo.prototype;
	g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a){default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.Placeholder{",", ","}",c,R.b(I,this.f))};g.N=function(){return this.h};g.L=function(){return 0+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};
	g.W=function(b,a){return Pd(Ud,a)?O.b(dd(S.b(Sd,this),this.h),a):new Qo(this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return new Qo(this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(I,this.f))};g.P=function(b,a){return new Qo(a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return c};g.ga=function(b,a){return a};function Ro(b,a,c,d){this.symbol=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=Ro.prototype;
	g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "symbol":return this.symbol;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.Variable{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[ck,this.symbol],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[ck,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Ro(this.symbol,this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return r(Q.b?Q.b(ck,a):Q.call(null,ck,a))?new Ro(c,this.h,this.f,null):new Ro(this.symbol,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[ck,this.symbol],null)],null),this.f))};
	g.P=function(b,a){return new Ro(this.symbol,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.symbol,H([c],0))};g.ga=function(b,a){var c=this.symbol;return Y.b?Y.b(a,c):Y.call(null,a,c)};function So(b,a,c,d){this.symbol=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=So.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "symbol":return this.symbol;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.SrcVar{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[ck,this.symbol],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[ck,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new So(this.symbol,this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return r(Q.b?Q.b(ck,a):Q.call(null,ck,a))?new So(c,this.h,this.f,null):new So(this.symbol,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[ck,this.symbol],null)],null),this.f))};
	g.P=function(b,a){return new So(this.symbol,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.symbol,H([c],0))};g.ga=function(b,a){var c=this.symbol;return Y.b?Y.b(a,c):Y.call(null,a,c)};function To(b,a,c){this.h=b;this.f=a;this.i=c;this.m=2229667594;this.A=8192}g=To.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a){default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.DefaultSrc{",", ","}",c,R.b(I,this.f))};g.N=function(){return this.h};g.L=function(){return 0+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(Ud,a)?O.b(dd(S.b(Sd,this),this.h),a):new To(this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return new To(this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(I,this.f))};g.P=function(b,a){return new To(a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return c};g.ga=function(b,a){return a};function Uo(b,a,c){this.h=b;this.f=a;this.i=c;this.m=2229667594;this.A=8192}g=Uo.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a){default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.RulesVar{",", ","}",c,R.b(I,this.f))};g.N=function(){return this.h};g.L=function(){return 0+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(Ud,a)?O.b(dd(S.b(Sd,this),this.h),a):new Uo(this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return new Uo(this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(I,this.f))};g.P=function(b,a){return new Uo(a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return c};g.ga=function(b,a){return a};function Vo(b,a,c,d){this.value=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=Vo.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "value":return this.value;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.Constant{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[ik,this.value],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[ik,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Vo(this.value,this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return r(Q.b?Q.b(ik,a):Q.call(null,ik,a))?new Vo(c,this.h,this.f,null):new Vo(this.value,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[ik,this.value],null)],null),this.f))};
	g.P=function(b,a){return new Vo(this.value,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.value,H([c],0))};g.ga=function(b,a){var c=this.value;return Y.b?Y.b(a,c):Y.call(null,a,c)};function Wo(b,a,c,d){this.symbol=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=Wo.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "symbol":return this.symbol;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.PlainSymbol{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[ck,this.symbol],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[ck,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Wo(this.symbol,this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return r(Q.b?Q.b(ck,a):Q.call(null,ck,a))?new Wo(c,this.h,this.f,null):new Wo(this.symbol,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[ck,this.symbol],null)],null),this.f))};
	g.P=function(b,a){return new Wo(this.symbol,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.symbol,H([c],0))};g.ga=function(b,a){var c=this.symbol;return Y.b?Y.b(a,c):Y.call(null,a,c)};function Xo(b){return G.b(new C(null,"_","_",-1201019570,null),b)?new Qo(null,null,null):null}function Yo(b){return b instanceof C&&G.b(D(Me(b)),"?")?new Ro(b,null,null,null):null}
	function Zo(b){return b instanceof C&&G.b(D(Me(b)),"$")?new So(b,null,null,null):null}function $o(b){return b instanceof C?null:new Vo(b,null,null,null)}function ap(b){return b instanceof C&&Fa(Yo(b))&&Fa(Zo(b))&&Fa(Xo(b))?new Wo(b,null,null,null):null}function bp(b){var a=Yo(b);if(r(a))return a;a=$o(b);return r(a)?a:Zo(b)}function cp(b,a,c,d,e){this.required=b;this.vb=a;this.h=c;this.f=d;this.i=e;this.m=2229667594;this.A=8192}g=cp.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "required":return this.required;case "free":return this.vb;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.RuleVars{",", ","}",c,R.b(new T(null,2,5,U,[new T(null,2,5,U,[Kl,this.required],null),new T(null,2,5,U,[Wj,this.vb],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 2+J(this.f)};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,2,[Wj,null,Kl,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new cp(this.required,this.vb,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(Kl,a):Q.call(null,Kl,a))?new cp(c,this.vb,this.h,this.f,null):r(Q.b?Q.b(Wj,a):Q.call(null,Wj,a))?new cp(this.required,c,this.h,this.f,null):new cp(this.required,this.vb,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,2,5,U,[new T(null,2,5,U,[Kl,this.required],null),new T(null,2,5,U,[Wj,this.vb],null)],null),this.f))};g.P=function(b,a){return new cp(this.required,this.vb,a,this.f,this.i)};
	g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.vb,H([Mo(a,this.required,H([c],0))],0))};g.ga=function(b,a){var c;c=this.required;c=Y.b?Y.b(a,c):Y.call(null,a,c);var d=this.vb;return Y.b?Y.b(c,d):Y.call(null,c,d)};function dp(b,a,c){this.h=b;this.f=a;this.i=c;this.m=2229667594;this.A=8192}g=dp.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a){default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.BindIgnore{",", ","}",c,R.b(I,this.f))};g.N=function(){return this.h};g.L=function(){return 0+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(Ud,a)?O.b(dd(S.b(Sd,this),this.h),a):new dp(this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return new dp(this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(I,this.f))};g.P=function(b,a){return new dp(a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return c};g.ga=function(b,a){return a};function ep(b,a,c,d){this.oa=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=ep.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "variable":return this.oa;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.BindScalar{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[Cj,this.oa],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[Cj,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new ep(this.oa,this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return r(Q.b?Q.b(Cj,a):Q.call(null,Cj,a))?new ep(c,this.h,this.f,null):new ep(this.oa,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[Cj,this.oa],null)],null),this.f))};
	g.P=function(b,a){return new ep(this.oa,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.oa,H([c],0))};g.ga=function(b,a){var c=this.oa;return Y.b?Y.b(a,c):Y.call(null,a,c)};function fp(b,a,c,d){this.Ab=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=fp.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "bindings":return this.Ab;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.BindTuple{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[ak,this.Ab],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[ak,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new fp(this.Ab,this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return r(Q.b?Q.b(ak,a):Q.call(null,ak,a))?new fp(c,this.h,this.f,null):new fp(this.Ab,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[ak,this.Ab],null)],null),this.f))};
	g.P=function(b,a){return new fp(this.Ab,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.Ab,H([c],0))};g.ga=function(b,a){var c=this.Ab;return Y.b?Y.b(a,c):Y.call(null,a,c)};function gp(b,a,c,d){this.pa=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=gp.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "binding":return this.pa;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.BindColl{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[Kk,this.pa],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[Kk,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new gp(this.pa,this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return r(Q.b?Q.b(Kk,a):Q.call(null,Kk,a))?new gp(c,this.h,this.f,null):new gp(this.pa,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[Kk,this.pa],null)],null),this.f))};
	g.P=function(b,a){return new gp(this.pa,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.pa,H([c],0))};g.ga=function(b,a){var c=this.pa;return Y.b?Y.b(a,c):Y.call(null,a,c)};function hp(b){return G.b(new C(null,"_","_",-1201019570,null),b)?Oo(new dp(null,null,null),b):null}
	function ip(b){if(r(function(){var a=Jo(b,2);return r(a)?G.b(hd(b),new C(null,"...","...",-1926939749,null)):a}())){var a=function(){var a=D(b);return jp.c?jp.c(a):jp.call(null,a)}();if(r(a))return Oo(new gp(a,null,null,null),b);throw $i(""+u("Cannot parse collection binding"),new m(null,2,[hl,jl,nl,b],null));}return null}function kp(b){var a=hp(b);return r(a)?a:jp.c?jp.c(b):jp.call(null,b)}
	function lp(b){var a=Ko(kp,b);if(r(a)){if(xd(a))throw $i(""+u("Tuple binding cannot be empty"),new m(null,2,[hl,jl,nl,b],null));return Oo(new fp(a,null,null,null),b)}return null}
	function jp(b){var a=ip(b);if(r(a))return a;a=Jo(b,1);a=r(a)?Ad(D(b)):a;a=r(a)?Oo(new gp(lp(D(b)),null,null,null),b):null;if(r(a))return a;a=lp(b);if(r(a))return a;a=hp(b);if(r(a))return a;a=Yo(b);a=r(a)?Oo(new ep(a,null,null,null),b):null;if(r(a))return a;throw $i(""+u("Cannot parse binding, expected (bind-scalar | bind-tuple | bind-coll | bind-rel)"),new m(null,2,[hl,jl,nl,b],null));}
	var mp=function mp(a){if(a?a.Ac:a)return a.Ac(a);var c;c=mp[ca(null==a?null:a)];if(!c&&(c=mp._,!c))throw Ia("IFindVars.-find-vars",a);return c.call(null,a)};Ro.prototype.Ac=function(){return new T(null,1,5,U,[this.symbol],null)};function np(b,a,c,d,e){this.$=b;this.X=a;this.h=c;this.f=d;this.i=e;this.m=2229667594;this.A=8192}g=np.prototype;g.Ac=function(){return mp(jd(this.X))};g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "fn":return this.$;case "args":return this.X;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.Aggregate{",", ","}",c,R.b(new T(null,2,5,U,[new T(null,2,5,U,[yj,this.$],null),new T(null,2,5,U,[cj,this.X],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 2+J(this.f)};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,2,[cj,null,yj,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new np(this.$,this.X,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(yj,a):Q.call(null,yj,a))?new np(c,this.X,this.h,this.f,null):r(Q.b?Q.b(cj,a):Q.call(null,cj,a))?new np(this.$,c,this.h,this.f,null):new np(this.$,this.X,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,2,5,U,[new T(null,2,5,U,[yj,this.$],null),new T(null,2,5,U,[cj,this.X],null)],null),this.f))};g.P=function(b,a){return new np(this.$,this.X,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;
	g.fa=function(b,a,c){return Mo(a,this.X,H([Mo(a,this.$,H([c],0))],0))};g.ga=function(b,a){var c;c=this.$;c=Y.b?Y.b(a,c):Y.call(null,a,c);var d=this.X;return Y.b?Y.b(c,d):Y.call(null,c,d)};function op(b,a,c,d,e,f){this.source=b;this.oa=a;this.pattern=c;this.h=d;this.f=e;this.i=f;this.m=2229667594;this.A=8192}g=op.prototype;g.Ac=function(){return mp(this.oa)};g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "source":return this.source;case "variable":return this.oa;case "pattern":return this.pattern;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.Pull{",", ","}",c,R.b(new T(null,3,5,U,[new T(null,2,5,U,[sk,this.source],null),new T(null,2,5,U,[Cj,this.oa],null),new T(null,2,5,U,[am,this.pattern],null)],null),this.f))};g.N=function(){return this.h};
	g.L=function(){return 3+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,3,[Cj,null,sk,null,am,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new op(this.source,this.oa,this.pattern,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(sk,a):Q.call(null,sk,a))?new op(c,this.oa,this.pattern,this.h,this.f,null):r(Q.b?Q.b(Cj,a):Q.call(null,Cj,a))?new op(this.source,c,this.pattern,this.h,this.f,null):r(Q.b?Q.b(am,a):Q.call(null,am,a))?new op(this.source,this.oa,c,this.h,this.f,null):new op(this.source,this.oa,this.pattern,this.h,M.g(this.f,a,c),null)};
	g.J=function(){return q(R.b(new T(null,3,5,U,[new T(null,2,5,U,[sk,this.source],null),new T(null,2,5,U,[Cj,this.oa],null),new T(null,2,5,U,[am,this.pattern],null)],null),this.f))};g.P=function(b,a){return new op(this.source,this.oa,this.pattern,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.pattern,H([Mo(a,this.oa,H([Mo(a,this.source,H([c],0))],0))],0))};
	g.ga=function(b,a){var c;c=this.source;c=Y.b?Y.b(a,c):Y.call(null,a,c);var d=this.oa;c=Y.b?Y.b(c,d):Y.call(null,c,d);d=this.pattern;return Y.b?Y.b(c,d):Y.call(null,c,d)};var pp=function pp(a){if(a?a.kc:a)return a.kc(a);var c;c=pp[ca(null==a?null:a)];if(!c&&(c=pp._,!c))throw Ia("IFindElements.find-elements",a);return c.call(null,a)};function qp(b,a,c,d){this.elements=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=qp.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "elements":return this.elements;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.FindRel{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[zk,this.elements],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[zk,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new qp(this.elements,this.h,qf(O.b(this.f,a)),null)};g.kc=function(){return this.elements};g.T=function(b,a,c){return r(Q.b?Q.b(zk,a):Q.call(null,zk,a))?new qp(c,this.h,this.f,null):new qp(this.elements,this.h,M.g(this.f,a,c),null)};
	g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[zk,this.elements],null)],null),this.f))};g.P=function(b,a){return new qp(this.elements,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.elements,H([c],0))};g.ga=function(b,a){var c=this.elements;return Y.b?Y.b(a,c):Y.call(null,a,c)};function rp(b,a,c,d){this.element=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=rp.prototype;
	g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "element":return this.element;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.FindColl{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[Jj,this.element],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[Jj,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new rp(this.element,this.h,qf(O.b(this.f,a)),null)};g.kc=function(){return new T(null,1,5,U,[this.element],null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(Jj,a):Q.call(null,Jj,a))?new rp(c,this.h,this.f,null):new rp(this.element,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[Jj,this.element],null)],null),this.f))};g.P=function(b,a){return new rp(this.element,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.element,H([c],0))};
	g.ga=function(b,a){var c=this.element;return Y.b?Y.b(a,c):Y.call(null,a,c)};function sp(b,a,c,d){this.element=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=sp.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "element":return this.element;default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.FindScalar{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[Jj,this.element],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};
	g.W=function(b,a){return Pd(new Td(null,new m(null,1,[Jj,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new sp(this.element,this.h,qf(O.b(this.f,a)),null)};g.kc=function(){return new T(null,1,5,U,[this.element],null)};g.T=function(b,a,c){return r(Q.b?Q.b(Jj,a):Q.call(null,Jj,a))?new sp(c,this.h,this.f,null):new sp(this.element,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[Jj,this.element],null)],null),this.f))};
	g.P=function(b,a){return new sp(this.element,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.element,H([c],0))};g.ga=function(b,a){var c=this.element;return Y.b?Y.b(a,c):Y.call(null,a,c)};function tp(b,a,c,d){this.elements=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=tp.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "elements":return this.elements;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.FindTuple{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[zk,this.elements],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[zk,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new tp(this.elements,this.h,qf(O.b(this.f,a)),null)};g.kc=function(){return this.elements};g.T=function(b,a,c){return r(Q.b?Q.b(zk,a):Q.call(null,zk,a))?new tp(c,this.h,this.f,null):new tp(this.elements,this.h,M.g(this.f,a,c),null)};
	g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[zk,this.elements],null)],null),this.f))};g.P=function(b,a){return new tp(this.elements,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.elements,H([c],0))};g.ga=function(b,a){var c=this.elements;return Y.b?Y.b(a,c):Y.call(null,a,c)};function up(b){return b instanceof np}function vp(b){return b instanceof op}
	function wp(b){var a=Yo(b);if(r(a))return a;a:{if(Ad(b)&&G.b(D(b),new C(null,"pull","pull",779986722,null))){if(3<=J(b)&&4>=J(b)){var c=(a=G.b(J(b),4))?md(b,1):new C(null,"$","$",-1580747756,null),a=a?E(E(b)):E(b),d=L(a,0),a=L(a,1),c=Zo(c),d=Yo(d),e=Yo(a),a=r(e)?e:$o(a);if(r(r(c)?r(d)?a:d:c)){a=new op(c,d,a,null,null,null);break a}}throw $i(""+u("Cannot parse pull expression, expect ['pull' src-var? variable (constant | variable)]"),new m(null,2,[hl,kj,Sj,b],null));}a=null}if(r(a))return a;a:{if(Ad(b)&&
	G.b(D(b),new C(null,"aggregate","aggregate",-1142967327,null))){if(3<=J(b)&&(L(b,0),c=L(b,1),a=ze(b,2),c=Yo(c),a=Ko(bp,a),r(r(c)?a:c))){a=new np(c,a,null,null,null);break a}throw $i(""+u("Cannot parse custom aggregate call, expect ['aggregate' variable fn-arg+]"),new m(null,2,[hl,kj,Sj,b],null));}a=null}r(a)?b=a:Ad(b)&&2<=J(b)?(a=L(b,0),b=ze(b,1),a=ap(a),b=Ko(bp,b),b=r(r(a)?b:a)?new np(a,b,null,null,null):null):b=null;return b}
	function xp(b){var a;a=Ko(wp,b);a=null==a?null:new qp(a,null,null,null);if(r(a))return a;a:{if(Ad(b)&&G.b(J(b),1)&&(a=D(b),Ad(a)&&G.b(J(a),2)&&G.b(hd(a),new C(null,"...","...",-1926939749,null)))){a=wp(D(a));a=null==a?null:new rp(a,null,null,null);break a}a=null}if(r(a))return a;Ad(b)&&G.b(J(b),2)&&G.b(hd(b),new C(null,".",".",1975675962,null))?(a=wp(D(b)),a=null==a?null:new sp(a,null,null,null)):a=null;if(r(a))return a;Ad(b)&&G.b(J(b),1)?(a=D(b),a=Ko(wp,a),a=null==a?null:new tp(a,null,null,null)):
	a=null;if(r(a))return a;throw $i(""+u("Cannot parse :find, expected: (find-rel | find-coll | find-tuple | find-scalar)"),new m(null,2,[hl,kj,Sj,b],null));}function yp(b){var a;a=Zo(b);a=r(a)?a:G.b(new C(null,"%","%",-950237169,null),b)?new Uo(null,null,null):null;return r(a)?Oo(new ep(a,null,null,null),b):jp(b)}function zp(b,a,c,d,e){this.source=b;this.pattern=a;this.h=c;this.f=d;this.i=e;this.m=2229667594;this.A=8192}g=zp.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "source":return this.source;case "pattern":return this.pattern;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.Pattern{",", ","}",c,R.b(new T(null,2,5,U,[new T(null,2,5,U,[sk,this.source],null),new T(null,2,5,U,[am,this.pattern],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 2+J(this.f)};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,2,[sk,null,am,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new zp(this.source,this.pattern,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(sk,a):Q.call(null,sk,a))?new zp(c,this.pattern,this.h,this.f,null):r(Q.b?Q.b(am,a):Q.call(null,am,a))?new zp(this.source,c,this.h,this.f,null):new zp(this.source,this.pattern,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,2,5,U,[new T(null,2,5,U,[sk,this.source],null),new T(null,2,5,U,[am,this.pattern],null)],null),this.f))};g.P=function(b,a){return new zp(this.source,this.pattern,a,this.f,this.i)};
	g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.pattern,H([Mo(a,this.source,H([c],0))],0))};g.ga=function(b,a){var c;c=this.source;c=Y.b?Y.b(a,c):Y.call(null,a,c);var d=this.pattern;return Y.b?Y.b(c,d):Y.call(null,c,d)};function Ap(b,a,c,d,e){this.$=b;this.X=a;this.h=c;this.f=d;this.i=e;this.m=2229667594;this.A=8192}g=Ap.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "fn":return this.$;case "args":return this.X;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.Predicate{",", ","}",c,R.b(new T(null,2,5,U,[new T(null,2,5,U,[yj,this.$],null),new T(null,2,5,U,[cj,this.X],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 2+J(this.f)};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,2,[cj,null,yj,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Ap(this.$,this.X,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(yj,a):Q.call(null,yj,a))?new Ap(c,this.X,this.h,this.f,null):r(Q.b?Q.b(cj,a):Q.call(null,cj,a))?new Ap(this.$,c,this.h,this.f,null):new Ap(this.$,this.X,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,2,5,U,[new T(null,2,5,U,[yj,this.$],null),new T(null,2,5,U,[cj,this.X],null)],null),this.f))};g.P=function(b,a){return new Ap(this.$,this.X,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;
	g.fa=function(b,a,c){return Mo(a,this.X,H([Mo(a,this.$,H([c],0))],0))};g.ga=function(b,a){var c;c=this.$;c=Y.b?Y.b(a,c):Y.call(null,a,c);var d=this.X;return Y.b?Y.b(c,d):Y.call(null,c,d)};function Bp(b,a,c,d,e,f){this.$=b;this.X=a;this.pa=c;this.h=d;this.f=e;this.i=f;this.m=2229667594;this.A=8192}g=Bp.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "fn":return this.$;case "args":return this.X;case "binding":return this.pa;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.Function{",", ","}",c,R.b(new T(null,3,5,U,[new T(null,2,5,U,[yj,this.$],null),new T(null,2,5,U,[cj,this.X],null),new T(null,2,5,U,[Kk,this.pa],null)],null),this.f))};g.N=function(){return this.h};
	g.L=function(){return 3+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,3,[cj,null,yj,null,Kk,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Bp(this.$,this.X,this.pa,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(yj,a):Q.call(null,yj,a))?new Bp(c,this.X,this.pa,this.h,this.f,null):r(Q.b?Q.b(cj,a):Q.call(null,cj,a))?new Bp(this.$,c,this.pa,this.h,this.f,null):r(Q.b?Q.b(Kk,a):Q.call(null,Kk,a))?new Bp(this.$,this.X,c,this.h,this.f,null):new Bp(this.$,this.X,this.pa,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,3,5,U,[new T(null,2,5,U,[yj,this.$],null),new T(null,2,5,U,[cj,this.X],null),new T(null,2,5,U,[Kk,this.pa],null)],null),this.f))};
	g.P=function(b,a){return new Bp(this.$,this.X,this.pa,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.pa,H([Mo(a,this.X,H([Mo(a,this.$,H([c],0))],0))],0))};g.ga=function(b,a){var c;c=this.$;c=Y.b?Y.b(a,c):Y.call(null,a,c);var d=this.X;c=Y.b?Y.b(c,d):Y.call(null,c,d);d=this.pa;return Y.b?Y.b(c,d):Y.call(null,c,d)};
	function Cp(b,a,c,d,e,f){this.source=b;this.name=a;this.X=c;this.h=d;this.f=e;this.i=f;this.m=2229667594;this.A=8192}g=Cp.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "source":return this.source;case "name":return this.name;case "args":return this.X;default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.RuleExpr{",", ","}",c,R.b(new T(null,3,5,U,[new T(null,2,5,U,[sk,this.source],null),new T(null,2,5,U,[fk,this.name],null),new T(null,2,5,U,[cj,this.X],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 3+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,3,[cj,null,fk,null,sk,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Cp(this.source,this.name,this.X,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(sk,a):Q.call(null,sk,a))?new Cp(c,this.name,this.X,this.h,this.f,null):r(Q.b?Q.b(fk,a):Q.call(null,fk,a))?new Cp(this.source,c,this.X,this.h,this.f,null):r(Q.b?Q.b(cj,a):Q.call(null,cj,a))?new Cp(this.source,this.name,c,this.h,this.f,null):new Cp(this.source,this.name,this.X,this.h,M.g(this.f,a,c),null)};
	g.J=function(){return q(R.b(new T(null,3,5,U,[new T(null,2,5,U,[sk,this.source],null),new T(null,2,5,U,[fk,this.name],null),new T(null,2,5,U,[cj,this.X],null)],null),this.f))};g.P=function(b,a){return new Cp(this.source,this.name,this.X,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.X,H([Mo(a,this.name,H([Mo(a,this.source,H([c],0))],0))],0))};
	g.ga=function(b,a){var c;c=this.source;c=Y.b?Y.b(a,c):Y.call(null,a,c);var d=this.name;c=Y.b?Y.b(c,d):Y.call(null,c,d);d=this.X;return Y.b?Y.b(c,d):Y.call(null,c,d)};function Dp(b,a,c,d,e,f){this.source=b;this.cb=a;this.ba=c;this.h=d;this.f=e;this.i=f;this.m=2229667594;this.A=8192}g=Dp.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "source":return this.source;case "vars":return this.cb;case "clauses":return this.ba;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.Not{",", ","}",c,R.b(new T(null,3,5,U,[new T(null,2,5,U,[sk,this.source],null),new T(null,2,5,U,[$l,this.cb],null),new T(null,2,5,U,[ml,this.ba],null)],null),this.f))};g.N=function(){return this.h};
	g.L=function(){return 3+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,3,[sk,null,ml,null,$l,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Dp(this.source,this.cb,this.ba,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(sk,a):Q.call(null,sk,a))?new Dp(c,this.cb,this.ba,this.h,this.f,null):r(Q.b?Q.b($l,a):Q.call(null,$l,a))?new Dp(this.source,c,this.ba,this.h,this.f,null):r(Q.b?Q.b(ml,a):Q.call(null,ml,a))?new Dp(this.source,this.cb,c,this.h,this.f,null):new Dp(this.source,this.cb,this.ba,this.h,M.g(this.f,a,c),null)};
	g.J=function(){return q(R.b(new T(null,3,5,U,[new T(null,2,5,U,[sk,this.source],null),new T(null,2,5,U,[$l,this.cb],null),new T(null,2,5,U,[ml,this.ba],null)],null),this.f))};g.P=function(b,a){return new Dp(this.source,this.cb,this.ba,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.ba,H([Mo(a,this.cb,H([Mo(a,this.source,H([c],0))],0))],0))};
	g.ga=function(b,a){var c;c=this.source;c=Y.b?Y.b(a,c):Y.call(null,a,c);var d=this.cb;c=Y.b?Y.b(c,d):Y.call(null,c,d);d=this.ba;return Y.b?Y.b(c,d):Y.call(null,c,d)};function Ep(b,a,c,d,e,f){this.source=b;this.Ya=a;this.ba=c;this.h=d;this.f=e;this.i=f;this.m=2229667594;this.A=8192}g=Ep.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "source":return this.source;case "rule-vars":return this.Ya;case "clauses":return this.ba;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.Or{",", ","}",c,R.b(new T(null,3,5,U,[new T(null,2,5,U,[sk,this.source],null),new T(null,2,5,U,[Vj,this.Ya],null),new T(null,2,5,U,[ml,this.ba],null)],null),this.f))};g.N=function(){return this.h};
	g.L=function(){return 3+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,3,[Vj,null,sk,null,ml,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Ep(this.source,this.Ya,this.ba,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(sk,a):Q.call(null,sk,a))?new Ep(c,this.Ya,this.ba,this.h,this.f,null):r(Q.b?Q.b(Vj,a):Q.call(null,Vj,a))?new Ep(this.source,c,this.ba,this.h,this.f,null):r(Q.b?Q.b(ml,a):Q.call(null,ml,a))?new Ep(this.source,this.Ya,c,this.h,this.f,null):new Ep(this.source,this.Ya,this.ba,this.h,M.g(this.f,a,c),null)};
	g.J=function(){return q(R.b(new T(null,3,5,U,[new T(null,2,5,U,[sk,this.source],null),new T(null,2,5,U,[Vj,this.Ya],null),new T(null,2,5,U,[ml,this.ba],null)],null),this.f))};g.P=function(b,a){return new Ep(this.source,this.Ya,this.ba,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.ba,H([Mo(a,this.Ya,H([Mo(a,this.source,H([c],0))],0))],0))};
	g.ga=function(b,a){var c;c=this.source;c=Y.b?Y.b(a,c):Y.call(null,a,c);var d=this.Ya;c=Y.b?Y.b(c,d):Y.call(null,c,d);d=this.ba;return Y.b?Y.b(c,d):Y.call(null,c,d)};function Fp(b,a,c,d){this.ba=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=Fp.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "clauses":return this.ba;default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.And{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[ml,this.ba],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};
	g.W=function(b,a){return Pd(new Td(null,new m(null,1,[ml,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Fp(this.ba,this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return r(Q.b?Q.b(ml,a):Q.call(null,ml,a))?new Fp(c,this.h,this.f,null):new Fp(this.ba,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[ml,this.ba],null)],null),this.f))};g.P=function(b,a){return new Fp(this.ba,a,this.f,this.i)};
	g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.ba,H([c],0))};g.ga=function(b,a){var c=this.ba;return Y.b?Y.b(a,c):Y.call(null,a,c)};function Gp(b){var a=Xo(b);if(r(a))return a;a=Yo(b);return r(a)?a:$o(b)}function Hp(b){if(Ad(b)){var a=Zo(D(b));return r(a)?new T(null,2,5,U,[a,E(b)],null):new T(null,2,5,U,[new To(null,null,null),b],null)}return null}
	function Ip(b){if(Ad(b)){var a=L(b,0);b=ze(b,1);b=null==b?I:b;var c=ap(a),a=r(c)?c:Yo(a);b=Ko(bp,b);return r(r(a)?b:a)?new T(null,2,5,U,[a,b],null):null}return null}var Y=function Y(a,c){return c instanceof Ro?kd.b(a,c):c instanceof Dp?S.b(a,$l.c(c)):c instanceof Ep?Y(a,Vj.c(c)):(c?r(r(null)?null:c.ea)||(c.Eb?0:Ga(Go,c)):Ga(Go,c))?Io(c,a):Ad(c)?v(Y,a,c):a};function Jp(b){return Y(I,b)}
	function Kp(b,a,c){a=Fo.b(ji(b),ji(Jp(a)));if(!xd(a))throw $i([u("Join variable not declared inside clauses: "),u(X(H([Zf(ck,a)],0)))].join(""),new m(null,2,[hl,Lj,nl,c],null));if(xd(b))throw $i(""+u("Join variables should not be empty"),new m(null,2,[hl,Lj,nl,c],null));}function Lp(b,a){Kp($l.c(b),ml.c(b),a);return b}
	function Mp(b,a){for(var c=Id(b)?kf(Cf,b):b,d=nd(c,Vj),e=Id(d)?kf(Cf,d):d,d=nd(e,Kl),e=nd(e,Wj),f=nd(c,ml),c=R.b(d,e),d=q(f),e=null,h=f=0;;)if(h<f){var k=e.M(null,h);Kp(c,new T(null,1,5,U,[k],null),a);h+=1}else if(d=q(d))e=d,Ed(e)?(d=y(e),h=A(e),e=d,f=J(d),d=h):(d=D(e),Kp(c,new T(null,1,5,U,[d],null),a),d=E(e),e=null,f=0),h=0;else break;return b}
	function Kf(b){if(Ad(b)&&G.b(new C(null,"and","and",668631710,null),D(b))){var a=Ko(Lf,E(b));if(r(qf(a)))return new Fp(a,null,null,null);throw $i(""+u("Cannot parse 'and' clause, expected [ 'and' clause+ ]"),new m(null,2,[hl,Lj,nl,b],null));}return null}
	function Lf(b){var a;a:{var c=Hp(b);if(r(c)){a=L(c,0);var d=L(c,1),c=L(d,0),d=ze(d,1);if(G.b(new C(null,"not","not",1044554643,null),c)){c=Ko(Lf,d);if(r(c)){a=Lp(new Dp(a,ce(li.c(Jp(c))),c,null,null,null),b);break a}throw $i(""+u("Cannot parse 'not' clause, expected [ src-var? 'not' clause+ ]"),new m(null,2,[hl,Lj,nl,b],null));}}a=null}if(r(a))return a;a:{c=Hp(b);if(r(c)){a=L(c,0);var e=L(c,1),c=L(e,0),d=L(e,1),e=ze(e,2);if(G.b(new C(null,"not-join","not-join",-645515756,null),c)){c=Ko(Yo,d);d=Ko(Lf,
	e);if(r(r(c)?d:c)){a=Lp(new Dp(a,c,d,null,null,null),b);break a}throw $i(""+u("Cannot parse 'not-join' clause, expected [ src-var? 'not-join' [variable+] clause+ ]"),new m(null,2,[hl,Lj,nl,b],null));}}a=null}if(r(a))return a;a:{c=Hp(b);if(r(c)&&(a=L(c,0),d=L(c,1),c=L(d,0),d=ze(d,1),G.b(new C(null,"or","or",1876275696,null),c))){c=Ko(Jf(),d);if(r(c)){a=Mp(new Ep(a,new cp(null,ce(li.c(Jp(c))),null,null,null),c,null,null,null),b);break a}throw $i(""+u("Cannot parse 'or' clause, expected [ src-var? 'or' clause+ ]"),
	new m(null,2,[hl,Lj,nl,b],null));}a=null}if(r(a))return a;a:{c=Hp(b);if(r(c)&&(a=L(c,0),d=L(c,1),e=L(d,0),c=L(d,1),d=ze(d,2),G.b(new C(null,"or-join","or-join",591375469,null),e))){if(Ad(c)){var f=Ad(D(c))?new T(null,2,5,U,[D(c),E(c)],null):new T(null,2,5,U,[null,c],null),e=L(f,0),f=L(f,1),e=Ko(Yo,e),f=Ko(Yo,f);if(xd(e)&&xd(f))throw $i(""+u("Cannot parse rule-vars, expected [ variable+ | ([ variable+ ] variable*) ]"),new m(null,2,[hl,Rj,nl,c],null));if(!r(No(R.b(e,f))))throw $i(""+u("Rule variables should be distinct"),
	new m(null,2,[hl,Rj,nl,c],null));c=new cp(e,f,null,null,null)}else throw $i(""+u("Cannot parse rule-vars, expected [ variable+ | ([ variable+ ] variable*) ]"),new m(null,2,[hl,Rj,nl,c],null));d=Ko(Lf,d);if(r(r(c)?d:c)){a=Mp(new Ep(a,c,d,null,null,null),b);break a}throw $i(""+u("Cannot parse 'or-join' clause, expected [ src-var? 'or-join' [variable+] clause+ ]"),new m(null,2,[hl,Lj,nl,b],null));}a=null}if(r(a))return a;a:{if(r(Jo(b,1))&&(c=Ip(D(b)),r(c))){a=L(c,0);c=L(c,1);a=new Ap(a,c,null,null,null);
	break a}a=null}if(r(a))return a;a:{if(r(Jo(b,2))&&(c=L(b,0),a=L(b,1),d=Ip(c),r(d))){c=L(d,0);d=L(d,1);a=jp(a);a=r(a)?new Bp(c,d,a,null,null,null):null;break a}a=null}if(r(a))return a;a:{c=Hp(b);if(r(c)&&(a=L(c,0),c=L(c,1),d=L(c,0),c=ze(c,1),d=ap(d),e=Ko(Gp,c),r(d))){if(xd(c))throw $i(""+u("rule-expr requieres at least one argument"),new m(null,2,[hl,Lj,nl,b],null));if(null==e)throw $i(""+u("Cannot parse rule-expr arguments, expected [ (variable | constant | '_')+ ]"),new m(null,2,[hl,Lj,nl,b],null));
	a=new Cp(a,d,e,null,null,null);break a}a=null}if(r(a))return a;a:{c=Hp(b);if(r(c)&&(a=L(c,0),c=L(c,1),c=Ko(Gp,c),r(c))){if(xd(c))throw $i(""+u("Pattern could not be empty"),new m(null,2,[hl,Lj,nl,b],null));a=Oo(new zp(a,c,null,null,null),b);break a}a=null}if(r(a))return a;throw $i(""+u("Cannot parse clause, expected (data-pattern | pred-expr | fn-expr | rule-expr | not-clause | not-join-clause | or-clause | or-join-clause)"),new m(null,2,[hl,Lj,nl,b],null));}
	function Np(b,a,c,d,e,f,h){this.find=b;this.Ta=a;this.Pa=c;this.Sa=d;this.h=e;this.f=f;this.i=h;this.m=2229667594;this.A=8192}g=Np.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "find":return this.find;case "with":return this.Ta;case "in":return this.Pa;case "where":return this.Sa;default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.parser.Query{",", ","}",c,R.b(new T(null,4,5,U,[new T(null,2,5,U,[ej,this.find],null),new T(null,2,5,U,[Rk,this.Ta],null),new T(null,2,5,U,[Zl,this.Pa],null),new T(null,2,5,U,[wj,this.Sa],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 4+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,4,[ej,null,wj,null,Rk,null,Zl,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Np(this.find,this.Ta,this.Pa,this.Sa,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(ej,a):Q.call(null,ej,a))?new Np(c,this.Ta,this.Pa,this.Sa,this.h,this.f,null):r(Q.b?Q.b(Rk,a):Q.call(null,Rk,a))?new Np(this.find,c,this.Pa,this.Sa,this.h,this.f,null):r(Q.b?Q.b(Zl,a):Q.call(null,Zl,a))?new Np(this.find,this.Ta,c,this.Sa,this.h,this.f,null):r(Q.b?Q.b(wj,a):Q.call(null,wj,a))?new Np(this.find,this.Ta,this.Pa,c,this.h,this.f,null):new Np(this.find,this.Ta,this.Pa,this.Sa,this.h,M.g(this.f,a,c),null)};
	g.J=function(){return q(R.b(new T(null,4,5,U,[new T(null,2,5,U,[ej,this.find],null),new T(null,2,5,U,[Rk,this.Ta],null),new T(null,2,5,U,[Zl,this.Pa],null),new T(null,2,5,U,[wj,this.Sa],null)],null),this.f))};g.P=function(b,a){return new Np(this.find,this.Ta,this.Pa,this.Sa,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};g.ea=!0;g.fa=function(b,a,c){return Mo(a,this.Sa,H([Mo(a,this.Pa,H([Mo(a,this.Ta,H([Mo(a,this.find,H([c],0))],0))],0))],0))};
	g.ga=function(b,a){return Y(Y(Y(Y(a,this.find),this.Ta),this.Pa),this.Sa)};function Op(b){for(var a=Sd,c=null;;){var d=D(b);if(r(d))d instanceof P?c=d:a=fg.S(a,new T(null,1,5,U,[c],null),yf(kd,I),d),b=E(b);else return a}}
	function Pp(b,a){var c=ji(Jp(ej.c(b))),d=ji(Rk.c(b)),e=ji(Jp(Zl.c(b))),f=ji(Jp(wj.c(b))),e=Fo.b(Do.b(c,d),Do.b(f,e)),c=Eo.b(c,d);if(!xd(e))throw $i([u("Query for unknown vars: "),u(X(H([Zf(ck,e)],0)))].join(""),new m(null,3,[hl,cm,$l,e,nl,a],null));if(!xd(c))throw $i([u(":in and :with should not use same variables: "),u(X(H([Zf(ck,c)],0)))].join(""),new m(null,3,[hl,cm,$l,c,nl,a],null));var h=Jp(Zl.c(b)),k=Lo(function(){return function(a){return a instanceof So}}(h),Zl.c(b)),l=Lo(function(){return function(a){return a instanceof
	Uo}}(h,k),Zl.c(b));if(!r(function(){var a=No(h);return r(a)?(a=No(k),r(a)?No(l):a):a}()))throw $i(""+u("Vars used in :in should be distinct"),new m(null,2,[hl,cm,nl,a],null));c=Jp(Rk.c(b));if(!r(No(c)))throw $i(""+u("Vars used in :with should be distinct"),new m(null,2,[hl,cm,nl,a],null));c=Mo(function(a){return a instanceof So},Zl.c(b),H([Ud],0));d=Mo(function(){return function(a){return a instanceof So}}(c),wj.c(b),H([Ud],0));c=Fo.b(d,c);if(!xd(c))throw $i([u("Where uses unknown source vars: "),
	u(X(H([Zf(ck,c)],0)))].join(""),new m(null,3,[hl,cm,$l,c,nl,a],null));c=Lo(function(a){return a instanceof Cp},wj.c(b));d=Lo(function(){return function(a){return a instanceof Uo}}(c),Zl.c(b));if(!xd(c)&&xd(d))throw $i(""+u("Missing rules var '%' in :in"),new m(null,2,[hl,cm,nl,a],null));};function Qp(b,a,c,d,e){this.Tb=b;this.nd=a;this.od=c;this.md=d;this.na=e;this.m=2147484416;this.A=0}g=Qp.prototype;g.T=function(b,a,c){return Rp.g?Rp.g(this,a,c):Rp.call(null,this,a,c)};g.Nb=function(b,a){return ib(this.Tb,a)};g.C=function(b,a){return hb.g(this.Tb,a,null)};g.B=function(b,a,c){return hb.g(this.Tb,a,c)};g.D=function(b,a,c){return Qb(Vb(this.Tb),a,c)};function Sp(b,a,c,d,e){return new Qp(b,a,c,d,e)}
	function Rp(b,a,c){var d=b.Tb,e=b.nd,f=b.od,h=b.md;b=b.na;var k=f.b?f.b(a,null):f.call(null,a,null);if(r(k))return Sp(d,M.g(O.b(e,k),h,a),M.g(f,a,h),h+1,b);a=Sp(M.g(d,a,c),M.g(e,h,a),M.g(f,a,h),h+1,b);return Tp.c?Tp.c(a):Tp.call(null,a)}function Tp(b){if(J(b.Tb)>b.na){var a=b.Tb,c=b.nd,d=b.od,e=b.md;b=b.na;var f=D(c),h=L(f,0),f=L(f,1);return Sp(O.b(a,f),O.b(c,h),O.b(d,f),e,b)}return b};function Up(b,a,c,d,e){this.Yb=b;this.Aa=a;this.h=c;this.f=d;this.i=e;this.m=2229667594;this.A=8192}g=Up.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "wildcard?":return this.Yb;case "attrs":return this.Aa;default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.pull-parser.PullSpec{",", ","}",c,R.b(new T(null,2,5,U,[new T(null,2,5,U,[wl,this.Yb],null),new T(null,2,5,U,[dl,this.Aa],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 2+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};
	g.W=function(b,a){return Pd(new Td(null,new m(null,2,[dl,null,wl,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Up(this.Yb,this.Aa,this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return r(Q.b?Q.b(wl,a):Q.call(null,wl,a))?new Up(c,this.Aa,this.h,this.f,null):r(Q.b?Q.b(dl,a):Q.call(null,dl,a))?new Up(this.Yb,c,this.h,this.f,null):new Up(this.Yb,this.Aa,this.h,M.g(this.f,a,c),null)};
	g.J=function(){return q(R.b(new T(null,2,5,U,[new T(null,2,5,U,[wl,this.Yb],null),new T(null,2,5,U,[dl,this.Aa],null)],null),this.f))};g.P=function(b,a){return new Up(this.Yb,this.Aa,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};var Vp=function Vp(a){if(a?a.Fb:a)return a.Fb(a);var c;c=Vp[ca(null==a?null:a)];if(!c&&(c=Vp._,!c))throw Ia("IPullSpecComponent.-as-spec",a);return c.call(null,a)};
	function Wp(b,a,c,d){this.V=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=Wp.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "attr":return this.V;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.pull-parser.PullAttrName{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[dm,this.V],null)],null),this.f))};g.N=function(){return this.h};
	g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.Fb=function(){return new T(null,2,5,U,[this.V,new m(null,1,[dm,this.V],null)],null)};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[dm,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Wp(this.V,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(dm,a):Q.call(null,dm,a))?new Wp(c,this.h,this.f,null):new Wp(this.V,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[dm,this.V],null)],null),this.f))};g.P=function(b,a){return new Wp(this.V,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};function Xp(b,a,c,d,e){this.V=b;this.Jb=a;this.h=c;this.f=d;this.i=e;this.m=2229667594;this.A=8192}g=Xp.prototype;
	g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "attr":return this.V;case "rattr":return this.Jb;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.pull-parser.PullReverseAttrName{",", ","}",c,R.b(new T(null,2,5,U,[new T(null,2,5,U,[dm,this.V],null),new T(null,2,5,U,[Hj,this.Jb],null)],null),this.f))};g.N=function(){return this.h};
	g.L=function(){return 2+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.Fb=function(){return new T(null,2,5,U,[this.Jb,new m(null,1,[dm,this.V],null)],null)};g.W=function(b,a){return Pd(new Td(null,new m(null,2,[Hj,null,dm,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Xp(this.V,this.Jb,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(dm,a):Q.call(null,dm,a))?new Xp(c,this.Jb,this.h,this.f,null):r(Q.b?Q.b(Hj,a):Q.call(null,Hj,a))?new Xp(this.V,c,this.h,this.f,null):new Xp(this.V,this.Jb,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,2,5,U,[new T(null,2,5,U,[dm,this.V],null),new T(null,2,5,U,[Hj,this.Jb],null)],null),this.f))};g.P=function(b,a){return new Xp(this.V,this.Jb,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};
	function Yp(b,a,c,d,e){this.V=b;this.na=a;this.h=c;this.f=d;this.i=e;this.m=2229667594;this.A=8192}g=Yp.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "attr":return this.V;case "limit":return this.na;default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.pull-parser.PullLimitExpr{",", ","}",c,R.b(new T(null,2,5,U,[new T(null,2,5,U,[dm,this.V],null),new T(null,2,5,U,[Kj,this.na],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 2+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};
	g.Fb=function(){return eg(Vp(this.V),new T(null,2,5,U,[1,Kj],null),this.na)};g.W=function(b,a){return Pd(new Td(null,new m(null,2,[Kj,null,dm,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Yp(this.V,this.na,this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return r(Q.b?Q.b(dm,a):Q.call(null,dm,a))?new Yp(c,this.na,this.h,this.f,null):r(Q.b?Q.b(Kj,a):Q.call(null,Kj,a))?new Yp(this.V,c,this.h,this.f,null):new Yp(this.V,this.na,this.h,M.g(this.f,a,c),null)};
	g.J=function(){return q(R.b(new T(null,2,5,U,[new T(null,2,5,U,[dm,this.V],null),new T(null,2,5,U,[Kj,this.na],null)],null),this.f))};g.P=function(b,a){return new Yp(this.V,this.na,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};function Zp(b,a,c,d,e){this.V=b;this.value=a;this.h=c;this.f=d;this.i=e;this.m=2229667594;this.A=8192}g=Zp.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "attr":return this.V;case "value":return this.value;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.pull-parser.PullDefaultExpr{",", ","}",c,R.b(new T(null,2,5,U,[new T(null,2,5,U,[dm,this.V],null),new T(null,2,5,U,[ik,this.value],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 2+J(this.f)};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.Fb=function(){return eg(Vp(this.V),new T(null,2,5,U,[1,Uj],null),this.value)};g.W=function(b,a){return Pd(new Td(null,new m(null,2,[ik,null,dm,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Zp(this.V,this.value,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(dm,a):Q.call(null,dm,a))?new Zp(c,this.value,this.h,this.f,null):r(Q.b?Q.b(ik,a):Q.call(null,ik,a))?new Zp(this.V,c,this.h,this.f,null):new Zp(this.V,this.value,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,2,5,U,[new T(null,2,5,U,[dm,this.V],null),new T(null,2,5,U,[ik,this.value],null)],null),this.f))};g.P=function(b,a){return new Zp(this.V,this.value,a,this.f,this.i)};
	g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};function $p(b,a,c){this.h=b;this.f=a;this.i=c;this.m=2229667594;this.A=8192}g=$p.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a){default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.pull-parser.PullWildcard{",", ","}",c,R.b(I,this.f))};g.N=function(){return this.h};g.L=function(){return 0+J(this.f)};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(Ud,a)?O.b(dd(S.b(Sd,this),this.h),a):new $p(this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return new $p(this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(I,this.f))};g.P=function(b,a){return new $p(a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};
	function aq(b,a,c,d){this.na=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=aq.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "limit":return this.na;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.pull-parser.PullRecursionLimit{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[Kj,this.na],null)],null),this.f))};g.N=function(){return this.h};
	g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.Fb=function(){return new T(null,2,5,U,[Ok,this.na],null)};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[Kj,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new aq(this.na,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(Kj,a):Q.call(null,Kj,a))?new aq(c,this.h,this.f,null):new aq(this.na,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[Kj,this.na],null)],null),this.f))};g.P=function(b,a){return new aq(this.na,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};function bq(b,a,c,d,e){this.V=b;this.Ib=a;this.h=c;this.f=d;this.i=e;this.m=2229667594;this.A=8192}g=bq.prototype;
	g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "attr":return this.V;case "porrl":return this.Ib;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.pull-parser.PullMapSpecEntry{",", ","}",c,R.b(new T(null,2,5,U,[new T(null,2,5,U,[dm,this.V],null),new T(null,2,5,U,[Mk,this.Ib],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 2+J(this.f)};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.Fb=function(){return hg(Vp(this.V),1,kd,Vp(this.Ib))};g.W=function(b,a){return Pd(new Td(null,new m(null,2,[Mk,null,dm,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new bq(this.V,this.Ib,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(dm,a):Q.call(null,dm,a))?new bq(c,this.Ib,this.h,this.f,null):r(Q.b?Q.b(Mk,a):Q.call(null,Mk,a))?new bq(this.V,c,this.h,this.f,null):new bq(this.V,this.Ib,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,2,5,U,[new T(null,2,5,U,[dm,this.V],null),new T(null,2,5,U,[Mk,this.Ib],null)],null),this.f))};g.P=function(b,a){return new bq(this.V,this.Ib,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};
	function cq(b,a){return a instanceof $p?M.g(b,wl,!0):hg(b,dl,gf,Vp(a))}function dq(b,a,c,d){this.Vb=b;this.h=a;this.f=c;this.i=d;this.m=2229667594;this.A=8192}g=dq.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "specs":return this.Vb;default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.pull-parser.PullPattern{",", ","}",c,R.b(new T(null,1,5,U,[new T(null,2,5,U,[Zk,this.Vb],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 1+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};
	g.Fb=function(){var b=new Up(!1,Sb(Sd),null,null,null),b=v(cq,b,this.Vb);return new T(null,2,5,U,[gl,gg(b,dl,ef)],null)};g.W=function(b,a){return Pd(new Td(null,new m(null,1,[Zk,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new dq(this.Vb,this.h,qf(O.b(this.f,a)),null)};g.T=function(b,a,c){return r(Q.b?Q.b(Zk,a):Q.call(null,Zk,a))?new dq(c,this.h,this.f,null):new dq(this.Vb,this.h,M.g(this.f,a,c),null)};
	g.J=function(){return q(R.b(new T(null,1,5,U,[new T(null,2,5,U,[Zk,this.Vb],null)],null),this.f))};g.P=function(b,a){return new dq(this.Vb,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};var eq=new Td(null,new m(null,3,[sj,null,"*",null,new C(null,"*","*",345799209,null),null],null),null);function fq(b){return b instanceof P||"string"===typeof b?to(b)?new Xp(uo(b),b,null,null,null):new Wp(b,null,null,null):null}
	var gq=new Td(null,new m(null,2,["...",null,new C(null,"...","...",-1926939749,null),null],null),null);function hq(b){return Ad(b)&&G.b(3,J(b))}var iq=new Td(null,new m(null,3,[new C(null,"limit","limit",284709164,null),null,Kj,null,"limit",null],null),null);
	function jq(b){var a=L(b,0),c=L(b,1),d=L(b,2);if(r(iq.c?iq.c(a):iq.call(null,a))){c=(a=null==d||"number"===typeof d&&0<d)?fq(c):a;if(r(c))return new Yp(c,d,null,null,null);throw $i(""+u('Expected ["limit" attr-name (positive-number | nil)]'),new m(null,2,[hl,xl,Sj,b],null));}return null}var kq=new Td(null,new m(null,3,[new C(null,"default","default",-347290801,null),null,Uj,null,"default",null],null),null);
	function lq(b){var a=L(b,0),c=L(b,1);b=function(){var b=fq(a);return r(b)?b:r(hq(a))?jq(a):null}();if(r(b)){var d=function(){var a;a=r(gq.c?gq.c(c):gq.call(null,c))?new aq(null,null,null,null):"number"===typeof c&&0<c?new aq(c,null,null,null):null;return r(a)?a:mq.c?mq.c(c):mq.call(null,c)}();if(r(d))return new bq(b,d,null,null,null);throw $i(""+u("Expected (pattern | recursion-limit)"),new m(null,2,[hl,xl,Sj,new T(null,2,5,U,[a,c],null)],null));}throw $i(""+u("Expected (attr-name | limit-expr)"),
	new m(null,2,[hl,xl,Sj,new T(null,2,5,U,[a,c],null)],null));}
	function nq(b){var a=fq(b);if(r(a))return a;a=r(eq.c?eq.c(b):eq.call(null,b))?new $p(null,null,null):null;if(r(a))return a;a=Bd(b)?lq(D(b)):null;if(r(a))return a;if(r(hq(b))){if(a=jq(b),!r(a))b:{var c=L(b,0),d=L(b,1),a=L(b,2);if(r(kq.c?kq.c(c):kq.call(null,c))){c=kq.c?kq.c(c):kq.call(null,c);d=r(c)?fq(d):c;if(r(d)){a=new Zp(d,a,null,null,null);break b}throw $i(""+u('Expected ["default" attr-name any-value]'),new m(null,2,[hl,xl,Sj,b],null));}a=null}}else a=null;if(r(a))return a;throw $i(""+u("Cannot parse attr-spec, expected: (attr-name | wildcard | map-spec | attr-expr)"),
	new m(null,2,[hl,xl,Sj,b],null));}function pq(b){return Bd(b)?Cl:r(eq.c?eq.c(b):eq.call(null,b))?Aj:Zj}function qq(b){return S.g(I,Mf.c(function(a){return kd.b(Sd,a)}),b)}function rq(b){b=Xi(pq,b);var a=r(qf(nd(b,Aj)))?new T(null,1,5,U,[new C(null,"*","*",345799209,null)],null):I;return S.g(S.b(a,nd(b,Zj)),wf.b(Mf.c(qq),Mi),nd(b,Cl))}function mq(b){return Ad(b)?new dq(S.g(I,Mf.c(nq),rq(b)),null,null,null):null}
	function sq(b){var a;a=null==b?null:mq(b);a=null==a?null:md(Vp(a),1);if(r(a))return a;throw $i(""+u("Cannot parse pull pattern, expected: [attr-spec+]"),new m(null,2,[hl,xl,Sj,b],null));};function tq(b,a){return v(gf,b,a)}function uq(b,a,c){return pd([vj,pk,qk,Ok,Zk,wl,Hl,am,em],[c,a,am,new m(null,2,[il,Sd,Pk,Ud],null),q(dl.c(b)),wl.c(b),Sb(Sd),b,Sb(I)])}function vq(b,a,c,d){return M.g(uq(b,a,c),dm,d)}function wq(b,a,c){var d=am.c(b);return M.j(b,pk,a,H([Zk,q(dl.c(d)),wl,wl.c(d),Hl,Sb(Sd),em,function(){var a=em.c(b);return q(c)?gf.b(a,c):a}()],0))}function xq(b,a,c){var d=Id(b)?kf(Cf,b):b,e=nd(d,il),d=nd(d,Pk);return M.j(b,il,gg(e,a,yf(Jc,0)),H([Pk,kd.b(d,c)],0))}
	function yq(b,a,c){var d;d=Pd(dg(b,new T(null,2,5,U,[Ok,Pk],null),Ud),c);return r(d)?kd.b(a,hg(b,em,gf,new m(null,1,[bl,c],null))):null}function zq(b,a){var c=Hl.c(a),c=null==c?null:Vb(c);return null==c?null:nd(c,b)}var Aq=xf(zq,kk);function Bq(b,a){var c=dm.c(b),c=xq(Ok.c(b),c,a);return M.g(vq(am.c(b),new T(null,1,5,U,[a],null),!1,kk),Ok,c)}
	function Cq(b){var a=L(b,0);b=ze(b,1);var c=q(pk.c(a));if(c){var a=wq(a,Ac(c),Aq.c?Aq.c(a):Aq.call(null,a)),c=D(c),d=yq(a,b,c);return r(d)?d:kd.j(b,a,H([Bq(a,c)],0))}c=Aq.c?Aq.c(a):Aq.call(null,a);d=em.c(a);c=q(c)?gf.b(d,c):d;return kd.b(b,M.j(a,qk,Bj,H([em,c],0)))}function Dq(b,a,c,d,e){var f=Id(d)?kf(Cf,d):d,h=nd(f,Ok),f=nd(f,am),k=od(nd(h,il),b,0);return G.b(Ok.c(nd(dl.c(f),b)),k)?kd.b(e,d):Cq(kd.j(e,d,H([new m(null,7,[qk,Ok,am,f,dm,b,vj,a,pk,c,Ok,h,em,Sb(I)],null)],0)))}
	var Eq=function(b){return function(a,c,d,e,f){a=xq(Ok.c(a),d,c);return M.g(vq(b,f,e,d),Ok,a)}}(new Up(!0,Sd,null,null,null));
	function Fq(b,a,c,d,e,f,h,k){var l=L(k,0),n=ze(k,1),p=od(h,Kj,1E3),t=qf(r(p)?S.g(I,Nf(p),f):f);if(r(t)){var x=jo(b,c,ul),z=go(b,c),B=z&&jo(b,c,uk),F=r(e)?function(){return function(a){return a.v}}(x,z,B,p,t,k,l,n):function(){return function(a){return a.e}}(x,z,B,p,t,k,l,n);if(Pd(h,gl))return kd.j(n,l,H([vq(gl.c(h),Zf(F,t),x,a)],0));if(Pd(h,Ok))return Dq(a,x,Zf(F,t),l,n);if(r(B?e:B))return kd.j(n,l,H([Eq(l,d,a,x,Zf(F,t))],0));var K=function(){return z?wf.b(function(){return function(a){return pd([bl],
	[a])}}(F,x,z,B,F,p,t,k,l,n),F):F}(),N=r(e)?!x:B;return kd.b(n,ig(l,Hl,hf,a,function(){var a=S.g(I,Mf.c(K),t);return N?D(a):a}()))}return kd.b(n,Pd(h,Uj)?ig(l,Hl,hf,a,Uj.c(h)):l)}var Gq=Wf(function(b){var a=L(b,0);b=L(b,1);return pf.b(a,dm.c(b))});function Hq(b,a,c){return vq(M.j(am.c(b),dl,c,H([wl,!1],0)),new T(null,1,5,U,[a],null),!1,tj)}function Iq(b,a){return kd.b(Ac(b),hg(D(b),Hl,tq,Vb(a)))}function Jq(b){var a=L(b,0);b=ze(b,1);var c=pj.c(a),a=zq(tj,a);return Iq(b,tq(c,r(a)?a:Sd))}
	function Kq(b,a){var c=L(a,0),d=ze(a,1),e=q(Xl.c(c));if(e){var f=D(e),e=L(f,0),f=L(f,1),h=od(cg(c,new T(null,2,5,U,[am,dl],null)),e,Sd);return Fq(b,e,e,rl.c(c),!0,f,h,kd.b(d,gg(c,Xl,Ac)))}e=qf(S.g(Sd,Gq,cg(c,new T(null,2,5,U,[am,dl],null))));return r(e)?(c=M.j(c,qk,lk,H([pj,Hl.c(c),Hl,Sb(Sd)],0)),kd.j(d,c,H([Hq(c,rl.c(c),e)],0))):Iq(d,Hl.c(c))}
	function Lq(b,a,c,d,e){var f=Xi(function(a){return a.a},En(b,Bk,new T(null,1,5,U,[d],null))),h=Id(a)?kf(Cf,a):a,k=nd(h,dm),h=nd(h,Ok),k=null!=k?xq(h,k,d):h;return Kq(b,kd.j(c,a,H([new m(null,6,[qk,Al,Hl,af(new m(null,1,[bl,d],null)),rl,d,am,e,Xl,q(f),Ok,k],null)],0)))}
	function Mq(b,a){for(;;){var c=a,d=L(c,0),e=ze(c,1);if(c=q(pk.c(d))){if(r(wl.c(d))){var f=b,d=M.j(d,Zk,I,H([rl,D(c),wl,!1],0)),c=e,h=Id(d)?kf(Cf,d):d,e=nd(h,rl),h=nd(h,am),k=yq(d,c,e);return r(k)?k:Lq(f,d,c,e,h)}if(f=q(Zk.c(d))){h=f;f=D(h);am.c(d);e=kd.b(e,M.g(d,Zk,Ac(h)));d=b;h=f;c=D(c);f=L(h,0);h=L(h,1);if(G.b(bl,f))d=r(qf(En(d,Bk,new T(null,1,5,U,[c],null))))?kd.b(Ac(e),ig(D(e),Hl,hf,bl,c)):e;else var k=dm.c(h),l=G.b(f,k),n=l?En(d,Bk,new T(null,2,5,U,[c,k],null)):En(d,dk,new T(null,2,5,U,[k,c],
	null)),d=Fq(d,f,k,c,l,n,h,e);return d}f=b;d=kd.b(e,wq(d,Ac(c),qf(ef(Hl.c(d)))));b=f;a=d}else return kd.b(e,M.g(d,qk,Bj))}}
	function Nq(b,a){for(;;)switch(qk.c(D(a))instanceof P?qk.c(D(a)).U:null){case "expand":var c=b,d=Kq(b,a);b=c;a=d;continue;case "expand-rev":c=b;d=Jq(a);b=c;a=d;continue;case "pattern":c=b;d=Mq(b,a);b=c;a=d;continue;case "recursion":c=b;d=Cq(a);b=c;a=d;continue;case "done":var c=a,e=L(c,0),f=ze(c,1),h=function(){var a=ef(em.c(e));return Fa(vj.c(e))?D(a):a}();if(q(f)){c=b;d=kd.b(Ac(f),function(){var a=D(f);return r(h)?ig(a,Hl,hf,dm.c(e),h):a}());b=c;a=d;continue}else return h;default:throw Error([u("No matching clause: "),
	u(qk.c(D(a)))].join(""));}}function Oq(b,a,c,d){c=S.g(I,Mf.c(function(a){return ho(b,a)}),c);return Nq(b,Ya(Bc,uq(a,c,d)))}function Pq(b,a,c){return Oq(b,sq(a),new T(null,1,5,U,[c],null),!1)}function Qq(b,a,c){return Oq(b,sq(a),c,!0)};var Rq=function Rq(a){if(a?a.hd:a)return a.hd();var c;c=Rq[ca(null==a?null:a)];if(!c&&(c=Rq._,!c))throw Ia("PushbackReader.read-char",a);return c.call(null,a)},Sq=function Sq(a,c){if(a?a.jd:a)return a.jd(0,c);var d;d=Sq[ca(null==a?null:a)];if(!d&&(d=Sq._,!d))throw Ia("PushbackReader.unread",a);return d.call(null,a,c)};function Tq(b,a,c){this.Y=b;this.buffer=a;this.Ka=c}Tq.prototype.hd=function(){return 0===this.buffer.length?(this.Ka+=1,this.Y[this.Ka]):this.buffer.pop()};
	Tq.prototype.jd=function(b,a){return this.buffer.push(a)};function Uq(b){var a=!/[^\t\n\r ]/.test(b);return r(a)?a:","===b}function Vq(b,a){var c;!(c=!/[^0-9]/.test(a))&&(c="+"===a||"-"===a)&&(c=Rq(b),Sq(b,c),c=!/[^0-9]/.test(c));return c}function Wq(b){throw Error(kf(u,b));}function Xq(b,a){for(var c=new ia(a),d=Rq(b);;){var e;if(!(e=null==d||Uq(d))){e=d;var f="#"!==e;e=f?(f="'"!==e)?(f=":"!==e)?Yq.c?Yq.c(e):Yq.call(null,e):f:f:f}if(e)return Sq(b,d),c.toString();c.append(d);d=Rq(b)}}
	function Zq(b){for(;;){var a=Rq(b);if("\n"===a||"\r"===a||null==a)return b}}var $q=zi("^([-+]?)(?:(0)|([1-9][0-9]*)|0[xX]([0-9A-Fa-f]+)|0([0-7]+)|([1-9][0-9]?)[rR]([0-9A-Za-z]+))(N)?$"),ar=zi("^([-+]?[0-9]+)/([0-9]+)$"),br=zi("^([-+]?[0-9]+(\\.[0-9]*)?([eE][-+]?[0-9]+)?)(M)?$"),cr=zi("^[:]?([^0-9/].*/)?([^0-9/][^/]*)$");function dr(b,a){var c=b.exec(a);return null!=c&&c[0]===a?1===c.length?c[0]:c:null}
	function er(b){if(r(dr($q,b))){b=dr($q,b);var a=b[2];if(null!=(G.b(a,"")?null:a))b=0;else{var a=r(b[3])?[b[3],10]:r(b[4])?[b[4],16]:r(b[5])?[b[5],8]:r(b[6])?[b[7],parseInt(b[6],10)]:[null,null],c=a[0];null==c?b=null:(a=parseInt(c,a[1]),b="-"===b[1]?-a:a)}}else r(dr(ar,b))?(b=dr(ar,b),b=parseInt(b[1],10)/parseInt(b[2],10)):b=r(dr(br,b))?parseFloat(b):null;return b}var fr=zi("^[0-9A-Fa-f]{2}$"),gr=zi("^[0-9A-Fa-f]{4}$");
	function hr(b,a,c){return r(xi(b,c))?c:Wq(H(["Unexpected unicode escape \\",a,c],0))}function ir(b){return String.fromCharCode(parseInt(b,16))}
	function jr(b){var a=Rq(b),c="t"===a?"\t":"r"===a?"\r":"n"===a?"\n":"\\"===a?"\\":'"'===a?'"':"b"===a?"\b":"f"===a?"\f":null;r(c)?a=c:"x"===a?(b=(new ia(Rq(b),Rq(b))).toString(),a=ir(hr(fr,a,b))):"u"===a?(b=(new ia(Rq(b),Rq(b),Rq(b),Rq(b))).toString(),a=ir(hr(gr,a,b))):a=/[^0-9]/.test(a)?Wq(H(["Unexpected unicode escape \\",a],0)):String.fromCharCode(a);return a}function kr(b){for(var a=Rq(b);;){var c;c=a;c=Uq.c?Uq.c(c):Uq.call(null,c);if(r(c))a=Rq(b);else return a}}
	function lr(b,a){for(var c=Sb(I);;){var d=kr(a);r(d)||Wq(H(["EOF while reading"],0));if(b===d)return Vb(c);var e=function(){var a=d;return Yq.c?Yq.c(a):Yq.call(null,a)}();if(r(e))var f=e,e=function(){var b=d;return f.b?f.b(a,b):f.call(null,a,b)}();else Sq(a,d),e=vr.S?vr.S(a,!0,null,!0):vr.call(null,a,!0,null);c=e===a?c:gf.b(c,e)}}function wr(b,a){return Wq(H(["Reader for ",a," not implemented yet"],0))}
	function xr(b,a){var c=Rq(b),d=yr.c?yr.c(c):yr.call(null,c);if(r(d))return d.b?d.b(b,a):d.call(null,b,a);d=zr.b?zr.b(b,c):zr.call(null,b,c);return r(d)?d:Wq(H(["No dispatch macro for ",c],0))}function Ar(b,a){return Wq(H(["Unmatched delimiter ",a],0))}function Br(b){return kf(He,lr(")",b))}function Cr(b){return lr("]",b)}function Dr(b){b=lr("}",b);uf(J(b))&&Wq(H(["Map literal must contain an even number of forms"],0));return kf(Cf,b)}
	function Er(b,a){for(var c=new ia(a),d=Rq(b);;){if(r(function(){var a=null==d;if(a||(a=Uq(d)))return a;a=d;return Yq.c?Yq.c(a):Yq.call(null,a)}())){Sq(b,d);var e=c.toString(),c=er(e);return r(c)?c:Wq(H(["Invalid number format [",e,"]"],0))}c.append(d);d=e=Rq(b)}}function Fr(b){for(var a=new ia,c=Rq(b);;){if(null==c)return Wq(H(["EOF while reading"],0));if("\\"===c)a.append(jr(b));else{if('"'===c)return a.toString();a.append(c)}c=Rq(b)}}
	function Gr(b){for(var a=new ia,c=Rq(b);;){if(null==c)return Wq(H(["EOF while reading"],0));if("\\"===c){a.append(c);var d=Rq(b);if(null==d)return Wq(H(["EOF while reading"],0));var e=function(){var b=a;b.append(d);return b}(),f=Rq(b)}else{if('"'===c)return a.toString();e=function(){var b=a;b.append(c);return b}();f=Rq(b)}a=e;c=f}}
	function Hr(b,a){var c=Xq(b,a),d=-1!=c.indexOf("/");r(r(d)?1!==c.length:d)?c=yc(c.substring(0,c.indexOf("/")),c.substring(c.indexOf("/")+1,c.length)):(d=xc(c),c="nil"===c?null:"true"===c?!0:"false"===c?!1:"/"===c?new C(null,"/","/",-1371932971,null):d);return c}
	function Mr(b){b=Xq(b,Rq(b));var a=dr(cr,b);b=a[0];var c=a[1],a=a[2];return void 0!==c&&":/"===c.substring(c.length-2,c.length)||":"===a[a.length-1]||-1!==b.indexOf("::",1)?Wq(H(["Invalid token: ",b],0)):null!=c&&0<c.length?Le.b(c.substring(0,c.indexOf("/")),a):Le.c(b)}function Nr(b){return function(a){return Ya(Ya(Bc,vr.S?vr.S(a,!0,null,!0):vr.call(null,a,!0,null)),b)}}function ss(){return function(){return Wq(H(["Unreadable form"],0))}}
	function ts(b){var a;a=vr.S?vr.S(b,!0,null,!0):vr.call(null,b,!0,null);a=a instanceof C?new m(null,1,[ol,a],null):"string"===typeof a?new m(null,1,[ol,a],null):a instanceof P?new gh([a,!0]):a;Bd(a)||Wq(H(["Metadata must be Symbol,Keyword,String or Map"],0));return((b=vr.S?vr.S(b,!0,null,!0):vr.call(null,b,!0,null))?b.m&262144||b.Hd||(b.m?0:Ga(zb,b)):Ga(zb,b))?dd(b,di.j(H([vd(b),a],0))):Wq(H(["Metadata can only be applied to IWithMetas"],0))}function us(b){return ji(lr("}",b))}
	function vs(b){return zi(Gr(b))}function ws(b){vr.S?vr.S(b,!0,null,!0):vr.call(null,b,!0,null);return b}function Yq(b){return'"'===b?Fr:":"===b?Mr:";"===b?Zq:"'"===b?Nr(new C(null,"quote","quote",1377916282,null)):"@"===b?Nr(new C(null,"deref","deref",1494944732,null)):"^"===b?ts:"`"===b?wr:"~"===b?wr:"("===b?Br:")"===b?Ar:"["===b?Cr:"]"===b?Ar:"{"===b?Dr:"}"===b?Ar:"\\"===b?Rq:"#"===b?xr:null}function yr(b){return"{"===b?us:"\x3c"===b?ss():'"'===b?vs:"!"===b?Zq:"_"===b?ws:null}
	function vr(b,a,c){for(;;){var d=Rq(b);if(null==d)return r(a)?Wq(H(["EOF while reading"],0)):c;if(!Uq(d))if(";"===d){var e=function(){var a=b,c=d;return Zq.b?Zq.b(a,c):Zq.call(null,a)}();b=e}else{var f=Yq(d),e=r(f)?function(){var a=b,c=d;return f.b?f.b(a,c):f.call(null,a,c)}():Vq(b,d)?Er(b,d):Hr(b,d);if(e!==b)return e}}}
	var xs=function(b,a){return function(c,d){return nd(r(d)?a:b,c)}}(new T(null,13,5,U,[null,31,28,31,30,31,30,31,31,30,31,30,31],null),new T(null,13,5,U,[null,31,29,31,30,31,30,31,31,30,31,30,31],null)),ys=/(\d\d\d\d)(?:-(\d\d)(?:-(\d\d)(?:[T](\d\d)(?::(\d\d)(?::(\d\d)(?:[.](\d+))?)?)?)?)?)?(?:[Z]|([-+])(\d\d):(\d\d))?/;function zs(b){b=parseInt(b,10);return Fa(isNaN(b))?b:null}
	function As(b,a,c,d){b<=a&&a<=c||Wq(H([[u(d),u(" Failed:  "),u(b),u("\x3c\x3d"),u(a),u("\x3c\x3d"),u(c)].join("")],0));return a}
	function Bs(b){var a=xi(ys,b);L(a,0);var c=L(a,1),d=L(a,2),e=L(a,3),f=L(a,4),h=L(a,5),k=L(a,6),l=L(a,7),n=L(a,8),p=L(a,9),t=L(a,10);if(Fa(a))return Wq(H([[u("Unrecognized date/time syntax: "),u(b)].join("")],0));var x=zs(c),z=function(){var a=zs(d);return r(a)?a:1}();b=function(){var a=zs(e);return r(a)?a:1}();var a=function(){var a=zs(f);return r(a)?a:0}(),c=function(){var a=zs(h);return r(a)?a:0}(),B=function(){var a=zs(k);return r(a)?a:0}(),F=function(){var a;a:if(G.b(3,J(l)))a=l;else if(3<J(l))a=
	l.substring(0,3);else for(a=new ia(l);;)if(3>a.Bb.length)a=a.append("0");else{a=a.toString();break a}a=zs(a);return r(a)?a:0}(),n=(G.b(n,"-")?-1:1)*(60*function(){var a=zs(p);return r(a)?a:0}()+function(){var a=zs(t);return r(a)?a:0}());return new T(null,8,5,U,[x,As(1,z,12,"timestamp month field must be in range 1..12"),As(1,b,function(){var a;a=0===ue(x,4);r(a)&&(a=Fa(0===ue(x,100)),a=r(a)?a:0===ue(x,400));return xs.b?xs.b(z,a):xs.call(null,z,a)}(),"timestamp day field must be in range 1..last day in month"),
	As(0,a,23,"timestamp hour field must be in range 0..23"),As(0,c,59,"timestamp minute field must be in range 0..59"),As(0,B,G.b(c,59)?60:59,"timestamp second field must be in range 0..60"),As(0,F,999,"timestamp millisecond field must be in range 0..999"),n],null)}
	var Cs,Ds=new m(null,4,["inst",function(b){var a;if("string"===typeof b)if(a=Bs(b),r(a)){b=L(a,0);var c=L(a,1),d=L(a,2),e=L(a,3),f=L(a,4),h=L(a,5),k=L(a,6);a=L(a,7);a=new Date(Date.UTC(b,c-1,d,e,f,h,k)-6E4*a)}else a=Wq(H([[u("Unrecognized date/time syntax: "),u(b)].join("")],0));else a=Wq(H(["Instance literal expects a string for its timestamp."],0));return a},"uuid",function(b){return"string"===typeof b?new Yi(b,null):Wq(H(["UUID literal expects a string as its representation."],0))},"queue",function(b){return Cd(b)?
	S.b(Pg,b):Wq(H(["Queue literal expects a vector for its elements."],0))},"js",function(b){if(Cd(b)){var a=[];b=q(b);for(var c=null,d=0,e=0;;)if(e<d){var f=c.M(null,e);a.push(f);e+=1}else if(b=q(b))c=b,Ed(c)?(b=y(c),e=A(c),c=b,d=J(b),b=e):(b=D(c),a.push(b),b=E(c),c=null,d=0),e=0;else break;return a}if(Bd(b)){a={};b=q(b);c=null;for(e=d=0;;)if(e<d){var h=c.M(null,e),f=L(h,0),h=L(h,1);a[Me(f)]=h;e+=1}else if(b=q(b))Ed(b)?(d=y(b),b=A(b),c=d,d=J(d)):(d=D(b),c=L(d,0),d=L(d,1),a[Me(c)]=d,b=E(b),c=null,d=
	0),e=0;else break;return a}return Wq(H([[u("JS literal expects a vector or map containing "),u("only string or unqualified keyword keys")].join("")],0))}],null);Cs=Bf?Bf(Ds):Af.call(null,Ds);var Es=Bf?Bf(null):Af.call(null,null);
	function zr(b,a){var c=Hr(b,a),d=nd(Pc.c?Pc.c(Cs):Pc.call(null,Cs),""+u(c)),e=Pc.c?Pc.c(Es):Pc.call(null,Es);return r(d)?(c=vr(b,!0,null),d.c?d.c(c):d.call(null,c)):r(e)?(d=vr(b,!0,null),e.b?e.b(c,d):e.call(null,c,d)):Wq(H(["Could not find tag parser for ",""+u(c)," in ",X(H([$g(Pc.c?Pc.c(Cs):Pc.call(null,Cs))],0))],0))}function Fs(b,a){var c=""+u(b);nd(Pc.c?Pc.c(Cs):Pc.call(null,Cs),c);Ff.S(Cs,M,c,a)};function Gs(b,a){var c;c="number"===typeof a||Ad(a)?ko(b,a):null;if(r(c)){var d=Hf(!1),e=Hf(Sd);return Hs.S?Hs.S(b,c,d,e):Hs.call(null,b,c,d,e)}return null}function Is(b,a,c){return jo(b,a,ul)?go(b,a)?v(function(a,c){return kd.b(a,Gs(b,xj.c(c)))},Ud,c):v(function(a,b){return kd.b(a,xj.c(b))},Ud,c):go(b,a)?Gs(b,xj.c(D(c))):xj.c(D(c))}
	function Js(b,a,c,d){a=qf(Dn(b,new T(null,3,5,U,[null,c,a],null)));return r(a)?jo(b,c,uk)?Gs(b,xk.c(D(a))):v(function(){return function(a,c){return kd.b(a,Gs(b,xk.c(c)))}}(a,a),Ud,a):d}function Ks(b){return r(b)?Zd(b):null}
	function Ls(b){Ms.c?Ms.c(b):Ms.call(null,b);return function c(d){return new Ne(null,function(){for(;;){var e=q(d);if(e){if(Ed(e)){var f=y(e),h=J(f),k=Re(h);a:for(var l=0;;)if(l<h){var n=w.b(f,l),p=L(n,0),n=L(n,1),p=jo(b.db,p,ul)?new T(null,2,5,U,[p,Ks(n)],null):new T(null,2,5,U,[p,n],null);k.add(p);l+=1}else{f=!0;break a}return f?Te(Xe(k),c(A(e))):Te(Xe(k),null)}f=D(e);k=L(f,0);f=L(f,1);return bd(jo(b.db,k,ul)?new T(null,2,5,U,[k,Ks(f)],null):new T(null,2,5,U,[k,f],null),c(Ac(e)))}return null}},null,
	null)}(function(){var c=b.Cb;return Pc.c?Pc.c(c):Pc.call(null,c)}())}function Ns(b,a,c,d){this.db=b;this.eid=a;this.Vc=c;this.Cb=d;this.m=2162164483;this.A=0}g=Ns.prototype;g.entry_set=function(){return Zd(Mf.b(Zd,Ls(this)))};
	g.forEach=function(){function b(a,b){for(var c=q(Ls(this)),h=null,k=0,l=0;;)if(l<k){var n=h.M(null,l),p=L(n,0),n=L(n,1);a.call(b,n,p,this);l+=1}else if(c=q(c))Ed(c)?(h=y(c),c=A(c),p=h,k=J(h),h=p):(h=D(c),p=L(h,0),n=L(h,1),a.call(b,n,p,this),c=E(c),h=null,k=0),l=0;else return null}function a(a){for(var b=q(Ls(this)),c=null,h=0,k=0;;)if(k<h){var l=c.M(null,k),n=L(l,0),l=L(l,1);a.g?a.g(l,n,this):a.call(null,l,n,this);k+=1}else if(b=q(b))Ed(b)?(c=y(b),b=A(b),n=c,h=J(c),c=n):(c=D(b),n=L(c,0),c=l=L(c,1),
	a.g?a.g(c,n,this):a.call(null,c,n,this),b=E(b),c=null,h=0),k=0;else return null}var c=null,c=function(c,e){switch(arguments.length){case 1:return a.call(this,c);case 2:return b.call(this,c,e)}throw Error("Invalid arity: "+arguments.length);};c.c=a;c.b=b;return c}();g.get=function(b){if(G.b(b,":db/id"))return this.eid;if(to(b))return Ks(Js(this.db,this.eid,uo(b),null));var a=Os?Os(this,b):Ps.call(null,this,b);return jo(this.db,b,ul)?Ks(a):a};g.key_set=function(){return Zd($g(this))};g.entries=function(){return Ug(Ls(this))};
	g.value_set=function(){return Zd(Mf.b(hd,Ls(this)))};g.toString=function(){return hc(this)};g.keys=function(){return Dc($g(this))};g.values=function(){return Dc(Mf.b(hd,Ls(this)))};g.equiv=function(b){return Qs.b?Qs.b(this,b):Qs.call(null,this,b)};g.has=function(b){return null!=this.get(b)};g.u=function(b,a){return Qs.b?Qs.b(this,a):Qs.call(null,this,a)};g.I=function(){return sc(this.eid)};g.J=function(){Ms.c?Ms.c(this):Ms.call(null,this);var b=q,a;a=this.Cb;a=Pc.c?Pc.c(a):Pc.call(null,a);return b(a)};
	g.L=function(){Ms.c?Ms.c(this):Ms.call(null,this);var b;b=this.Cb;b=Pc.c?Pc.c(b):Pc.call(null,b);return J(b)};g.C=function(b,a){return Rs?Rs(this,a,null):Ps.call(null,this,a,null)};g.B=function(b,a,c){return Rs?Rs(this,a,c):Ps.call(null,this,a,c)};g.Nb=function(b,a){return pf.b(fl,Rs?Rs(this,a,fl):Ps.call(null,this,a,fl))};
	g.call=function(){function b(a,b,c){return Rs?Rs(this,b,c):Ps.call(null,this,b,c)}function a(a,b){return Os?Os(this,b):Ps.call(null,this,b)}var c=null,c=function(c,e,f){switch(arguments.length){case 2:return a.call(this,0,e);case 3:return b.call(this,0,e,f)}throw Error("Invalid arity: "+arguments.length);};c.b=a;c.g=b;return c}();g.apply=function(b,a){return this.call.apply(this,[this].concat(Qa(a)))};g.c=function(b){return Os?Os(this,b):Ps.call(null,this,b)};
	g.b=function(b,a){return Rs?Rs(this,b,a):Ps.call(null,this,b,a)};g.D=function(b,a,c){var d=this;return Qb(M.g(function(){var a=d.Cb;return Pc.c?Pc.c(a):Pc.call(null,a)}(),bl,d.eid),a,c)};function Hs(b,a,c,d){return new Ns(b,a,c,d)}function Qs(b,a){return a instanceof Ns&&G.b(b.eid,a.eid)}
	function Ps(){switch(arguments.length){case 2:return Os(arguments[0],arguments[1]);case 3:return Rs(arguments[0],arguments[1],arguments[2]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function Os(b,a){return Rs(b,a,null)}
	function Rs(b,a,c){if(G.b(a,bl))return b.eid;if(to(a))return Js(b.db,b.eid,uo(a),c);var d=function(){var a=b.Cb;return Pc.c?Pc.c(a):Pc.call(null,a)}().call(null,a);if(r(d))return d;if(r(function(){var a=b.Vc;return Pc.c?Pc.c(a):Pc.call(null,a)}()))return c;d=qf(Dn(b.db,new T(null,2,5,U,[b.eid,a],null)));r(d)&&(c=Is(b.db,a,d),If(b.Cb,M.g(function(){var a=b.Cb;return Pc.c?Pc.c(a):Pc.call(null,a)}(),a,c)));return c}
	function Ss(b,a){return ge(function(a,d,e){return M.g(a,d,jo(b,d,uk)?jo(b,d,ul)?ji(Mf.b(Ms,e)):Ms.c?Ms.c(e):Ms.call(null,e):e)},Sd,a)}function Ts(b,a){return v(function(a,d){var e=Wl.c(D(d));return M.g(a,e,Is(b,e,d))},Sd,vi(Wl,a))}function Ms(b){var a;a=b.Vc;a=Pc.c?Pc.c(a):Pc.call(null,a);r(a)||(a=qf(Dn(b.db,new T(null,1,5,U,[b.eid],null))),r(a)&&(a=Ss(b.db,Ts(b.db,a)),ec(b.Cb,a),ec(b.Vc,!0)));return b}fa("datascript.impl.entity.Entity.prototype.get",Ns.prototype.get);
	fa("datascript.impl.entity.Entity.prototype.has",Ns.prototype.has);fa("datascript.impl.entity.Entity.prototype.forEach",Ns.prototype.forEach);fa("datascript.impl.entity.Entity.prototype.key_set",Ns.prototype.key_set);fa("datascript.impl.entity.Entity.prototype.value_set",Ns.prototype.value_set);fa("datascript.impl.entity.Entity.prototype.entry_set",Ns.prototype.entry_set);fa("datascript.impl.entity.Entity.prototype.keys",Ns.prototype.keys);fa("datascript.impl.entity.Entity.prototype.values",Ns.prototype.values);
	fa("datascript.impl.entity.Entity.prototype.entries",Ns.prototype.entries);fa("cljs.core.ES6Iterator.prototype.next",Cc.prototype.next);fa("cljs.core.ES6EntriesIterator.prototype.next",Tg.prototype.next);function Us(b,a,c,d,e,f){this.Lb=b;this.Mb=a;this.rules=c;this.h=d;this.f=e;this.i=f;this.m=2229667594;this.A=8192}g=Us.prototype;g.C=function(b,a){return hb.g(this,a,null)};g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "rels":return this.Lb;case "sources":return this.Mb;case "rules":return this.rules;default:return od(this.f,a,c)}};
	g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.query.Context{",", ","}",c,R.b(new T(null,3,5,U,[new T(null,2,5,U,[Ik,this.Lb],null),new T(null,2,5,U,[ek,this.Mb],null),new T(null,2,5,U,[yk,this.rules],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 3+J(this.f)};g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};
	g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,3,[ek,null,yk,null,Ik,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Us(this.Lb,this.Mb,this.rules,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(Ik,a):Q.call(null,Ik,a))?new Us(c,this.Mb,this.rules,this.h,this.f,null):r(Q.b?Q.b(ek,a):Q.call(null,ek,a))?new Us(this.Lb,c,this.rules,this.h,this.f,null):r(Q.b?Q.b(yk,a):Q.call(null,yk,a))?new Us(this.Lb,this.Mb,c,this.h,this.f,null):new Us(this.Lb,this.Mb,this.rules,this.h,M.g(this.f,a,c),null)};
	g.J=function(){return q(R.b(new T(null,3,5,U,[new T(null,2,5,U,[Ik,this.Lb],null),new T(null,2,5,U,[ek,this.Mb],null),new T(null,2,5,U,[yk,this.rules],null)],null),this.f))};g.P=function(b,a){return new Us(this.Lb,this.Mb,this.rules,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};function Vs(b,a,c,d,e){this.Aa=b;this.Wb=a;this.h=c;this.f=d;this.i=e;this.m=2229667594;this.A=8192}g=Vs.prototype;g.C=function(b,a){return hb.g(this,a,null)};
	g.B=function(b,a,c){switch(a instanceof P?a.U:null){case "attrs":return this.Aa;case "tuples":return this.Wb;default:return od(this.f,a,c)}};g.D=function(b,a,c){return V(a,function(){return function(b){return V(a,Fi,""," ","",c,b)}}(this),"#datascript.query.Relation{",", ","}",c,R.b(new T(null,2,5,U,[new T(null,2,5,U,[dl,this.Aa],null),new T(null,2,5,U,[ij,this.Wb],null)],null),this.f))};g.N=function(){return this.h};g.L=function(){return 2+J(this.f)};
	g.I=function(){var b=this.i;return null!=b?b:this.i=b=Be(this)};g.u=function(b,a){var c;c=r(a)?(c=this.constructor===a.constructor)?Sg(this,a):c:a;return r(c)?!0:!1};g.W=function(b,a){return Pd(new Td(null,new m(null,2,[ij,null,dl,null],null),null),a)?O.b(dd(S.b(Sd,this),this.h),a):new Vs(this.Aa,this.Wb,this.h,qf(O.b(this.f,a)),null)};
	g.T=function(b,a,c){return r(Q.b?Q.b(dl,a):Q.call(null,dl,a))?new Vs(c,this.Wb,this.h,this.f,null):r(Q.b?Q.b(ij,a):Q.call(null,ij,a))?new Vs(this.Aa,c,this.h,this.f,null):new Vs(this.Aa,this.Wb,this.h,M.g(this.f,a,c),null)};g.J=function(){return q(R.b(new T(null,2,5,U,[new T(null,2,5,U,[dl,this.Aa],null),new T(null,2,5,U,[ij,this.Wb],null)],null),this.f))};g.P=function(b,a){return new Vs(this.Aa,this.Wb,a,this.f,this.i)};g.O=function(b,a){return Cd(a)?kb(this,w.b(a,0),w.b(a,1)):v(Ya,this,a)};
	function Ws(b,a){return Eo.b(ji($g(b)),ji($g(a)))}function Xs(b){return S.g(I,Mi,b)}
	var Ys=function Ys(a,c){return G.b(new C(null,"_","_",-1201019570,null),a)?!0:G.b(new T(null,1,5,U,[new C(null,"*","*",345799209,null)],null),a)?Ad(c):a instanceof C?G.b(c,a):Ad(a)?G.b(jd(a),new C(null,"*","*",345799209,null))?Ad(c)&&rf(function(a){var c=L(a,0);a=L(a,1);return Ys(c,a)},Mf.g(Eg,mi(a),c)):Ad(c)&&G.b(J(c),J(a))&&rf(function(a){var c=L(a,0);a=L(a,1);return Ys(c,a)},Mf.g(Eg,a,c)):a.c?a.c(c):a.call(null,c)};function Zs(b){return b instanceof C&&G.b("$",D(Me(b)))}
	function $s(b){return b instanceof C&&G.b("?",D(Me(b)))}function at(b){return b instanceof P||"string"===typeof b}function bt(b){return Ys(new T(null,2,5,U,[at,new C(null,"_","_",-1201019570,null)],null),b)}function ct(b,a,c,d){var e=a.length,f=d.length,h;h=e+f;h=Oa?Array(h):Ma.call(null,h);for(var k=0;;)if(k<e)h[k]=b[a[k]],k+=1;else break;for(b=0;;)if(b<f)h[e+b]=c[d[b]],b+=1;else break;return h}function dt(b,a){return new Vs(dl.c(b),R.b(ij.c(b),ij.c(a)),null,null,null)}
	var et=function et(){switch(arguments.length){case 0:return et.R();case 2:return et.b(arguments[0],arguments[1]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}};et.R=function(){return new Vs(Sd,new T(null,1,5,U,[Oa?[]:Ma.call(null,0)],null),null,null,null)};
	et.b=function(b,a){var c=$g(dl.c(b)),d=$g(dl.c(a)),e=Zd(Mf.b(dl.c(b),c)),f=Zd(Mf.b(dl.c(a),d));return new Vs(ni(R.b(c,d),bg()),ef(v(function(b,c,d,e){return function(f,t){return v(function(a,b,c,d){return function(a,b){return gf.b(a,ct(t,c,b,d))}}(b,c,d,e),f,ij.c(a))}}(c,d,e,f),Sb(I),ij.c(b))),null,null,null)};et.H=2;var ft=function ft(){return ft.j(0<arguments.length?new za(Array.prototype.slice.call(arguments,0),0):null)};ft.j=function(b){var a=J(b);return pf.b(Of(a/2,b),Pf(a/2,b))};ft.H=0;
	ft.F=function(b){return ft.j(q(b))};var gt=function gt(){return gt.j(arguments[0],arguments[1],2<arguments.length?new za(Array.prototype.slice.call(arguments,2),0):null)};gt.j=function(b,a,c){return v(function(c,e){var f=D(Dn(b,new T(null,2,5,U,[a,e],null)));r(f)?(f=xj.c(f),f=new Kc(f)):f=null;return f},null,c)};gt.H=2;gt.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return gt.j(a,b,c)};
	var ht=pd([new C(null,"true?","true?",-1600332395,null),new C(null,"odd?","odd?",-1458588199,null),new C(null,"get-else","get-else",1312024065,null),new C(null,"\x3e\x3d","\x3e\x3d",1016916022,null),new C(null,"\x3e","\x3e",1085014381,null),new C(null,"get-some","get-some",409442058,null),new C(null,"nil?","nil?",1612038930,null),new C(null,"inc","inc",324505433,null),new C(null,"/","/",-1371932971,null),new C(null,"quot","quot",-1125214196,null),new C(null,"false?","false?",-1522377573,null),new C(null,
	"identity","identity",-1007039734,null),new C(null,"-differ?","-differ?",1465687357,null),new C(null,"\x3c\x3d","\x3c\x3d",1244895369,null),new C(null,"\x3d","\x3d",-1501502141,null),new C(null,"min","min",2085523049,null),new C(null,"+","+",-740910886,null),new C(null,"missing?","missing?",-1710383910,null),new C(null,"ground","ground",-1460862835,null),new C(null,"\x3d\x3d","\x3d\x3d",-234118149,null),new C(null,"max","max",1701898075,null),new C(null,"*","*",345799209,null),new C(null,"zero?",
	"zero?",325758897,null),new C(null,"!\x3d","!\x3d",-201205829,null),new C(null,"dec","dec",-766002333,null),new C(null,"vector","vector",-751469611,null),new C(null,"not\x3d","not\x3d",1466536204,null),new C(null,"str","str",-1564826950,null),new C(null,"\x3c","\x3c",993667236,null),new C(null,"-","-",-471816912,null),new C(null,"pos?","pos?",-244377722,null),new C(null,"mod","mod",1510044207,null),new C(null,"rem","rem",664046770,null),new C(null,"even?","even?",-1827825394,null),new C(null,"neg?",
	"neg?",-1902175577,null)],[function(b){return!0===b},uf,function(b,a,c,d){b=D(Dn(b,new T(null,2,5,U,[a,c],null)));return r(b)?xj.c(b):d},qe,pe,gt,Da,Jc,me,ve,function(b){return!1===b},he,ft,oe,G,se,je,function(b,a,c){return null==nd(Gs(b,a),c)},he,xe,re,le,function(b){return 0===b},pf,function(b){return b-1},Eg,pf,u,ne,ke,function(b){return 0<b},ue,function(b,a){return b-a*ve(b,a)},tf,ye]),it=function(){function b(b){var e=a(b);return c(function(){return function(a){return function k(b){return new Ne(null,
	function(a){return function(){for(;;){var c=q(b);if(c){if(Ed(c)){var d=y(c),e=J(d),f=Re(e);a:for(var B=0;;)if(B<e){var F=w.b(d,B)-a;f.add(F*F);B+=1}else{d=!0;break a}return d?Te(Xe(f),k(A(c))):Te(Xe(f),null)}f=D(c)-a;return bd(f*f,k(Ac(c)))}return null}}}(a),null,null)}}(e)(b)}())/J(b)}function a(a){return c(a)/J(a)}function c(a){return v(je,0,a)}return pd([new C(null,"variance","variance",-1522424942,null),new C(null,"count","count",-514511684,null),new C(null,"median","median",-2084869638,null),
	new C(null,"sum","sum",1777518341,null),new C(null,"min","min",2085523049,null),new C(null,"max","max",1701898075,null),new C(null,"count-distinct","count-distinct",-1566572514,null),new C(null,"distinct","distinct",-148347594,null),new C(null,"avg","avg",1837937727,null),new C(null,"stddev","stddev",775056588,null),new C(null,"rand","rand",-1745930995,null),new C(null,"sample","sample",1719555128,null)],[b,J,function(a){var b=Yd(Vd,a);a=J(a);var c=a>>1,h=md(b,c);return tf(a)?(h+md(b,c-1))/2:h},c,
	function(){function a(b,c){return ce(v(function(a,c){return J(a)<b?Yd(tn,kd.b(a,c)):0>tn(c,jd(a))?Yd(tn,kd.b(mi(a),c)):a},I,c))}function b(a){return v(function(a,b){return 0>tn(b,a)?b:a},D(a),E(a))}var c=null,c=function(c,f){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,f)}throw Error("Invalid arity: "+arguments.length);};c.c=b;c.b=a;return c}(),function(){function a(b,c){return ce(v(function(a,c){return J(a)<b?Yd(tn,kd.b(a,c)):0<tn(c,D(a))?Yd(tn,kd.b(E(a),c)):
	a},I,c))}function b(a){return v(function(a,b){return 0<tn(b,a)?b:a},D(a),E(a))}var c=null,c=function(c,f){switch(arguments.length){case 1:return b.call(this,c);case 2:return a.call(this,c,f)}throw Error("Invalid arity: "+arguments.length);};c.c=b;c.b=a;return c}(),function(a){return J(li.c(a))},wf.b(ce,li),a,function(a){a=b(a);return Math.sqrt(a)},function(){function a(b,c){return ce(Uf(b,function(){return Wi(c)}))}var b=null,b=function(b,c){switch(arguments.length){case 1:return Wi(b);case 2:return a.call(this,
	b,c)}throw Error("Invalid arity: "+arguments.length);};b.c=function(a){return Wi(a)};b.b=a;return b}(),function(a,b){return ce(Of(a,$d(b)))}])}();function jt(b){b="string"===typeof b?vr(new Tq(b,[],-1),!1,null):b;return Xi(id,b)}function kt(b){var a=Md.c?Md.c(b):Md.call(null,b);return r(a)?a:Ea.c?Ea.c(b):Ea.call(null,b)}function lt(b){b=Mf.b(ck,ce(li.c(Jp(b))));return new Vs(ni(b,bg()),I,null,null,null)}
	var mt=function mt(a,c){if(a?a.lc:a)return a.lc(a,c);var d;d=mt[ca(null==a?null:a)];if(!d&&(d=mt._,!d))throw Ia("IBinding.in-\x3erel",a);return d.call(null,a,c)};dp.prototype.lc=function(){return et.R()};ep.prototype.lc=function(b,a){return new Vs(new gh([cg(this,new T(null,2,5,U,[Cj,ck],null)),0]),new T(null,1,5,U,[Aa(new T(null,1,5,U,[a],null))],null),null,null,null)};
	gp.prototype.lc=function(b,a){if(Fa(kt(a)))throw $i([u("Cannot bind value "),u(X(H([a],0))),u(" to collection "),u(X(H([Po(this)],0)))].join(""),new m(null,3,[hl,gk,ik,a,Kk,Po(this)],null));return xd(a)?lt(this):de(dt,Mf.b(function(a){return function(b){return mt(Kk.c(a),b)}}(this),a))};
	fp.prototype.lc=function(b,a){if(Fa(kt(a)))throw $i([u("Cannot bind value "),u(X(H([a],0))),u(" to tuple "),u(X(H([Po(this)],0)))].join(""),new m(null,3,[hl,gk,ik,a,Kk,Po(this)],null));if(J(a)<J(ak.c(this)))throw $i([u("Not enough elements in a collection "),u(X(H([a],0))),u(" to bind tuple "),u(X(H([Po(this)],0)))].join(""),new m(null,3,[hl,gk,ik,a,Kk,Po(this)],null));return de(et,Mf.g(function(){return function(a,b){return mt(a,b)}}(this),ak.c(this),a))};
	function nt(b,a){var c=L(a,0),d=L(a,1);return c instanceof ep&&Cj.c(c)instanceof So?fg.Z(b,new T(null,1,5,U,[ek],null),M,cg(c,new T(null,2,5,U,[Cj,ck],null)),d):c instanceof ep&&Cj.c(c)instanceof Uo?M.g(b,yk,jt(d)):fg.S(b,new T(null,1,5,U,[Ik],null),kd,mt(c,d))}function ot(b,a,c){return v(nt,b,ni(a,c))}var pt=null,qt=null;
	function rt(b,a){var c=b.c?b.c(a):b.call(null,a);return null!=pt&&Pd(pt,a)?function(a){return function(b){b=b[a];return"number"===typeof b?b:ko(qt,b)}}(c):function(a){return function(b){return b[a]}}(c)}function st(b){return 1===J(b)?D(b):function(a){return function(b){return $e(a.map(function(){return function(a){return a.c?a.c(b):a.call(null,b)}}(a)))}}(Zd(b))}
	function tt(b,a){for(var c=a,d=Sb(Sd);;){var e=D(c);if(r(e)){var f;f=e;f=b.c?b.c(f):b.call(null,f);c=E(c);d=hf.g(d,f,kd.b(od(d,f,Bc),e))}else return Vb(d)}}
	function yt(b,a){var c=ij.c(b),d=ij.c(a),e=dl.c(b),f=dl.c(a),h=ce(Ws(dl.c(b),dl.c(a))),k=Mf.b(function(a,b,c){return function(a){return rt(c,a)}}(c,d,e,f,h),h),l=Mf.b(function(a,b,c,d){return function(a){return rt(d,a)}}(c,d,e,f,h,k),h),n=$g(e),p=ce(Fo.b(ji($g(f)),ji($g(e)))),t=Zd(Mf.b(e,n)),x=Zd(Mf.b(f,p)),z=st(k),B=tt(z,c),F=st(l),c=ef(v(function(a,b,c,d,e,f,h,k,l,n,p,t,x,z){return function(B,F){var Gb=z.c?z.c(F):z.call(null,F),Hb=nd(x,Gb);return r(Hb)?v(function(a,b,c,d,e,f,h,k,l,n,p,t,x,z){return function(a,
	b){return gf.b(a,ct(b,x,F,z))}}(Hb,Hb,Gb,a,b,c,d,e,f,h,k,l,n,p,t,x,z),B,Hb):B}}(c,d,e,f,h,k,l,n,p,t,x,z,B,F),Sb(I),d));return new Vs(ni(R.b(n,p),bg()),c,null,null,null)}function zt(b,a){var c=Zf(function(a){return a instanceof C?null:a},a),d=Dn(b,c),c=S.b(Sd,Xf(function(){return function(a){var b=L(a,0);L(a,1);return $s(b)}}(c,d),Mf.g(Eg,a,new T(null,4,5,U,["e","a","v","tx"],null))));return new Vs(c,d,null,null,null)}
	function At(b,a){var c=Xf(function(b){a:for(var c=a;;){var d=b;if(r(r(d)?c:d)){var d=D(b),k=D(c);if(k instanceof C||G.b(d,k))b=E(b),c=E(c);else{b=!1;break a}}else{b=!0;break a}}return b},b),d=S.b(Sd,Xf(function(){return function(a){var b=L(a,0);L(a,1);return $s(b)}}(c),Mf.g(Eg,a,bg())));return new Vs(d,Mf.b(Zd,c),null,null,null)}function Bt(b){return r(Zs(D(b)))?b:R.b(new T(null,1,5,U,[new C(null,"$","$",-1580747756,null)],null),b)}
	function Ct(b,a){return(b?r(r(null)?null:b.ld)||(b.Eb?0:Ga(Cn,b)):Ga(Cn,b))?zt(b,a):At(b,a)}function Dt(b,a){for(var c=b,d=a,e=I;;){var f=D(c);if(r(f))r(qf(Ws(dl.c(d),dl.c(f))))?(c=E(c),d=yt(f,d)):(c=E(c),e=kd.b(e,f));else return kd.b(e,d)}}function Et(b,a){var c=sf(function(b){return Pd(dl.c(b),a)?b:null},Ik.c(b));if(r(c)){var d=D(ij.c(c));return r(d)?d[dl.c(c).call(null,a)]:null}return null}
	function Ft(b,a){var c=Xf(function(b){return!xd(Eo.b(ji(a),ji($g(dl.c(b)))))},Ik.c(b)),d=de(et,c);return new T(null,2,5,U,[fg.g(b,new T(null,1,5,U,[Ik],null),function(a){return function(b){return Yf(ji(a),b)}}(c,d)),d],null)}function Gt(b,a,c,d){return function(e){var f=Mf.b(function(c){if(c instanceof C){var d=nd(ek.c(b),c);return r(d)?d:e[nd(dl.c(a),c)]}return c},d);return kf(c,f)}}
	function Ht(b,a){var c=L(a,0),d=L(c,0),e=ze(c,1),f=function(){var a=nd(ht,d);return r(a)?a:Et(b,d)}(),h=Ft(b,Xf(vc,e)),k=L(h,0),l=L(h,1),n=r(f)?function(){return fg.g(l,new T(null,1,5,U,[ij],null),function(a){return function(b){return Xf(a,b)}}(Gt(k,l,f,e),a,c,d,e,f,h,k,l))}():M.g(l,new T(null,1,5,U,[ij],null),I);return fg.S(k,new T(null,1,5,U,[Ik],null),kd,n)}
	function It(b,a){var c=L(a,0),d=L(c,0),e=ze(c,1),f=L(a,1),h=jp(f),k=function(){var a=nd(ht,d);return r(a)?a:Et(b,d)}(),l=Ft(b,Xf(vc,e)),n=L(l,0),p=L(l,1),t=r(k)?function(){var b=Gt(n,p,k,e),t=qf(ij.c(p));return r(t)?de(dt,Mf.b(function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(a){var b=c.c?c.c(a):c.call(null,a),b=mt(l,b);return et.b(new Vs(dl.c(x),new T(null,1,5,U,[a],null),null,null,null),b)}}(t,t,b,a,c,d,e,f,h,k,l,n,p),t)):et.b(p,lt(h))}():et.b(M.g(p,new T(null,1,5,U,[ij],null),I),lt(h));return fg.S(n,
	new T(null,1,5,U,[Ik],null),kd,t)}function Jt(b,a){return Ad(a)&&Pd(yk.c(b),r(Zs(D(a)))?hd(a):D(a))}var Kt=Bf?Bf(0):Af.call(null,0);
	function Lt(b,a){var c=L(b,0),d=ze(b,1),e=Ff.b(Kt,Jc),f=nd(yk.c(a),c);return function(a,b,c,d,e){return function x(f){return new Ne(null,function(a,b,c,d,e){return function(){for(;;){var h=q(f);if(h){var k=h;if(Ed(k)){var l=y(k),n=J(l),p=Re(n);return function(){for(var f=0;;)if(f<n){var x=w.b(l,f),z=x,va=L(z,0),Pa=L(va,0),$a=ze(va,1),Na=ze(z,1),Ja=ni($a,c);We(p,hn(function(a,b,c,d,e,f,h,k,l,n,p,x,z,B,F,K,N){return function(a){if(r($s(a))){var b=h.c?h.c(a):h.call(null,a);return r(b)?b:xc([u(Me(a)),
	u("__auto__"),u(N)].join(""))}return a}}(f,z,va,Pa,$a,Na,Ja,x,l,n,p,k,h,a,b,c,d,e),Na));f+=1}else return!0}()?Te(Xe(p),x(A(k))):Te(Xe(p),null)}var Pa=D(k),va=Pa,Na=L(va,0),eb=L(Na,0),Ja=ze(Na,1),$a=ze(va,1),jb=ni(Ja,c);return bd(hn(function(a,b,c,d,e,f,h,k,l,n,p,x,z){return function(a){if(r($s(a))){var b=f.c?f.c(a):f.call(null,a);return r(b)?b:xc([u(Me(a)),u("__auto__"),u(z)].join(""))}return a}}(va,Na,eb,Ja,$a,jb,Pa,k,h,a,b,c,d,e),$a),x(Ac(k)))}return null}}}(a,b,c,d,e),null,null)}}(b,c,d,e,f)(f)}
	function Mt(b,a){var c=Yf(function(a){var b=L(a,0);a=L(a,1);return G.b(b,a)},Mf.g(Eg,b,a));return new T(null,2,5,U,[Mf.b(D,c),Mf.b(hd,c)],null)}
	function Nt(b,a){var c=L(b,0),d=ze(b,1),e=nd(a,c);return function(a,b,c,d){return function p(e){return new Ne(null,function(a,b,c){return function(){for(;;){var a=q(e);if(a){if(Ed(a)){var b=y(a),d=J(b),f=Re(d);a:for(var h=0;;)if(h<d){var k=w.b(b,h),l=Mt(c,k),k=L(l,0),l=L(l,1),k=new T(null,1,5,U,[R.j(new T(null,1,5,U,[new C(null,"-differ?","-differ?",1465687357,null)],null),k,H([l],0))],null);f.add(k);h+=1}else{b=!0;break a}return b?Te(Xe(f),p(A(a))):Te(Xe(f),null)}f=D(a);b=Mt(c,f);f=L(b,0);b=L(b,
	1);return bd(new T(null,1,5,U,[R.j(new T(null,1,5,U,[new C(null,"-differ?","-differ?",1465687357,null)],null),f,H([b],0))],null),p(Ac(a)))}return null}}}(a,b,c,d),null,null)}}(b,c,d,e)(e)}function Ot(b){var a=Bf?Bf(I):Af.call(null,I);hn(function(a){return function(b){r($s.c?$s.c(b):$s.call(null,b))&&Ff.g(a,kd,b);return b}}(a),b);return Pc.c?Pc.c(a):Pc.call(null,a)}
	function Pt(b,a){var c=function(a){return function(b){b=L(b,0);L(b,0);b=ze(b,1);return rf(a,b)}}(ji(Ot(b)));return new T(null,2,5,U,[Xf(c,a),Yf(c,a)],null)}
	function Qt(b,a){for(var c=Xf($s,a),d=ni(c,bg()),e=function(){return function(a,b){return v(Rt,a,b)}}(c,d),f=function(a,b,c){return function(d){return sf(function(){return function(a){return xd(ij.c(a))}}(a,b,c),Ik.c(d))}}(c,d,e),h=Ya(Bc,new m(null,5,[zl,I,jk,b,ml,new T(null,1,5,U,[a],null),bj,Sd,Sk,Sd],null)),k=new Vs(d,I,null,null,null);;){var l=D(h);if(r(l)){var n=l,p=ui(function(){return function(a){return Fa(Jt(b,a))}}(h,k,n,l,c,d,e,f),ml.c(n)),t=L(p,0),x=L(p,1),z=L(x,0),B=ze(x,1);if(null==z)var F=
	e(jk.c(n),t),K=function(){var a=F;return St?St(a,c):Tt.call(null,a,c)}(),N=new Vs(d,K,null,null,null),K=E(h),N=dt(k,N);else{var W=z,xa=L(W,0),Ua=ze(W,1),gb=Nt(z,bj.c(n)),aa=Pt(R.b(zl.c(n),t),R.b(gb,Sk.c(n))),uc=L(aa,0),Pa=L(aa,1);if(r(sf(function(){return function(a){return G.b(a,new T(null,1,5,U,[He(new C(null,"-differ?","-differ?",1465687357,null))],null))}}(h,k,W,xa,Ua,gb,aa,uc,Pa,p,t,x,z,B,n,l,c,d,e,f),uc)))K=E(h);else{var va=R.b(t,uc),Na=e(jk.c(n),va);if(r(f(Na)))K=E(h);else var eb=M.g(bj.c(n),
	xa,kd.b(od(bj.c(n),xa,I),Ua)),Ja=Lt(z,b),K=R.b(function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,aa,xa,Ua,gb,va){return function Dd(Pa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N){return function(){for(;;){var a=q(Pa);if(a){if(Ed(a)){var b=y(a),d=J(b),h=Re(d);a:for(var k=0;;)if(k<d){var l=w.b(b,k),l=new m(null,5,[zl,e,jk,f,ml,Xs(H([l,N],0)),bj,c,Sk,x],null);h.add(l);k+=1}else{b=!0;break a}return b?Te(Xe(h),Dd(A(a))):Te(Xe(h),null)}h=D(a);return bd(new m(null,5,
	[zl,e,jk,f,ml,Xs(H([h,N],0)),bj,c,Sk,x],null),Dd(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,aa,xa,Ua,gb,va),null,null)}}(h,k,eb,Ja,va,Na,W,xa,Ua,gb,aa,uc,Pa,p,t,x,z,B,n,l,c,d,e,f)(Ja)}(),E(h))}N=k}h=K;k=N}else return k}}
	function Ut(b,a){if(b?r(r(null)?null:b.Oc)||(b.Eb?0:Ga(Hn,b)):Ga(Hn,b)){var c=L(a,0),d=L(a,1),e=L(a,2),f=L(a,3);return Gg(new T(null,4,5,U,[r(bt(c))?ho(b,c):c,d,r(function(){if(r(e)){var a=at(d);return r(a)?(a=go(b,d))?bt(e):a:a}return e}())?ho(b,e):e,r(bt(f))?ho(b,f):f],null),0,J(a))}return a}function Vt(b,a){var c=L(a,0),d=L(a,1),e=L(a,2),f=L(a,3),c=r($s(c))?kd.b(Ud,c):Ud,f=r($s(f))?kd.b(c,f):c,c=$s(e),d=r(c)?Fa($s(d))&&go(b,d):c;return r(d)?kd.b(f,e):f}
	function Rt(b,a){if(r(function(){var b=new T(null,1,5,U,[new T(null,2,5,U,[vc,new C(null,"*","*",345799209,null)],null)],null);return Ys.b?Ys.b(b,a):Ys.call(null,b,a)}()))return Ht(b,a);if(r(function(){var b=new T(null,2,5,U,[new T(null,2,5,U,[vc,new C(null,"*","*",345799209,null)],null),new C(null,"_","_",-1201019570,null)],null);return Ys.b?Ys.b(b,a):Ys.call(null,b,a)}()))return It(b,a);if(r(function(){var b=new T(null,1,5,U,[new C(null,"*","*",345799209,null)],null);return Ys.b?Ys.b(b,a):Ys.call(null,
	b,a)}())){var c=Bt(a),d=L(c,0),c=ze(c,1),d=nd(ek.c(b),d),c=Ut(d,c),e=Ct(d,c),f=d?r(r(null)?null:d.Oc)?!0:d.Eb?!1:Ga(Hn,d):Ga(Hn,d),h=qt,k=pt;qt=f?d:null;pt=f?Vt(d,c):null;try{return fg.S(b,new T(null,1,5,U,[Ik],null),Dt,e)}finally{pt=k,qt=h}}else throw Error([u("No matching clause: "),u(a)].join(""));}
	function Wt(b,a){if(r(Jt(b,a))){var c=r(Zs(D(a)))?new T(null,2,5,U,[D(a),E(a)],null):new T(null,2,5,U,[new C(null,"$","$",-1580747756,null),a],null),d=L(c,0),c=L(c,1),d=cg(b,new T(null,2,5,U,[ek,d],null)),d=Qt(M.g(b,ek,new m(null,1,[new C(null,"$","$",-1580747756,null),d],null)),c);return fg.S(b,new T(null,1,5,U,[Ik],null),Dt,d)}return Rt(b,a)}
	function Tt(){switch(arguments.length){case 2:return St(arguments[0],arguments[1]);case 3:return Xt(arguments[0],arguments[1],arguments[2]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}}function St(b,a){var c=Ik.c(b),d=Xt,e;e=J(a);e=Oa?Array(e):Ma.call(null,e);return d(new T(null,1,5,U,[e],null),c,a)}
	function Xt(b,a,c){for(;;){var d=D(a);if(r(d)){var e=d,f=ei(dl.c(e),c);if(xd(f))var h=b,k=E(a),l=c;else var n=Zd(Mf.b(function(a,b,c,d){return function(a){return nd(d,a)}}(b,a,c,f,e,d),c)),p=J(c),h=function(){return function(a,b,c,d,e,f,h,k){return function Ua(l){return new Ne(null,function(a,b,c,d,e,f,h,k){return function(){for(var n=l;;){var p=q(n);if(p){var t=p,x=D(t);if(p=q(function(a,b,c,d,e,f,h,k,l,n,p,t){return function Pj(x){return new Ne(null,function(a,b,c,d,e,f,h,k,l){return function(){for(;;){var a=
	q(x);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){for(var f=w.b(b,a),h=d,n=Qa(e),p=0;;)if(p<l){var t=k[p];r(t)&&(n[p]=f[t]);p+=1}else break;h.add(n);a+=1}else return!0}()?Te(Xe(d),Pj(A(a))):Te(Xe(d),null)}var f=D(a);return bd(function(){for(var a=Qa(e),b=0;;)if(b<l){var c=k[b];r(c)&&(a[b]=f[c]);b+=1}else break;return a}(),Pj(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t),null,null)}}(n,a,b,c,x,t,p,d,e,f,h,k)(ij.c(h))))return R.b(p,Ua(Ac(n)));n=Ac(n)}else return null}}}(a,
	b,c,d,e,f,h,k),null,null)}}(b,a,c,n,p,f,e,d)(b)}(),k=E(a),l=c;b=h;a=k;c=l}else return b}}function Yt(b,a){return ji(Mf.b(ce,St(b,a)))}var Zt=function Zt(a,c){if(a?a.mc:a)return a.mc(a,c);var d;d=Zt[ca(null==a?null:a)];if(!d&&(d=Zt._,!d))throw Ia("IContextResolve.-context-resolve",a);return d.call(null,a,c)};Ro.prototype.mc=function(b,a){return Et(a,this.symbol)};So.prototype.mc=function(b,a){return cg(a,new T(null,2,5,U,[ek,this.symbol],null))};Wo.prototype.mc=function(){return nd(it,this.symbol)};
	Vo.prototype.mc=function(){return this.value};function $t(b,a,c){return ag(function(b,e,f){if(r(up(b))){e=Zt(yj.c(b),a);b=Mf.b(function(){return function(b){return Zt(b,a)}}(e),mi(cj.c(b)));var h=Mf.b(function(){return function(a){return md(a,f)}}(e,b),c);return kf(e,R.b(b,new T(null,1,5,U,[h],null)))}return e},b,D(c))}function au(b){var a=vf(up);return Yf(Da,Mf.g(function(b,d){return r(a.c?a.c(b):a.call(null,b))?d:null},b,bg()))}
	function bu(b,a,c){var d=au(b),e=function(a){return function(b){return Mf.b(function(){return function(a){return md(b,a)}}(a),a)}}(d);c=Xi(e,c);return function(c,d,e){return function n(p){return new Ne(null,function(){return function(){for(;;){var c=q(p);if(c){if(Ed(c)){var d=y(c),e=J(d),f=Re(e);a:for(var h=0;;)if(h<e){var k=w.b(d,h);L(k,0);k=L(k,1);k=$t(b,a,k);f.add(k);h+=1}else{d=!0;break a}return d?Te(Xe(f),n(A(c))):Te(Xe(f),null)}f=D(c);L(f,0);f=L(f,1);return bd($t(b,a,f),n(Ac(c)))}return null}}}(c,
	d,e),null,null)}}(d,e,c)(c)}var cu=function cu(a,c){if(a?a.nc:a)return a.nc(a,c);var d;d=cu[ca(null==a?null:a)];if(!d&&(d=cu._,!d))throw Ia("IPostProcess.-post-process",a);return d.call(null,a,c)};qp.prototype.nc=function(b,a){return a};rp.prototype.nc=function(b,a){return S.g(I,Mf.c(D),a)};sp.prototype.nc=function(b,a){return id(a)};tp.prototype.nc=function(b,a){return D(a)};
	function du(b,a,c){return function(a){return function f(b){return new Ne(null,function(a){return function(){for(;;){var c=q(b);if(c){var d=c;if(Ed(d)){var p=y(d),t=J(p),x=Re(t);return function(){for(var b=0;;)if(b<t){var f=w.b(p,b);We(x,$f(function(){return function(a,b){if(r(a)){var c=L(a,0),d=L(a,1);return Oq(c,d,new T(null,1,5,U,[b],null),!1)}return b}}(b,f,p,t,x,d,c,a),a,f));b+=1}else return!0}()?Te(Xe(x),f(A(d))):Te(Xe(x),null)}var z=D(d);return bd($f(function(){return function(a,b){if(r(a)){var c=
	L(a,0),d=L(a,1);return Oq(c,d,new T(null,1,5,U,[b],null),!1)}return b}}(z,d,c,a),a,z),f(Ac(d)))}return null}}}(a),null,null)}}(function(){return function e(b){return new Ne(null,function(){for(;;){var c=q(b);if(c){if(Ed(c)){var k=y(c),l=J(k),n=Re(l);a:for(var p=0;;)if(p<l){var t=w.b(k,p),t=r(vp(t))?new T(null,2,5,U,[Zt(sk.c(t),a),sq(Zt(am.c(t),a))],null):null;n.add(t);p+=1}else{k=!0;break a}return k?Te(Xe(n),e(A(c))):Te(Xe(n),null)}n=D(c);return bd(r(vp(n))?new T(null,2,5,U,[Zt(sk.c(n),a),sq(Zt(am.c(n),
	a))],null):null,e(Ac(c)))}return null}},null,null)}(b)}())(c)}var eu=Hf(Sp(Sd,function(){a:for(var b=q(0<arguments.length?new za(Array.prototype.slice.call(arguments,0),0):null),a=$h;;)if(b)var c=E(E(b)),a=M.g(a,D(b),hd(b)),b=c;else break a;return a}(),Sd,0,100));
	function fu(b){var a=od(Pc.c?Pc.c(eu):Pc.call(null,eu),b,null);if(r(a))return a;if(Bd(b))a=b;else if(Ad(b))a=Op(b);else throw $i(""+u("Query should be a vector or a map"),new m(null,2,[hl,cm,nl,b],null));var c=a,a=xp(ej.c(c)),d=Rk.c(c);if(r(d)){var e=Ko(Yo,d);if(r(e))d=e;else throw $i(""+u("Cannot parse :with clause, expected [ variable+ ]"),new m(null,2,[hl,Nk,nl,d],null));}else d=null;var e=Zl.b(c,new T(null,1,5,U,[new C(null,"$","$",-1580747756,null)],null)),f=Ko(yp,e);if(!r(f))throw $i(""+u("Cannot parse :in clause, expected (src-var | % | bind-scalar | bind-tuple | bind-coll | bind-rel)"),
	new m(null,2,[hl,dj,nl,e],null));e=f;c=wj.b(c,I);f=Ko(Lf,c);if(!r(f))throw $i(""+u("Cannot parse :where clause, expected [clause+]"),new m(null,2,[hl,Lj,nl,c],null));a=new m(null,4,[ej,a,Rk,d,Zl,e,wj,f],null);a=new Np(ej.c(a),Rk.c(a),Zl.c(a),wj.c(a),null,O.j(a,ej,H([Rk,Zl,wj],0)),null);Pp(a,b);ec(eu,M.g(vb(eu),b,a));return a}var gu=function gu(){return gu.j(arguments[0],1<arguments.length?new za(Array.prototype.slice.call(arguments,1),0):null)};
	gu.j=function(b,a){var c=fu(b),d=ej.c(c),e=pp(d),f=Vf(mp,H([pp(d)],0)),h=J(e),k=Rk.c(c),l=R.b(f,Mf.b(ck,k)),n=Ad(b)?Op(b):b,p=wj.c(n),t=ot(new Us(I,Sd,Sd,null,null,null),Zl.c(c),a),x=Yt(v(Wt,t,p),l),c=r(Rk.c(n))?Zf(function(a,b,c,d,e,f){return function(a){return ce(Gg(a,0,f))}}(x,c,d,e,f,h,k,l,n,p,t,x),x):x,c=r(sf(up,e))?bu(e,t,c):c,e=r(sf(vp,e))?du(e,t,c):c;return cu(d,e)};gu.H=1;gu.F=function(b){var a=D(b);b=E(b);return gu.j(a,b)};function hu(b){return b instanceof Un}function iu(b,a,c){if(r(hu(b)))throw $i("Filtered DB cannot be modified",new m(null,1,[hl,tk],null));b=new m(null,5,[Gk,b,Oj,b,Hk,I,fj,Sd,tl,c],null);b=new io(Gk.c(b),Oj.c(b),Hk.c(b),fj.c(b),tl.c(b),null,O.j(b,Gk,H([Oj,Hk,fj,tl],0)),null);return Bo(b,a)}function ju(b,a){return Oj.c(iu(b,a,null))}
	var ku=function ku(){switch(arguments.length){case 2:return ku.b(arguments[0],arguments[1]);case 3:return ku.g(arguments[0],arguments[1],arguments[2]);case 4:return ku.S(arguments[0],arguments[1],arguments[2],arguments[3]);case 5:return ku.Z(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4]);case 6:return ku.wa(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4],arguments[5]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}};
	ku.b=function(b,a){return En(b,a,I)};ku.g=function(b,a,c){return En(b,a,new T(null,1,5,U,[c],null))};ku.S=function(b,a,c,d){return En(b,a,new T(null,2,5,U,[c,d],null))};ku.Z=function(b,a,c,d,e){return En(b,a,new T(null,3,5,U,[c,d,e],null))};ku.wa=function(b,a,c,d,e,f){return En(b,a,new T(null,4,5,U,[c,d,e,f],null))};ku.H=6;
	var lu=function lu(){switch(arguments.length){case 2:return lu.b(arguments[0],arguments[1]);case 3:return lu.g(arguments[0],arguments[1],arguments[2]);case 4:return lu.S(arguments[0],arguments[1],arguments[2],arguments[3]);case 5:return lu.Z(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4]);case 6:return lu.wa(arguments[0],arguments[1],arguments[2],arguments[3],arguments[4],arguments[5]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}};
	lu.b=function(b,a){return Fn(b,a,I)};lu.g=function(b,a,c){return Fn(b,a,new T(null,1,5,U,[c],null))};lu.S=function(b,a,c,d){return Fn(b,a,new T(null,2,5,U,[c,d],null))};lu.Z=function(b,a,c,d,e){return Fn(b,a,new T(null,3,5,U,[c,d,e],null))};lu.wa=function(b,a,c,d,e,f){return Fn(b,a,new T(null,4,5,U,[c,d,e,f],null))};lu.H=6;
	function mu(b,a,c){var d=Bf?Bf(null):Af.call(null,null);Ff.b(b,function(b){return function(d){d=iu(d,a,c);Ef.b?Ef.b(b,d):Ef.call(null,b,d);return Oj.c(d)}}(d));return Pc.c?Pc.c(d):Pc.call(null,d)}var nu=function nu(){switch(arguments.length){case 2:return nu.b(arguments[0],arguments[1]);case 3:return nu.g(arguments[0],arguments[1],arguments[2]);default:throw Error([u("Invalid arity: "),u(arguments.length)].join(""));}};nu.b=function(b,a){return nu.g(b,1*Math.random(),a)};
	nu.g=function(b,a,c){Ff.S(Bl.c(vd(b)),M,a,c);return a};nu.H=3;
	for(var ou=q(new m(null,2,[new C("datascript","Datom","datascript/Datom",-901340080,null),function(b){return kf(qn,b)},new C("datascript","DB","datascript/DB",-487332776,null),function(b){var a=Id(b)?kf(Cf,b):b,c=nd(a,gj),d=nd(a,Xl);return eo(Mf.b(function(){return function(a){var b=L(a,0),c=L(a,1),d=L(a,2);a=L(a,3);return new kn(b,c,d,a,!0)}}(b,a,c,d),d),c)}],null)),pu=null,qu=0,ru=0;;)if(ru<qu){var su=pu.M(null,ru),tu=L(su,0),uu=L(su,1);Fs(tu,uu);ru+=1}else{var vu=q(ou);if(vu){var wu=vu;if(Ed(wu))var xu=
	y(wu),yu=A(wu),zu=xu,Au=J(xu),ou=yu,pu=zu,qu=Au;else{var Bu=D(wu),Cu=L(Bu,0),Du=L(Bu,1);Fs(Cu,Du);ou=E(wu);pu=null;qu=0}ru=0}else break}Bf||Af.call(null,-1E6);function Eu(b,a){var c=b.toString(16),d=J(c);return d>a?c.substring(0,a):d<a?[u(kf(u,Sf(a-d,"0"))),u(c)].join(""):c};function Fu(b){return"string"===typeof b&&G.b(b.substring(0,1),":")?Le.c(b.substring(1)):b}
	function Gu(b){if(Bd(b))return M.g(O.b(b,":db/id"),bl,b.c?b.c(":db/id"):b.call(null,":db/id"));if(G.b(D(b),":db.fn/call")){var a=L(b,0),c=L(b,1),d=ze(b,2);return R.b(new T(null,2,5,U,[vk,function(a,b,c){return function(){function a(c){var d=null;if(0<arguments.length){for(var d=0,e=Array(arguments.length-0);d<e.length;)e[d]=arguments[d+0],++d;d=new za(e,0)}return b.call(this,d)}function b(a){a=kf(c,a);return Hu.c?Hu.c(a):Hu.call(null,a)}a.H=0;a.F=function(a){a=q(a);return b(a)};a.j=b;return a}()}(b,
	a,c,d)],null),d)}return Ad(b)?(a=L(b,0),b=ze(b,1),R.b(new T(null,1,5,U,[Fu(a)],null),b)):null}function Hu(b){return Mf.b(Gu,Ti(b))}function Iu(b){return Ea(b)?new kn(b[0],b[1],b[2],function(){var a=b[3];return r(a)?a:536870912}(),function(){var a=b[4];return r(a)?a:!0}()):new kn(b.e,b.a,b.v,function(){var a=b.tx;return r(a)?a:536870912}(),function(){var a=b.added;return r(a)?a:!0}())}function Ju(b){return Qi(hn(function(a){return a instanceof P?""+u(a):a},b))}
	var Ku=function Ku(){return Ku.j(0<arguments.length?new za(Array.prototype.slice.call(arguments,0),0):null)};fa("datascript.js.empty_db",Ku);Ku.j=function(b){b=L(b,0);b=hn(Fu,Ti(b));return Pn?Pn(b):Qn.call(null,b)};Ku.H=0;Ku.F=function(b){return Ku.j(q(b))};var Lu=function Lu(){return Lu.j(arguments[0],1<arguments.length?new za(Array.prototype.slice.call(arguments,1),0):null)};fa("datascript.js.init_db",Lu);
	Lu.j=function(b,a){var c=L(a,0),d=Mf.b(Iu,b),c=hn(Fu,Ti(c));return eo?eo(d,c):bo.call(null,d,c)};Lu.H=1;Lu.F=function(b){var a=D(b);b=E(b);return Lu.j(a,b)};var Mu=function Mu(){return Mu.j(arguments[0],1<arguments.length?new za(Array.prototype.slice.call(arguments,1),0):null)};fa("datascript.js.q",Mu);Mu.j=function(b,a){var c=vr(new Tq(b,[],-1),!1,null),c=lf(gu,c,a);return Qi(c)};Mu.H=1;Mu.F=function(b){var a=D(b);b=E(b);return Mu.j(a,b)};
	fa("datascript.js.pull",function(b,a,c){a=vr(new Tq(a,[],-1),!1,null);b=Pq.g?Pq.g(b,a,c):Pq.call(null,b,a,c);return Ju(b)});fa("datascript.js.pull_many",function(b,a,c){a=vr(new Tq(a,[],-1),!1,null);b=Qq.g?Qq.g(b,a,c):Qq.call(null,b,a,c);return Ju(b)});fa("datascript.js.db_with",function(b,a){return ju(b,Hu(a))});fa("datascript.js.entity",Gs);fa("datascript.js.touch",Ms);fa("datascript.js.entity_db",function(b){return b.db});
	fa("datascript.js.filter",function(b,a){if(r(hu(b))){var c=b.Ea;return new Un(c,function(b,c){return function(f){var h=a.b?a.b(c,f):a.call(null,c,f);return r(h)?b.Na.call(null,f):h}}(b,c),null,null,null)}return new Un(b,function(c){return a.b?a.b(b,c):a.call(null,b,c)},null,null,null)});fa("datascript.js.is_filtered",hu);var Nu=function Nu(){return Nu.j(0<arguments.length?new za(Array.prototype.slice.call(arguments,0),0):null)};fa("datascript.js.create_conn",Nu);
	Nu.j=function(b){b=L(b,0);b=hn(Fu,Ti(b));b=Pn?Pn(b):Qn.call(null,b);var a=new m(null,1,[Bl,Bf?Bf(Sd):Af.call(null,Sd)],null);return Af.g?Af.g(b,ta,a):Af.call(null,b,ta,a)};Nu.H=0;Nu.F=function(b){return Nu.j(q(b))};fa("datascript.js.db",function(b){return Pc.c?Pc.c(b):Pc.call(null,b)});var Uu=function Uu(){return Uu.j(arguments[0],arguments[1],2<arguments.length?new za(Array.prototype.slice.call(arguments,2),0):null)};fa("datascript.js.transact",Uu);
	Uu.j=function(b,a,c){c=L(c,0);a=Hu(a);a=mu(b,a,c);c=Gk.c(a);for(var d=Oj.c(a),e=Aa(Hk.c(a)),f=fj.c(a),h={},f=q(f),k=null,l=0,n=0;;)if(n<l){var p=k.M(null,n),t=L(p,0),p=L(p,1);h[""+u(t)]=p;n+=1}else if(f=q(f))Ed(f)?(l=y(f),f=A(f),k=l,l=J(l)):(l=D(f),k=L(l,0),l=L(l,1),h[""+u(k)]=l,f=E(f),k=null,l=0),n=0;else break;a={db_before:c,db_after:d,tx_data:e,tempids:h,tx_meta:tl.c(a)};c=q;b=Bl.c(vd(b));b=Pc.c?Pc.c(b):Pc.call(null,b);b=c(b);c=null;for(e=d=0;;)if(e<d)h=c.M(null,e),L(h,0),h=L(h,1),f=a,h.c?h.c(f):
	h.call(null,f),e+=1;else if(b=q(b))Ed(b)?(d=y(b),b=A(b),c=d,d=J(d)):(c=D(b),L(c,0),c=L(c,1),d=a,c.c?c.c(d):c.call(null,d),b=E(b),c=null,d=0),e=0;else break;return a};Uu.H=2;Uu.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return Uu.j(a,b,c)};fa("datascript.js.listen",nu);fa("datascript.js.unlisten",function(b,a){return Ff.g(Bl.c(vd(b)),O,a)});fa("datascript.js.resolve_tempid",function(b,a){return b[""+u(a)]});
	var Vu=function Vu(){return Vu.j(arguments[0],arguments[1],2<arguments.length?new za(Array.prototype.slice.call(arguments,2),0):null)};fa("datascript.js.datoms",Vu);Vu.j=function(b,a,c){return Aa(mf(ku,b,Fu(a),c))};Vu.H=2;Vu.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return Vu.j(a,b,c)};var Wu=function Wu(){return Wu.j(arguments[0],arguments[1],2<arguments.length?new za(Array.prototype.slice.call(arguments,2),0):null)};fa("datascript.js.seek_datoms",Wu);
	Wu.j=function(b,a,c){return Aa(mf(lu,b,Fu(a),c))};Wu.H=2;Wu.F=function(b){var a=D(b),c=E(b);b=D(c);c=E(c);return Wu.j(a,b,c)};fa("datascript.js.index_range",function(b,a,c,d){return Aa(Gn.S?Gn.S(b,a,c,d):Gn.call(null,b,a,c,d))});
	fa("datascript.js.squuid",function(){var b=u,a=[u(Eu((new Date).getTime()/1E3|0,8)),u("-"),u(Eu(Vi(65536),4)),u("-"),u(Eu(Vi(65536)&4095|16384,4)),u("-"),u(Eu(Vi(65536)&16383|32768,4)),u("-"),u(Eu(Vi(65536),4)),u(Eu(Vi(65536),4)),u(Eu(Vi(65536),4))].join("");return""+b(new Yi(a,null))});fa("datascript.js.squuid_time_millis",function(b){b=""+u(new Yi(b,null));return 1E3*parseInt(b.substring(0,8),16)});ya();function Xu(b){var a=Yd(Vd,b),c=J(b)-1;b=.5*J(b)|0;return md(a,c<b?c:b)}function Yu(b){b=""+u(1<b?b.toFixed(1):.1<b?b.toFixed(2):.001<b?b.toFixed(4):1E-6<b?b.toFixed(7):b);return 6>=J(b)?[u(kf(u,Sf(6-J(b)," "))),u(b)].join(""):b}function Z(){return Date.now()}function Zu(){return new Date};ya();var $u=Hf(0),av=new m(null,1,[ok,new m(null,2,[bk,hk,$k,ul],null)],null),bv=Tf(function(){return new m(null,6,[bl,ec($u,vb($u)+1),fk,Wi(new T(null,8,5,U,"Ivan Petr Sergei Oleg Yuri Dmitry Fedor Denis".split(" "),null)),rj,Wi(new T(null,6,5,U,"Ivanov Petrov Sidorov Kovalev Kuznetsov Voronoi".split(" "),null)),Ej,Wi(new T(null,2,5,U,[Nj,hj],null)),Dj,Vi(10),bm,Vi(1E5)],null)}),cv;cv=wf.b(Mf.c(function(b){return ge(function(a,c,d){return kd.b(a,qn.g(bl.c(b),c,d))},I,O.b(b,bl))}),Mi);
	function dv(b,a){return ju(Pn?Pn(av):Qn.call(null,av),ev(1,b,a))}function ev(b,a,c){if(0<a){var d=Mf.b(function(a){return b*c+a},ti(c));return R.b(Mf.b(function(){return function(a){return new m(null,3,[ll,b,fk,"Ivan",ok,a],null)}}(d),d),Vf(function(){return function(b){return ev(b,a-1,c)}}(d),H([d],0)))}return new T(null,1,5,U,[new m(null,2,[bl,b,fk,"Ivan"],null)],null)}
	function fv(b,a){return ju(Pn?Pn(av):Qn.call(null,av),kf(R,function(){return function d(a){return new Ne(null,function(){for(var f=a;;){var h=q(f);if(h){var k=h,l=D(k);if(h=q(function(a,d,e,f){return function B(h){return new Ne(null,function(a,d){return function(){for(;;){var a=q(h);if(a){if(Ed(a)){var e=y(a),f=J(e),k=Re(f);a:for(var l=0;;)if(l<f){var n=w.b(e,l),p=d*(b+1)+n+1;k.add(new T(null,2,5,U,[new m(null,3,[bl,d*(b+1)+n,fk,"Ivan",ok,p],null),new m(null,2,[bl,p,fk,"Ivan"],null)],null));l+=1}else{e=
	!0;break a}return e?Te(Xe(k),B(A(a))):Te(Xe(k),null)}k=D(a);e=d*(b+1)+k+1;return bd(new T(null,2,5,U,[new m(null,3,[bl,d*(b+1)+k,fk,"Ivan",ok,e],null),new m(null,2,[bl,e,fk,"Ivan"],null)],null),B(Ac(a)))}return null}}}(a,d,e,f),null,null)}}(f,l,k,h)(ti(b))))return R.b(h,d(Ac(f)));f=Ac(f)}else return null}},null,null)}(ti(a))}()))}
	function gv(){for(var b=q(new T(null,3,5,U,[100,500,2E3],null)),a=null,c=0,d=0;;)if(d<c){for(var e=a.M(null,d),f=q(new T(null,1,5,U,[1],null)),h=null,k=0,l=0;;)if(l<k){var n=h.M(null,l),p=pi(n,n,Of(e,bv)),t=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[Il,"db_with",Ak,e,Dk,n],null))].join("")],0)),x=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)v(ju,ao?ao():Qn.call(null),p),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),z=S.b(I,function(){return function(a,b,c,d,e,f,h,k,
	l,n,p,t,x){return function Ve(z){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p){return function(){for(;;){var a=q(z);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)v(ju,ao?ao():Qn.call(null),p),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),Ve(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>
	d)v(ju,ao?ao():Qn.call(null),p),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Ve(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x),null,null)}}(f,h,k,l,b,a,c,d,t,x,p,n,e)(ti(5))}()),B=de(se,z),F=Xu(z),K=de(re,z);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[Il,"db_with",Ak,e,Dk,n],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,F,mj,B,Uk,K,Tj,z],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(B),"] [ med:",Yu(F),
	"] [ max:",Yu(K),"] ms"],0));l+=1}else{var N=q(f);if(N){var W=N;if(Ed(W))F=y(W),z=A(W),B=F,F=J(F),f=z,h=B,k=F;else{var xa=D(W),Ua=pi(xa,xa,Of(e,bv)),gb=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[Il,"db_with",Ak,e,Dk,xa],null))].join("")],0)),aa=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)v(ju,ao?ao():Qn.call(null),Ua),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),z=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B){return function ch(F){return new Ne(null,
	function(a,b,c,d,e,f,h,k,l,n,p){return function(){for(;;){var a=q(F);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)v(ju,ao?ao():Qn.call(null),p),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),ch(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)v(ju,ao?ao():Qn.call(null),p),d+=1;else break;
	d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),ch(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B),null,null)}}(f,h,k,l,b,a,c,d,gb,aa,Ua,xa,W,N,e)(ti(5))}()),B=de(se,z),F=Xu(z),K=de(re,z);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[Il,"db_with",Ak,e,Dk,xa],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,F,mj,B,Uk,K,Tj,z],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(B),"] [ med:",Yu(F),"] [ max:",Yu(K),"] ms"],0));f=E(W);
	h=null;k=0}l=0}else break}d+=1}else{var uc=q(b);if(uc){var Pa=uc;if(Ed(Pa))F=y(Pa),z=A(Pa),B=F,F=J(F),b=z,a=B,c=F;else{for(var e=D(Pa),va=q(new T(null,1,5,U,[1],null)),Na=null,eb=0,Ja=0;;)if(Ja<eb){var $a=Na.M(null,Ja),jb=pi($a,$a,Of(e,bv)),Bb=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[Il,"db_with",Ak,e,Dk,$a],null))].join("")],0)),Gb=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)v(ju,ao?ao():Qn.call(null),jb),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),z=S.b(I,function(){return function(a,
	b,c,d,e,f,h,k,l,n,p,t,x,z,B){return function ch(F){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p){return function(){for(;;){var a=q(F);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)v(ju,ao?ao():Qn.call(null),p),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),ch(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=
	10;;){for(var d=0;;)if(10>d)v(ju,ao?ao():Qn.call(null),p),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),ch(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B),null,null)}}(va,Na,eb,Ja,b,a,c,d,Bb,Gb,jb,$a,e,Pa,uc)(ti(5))}()),B=de(se,z),F=Xu(z),K=de(re,z);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[Il,"db_with",Ak,e,Dk,$a],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,F,mj,B,Uk,K,Tj,z],null)],0)),
	"}"],0)):Ki(H(["[ min:",Yu(B),"] [ med:",Yu(F),"] [ max:",Yu(K),"] ms"],0));Ja+=1}else{var Hb=q(va);if(Hb){var bc=Hb;if(Ed(bc))F=y(bc),z=A(bc),B=F,F=J(F),va=z,Na=B,eb=F;else{var Tb=D(bc),pc=pi(Tb,Tb,Of(e,bv)),lc=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[Il,"db_with",Ak,e,Dk,Tb],null))].join("")],0)),zc=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)v(ju,ao?ao():Qn.call(null),pc),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),z=S.b(I,function(){return function(a,b,c,d,
	e,f,h,k,l,n,p,t,x,z,B,F,K){return function dh(N){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p){return function(){for(;;){var a=q(N);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)v(ju,ao?ao():Qn.call(null),p),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),dh(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=
	0;;)if(10>d)v(ju,ao?ao():Qn.call(null),p),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),dh(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K),null,null)}}(va,Na,eb,Ja,b,a,c,d,lc,zc,pc,Tb,bc,Hb,e,Pa,uc)(ti(5))}()),B=de(se,z),F=Xu(z),K=de(re,z);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[Il,"db_with",Ak,e,Dk,Tb],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,F,mj,B,Uk,K,Tj,z],null)],0)),"}"],0)):
	Ki(H(["[ min:",Yu(B),"] [ med:",Yu(F),"] [ max:",Yu(K),"] ms"],0));va=E(bc);Na=null;eb=0}Ja=0}else break}b=E(Pa);a=null;c=0}d=0}else return null}}fa("datascript.bench.bench_db_with",gv);
	function hv(){for(var b=q(new T(null,5,5,U,[100,500,2E3,5E3,2E4],null)),a=null,c=0,d=0;;)if(d<c){var e=a.M(null,d),f=S.g(I,wf.b(cv,Nf(e)),bv),h=r(null)?null:Ki(H([[u("\n"),u(new m(null,2,[Il,"init_db",Ak,e],null))].join("")],0)),k=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){var e=f;co?co(e):bo.call(null,e);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),l=S.b(I,function(){return function(a,b,c,d,e,f,h,k){return function Na(l){return new Ne(null,function(a,b,c,d,e,f,
	h){return function(){for(;;){var a=q(l);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var k=1E3+f,l=10;;){for(var n=0;;)if(10>n){var p=h;co?co(p):bo.call(null,p);n+=1}else break;n=Z();if(n<k)l=10+l;else{f=(n-f)/l;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),Na(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){var e=h;co?co(e):bo.call(null,e);d+=1}else break;d=Z();if(d<b)c=10+
	c;else return(d-a)/c}}(),Na(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k),null,null)}}(b,a,c,d,h,k,f,e)(ti(5))}()),n=de(se,l),p=Xu(l),t=de(re,l);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,2,[Il,"init_db",Ak,e],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,p,mj,n,Uk,t,Tj,l],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(n),"] [ med:",Yu(p),"] [ max:",Yu(t),"] ms"],0));d+=1}else{var x=q(b);if(x){var z=x;if(Ed(z))p=y(z),l=A(z),n=
	p,p=J(p),b=l,a=n,c=p;else{var e=D(z),B=S.g(I,wf.b(cv,Nf(e)),bv),F=r(null)?null:Ki(H([[u("\n"),u(new m(null,2,[Il,"init_db",Ak,e],null))].join("")],0)),K=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){var e=B;co?co(e):bo.call(null,e);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),l=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n){return function Ja(p){return new Ne(null,function(a,b,c,d,e,f,h){return function(){for(;;){var a=q(p);if(a){if(Ed(a)){var b=y(a),c=J(b),
	d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var k=1E3+f,l=10;;){for(var n=0;;)if(10>n){var p=h;co?co(p):bo.call(null,p);n+=1}else break;n=Z();if(n<k)l=10+l;else{f=(n-f)/l;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),Ja(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){var e=h;co?co(e):bo.call(null,e);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Ja(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n),null,
	null)}}(b,a,c,d,F,K,B,e,z,x)(ti(5))}()),n=de(se,l),p=Xu(l),t=de(re,l);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,2,[Il,"init_db",Ak,e],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,p,mj,n,Uk,t,Tj,l],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(n),"] [ med:",Yu(p),"] [ max:",Yu(t),"] ms"],0));b=E(z);a=null;c=0}d=0}else return null}}fa("datascript.bench.bench_init_db",hv);
	function iv(){for(var b=q(new T(null,4,5,U,[new T(null,2,5,U,["q1",new T(null,4,5,U,[ej,new C(null,"?e","?e",-1194391683,null),wj,new T(null,3,5,U,[new C(null,"?e","?e",-1194391683,null),fk,"Ivan"],null)],null)],null),new T(null,2,5,U,["q2",new T(null,6,5,U,[ej,new C(null,"?e","?e",-1194391683,null),new C(null,"?a","?a",1314302913,null),wj,new T(null,3,5,U,[new C(null,"?e","?e",-1194391683,null),fk,"Ivan"],null),new T(null,3,5,U,[new C(null,"?e","?e",-1194391683,null),Dj,new C(null,"?a","?a",1314302913,
	null)],null)],null)],null),new T(null,2,5,U,["q3",new T(null,7,5,U,[ej,new C(null,"?e","?e",-1194391683,null),new C(null,"?a","?a",1314302913,null),wj,new T(null,3,5,U,[new C(null,"?e","?e",-1194391683,null),fk,"Ivan"],null),new T(null,3,5,U,[new C(null,"?e","?e",-1194391683,null),Dj,new C(null,"?a","?a",1314302913,null)],null),new T(null,3,5,U,[new C(null,"?e","?e",-1194391683,null),Ej,Nj],null)],null)],null),new T(null,2,5,U,["q4",new T(null,9,5,U,[ej,new C(null,"?e","?e",-1194391683,null),new C(null,
	"?l","?l",550795544,null),new C(null,"?a","?a",1314302913,null),wj,new T(null,3,5,U,[new C(null,"?e","?e",-1194391683,null),fk,"Ivan"],null),new T(null,3,5,U,[new C(null,"?e","?e",-1194391683,null),rj,new C(null,"?l","?l",550795544,null)],null),new T(null,3,5,U,[new C(null,"?e","?e",-1194391683,null),Dj,new C(null,"?a","?a",1314302913,null)],null),new T(null,3,5,U,[new C(null,"?e","?e",-1194391683,null),Ej,Nj],null)],null)],null)],null)),a=null,c=0,d=0;;)if(d<c){for(var e=a.M(null,d),f=L(e,0),h=L(e,
	1),k=q(new T(null,4,5,U,[100,500,2E3,2E4],null)),l=null,n=0,p=0;;)if(p<n){var t=l.M(null,p),x=ju(ao?ao():Qn.call(null),Of(t,bv)),z=r(null)?null:Ki(H([[u("\n"),u(new m(null,2,[Il,f,Ak,t],null))].join("")],0)),B=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){var e=h,f=x;gu.b?gu.b(e,f):gu.call(null,e,f);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),F=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B){return function Qj(F){return new Ne(null,function(a,b,c,d,
	e,f,h,k,l,n,p,t,x,z,B){return function(){for(;;){var a=q(F);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l){var n=B,t=p;gu.b?gu.b(n,t):gu.call(null,n,t);l+=1}else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),Qj(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){var e=B,f=p;gu.b?gu.b(e,f):gu.call(null,
	e,f);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Qj(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B),null,null)}}(k,l,n,p,b,a,c,d,z,B,x,t,e,f,h)(ti(5))}()),K=de(se,F),N=Xu(F),W=de(re,F);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,2,[Il,f,Ak,t],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,N,mj,K,Uk,W,Tj,F],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(K),"] [ med:",Yu(N),"] [ max:",Yu(W),"] ms"],0));p+=
	1}else{var xa=q(k);if(xa){var Ua=xa;if(Ed(Ua))N=y(Ua),F=A(Ua),K=N,N=J(N),k=F,l=K,n=N;else{var gb=D(Ua),aa=ju(ao?ao():Qn.call(null),Of(gb,bv)),uc=r(null)?null:Ki(H([[u("\n"),u(new m(null,2,[Il,f,Ak,gb],null))].join("")],0)),Pa=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){var e=h,f=aa;gu.b?gu.b(e,f):gu.call(null,e,f);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),F=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K){return function lg(N){return new Ne(null,
	function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K){return function(){for(;;){var a=q(N);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l){var n=K,t=p;gu.b?gu.b(n,t):gu.call(null,n,t);l+=1}else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),lg(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){var e=K,f=p;gu.b?
	gu.b(e,f):gu.call(null,e,f);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),lg(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K),null,null)}}(k,l,n,p,b,a,c,d,uc,Pa,aa,gb,Ua,xa,e,f,h)(ti(5))}()),K=de(se,F),N=Xu(F),W=de(re,F);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,2,[Il,f,Ak,gb],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,N,mj,K,Uk,W,Tj,F],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(K),"] [ med:",
	Yu(N),"] [ max:",Yu(W),"] ms"],0));k=E(Ua);l=null;n=0}p=0}else break}d+=1}else{var va=q(b);if(va){var Na=va;if(Ed(Na))N=y(Na),F=A(Na),K=N,N=J(N),b=F,a=K,c=N;else{for(var eb=D(Na),f=L(eb,0),h=L(eb,1),Ja=q(new T(null,4,5,U,[100,500,2E3,2E4],null)),$a=null,jb=0,Bb=0;;)if(Bb<jb){var Gb=$a.M(null,Bb),Hb=ju(ao?ao():Qn.call(null),Of(Gb,bv)),bc=r(null)?null:Ki(H([[u("\n"),u(new m(null,2,[Il,f,Ak,Gb],null))].join("")],0)),Tb=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){var e=h,f=Hb;gu.b?
	gu.b(e,f):gu.call(null,e,f);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),F=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K){return function lg(N){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B){return function(){for(;;){var a=q(N);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l){var n=B,t=p;gu.b?gu.b(n,t):gu.call(null,n,t);l+=1}else break;l=Z();if(l<
	h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),lg(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){var e=B,f=p;gu.b?gu.b(e,f):gu.call(null,e,f);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),lg(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K),null,null)}}(Ja,$a,jb,Bb,b,a,c,d,bc,Tb,Hb,Gb,eb,f,h,Na,va)(ti(5))}()),K=de(se,F),N=Xu(F),W=de(re,F);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,
	2,[Il,f,Ak,Gb],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,N,mj,K,Uk,W,Tj,F],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(K),"] [ med:",Yu(N),"] [ max:",Yu(W),"] ms"],0));Bb+=1}else{var pc=q(Ja);if(pc){var lc=pc;if(Ed(lc))N=y(lc),F=A(lc),K=N,N=J(N),Ja=F,$a=K,jb=N;else{var zc=D(lc),ee=ju(ao?ao():Qn.call(null),Of(zc,bv)),ff=r(null)?null:Ki(H([[u("\n"),u(new m(null,2,[Il,f,Ak,zc],null))].join("")],0)),fe=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=
	0;;)if(10>d){var e=h,f=ee;gu.b?gu.b(e,f):gu.call(null,e,f);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),F=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return function Dl(aa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K){return function(){for(;;){var a=q(aa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l){var n=K,t=p;gu.b?gu.b(n,t):gu.call(null,
	n,t);l+=1}else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),Dl(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){var e=K,f=p;gu.b?gu.b(e,f):gu.call(null,e,f);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Dl(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W),null,null)}}(Ja,$a,jb,Bb,b,a,c,d,ff,fe,ee,zc,lc,pc,eb,f,h,Na,va)(ti(5))}()),K=de(se,F),N=Xu(F),W=de(re,F);r(null)?Ki(H(["{ :context",
	X(H([null],0)),"\n  :spec   ",X(H([new m(null,2,[Il,f,Ak,zc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,N,mj,K,Uk,W,Tj,F],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(K),"] [ med:",Yu(N),"] [ max:",Yu(W),"] ms"],0));Ja=E(lc);$a=null;jb=0}Bb=0}else break}b=E(Na);a=null;c=0}d=0}else return null}}fa("datascript.bench.bench_queries",iv);
	function jv(){for(var b=q(new T(null,7,5,U,[new T(null,2,5,U,["wide 3\u00d73",dv(3,3)],null),new T(null,2,5,U,["wide 5\u00d73",dv(5,3)],null),new T(null,2,5,U,["wide 7\u00d73",dv(7,3)],null),new T(null,2,5,U,["wide 4\u00d76",dv(4,6)],null),new T(null,2,5,U,["long 10\u00d73",fv(10,3)],null),new T(null,2,5,U,["long 30\u00d73",fv(30,3)],null),new T(null,2,5,U,["long 30\u00d75",fv(30,5)],null)],null)),a=null,c=0,d=0;;)if(d<c){var e=a.M(null,d),f=L(e,0),h=L(e,1),k=r(null)?null:Ki(H([[u("\n"),u(new m(null,
	2,[Il,"rules",nl,f],null))].join("")],0)),l=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){var e=new T(null,8,5,U,[ej,new C(null,"?e","?e",-1194391683,null),new C(null,"?e2","?e2",-1790911809,null),Zl,new C(null,"$","$",-1580747756,null),new C(null,"%","%",-950237169,null),wj,He(new C(null,"follows","follows",883339250,null),new C(null,"?e","?e",-1194391683,null),new C(null,"?e2","?e2",-1790911809,null))],null),f=h,k=new T(null,2,5,U,[new T(null,2,5,U,[He(new C(null,"follows","follows",
	883339250,null),new C(null,"?x","?x",-555096650,null),new C(null,"?y","?y",-1454470631,null)),new T(null,3,5,U,[new C(null,"?x","?x",-555096650,null),ok,new C(null,"?y","?y",-1454470631,null)],null)],null),new T(null,3,5,U,[He(new C(null,"follows","follows",883339250,null),new C(null,"?x","?x",-555096650,null),new C(null,"?y","?y",-1454470631,null)),new T(null,3,5,U,[new C(null,"?x","?x",-555096650,null),ok,new C(null,"?t","?t",1786819229,null)],null),He(new C(null,"follows","follows",883339250,null),
	new C(null,"?t","?t",1786819229,null),new C(null,"?y","?y",-1454470631,null))],null)],null);gu.g?gu.g(e,f,k):gu.call(null,e,f,k);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),n=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l){return function Ja(n){return new Ne(null,function(a,b,c,d,e,f,h,k,l){return function(){for(;;){var a=q(n);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var n=0;;)if(10>
	n){var p=new T(null,8,5,U,[ej,new C(null,"?e","?e",-1194391683,null),new C(null,"?e2","?e2",-1790911809,null),Zl,new C(null,"$","$",-1580747756,null),new C(null,"%","%",-950237169,null),wj,He(new C(null,"follows","follows",883339250,null),new C(null,"?e","?e",-1194391683,null),new C(null,"?e2","?e2",-1790911809,null))],null),t=l,x=new T(null,2,5,U,[new T(null,2,5,U,[He(new C(null,"follows","follows",883339250,null),new C(null,"?x","?x",-555096650,null),new C(null,"?y","?y",-1454470631,null)),new T(null,
	3,5,U,[new C(null,"?x","?x",-555096650,null),ok,new C(null,"?y","?y",-1454470631,null)],null)],null),new T(null,3,5,U,[He(new C(null,"follows","follows",883339250,null),new C(null,"?x","?x",-555096650,null),new C(null,"?y","?y",-1454470631,null)),new T(null,3,5,U,[new C(null,"?x","?x",-555096650,null),ok,new C(null,"?t","?t",1786819229,null)],null),He(new C(null,"follows","follows",883339250,null),new C(null,"?t","?t",1786819229,null),new C(null,"?y","?y",-1454470631,null))],null)],null);gu.g?gu.g(p,
	t,x):gu.call(null,p,t,x);n+=1}else break;n=Z();if(n<h)k=10+k;else{f=(n-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),Ja(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){var e=new T(null,8,5,U,[ej,new C(null,"?e","?e",-1194391683,null),new C(null,"?e2","?e2",-1790911809,null),Zl,new C(null,"$","$",-1580747756,null),new C(null,"%","%",-950237169,null),wj,He(new C(null,"follows","follows",883339250,null),new C(null,"?e","?e",-1194391683,null),
	new C(null,"?e2","?e2",-1790911809,null))],null),f=l,h=new T(null,2,5,U,[new T(null,2,5,U,[He(new C(null,"follows","follows",883339250,null),new C(null,"?x","?x",-555096650,null),new C(null,"?y","?y",-1454470631,null)),new T(null,3,5,U,[new C(null,"?x","?x",-555096650,null),ok,new C(null,"?y","?y",-1454470631,null)],null)],null),new T(null,3,5,U,[He(new C(null,"follows","follows",883339250,null),new C(null,"?x","?x",-555096650,null),new C(null,"?y","?y",-1454470631,null)),new T(null,3,5,U,[new C(null,
	"?x","?x",-555096650,null),ok,new C(null,"?t","?t",1786819229,null)],null),He(new C(null,"follows","follows",883339250,null),new C(null,"?t","?t",1786819229,null),new C(null,"?y","?y",-1454470631,null))],null)],null);gu.g?gu.g(e,f,h):gu.call(null,e,f,h);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Ja(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l),null,null)}}(b,a,c,d,k,l,e,f,h)(ti(5))}()),p=de(se,n),t=Xu(n),x=de(re,n);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,
	2,[Il,"rules",nl,f],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,t,mj,p,Uk,x,Tj,n],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(p),"] [ med:",Yu(t),"] [ max:",Yu(x),"] ms"],0));d+=1}else{var z=q(b);if(z){var B=z;if(Ed(B))t=y(B),n=A(B),p=t,t=J(t),b=n,a=p,c=t;else{var F=D(B),f=L(F,0),h=L(F,1),K=r(null)?null:Ki(H([[u("\n"),u(new m(null,2,[Il,"rules",nl,f],null))].join("")],0)),N=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){var e=new T(null,
	8,5,U,[ej,new C(null,"?e","?e",-1194391683,null),new C(null,"?e2","?e2",-1790911809,null),Zl,new C(null,"$","$",-1580747756,null),new C(null,"%","%",-950237169,null),wj,He(new C(null,"follows","follows",883339250,null),new C(null,"?e","?e",-1194391683,null),new C(null,"?e2","?e2",-1790911809,null))],null),f=h,k=new T(null,2,5,U,[new T(null,2,5,U,[He(new C(null,"follows","follows",883339250,null),new C(null,"?x","?x",-555096650,null),new C(null,"?y","?y",-1454470631,null)),new T(null,3,5,U,[new C(null,
	"?x","?x",-555096650,null),ok,new C(null,"?y","?y",-1454470631,null)],null)],null),new T(null,3,5,U,[He(new C(null,"follows","follows",883339250,null),new C(null,"?x","?x",-555096650,null),new C(null,"?y","?y",-1454470631,null)),new T(null,3,5,U,[new C(null,"?x","?x",-555096650,null),ok,new C(null,"?t","?t",1786819229,null)],null),He(new C(null,"follows","follows",883339250,null),new C(null,"?t","?t",1786819229,null),new C(null,"?y","?y",-1454470631,null))],null)],null);gu.g?gu.g(e,f,k):gu.call(null,
	e,f,k);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),n=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p){return function jb(t){return new Ne(null,function(a,b,c,d,e,f,h,k,l){return function(){for(;;){var a=q(t);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var n=0;;)if(10>n){var p=new T(null,8,5,U,[ej,new C(null,"?e","?e",-1194391683,null),new C(null,"?e2","?e2",-1790911809,null),Zl,new C(null,
	"$","$",-1580747756,null),new C(null,"%","%",-950237169,null),wj,He(new C(null,"follows","follows",883339250,null),new C(null,"?e","?e",-1194391683,null),new C(null,"?e2","?e2",-1790911809,null))],null),t=l,x=new T(null,2,5,U,[new T(null,2,5,U,[He(new C(null,"follows","follows",883339250,null),new C(null,"?x","?x",-555096650,null),new C(null,"?y","?y",-1454470631,null)),new T(null,3,5,U,[new C(null,"?x","?x",-555096650,null),ok,new C(null,"?y","?y",-1454470631,null)],null)],null),new T(null,3,5,U,
	[He(new C(null,"follows","follows",883339250,null),new C(null,"?x","?x",-555096650,null),new C(null,"?y","?y",-1454470631,null)),new T(null,3,5,U,[new C(null,"?x","?x",-555096650,null),ok,new C(null,"?t","?t",1786819229,null)],null),He(new C(null,"follows","follows",883339250,null),new C(null,"?t","?t",1786819229,null),new C(null,"?y","?y",-1454470631,null))],null)],null);gu.g?gu.g(p,t,x):gu.call(null,p,t,x);n+=1}else break;n=Z();if(n<h)k=10+k;else{f=(n-f)/k;break a}}}e.add(f);a+=1}else return!0}()?
	Te(Xe(d),jb(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){var e=new T(null,8,5,U,[ej,new C(null,"?e","?e",-1194391683,null),new C(null,"?e2","?e2",-1790911809,null),Zl,new C(null,"$","$",-1580747756,null),new C(null,"%","%",-950237169,null),wj,He(new C(null,"follows","follows",883339250,null),new C(null,"?e","?e",-1194391683,null),new C(null,"?e2","?e2",-1790911809,null))],null),f=l,h=new T(null,2,5,U,[new T(null,2,5,U,[He(new C(null,"follows",
	"follows",883339250,null),new C(null,"?x","?x",-555096650,null),new C(null,"?y","?y",-1454470631,null)),new T(null,3,5,U,[new C(null,"?x","?x",-555096650,null),ok,new C(null,"?y","?y",-1454470631,null)],null)],null),new T(null,3,5,U,[He(new C(null,"follows","follows",883339250,null),new C(null,"?x","?x",-555096650,null),new C(null,"?y","?y",-1454470631,null)),new T(null,3,5,U,[new C(null,"?x","?x",-555096650,null),ok,new C(null,"?t","?t",1786819229,null)],null),He(new C(null,"follows","follows",883339250,
	null),new C(null,"?t","?t",1786819229,null),new C(null,"?y","?y",-1454470631,null))],null)],null);gu.g?gu.g(e,f,h):gu.call(null,e,f,h);d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),jb(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p),null,null)}}(b,a,c,d,K,N,F,f,h,B,z)(ti(5))}()),p=de(se,n),t=Xu(n),x=de(re,n);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,2,[Il,"rules",nl,f],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,
	4,[Qk,t,mj,p,Uk,x,Tj,n],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(p),"] [ med:",Yu(t),"] [ max:",Yu(x),"] ms"],0));b=E(B);a=null;c=0}d=0}else return null}}fa("datascript.bench.bench_rules",jv);
	function kv(){for(var b=q(new T(null,3,5,U,[new T(null,2,5,U,["sorted-set",ki()],null),new T(null,2,5,U,["vector",I],null),new T(null,2,5,U,["btset",gn(Vd)],null)],null)),a=null,c=0,d=0;;)if(d<c){for(var e=a.M(null,d),f=L(e,0),h=L(e,1),k=q(new T(null,3,5,U,[100,500,2E4],null)),l=null,n=0,p=0;;)if(p<n){var t=l.M(null,p),x=$d(ti(t)),z=$d(x),B=S.b(h,x),F=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-conj",Ak,t],null))].join("")],0)),K=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>
	d)S.b(h,x),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),N=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K){return function qd(N){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,qo){return function(){for(;;){var a=q(N);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)S.b(qo,p),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=
	1}else return!0}()?Te(Xe(d),qd(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)S.b(qo,p),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),qd(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K),null,null)}}(k,l,n,p,b,a,c,d,F,K,x,z,B,t,e,f,h)(ti(5))}()),W=de(se,N),xa=Xu(N),Ua=de(re,N);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-conj",Ak,t],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,
	Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,xa,mj,W,Uk,Ua,Tj,N],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(W),"] [ med:",Yu(xa),"] [ max:",Yu(Ua),"] ms"],0));if(r(yi(/set/,f))){var gb=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-disj",Ak,t],null))].join("")],0)),aa=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)v(wd,B,z),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),uc=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K){return function qd(N){return new Ne(null,
	function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(N);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)v(wd,x,t),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),qd(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)v(wd,x,t),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-
	a)/c}}(),qd(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K),null,null)}}(k,l,n,p,b,a,c,d,gb,aa,x,z,B,t,e,f,h)(ti(5))}()),Pa=de(se,uc),va=Xu(uc),Na=de(re,uc);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-disj",Ak,t],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,va,mj,Pa,Uk,Na,Tj,uc],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(Pa),"] [ med:",Yu(va),"] [ max:",Yu(Na),"] ms"],0));var eb=r(null)?null:
	Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-lookup",Ak,t],null))].join("")],0)),Ja=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(z),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,k);Pd(B,l);k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),Pd(B,e),e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),$a=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K){return function qd(N){return new Ne(null,function(a,
	b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(N);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l){for(var n=q(t),p=null,z=0,B=0;;)if(B<z){var F=p.M(null,B);Pd(x,F);B+=1}else if(n=q(n))p=n,Ed(p)?(n=y(p),B=A(p),p=n,z=J(n),n=B):(n=D(p),Pd(x,n),n=E(p),p=null,z=0),B=0;else break;l+=1}else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),qd(A(a))):
	Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(t),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,k);Pd(x,l);k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),Pd(x,e),e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),qd(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K),null,null)}}(k,l,n,p,b,a,c,d,eb,Ja,x,z,B,t,e,f,h)(ti(5))}()),jb=de(se,$a),Bb=Xu($a),Gb=de(re,$a);r(null)?Ki(H(["{ :context",
	X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-lookup",Ak,t],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,Bb,mj,jb,Uk,Gb,Tj,$a],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(jb),"] [ med:",Yu(Bb),"] [ max:",Yu(Gb),"] ms"],0))}var Hb=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-iterate",Ak,t],null))].join("")],0)),bc=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(B),f=null,h=0,k=0;;)if(k<h){var l=
	f.M(null,k);1+l;k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),1+e,e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Tb=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K){return function qd(N){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(N);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=
	0;;)if(10>l){for(var n=q(x),p=null,t=0,z=0;;)if(z<t){var B=p.M(null,z);1+B;z+=1}else if(n=q(n))p=n,Ed(p)?(n=y(p),z=A(p),p=n,t=J(n),n=z):(n=D(p),1+n,n=E(p),p=null,t=0),z=0;else break;l+=1}else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),qd(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(x),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,k);1+l;k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),
	e=k):(e=D(f),1+e,e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),qd(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K),null,null)}}(k,l,n,p,b,a,c,d,Hb,bc,x,z,B,t,e,f,h)(ti(5))}()),pc=de(se,Tb),lc=Xu(Tb),zc=de(re,Tb);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-iterate",Ak,t],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,lc,mj,pc,Uk,zc,Tj,Tb],null)],
	0)),"}"],0)):Ki(H(["[ min:",Yu(pc),"] [ med:",Yu(lc),"] [ max:",Yu(zc),"] ms"],0));var ee=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-reduce",Ak,t],null))].join("")],0)),ff=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)v(je,0,q(B)),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),fe=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K){return function qd(N){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=
	q(N);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)v(je,0,q(x)),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),qd(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)v(je,0,q(x)),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),qd(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,
	z,B,F,K),null,null)}}(k,l,n,p,b,a,c,d,ee,ff,x,z,B,t,e,f,h)(ti(5))}()),Od=de(se,fe),jj=Xu(fe),Bg=de(re,fe);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-reduce",Ak,t],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,jj,mj,Od,Uk,Bg,Tj,fe],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(Od),"] [ med:",Yu(jj),"] [ max:",Yu(Bg),"] ms"],0));p+=1}else{var bh=q(k);if(bh){var Ue=bh;if(Ed(Ue))var Pj=y(Ue),ut=A(Ue),vt=Pj,wt=
	J(Pj),k=ut,l=vt,n=wt;else{var Mc=D(Ue),kg=$d(ti(Mc)),Ve=$d(kg),Dd=S.b(h,kg),ch=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-conj",Ak,Mc],null))].join("")],0)),xt=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)S.b(h,kg),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),dh=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return function xb(aa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,qo,N){return function(){for(;;){var a=
	q(aa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)S.b(N,p),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),xb(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)S.b(N,p),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),xb(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,
	K,N,W),null,null)}}(k,l,n,p,b,a,c,d,ch,xt,kg,Ve,Dd,Mc,Ue,bh,e,f,h)(ti(5))}()),Qj=de(se,dh),oq=Xu(dh),lg=de(re,dh);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-conj",Ak,Mc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,oq,mj,Qj,Uk,lg,Tj,dh],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(Qj),"] [ med:",Yu(oq),"] [ max:",Yu(lg),"] ms"],0));if(r(yi(/set/,f))){var Ou=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,
	f,Il,"set-disj",Ak,Mc],null))].join("")],0)),Dl=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)v(wd,Dd,Ve),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),El=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return function xb(aa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(aa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=
	10;;){for(var l=0;;)if(10>l)v(wd,x,t),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),xb(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)v(wd,x,t),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),xb(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W),null,null)}}(k,l,n,p,b,a,c,d,Ou,Dl,kg,Ve,Dd,Mc,Ue,bh,e,f,h)(ti(5))}()),mr=de(se,El),nr=Xu(El),or=de(re,El);r(null)?Ki(H(["{ :context",
	X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-disj",Ak,Mc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,nr,mj,mr,Uk,or,Tj,El],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(mr),"] [ med:",Yu(nr),"] [ max:",Yu(or),"] ms"],0));var Pu=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-lookup",Ak,Mc],null))].join("")],0)),Qu=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(Ve),f=null,h=0,k=0;;)if(k<h){var l=
	f.M(null,k);Pd(Dd,l);k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),Pd(Dd,e),e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Fl=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return function xb(aa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(aa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=
	1E3+f,k=10;;){for(var l=0;;)if(10>l){for(var n=q(t),p=null,z=0,B=0;;)if(B<z){var F=p.M(null,B);Pd(x,F);B+=1}else if(n=q(n))p=n,Ed(p)?(n=y(p),B=A(p),p=n,z=J(n),n=B):(n=D(p),Pd(x,n),n=E(p),p=null,z=0),B=0;else break;l+=1}else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),xb(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(t),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,k);Pd(x,l);k+=1}else if(e=q(e))f=
	e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),Pd(x,e),e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),xb(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W),null,null)}}(k,l,n,p,b,a,c,d,Pu,Qu,kg,Ve,Dd,Mc,Ue,bh,e,f,h)(ti(5))}()),pr=de(se,Fl),qr=Xu(Fl),rr=de(re,Fl);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-lookup",Ak,Mc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",
	X(H([new m(null,4,[Qk,qr,mj,pr,Uk,rr,Tj,Fl],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(pr),"] [ med:",Yu(qr),"] [ max:",Yu(rr),"] ms"],0))}var Ru=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-iterate",Ak,Mc],null))].join("")],0)),Su=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(Dd),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,k);1+l;k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),1+e,e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=
	10+c;else return(d-a)/c}}(),Gl=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return function xb(aa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(aa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l){for(var n=q(x),p=null,t=0,z=0;;)if(z<t){var B=p.M(null,z);1+B;z+=1}else if(n=q(n))p=n,Ed(p)?(n=y(p),z=A(p),p=n,t=J(n),n=z):(n=
	D(p),1+n,n=E(p),p=null,t=0),z=0;else break;l+=1}else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),xb(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(x),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,k);1+l;k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),1+e,e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),xb(Ac(a)))}return null}}}(a,
	b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W),null,null)}}(k,l,n,p,b,a,c,d,Ru,Su,kg,Ve,Dd,Mc,Ue,bh,e,f,h)(ti(5))}()),sr=de(se,Gl),tr=Xu(Gl),ur=de(re,Gl);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-iterate",Ak,Mc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,tr,mj,sr,Uk,ur,Tj,Gl],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(sr),"] [ med:",Yu(tr),"] [ max:",Yu(ur),"] ms"],0));var Tu=r(null)?null:Ki(H([[u("\n"),u(new m(null,
	3,[ql,f,Il,"set-reduce",Ak,Mc],null))].join("")],0)),lv=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)v(je,0,q(Dd)),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Ll=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return function xb(aa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(aa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=
	1E3+f,k=10;;){for(var l=0;;)if(10>l)v(je,0,q(x)),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),xb(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)v(je,0,q(x)),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),xb(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W),null,null)}}(k,l,n,p,b,a,c,d,Tu,lv,kg,Ve,Dd,Mc,Ue,bh,e,f,h)(ti(5))}()),Ir=de(se,Ll),Jr=Xu(Ll),Kr=de(re,Ll);r(null)?
	Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-reduce",Ak,Mc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,Jr,mj,Ir,Uk,Kr,Tj,Ll],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(Ir),"] [ med:",Yu(Jr),"] [ max:",Yu(Kr),"] ms"],0));k=E(Ue);l=null;n=0}p=0}else break}d+=1}else{var ae=q(b);if(ae){var Zc=ae;if(Ed(Zc))var Lr=y(Zc),mv=A(Zc),nv=Lr,ov=J(Lr),b=mv,a=nv,c=ov;else{for(var be=D(Zc),f=L(be,0),h=L(be,1),Jd=q(new T(null,
	3,5,U,[100,500,2E4],null)),Kd=null,Ld=0,$c=0;;)if($c<Ld){var Nc=Kd.M(null,$c),wg=$d(ti(Nc)),Ih=$d(wg),bf=S.b(h,wg),pv=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-conj",Ak,Nc],null))].join("")],0)),qv=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)S.b(h,wg),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Ml=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return function xb(aa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,
	F,K){return function(){for(;;){var a=q(aa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)S.b(K,p),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),xb(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)S.b(K,p),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),xb(Ac(a)))}return null}}}(a,
	b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W),null,null)}}(Jd,Kd,Ld,$c,b,a,c,d,pv,qv,wg,Ih,bf,Nc,be,f,h,Zc,ae)(ti(5))}()),Or=de(se,Ml),Pr=Xu(Ml),Qr=de(re,Ml);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-conj",Ak,Nc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,Pr,mj,Or,Uk,Qr,Tj,Ml],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(Or),"] [ med:",Yu(Pr),"] [ max:",Yu(Qr),"] ms"],0));if(r(yi(/set/,f))){var rv=r(null)?null:
	Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-disj",Ak,Nc],null))].join("")],0)),sv=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)v(wd,bf,Ih),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Nl=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return function xb(aa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(aa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=
	d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)v(wd,x,t),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),xb(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)v(wd,x,t),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),xb(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W),null,null)}}(Jd,Kd,Ld,$c,b,a,c,d,rv,sv,wg,Ih,bf,Nc,be,f,h,Zc,ae)(ti(5))}()),Rr=de(se,Nl),Sr=Xu(Nl),
	Tr=de(re,Nl);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-disj",Ak,Nc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,Sr,mj,Rr,Uk,Tr,Tj,Nl],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(Rr),"] [ med:",Yu(Sr),"] [ max:",Yu(Tr),"] ms"],0));var tv=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-lookup",Ak,Nc],null))].join("")],0)),uv=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){for(var e=
	q(Ih),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,k);Pd(bf,l);k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),Pd(bf,e),e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Ol=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return function xb(aa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(aa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,
	a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l){for(var n=q(t),p=null,z=0,B=0;;)if(B<z){var F=p.M(null,B);Pd(x,F);B+=1}else if(n=q(n))p=n,Ed(p)?(n=y(p),B=A(p),p=n,z=J(n),n=B):(n=D(p),Pd(x,n),n=E(p),p=null,z=0),B=0;else break;l+=1}else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),xb(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(t),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,
	k);Pd(x,l);k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),Pd(x,e),e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),xb(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W),null,null)}}(Jd,Kd,Ld,$c,b,a,c,d,tv,uv,wg,Ih,bf,Nc,be,f,h,Zc,ae)(ti(5))}()),Ur=de(se,Ol),Vr=Xu(Ol),Wr=de(re,Ol);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-lookup",Ak,Nc],null)],0)),"\n  :env    ",X(H([new m(null,
	1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,Vr,mj,Ur,Uk,Wr,Tj,Ol],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(Ur),"] [ med:",Yu(Vr),"] [ max:",Yu(Wr),"] ms"],0))}var vv=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-iterate",Ak,Nc],null))].join("")],0)),wv=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(bf),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,k);1+l;k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),1+e,e=E(f),f=null,h=0),k=0;else break;
	d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Pl=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return function xb(aa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(aa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l){for(var n=q(x),p=null,t=0,z=0;;)if(z<t){var B=p.M(null,z);1+B;z+=1}else if(n=q(n))p=n,Ed(p)?(n=
	y(p),z=A(p),p=n,t=J(n),n=z):(n=D(p),1+n,n=E(p),p=null,t=0),z=0;else break;l+=1}else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),xb(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(x),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,k);1+l;k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),1+e,e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/
	c}}(),xb(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W),null,null)}}(Jd,Kd,Ld,$c,b,a,c,d,vv,wv,wg,Ih,bf,Nc,be,f,h,Zc,ae)(ti(5))}()),Xr=de(se,Pl),Yr=Xu(Pl),Zr=de(re,Pl);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-iterate",Ak,Nc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,Yr,mj,Xr,Uk,Zr,Tj,Pl],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(Xr),"] [ med:",Yu(Yr),"] [ max:",Yu(Zr),"] ms"],0));
	var xv=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-reduce",Ak,Nc],null))].join("")],0)),yv=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)v(je,0,q(bf)),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Ql=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return function xb(aa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(aa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=
	0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)v(je,0,q(x)),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),xb(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)v(je,0,q(x)),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),xb(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W),null,null)}}(Jd,Kd,Ld,$c,b,a,c,d,xv,yv,wg,Ih,bf,Nc,be,f,h,Zc,
	ae)(ti(5))}()),$r=de(se,Ql),as=Xu(Ql),bs=de(re,Ql);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-reduce",Ak,Nc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,as,mj,$r,Uk,bs,Tj,Ql],null)],0)),"}"],0)):Ki(H(["[ min:",Yu($r),"] [ med:",Yu(as),"] [ max:",Yu(bs),"] ms"],0));$c+=1}else{var Jh=q(Jd);if(Jh){var cf=Jh;if(Ed(cf))var cs=y(cf),zv=A(cf),Av=cs,Bv=J(cs),Jd=zv,Kd=Av,Ld=Bv;else{var Oc=D(cf),xg=$d(ti(Oc)),
	Kh=$d(xg),df=S.b(h,xg),Cv=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-conj",Ak,Oc],null))].join("")],0)),Dv=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)S.b(h,xg),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Rl=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,aa,va){return function rd(xa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W){return function(){for(;;){var a=q(xa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);
	return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)S.b(W,p),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),rd(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)S.b(W,p),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),rd(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,aa,va),null,null)}}(Jd,Kd,Ld,$c,b,a,c,d,Cv,
	Dv,xg,Kh,df,Oc,cf,Jh,be,f,h,Zc,ae)(ti(5))}()),ds=de(se,Rl),es=Xu(Rl),fs=de(re,Rl);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-conj",Ak,Oc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,es,mj,ds,Uk,fs,Tj,Rl],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(ds),"] [ med:",Yu(es),"] [ max:",Yu(fs),"] ms"],0));if(r(yi(/set/,f))){var Ev=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-disj",Ak,Oc],null))].join("")],
	0)),Fv=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)v(wd,df,Kh),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Sl=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,aa,va){return function rd(xa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(xa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)v(wd,x,
	t),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),rd(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)v(wd,x,t),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),rd(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,aa,va),null,null)}}(Jd,Kd,Ld,$c,b,a,c,d,Ev,Fv,xg,Kh,df,Oc,cf,Jh,be,f,h,Zc,ae)(ti(5))}()),gs=de(se,Sl),hs=Xu(Sl),is=de(re,Sl);r(null)?Ki(H(["{ :context",X(H([null],
	0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-disj",Ak,Oc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,hs,mj,gs,Uk,is,Tj,Sl],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(gs),"] [ med:",Yu(hs),"] [ max:",Yu(is),"] ms"],0));var Gv=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-lookup",Ak,Oc],null))].join("")],0)),Hv=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(Kh),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,
	k);Pd(df,l);k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),Pd(df,e),e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Tl=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,aa,va){return function rd(xa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(xa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+
	f,k=10;;){for(var l=0;;)if(10>l){for(var n=q(t),p=null,z=0,B=0;;)if(B<z){var F=p.M(null,B);Pd(x,F);B+=1}else if(n=q(n))p=n,Ed(p)?(n=y(p),B=A(p),p=n,z=J(n),n=B):(n=D(p),Pd(x,n),n=E(p),p=null,z=0),B=0;else break;l+=1}else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),rd(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(t),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,k);Pd(x,l);k+=1}else if(e=q(e))f=
	e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),Pd(x,e),e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),rd(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,aa,va),null,null)}}(Jd,Kd,Ld,$c,b,a,c,d,Gv,Hv,xg,Kh,df,Oc,cf,Jh,be,f,h,Zc,ae)(ti(5))}()),js=de(se,Tl),ks=Xu(Tl),ls=de(re,Tl);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-lookup",Ak,Oc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],
	0)),"\n  :results",X(H([new m(null,4,[Qk,ks,mj,js,Uk,ls,Tj,Tl],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(js),"] [ med:",Yu(ks),"] [ max:",Yu(ls),"] ms"],0))}var Iv=r(null)?null:Ki(H([[u("\n"),u(new m(null,3,[ql,f,Il,"set-iterate",Ak,Oc],null))].join("")],0)),Jv=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(df),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,k);1+l;k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),1+e,e=E(f),f=null,h=0),k=0;else break;d+=1}else break;
	d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Ul=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,aa,va){return function rd(xa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(xa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l){for(var n=q(x),p=null,t=0,z=0;;)if(z<t){var B=p.M(null,z);1+B;z+=1}else if(n=q(n))p=n,Ed(p)?(n=y(p),z=A(p),
	p=n,t=J(n),n=z):(n=D(p),1+n,n=E(p),p=null,t=0),z=0;else break;l+=1}else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),rd(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d){for(var e=q(x),f=null,h=0,k=0;;)if(k<h){var l=f.M(null,k);1+l;k+=1}else if(e=q(e))f=e,Ed(f)?(e=y(f),k=A(f),f=e,h=J(e),e=k):(e=D(f),1+e,e=E(f),f=null,h=0),k=0;else break;d+=1}else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),rd(Ac(a)))}return null}}}(a,
	b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,aa,va),null,null)}}(Jd,Kd,Ld,$c,b,a,c,d,Iv,Jv,xg,Kh,df,Oc,cf,Jh,be,f,h,Zc,ae)(ti(5))}()),ms=de(se,Ul),ns=Xu(Ul),os=de(re,Ul);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-iterate",Ak,Oc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,ns,mj,ms,Uk,os,Tj,Ul],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(ms),"] [ med:",Yu(ns),"] [ max:",Yu(os),"] ms"],0));var Kv=r(null)?null:Ki(H([[u("\n"),
	u(new m(null,3,[ql,f,Il,"set-reduce",Ak,Oc],null))].join("")],0)),Lv=function(){for(var a=Z(),b=500+a,c=10;;){for(var d=0;;)if(10>d)v(je,0,q(df)),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),Vl=S.b(I,function(){return function(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,aa,va){return function rd(xa){return new Ne(null,function(a,b,c,d,e,f,h,k,l,n,p,t,x){return function(){for(;;){var a=q(xa);if(a){if(Ed(a)){var b=y(a),c=J(b),d=Re(c);return function(){for(var a=0;;)if(a<c){w.b(b,a);var e=d,
	f;a:{f=Z();for(var h=1E3+f,k=10;;){for(var l=0;;)if(10>l)v(je,0,q(x)),l+=1;else break;l=Z();if(l<h)k=10+k;else{f=(l-f)/k;break a}}}e.add(f);a+=1}else return!0}()?Te(Xe(d),rd(A(a))):Te(Xe(d),null)}D(a);return bd(function(){for(var a=Z(),b=1E3+a,c=10;;){for(var d=0;;)if(10>d)v(je,0,q(x)),d+=1;else break;d=Z();if(d<b)c=10+c;else return(d-a)/c}}(),rd(Ac(a)))}return null}}}(a,b,c,d,e,f,h,k,l,n,p,t,x,z,B,F,K,N,W,aa,va),null,null)}}(Jd,Kd,Ld,$c,b,a,c,d,Kv,Lv,xg,Kh,df,Oc,cf,Jh,be,f,h,Zc,ae)(ti(5))}()),ps=
	de(se,Vl),qs=Xu(Vl),rs=de(re,Vl);r(null)?Ki(H(["{ :context",X(H([null],0)),"\n  :spec   ",X(H([new m(null,3,[ql,f,Il,"set-reduce",Ak,Oc],null)],0)),"\n  :env    ",X(H([new m(null,1,[Fk,Zu()],null)],0)),"\n  :results",X(H([new m(null,4,[Qk,qs,mj,ps,Uk,rs,Tj,Vl],null)],0)),"}"],0)):Ki(H(["[ min:",Yu(ps),"] [ med:",Yu(qs),"] [ max:",Yu(rs),"] ms"],0));Jd=E(cf);Kd=null;Ld=0}$c=0}else break}b=E(Zc);a=null;c=0}d=0}else return null}}fa("datascript.bench.bench_btset",kv);
	fa("datascript.bench.bench_all",function(){gv();hv();iv();jv();return kv()});
	        ;return this.datascript.js;

	    }.call({});

	});


/***/ },
/* 224 */
/***/ function(module, exports) {

	"use strict";

	module.exports = function mergeObj() {
	  var obj = {};

	  for (var _len = arguments.length, args = Array(_len), _key = 0; _key < _len; _key++) {
	    args[_key] = arguments[_key];
	  }

	  args.forEach(function (arg) {
	    Object.keys(arg).forEach(function (k) {
	      obj[k] = arg[k];
	    });
	  });
	  return obj;
	};

/***/ },
/* 225 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var constants = __webpack_require__(226);

	function updateUserLocation(loc) {
	  return {
	    type: constants.UPDATE_USER_LOCATION,
	    location: loc
	  };
	}

	module.exports = { updateUserLocation: updateUserLocation };

/***/ },
/* 226 */
/***/ function(module, exports) {

	"use strict";

	var UPDATE_USER_LOCATION = "UPDATE_USER_LOCATION";

	module.exports = { UPDATE_USER_LOCATION: UPDATE_USER_LOCATION };

/***/ },
/* 227 */
/***/ function(module, exports, __webpack_require__) {

	'use strict';

	var constants = __webpack_require__(226);
	var ds = __webpack_require__(223);

	// Not so much a bunch of reducers anymore, just a single one that
	// operates on the db. You could still easily separate this out in to
	// smaller action handlers though and still combine them into a single
	// reducer.
	function handler(db, action) {
	  switch (action.type) {
	    case constants.UPDATE_USER_LOCATION:
	      return ds.db_with(db, [[":db/add", 1, "location", action.location]]);
	    default:
	      return db;
	  }
	}

	module.exports = handler;

/***/ }
/******/ ]);